---
title: "Dynamically Feasible Planning and Control in Complex Environments: a Scalable Systematic Approach"
title_zh: 复杂环境下的动态可行规划与控制：一种可扩展的系统化方法
authors: "Miguel Castroviejo-Fernandez, Ilya Kolmanovsky"
date: 2026-07-13
pdf: "https://arxiv.org/pdf/2607.12178v1"
tags: ["query:profile-1"]
score: 8.0
evidence: 低在线计算开销的安全参考跟踪
tldr: 本研究针对具有非凸约束（多胞形并集）的线性离散时间系统，提出了一种生成安全集并实现参考调节器的系统方法。该方案通过理论分析保证了系统的实时安全性及参考指令的有限时间收敛性。其核心优势在于极低的在线计算开销，且在处理复杂环境约束时具有良好的可扩展性和计算效率，为动态环境下的可行路径规划与控制提供了系统化支持。
selection_source: fresh_fetch
motivation: 旨在解决线性系统在包含非凸约束的复杂环境中，如何实现高效且具有安全保障的动态可行规划与控制问题。
method: 提出了一种基于多胞形并集表示非凸约束的安全集生成方法，并结合参考调节器方案实现安全参考跟踪。
result: 理论证明了该方案能确保任何时刻的安全性及参考指令的有限时间收敛，且在线计算开销极低。
conclusion: 该方法为复杂环境下的系统控制提供了一种可扩展且计算高效的系统化解决方案，并通过仿真验证了其有效性。
---

## 摘要
本文提出了一种为受非凸约束限制的线性离散时间系统生成安全集的方法，这些约束可以表示为多胞形的并集。随后展示了如何实现参考调节器（reference governor）以执行安全参考跟踪任务。文中对安全集进行了理论分析，并推导了参考调节器方案的性质。其保证包括任何时刻的安全性，以及所应用的参考指令在有限时间内收敛到任何严格容许的参考指令。对于所提出的参考调节器，在线计算开销较低。此外，结果表明，对于复杂约束集的特定实例，可以高效地计算安全集。报告了大量的仿真结果，展示了该方法的适用性以及在线/离线计算时间。

## Abstract
In this article we present a method to generate safe sets for linear discrete-time systems subject to non-convex constraints that can be represented as a union of polytopes. It is then shown how a reference governor can be implemented for safe reference tracking tasks. A theoretical analysis of the safe set is presented and properties of the reference governor scheme are derived. The guarantees include safety at any time as well as finite-time convergence of the applied reference command to any strictly admissible reference command. For the proposed reference governor, online computational overhead is low. Moreover, it is shown that for specific instances of the complex constraint sets, the safe set can be computed efficiently. Extensive simulation results demonstrating the applicability of the method and online/offline computation times are reported.

---

## 论文详细总结（自动生成）

### 论文总结：复杂环境下的动态可行规划与控制

#### 1. 论文的核心问题与整体含义（研究动机和背景）
本文主要解决线性离散时间系统在**非凸约束环境**下的安全参考跟踪（Safe Reference Tracking）问题。
*   **研究动机**：在复杂的实际场景（如无人机城市飞行、卫星近距离交会）中，障碍物构成的约束通常是非凸的。传统的模型预测控制（MPC）或参考调节器（RG）在处理凸约束（如单个多胞形）时非常成熟，但在非凸环境下往往面临计算爆炸或缺乏动态可行性保证的问题。
*   **核心问题**：如何系统地为非凸约束（表示为多胞形的并集）生成安全集，并设计一种在线计算开销低、且能保证系统动态安全和有限时间收敛的控制方案。

#### 2. 论文提出的方法论
论文提出了一种基于“连接多胞形集合”的系统化框架，核心步骤如下：
*   **约束建模**：将非凸约束区域表示为一系列相互连接的多胞形（Connected Collection of Polytopes），要求相邻多胞形至少共享一个面（Gate）。
*   **弱扩展（Weak Extension）概念**：这是本文的关键创新。为了实现从一个多胞形到另一个多胞形的平滑过渡，作者定义了“弱扩展”区域。它是由相邻两个多胞形的“开口”部分组成的凸多胞形，作为两个区域之间的安全桥梁。
*   **安全集生成（离线）**：
    *   为每个独立的多胞形及其对应的“弱扩展”区域分别计算**最大输出容许集（MOAS）**。
    *   利用 MOAS 的性质确保系统在这些集合内运行是动态安全的。
*   **参考调节器设计（在线）**：
    *   **路径规划**：在多胞形邻接图上利用 Dijkstra 算法或基于距离权重的算法生成索引路径。
    *   **中间点生成**：在每个过渡区域（弱扩展）内设置中间参考指令。
    *   **指令切换逻辑**：在线监测当前状态是否进入下一个安全集，通过求解一个极小规模的二次规划（QP）问题来更新实际应用的参考指令。
*   **计算加速**：针对由超矩形（Hyperrectangles）组成的约束，利用 MOAS 的缩放和平移不变性，大幅减少离线计算量。

#### 3. 实验设计
论文通过三个具有代表性的仿真场景验证了方法的有效性：
*   **2D 质量-弹簧-阻尼系统（4D 状态空间）**：在具有严格速度限制和随机多胞形序列的环境中移动，测试系统在高度欠阻尼特性下的约束满足能力。
*   **轨道近距离操作（6D 耦合系统）**：模拟卫星在 Chief 航天器周围的避障机动，环境被分解为 84 个超矩形。
*   **城市环境下的四旋翼无人机（非线性系统）**：通过反馈线性化处理，在包含 35 座随机建筑（分解为 141 个超矩形）的复杂环境中进行 140 次随机起始点的导航实验。
*   **Benchmark（基准对比）**：在离线计算效率方面，将提出的“缩放平移法”与“逐个独立计算 MOAS 法”进行了对比。

#### 4. 资源与算力
*   **硬件平台**：MacBook Pro, Apple M2 Pro 芯片, 16 GB RAM。
*   **软件环境**：MATLAB。
*   **算力开销**：
    *   **在线计算**：四旋翼实验中，求解单步指令的平均时间仅为 **2.5 ms**，最大不超过 3 ms。
    *   **离线计算**：对于包含 50 个障碍物的复杂场景，生成所有安全集的时间约为 12.8 秒（使用加速算法后，比传统方法快 5 个数量级）。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   针对四旋翼无人机进行了 **140 组** 不同初始条件的仿真。
    *   针对离线计算效率，在不同障碍物数量（5, 10, 30, 50）下各进行了 **100 次** 随机场景测试。
*   **充分性评价**：实验覆盖了从简单线性系统到复杂非线性系统（经线性化）的跨度，且包含了随机生成的障碍物场景，证明了算法的鲁棒性和通用性。对比实验清晰地展示了该方法在处理大规模多胞形并集时的扩展性。

#### 6. 论文的主要结论与发现
*   **安全性保证**：理论证明并仿真验证了系统在任何时刻都不会违反非凸约束。
*   **收敛性**：证明了应用的参考指令能在有限时间内收敛到最终的目标设定点。
*   **高效性**：通过“弱扩展”和“MOAS 缩放性质”，该方法将复杂的非凸规划问题转化为一系列极小规模的凸优化问题，极大地降低了在线计算负担。
*   **可扩展性**：实验表明，随着环境复杂度（多胞形数量）增加，在线计算时间保持稳定，离线计算时间增长受控。

#### 7. 优点（亮点）
*   **系统化强**：提供了一套从环境分解、安全集生成到在线控制的完整闭环方案。
*   **动态可行性**：与纯几何路径规划不同，该方法直接考虑了系统的动力学限制（如速度、加速度、执行器饱和）。
*   **计算极其高效**：在线部分仅需解决 3 维（或低维）的 QP 问题，非常适合嵌入式实时系统。
*   **理论完备**：对“弱扩展”的凸性、多胞形性质以及控制器的收敛性给出了严谨的数学证明。

#### 8. 不足与局限
*   **保守性**：系统必须依次进入每个子区域的 MOAS，这可能导致生成的轨迹不如全局最优 MPC 那样“激进”，在某些转角处可能出现减速。
*   **环境分解依赖**：算法性能高度依赖于前端如何将非凸空间分解为多胞形并集，若分解质量差（如产生极小重叠），可能影响可行性。
*   **线性系统限制**：虽然通过反馈线性化可应用于某些非线性系统，但对于无法精确线性化的强非线性系统，该方法的理论保证可能失效。
*   **静态环境假设**：目前主要针对静态障碍物，未讨论动态障碍物（如移动物体）的实时避障。

（完）
