---
title: Exact Solutions to a Class of Constrained Optimal Control Problems via Lossless Convexification for Digital Control
title_zh: 通过面向数字控制的无损凸化求解一类受限最优控制问题的精确解
authors: "Vaibhav Upadhyay, Siddhartha Ganguly, Debasish Chatterjee"
date: 2026-07-13
pdf: "https://arxiv.org/pdf/2607.12109v1"
tags: ["query:q7"]
score: 9.0
evidence: 航空航天应用的受限最优控制
tldr: 本研究针对航空航天领域中具有环形控制约束的非凸连续时间最优控制问题，提出了一种基于无损凸化（Lossless Convexification）的数值求解技术。通过将原问题转化为凸优化问题，并利用分段常数函数对控制空间进行有限参数化，该方法在保证满足连续时间约束的同时，实现了对数字控制系统精确解的高效计算。在三自由度航天器着陆任务中的应用证明了其在实际引导与控制任务中的潜力。
selection_source: fresh_fetch
motivation: 旨在解决航空航天应用中线性系统面临的带非凸控制约束的连续时间最优控制难题。
method: 采用无损凸化技术将非凸约束转化为凸约束，并结合分段常数函数对控制空间进行参数化处理。
result: 建立了一种高效的数值方法，能够确保在紧时间间隔内满足无数个约束条件并获得精确解。
conclusion: 该方法在航天器着陆场景中表现出色，为实时航空航天制导与控制提供了有效的数值方案。
---

## 摘要
本文建立了一种新的数值可行技术，用于解决航空航天应用中常见的线性系统的一类受限、非凸、连续时间最优控制问题（OCP）。该方法采用无损凸化技术，将具有环形控制幅值约束的原始非凸 OCP 转化为凸问题，随后通过分段常值函数对控制空间进行有限参数化，建立了一种高效的数值方法，在保证精确解的同时，确保在紧时间间隔内满足不可数族约束。该方法的有效性在涉及三自由度（DoF）的航天器着陆问题上得到了验证，突显了其在实际航空航天制导与控制任务中的潜力。

## Abstract
This article establishes a new numerically viable technique for solving a class of constrained, nonconvex, continuous-time optimal control problems (OCPs) for linear systems that commonly arise in aerial and aerospace applications. The lossless convexification technique is employed to translate the original nonconvex OCP with annular control magnitude constraints into a convex problem, and then by finitely parametrizing the control space with piecewise constant functions, an efficient numerical approach is established that guarantees exact solutions while ensuring the satisfaction of an uncountable family of constraints over a compact time interval. The effectiveness of the approach is demonstrated on a spacecraft landing problem involving three degrees of freedom (DoF), underscoring its potential for real-world aerospace guidance and control tasks.

---

## 论文详细总结（自动生成）

这篇论文针对航空航天领域中一类具有挑战性的非凸最优控制问题（OCP）提出了全新的数值求解框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：在航天器着陆等应用中，控制输入（如推力）通常具有“环形”约束（即推力既有上限也有下限，不能为零或过小），这导致控制空间是非凸的。
*   **核心挑战**：
    1.  **非凸性**：传统的直接数值方法在处理非凸约束时容易陷入局部最优或不收敛。
    2.  **连续时间约束**：标准方法仅在离散点上强制执行约束，无法保证在离散点之间（采样间隙）约束依然满足，这在安全临界任务中是致命的。
*   **整体含义**：本文旨在为线性系统提供一种既能处理非凸约束，又能保证在整个连续时间范围内精确满足约束的数值可行方案，特别适配数字控制系统的实现。

### 2. 方法论
*   **核心思想**：结合“无损凸化（Lossless Convexification）”与“凸半无限规划（CSIP）”理论。
*   **关键技术细节**：
    1.  **无损凸化**：引入松弛变量 $s(t)$，将非凸的环形约束 $\rho_1 \le \|u(t)\| \le \rho_2$ 转化为凸的二阶锥约束 $\|u(t)\| \le s(t)$ 及其线性边界。论文证明了在特定条件下，这种松弛是“无损”的，即凸问题的最优解即为原问题的最优解。
    2.  **分段常数参数化**：为了适配数字控制，将控制轨迹 $u(t)$ 参数化为分段常数函数。
    3.  **CSIP 转化**：参数化后的问题变成了一个具有不可数无限多约束（因为路径约束必须在整个时间区间 $t \in [0, T]$ 内成立）的凸优化问题。
    4.  **精确解定理**：论文提出了两个核心定理（Theorem 8 & 11），证明通过在有限个精心挑选的时间点（$e_N$ 个点）强制执行约束，即可获得连续时间下的精确最优解。
*   **算法流程（GlbOpt 架构）**：
    *   **内层**：解决一个有限约束的凸优化问题（如二次规划或锥规划）。
    *   **外层**：使用全局优化算法（如模拟退火、差分进化或梯度法）寻找最容易违反约束的时间点，并将其加入约束集，直到满足停止准则。

### 3. 实验设计
*   **实验场景**：三自由度（3-DoF）航天器动力下降着陆问题。
    *   **能量最优着陆**：最小化控制输入的平方和。
    *   **燃料最优着陆**：最小化控制输入的 $L_1$ 范数。
    *   **扰动场景**：加入正弦扰动以测试算法的鲁棒性和可行性检测能力。
*   **对比方法（Benchmark）**：
    *   **DLCvx**：离散时间无损凸化方法。
    *   **QuITO**：基于拟插值的直接转录轨迹优化软件。
    *   **场景法（Scenario-based）**：基于随机采样的鲁棒优化技术。
    *   **不同全局优化算子**：对比了差分进化（Diff. evol.）、模拟退火（Sim. anneal.）和梯度法（gradOL）。

### 4. 资源与算力
*   **硬件环境**：配备 16GB RAM 和第 11 代 Intel Core i7-11800H 处理器（2.30GHz）的 PC，运行 Ubuntu 20.04。
*   **软件环境**：使用 Julia v1.10.0 编写，调用 IPOPT 求解器和 JuMP 建模包。
*   **计算耗时**：
    *   使用梯度法（gradOL）时，计算时间约为 7.84 秒。
    *   使用模拟退火时，约为 48-86 秒。
    *   使用差分进化时，约为 128 秒。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了四组主要数值实验（能量最优、燃料最优、激活非凸约束、带扰动检测），并针对 200 次独立运行的结果进行了统计对比（见 Table 1）。
*   **充分性评价**：实验设计较为充分。不仅验证了算法在不同目标函数下的有效性，还特别设计了“扰动实验”来证明该方法能检测出传统离散化方法无法发现的不适定（Infeasible）情况。对比实验涵盖了当前主流的轨迹优化技术，评估维度包括计算时间、最优值和连续时间安全性。

### 6. 主要结论与发现
*   **精确性**：该方法能够恢复连续时间 OCP 的精确最优值，而传统方法由于离散化误差，其最优值通常偏高或存在约束违反。
*   **安全性保证**：通过 CSIP 框架，算法能够确保在整个时间视界内（而非仅在网格点上）满足安全约束。
*   **可行性检测**：在某些极端情况下，传统方法可能报告“可行”但实际轨迹违反约束，而本文方法能准确识别出不可行性。
*   **计算效率**：虽然引入全局优化增加了负担，但通过梯度法（gradOL）优化后，其速度与传统高精度离散化方法相当。

### 7. 优点
*   **理论严谨**：建立了从非凸连续问题到有限维凸优化问题的严谨数学映射。
*   **安全性高**：解决了轨迹优化中常见的“采样间隙约束违反”问题。
*   **模块化强**：`GlbOpt` 架构允许灵活更换内层凸求解器和外层全局优化算子。

### 8. 不足与局限
*   **计算成本**：相比于简单的直接转录法，全局优化层的存在使得计算时间较长，目前**不适合在线实时模型预测控制（MPC）**，更适合离线轨迹生成。
*   **系统限制**：目前的方法论主要针对**线性系统**，对于强非凸的动力学系统（如考虑复杂气动力的重返大气层问题）的适用性有待进一步研究。
*   **全局优化依赖**：算法的最终性能在一定程度上取决于外层全局优化算法的收敛速度和参数设置。

（完）
