---
title: "From Distances to Trajectories: Real-Time Signed Distance Function Mapping and Distance-Accelerated Motion Planning for UAVs"
title_zh: 从距离到轨迹：面向无人机的实时符号距离函数建图与距离加速运动规划
authors: "Jason Stanley, Zhirui Dai, Qihao Qian, Tzu-Chin Ho, Tianxing Fan, Siddharth Saha, Christopher Barngrover, Ki Myung Brian Lee, Nikolay Atanasov"
date: 2026-07-21
pdf: "https://arxiv.org/pdf/2607.19306v1"
tags: ["query:profile-1"]
score: 8.0
evidence: 实时距离加速运动规划与轨迹优化
tldr: "本研究针对无人机在复杂环境下的自主飞行，提出了一种基于符号距离函数（SDF）的建图与规划协同设计方案。通过开发OREN网络，结合八叉树先验与神经残差，实现了高效且可微的在线SDF重建；同时提出Bubble*搜索算法，利用距离信息生成安全气泡序列，确保了规划的完备性与安全性。该系统在计算受限的无人机上实现了实时运行，显著提升了建图精度与路径规划效率。"
selection_source: fresh_fetch
motivation: 传统无人机导航将建图与规划分离且多依赖二进制占据图，缺乏距离信息，限制了复杂环境下的规划效率与安全性。
method: "提出OREN网络结合八叉树先验与神经残差进行SDF重建，并配合Bubble*算法通过搜索安全气泡序列实现快速路径规划。"
result: "实验显示OREN提升了22%的SDF估计精度，而Bubble*在复杂环境下的规划速度比传统基准方法快3至10倍。"
conclusion: 该集成方案成功在计算受限的无人机平台上实现了实时、高精度且具备形式化安全保障的自主导航系统。
---

## 摘要
在杂乱环境中的自主飞行要求机器人能够实时在机载端构建周围环境的几何地图，并规划出安全且动力学可行的轨迹。传统方法将建图和规划视为独立的阶段，且通常依赖二进制占据信息进行碰撞检测。我们认为这两个阶段应该围绕单一表示形式进行协同设计：即符号距离函数（SDF）。通过编码到最近障碍物的距离，SDF 比单纯的占据信息为规划和轨迹优化提供了更丰富的信息。我们开发了一种八叉树残差网络（OREN），它将显式八叉树先验与隐式神经残差相结合，从点云观测中在线重建 SDF，兼具体素方法的效率以及神经方法的精度和可微性。与此同时，我们开发了 Bubble*，这是一种基于搜索的规划器，它利用距离信息生成最大的无碰撞球体（我们称之为“气泡”），并具有终止性、完备性和故障检测的形式化保证。与基于网格的 A* 搜索相比，在气泡图上进行规划显著减少了碰撞检测次数，并返回一个气泡序列，为轨迹优化形成安全走廊。我们在四旋翼无人机上演示了集成的 OREN-Bubble* 方法，在严格的计算限制下实时导航于未知的室内环境。实验结果表明，与基准方法相比，OREN 将 SDF 估计精度提高了 22%，而 Bubble* 在杂乱环境中仅需 1-3 秒即可找到跨度约 90 米的轨迹，而基准方法在相同环境下最高需要 10 秒。

## Abstract
Autonomous flight in cluttered environments requires a robot to build a geometric map of its surroundings and plan safe, dynamically feasible trajectories, all onboard and in real time. Conventional approaches treat mapping and planning as separate stages and often rely on binary occupancy for collision checking. We argue that these two stages should be co-designed around a single representation: a signed distance function (SDF). By encoding distance to the nearest obstacle, an SDF provides richer information for planning and trajectory optimization than occupancy alone. We develop an Octree REsidual Network (OREN) that pairs an explicit octree prior with an implicit neural residual to reconstruct SDFs online from point cloud observations with the efficiency of volumetric methods and the accuracy and differentiability of neural methods. In tandem, we develop Bubble$^\star$, a search-based planner that exploits the distance information to grow maximal collision-free balls, which we call bubbles, with formal guarantees of termination, completeness, and failure detection. Planning over a graph of bubbles significantly reduces collision checks compared to a grid-based A$^\star$ search and returns a bubble sequence that forms a safe corridor for trajectory optimization. We demonstrate the integrated OREN-Bubble$^\star$ approach onboard a quadrotor, navigating unseen indoor environments in real time under tight compute constraints. OREN improves SDF estimation by $22$% compared to baselines, while Bubble$^\star$ finds trajectories spanning $\approx 90$ m through a cluttered environment in $1$-$3$ sec., whereas baselines take up to $10$ sec. in the same environment.

---

## 论文详细总结（自动生成）

以下是对论文《From Distances to Trajectories: Real-Time Signed Distance Function Mapping and Distance-Accelerated Motion Planning for UAVs》的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：无人机（UAV）在复杂、未知的杂乱环境中飞行，需要实时构建地图并规划安全轨迹。
*   **核心痛点**：
    1.  **表示形式脱节**：传统方法通常将建图（占据图）与规划（路径搜索）分离，且二进制占据图缺乏距离信息，不利于轨迹优化。
    2.  **计算效率瓶颈**：在复杂空间中，传统的 A* 或 JPS 算法因频繁的碰撞检测导致计算开销巨大。
*   **整体含义**：本文提出了一种以**符号距离函数（SDF）**为核心的协同设计方案。通过 OREN 网络实现高精度实时建图，并利用 Bubble* 算法实现距离加速的运动规划，实现了从原始传感器数据到安全动力学轨迹的高效闭环。

### 2. 方法论
该系统由两个核心组件组成：

#### **A. OREN (Octree Residual Network) 建图**
*   **核心思想**：结合显式几何先验与隐式神经表示。
*   **技术细节**：
    *   **八叉树先验**：利用八叉树（Octree）快速处理点云，生成粗略的 SDF 估计。
    *   **神经残差**：在八叉树节点上挂载轻量级多层感知机（MLP），学习局部几何的细微偏差（残差）。
    *   **优势**：既保留了体素方法（如 Voxblox）的更新效率，又获得了神经隐式表示的连续性、高精度和可微性。

#### **B. Bubble* 规划器**
*   **核心思想**：利用 SDF 提供的距离信息生成“安全气泡”（Safe Bubbles），在气泡图上进行搜索。
*   **算法流程**：
    1.  **气泡生成**：在当前位置根据 SDF 值 $d(\mathbf{x})$ 生成一个半径为 $d$ 的最大无碰撞球体。
    2.  **图搜索**：在气泡边缘采样新节点，通过连接一系列重叠的气泡形成“安全走廊”。
    3.  **形式化保证**：证明了该算法在有限空间内具有**终止性**、**完备性**（若存在路径必能找到）和**故障检测能力**。
*   **轨迹优化**：将气泡序列转化为凸多面体约束，通过二次规划（QP）生成平滑且动力学可行的多项式轨迹。

### 3. 实验设计
*   **实验场景**：包括仿真环境（杂乱的森林、办公室）和真实的室内物理环境（实验室、走廊）。
*   **基准方法 (Benchmarks)**：
    *   **建图对比**：Voxblox（经典体素方法）、FIESTA（快速 SDF 更新）、VDBFusion。
    *   **规划对比**：标准 A* 算法、JPS (Jump Point Search)。
*   **评估指标**：SDF 重建误差（RMSE）、规划耗时、碰撞检测次数、路径长度及成功率。

### 4. 资源与算力
*   **硬件平台**：实验在搭载 **NVIDIA Jetson Orin NX** 的四旋翼无人机上完成。
*   **实时性表现**：
    *   OREN 建图在机载端以实时频率运行。
    *   Bubble* 在复杂环境中（跨度约 90 米）的规划耗时仅为 **1-3 秒**。
*   **训练细节**：文中未详细给出 MLP 的具体训练时长，但强调了其轻量化设计是为了适配嵌入式设备的实时推理。

### 5. 实验数量与充分性
*   **实验规模**：进行了多组仿真对比实验和实际飞行测试。
*   **充分性评价**：
    *   **消融实验**：验证了引入神经残差后，SDF 精度相比纯八叉树方法有显著提升。
    *   **对比广泛**：涵盖了当前主流的建图与规划算法。
    *   **客观性**：通过在不同复杂度的环境（从简单到极度拥挤）下测试，证明了 Bubble* 在复杂场景下的优势更明显。实验设计较为全面且具有说服力。

### 6. 主要结论与发现
*   **精度提升**：OREN 相比于传统基准方法，将 SDF 估计精度提高了约 **22%**。
*   **效率飞跃**：在杂乱环境中，Bubble* 的规划速度比 A* 或 JPS 快 **3 到 10 倍**，主要得益于碰撞检测次数的大幅减少（从数万次降至数百次）。
*   **系统集成**：成功证明了基于 SDF 的统一表示可以显著提升无人机在资源受限情况下的自主导航性能。

### 7. 优点
*   **协同设计**：将建图的输出直接转化为规划的加速器，这种端到端的表示一致性非常高效。
*   **理论与工程结合**：Bubble* 不仅速度快，还提供了机器人领域急需的完备性和安全性形式化证明。
*   **落地性强**：在计算能力有限的 Jetson 平台上实现了闭环运行，具有很高的实用价值。

### 8. 不足与局限
*   **动态障碍物**：目前的研究主要集中在静态环境，对于高速运动物体的实时避障处理尚待验证。
*   **传感器噪声敏感度**：虽然 OREN 有一定的平滑作用，但在极端传感器噪声下，SDF 的梯度准确性可能会受到影响，进而影响轨迹优化。
*   **环境泛化性**：神经残差网络可能需要针对特定类型的环境（如室内 vs 室外）进行预训练或微调，以保持最佳精度。

（完）
