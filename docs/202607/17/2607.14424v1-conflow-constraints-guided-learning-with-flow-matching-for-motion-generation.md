---
title: "ConFlow: Constraints-Guided Learning with Flow Matching for Motion Generation"
title_zh: "ConFlow: 约束引导的流匹配运动生成学习"
authors: "Nutan Chen, Jianxiang Feng, Marvin Alles, Botond Cseke"
date: 2026-07-15
pdf: "https://arxiv.org/pdf/2607.14424v1"
tags: ["query:q4"]
score: 8.0
evidence: 约束引导的流匹配机器人运动生成
tldr: ConFlow是一个针对机器人运动生成的约束引导流匹配框架。针对现有方法在训练与推理阶段约束不匹配的问题，该研究通过可微分势垒函数将约束直接融入训练目标，并利用条件高斯过程优化轨迹平滑度。此外，引入不可行示例作为负监督，显著提升了避障成功率和轨迹质量，有效缩小了生成模型在复杂任务中的性能差距。
selection_source: fresh_fetch
motivation: 现有的流匹配运动生成方法通常在推理阶段才引入约束，导致训练与推理之间存在不匹配，难以处理数据中未包含的特定任务约束。
method: 提出ConFlow框架，通过可微分代价函数将约束融入训练，并采用条件高斯过程作为源分布以满足边界条件，同时利用负样本进行监督学习。
result: 在双机器人导航任务中，ConFlow相比标准流匹配基线显著降低了碰撞率，并提升了轨迹的整体质量。
conclusion: 将约束集成到训练阶段是解决生成式运动模型训练与推理差距、提高约束满足率的有效途径。
---

## 摘要
近年来，流匹配（Flow Matching）已成为机器人运动生成领域生成式建模的一种重要方法。在其通用形式中，流匹配是一种基于常微分方程（ODE）的神经采样器，通过回归与作为数据的运动样本相关的经验流场进行训练。然而，在机器人运动生成中，我们经常面临收集的数据中可能不存在的额外约束。目前大多数方法在可用数据上训练流，并利用推理时引导（inference-time guidance）来强制执行特定任务的约束。为了解决这种不匹配，我们提出了 ConFlow，这是一个约束引导的流匹配框架，通过可微障碍函数或代价函数将约束信息直接整合到训练目标中。为了满足平滑度和边界条件等设计规范，我们建议将流匹配训练中使用的标准高斯源分布替换为条件高斯过程。我们的方法还利用不可行的演示作为负监督，在不需要额外专家数据的情况下提高了约束满足率。在双机器人导航任务上的实验表明，无论是否使用推理时引导，ConFlow 都能比标准流匹配基准实现更低的碰撞率和更高的轨迹质量。这些结果验证了训练时约束集成是缩小生成式运动模型中训练与推理差距的一种有效方法。

## Abstract
In recent years Flow Matching has become a prominent method for generative modeling robot motion generation. In its generic form Flow Matching is an ODE-based neural sampler that is trained by regressing empirical flow fields associated with motion samples as data. However, in robot motion generation we often have additional constraints that might not be present in the collected data. The majority of current approaches train the flow on the available data and use inference-time guidance to enforce task-specific constraints. To address this mismatch, we propose \textbf{ConFlow}, a constraint-guided flow matching framework that incorporates constraint information directly into the training objective via differentiable barrier or cost functions. To address design specifications such as smoothness and boundary conditions, we propose replacing the standard Gaussian source distribution used in flow matching training with a conditional Gaussian Process. Our approach also uses infeasible demonstrations as negative supervision, improving constraint satisfaction without requiring additional expert data. Experiments on a two-robot navigation task demonstrate that ConFlow achieves lower collision rates and higher trajectory quality than standard flow matching baselines, with or without inference-time guidance. These results validate training-time constraint integration as an effective approach to closing the training--inference gap in generative motion models.

---

## 论文详细总结（自动生成）

这篇论文提出了 **ConFlow**，一个旨在解决生成式机器人运动模型中“训练与推理不匹配”问题的约束引导流匹配框架。以下是对该论文的深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的生成式运动模型（如扩散模型、流匹配）通常在专家演示数据上进行标准训练，而避障、平滑度等特定任务约束往往仅在**推理阶段**通过引导（Guidance）来强制执行。
*   **研究背景**：这种“训练-推理脱节”会导致模型生成物理上不可行或超出分布（OOD）的轨迹。此外，获取大规模专家数据成本高昂，而大量包含失败尝试（负样本）的非专家数据往往被浪费。
*   **整体含义**：论文旨在通过将约束直接整合到训练目标中，并优化起始分布，使模型在训练阶段就“学会”遵守约束，从而提高生成轨迹的安全性和质量。

### 2. 方法论
ConFlow 的核心思想结合了约束引导训练、结构化源分布和负监督学习：
*   **约束引导的训练目标**：在标准流匹配损失函数中引入一个修正项。利用可微的 **SoftPlus 势垒函数**计算碰撞代价，并将其梯度作为修正速度场 $u_{ct}$。
*   **负监督机制**：引入违反约束的指示函数 $M(x_1)$。对于数据集中发生碰撞的“负演示”，修正项会被激活，引导流场远离碰撞区域；对于专家数据，则保持标准训练。
*   **条件高斯过程（CGP）源分布**：
    *   **平滑性**：用高斯过程（GP）取代标准高斯噪声作为流匹配的起始分布 $p_0$，利用其时间相关性预设轨迹平滑度。
    *   **边界约束**：通过对 GP 进行条件化处理，使起始轨迹 $x_0$ 的起点和终点与目标轨迹 $x_1$ 完全一致，简化了流场学习边界条件的负担。
*   **算法流程**：采样起始轨迹（来自 CGP）和目标轨迹 $\rightarrow$ 计算中间状态和目标速度 $\rightarrow$ 若目标轨迹违规，则叠加约束梯度 $\rightarrow$ 训练神经网络回归该引导后的速度场。

### 3. 实验设计
*   **实验场景**：双机器人位置交换任务（2D 空间）。两个机器人需要穿过对方路径到达目标点，过程中需避免相互碰撞及环境障碍物。
*   **数据集**：包含 10,000 条轨迹，其中约 30% 为包含碰撞的负样本。
*   **对比基准（Baselines）**：
    1.  **标准流匹配（FM）**：仅在专家数据上训练。
    2.  **混合数据 FM**：在正负样本上进行朴素训练。
    3.  **ConFlow 消融版**：不使用负样本的 ConFlow。
*   **评估指标**：机器人碰撞率、障碍物碰撞率（测试泛化性）、轨迹平滑度（中位数及 95 分位数）、端点误差。
*   **推理设置**：分别在“无引导”、“机器人避障引导”和“障碍物避障引导”三种模式下测试。

### 4. 资源与算力
*   **网络架构**：使用残差多层感知机（ResMLP），隐藏层维度 1024，包含 4 个残差块。
*   **训练细节**：使用 AdamW 优化器，学习率 $3 \times 10^{-4}$，Batch Size 为 2048，训练迭代次数为 20,001 次。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长，但从参数量和迭代次数来看，该实验在单张消费级 GPU（如 RTX 3090/4090）上即可完成。

### 5. 实验数量与充分性
*   **实验规模**：每种模型生成 256 条轨迹进行评估。
*   **充分性**：实验涵盖了核心方法的消融研究（验证了负数据和 CGP 的各自贡献），并测试了模型对训练中未见的“环境障碍物”的泛化能力。
*   **客观性**：对比了不同推理引导强度下的表现，证明了 ConFlow 在无推理引导时依然具有极强的安全性，实验设计较为公平。但实验场景仅限于 2D 合成任务，缺乏高维空间（如 7 自由度机械臂）的验证。

### 6. 主要结论与发现
*   **内化约束**：ConFlow 在**无推理引导**下的碰撞率（0.016）甚至低于标准 FM 在**有引导**下的表现，说明模型已将避障行为内化到权重中。
*   **负样本的价值**：朴素 FM 加入负样本会导致性能下降（信号模糊），但 ConFlow 通过引导目标函数，成功将负样本转化为有益的监督信号。
*   **CGP 的优越性**：相比高斯噪声，CGP 将端点误差降低了两个数量级，并显著提升了轨迹平滑度，消除了生成轨迹中的高频振荡。

### 7. 优点
*   **解决脱节问题**：通过训练时引导，有效缩小了生成模型在部署时的性能差距。
*   **数据效率**：提出了一种利用“失败数据”的有效方式，降低了对纯专家数据的依赖。
*   **数学严谨性**：利用条件高斯过程优雅地解决了流匹配在处理固定边界条件时的结构化先验问题。

### 8. 不足与局限
*   **场景复杂度有限**：实验仅在 2D 导航任务中进行，尚未在具有复杂动力学约束或高维关节空间的真实机器人平台上验证。
*   **约束类型单一**：主要针对距离相关的碰撞约束，对于更复杂的逻辑约束（如接触力约束、时序逻辑约束）的适用性尚待探讨。
*   **超参数敏感性**：约束权重 $\lambda_c$ 和 GP 核函数的长度缩放等参数可能需要针对不同任务进行精细调优。

（完）
