---
title: Safe Execution of RL Policies Via Acceleration-Based CBF-QP Constraint Enforcement for Real-World Robotic Deployments
title_zh: 通过基于加速度的 CBF-QP 约束强制执行实现真实世界机器人部署中的强化学习策略安全执行
authors: "Bastien Muraccioli, Alice Cariou, Pierre-Alexandre Leziart, Mathieu Celerier, Arnaud Demont, Gentiane Venture, Mehdi Benallegue"
date: 2026-07-16
pdf: "https://arxiv.org/pdf/2607.14488v1"
tags: ["query:profile-1"]
score: 9.0
evidence: 通过QP为机械臂实时执行碰撞约束
tldr: 针对强化学习（RL）在机器人部署中缺乏安全保障的问题，本文提出Acc-CBF-QP安全滤波器。该方法利用基于加速度的控制屏障函数（CBF）和二次规划（QP），在不修改训练过程的情况下，实时强制执行关节位置、速度、力矩及碰撞约束。通过在机械臂和人形机器人上的实验证明，该方法能显著减少违规行为并提升系统鲁棒性，且已开源。
selection_source: fresh_fetch
motivation: 强化学习策略在处理机器人安全边界和分布外状态时缺乏理论保证，容易导致硬件损坏或任务失败。
method: 提出一种基于加速度的CBF-QP安全滤波器，通过最小化力矩或加速度偏差，在运行时将RL指令约束在安全集合内。
result: "在Unitree H1人形机器人硬件上将违规率降低了92%，在Kinova机械臂上完全消除了违规，同时保持了标称任务性能。"
conclusion: 该方法为RL策略在复杂机器人系统上的安全部署提供了一种高效、通用的实时约束执行框架。
---

## 摘要
强化学习 (RL) 在解决复杂机器人控制问题方面展现了卓越的能力，但其缺乏安全保证严重限制了在硬件上的部署。特别是由于足式机器人和机械臂通常在安全临界边界附近运行，分布外 (out-of-distribution) 状态可能导致部署失败。为解决这一问题，我们引入了 Acc-CBF-QP，这是一种基于加速度的二次规划 (QP) 安全过滤器，利用控制屏障函数 (CBF) 在运行时将任何 RL 策略约束在安全集合内，且无需修改训练过程。该方法适用于无约束和安全强化学习 (Safe-RL) 策略，并在统一的优化框架内强制执行关节位置、速度、扭矩和碰撞约束。一项关键贡献是制定了 RL+QP 任务，用于在约束即将被违反时调节与 RL 指令的偏差。我们引入了 TorqueTask（最小化扭矩偏差）和 Forward Dynamics Task（最小化诱导加速度偏差），从而为安全与性能之间的权衡提供了原则性的控制。在 7 自由度 Kinova Gen3 机械臂和 19 自由度宇树 (Unitree) H1 人形机器人上的仿真和硬件实验表明，约束违反情况显著减少。在真实的 H1 硬件上，单独使用 Safe-RL 策略会产生 10.04 次/秒的违反，而在加入 Acc-CBF-QP 后，这一数字减少了 92%，降至 0.80 次/秒。在 Kinova Gen3 上，Acc-CBF-QP 完全消除了违反情况。在无违反的情况下，RL 目标的常规任务性能得以保留。在 H1 的激进速度指令下，Acc-CBF-QP 通过防止约束诱发的停机改进了执行效果，从而延长了存活时间。完整的流水线已开源。

## Abstract
Reinforcement Learning (RL) has demonstrated remarkable capabilities for solving complex robotic control problems, but its lack of safety guarantees severely limits deployment on hardware. In particular, as legged robots and manipulators often operate near safety-critical boundaries, out-of-distribution states can lead to failure upon deployment. To address this, we introduce Acc-CBF-QP, an acceleration-based Quadratic Program (QP) safety filter using Control Barrier Functions (CBFs) that constrains any RL policy onto a safe set at runtime without modifying training. The method applies to unconstrained and Safe-RL policies, and enforces joint position, velocity, torque, and collision constraints within a unified optimization framework. A key contribution is the formulation of RL+QP tasks that regulate deviation from the RL command when constraints would otherwise be violated. We introduce a TorqueTask, minimizing torque deviation, and a Forward Dynamics Task, minimizing induced acceleration deviation, thus providing principled control over safety-performance trade-offs. Experiments on a 7-DoF Kinova Gen3 manipulator and a 19-DoF Unitree H1 humanoid, both in simulation and on hardware, highlight substantial reductions in constraint violations. On the real H1 hardware, a Safe-RL policy alone yielded 10.04 violations/s, which were reduced by 92% to 0.80 violations/s when augmented with Acc-CBF-QP. On the Kinova Gen3, Acc-CBF-QP fully eliminated violations. Nominal task performance of the RL objective is preserved in violation-free regimes. Under aggressive velocity commands on H1, Acc-CBF-QP improves execution by preventing constraint-induced shutdowns, yielding longer survival times. The full pipeline is open-source.

---

## 论文详细总结（自动生成）

以下是对论文《Safe Execution of RL Policies Via Acceleration-Based CBF-QP Constraint Enforcement for Real-World Robotic Deployments》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
强化学习（RL）在处理复杂机器人控制任务（如灵巧操作和敏捷行走）中表现出色，但其**缺乏严格的安全保证**，限制了在真实硬件上的部署。
*   **痛点**：机器人常在安全边界运行，RL 策略在遇到分布外（OoD）状态或模型偏差时，容易违反物理约束（如关节限位、力矩限制、碰撞），导致硬件损坏或系统停机。
*   **现状**：现有的 Safe-RL 方法多在训练阶段嵌入约束，但这通常过于保守，且在部署时面对未建模动态时仍会失效。
*   **目标**：开发一种无需重新训练、可实时运行的安全滤波器，将 RL 策略的输出映射到安全的可行域内。

### 2. 方法论：Acc-CBF-QP 安全滤波器
论文提出了一种基于加速度的二次规划（QP）框架，结合控制屏障函数（CBF）来强制执行约束。
*   **核心思想**：将 RL 策略生成的指令视为“名义参考”，通过一个运行时的 QP 优化问题进行过滤。优化变量是广义加速度 $\ddot{q}$，目标是最小化与 RL 指令的偏差，同时满足所有安全约束。
*   **关键技术细节**：
    *   **CBF 约束强制执行**：利用一阶（速度限制）和二阶（位置限制、碰撞避免）指数控制屏障函数（ECBF），将安全要求转化为关于加速度 $\ddot{q}$ 的线性不等式约束。
    *   **两种任务表述**：
        1.  **TorqueTask**：直接最小化 QP 输出力矩与 RL 期望力矩之间的偏差。
        2.  **FDTask（前向动力学任务）**：将 RL 力矩转换为参考加速度，最小化加速度空间的偏差。
    *   **扰动补偿**：集成广义动量观测器（GMO）和倾斜观测器（Tilt Observer），实时估计外部负载和建模误差，以增强在浮动基座系统（如人形机器人）上的鲁棒性。
*   **算法流程**：RL 策略输出指令 $\rightarrow$ 计算 RL 诱导力矩 $\rightarrow$ 结合当前状态和观测器估计进入 QP 求解器 $\rightarrow$ 输出安全的加速度/力矩 $\rightarrow$ 作用于机器人。

### 3. 实验设计
研究在两个截然不同的平台上进行了验证：
*   **场景与平台**：
    1.  **Kinova Gen3（7自由度机械臂）**：执行末端到达任务。对比了标称性能和分布外（OoD）初始位姿下的安全性。
    2.  **Unitree H1（19自由度人形机器人）**：执行行走任务。测试了平地激进速度指令和不平整地形下的表现。
*   **Benchmark（基准方法）**：
    *   **RL-only**：原始 RL 策略。
    *   **Safe-RL (CaT)**：使用“约束即终止”框架训练的安全强化学习策略。
    *   **RL-Sat**：带简单力矩饱和限制的 RL 策略。
*   **对比维度**：约束违反率（次/秒）、存活时间（Survival Duration）、任务轨迹偏差、计算耗时。

### 4. 资源与算力
*   **推理算力**：论文提到在 Unitree H1 的车载电脑（**Intel Core Ultra 7 155H**）上，整个流水线的单次循环耗时约为 **0.35ms**，远低于 400Hz 控制周期（2.5ms）的要求。
*   **训练算力**：文中**未明确说明**训练 RL 策略所使用的 GPU 型号、数量或具体训练时长。

### 5. 实验数量与充分性
*   **实验规模**：
    *   机械臂实验涵盖了 10 个循环的动态/准静态分析。
    *   人形机器人实验进行了 **200 组配对仿真试验**，涵盖了随机初始条件和地形。
    *   包含真实的硬件部署实验（H1 人形机器人和 Kinova 机械臂）。
*   **充分性评价**：实验设计较为充分。通过仿真大规模测试了统计学意义上的“存活率”，通过硬件实验验证了理论在现实噪声和模型偏差下的有效性。对比了两种不同的任务表述（Torque vs FD），并讨论了 QP 不可行时的回退策略，体现了客观性。

### 6. 主要结论与发现
*   **显著提升安全性**：在 H1 硬件上，Acc-CBF-QP 将 Safe-RL 策略的违规率从 10.04 次/秒降低到 0.80 次/秒（**降低 92%**）；在机械臂上完全消除了违规。
*   **保留性能**：在无约束违反的区域，RL+QP 的轨迹与原始 RL 几乎重合，证明了该滤波器不会干扰正常任务执行。
*   **增强鲁棒性**：在激进指令下，该方法通过将非法指令转化为物理可行的动作，使机器人的存活时间提升了约 20%。
*   **任务表述差异**：TorqueTask 对模型误差更鲁棒，而 FDTask 在模型精确时能更好地保持加速度一致性。

### 7. 优点与亮点
*   **即插即用**：不需要修改 RL 训练过程，可直接应用于预训练模型。
*   **统一框架**：在一个 QP 中同时处理位置、速度、力矩和自碰撞约束。
*   **开源贡献**：提供了完整的开源流水线，对社区具有实用价值。
*   **现实可行性**：通过扰动观测器解决了机器人领域常见的“模型不准”问题，使其在真实人形机器人上表现稳健。

### 8. 不足与局限
*   **QP 不可行性（Infeasibility）**：在极端冲突的约束下，QP 可能无解。虽然文中提出了“回退到阻尼模式”等策略，但此时无法提供严格的安全保证。
*   **依赖模型精度**：尽管有观测器，但 CBF 的有效性仍高度依赖于动力学模型的质量（如 H1 实验中提到的 8kg 质量偏差）。
*   **对比范围有限**：主要对比了 CaT 框架，未广泛对比其他最新的运行时安全滤波器（如基于预测的滤波器）。
*   **稳定性证明**：论文证明了输入到状态的安全性（ISSf），但未给出闭环系统的全局稳定性证明。

（完）
