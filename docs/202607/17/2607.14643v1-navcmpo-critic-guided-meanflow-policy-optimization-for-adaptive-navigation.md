---
title: "NavCMPO: Critic-Guided MeanFlow Policy Optimization for Adaptive Navigation"
title_zh: NavCMPO：面向自适应导航的评论家引导的 MeanFlow 策略优化
authors: "Junjie An, Yi Wu, Xiao Liu, Yiqun Zhou, Yuechen Wu, Xiaoqing Guan, You Wang, Guang Li"
date: 2026-07-16
pdf: "https://arxiv.org/pdf/2607.14643v1"
tags: ["query:q3"]
score: 8.0
evidence: 用于避障的评论家引导轨迹细化
tldr: 针对无地图视觉导航中扩散策略推理延迟高和受限于专家数据的问题，本文提出NavCMPO框架。该框架结合了少步MeanFlow轨迹生成与评论家引导的轨迹细化（CGTR），并通过强化学习进行微调。在降低推理延迟的同时，显著提升了避障性能和成功率，并在四足机器人上验证了实机迁移能力。
selection_source: fresh_fetch
motivation: 旨在解决扩散模型在导航任务中推理速度慢以及行为克隆性能受限于专家演示质量的局限性。
method: 采用MeanFlow轨迹生成，结合基于障碍物点云监督的评论家引导细化（CGTR）及带行为克隆正则化的PPO微调。
result: "在InternVLA-N1基准上成功率提升至74.7%，且推理延迟从85ms显著降低至60ms。"
conclusion: NavCMPO通过结合生成式模型与强化学习优化，实现了兼顾高效推理与强避障能力的自适应视觉导航系统。
---

## 摘要
端到端扩散策略在无地图视觉导航中表现出强大的性能，但其迭代去噪过程引入了显著的推理延迟，而行为克隆则将性能限制在专家演示的质量水平。我们提出了 NavCMPO，这是一个两阶段自适应导航框架，结合了少步 MeanFlow 轨迹生成、评论家引导的细化以及强化学习微调。在预训练期间，障碍物邻近度预测任务促使视觉表示捕获具有障碍物感知能力的空间信息。为了补偿少步生成导致的避障性能下降，评论家引导的轨迹细化（CGTR）利用经过障碍物点云监督训练的评论家所提供的梯度来细化中间轨迹。在自适应阶段，使用带有行为克隆正则化的近端策略优化（PPO）对 MeanFlow 策略进行微调，同时更新评论家以适应特定实体的观测变化。在 InternVLA-N1 基准测试中，在相同的训练预算下，NavCMPO 实现了 74.7% 的平均成功率，比重新训练的 NavDP 基准高出 6.4 个百分点，同时将推理延迟从 85 毫秒降低至 60 毫秒。在 Unitree Go2 上的实验进一步证明了有效的从仿真到现实（sim-to-real）的迁移。

## Abstract
End-to-end diffusion-based policies have demonstrated strong performance in mapless visual navigation, but their iterative denoising process introduces substantial inference latency, while behavior cloning limits performance to the quality of expert demonstrations. We present NavCMPO, a two-stage adaptive navigation framework that combines few-step MeanFlow trajectory generation, critic-guided refinement, and reinforcement learning fine-tuning. During pre-training, an obstacle proximity prediction task encourages the visual representation to capture obstacle-aware spatial information. To compensate for the degradation in obstacle avoidance caused by few-step generation, Critic-Guided Trajectory Refinement (CGTR) uses gradients from a critic trained with obstacle-point-cloud supervision to refine intermediate trajectories. During adaptation, the MeanFlow policy is fine-tuned using Proximal Policy Optimization with behavior-cloning regularization, while the critic is updated to accommodate embodiment-specific observation changes. Under a matched training budget on the InternVLA-N1 benchmark, NavCMPO achieves an average success rate of 74.7\%, exceeding the retrained NavDP baseline by 6.4 percentage points, while reducing inference latency from 85\,ms to 60\,ms. Experiments on a Unitree Go2 further demonstrate effective sim-to-real transfer.

---

## 论文详细总结（自动生成）

### NavCMPO: 评论家引导的 MeanFlow 策略优化自适应导航论文总结

#### 1. 核心问题与整体含义
论文针对自主视觉导航中的两个关键挑战：
*   **推理延迟高**：目前最先进的扩散策略（Diffusion Policy）依赖多步迭代去噪，导致推理延迟大，难以在资源受限的移动机器人上实时运行。
*   **性能受限**：传统的行为克隆（BC）方法使模型性能受限于专家演示数据的质量，且在面对部署环境的分布偏移（如机器人硬件变化）时表现脆弱。
**研究动机**：提出一种既能保持低延迟生成，又能通过强化学习（RL）实现性能突破和环境自适应的端到端导航框架。

#### 2. 方法论
NavCMPO 采用两阶段框架（预训练与在线微调），核心思想是结合流匹配（Flow Matching）的高效生成能力与评论家（Critic）的引导修正。
*   **MeanFlow 策略生成**：将复杂的扩散去噪过程简化为 MeanFlow 轨迹生成，通过 5 步集成（Integration）显著降低推理时间。
*   **障碍物邻近度预测 (OPP)**：在预训练阶段引入辅助任务，要求视觉编码器预测周围 12 个扇区的障碍物距离，从而增强特征的空间几何感知能力。
*   **评论家引导的轨迹细化 (CGTR)**：针对少步生成可能导致的避障精度下降，利用一个基于障碍物点云监督训练的 Critic 网络，在倒数第二步生成时计算梯度，显式地将中间轨迹推向更安全、更接近目标的区域。
*   **PPO 强化学习微调**：在自适应阶段，使用 PPO 算法对策略进行微调。为了防止灾难性遗忘，引入了行为克隆（BC）正则化项，并同步更新 Critic 以适应特定机器人的观测变化。

#### 3. 实验设计
*   **仿真环境**：基于 IsaacSim 构建，使用 InternVLA-N1 基准。场景包括 Cluttered-Easy/Hard（杂乱环境）、Home（家庭）和 Commercial（商业区）。
*   **实机验证**：使用 Unitree Go2 四足机器人，在室内走廊、复杂室内窄道及室外非结构化道路上进行点到点导航测试。
*   **对比方法**：
    *   强化学习基准：DD-PPO。
    *   传统/学习规划器：iPlanner, ViPlanner。
    *   生成式策略：NavDP（基于扩散模型）、MP1（单步 MeanFlow）。

#### 4. 资源与算力
论文明确列出了训练所需的计算资源：
*   **预训练阶段**：使用 **8 台 NVIDIA A800 GPU**，Batch Size 为 128，训练时长为 **24 小时**。
*   **微调阶段**：使用 **1 台 NVIDIA RTX 4090 GPU**，Batch Size 为 8，训练时长为 **12 小时**。
*   **推理测试**：在 RTX 4090 上测量推理延迟。

#### 5. 实验数量与充分性
实验设计较为全面且具有说服力：
*   **基准对比**：在多个标准场景下与 5 种主流方法进行了量化对比（SR 成功率、SPL 路径效率）。
*   **消融实验**：分别验证了 OPP 辅助任务、CGTR 引导、RL 微调、Critic 更新及 BC 正则化的有效性。
*   **参数敏感性分析**：探讨了去噪步数（1-10步）对性能与延迟的影响，以及 CGTR 引导系数 $\eta$ 的取值。
*   **公平性**：在对比 NavDP 时，作者在相同的训练预算和算力下重新训练了基准模型，确保了比较的客观性。

#### 6. 主要结论与发现
*   **性能提升**：NavCMPO 在平均成功率上达到 74.7%，比 NavDP 基准高出 **6.4%**。
*   **延迟降低**：推理延迟从 NavDP 的 85ms 降低至 **60ms**，实现了更好的质量-速度权衡。
*   **CGTR 的必要性**：实验证明单步生成会导致轨迹坍缩，而 5 步生成配合 CGTR 引导可以达到甚至超过 10 步扩散模型的避障水平。
*   **Sim-to-Real 成功**：通过在仿真中对 Go2 模型进行微调，策略能成功迁移至真实四足机器人，并在复杂窄道中表现出比 NavDP 更强的鲁棒性。

#### 7. 优点
*   **效率与精度的平衡**：通过 MeanFlow 和 CGTR 巧妙地解决了扩散模型在实时机器人系统中的延迟痛点。
*   **自适应能力**：两阶段框架允许模型快速适配不同的机器人实体（Embodiment）和观测分布。
*   **引导机制创新**：CGTR 不需要显式的几何成本函数，而是利用学习到的 Critic 梯度进行局部修正，具有较强的灵活性。

#### 8. 不足与局限
*   **微调环境限制**：目前的策略微调主要在仿真中完成，虽然有实机验证，但尚未实现完全在真实世界中的安全在线强化学习。
*   **对比范围**：未直接对比“NavDP + 同样的 RL 微调”后的表现，因此难以完全断定性能提升全部源于 MeanFlow 架构还是 RL 流程。
*   **应用场景**：主要集中在局部路径规划，对于需要长期记忆或复杂语义指令的大规模全局导航任务，其表现尚待验证。

（完）
