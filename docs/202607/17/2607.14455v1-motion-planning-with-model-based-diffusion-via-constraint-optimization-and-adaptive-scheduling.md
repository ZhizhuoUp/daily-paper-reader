---
title: Motion Planning with Model-Based Diffusion via Constraint Optimization and Adaptive Scheduling
title_zh: 基于约束优化与自适应调度的模型驱动扩散运动规划
authors: "Zhilin He, Bowei Li, Jianlin Dou, Yuner Zhang, Changliu Liu"
date: 2026-07-16
pdf: "https://arxiv.org/pdf/2607.14455v1"
tags: ["query:profile-1"]
score: 9.0
evidence: 非凸环境下的避障运动规划轨迹优化
tldr: 针对复杂非凸约束下的单机器人运动规划，本文提出MD-COAS框架。该方法改进了基于模型的扩散模型，通过统一非精确增广拉格朗日法的软约束先验与基于凸可行集的硬约束投影算子，并引入自适应调度机制来协同优化安全性与扩散过程。实验证明，该方法在安全性、成功率、收敛速度和路径成本方面均优于现有基准。
selection_source: fresh_fetch
motivation: 现有基于扩散的运动规划方法在集成软硬约束方面存在局限，且固定安全策略忽略了扩散过程的动态变化。
method: 提出MD-COAS框架，将iALM软约束引导与CFS硬投影算子相结合，并引入自适应调度来协同优化安全增强与扩散去噪。
result: 在2D非凸基准和7自由度机械臂任务中，该方法实现了更高的安全性、成功率以及更快的收敛速度。
conclusion: MD-COAS通过融合约束优化与扩散模型，有效解决了复杂受限环境下的高效运动规划难题。
---

## 摘要
在高度非凸的受限环境中，单机器人运动规划（SRMP）是一项具有挑战性的任务，机器人必须在复杂的约束和计算限制下满足无碰撞保证、动力学可行性和任务相关约束。最近的基于模型的扩散（MBD）方法将 SRMP 重新表述为轨迹优化，利用已知动力学从轨迹后验分布中采样，并通过对展开样本进行解析评分函数估计，引导扩散去噪过程生成低成本、清晰的轨迹，且无需演示学习。虽然现有工作进一步将 MBD 适配到受限环境并展示了极具前景的性能，但仍受到以下限制：（1）仅通过软可行性扩散先验或硬投影算子强制执行安全性，缺乏整合两者的统一框架；（2）固定的安全性执行忽略了扩散调度的变化。因此，我们为 SRMP 提出了基于约束优化与自适应调度的模型驱动扩散（MD-COAS），该方法统一了非精确增广拉格朗日方法（iALM）软扩散先验与基于凸可行集（CFS）的硬投影算子，并对安全性执行与扩散调度进行自适应调度和协同优化。实验表明，在随机生成的高度非凸 2D 基准测试和 7 自由度机械臂避障任务中，与基准规划器相比，我们的方法实现了更高的安全性与成功率、更快的收敛速度以及更低的最终成本。

## Abstract
Single-Robot Motion Planning (SRMP) in highly non-convex constrained environments, where robots must satisfy collision-free guarantees, dynamic feasibility, and task-related constraints, is challenging under complex constraints and computational limits. Recent Model-Based Diffusion (MBD) approaches recast the SRMP as trajectory optimization that samples from a posterior over trajectories, using known dynamics, and analytically estimates the score function from rollout samples to guide diffusion denoising toward a low-cost, clean trajectory without demonstration learning. While existing works further adapt MBD to constrained environments and showcase promising performance, they are still limited by (1) enforcing safety either via soft feasibility diffusion priors or hard projection operators, but lack a unified framework to integrate both, and (2) fixing safety enforcement to neglect the changing of diffusion scheduling. Therefore, we introduce Model-Based Diffusion via Constraint Optimization and Adaptive Scheduling (MD-COAS) for SRMP that unifies the inexact Augmented Lagrangian Method (iALM) soft diffusion prior with a Convex Feasible Set (CFS)-based hard projection operator, and adaptively schedules and co-optimizes safety enforcement, along with diffusion scheduling. Experiments demonstrate that our method achieves higher safety \& success rates, faster convergence, and lower final costs than baseline planners on randomly generated highly non-convex 2D benchmarks and a 7-DoF robot arm avoidance task.

---

## 论文详细总结（自动生成）

以下是对论文《Motion Planning with Model-Based Diffusion via Constraint Optimization and Adaptive Scheduling》（MD-COAS）的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是**高度非凸受限环境下的单机器人运动规划（SRMP）**。
*   **研究动机**：传统的采样规划器（如 RRT）在复杂约束下效率低下，而新兴的扩散模型（Diffusion Models）虽能生成多样化轨迹，但通常依赖大量演示数据，且难以严格保证动力学和安全性。
*   **现有挑战**：现有的基于模型的扩散（MBD）方法在处理约束时存在两个局限：
    1.  **约束处理不统一**：要么仅使用“软”可行性先验（容易导致碰撞），要么仅使用“硬”投影算子（过于保守且可能破坏扩散分布）。
    2.  **调度固定化**：忽略了扩散过程是一个从高噪声到清晰轨迹的动态演化过程，在所有去噪步骤中使用固定的安全增强参数是不合理的。

### 2. 方法论：MD-COAS 框架
论文提出了 **MD-COAS**，其核心思想是将约束优化（Constraint Optimization）深度集成到扩散去噪过程中，并引入自适应调度（Adaptive Scheduling）。

*   **核心技术细节**：
    1.  **iALM 软可行性先验**：利用非精确增广拉格朗日法（iALM）构建目标分布。通过引入对偶变量 $\lambda$ 和惩罚参数 $\rho$，为轻微违规的轨迹分配连续权重，避免了硬指标函数导致的采样“死区”。
    2.  **CFS 硬投影算子**：引入凸可行集（CFS）方法作为硬修正步骤。它通过对碰撞约束进行局部线性化，构建凸子问题并利用二次规划（QP）将扩散生成的名义轨迹投影回安全区域。
    3.  **自适应调度（AS）**：这是本文的最大创新点。
        *   **参数演化**：根据当前去噪步骤的违规程度（残差 $r_k$），动态更新 iALM 的对偶变量和惩罚项。
        *   **计算预算控制**：引入计算对偶变量 $\nu$，在保证安全性的前提下，根据预设的计算预算动态调整硬投影的触发概率 $p_k$、迭代次数 $I_k$ 和活跃约束集大小 $H_k$。
*   **算法流程**：在每一步反向扩散中，先采样名义轨迹，计算违规残差，更新软先验参数，根据调度策略决定是否执行 CFS 硬投影，最后通过动力学展开和重要性采样更新轨迹。

### 3. 实验设计
*   **实验场景**：
    1.  **2D 复杂避障基准**：包含 Easy（简单几何体）、Constrained（高密度障碍物）和 Union（非凸组合障碍物）三个等级，共 10 个难度级别（L1-L10）。
    2.  **7-DoF 机械臂避障**：在 D3IL 环境中进行，要求 7 自由度机械臂在关节空间规划轨迹，使末端执行器避开圆柱形障碍物到达目标线。
*   **对比基准（Benchmarks）**：
    *   **模型驱动类**：MBD（原始模型驱动扩散）、EB-MBD（引入涌现障碍物项）、MDOC（基于 CBF 的硬过滤）。
    *   **数据驱动类**：SafeDiffuser（基于 CBF 的安全扩散）、DPCC-C（带约束的扩散预测控制）。
*   **消融实验**：MD-COAS-A（仅自适应软先验）、MD-COAS-F（固定参数的硬投影）。

### 4. 资源与算力
*   **硬件环境**：工作站配备了 **1 块 NVIDIA RTX A6000 GPU (48GB)** 和 9 核 CPU。
*   **软件实现**：使用 `jaxopt` 在 GPU 上并行求解 CFS 投影中的 QP 问题。
*   **训练/运行时间**：由于是训练无关（Training-free）的方法，无需离线训练时长。文中给出了推理阶段的规划时间，MD-COAS 在 2D 场景下每条轨迹约需数秒，在 7-DoF 场景下约 13-15 秒。

### 5. 实验数量与充分性
*   **实验规模**：
    *   2D 场景：每个难度级别测试 10 个随机种子，每个种子生成 20 个多模态轨迹。
    *   7-DoF 场景：测试 3 种障碍物半径难度，每个难度 5 个随机种子，每个种子 20 个多模态轨迹。
*   **充分性评价**：实验设计较为**充分且客观**。作者不仅对比了同类最先进的 MBD 方法，还跨类别对比了数据驱动的方法。通过不同难度的地图（尤其是非凸 Union 地图）验证了算法在极端情况下的鲁棒性。消融实验清晰地展示了“约束优化”和“自适应调度”各自的贡献。

### 6. 主要结论与发现
1.  **安全性与成功率（SSR）**：MD-COAS 在所有难度下均保持了极高的成功率（2D 简单/中等难度 100%，极难 Union 地图约 50%，远超基准）。
2.  **收敛速度**：相比固定参数的方法，自适应调度使模型在扩散早期能更快地向可行区域靠拢。
3.  **成本优化**：生成的轨迹在保持安全的同时，路径成本（Cost）最低。
4.  **多模态保持**：与 MDOC 等容易导致模式坍缩（Collapse）的方法不同，MD-COAS 能生成多样化的避障路径。

### 7. 优点（亮点）
*   **统一框架**：首次在扩散模型中统一了 iALM 软引导和 CFS 硬投影，兼顾了采样的信息量和最终的安全性。
*   **动态适配**：引入自适应调度机制，使安全增强力度与去噪阶段相匹配，提高了计算效率。
*   **无需数据**：作为一种基于模型的方法，它不需要昂贵的专家演示数据进行预训练，具有更强的通用性。

### 8. 不足与局限
*   **计算开销**：尽管有自适应调度，但每一步去噪中涉及的 QP 求解（尤其是高维空间）仍然带来了一定的计算负担，实时性尚有提升空间。
*   **局部最优风险**：CFS 算子依赖于局部线性化，在极度扭曲的非凸空间中，如果初始噪声分布太差，仍可能陷入局部最优。
*   **单机限制**：目前仅验证了单机器人场景，尚未扩展到多机器人协同等更复杂的动态交互环境。

（完）
