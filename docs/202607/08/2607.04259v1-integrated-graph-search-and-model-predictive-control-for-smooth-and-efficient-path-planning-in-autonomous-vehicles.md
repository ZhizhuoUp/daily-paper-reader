---
title: Integrated Graph Search and Model Predictive Control for Smooth and Efficient Path Planning in Autonomous Vehicles
title_zh: 自动驾驶车辆中用于平滑高效路径规划的集成图搜索与模型预测控制
authors: "Duc-Tien Bui, Ngoc Thinh Nguyen, Hung Duy Nguyen, Dong Bi, Tomislav Mihalj, Arno Eichberger"
date: 2026-07-05
pdf: "https://arxiv.org/pdf/2607.04259v1"
tags: ["query:q1"]
score: 9.0
evidence: 基于MPC的路径优化及避障约束
tldr: "本研究针对自动驾驶路径规划中安全性、舒适性与计算效率的平衡难题，提出了一种集成图搜索与模型预测控制（MPC）的顺序规划框架。该方法首先利用Dijkstra算法在离散网格中搜索粗略路径，并据此构建空间变化的凸侧向安全走廊，将避障约束转化为连续可行性约束。随后，在MPC框架下通过惩罚侧向偏移的三阶空间导数来优化路径平滑度。实验表明，该方法在多种超车场景下显著提升了路径平滑性，并将计算成本降低了约29%，为结构化环境下的高效路径规划提供了新方案。"
selection_source: fresh_fetch
motivation: 旨在解决自动驾驶路径规划中如何在保证安全避障的同时，兼顾路径平滑性与实时计算效率的挑战。
method: 结合Dijkstra图搜索生成的安全走廊与MPC优化算法，通过惩罚高阶导数实现路径的精细化平滑处理。
result: "仿真结果显示，该方法在降低横向加速度和曲率的同时，在直道和弯道场景下分别减少了28.08%和29.52%的计算开销。"
conclusion: 证明了利用图搜索结构引导MPC优化能有效提升自动驾驶车辆在复杂驾驶环境下的规划性能与运行效率。
---

## 摘要
路径规划是自动驾驶车辆的核心组成部分，在确保计算效率的同时实现安全、舒适且符合动力学约束的路径仍然是一个重大挑战。本文提出了一种序列路径规划框架，其中显式利用通过图搜索获得的粗略路径来引导基于模型预测控制（MPC）的路径精细化。首先通过在离散网格上进行 Dijkstra 搜索获得粗略路径，然后将其用于构建空间变化的凸横向安全走廊，该走廊显式捕捉避障约束，将离散的避障决策转化为优化的连续可行性约束。在该走廊内，构建了一个 MPC 问题来精细化路径，通过在预测时域内惩罚横向偏移的三阶空间导数，在保持路径平滑性的同时实现高效优化。所提算法在直线和弯道道路的多种超车场景中进行了评估，包括单目标车辆和多目标车辆的情况，并使用了高保真环境仿真（即 CarMaker）。与之前使用多项式拟合和二次规划方法的研究相比，所提方法在直线道路上降低了 28.08% 的计算成本，在弯道道路上降低了 29.52%，同时持续实现了更低的横向加速度、曲率和加加速度（jerk）。这些结果表明，在 MPC 框架中利用图搜索结构，为结构化驾驶环境下的自动驾驶车辆在路径平滑性和计算效率之间提供了有效的平衡。

## Abstract
Path planning is a fundamental component of autonomous vehicles, where achieving safe, comfortable, and dynamically feasible paths while ensuring computational efficiency remains a significant challenge. This paper presents a sequential path planning framework in which a rough path obtained from graph search is explicitly exploited to guide a Model Predictive Control (MPC)-based path refinement. A rough path is first obtained via Dijkstra search on a discretized grid and is then used to construct a spatially varying convex lateral safety corridor that explicitly captures obstacle avoidance constraints, transforming discrete obstacle avoidance decisions into continuous feasibility constraints for optimization. Within this corridor, an MPC problem is formulated to refine the path, enabling efficient optimization while maintaining path smoothness by penalizing the third-order spatial derivative of the lateral offset over a prediction horizon. The proposed algorithm is evaluated in multiple overtaking scenarios on both straight and curved roads, including cases with single and multiple target vehicles, using high-fidelity environment simulations (i.e., CarMaker). Compared with the previous study, which used polynomial fitting and a quadratic programming method, the proposed approach consistently achieves lower lateral acceleration, curvature, and jerk while reducing computational cost by 28.08% on straight roads and 29.52% on curved roads. These results demonstrate that exploiting graph-search structure within an MPC formulation provides an effective balance between path smoothness and computational efficiency for autonomous vehicles in structured driving environments.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种结合图搜索与模型预测控制（MPC）的自动驾驶路径规划框架，旨在解决路径平滑性与计算效率之间的矛盾。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在保证自动驾驶车辆安全避障的同时，生成符合动力学约束、乘客舒适（平滑）且具备实时计算效率的路径。
*   **研究动机**：
    *   传统图搜索算法（如Dijkstra、A*）计算快但路径不平滑，难以直接用于车辆控制。
    *   纯优化算法（如MPC、二次规划QP）虽然路径平滑，但对初始值敏感，在复杂动态环境下容易陷入局部最优或计算开销过大。
    *   采样算法（如RRT）生成的路径通常具有随机性，需要繁琐的后处理。

### 2. 提出的方法论
论文提出了一种**顺序路径规划框架**，核心步骤如下：
*   **离散网格与图搜索**：在Frenet坐标系下对环境进行网格化，利用**Dijkstra算法**在离散空间内搜索出一条避开障碍物的“粗略路径”（Rough Path）。
*   **凸安全走廊构建**：基于粗略路径，显式地提取出空间变化的横向边界（$l_{min}$ 和 $l_{max}$），形成一个**凸侧向安全走廊**。这一步将离散的避障决策（左绕或右绕）转化为连续的几何约束。
*   **基于MPC的路径精细化**：
    *   **状态空间模型**：将路径建模为关于纵向位移 $s$ 的函数，状态向量包含横向偏移、一阶导数（航向）和二阶导数（曲率）。
    *   **控制输入**：将横向偏移的**三阶空间导数**作为控制输入，通过在代价函数中惩罚该项来保证路径的极高平滑度（类似减少Jerk）。
    *   **约束优化**：在安全走廊约束下，利用MPC求解器（如MATLAB的mpcActiveSetSolver）在预测时域内滚动优化，输出最终平滑路径。

### 3. 实验设计
*   **仿真环境**：使用 **IPG CarMaker 11.1.1**（高保真动力学仿真）与 MATLAB/Simulink 联合仿真。
*   **测试场景**：
    1.  **直线道路单车超车**：AV以110km/h超车90km/h的目标车。
    2.  **弯道道路单车超车**：在半径500m的弯道上，AV以100km/h超车80km/h的目标车。
    3.  **多目标车复杂场景**：涉及多车减速、切入、加速等动态交互。
*   **对比基准（Benchmark）**：对比了前人研究中采用的“离散采样点 + 多项式拟合 + 二次规划（QP）”的方法。

### 4. 资源与算力
*   **硬件平台**：HP ZBook Fury G8 移动工作站。
*   **处理器**：第11代 Intel Core i7-11800H (2.30 GHz, 8核16线程)。
*   **内存**：32 GB DDR4 RAM。
*   **算力说明**：文中未提及使用GPU，该算法主要依赖CPU进行数值优化求解。

### 5. 实验数量与充分性
*   **实验规模**：论文详细展示了3组典型场景的仿真结果。
*   **充分性评价**：
    *   **客观性**：通过横向加速度、加加速度（Jerk）、曲率、方向盘转角和计算耗时等多个量化指标进行对比，数据较为详实。
    *   **局限性**：实验主要集中在结构化道路（高速/干道）的超车任务，未涵盖城市复杂路口、非结构化道路或极端天气等边缘场景。实验组数相对较少，缺乏大规模交通流下的统计学表现。

### 6. 主要结论与发现
*   **效率提升**：相比于传统的QP优化方法，所提算法在直线道路上计算成本降低了 **28.08%**，在弯道上降低了 **29.52%**。
*   **平滑性增强**：生成的路径在横向加速度、曲率和Jerk指标上均显著低于对比方法，提升了行车舒适性。
*   **实时性**：平均每步计算耗时约为 **0.0017秒**，远低于对比方法的0.0021秒，证明了其在高速行驶下的实时可行性。

### 7. 优点
*   **结构化引导**：利用图搜索为MPC提供了一个极佳的初始可行域（安全走廊），避免了优化算法在复杂约束下的收敛难题。
*   **约束简化**：将复杂的避障逻辑简化为线性约束，极大地降低了优化问题的维度和求解难度。
*   **高保真验证**：使用了CarMaker进行验证，比简单的质点模型仿真更具说服力。

### 8. 不足与局限
*   **环境假设**：主要针对结构化道路，对于完全随机或无车道线的场景适用性待验证。
*   **动态预测**：论文重点在于路径规划，对目标车辆未来轨迹的预测模型描述较少，主要依赖实时重规划来应对动态变化。
*   **参数敏感性**：MPC的权重分配（如 $w_l, w_u$ 等）仍需经验调试，缺乏自动调优机制。

（完）
