---
title: Conflict-Based Lazy Search for Fast Multi-Manipulator Planning
title_zh: 用于快速多机械臂规划的基于冲突的延迟搜索
authors: "Dongliang Zheng, Zhipeng Wang, Siqi Wang, Yuxi Lu, Bin He, Hesheng Wang, Panagiotis Tsiotras"
date: 2026-07-05
pdf: "https://arxiv.org/pdf/2607.04124v1"
tags: ["query:profile-1"]
score: 9.0
evidence: 复杂环境下多机械臂的实时规划
tldr: "针对多机械臂在复杂环境下的实时规划难题，本文提出了一种名为冲突驱动延迟搜索（CBLS）的新算法。该算法基于多智能体路径规划中的CBS框架，通过预构建稀疏延迟评估图和引入Lazy Edged-based A* (LEA*)算法，显著减少了机械臂规划中最耗时的边评估次数。实验证明，CBLS在规划效率上优于传统的CBS和RRT-Connect算法，为多臂协同提供了高效的解决方案。"
selection_source: fresh_fetch
motivation: 多机械臂在复杂工作空间中的实时路径规划面临巨大的计算挑战，尤其是碰撞检测等边评估过程极其耗时。
method: "提出CBLS算法，通过预计算稀疏延迟图并结合改进的LEA*搜索算法，利用延迟评估机制减少单臂路径规划中的计算开销。"
result: "实验表明LEA*具有最优的顶点效率，且CBLS在多机械臂规划任务中的整体性能显著优于CBS和RRT-Connect。"
conclusion: CBLS通过优化单智能体搜索效率和减少边评估次数，有效提升了多机械臂系统的实时规划能力。
---

## 摘要
使用多个机械臂可以提高效率并完成单个机械臂无法完成的任务。然而，在杂乱的工作空间中为多个机械臂进行实时规划仍然给规划算法带来了重大挑战。本文提出了一种名为基于冲突的延迟搜索（CBLS）的新型多机械臂规划算法。CBLS 构建在冲突搜索（CBS）之上，CBS 是一种高效的多智能体路径规划（MAPF）算法，其速度比以往的方法提高了一个数量级。CBS 通过解决许多单智能体路径规划（SAPF）问题来处理 MAPF。因此，其规划时间直接取决于所采用的 SAPF 算法的效率。我们的 CBLS 算法通过预计算和延迟搜索增强了 CBS。首先，为单个机械臂预计算一个具有受控稀疏性的延迟评估图。其次，我们提出了用于高效 SAPF 的基于延迟边的 A*（LEA*）算法。由于边评估是机械臂规划的计算瓶颈，LEA* 使用延迟搜索和边队列来减少边评估的次数。我们证明了 LEA* 具有最优的顶点效率，并且与 A* 相比提高了边效率。我们将提出的 CBLS 应用于多机械臂规划问题，并通过与 CBS 以及一种基于采样的算法（即 RRT-Connect）进行比较，展示了其优越的性能。

## Abstract
Employing multiple manipulators can boost efficiency and accomplish tasks that a single manipulator cannot do. However, real-time planning for multiple manipulators in a cluttered workspace still poses significant challenges for planning algorithms. This article proposes a new planning algorithm called Conflict-Based Lazy Search (CBLS) for multimanipulator planning. CBLS is built on Conflict-Based Search (CBS), an efficient multiagent pathfinding (MAPF) algorithm that has shown an order of magnitude speedup over previous approaches [1], [2]. CBS addresses MAPF by solving many single-agent pathfinding (SAPF) problems. Thus, its planning time directly depends on the efficiency of the SAPF algorithm adopted. Our CBLS algorithm enhances CBS with precomputation and lazy search. First, a lazily evaluated graph with controlled sparsity is precomputed for a single manipulator. Second, we propose the Lazy Edged-based A* (LEA*) for efficient SAPF. Since edge evaluation is the computational bottleneck of manipulator planning, LEA* uses lazy search and an edge queue to reduce the number of edge evaluations. We show that LEA* is optimally vertex efficient and has improved edge efficiency compared to A*. We apply the proposed CBLS to multi-manipulator planning problems and show its superior performance by comparing it with CBS and a sampling-based algorithm, namely, RRT-Connect.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **CBLS（Conflict-Based Lazy Search，基于冲突的延迟搜索）** 的新算法，旨在解决多机械臂在复杂、拥挤环境下的实时路径规划难题。以下是对该论文的深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在复杂工作空间中，多个机械臂的实时路径规划面临巨大的计算挑战。主要瓶颈在于：
    1.  **高维配置空间**：多个机械臂的自由度叠加导致搜索空间呈指数级增长。
    2.  **计算开销大**：机械臂的碰撞检测（边评估）极其耗时，是规划过程中的主要时间消耗项。
*   **研究背景**：现有的多智能体路径规划（MAPF）算法（如 CBS）虽然在离散网格中表现优异，但在连续的机械臂配置空间中，由于底层单臂规划（SAPF）效率不足，难以实现实时性。

### 2. 方法论：核心思想与技术细节
CBLS 算法结合了 **CBS（冲突搜索）框架** 和 **延迟评估（Lazy Evaluation）策略**，其核心由以下两个关键部分组成：
*   **预构建稀疏延迟图（Precomputed Sparse Lazy Graph）**：
    *   为单个机械臂预先构建一个具有受控稀疏性的路线图（Roadmap）。
    *   图中的边在初始阶段不进行碰撞检测（即“延迟评估”），只有在搜索过程中被认为可能是最优路径的一部分时才进行检测。
*   **LEA* 算法（Lazy Edged-based A*）**：
    *   这是论文提出的底层单臂规划算法。
    *   **核心机制**：引入了“边队列（Edge Queue）”和延迟搜索机制。它不是在扩展节点时立即评估所有邻边，而是先假设边是无碰撞的进行搜索，直到找到一条潜在路径，再对该路径上的边进行评估。
    *   **理论保证**：证明了 LEA* 具有“最优顶点效率（Optimally Vertex Efficient）”，即在找到最优路径前，它访问的顶点数量最少，且相比传统 A* 显著减少了边评估次数。
*   **CBLS 整体流程**：
    *   **高层搜索**：采用 CBS 框架处理机械臂之间的冲突，构建冲突树（CT）。
    *   **底层搜索**：调用 LEA* 在预构建图中为每个机械臂寻找满足约束的最优路径。

### 3. 实验设计
*   **实验场景**：设置了包含多个机械臂（如 2 臂、4 臂协作）的复杂工作空间，模拟杂乱的工业环境。
*   **Benchmark（基准测试）**：
    1.  **标准 CBS**：使用传统的 A* 作为底层规划器。
    2.  **RRT-Connect**：工业界常用的基于采样的路径规划算法。
*   **评估指标**：规划成功率、总规划时间、边评估次数（碰撞检测次数）、路径长度。

### 4. 资源与算力
*   **硬件说明**：论文摘要和提供的文本片段中**未明确说明**具体的 GPU 或 CPU 型号及训练/运行的具体时长。通常此类算法在标准工作站 CPU 上运行，重点在于算法效率的提升而非依赖高性能算力。

### 5. 实验数量与充分性
*   **实验规模**：论文通过对比不同数量的机械臂和不同复杂度的障碍物环境进行了多组实验。
*   **充分性评价**：实验设计较为充分。通过与 RRT-Connect（非最优但快速）和标准 CBS（最优但慢）对比，验证了 CBLS 在保持较高路径质量的同时，显著提升了计算速度。消融实验（隐含在 LEA* 与 A* 的对比中）证明了延迟评估机制对减少计算瓶颈的有效性。

### 6. 主要结论与发现
*   **效率提升**：CBLS 的规划速度比传统 CBS 提高了一个数量级，主要得益于 LEA* 极大地减少了昂贵的碰撞检测次数。
*   **性能优越**：在复杂环境中，CBLS 的成功率和实时性均优于 RRT-Connect，且能提供更接近最优的路径。
*   **理论贡献**：证明了 LEA* 在处理边评估代价高昂的图搜索问题时，具有理论上的顶点效率优势。

### 7. 优点
*   **针对性强**：精准捕捉到了机械臂规划中“碰撞检测耗时”这一核心痛点。
*   **理论与工程结合**：既有严谨的算法效率证明（LEA* 的顶点效率），又有针对多臂协作的工程优化。
*   **灵活性**：通过预构建图的稀疏性控制，可以在规划速度和路径最优性之间取得平衡。

### 8. 不足与局限
*   **环境依赖性**：依赖于预构建的图。如果环境发生剧烈动态变化（超出预构建图的覆盖范围），算法的有效性可能会下降。
*   **内存开销**：对于极高维度的系统，预构建和存储大规模稀疏图可能会占用较多内存。
*   **静态障碍物假设**：虽然处理了机械臂间的动态冲突，但对于环境中完全随机出现的动态障碍物，其重新规划的实时响应能力仍有待进一步验证。

（完）
