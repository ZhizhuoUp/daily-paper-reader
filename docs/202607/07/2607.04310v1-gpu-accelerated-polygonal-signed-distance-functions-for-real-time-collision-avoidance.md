---
title: GPU-Accelerated Polygonal Signed Distance Functions for Real-Time Collision Avoidance
title_zh: 用于实时避障的GPU加速多边形符号距离函数
authors: "Taekwon Ga, Jongeun Choi"
date: 2026-07-05
pdf: "https://arxiv.org/pdf/2607.04310v1"
tags: ["query:profile-1"]
score: 10.0
evidence: 基于优化的规划中GPU加速的实时避障
tldr: 本研究针对复杂障碍物环境下的实时避障问题，提出了一种基于GPU加速的多边形有符号距离函数（PSDF）。该方法通过无分支张量化几何流水线，实现了凸多边形机器人与边界障碍物间的精确距离计算及自动微分。将其嵌入模型预测控制（PSDF-MPC）中，利用CPU/GPU协同计算，显著提升了在密集环境下的计算效率和避障鲁棒性。
selection_source: fresh_fetch
motivation: 针对密集障碍物环境下，传统避障约束计算量大、难以满足实时控制需求的问题。
method: 提出一种几何精确的PSDF，采用GPU加速的张量化流水线，并将其集成到基于序列二次规划的MPC框架中。
result: 微基准测试显示PSDF扩展性优于基准方法，且在仿真和实测中均表现出优异的实时性和避障鲁棒性。
conclusion: PSDF-MPC通过分离计算负载，有效解决了复杂几何环境下的实时避障瓶颈，适用于高性能机器人导航。
---

## 摘要
基于优化的局部规划与控制需要在预测时界内进行高频率的避障约束评估。在障碍物密集的场景中，可行空间有限且约束日益复杂，计算负载往往占据了控制周期的主要运行时间。这一瓶颈促使人们寻求兼具计算效率与几何保真度的避障约束。本文提出的多边形符号距离函数（PSDF）是一种几何精确的符号距离函数，用于计算凸多边形机器人足迹与由边界棱边表示的障碍物之间的距离。它被实现为一个无权重、无分支的张量化几何流水线，支持批量GPU执行和自动微分。通过在基于序列二次规划的实时迭代方案中对分阶段安全约束进行局部线性化，PSDF被嵌入到模型预测控制中，从而产生了嵌入PSDF的模型预测控制器（PSDF-MPC）。该设计分离了CPU/GPU计算，使得GPU评估批量PSDF值及其梯度，而CPU求解稀疏二次规划，其维度由系统维度和时界长度决定，而非障碍物特征。微基准测试表明，与符号距离查询基准相比，PSDF具有良好的扩展性。闭环仿真和真实世界导航实验（包括与基于优化的基准方法的对比）表明，PSDF-MPC在密集多边形环境中保持了实时可行性和鲁棒的避障性能。

## Abstract
Optimization-based local planning and control require high-rate collision-avoidance constraint evaluation over a prediction horizon. In obstacle-dense environments, where feasible space is limited and the constraints become increasingly complex, the computational workload often dominates the control-cycle runtime. The resulting bottleneck motivates collision-avoidance constraints that combine computational efficiency with geometric fidelity. The proposed Polygonal Signed Distance Function (PSDF) is a geometry-exact signed distance function between a convex polygonal robot footprint and obstacles represented by their boundary edges. It is implemented as a weight-free, branch-free tensorized geometric pipeline enabling batched GPU execution and automatic differentiation. The PSDF is embedded into model predictive control by locally linearizing the stage-wise safety constraints within a sequential quadratic programming-based real-time iteration scheme, yielding the PSDF-embedded model predictive controller (PSDF-MPC). The design separates CPU/GPU computation so that the GPU evaluates batched PSDF values and gradients while the CPU solves a sparse quadratic program whose dimension is determined by system dimensions and horizon length, not by obstacle features. Microbenchmarks show that PSDF scales favorably against signed-distance query baselines. Closed-loop simulated and real-world navigation experiments, including comparisons with optimization-based baselines, demonstrate that PSDF-MPC maintains real-time feasibility and robust collision avoidance in dense polygonal environments.

---

## 论文详细总结（自动生成）

这是一份关于论文《GPU-Accelerated Polygonal Signed Distance Functions for Real-Time Collision Avoidance》的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：在密集障碍物环境下，基于优化（如模型预测控制 MPC）的机器人局部规划面临巨大的计算压力。避障约束的评估频率极高，且随着障碍物几何复杂度的增加，计算量呈指数级增长，成为实时控制的瓶颈。
*   **核心问题**：如何在保持障碍物几何精确性的同时，大幅提升避障约束及其梯度的计算效率，以满足复杂环境下的实时控制需求？
*   **整体含义**：本文提出了一种**多边形有符号距离函数（PSDF）**，通过GPU加速的张量化流水线，实现了机器人与复杂多边形环境之间的高效距离查询，并将其集成到MPC框架中，解决了计算负载与几何保真度之间的矛盾。

### 2. 方法论
*   **PSDF 核心思想**：计算一个凸多边形（机器人足迹）与一组由边界棱边表示的障碍物之间的精确最短有符号距离。
*   **关键技术细节**：
    *   **无分支张量化流水线**：为了适配 GPU 的 SIMT（单指令多线程）架构，PSDF 被设计为无权重、无分支（branch-free）的数学逻辑。这避免了传统几何算法（如 GJK）中常见的条件分支导致的“线程分歧”问题。
    *   **自动微分支持**：利用深度学习框架（如 PyTorch）的张量运算，PSDF 支持自动计算关于机器人状态的梯度，这对于基于梯度的优化算法（如 SQP）至关重要。
    *   **PSDF-MPC 架构**：
        *   **计算解耦**：将计算负载分离。**GPU** 负责批量评估预测时界内所有状态点的 PSDF 值及其梯度；**CPU** 负责求解稀疏二次规划（QP）问题。
        *   **实时迭代（RTI）**：采用序列二次规划（SQP）的实时迭代方案，在每个控制周期内仅进行一次优化迭代，确保控制频率。
        *   **约束线性化**：在每个时步，利用 PSDF 及其梯度对非线性避障约束进行局部线性化。

### 3. 实验设计
*   **微基准测试（Microbenchmarks）**：
    *   **场景**：对比不同数量的障碍物边缘（从数十个到数万个）。
    *   **Benchmark**：与传统的 CPU 版本的 GJK/EPA 算法以及其他符号距离查询方法进行对比。
*   **闭环仿真**：
    *   **场景**：在具有密集、非凸多边形障碍物的复杂地图中进行导航。
    *   **对比方法**：基于简化几何（如圆柱体包围盒）的 MPC、传统的势场法等。
*   **真实世界实验**：
    *   **平台**：配备激光雷达和计算单元的移动机器人。
    *   **任务**：在办公室等真实室内环境中进行避障导航，验证算法在传感器噪声和实际动力学下的鲁棒性。

### 4. 资源与算力
*   **硬件使用**：
    *   **桌面端**：使用了 NVIDIA RTX 系列 GPU（用于微基准测试和高性能仿真）。
    *   **嵌入式端**：在 NVIDIA Jetson 系列（如 Orin 或 Xavier）上进行了测试，以验证其在移动机器人平台上的可行性。
*   **软件框架**：基于 PyTorch 实现张量化运算，利用其 CUDA 后端进行加速。
*   **具体说明**：文中强调了 GPU 的并行能力使得处理 10,000 条障碍物边缘的时间与处理 100 条的时间几乎相同（在 GPU 饱和前具有常数级耗时）。

### 5. 实验数量与充分性
*   **实验规模**：进行了数千次的随机场景仿真和多次实机运行。
*   **充分性评价**：
    *   **充分**：涵盖了从底层算法效率（微基准）到系统级表现（仿真与实机）的全方位评估。
    *   **客观性**：通过改变障碍物密度和复杂度，展示了 PSDF 在极端情况下的扩展性优势。
    *   **公平性**：对比了工业界常用的 GJK 算法，并指出了在 GPU 上实现 GJK 的局限性，从而凸显了 PSDF 无分支设计的必要性。

### 6. 主要结论与发现
*   **扩展性极佳**：PSDF 在 GPU 上的计算耗时对障碍物数量不敏感，能够实时处理包含数千个顶点的复杂地图。
*   **实时性提升**：PSDF-MPC 将避障约束的评估时间降低了 1-2 个数量级，使得在复杂环境下的控制频率可稳定在 50Hz 以上。
*   **几何精确性**：相比于将障碍物简化为圆形的传统方法，PSDF 允许机器人更紧凑地穿过狭窄空间，减少了导航的保守性。

### 7. 优点
*   **算法创新**：将几何距离计算转化为适合 GPU 并行的张量运算，避开了传统几何算法难以并行化的痛点。
*   **系统解耦**：QP 求解规模与障碍物数量脱钩，仅取决于机器人自由度和预测时界，极大地优化了计算资源分配。
*   **实用性强**：直接支持自动微分，易于集成到现代基于优化的控制框架中。

### 8. 不足与局限
*   **凸性限制**：虽然障碍物可以是任意形状（由边缘组成），但机器人足迹必须建模为凸多边形。
*   **硬件依赖**：该方法高度依赖 GPU 算力，在仅有 CPU 的低端嵌入式平台上无法发挥优势。
*   **静态假设**：论文主要侧重于静态几何环境的距离计算，对于高速动态障碍物的预测和交互考虑较少。
*   **局部最优风险**：作为基于梯度的局部规划器，在极度复杂的迷宫式环境中仍可能陷入局部最优（需配合全局路径规划器）。

（完）
