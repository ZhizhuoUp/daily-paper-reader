---
title: Reachability-Based Safe-Start Regions for Approach to a Tumbling Target with Rotating LOS Constraints
title_zh: 基于可达性的旋转视线约束下翻滚目标接近过程的安全启动区域
authors: "Omer Burak Iskender, Keck Voon Ling, Wee Seng Lim, Erick Lansard"
date: 2026-07-02
pdf: "https://arxiv.org/pdf/2607.02128v1"
tags: ["query:profile-1"]
score: 9.0
evidence: 针对翻滚空间目标的收缩时域MPC自主接近
tldr: 本研究针对翻滚非合作目标的自主接近任务，提出了一种基于可达性的安全启动区域构建方法。针对旋转视线（LOS）约束产生的时变多面体限制，通过方向性约束侵蚀和同步范围边界两个准则确定安全区域。采用MPC控制器实现闭环引导，并设计了三阶段跟踪律。该方法在保证物理可行性的同时，计算速度比Hamilton-Jacobi方法快250倍，为机载实时决策提供了可靠的内部证书。
selection_source: fresh_fetch
motivation: 针对翻滚目标接近过程中旋转视线约束带来的时变复杂性，寻求一种高效且物理可行的安全启动区域判定方法。
method: 结合方向性约束侵蚀准则与同步范围边界构建解析安全区域，并利用基于CWH动力学的收缩视界MPC进行闭环引导。
result: 该解析边界在预测闭环可行性方面表现出0.80的精度和0.91的召回率，且计算速度比传统的Hamilton-Jacobi方法快250倍。
conclusion: 该解析安全启动区域是机载实时“去/留”决策的有效内部证明，在计算效率和物理保真度之间取得了良好平衡。
---

## 摘要
本文提出了一种可达性感知的制导架构，用于在旋转视线（LOS）对接走廊约束下，自主接近翻滚的非合作目标。LOS 容许集随目标体坐标系旋转，从而在追踪器的相对坐标系中产生随时间变化的多面体约束。通过两个保守准则构建安全启动区域：(i) 定向逐约束侵蚀，即在推力能够阻止旋转诱导漂移之前所消耗的余量；(ii) 同步距离边界 $r < 2a_{\max}/ω_t^2$，确保追踪器能够抵消视在旋转速度而不越过保持点。闭环制导采用具有 Clohessy-Wiltshire-Hill (CWH) 预测动力学的收缩时界 MPC 控制器，并在二次规划中包含显式 LOS 走廊约束。真实值传播使用带有子步长的精确离散 CWH 状态转移矩阵，因此可行性声明在物理上是真实的：未应用参考混合或状态投影。一种三阶段跟踪律管理从远程惯性接近到体坐标系同向旋转及同步保持的过渡。将解析安全启动区域与四种标准可达性引擎（后向和前向多面体可达集、Hamilton-Jacobi 水平集以及闭环蒙特卡洛）进行了基准测试：在 500 个案例的扫描中，闭式准则比 Hamilton-Jacobi 可达性快 250 倍，同时预测闭环可行性的精确率为 0.80，召回率为 0.91。剩余 6% 的误报率以及与 Hamilton-Jacobi 之间的 IoU 差距量化了一个结构特性：同步集（到达并同向旋转）是位置可达集的真子集，且差距随翻滚速率的增加而扩大。因此，在 Hamilton-Jacobi 计算成本极高的情况下，该解析边界是星载执行/不执行决策的可靠内部凭证。

## Abstract
This paper presents a reachability-aware guidance architecture for autonomous approach to a tumbling, uncooperative target under a rotating line-of-sight (LOS) docking corridor. The LOS admissible set rotates with the target body frame, producing time-varying polyhedral constraints in the chaser's relative coordinates. A safe-start region is constructed via two conservative criteria: (i) directional per-constraint erosion, the margin consumed by rotation-induced drift before thrust can arrest it, and (ii) a synchronization range bound $r < 2a_{\max}/ω_t^2$ ensuring the chaser can cancel the apparent rotational velocity without overshooting the hold point. Closed-loop guidance uses a receding-horizon MPC controller with Clohessy-Wiltshire-Hill (CWH) prediction dynamics and explicit LOS corridor constraints in the quadratic program. Truth propagation uses the exact discrete CWH state-transition matrix with sub-stepping, so feasibility claims are physically honest: no reference blending or state projection is applied. A three-regime tracking law manages the transition from long-range inertial approach to body-frame co-rotation and synchronized hold. The analytical safe-start region is benchmarked against four standard reachability engines (backward and forward polytopic reachable sets, Hamilton-Jacobi level sets, and closed-loop Monte Carlo): the closed-form criteria are 250x faster than Hamilton-Jacobi reachability while predicting closed-loop feasibility with precision 0.80 and recall 0.91 on a 500-case sweep. The residual 6% false-positive rate and the IoU gap against Hamilton-Jacobi quantify a structural property: the synchronization set (reach and co-rotate) is a strict subset of the positional reachable set, the gap widening with tumble rate. The analytical bound is thus a sound inner certificate for onboard go/no-go decisions where Hamilton-Jacobi is prohibitively expensive.

---

## 论文详细总结（自动生成）

### 论文总结：基于可达性的旋转视线约束下翻滚目标接近过程的安全启动区域

#### 1. 核心问题与整体含义
本文研究的核心问题是：在面对一个**翻滚（Tumbling）且非合作**的空间目标时，追踪航天器从哪些初始状态出发，能够安全地进入并保持在随目标旋转的视线（LOS）对接走廊内。
*   **研究背景**：传统的交会对接通常假设目标是稳定的，但对于空间碎片清除或故障卫星维修，目标往往存在旋转，导致对接走廊在惯性空间中随时间快速变化。
*   **研究动机**：现有的 Hamilton-Jacobi (HJ) 可达性分析虽然精确，但计算量巨大，难以在星载计算机上实时运行。本文旨在提出一种计算高效、物理保真度高的“安全启动区域”判定方法，为自主任务提供实时的“去/留（Go/No-go）”决策支持。

#### 2. 方法论
论文提出了一套结合解析准则与模型预测控制（MPC）的制导架构：
*   **动力学模型**：采用三维 Clohessy-Wiltshire-Hill (CWH) 方程描述相对运动，并使用精确的离散状态转移矩阵进行预测和仿真，确保物理真实性。
*   **安全启动区域解析准则**：
    1.  **定向逐约束侵蚀（Directional Per-Constraint Erosion）**：量化由于目标旋转引起的视在漂移在推力介入前消耗的几何余量。
    2.  **同步距离边界（Synchronization Range Bound）**：推导出一个闭式解 $r < 2a_{\max}/\omega_t^2$，定义了追踪器能够抵消旋转速度而不发生超调的最大距离。
*   **制导与控制**：
    1.  **三阶段跟踪律**：分为远程惯性接近、近程体坐标系跟踪、同步保持三个阶段。
    2.  **安全过滤 MPC**：在每个步长求解一个轻量级的二次规划（QP），确保在预测时界内满足时变的 LOS 约束。

#### 3. 实验设计
*   **实验场景**：低地球轨道（LEO）下的三维相对运动接近任务。
*   **Benchmark（基准方法）**：
    *   **Hamilton-Jacobi (HJ) 可达性**：作为位置可达性的“地面真值”。
    *   **后向可达集 (BRS) 与前向可达集 (FRS)**：基于多面体计算的方法。
    *   **闭环蒙特卡洛 (MC) 仿真**：通过大量随机初始条件的实际飞行结果验证可行性。
*   **对比指标**：计算耗时、IoU（交并比）、精确率（Precision）、召回率（Recall）及 Matthews 相关系数。

#### 4. 资源与算力
*   **软件工具**：使用了基于 JAX 的 `hj_reachability` 库进行 HJ 计算。
*   **算力说明**：文中**未明确列出**具体的 GPU 或 CPU 型号。但通过耗时对比指出：解析方法耗时约 0.01s，而 HJ 方法耗时约 2.4s，闭环蒙特卡洛则需数百秒。

#### 5. 实验数量与充分性
*   **实验规模**：进行了 **500 组** 闭环随机案例扫描，涵盖了不同的推力水平（$a_{\max}$）、翻滚速率（$\omega_t$）和初始距离（$r_0$）。
*   **充分性**：实验设计较为全面，不仅对比了不同算法的计算效率，还深入探讨了“同步可达性”与“位置可达性”之间的结构性差异。通过消融实验式的对比，验证了 CWH 动力学耦合对结果的影响，实验结果具有较高的客观性和说服力。

#### 6. 主要结论与发现
*   **计算效率**：提出的解析准则比 HJ 可达性分析快约 **250 倍**。
*   **预测精度**：在预测闭环 MPC 是否成功方面，该方法达到了 **0.92 的准确率**，召回率高达 0.91。
*   **结构性发现**：同步集（要求到达并匹配速度）是位置可达集（仅要求到达）的**真子集**。随着翻滚速率增加，两者之间的差距显著扩大。
*   **物理保真度**：证明了在仿真中使用简单的双积分模型会产生“幻影增量 V”，导致结果过于乐观；必须使用 CWH 动力学才能反映真实的物理限制。

#### 7. 优点
*   **高效性**：极低的计算开销使其非常适合星载实时应用。
*   **物理严谨**：不依赖参考混合或状态投影，所有可行性结论均基于真实的动力学约束。
*   **决策参考**：提供了一个可靠的“内部证书（Inner Certificate）”，能够保守地保证任务安全性。

#### 8. 不足与局限
*   **误报风险**：在高翻滚速率下存在约 6% 的误报率（即解析判定安全但实际飞行失败），这主要是由于 MPC 离散化和推力矢量分配（径向与切向竞争推力预算）导致的。
*   **模型限制**：目前主要针对近圆轨道的 CWH 模型，对于大椭圆轨道的适用性尚在扩展中。
*   **保守性**：作为一种“内部近似”，它可能会排除掉一些虽然可行但处于边缘状态的初始位置。

（完）
