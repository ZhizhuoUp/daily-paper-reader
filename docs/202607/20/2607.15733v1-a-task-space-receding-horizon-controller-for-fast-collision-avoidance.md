---
title: A Task-Space Receding Horizon Controller for Fast Collision Avoidance
title_zh: 一种用于快速避障的任务空间后退水平控制器
authors: "Mattia Penzotti, Marco Controzzi"
date: 2026-07-17
pdf: "https://arxiv.org/pdf/2607.15733v1"
tags: ["query:profile-1"]
score: 10.0
evidence: 机械臂避障的实时收缩地平线控制器
tldr: 针对机器人机械臂在动态环境中的实时避障问题，本文提出一种任务空间收缩时界控制器。该方法结合了反应式方法的计算效率与模型预测控制的前瞻性，通过短时界接触一致性外推生成满足非穿透约束的末端参考，并计算平滑过渡指令。实验证明，该方法在处理高自由度系统和动态障碍物时，比传统MPC和反应式方法具有更高的成功率和实时性。
selection_source: fresh_fetch
motivation: 传统的全模型预测控制计算开销随约束增加而剧增，而反应式避障方法在动态复杂环境中容易因缺乏前瞻性而陷入局部最优。
method: 提出一种基于任务空间的收缩时界控制器，利用迭代动力学求解器在膨胀几何体上进行短时界外推，生成考虑接触和自避障的末端参考并计算平滑控制量。
result: 在40自由度仿真和6自由度硬件实验中，该方法在保持实时计算速度的同时，显著提升了在动态杂乱环境中的避障成功率。
conclusion: 该控制器成功平衡了预测能力、响应速度与计算成本，为复杂动态环境下的机器人实时避障提供了一种高效且稳健的解决方案。
---

## 摘要
机器人操作臂的实时避障需要对突发的障碍物运动做出快速反应，并具备前瞻性以避免陷入近未来的约束。全模型预测控制（MPC）可以提供这种预见性，但其在线计算成本会随着时域长度、模型保真度和激活几何约束的数量而迅速增加。相反，无时域的反应式方法虽然计算效率高，但在动态杂乱环境中可能存在短视问题。我们提出了一种任务空间后退水平控制器，该控制器利用短时的接触一致性展开（rollout）来生成满足内部非穿透约束的末端运动学参考，随后仅计算朝向该参考的平滑最小加速度过渡的第一个输入。从闭环逆运动学调节律出发，该展开过程通过在膨胀的凸机器人和障碍物几何体上运行的迭代动力学求解器执行，从而使机器人与障碍物的接触、动态障碍物运动以及自碰撞能够塑造末端参考，而无需进行完整的受约束轨迹优化。我们分析了接触非激活状态下的闭环系统，并在标准正则性假设下证明了局部指数级任务空间调节。对于在展开过程中激活的接触，我们刻画了相应的离散更新，并界定了移动障碍物对正则操作集的影响。在 40 自由度多链系统上的仿真表明，中间长度的时域能够平衡预见性、响应能力和计算成本。在 6 自由度平台上的硬件实验证明了在无需精确惯性参数估计的情况下，系统具有一致的虚实迁移（sim-to-real）表现；与动态优化结构（dynamic optimization fabrics）和模型预测控制（MPC）基准的对比显示，该方法在动态杂乱环境中的成功率更高，同时在测试场景下保持了与实时执行兼容的求解时间。

## Abstract
Real-time collision avoidance for robotic manipulators requires fast reactions to unexpected obstacle motion and lookahead to avoid becoming trapped by near-future constraints. Full model predictive control can provide this foresight, but its online cost may grow quickly with horizon length, model fidelity, and the number of active geometric constraints. Conversely, horizon-free reactive methods are computationally efficient but can be short-sighted in dynamic clutter. We present a task-space receding-horizon controller that uses a short contact-consistent rollout to generate a terminal kinematic reference satisfying internal non-penetration constraints, then computes only the first input of a smooth minimum-acceleration transition toward that reference. Starting from a closed-loop inverse-kinematics regulation law, the rollout is performed with an iterative dynamics solver operating on inflated convex robot and obstacle geometries, so that robot-obstacle contacts, dynamic obstacle motion, and self-collisions can shape the terminal reference without requiring full constrained trajectory optimization. We analyze the contact-inactive closed loop and show local exponential task-space regulation under standard regularity assumptions. For contacts activated inside the rollout, we characterize the corresponding discrete updates and bound the effect of moving obstacles on regular operating sets. Simulations on a 40-DOF multi-chain system show that intermediate horizons balance anticipation, responsiveness, and computational cost. Hardware experiments on a 6-DOF platform demonstrate consistent sim-to-real behavior without accurate inertial parameter estimation, and comparisons against dynamic optimization fabrics and model predictive control (MPC) baselines show improved success rates in dynamic clutter while preserving solve times compatible with real-time execution in the tested regimes.

---

## 论文详细总结（自动生成）

以下是对论文《A Task-Space Receding Horizon Controller for Fast Collision Avoidance》（一种用于快速避障的任务空间后退水平控制器）的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究背景**：机器人操作臂在动态、杂乱的环境中执行任务时，必须实时避开障碍物。
*   **核心痛点**：
    *   **全模型预测控制 (MPC)**：虽然具备前瞻性（Lookahead），能预见未来的碰撞，但随着预测时界增加、模型复杂度提升及约束增多，计算开销呈指数级增长，难以满足高频实时性要求。
    *   **反应式方法 (Reactive Methods)**：计算效率极高，但由于缺乏时界预测，容易在动态环境中陷入局部最优（如死胡同或振荡），表现出“短视”行为。
*   **研究目标**：提出一种兼具反应式方法的计算效率和 MPC 前瞻性的新型控制器，实现在复杂动态环境下的高效、稳健避障。

### 2. 方法论
*   **核心思想**：采用“任务空间展开（Rollout）+ 最小加速度过渡”的策略。不进行全轨迹优化，而是通过短时界的物理模拟生成一个“安全参考点”。
*   **关键技术细节**：
    1.  **接触一致性展开 (Contact-Consistent Rollout)**：利用迭代动力学求解器，在膨胀的凸几何体（机器人连杆和障碍物）上进行短时间的外推。
    2.  **非穿透约束处理**：在展开过程中，机器人与障碍物、自碰撞以及关节极限被视为硬约束。通过模拟这些“接触”，末端执行器的参考位置会被障碍物自然地“推开”或“塑造”。
    3.  **参考生成与控制**：展开过程产生一个满足约束的末端运动学参考。控制器随后仅计算通往该参考的第一个控制输入，采用平滑的最小加速度过渡律。
    4.  **稳定性分析**：论文在数学上证明了在接触非激活状态下，系统具有局部指数级的任务空间调节能力，并刻画了接触激活时的离散状态更新。

### 3. 实验设计
*   **实验场景**：
    1.  **高维仿真**：一个具有 40 自由度 (DOF) 的复杂多链机器人系统，用于测试算法在极高维度下的计算扩展性。
    2.  **硬件实验**：使用 6 自由度机械臂在真实物理环境中进行动态避障测试。
*   **Benchmark（基准方法）**：
    *   **Dynamic Optimization Fabrics (DOF)**：一种先进的反应式避障框架。
    *   **标准 MPC**：作为预测类方法的基准。
*   **对比维度**：成功率、求解时间（实时性）、对动态障碍物的响应能力、时界长度对性能的影响。

### 4. 资源与算力
*   **算力说明**：文中未详细列出具体的 GPU 型号或训练时长。
*   **实时性强调**：作者强调该算法在测试场景下保持了与实时执行兼容的求解时间（Compatible with real-time execution），即使在 40-DOF 的复杂系统中也能高效运行，这暗示了其对算力要求较低，可在常规嵌入式或工业 PC 上运行。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了多组仿真实验，通过改变时界长度（Horizon length）来分析预见性与计算成本的权衡。
    *   硬件实验中包含了动态障碍物干扰和杂乱环境测试。
*   **充分性评价**：实验设计较为充分。通过 40-DOF 的仿真证明了算法的**可扩展性**，通过 6-DOF 硬件实验证明了**实用性**和**虚实迁移（Sim-to-Real）**的稳健性。对比了主流的反应式和预测式方法，结果具有说服力。

### 6. 主要结论与发现
*   **平衡点**：中间长度的时界（Intermediate horizons）是性能的最优解，既能提供足够的预见性避免陷入局部最优，又不会引入过高的计算负担。
*   **性能优越**：在动态杂乱环境中，该方法的避障成功率显著高于传统的 MPC 和反应式方法（如 DOF）。
*   **模型鲁棒性**：该方法对精确的惯性参数估计依赖较低，表现出良好的硬件适应性。

### 7. 优点
*   **高效性**：通过将复杂的轨迹优化简化为物理一致的“展开”过程，极大地降低了计算复杂度。
*   **前瞻性**：解决了传统反应式方法容易被动态障碍物“困住”的问题。
*   **高维支持**：能够处理极高自由度的机器人系统，这在传统 MPC 中是非常困难的。

### 8. 不足与局限
*   **局部性限制**：虽然引入了时界，但本质上仍属于局部规划范畴，在极度复杂的迷宫式环境中可能仍需全局规划器的配合。
*   **几何简化**：依赖于将机器人和障碍物简化为凸几何体组合，对于形状极度复杂的非凸物体，可能存在近似误差。
*   **理论边界**：虽然证明了局部稳定性，但在多点频繁接触的极端非线性情况下的全局收敛性仍有待进一步探讨。

（完）
