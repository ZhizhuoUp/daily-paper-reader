---
title: Physics-enhanced reinforcement learning for real-time optimal control of dynamical systems
title_zh: 物理增强强化学习用于动力系统的实时最优控制
authors: "Matteo Tomasetto, Nicolò Botteghi, Gabriele Bruni, Andrea Manzoni"
date: 2026-07-17
pdf: "https://arxiv.org/pdf/2607.16177v1"
tags: ["query:q2"]
score: 8.0
evidence: 动力系统的实时最优控制
tldr: 针对强化学习在处理高维动力系统时采样效率低和维度灾难的问题，本文提出了一种物理增强强化学习（PEARL）范式。该方法将传统最优控制与RL结合，利用动力系统的可微性，通过actor-adjoint算法和自动微分计算策略梯度，显著提升了采样效率和高维空间的扩展性，在非定常流场导航任务中表现优异。
selection_source: fresh_fetch
motivation: 传统强化学习在处理高维、参数化动力系统时存在采样效率低下和难以应对维度灾难的局限性。
method: 提出PEARL框架，利用自动微分和伴随敏感性分析在短时域内计算策略梯度，并结合神经网络近似未来收益。
result: 在非定常流场导航实验中，PEARL在采样效率、泛化能力和处理高维状态动作空间方面均显著优于现有先进RL算法。
conclusion: 物理增强的强化学习范式成功弥合了传统控制理论与现代RL的鸿沟，为复杂动力系统的实时最优控制提供了高效且可扩展的方案。
---

## 摘要
强化学习 (RL) 近年来已成为非线性和复杂动力系统的一种极具前景的反馈控制策略。然而，RL 算法的样本效率较低，需要与环境进行大量交互才能合成最优控制策略。因此，由于高维空间中探索-利用困境带来的维度灾难，RL 的应用通常局限于稀疏的传感器和执行器。在这项工作中，我们通过一种专为高维和参数化动力系统控制量身定制的新型物理增强强化学习 (PEARL) 范式，将 RL 与传统的动力系统最优控制结合起来，并利用了其动力学的可微性。具体而言，PEARL 采用了一种 Actor-Adjoint 算法，该算法利用自动微分计算短时域内的策略梯度，并利用神经网络近似未来回报的基于伴随的敏感度，在缓解长期梯度不稳定性的同时，显著减少了环境交互次数。通过非定常流中两个具有挑战性的参数化导航问题，我们证明了 PEARL：(i) 有效利用可微环境，性能优于最先进的 RL 算法；(ii) 得益于物理引导的策略学习，具有极高的样本效率；(iii) 能够跨多种场景泛化，这在处理参数化系统时至关重要；(iv) 能够将 RL 扩展到高维状态和动作空间，而无需低维状态表示或多智能体策略。

## Abstract
Reinforcement learning (RL) has recently emerged as a promising feedback control strategy for nonlinear and complex dynamical systems. However, RL algorithms are sample inefficient and require a large number of interaction with the environment to synthesize optimal control strategies. Consequently, applications of RL are typically limited to sparse sensors and actuators due to the curse of dimensionality entailed by the exploration-exploitation dilemma in high-dimensional spaces. In this work, we bridge RL and traditional optimal control for dynamical system with a novel Physics-EnhAnced Reinforcement Learning (PEARL) paradigm tailored to the control of high-dimensional and parametric dynamical systems, exploiting the differentibility of their dynamics. Specifically, PEARL employs an actor-adjoint algorithm that leverages automatic differentiation to compute policy gradients over short horizons and adjoint-based sensitivities of future returns approximated via neural networks, significantly reducing the number of environment interactions, while mitigating long-term gradient instabilities. Through two challenging parametric navigation problems in unsteady flows, we show that PEARL (i) effectively exploits differentiable environments to outperform state-of-the-art RL algorithms, (ii) is sample efficient, thanks to the physics-guided policy learning, (iii) generalizes across multiple scenarios, which is crucial when dealing with parametric systems, and (iv) enables scaling RL to high-dimensional state and action spaces, without requiring low-dimensional state representations or multi-agent strategies.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **PEARL（Physics-EnhAnced Reinforcement Learning，物理增强强化学习）** 的新框架，旨在解决高维动力系统在实时最优控制中面临的挑战。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：传统的深度强化学习（DRL）在处理高维、连续动作空间（如流体力学控制）时，存在**样本效率极低**和**维度灾难**的问题。而传统的最优控制（OC）理论虽然利用了物理规律，但通常是开环的，且在处理复杂参数化系统时计算成本极高，难以实现实时反馈。
*   **核心问题**：如何结合强化学习的闭环自适应能力与最优控制的物理一致性梯度信息，实现对高维、参数化动力系统的高效实时控制？

### 2. 方法论：核心思想与关键技术
PEARL 框架的核心是 **Actor-Adjoint（行为者-伴随）算法**，其关键技术细节包括：
*   **可微环境利用**：假设环境动力学是可微的（如由 ODE/PDE 描述），利用自动微分（AD）直接计算策略梯度，而非像传统 RL 那样通过随机采样估计梯度。
*   **短时域优化（Short-Horizon Optimization）**：为了避免长程反向传播（BPTT）导致的梯度消失或爆炸，PEARL 将长任务分解为多个短时域（h 步）。
*   **伴随网络（Adjoint Network）**：这是 PEARL 的核心创新。不同于现有方法（如 SHAC）预测“状态价值（Value）”，PEARL 使用神经网络直接预测**伴随变量（即价值函数相对于状态的梯度）**。
*   **物理增强的 TD-λ 方案**：利用伴随方程（Adjoint Equation）生成目标数据来训练伴随网络，从而捕捉长期的物理敏感性，确保短时域优化不会陷入近视（Myopic）策略。

### 3. 实验设计
论文通过两个具有挑战性的非定常流场导航问题进行验证：
*   **场景 A：领航者-跟随者游戏（Leader-Follower Game）**
    *   任务：在双涡流（Double Gyre）中控制一个粒子（跟随者）追踪被动运输的领航者。
    *   特点：参数化初始位置，考察策略的泛化能力。
*   **场景 B：平均场领航者-跟随者游戏（Mean-Field Game）**
    *   任务：控制高维密度分布（由平流扩散方程描述）去追踪目标。
    *   维度：状态空间维度 $N_y = 2145$，动作空间维度 $N_u = 4290$，属于极高维控制任务。
*   **Benchmark 与对比方法**：
    *   无模型 RL：PPO、TD3。
    *   基于梯度的 RL：BPTT（全时域）、截断 BPTT、SHAC（短时域行为者-评论家）。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：提到使用了双分支神经网络架构（分别嵌入状态和场景参数），神经元数量根据任务复杂度在 64 到 1024 之间。代码实现基于可微物理模拟环境。

### 5. 实验数量与充分性
*   **实验规模**：针对两个主要场景分别进行了密集奖励（Dense Reward）和稀疏奖励（Sparse Reward）的测试。
*   **充分性评价**：实验设计较为充分。通过对比无模型 RL 和多种基于梯度的变体，证明了 PEARL 在不同奖励机制下的鲁棒性。特别是平均场实验，展示了该方法处理数千维动作空间的能力，这是传统 RL 难以企及的。
*   **客观性**：使用了相同的网络架构和超参数进行公平对比，并提供了评估奖励的均值和方差。

### 6. 主要结论与发现
*   **性能卓越**：PEARL 在所有测试任务中均优于 PPO、TD3 和 SHAC，尤其在稀疏奖励环境下表现出极强的学习能力。
*   **样本效率高**：得益于物理引导的精确梯度，PEARL 达到最优性能所需的训练迭代次数远少于无模型方法。
*   **高维扩展性**：成功解决了 4000+ 维度的分布式控制问题，且无需进行降维处理或使用多智能体分解。
*   **泛化能力**：单一策略能够适应不同的初始条件和流场参数，实现了真正的参数化闭环控制。

### 7. 优点（亮点）
*   **理论融合**：成功弥合了强化学习（Bellman 原理）与最优控制（伴随方法）之间的鸿沟。
*   **梯度质量**：通过伴随网络直接学习“价值梯度”，比学习“价值函数”再求导更精确，减少了函数近似带来的误差。
*   **实时性**：训练完成后，策略网络可实现毫秒级的实时推理，满足动态系统控制需求。

### 8. 不足与局限
*   **环境依赖**：PEARL 严格依赖于**可微的环境模型**。如果系统动力学完全未知或不可微，该方法无法直接应用。
*   **短时域偏差**：虽然伴随网络缓解了偏差，但短时域长度 $h$ 的选择仍可能影响复杂长程任务的收敛性。
*   **计算开销**：虽然样本效率高，但单次迭代中计算伴随方程和自动微分的计算强度高于简单的无模型更新。
*   **现实迁移**：目前仅在数值模拟（双涡流）中验证，尚未在具有传感器噪声和时滞的真实物理实验中测试。

（完）
