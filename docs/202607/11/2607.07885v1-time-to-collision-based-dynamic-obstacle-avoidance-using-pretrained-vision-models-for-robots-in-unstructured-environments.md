---
title: Time-to-Collision Based Dynamic Obstacle Avoidance Using Pretrained Vision Models for Robots in Unstructured Environments
title_zh: 非结构化环境下基于预训练视觉模型和碰撞时间的机器人动态避障
authors: "Erik Jagnandan, Mulugeta Haile, Gregory Barber, Pratik Chaudhari"
date: 2026-07-08
pdf: "https://arxiv.org/pdf/2607.07885v1"
tags: ["query:q1"]
score: 9.0
evidence: 非结构化环境下机器人的动态避障
tldr: 针对非结构化户外环境中移动机器人的动态避障难题，本文提出一种基于预训练视觉模型的数据高效方法。该方法利用UniDepth进行单目深度估计，结合SuperPoint和SuperGlue进行长序列特征跟踪，通过束调整计算关键点的碰撞时间（TTC），并据此选择避障动作。该方案无需大规模机器人训练数据，在真实世界数据集上表现出良好的检测率和避障准确性。
selection_source: fresh_fetch
motivation: 解决非结构化户外环境下动态避障中，大规模机器人特定训练数据匮乏以及仿真到现实迁移困难的问题。
method: 利用预训练单目深度模型UniDepth和特征匹配算法跟踪关键点，通过束调整计算碰撞时间（TTC）并选择最优避障运动原语。
result: "在M3ED数据集上，该系统成功检测到绝大多数物理障碍物，并在84%的真阳性检测中生成了正确的避障方向。"
conclusion: 该方法无需模型训练，仅需极少量数据进行参数微调，证明了在复杂环境下实现可解释且泛化性强的避障行为的高效性。
---

## 摘要
在非结构化户外环境中，动态避障仍然是自主移动机器人面临的关键挑战，特别是当大规模机器人特定训练数据和基于模拟的策略不切实际时。我们提出了一种数据高效且可解释的基于视觉的动态避障方法，该方法完全在真实世界数据上运行，避免了模拟训练策略中固有的“模拟到现实”（sim-to-real）迁移问题。我们的方法利用大型预训练单目深度估计模型 UniDepth，从 RGB 视频中生成稠密深度图，在推理时无需双目相机或激光雷达（LiDAR）。动态避障是通过扩展 SuperPoint 和 SuperGlue 特征对应流水线来实现的，以在长帧序列中跟踪关键点，利用相机内参和预测深度将其 2D 像素空间位置投影到 3D 空间，运行由这些 3D 关键点初始化的光束法平差（bundle adjustment），并计算每个关键点的碰撞时间（TTC）。然后在地面平面上选择一个 2D 运动原语，使机器人远离最小 TTC 关键点的最近相遇点（closest point of approach）。在 M3ED 数据集的真实世界数据评估中，我们的流水线在识别地面真值 TTC 低于 1 秒的帧时，达到了 0.49 的精确率和 0.38 的召回率，并在 84% 的真阳性检测中正确生成了规避运动方向。至关重要的是，在测试序列中存在的 22 个独特物理障碍物中，它为其中的 20 个检测到了至少一帧 TTC 小于 1 秒的情况。与需要数千小时机器人特定训练数据的端到端学习方法不同，我们的方法完全消除了模型训练，仅需 74 秒的数据进行超参数调优。这展示了卓越的数据效率，同时在不同障碍物类型中保持了可解释且可泛化的行为。

## Abstract
Dynamic obstacle avoidance in unstructured outdoor environments remains a critical challenge for autonomous mobile robots, particularly when large-scale robot-specific training data and simulation-based policies are impractical. We present a data-efficient, interpretable method for vision-based dynamic obstacle avoidance that operates entirely on real-world data, avoiding the sim-to-real transfer problem inherent in simulation-trained policies. Our approach leverages UniDepth, a large pretrained monocular depth estimation model, to produce dense depth maps from RGB video without requiring stereo cameras or LiDAR at inference time. Dynamic obstacle avoidance is achieved by extending the SuperPoint and SuperGlue feature correspondence pipeline to track keypoints across long frame sequences, projecting their 2D pixel-space positions into 3D using camera intrinsics and predicted depth, running bundle adjustment initialized from these 3D keypoints, and computing per-keypoint time-to-collision (TTC). A 2D motion primitive in the ground plane is then selected to move the robot away from the closest point of approach of the minimum-TTC keypoint. Evaluated on real-world data from the M3ED dataset, our pipeline achieves a precision of 0.49 and a recall of 0.38 in identifying frames with a ground truth TTC below 1 second, and correctly generates the evasive motion direction in 84\% of true positive detections. Crucially, it detects at least one frame with TTC less than 1 second for 20 out of 22 unique physical obstacles present in our test sequences. Unlike end-to-end learned methods that demand thousands of hours of robot-specific training data, our approach eliminates model training entirely, requiring only 74 seconds of data for hyperparameter tuning. This demonstrates exceptional data efficiency while preserving interpretable and generalizable behavior across diverse obstacle types.

---

## 论文详细总结（自动生成）

以下是对论文《Time-to-Collision Based Dynamic Obstacle Avoidance Using Pretrained Vision Models for Robots in Unstructured Environments》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：在非结构化户外环境中，移动机器人的动态避障面临两大难题：一是端到端学习方法（如 Transformer 策略）需要海量的机器人特定训练数据；二是基于强化学习（RL）的仿真训练存在严重的“仿真到现实”（sim-to-real）迁移鸿沟，且模型缺乏可解释性。
*   **核心目标**：开发一种无需大规模训练、完全基于真实世界数据、且具有几何可解释性的视觉避障系统，利用现有的视觉基础模型（Vision Foundation Models）实现高效避障。

### 2. 核心方法论
该方法构建了一个从感知到动作的几何流水线，核心步骤如下：
*   **单目深度估计**：使用预训练的 **UniDepth** 模型从单帧 RGB 图像中提取具有度量尺度（Metric Scale）的稠密深度图。
*   **特征跟踪**：利用 **SuperPoint** 提取关键点，并通过 **SuperGlue** 在连续帧间建立特征对应关系，将其扩展为长序列跟踪。
*   **3D 投影与优化**：利用相机内参将 2D 像素坐标投影至 3D 空间。使用 **XM Solver**（一种基于半正定规划松弛的优化器）对最近 5 帧的关键点轨迹进行缩放束调整（Scaled Bundle Adjustment），以平滑噪声并提高 3D 轨迹精度。
*   **碰撞时间（TTC）计算**：
    *   通过线性回归估计关键点的 3D 速度。
    *   计算每个关键点的 TTC = 深度 / 相对速度（仅考虑向相机靠近的点）。
    *   计算最近相遇点（CPA），排除 CPA > 3m 的无威胁点。
*   **运动原语生成**：选择 TTC 最小的关键点，在地面平面上生成一个背离该点 CPA 方向的 2D 避障向量。

### 3. 实验设计
*   **数据集与场景**：使用 **M3ED 数据集**，涵盖森林（spot-forest）和城市（spot-outdoor-day）等复杂户外环境，数据采集自 Boston Dynamics Spot 四足机器人。
*   **基准测试（Benchmark）**：以同步采集的 **LiDAR 数据** 计算出的地面真值（Ground Truth）TTC 作为评估标准。
*   **对比与评估指标**：
    *   评估系统识别“TTC < 1秒”危险帧的精确率（Precision）和召回率（Recall）。
    *   评估在检测成功时，生成的规避动作方向是否与真值一致（向量点积为正）。

### 4. 资源与算力
*   **训练成本**：论文明确指出**无需任何模型训练**。
*   **微调/调优**：仅使用了 74 秒的数据（spot-forest-easy-1 序列）进行超参数调优（如 TTC 阈值、跟踪窗口长度等）。
*   **算力说明**：文中未详细列出具体的 GPU 型号或推理延迟，但提到 XM Solver 使用了 CUDA 加速。由于依赖大型 Transformer 模型（UniDepth），实时部署可能需要高性能边缘计算设备。

### 5. 实验数量与充分性
*   **实验规模**：在 12 个不同的真实世界序列上进行了评估（4 个森林序列，8 个城市序列），涉及 22 个独特的物理障碍物。
*   **充分性**：实验覆盖了多种光照和地形，具有较好的环境多样性。
*   **公平性**：通过与 LiDAR 真值直接对比，评估过程客观。但未与其他 SOTA 避障算法（如基于光流的方法）进行直接的量化对比实验。

### 6. 主要结论与发现
*   **检测能力**：在帧级别上，精确率为 0.49，召回率为 0.38。虽然召回率较低，但在**障碍物级别**上，系统成功检测到了 22 个障碍物中的 20 个（至少检测到一帧危险）。
*   **动作准确性**：在所有真阳性（True Positive）检测中，**84%** 的规避动作方向是正确的。
*   **数据效率**：证明了利用预训练视觉模型可以在极少数据（几十秒）的情况下实现复杂的机器人控制任务。

### 7. 优点
*   **极高的数据效率**：无需数千小时的机器人数据，避开了昂贵的采集过程。
*   **无仿真鸿沟**：直接在真实数据上运行，避免了仿真环境物理引擎不真实的问题。
*   **可解释性强**：基于几何结构和 TTC 物理定义，比黑盒神经网络策略更易于调试和验证安全性。
*   **传感器简化**：仅需单目 RGB 相机即可实现通常需要 LiDAR 或双目相机才能完成的度量级避障。

### 8. 不足与局限
*   **帧级召回率较低**：由于 SuperPoint/SuperGlue 在长序列跟踪中可能丢失目标，或 UniDepth 深度估计的不连续性导致轨迹重置，造成了较多漏检。
*   **依赖深度精度**：单目深度估计在低纹理或极端光照下仍存在误差，这些误差会直接传播到 TTC 计算中，导致误报。
*   **实时性挑战**：UniDepth 和 XM Solver 的计算开销较大，论文未给出在嵌入式平台上的实时运行表现。
*   **运动假设限制**：TTC 计算假设相对速度恒定，在障碍物做剧烈变速运动时可能失效。

（完）
