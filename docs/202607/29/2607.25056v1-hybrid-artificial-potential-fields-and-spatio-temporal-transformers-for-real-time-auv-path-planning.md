---
title: Hybrid Artificial Potential Fields and Spatio-Temporal Transformers for Real-Time AUV Path Planning
title_zh: 用于实时 AUV 路径规划的混合人工势场与时空 Transformer
authors: "Khadija Rais, Abdelmadjid Benmachiche, Imene Soualmia"
date: 2026-07-27
pdf: "https://arxiv.org/pdf/2607.25056v1"
tags: ["query:q1"]
score: 8.0
evidence: 使用混合人工势场法的实时避障路径规划
tldr: 针对自主水下航行器（AUV）在复杂环境下的路径规划挑战，本文对比了13种算法，并提出一种结合人工势场法（APF）与时空Transformer（ST-Transformer）的混合模型。该模型在保持高任务完成率的同时，显著优化了路径长度、碰撞率和计算效率，为资源受限的水下实时导航提供了平衡全局最优与局部避障的稳健方案。
selection_source: fresh_fetch
motivation: AUV在复杂水下环境中需要兼顾路径最优性、避障安全性和实时计算效率，而传统算法难以在这些指标间取得平衡。
method: 提出一种将人工势场法（APF）的反应式避障能力与时空Transformer（ST-Transformer）的全局规划能力相结合的混合架构。
result: 混合模型在多场景测试中实现了最短平均路径、极低碰撞率及高效计算时间，性能全面优于传统算法和单一学习模型。
conclusion: 混合APF+ST框架是实现AUV实时导航的理想方案，能有效协调资源受限系统中的动态避障与全局路径优化。
---

## 摘要
自主水下航行器 (AUV) 在复杂、非结构化的环境中运行，高效且安全的路径规划对于任务成功和节能至关重要。本文对 13 种路径规划算法进行了全面的对比评估，涵盖了从经典的图搜索方法（A*、Dijkstra）和基于采样的算法（RRT*）到元启发式算法（PSO、GA、ACO、BCO）以及基于学习的架构。重点介绍了一种将人工势场 (APF) 与时空 (ST) Transformer 相结合的混合方法。在基于高分辨率水下地形图的五个导航场景中进行的评估显示，所有算法均实现了 100% 的任务完成率；然而，在路径最优性、避障能力和计算负载方面出现了显著的权衡。混合 APF + ST-Transformer 表现出卓越的平衡性能，实现了最短的平均路径长度（943.15 单位）、较低的碰撞率（0.031）和高效的计算时间（0.96 秒），优于需要回退机制的独立学习模型以及产生更高延迟的经典方法。虽然经典算法保证了无碰撞路径，但其过长的路径长度和处理时间使其不太适合动态水下作业。相反，元启发式方法引入了不适合严格能量限制的轨迹复杂性。基于这些发现，推荐将混合 APF + ST 框架作为实时 AUV 导航的主要方法，为资源受限的水下系统提供了一种协调反应式避障与全局路径最优性的鲁棒解决方案。

## Abstract
Autonomous Underwater Vehicles (AUVs) operate in complex, unstructured environments where efficient and safe path planning is critical for mission success and energy conservation. This paper presents a comprehensive comparative evaluation of thirteen path planning algorithms, ranging from classical graph-search methods (A*, Dijkstra) and sampling-based approaches (RRT*) to metaheuristics (PSO, GA, ACO, BCO) and learning-based architectures. Special emphasis is placed on a proposed hybrid approach combining Artificial Potential Fields (APF) with a Spatio-Temporal (ST) Transformer. Evaluated across five navigation scenarios on high-resolution underwater terrain maps, all algorithms achieved 100\% task completion; however, significant trade-offs emerged in path optimality, collision avoidance, and computational load. The Hybrid APF + ST-Transformer demonstrated superior balanced performance, achieving the shortest average path length (943.15 units), a low collision rate (0.031), and efficient computation time (0.96 s), outperforming standalone learning models, which required fallback mechanisms and classical methods that incurred higher latency. While classical algorithms guaranteed collision-free paths, their excessive path lengths and processing times render them less suitable for dynamic underwater operations. Conversely, metaheuristic approaches introduced trajectory complexity unsuitable for strict energy constraints. Based on these findings, the Hybrid APF + ST framework is recommended as a principal approach for real-time AUV navigation, offering a robust solution that harmonizes reactive obstacle avoidance with global path optimality in resource-constrained underwater systems.

---

## 论文详细总结（自动生成）

这篇论文提出了一种结合人工势场法（APF）与时空 Transformer（ST-Transformer）的混合路径规划框架，旨在解决自主水下航行器（AUV）在复杂水下环境中的实时导航问题。以下是对该论文的深度总结：

### 1. 核心问题与整体含义
*   **研究背景**：AUV 运行在非结构化、高风险且能源受限的水下环境中。现有的路径规划算法在全局最优性、实时响应速度和避障安全性之间难以兼得。
*   **核心问题**：经典算法（如 A*）计算开销大且路径冗长；元启发式算法（如 PSO、GA）轨迹过于复杂且耗能；纯学习模型在未见场景下缺乏安全性保障。
*   **研究动机**：开发一种既能利用物理模型（APF）实现快速反应避障，又能利用深度学习（Transformer）捕捉空间依赖并优化全局路径的混合架构。

### 2. 方法论
*   **核心思想**：将路径规划视为受物理力引导和数据驱动预测共同作用的过程。
*   **关键技术细节**：
    *   **人工势场（APF）组件**：作为底层反应式控制。通过引力函数将 AUV 拉向目标，通过地形梯度计算斥力函数使 AUV 远离高海拔（障碍物）区域。引入动量机制平滑运动更新。
    *   **时空 Transformer 组件**：作为高层预测引导。利用自注意力机制处理历史轨迹序列和上下文信息（起点/终点），预测下一个最优航路点。
    *   **混合控制策略**：通过加权公式融合 APF 产生的力、Transformer 预测的方向以及直接指向目标的方向。权重分配遵循“安全第一”原则（APF 权重最高）。
    *   **自适应机制**：针对长距离任务引入自适应引力缩放，并设计了停滞检测机制，通过随机扰动帮助 AUV 跳出局部最优陷阱。

### 3. 实验设计
*   **实验场景**：在 992×992 高分辨率水下地形图上设计了 5 种典型导航场景（对角线、交叉、中等距离、反向、长距离）。
*   **对比方法（Benchmark）**：共对比了 13 种算法，分为四类：
    *   **经典图搜索**：A*、Dijkstra、Theta*。
    *   **采样算法**：RRT*。
    *   **元启发式算法**：遗传算法（GA）、粒子群算法（PSO）、蚁群算法（ACO）、蜜蜂采蜜算法（BCO）。
    *   **学习与混合类**：独立 APF、独立 ST-Transformer、APF+A* 混合、Theta*+APF 混合以及本文提出的混合模型。
*   **评估指标**：平均路径长度、路径平滑度、平均碰撞率、计算时间。

### 4. 资源与算力
*   **算力说明**：论文中**未明确指出**具体的训练硬件（如 GPU 型号、数量）或训练时长。
*   **推理效率**：论文重点强调了推理阶段的实时性，指出混合模型在 AUV 板载计算环境下的平均计算时间为 0.96 秒。

### 5. 实验数量与充分性
*   **实验规模**：针对 13 种算法在 5 个不同复杂度的场景下进行了详尽测试，所有算法均实现了 100% 的任务成功率。
*   **充分性评价**：实验设计较为充分，涵盖了从传统到前沿的主流算法，对比维度全面。通过多场景测试验证了算法的鲁棒性，但实验主要集中在 2D 仿真层面。

### 6. 主要结论与发现
*   **混合模型优势**：混合 APF + ST-Transformer 在综合性能上表现最佳，实现了最短的平均路径（943.15 单位）和极低的碰撞率（0.031），且计算效率满足实时性要求。
*   **经典算法局限**：A* 和 Dijkstra 虽然能保证 0 碰撞，但路径长度最长且计算延迟高（超过 7 秒），不适合动态环境。
*   **元启发式表现**：虽然能找到较短路径，但产生的轨迹波动剧烈（平滑度数值极高），会增加 AUV 的机械损耗和能耗。
*   **学习模型风险**：未经训练或独立的 Transformer 模型虽然速度极快，但在复杂地形下碰撞率较高，必须依赖回退机制。

### 7. 优点
*   **架构创新**：成功将反应式避障（局部）与预测性规划（全局）结合，弥补了单一方法的短板。
*   **性能平衡**：在保证安全性的前提下，显著优化了路径长度，这对能源受限的 AUV 至关重要。
*   **对比详尽**：提供了大规模的算法基准测试，为 AUV 路径规划领域的算法选择提供了重要参考。

### 8. 不足与局限
*   **维度限制**：实验基于 2D 网格地图，忽略了真实水下环境中的 3D 动力学、水流干扰和压力变化。
*   **传感器噪声**：未充分考虑声呐等传感器在实际应用中的不确定性和噪声影响。
*   **应用限制**：目前仍处于仿真阶段，缺乏在真实 AUV 硬件平台或高保真物理引擎中的验证。

（完）
