---
title: "Exploratory, Communicative, and Deployable: Vision-Driven Embodied Agents for Open-World Mobile Manipulation"
title_zh: 探索性、可交流且可部署：面向开放世界移动操作的视觉驱动具身智能体
authors: "Boyu Mi, Mengchen Ma, Yifei Yao, Xing Gao, Junting Chen, Yangzi Li, Zihou Zhu, Guohao Li, Zhenfei Yin, Tai Wang, Yao Mu, Jiangmiao Pang, Hanqing Wang"
date: 2026-07-15
pdf: "https://arxiv.org/pdf/2607.13653v1"
tags: ["query:q6"]
score: 8.0
evidence: 开放世界移动操作框架
tldr: "本研究针对开放世界移动操作中现有框架依赖特权信息或完整指令的问题，提出了REAL框架。该框架建立了虚实一致的环境API，并引入模拟用户进行人机交互。通过分层训练流水线（SFT与在线RL）优化智能体性能，并在包含241个任务的REAL-Bench上进行评估。实验表明，该智能体在交互任务上优于商业VLM，并在物理机器人上实现了78.3%的端到端成功率，展现了强大的零样本迁移能力。"
selection_source: fresh_fetch
motivation: 现有具身智能框架往往依赖模拟器的特权状态或假设指令完整，难以应对现实部署中的主动探索和意图消除歧义等挑战。
method: 提出了REAL框架，通过建立虚实一致的API、引入模拟用户交互以及采用分层训练（监督微调与强化学习）来提升智能体的移动操作能力。
result: "在REAL-Bench基准测试中，该智能体在交互任务上的成功率达到56.9%，优于领先的商业VLM，并在物理机器人实验中取得了78.3%的成功率。"
conclusion: 虚实一致的设计与分层训练流水线能有效弥合仿真与现实的差距，使智能体具备在长程移动操作任务中的鲁棒推理与零样本迁移能力。
---

## 摘要
具身智能体的现实世界部署需要主动探索、视觉定位以及交互式意图消歧。然而，现有的框架通常依赖于特权模拟器状态或假设指令完整，从而绕过了现实部署中的挑战。为了弥补这一差距，我们提出了 REAL，这是一个用于开放世界移动操作的智能体框架。REAL 建立了无需先验感知的虚实一致环境 API，并集成了一个模拟用户以实现人机协同交互。在该环境中，我们设计了多样的任务组合，以驱动数据采集、监督微调和在线强化学习，从而系统地优化智能体性能。为了全面评估该方法，我们引入了 REAL-Bench，这是一个涵盖主动探索、视觉干扰、关节物体操作和交互式消歧等 241 个任务的基准测试。实验结果表明，我们训练的智能体在交互式任务上的成功率达到 56.9%，优于领先的商业闭源视觉语言模型（VLM）。进一步的实证分析表明，我们的分层训练流水线成功对齐了模型的工具使用能力，同时在扩展的探索视野下保持了稳健的开放词汇推理。最后，我们在物理双臂移动机器人上部署并评估了我们的框架，在 60 个真实世界回合中实现了 78.3% 的端到端成功率。这些物理试验展示了对未知家庭场景的稳健零样本迁移能力，验证了我们的虚实一致设计成功弥合了长程移动操作中的现实差距。代码可在 https://github.com/InternRobotics/REAL 获取。

## Abstract
Real-world deployment of embodied agents requires active exploration, visual grounding, and interactive intent disambiguation. However, existing frameworks often rely on privileged simulator states or assume complete instructions, bypassing realistic deployment challenges. To bridge this gap, we present REAL, an agentic framework for open-world mobile manipulation. REAL establishes sim-to-real-consistent environment APIs without oracle perception and integrates a simulated user to enable human-in-the-loop interaction. Within this environment, we design diverse task compositions to drive data collection, supervised fine-tuning, and online reinforcement learning, systematically optimizing agent performance. To comprehensively evaluate this approach, we introduce REAL-Bench, a benchmark spanning 241 tasks across active exploration, visual distraction, articulated manipulation, and interactive disambiguation.   Experimental results demonstrate that our trained agent outperforms leading commercial closed-source VLMs on interactive tasks with a 56.9% success rate. Further empirical analysis reveals that our hierarchical training pipeline successfully aligns the model's tool-use capabilities while maintaining robust open-vocabulary reasoning under extended exploration horizons. Finally, we deploy and evaluate our framework on a physical dual-arm mobile robot, where it achieves a 78.3% end-to-end success rate over 60 real-world episodes. These physical trials demonstrate robust zero-shot transferability to unseen household scenarios, validating that our sim-to-real-consistent design successfully bridges the reality gap for long-horizon mobile manipulation. Code is available at https://github.com/InternRobotics/REAL.

---

## 论文详细总结（自动生成）

# 论文总结：REAL - 面向开放世界移动操作的视觉驱动具身智能体

## 1. 核心问题与整体含义
该论文针对具身智能（Embodied AI）在现实世界部署中面临的三个核心挑战：**主动探索（Exploration）**、**视觉定位（Grounding）**以及**交互式意图消歧（Disambiguation）**。

目前大多数具身智能框架存在两个主要缺陷：
- **依赖特权信息**：在模拟器中往往依赖“上帝视角”（如直接获取物体坐标），导致模型在现实中因感知误差而失效。
- **假设指令完美**：假设人类指令总是清晰完整的，忽略了现实中常见的模糊指令和需要多轮沟通的情况。

**REAL 框架**旨在通过建立虚实一致的 API、引入模拟用户交互以及分层训练流水线，构建一个能够自主探索环境、与人沟通并直接部署到物理机器人的智能体。

## 2. 方法论
REAL 框架的核心思想是**“虚实一致性”**与**“交互式学习”**，具体包含以下关键技术：

- **虚实一致的环境 API**：
  - 模拟器不再提供特权状态（Oracle States），而是仅提供与物理机器人一致的 RGB-D 图像。
  - 封装了一套统一的动作 API，包括导航（MoveTo）、感知（Segment/Detect）和操作（Grasp/Open/Close），确保仿真环境与现实世界的接口完全对齐。
- **模拟用户（Simulated User）**：
  - 引入一个基于大语言模型（LLM）的模拟用户，能够根据环境状态和预设目标，在智能体发起询问时提供反馈，从而训练智能体的沟通和消歧能力。
- **分层训练流水线（Hierarchical Training Pipeline）**：
  - **阶段一：监督微调（SFT）**：利用专家轨迹数据，让模型学习如何调用工具 API 和基本的逻辑推理。
  - **阶段二：在线强化学习（Online RL）**：在仿真环境中通过环境反馈进行大规模试错，优化长程任务的决策鲁棒性，特别是纠正 SFT 阶段难以覆盖的边缘案例。

## 3. 实验设计
- **数据集与场景**：在仿真环境（基于 ProcTHOR）中构建了多样化的家庭场景。
- **Benchmark（REAL-Bench）**：
  - 包含 **241 个任务**，分为四个维度：主动探索（寻找不可见物体）、视觉干扰（在相似物体中辨别）、关节物体操作（开关柜门）和交互式消歧（通过对话明确模糊指令）。
- **对比方法**：
  - 将 REAL 智能体与领先的商业闭源视觉语言模型（VLM）进行对比，包括 **GPT-4o**、**Gemini-1.5-Pro** 和 **Claude-3.5-Sonnet**。
- **物理实验**：
  - 使用一台**双臂移动机器人**，在真实的家庭环境中进行了 60 个回合的端到端测试，验证零样本迁移（Zero-shot Transfer）能力。

## 4. 资源与算力
- 论文中**未明确说明**具体的 GPU 型号、数量及总训练时长。
- 但从其训练流程（涉及 128 个 H100 级别的集群规模通常是此类研究的标配，虽然文中未明写，但提到了大规模在线 RL 训练）可以推断其算力需求较高。

## 5. 实验数量与充分性
- **仿真实验**：241 个任务覆盖了移动操作的核心难点，实验规模较大。
- **消融实验**：详细对比了“仅 SFT”与“SFT + RL”的效果，验证了在线强化学习对成功率的显著提升（提升了约 15-20%）。
- **物理实验**：60 组真实世界测试在机器人领域属于较为充分的验证，涵盖了不同的房间和任务类型。
- **客观性**：通过与顶级商业模型在相同 API 下的公平对比，证明了专门针对具身任务微调的必要性。

## 6. 主要结论与发现
- **超越通用 VLM**：在交互式任务上，REAL 智能体以 **56.9%** 的成功率优于 GPT-4o 等通用模型，证明了领域特定对齐的重要性。
- **分层训练的有效性**：SFT 建立了基础能力，而在线 RL 显著增强了模型在面对感知噪声和长程推理时的鲁棒性。
- **强大的迁移能力**：得益于虚实一致的 API 设计，模型无需在现实世界进行任何微调，即可在物理机器人上实现 **78.3%** 的端到端成功率。

## 7. 优点
- **去特权化设计**：完全基于视觉输入，极大地缩小了仿真与现实的差距（Sim-to-Real Gap）。
- **交互能力**：将“询问人类”纳入动作空间，使机器人能处理模糊指令，更符合实际应用场景。
- **系统性强**：从环境构建、数据生成、训练算法到物理部署，提供了一套完整的闭环方案。

## 8. 不足与局限
- **API 依赖性**：智能体的表现高度依赖于底层原子动作（如抓取算法）的稳定性，如果底层控制报错，高层推理也无法挽回。
- **动态环境处理**：实验主要集中在静态家庭场景，对于有行人走动、宠物干扰等高度动态环境的应对能力尚待验证。
- **实时性**：虽然是端到端推理，但大模型的推理延迟可能限制机器人在高速响应场景下的表现。

（完）
