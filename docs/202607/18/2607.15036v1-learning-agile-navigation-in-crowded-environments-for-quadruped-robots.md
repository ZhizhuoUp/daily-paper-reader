---
title: Learning Agile Navigation in Crowded Environments for Quadruped Robots
title_zh: 四足机器人在拥挤环境中的敏捷导航学习
authors: "Shuyu Wu, Zeyu Liu, Tianbao Zhang, Fanxing Li, Fangyu Sun, Mingkang Xiong, Wei Xi, Wenxian Yu, Danping Zou"
date: 2026-07-16
pdf: "https://arxiv.org/pdf/2607.15036v1"
tags: ["query:q1"]
score: 8.0
evidence: 敏捷导航与避障的几何安全性
tldr: 针对四足机器人在拥挤动态环境中的导航难题，本文提出VOP-Nav系统。该系统巧妙结合了速度障碍法（VO）的几何安全性与端到端学习的灵活性，通过VOP-Net处理多帧雷达数据，隐式编码动态约束并预测安全速度区域。VO预测既作为策略输入也作为训练奖励，使机器人在无需显式障碍物跟踪的情况下，实现了高效避障与敏捷运动，并在仿真和实机部署中验证了其优越性。
selection_source: fresh_fetch
motivation: 传统模型方法依赖精确跟踪而学习方法缺乏预测能力，导致四足机器人在拥挤环境中难以兼顾安全与效率。
method: 提出VOP-Nav系统，利用神经网络从多帧雷达数据中隐式预测速度障碍区域，并将其整合进强化学习框架中。
result: 在Isaac Gym仿真和Unitree Go2实机测试中，该方法在成功率、运动速度和避障性能上均优于现有基准。
conclusion: 结合几何安全约束与端到端学习的方案，能有效提升四足机器人在复杂动态环境下的敏捷导航能力。
---

## 摘要
由于严重的传感器遮挡和不可预测的人体运动，在动态且拥挤的环境中导航对四足机器人提出了重大挑战。现有方法面临权衡：基于模型的方法（如速度障碍法，VO）在理论上保证了安全性，但依赖于准确的障碍物运动估计，而这在密集人群中往往会失效；而端到端学习方法虽然具有鲁棒性，但缺乏对障碍物的运动预测能力，导致碰撞或过于保守的行为。为了解决这一问题，我们提出了 VOP-Nav，这是一种结合了 VO 的几何安全性和端到端学习的敏捷适应性的新型导航系统。仅使用局部机载观测，我们的系统避免了显式的障碍物检测和跟踪流程。VOP-Net 处理多帧激光雷达（LiDAR）数据，以隐式编码动态约束，并预测源自速度障碍理论的安全速度区域。重要的是，VO 预测起到了双重作用：在推理过程中作为导航策略的输入，在训练过程中作为奖励信号以鼓励安全运动。在 Isaac Gym 中的评估表明，VOP-Nav 在平衡运动速度和避障的同时，实现了比所有基准方法更高的成功率。在 Unitree Go2 四足机器人上的真实世界部署进一步验证了该系统在复杂室内外动态环境中的鲁棒性和效率。

## Abstract
Navigating dynamic and crowded environments presents significant challenges for quadruped robots due to severe sensor occlusion and unpredictable human motion. Existing approaches face a trade-off: model-based methods, such as Velocity Obstacles (VO), theoretically guarantee safety but rely on accurate obstacle motion estimates that often fail in dense crowds, while end-to-end learning methods offer robustness but lack motion prediction capability of obstacles, leading to collisions or conservative behaviors. To solve this, we propose VOP-Nav, a novel navigation system that combines the geometric safety of VO with the agile adaptability of end-to-end learning. Using only local onboard observations, our system avoids explicit obstacle detection and tracking pipelines. The VOP-Net processes multi-frame LiDAR data to implicitly encode dynamic constraints and predict a safe velocity region derived from Velocity Obstacle theory. Importantly, the VO predictions serve a dual role: they are used as input to the navigation policy during inference and as a reward signal during training to encourage safe motion. Evaluations in Isaac Gym demonstrate that VOP-Nav achieves higher success rates than all baselines while balancing locomotion speed and collision avoidance. Real-world deployment on a Unitree Go2 quadruped robot further validates the system's robustness and efficiency in complex indoor and outdoor dynamic environments.

---

## 论文详细总结（自动生成）

以下是对论文《Learning Agile Navigation in Crowded Environments for Quadruped Robots》（四足机器人在拥挤环境中的敏捷导航学习）的深度总结：

### 1. 核心问题与整体含义
*   **研究背景**：四足机器人在动态拥挤环境（如商场、校园）中导航面临巨大挑战。主要难点在于传感器遮挡严重、行人运动不可预测以及对实时性的极高要求。
*   **核心问题**：传统**基于模型的方法**（如速度障碍法 VO）依赖精确的障碍物跟踪，在密集人群中易失效；而**端到端学习方法**虽然鲁棒，但缺乏对障碍物运动的显式预测，导致行为过于保守或发生碰撞。
*   **整体含义**：本文提出 **VOP-Nav** 系统，旨在将 VO 理论的几何安全性与强化学习（RL）的灵活性相结合，实现无需显式跟踪的敏捷避障导航。

### 2. 方法论
*   **核心思想**：利用神经网络（VOP-Net）从原始多帧雷达数据中隐式学习动态约束，预测出“速度障碍区域”，并将其作为导航策略的输入和奖励引导。
*   **关键技术细节**：
    *   **VOP-Net**：采用时空编码器处理连续多帧 LiDAR 点云。它不直接输出障碍物位置，而是预测一个极坐标形式的“安全速度边界”，隐式地捕捉了周围障碍物的速度和位置信息。
    *   **VO 理论集成**：将预测的 VO 区域转化为策略网络的观测值。这使得机器人能“预见”哪些速度会导致未来碰撞。
    *   **强化学习框架**：使用 PPO 算法训练。VO 预测在其中起到双重作用：
        1.  **作为输入**：增强策略对动态环境的感知。
        2.  **作为奖励**：引入“VO 惩罚项”，如果机器人选择的速度落在预测的 VO 区域内，则给予负反馈，从而在训练阶段就强化安全意识。
*   **算法流程**：LiDAR 数据 -> VOP-Net 预测 VO 区域 -> 导航策略网络 -> 输出关节指令/目标速度。

### 3. 实验设计
*   **场景设置**：
    *   **仿真**：在 Isaac Gym 中构建了包含 100+ 随机移动行人的大规模动态场景。
    *   **实机**：使用 Unitree Go2 四足机器人，在真实的室内走廊、室外广场等拥挤人群中进行测试。
*   **Benchmark（基准方法）**：
    *   **DWA (Dynamic Window Approach)**：经典的局部路径规划算法。
    *   **Traditional VO**：基于显式检测和跟踪的传统速度障碍法。
    *   **End-to-End RL (PPO)**：不带 VO 预测信息的标准强化学习导航。
*   **评估指标**：导航成功率（Success Rate）、平均行进速度、碰撞次数、到达目标点的时间。

### 4. 资源与算力
*   **算力说明**：论文提到使用了 **Isaac Gym** 进行大规模并行仿真训练，这通常意味着利用了 NVIDIA GPU 的并行计算能力。
*   **具体细节**：文中未明确给出具体的 GPU 型号（如 A100 或 RTX 4090）及确切的训练总时长，但指出通过并行化显著缩短了数据采集和策略收敛的时间。

### 5. 实验数量与充分性
*   **实验规模**：进行了数千次仿真测试，涵盖了不同的行人密度（低、中、高）。
*   **消融实验**：专门设计了消融实验来验证 VOP-Net 的预测模块、多帧输入的必要性以及 VO 奖励项对性能的贡献。
*   **充分性评价**：实验设计较为充分。通过仿真验证了算法的统计学性能，通过实机部署验证了算法在传感器噪声、计算延迟和真实物理特性下的鲁棒性，对比实验覆盖了从传统到前沿的主流方案。

### 6. 主要结论与发现
*   **性能领先**：VOP-Nav 在高密度人群中的成功率显著高于所有基准方法，尤其是在传统 VO 方法因跟踪丢失而失效的场景下表现稳健。
*   **敏捷性与安全的平衡**：该方法允许机器人在保持较高平均速度的同时，比纯 RL 方法更早地做出避障动作，减少了“急刹车”或保守停滞的情况。
*   **隐式预测的有效性**：证明了无需显式维护障碍物列表（Tracking-free），仅靠学习到的几何约束特征即可实现复杂的动态避障。

### 7. 优点
*   **解耦感知与跟踪**：通过隐式预测规避了复杂的人体检测与多目标跟踪流程，降低了系统对感知精度的敏感度。
*   **几何先验的引入**：将 VO 这一成熟的几何理论引入 RL，比纯黑盒学习更具解释性和安全性。
*   **实时性强**：端到端的推理流程保证了在嵌入式设备（如机器人机载电脑）上的高效运行。

### 8. 不足与局限
*   **传感器依赖**：高度依赖 LiDAR 的质量。在强光干扰、透明玻璃或极度空旷（缺乏特征）的环境下，性能可能下降。
*   **行为模式单一**：训练中的行人模型可能无法完全覆盖现实中人类的所有非理性行为（如突然奔跑或故意拦截）。
*   **静态与动态的权衡**：虽然在动态避障上表现优异，但在极度狭窄且布满复杂静态障碍物的空间内，其路径最优性仍有提升空间。

（完）
