---
title: "Jetson-PI: Towards Onboard Real-Time Robot Control via Foresight-Aligned Asynchronous Inference"
title_zh: Jetson-PI：通过前瞻对齐的异步推理实现机载实时机器人控制
authors: "Zebin Yang, Qi Wang, Yunhe Wang, Xiurui Guo, Bo Yu, Shaoshan Liu, Jiafeng Xu, Hao Dong, Meng Li"
date: 2026-07-14
pdf: "https://arxiv.org/pdf/2607.12659v1"
tags: ["query:q5"]
score: 8.0
evidence: 通过异步推理实现机载实时机器人控制
tldr: 针对视觉-语言-动作（VLA）模型在Jetson Orin等低功耗设备上推理延迟高、控制频率低的问题，本文提出Jetson-PI。该方案通过前瞻对齐的异步推理机制，利用轻量级未来修正模块解决感知与执行的不对齐，并结合置信度调度与系统级加速技术。实验表明，Jetson-PI显著提升了控制频率，并在LIBERO基准测试中取得了更高的任务成功率。
selection_source: fresh_fetch
motivation: VLA模型在嵌入式设备上部署时，高推理延迟导致的异步执行会引发感知与执行的不对齐以及反应时间过长的问题。
method: 引入轻量级未来修正模块预测环境表征，并采用基于置信度的调度优化及CUDA图重用等系统级加速手段。
result: "在Jetson Orin上实现了比原生PyTorch高8.66倍的控制频率，且在LIBERO基准测试中的成功率比VLASH高出14.8%。"
conclusion: Jetson-PI通过算法与系统层面的协同设计，为低功耗板载设备实现高效、实时的机器人控制提供了有效路径。
---

## 摘要
视觉-语言-动作 (VLA) 模型在多种具身任务中取得了令人印象深刻的性能。然而，由于其高计算复杂度，在 Jetson Orin 等低功耗机载设备上部署 VLA 模型仍然具有挑战性，这导致了显著的推理延迟和较低的控制频率。异步推理可以通过并行化动作执行和后续推理来部分掩盖这种延迟，但它引入了两个关键问题：感知-执行失配和反应时间长。在本文中，我们提出了 Jetson-PI，这是一种通过前瞻对齐异步修正（Foresight-Aligned Asynchronous Correction）在机载设备上高效部署 VLA 的方法。为了解决失配问题，我们训练了一个轻量级的未来修正模块，该模块根据已提交的动作预测未来的环境表示，使动作专家能够直接预测未来时间步的动作。为了缩短反应时间，我们引入了基于置信度的调度优化，自适应地平衡 VLM 和动作专家的调用，并辅以系统级加速，包括 CUDA 图重用、GPU 常驻中间缓冲和流展开。广泛的实验表明，与原生 PyTorch 和 vla.cpp 相比，Jetson-PI 在 NVIDIA Jetson Orin 上的控制频率分别提高了 8.66 倍和 5.41 倍，同时在 LIBERO 基准测试中的平均成功率比 VLASH 高出 14.8%。我们的异步算法代码可在 https://github.com/PKU-SEC-Lab/Jetson-PI 获取，基于 llama.cpp 的高效推理引擎可在 https://github.com/PKU-SEC-Lab/Jetson-PI-Edge 获取。

## Abstract
Vision-Language-Action (VLA) models have achieved impressive performance on diverse embodied tasks. However, deploying VLA models on low-power onboard devices, such as the Jetson Orin, remains challenging due to their high computational complexity, which leads to substantial inference latency and low control frequency. Asynchronous inference can partially mask this latency by parallelizing action execution and subsequent inference, but it introduces two critical issues: perception-execution misalignment and long reaction time. In this paper, we propose Jetson-PI, a method for efficient VLA deployment on onboard devices via Foresight-Aligned Asynchronous Correction. To address misalignment, we train a lightweight future correction module that predicts future environment representation conditioned on committed actions, enabling the action expert to directly predict actions from the future time step. To reduce reaction time, we introduce confidence-based scheduling optimization that adaptively balances VLM and action expert invocations, complemented by system-level accelerations including CUDA graph reuse, GPU-resident intermediate buffering, and flow unrolling. Extensive experiments demonstrate that Jetson-PI achieves 8.66x and 5.41x improvements in control frequency compared with naive PyTorch and vla.cpp on NVIDIA Jetson Orin, while outperforming VLASH by 14.8\% in average success rate on the LIBERO benchmark. The code of our asynchronous algorithm is available on https://github.com/PKU-SEC-Lab/Jetson-PI, and our efficient llama.cpp-based inference engine is available on https://github.com/PKU-SEC-Lab/Jetson-PI-Edge.

---

## 论文详细总结（自动生成）

### 论文总结：Jetson-PI —— 通过前瞻对齐的异步推理实现机载实时机器人控制

#### 1. 核心问题与整体含义（研究动机和背景）
视觉-语言-动作（VLA）模型虽然在具身智能任务中表现卓越，但其巨大的计算开销使得在低功耗机载设备（如 NVIDIA Jetson Orin）上的部署面临严峻挑战。高推理延迟会导致控制频率极低（如 0.7 Hz），造成机器人动作停顿。
*   **研究动机**：现有的异步推理虽然能通过并行化掩盖延迟，但会引入两个致命问题：
    1.  **感知-执行失配（Misalignment）**：推理结束时，环境已随动作执行发生变化，导致基于旧图像预测的动作不再准确。
    2.  **反应时间长**：机器人的响应速度受限于 VLA 的整体推理时间。
*   **核心目标**：在低功耗边缘设备上实现高频率、低延迟且感知对齐的实时机器人控制。

#### 2. 方法论
Jetson-PI 提出了“前瞻对齐异步修正”方案，包含三个核心维度：
*   **前瞻对齐异步修正（Foresight-Aligned Asynchronous Correction）**：
    *   训练一个仅 40M 参数（约占模型总量 1%）的轻量级**未来修正模块**。
    *   该模块以当前 VLM 隐藏状态和已提交的动作序列为输入，预测推理完成时刻（未来 $t+\Delta$）的环境表征。
    *   动作专家（Action Expert）直接基于预测的未来状态生成动作，从而消除感知滞后。
*   **基于置信度的调度优化（Confidence-based Scheduling Optimization）**：
    *   修正模块会输出预测的“置信度”。
    *   当置信度高时，跳过耗时的 VLM 调用，仅运行动作专家和修正模块；当置信度低（如遇到复杂环境变化）时，重新调用 VLM 刷新感知。
*   **系统级加速设计**：
    *   基于 `llama.cpp` 构建推理引擎。
    *   **CUDA 图重用**：针对 VLA 固定的 Token 长度，避免重复构建计算图。
    *   **GPU 常驻中间缓冲**：减少组件间（ViT/LLM/AE）的数据拷贝。
    *   **流匹配展开（Flow Unrolling）**：融合多次去噪迭代，减少算子启动开销。

#### 3. 实验设计
*   **数据集/基准测试**：使用 **LIBERO** 仿真基准（包括 Spatial, Object, Goal, LIBERO-10 四个子集）。
*   **对比方法**：
    *   **Sync**：同步推理基准。
    *   **Naive Async**：原生异步推理。
    *   **RTC**：动作块修复技术。
    *   **VLASH**：基于未来机器人状态预测的异步方法。
    *   **vla.cpp**：现有的边缘端推理框架。
*   **硬件平台**：NVIDIA Jetson Orin (30W/50W), Jetson Thor, 以及 RTX 4090/A6000 作为性能参考。
*   **真实场景**：使用 PrimeBot X2-W 机器人进行“折叠衣服”复杂任务验证。

#### 4. 资源与算力
*   **训练阶段**：在 **NVIDIA A100 GPU** 上进行。采用两阶段训练：第一阶段训练压缩器和动作专家（30,000 步），第二阶段训练未来修正模块（25,000 步）。
*   **部署阶段**：主要针对 **Jetson Orin**（32GB/64GB）和 **Jetson Thor**。
*   **模型规模**：以 $\pi_{0.5}$ 为基础模型，修正模块参数量约为 40M。

#### 5. 实验数量与充分性
*   **仿真实验**：在 4 个 LIBERO 数据集上进行了详尽测试，并模拟了从 $\Delta=1$ 到 $\Delta=9$ 的不同推理延迟场景，验证了方法在不同硬件延迟下的鲁棒性。
*   **消融实验**：针对置信度阈值 $\theta$ 进行了消融分析，展示了控制频率与成功率之间的权衡。
*   **系统性能**：对比了原生 PyTorch、vla.cpp 与 Jetson-PI 在不同设备上的推理耗时拆解。
*   **充分性评价**：实验覆盖了从算法精度到系统延迟、从仿真到实机的全维度，对比基准选择合理，实验设计较为客观公平。

#### 6. 主要结论与发现
*   **控制频率提升**：在 Jetson Orin 上，Jetson-PI 的控制频率比原生 PyTorch 提升了 **8.66 倍**，比 vla.cpp 提升了 **5.41 倍**。
*   **任务成功率**：在长延迟（$\Delta=9$）场景下，平均成功率比 SOTA 方法 VLASH 高出 **14.8%**。
*   **能效比**：相比使用 RTX 4090，使用 Jetson Orin 部署可将机器人电池寿命延长 **6 倍**。
*   **调度有效性**：置信度调度能有效在关键动作点（如抓取、放置）自动触发 VLM 刷新，而在平滑路径阶段节省算力。

#### 7. 优点
*   **算法与系统协同**：不仅在算法层面解决失配，还针对边缘硬件特性做了深度的系统级优化。
*   **轻量且高效**：修正模块极小，几乎不增加推理负担，却显著解决了异步推理的精度下降问题。
*   **自适应性强**：通过置信度动态调整计算负载，能够适配不同算力的边缘设备。
*   **开源贡献**：提供了基于 `llama.cpp` 的高效边缘推理引擎，具有较高的实用价值。

#### 8. 不足与局限
*   **算力鸿沟**：尽管提升巨大，但在边缘设备上的表现仍无法完全抹平与高端 GPU 集群的差距，尤其是在模型参数进一步扩大时。
*   **环境预测限制**：未来修正模块本质上是隐式世界模型，对于极度不可预测或高度动态的环境，其预测精度可能受限。
*   **训练依赖**：需要针对特定的 VLA 模型进行两阶段微调，迁移到全新架构模型时可能需要重新训练修正模块。

（完）
