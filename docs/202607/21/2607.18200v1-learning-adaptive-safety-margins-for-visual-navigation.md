---
title: Learning Adaptive Safety Margins for Visual Navigation
title_zh: 学习视觉导航中的自适应安全裕度
authors: "Junyi Hu, Shuaihang Yuan, Geeta Chandra Raju Bethala, Anthony Tzes, Yi Fang"
date: 2026-07-20
pdf: "https://arxiv.org/pdf/2607.18200v1"
tags: ["query:q1"]
score: 8.0
evidence: 导航中的无碰撞路径与自适应安全裕度
tldr: 针对室内机器人导航中固定安全裕度导致的保守绕路或冒险碰撞问题，本文提出一种学习自适应安全裕度的方法。该方法利用上下文感知的安全评价器对扩散模型生成的候选轨迹进行排序，通过结合安全预算、效率优化和距离约束匹配，并采用教师-学生蒸馏机制将仿真几何知识迁移至纯视觉感知。实验表明，该方法在多个基准测试中表现优异，并成功在类人机器人上实现了零样本跨场景部署。
selection_source: fresh_fetch
motivation: 传统的固定安全裕度无法兼顾导航的安全性与效率，导致机器人在复杂环境中容易出现过度保守的绕路或危险的碰撞。
method: 提出一种包含安全惩罚、效率增益和距离约束匹配的自适应安全评价器，用于对扩散规划器生成的轨迹进行优选，并利用特权几何信息进行蒸馏训练。
result: 在HM3D和MP3D数据集上取得了最高的成功率和路径效率，并成功在Unitree G1类人机器人上完成了无微调的实机部署。
conclusion: 学习自适应的安全裕度能有效解决视觉导航中的感知偏差问题，显著增强机器人在复杂室内环境中的鲁棒性和泛化性。
---

## 摘要
机器人在拥挤的室内空间中经常失败，原因并非无法生成无碰撞路径，而是因为固定的安全裕度校准不当：保守的裕度会导致绕路和超时，而宽松的裕度在感知偏差下会导致靠近边界的危险捷径。基于扩散模型的规划器能从第一人称 RGB-D 图像中生成多样化的候选轨迹，但如何进行可靠的选择仍是瓶颈。我们提出了一种上下文条件的安全性评价器（safety critic），用于学习对扩散提议进行排序的自适应间隙偏好，并将其分解为三个互补项：(i) 安全项，包含间隙预算惩罚和控制屏障函数（CBF）残差，用于确保路径点和转换过程的安全性；(ii) 效率项，结合了平滑度惩罚和安全门控的绕路率惩罚，在避免绕路的同时不鼓励冒险的捷径；(iii) 距离约束匹配项，将学习到的预算锚定到实际的欧几里得符号距离场（ESDF）间隙，以防止裕度崩溃。我们在仿真中使用特权 ESDF 几何信息训练评价器，并通过两阶段教师-学生程序将其蒸馏为仅依赖感知的选择器。在 HM3D 和 MP3D 的 PointGoal 导航任务中（包括跨数据集迁移），我们的方法在强大的扩散、优化和强化学习基准模型中实现了最高的成功率（SR）和路径长度加权成功率（SPL）。该方法纯粹在仿真中训练，可直接迁移至宇树（Unitree）G1 人形机器人，并在无需针对特定任务微调的情况下在拥挤的室内场景中完成导航。

## Abstract
Robots in cluttered indoor spaces often fail not because they cannot generate collision-free paths, but because a fixed safety margin is mis-calibrated: conservative margins cause detours and timeouts, while permissive margins lead to near-boundary shortcuts under perception bias. Diffusion-based planners propose diverse trajectory candidates from egocentric RGB-D, yet reliable selection remains the bottleneck. We propose a context-conditioned safety critic that learns an adaptive clearance preference for ranking diffusion proposals, decomposed into three complementary terms: (i) a safety term with a clearance-budget penalty and a control-barrier-function residual for waypoint- and transition-wise safety, (ii) an efficiency term combining a smoothness penalty with a safety-gated detour-ratio penalty that avoids detours without incentivizing risky shortcuts, and (iii) a distance-constraint matching term that anchors the learned budget to realized ESDF clearances to prevent margin collapse. We train the critic with privileged ESDF geometry in simulation and distill it into a perception-only selector via a two-stage teacher-student procedure. On PointGoal navigation in HM3D and MP3D, including cross-dataset transfer, our method achieves the highest success rate (SR) and success weighted by path length (SPL) among strong diffusion, optimization, and RL baselines. Trained purely in simulation, it transfers to a Unitree G1 humanoid and navigates cluttered indoor scenes without task-specific tuning.

---

## 论文详细总结（自动生成）

以下是对论文《Learning Adaptive Safety Margins for Visual Navigation》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在室内视觉导航中，传统的规划器通常使用**固定的安全裕度**（Safety Margin）。这导致了两个极端：
    *   **过度保守**：在开阔区域因裕度过大导致不必要的绕路或任务超时。
    *   **过度冒险**：在拥挤区域因感知偏差（如深度图噪声）导致机器人选择过于靠近障碍物的危险捷径。
*   **研究背景**：虽然基于扩散模型（Diffusion-based）的规划器能生成多样化的候选路径，但如何从这些路径中选出既安全又高效的一条（即“选择器”瓶颈）仍是难题。本文旨在学习一种**上下文自适应的安全偏好**，使机器人能根据环境复杂度动态调整安全距离。

### 2. 方法论
该方法采用“生成-选择”（Generate-Select）架构，核心是**可学习的安全评价器（Safety Critic）**：
*   **扩散轨迹生成器**：利用 Transformer 编码 RGB-D 观测，通过扩散模型生成 $K$ 条候选轨迹。
*   **自适应安全评价器（核心贡献）**：评价器由三个互补项组成：
    1.  **安全项 (Safety Term)**：包含一个可学习的间隙预算惩罚（针对路径点）和控制屏障函数（CBF）残差（针对状态转换），确保物理层面的安全性。
    2.  **效率项 (Efficiency Term)**：结合路径平滑度惩罚和“安全门控”的绕路率惩罚。只有在安全间隙充足时才鼓励缩短路径，防止在危险区域盲目追求效率。
    3.  **平衡项 (Balance Term)**：将学习到的安全预算锚定到真实的 ESDF（符号距离场）间隙上，防止模型为了得分而使安全裕度崩溃。
*   **两阶段教师-学生训练**：
    *   **第一阶段（教师）**：在仿真环境中使用特权的 ESDF 几何信息训练评价器，学习如何区分专家路径与非专家路径。
    *   **第二阶段（学生）**：将教师评价器的知识蒸馏到一个仅依赖 RGB-D 输入的选择器中，使其在部署时无需构建地图或 ESDF。

### 3. 实验设计
*   **数据集/场景**：
    *   **仿真**：使用 Habitat 平台，在 HM3D（大规模室内场景）和 MP3D（复杂建筑场景）上进行测试。
    *   **实机**：部署在 **Unitree G1 类人机器人**上，涵盖走廊（简单）、公寓（中等）、实验室（困难/拥挤）三种真实场景。
*   **基准模型 (Benchmarks)**：
    *   **NavDP**：基于扩散模型但使用固定约束的选择器。
    *   **iPlanner / EGO-Planner**：基于优化的路径规划方法。
    *   **PointNav (DD-PPO)**：端到端强化学习基准。
    *   **ViPlanner**：结合视觉语义学习的导航方法。
*   **对比指标**：成功率 (SR) 和 路径长度加权成功率 (SPL)。

### 4. 资源与算力
*   **推理算力**：论文提到在笔记本 GPU (**NVIDIA RTX 4060**) 上进行了推理耗时测试。当候选轨迹数 $K=16$ 时，生成器耗时约 56ms/step，选择器耗时约 21ms/step。
*   **训练算力**：文中**未明确说明**训练所用的具体 GPU 型号、数量及总训练时长，但提到了使用 AdamW 优化器、自动混合精度（AMP）等标准深度学习训练配置。

### 5. 实验数量与充分性
*   **实验规模**：在仿真中每个测试集运行了 300 个 Episode，实机测试每个场景 10 个 Episode。
*   **充分性**：
    *   **跨数据集迁移**：进行了 HM3D 到 MP3D 的迁移实验，验证了模型的泛化能力。
    *   **消融实验**：分别移除了安全项、效率项和平衡项，证明了三个组件对最终性能的贡献，实验设计较为严谨。
    *   **灵敏度分析**：测试了不同 ESDF 体素大小和截断距离对性能的影响，体现了实验的客观性。

### 6. 主要结论与发现
*   **性能领先**：在所有仿真基准测试中，该方法在 SR 和 SPL 上均排名第一。在跨数据集任务中，其 SR 提升了约 7.4% - 11.6%。
*   **自适应能力**：实机测试证明，该模型能根据通道狭窄程度（DoN 指标）动态收缩安全裕度。在极窄空间内，它能通过收缩裕度通过，而在固定裕度模型下则会因判定“不可通行”而停滞。
*   **零样本迁移**：纯仿真训练的模型无需任何实机微调即可在类人机器人上稳定运行，证明了蒸馏策略的有效性。

### 7. 优点
*   **动态平衡**：成功解决了导航中“效率”与“安全”的经典矛盾，通过可学习的裕度适应复杂环境。
*   **无需在线建图**：部署时仅需 RGB-D，降低了对计算资源和高精度定位系统的依赖。
*   **结构化设计**：评价器的三项分解逻辑清晰，CBF 的引入增强了控制理论上的安全性保障。

### 8. 不足与局限
*   **训练依赖特权信息**：虽然推理不需要 ESDF，但训练阶段高度依赖高质量的 ESDF 标注，这在某些极端复杂或动态变化的仿真环境中可能难以获取。
*   **实时性挑战**：虽然在 4060 上能运行，但生成+选择的总耗时接近 80ms，对于高速移动的机器人来说，帧率仍有提升空间。
*   **感知边界**：论文提到在极度拥挤（DoN=0.9）时模型会拒绝通行，虽然这是为了安全，但在某些必须“擦边”通过的极端任务中可能显得过于保守。

（完）
