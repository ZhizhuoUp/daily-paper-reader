---
title: "FA-RDP: A Frequency-Adaptive Reactive Diffusion Policy for Contact-Rich Manipulation"
title_zh: FA-RDP：一种用于高接触操作的频率自适应反应式扩散策略
authors: "Lifeng Zhuo, Wendi Chen, Han Xue, Shirun Tang, Jun Lv, Cewu Lu, Chuan Wen"
date: 2026-07-30
pdf: "https://arxiv.org/pdf/2607.28596v1"
tags: ["query:profile-1"]
score: 8.0
evidence: 接触密集型操作的反应式扩散策略
tldr: 针对接触密集型操作中动作多模态与实时反应性的矛盾，本文提出FA-RDP。该方法通过多频率视觉-力觉Transformer和多模态指示器，在接触前采用低频多步采样以保留动作多样性，在接触后切换至高频单步采样以增强反应性。结合流形一致性蒸馏技术，FA-RDP在保持动作流形约束的同时显著提升了任务成功率，为复杂机器人操作提供了更灵活的决策机制。
selection_source: fresh_fetch
motivation: 传统的扩散策略采用固定采样频率，难以同时兼顾接触前对多样化路径的需求和接触后对力反馈的高频响应需求。
method: 提出频率自适应反应式扩散策略，通过学习多模态指示器动态切换采样步数，并利用流形一致性蒸馏（MCD）确保动作符合机器人运动约束。
result: 在三项典型的接触密集型操作任务实验中，FA-RDP在保持接触前轨迹多样性的同时，实现了最高的任务成功率。
conclusion: 该研究通过动态调整推理频率和采样策略，有效解决了扩散模型在复杂交互任务中的多模态与反应性权衡问题。
---

## 摘要
在高接触操作中，动作的多模态性和反应性在单个回合的不同阶段占据主导地位。在接触发生前，多条轨迹可能同样有效，因此保留多样化的动作模式至关重要。接触发生后，几何约束和力限制缩小了解空间，而成功的执行则需要对力反馈做出快速响应。然而，标准的扩散策略在整个回合中使用固定的推理频率和采样步数，这导致了一个根本性的权衡：低频、多步采样能更好地保留接触前的多模态性，但对力反馈响应缓慢；而高频采样虽然提高了反应性，但往往会导致接触前不同模式的崩溃。为了解决这一权衡，我们提出了 FA-RDP，一种频率自适应的反应式扩散策略。一个共享的多频视觉-力觉 Transformer 在低频和高频下同时预测动作块，而一个学习到的多模态指示器则在接触前动态选择多步低频采样，并在动作歧义性降低时选择单步高频采样。我们进一步引入了流形一致性蒸馏（MCD），它重新参数化扩散网络以在机器人动作流形上预测动作，同时保留基于 DDPM 的残差监督。在三个高接触操作任务上的实验表明，FA-RDP 在保持多样化的接触前轨迹模式的同时，实现了最高的成功率。代码和视频可在 https://fa-rdp.github.io 获取。

## Abstract
In contact-rich manipulation, action multimodality and reactivity dominate different stages of a single episode. Before contact, multiple trajectories might be equally valid, making it important to preserve diverse action modes. After contact, geometric constraints and force limits narrow the solution space, while successful execution demands rapid responses to force feedback. However, standard diffusion policies use a fixed inference frequency and sampling steps throughout the episode, forcing a fundamental compromise: low-frequency, multi-step sampling better preserves pre-contact multimodality but responds slowly to force feedback, whereas high-frequency sampling improves reactivity but tends to collapse distinct pre-contact modes. To resolve this tradeoff, we present FA-RDP, a frequency-adaptive reactive diffusion policy. A shared multi-frequency visual-force Transformer predicts action chunks at both low and high frequencies, while a learned multimodality indicator dynamically selects multi-step low-frequency sampling before contact and one-step high-frequency sampling as action ambiguity decreases. We further introduce Manifold Consistency Distillation (MCD), which reparameterizes the diffusion network to predict actions on the robot action manifold while retaining DDPM-based residual supervision. Experiments on three contact-rich manipulation tasks show that FA-RDP achieves the highest success rate while preserving diverse pre-contact trajectory modes. Code and videos are available at https://fa-rdp.github.io.

---

## 论文详细总结（自动生成）

以下是对论文《FA-RDP: A Frequency-Adaptive Reactive Diffusion Policy for Contact-Rich Manipulation》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机）
在高接触（Contact-rich）机器人操作任务中，策略面临两个阶段性的矛盾需求：
*   **接触前（Pre-contact）：** 存在多模态性。例如，机器人可以从左侧或右侧绕过障碍物接近目标，策略需要保留这种多样化的路径选择能力。
*   **接触后（Post-contact）：** 存在强约束性。物理接触限制了动作空间，且需要极高频率的力反馈响应（Reactivity）以维持接触力或防止滑脱。
*   **现有痛点：** 标准扩散策略（Diffusion Policy）通常使用固定的推理频率和采样步数。低频多步采样能保留多模态但响应慢；高频采样（如通过蒸馏减少步数）虽响应快，但往往导致多模态分布崩溃且动作不平滑。

### 2. 方法论
FA-RDP 提出了一种频率自适应的反应式扩散策略，包含三个核心组件：
*   **多频率视觉-力觉 Transformer：** 采用共享权重的骨干网络，通过“频率感知位置编码（Frequency-aware Positional Encoding）”处理不同频率。低频模式（10Hz）在时间网格上采样稀疏索引，高频模式（30Hz）采样连续索引，使单一网络能同时预测不同密度的动作块。
*   **多模态指示器（Multimodality Indicator）：** 一个学习到的预测头，根据视觉输入估计当前动作的歧义程度。在接触前（歧义高）选择低频多步采样以保证多样性；在接触后（歧义低、受力强）切换至高频单步采样。
*   **流形一致性蒸馏（Manifold Consistency Distillation, MCD）：** 
    *   为了实现高频下的单步推理，研究者将扩散网络重新参数化，使其直接在机器人动作流形上预测动作块，而非预测噪声（epsilon）或速度。
    *   结合样本回归损失（SRL）进行蒸馏，确保学生模型在单步推理时既能保持教师模型的轨迹一致性，又能紧贴演示数据的分布。
*   **算法流程：** 每一轮循环中，策略首先通过指示器判断频率模式，缓存慢速视觉特征，然后在快速循环中不断刷新力传感器数据，进行闭环推理并执行最新动作。

### 3. 实验设计
*   **实验场景：** 在物理机器人（Flexiv Rizon 4s）上设计了三个具有挑战性的任务：
    1.  **双盒翻转（Dual Box Flipping）：** 需绕过障碍块并维持压力翻转盒子。
    2.  **双开关切换（Dual Switch Toggling）：** 需精准触碰并沿特定轨迹施力。
    3.  **双按钮按下（Dual Button Pressing）：** 需对准轴线并持续施力直至触发。
*   **基准方法（Benchmarks）：**
    *   **Diffusion Policy (DP)：** 仅视觉的扩散策略。
    *   **RDP：** 层次化慢-快视觉-触觉策略。
    *   **ImplicitRDP：** 固定频率的端到端视觉-力觉扩散策略。
    *   **Regression Policy w/ Force：** 基于 MSE 的非扩散回归策略。
    *   **消融实验：** 对比了 MeanFlow、Consistency Policy 等不同的蒸馏加速方案。

### 4. 资源与算力
*   **硬件环境：** 实验使用了 **NVIDIA GeForce RTX 5090 GPU** 和 Intel Core Ultra 9 285K CPU。
*   **推理延迟：** 高频模式推理延迟低于 **30ms**，低频模式低于 50ms。
*   **训练细节：** 论文提到采用三阶段训练（阶段1：多频骨干训练；阶段2：指示器校准；阶段3：流形一致性蒸馏），但未明确给出总训练时长。

### 5. 实验数量与充分性
*   **实验规模：** 每个任务收集了 60 个演示样本，每种方法进行 20 次测试。在多模态研究中，针对特定任务运行了 80 次试验以统计分布。
*   **充分性：** 实验设计较为全面，涵盖了成功率对比、多模态分布可视化、指示器数值随时间变化的曲线分析以及针对不同蒸馏算法的消融实验。
*   **公平性：** 所有方法共享相同的底层 100Hz 力补偿控制层，确保了性能提升源于高层策略而非底层控制器的差异。

### 6. 主要结论与发现
*   **性能领先：** FA-RDP 在三项任务中的平均成功率达到 **81.7%**，远高于 ImplicitRDP（51.7%）和 RDP（35.0%）。
*   **频率自适应的必要性：** 实验证明，在接触前保持低频多步采样能有效覆盖所有可能的接近路径（多模态），而接触后切换至高频单步采样能显著减少因力反馈延迟导致的脱离接触或碰撞。
*   **蒸馏效果：** 提出的 MCD 方法在单步推理下的表现显著优于传统的 Consistency Policy 和 MeanFlow，证明了在动作流形上进行回归的稳定性。

### 7. 优点
*   **动态平衡：** 巧妙地解决了扩散模型在“生成多样性”与“实时反应性”之间的固有权衡。
*   **端到端架构：** 避免了显式的层次化设计带来的信息损失，通过共享骨干网络提高了参数效率。
*   **鲁棒性：** 引入流形约束和残差监督，使得单步扩散推理在复杂的力交互任务中依然稳健。

### 8. 不足与局限
*   **训练复杂性：** 采用三阶段训练流程，相比于标准的扩散策略，训练管线更为复杂。
*   **模态局限：** 目前仅验证了视觉和力觉（F/T 传感器），尚未测试更复杂的触觉阵列（Tactile Arrays）。
*   **任务泛化：** 实验主要集中在单任务策略上，尚未在大规模多任务数据集上验证其通用性。
*   **阈值依赖：** 频率切换依赖于预设的指示器阈值，在不同环境下的迁移可能需要重新校准。

（完）
