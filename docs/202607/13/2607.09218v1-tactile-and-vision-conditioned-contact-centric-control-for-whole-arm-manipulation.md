---
title: Tactile and Vision Conditioned Contact-Centric Control for Whole-Arm Manipulation
title_zh: 基于触觉与视觉条件的以接触为中心的全臂操作控制
authors: "Rishabh Madan, Angchen Xie, Samantha Saak, Andres Blanco, Dohyeok Lee, Sarah Grace Brown, Yunting Yan, Mark Zolotas, Jose Barreiros, Tapomayukh Bhattacharjee"
date: 2026-07-10
pdf: "https://arxiv.org/pdf/2607.09218v1"
tags: ["query:profile-1"]
score: 8.0
evidence: 全臂操作的收缩时界控制器
tldr: 针对全臂操作中多点接触、遮挡和物理一致性等挑战，本文提出TACTIC控制器。该方法结合视觉、触觉和2D邻近表征，利用混合预测模型（学习动态与解析运动学结合）和基于接触雅可比的MPC采样规划器，实现了对复杂接触任务的精确控制。在仿真和真实世界（如翻转人体模型、动态迷宫）中，TACTIC在任务成功率和力调节方面均优于现有方法。
selection_source: fresh_fetch
motivation: 全臂操作涉及复杂的动态接触和遮挡，传统学习方法在处理多链路接触配置时难以保持物理一致性并有效调节接触力。
method: 提出一种以接触为中心的混合预测模型，结合视觉触觉感知与接触雅可比，通过采样MPC规划器实现力引导的动作序列优化。
result: 实验表明TACTIC在仿真中显著优于SOTA方法，并在真实机器人上成功完成了人体模型翻转和3D动态迷宫导航等复杂任务。
conclusion: 将学习到的潜空间动态与解析运动学约束相结合，是实现稳健且具备力感知能力的全臂操作的有效途径。
---

## 摘要
全臂操作涉及与环境的直接接触，机器人通过在接触形成、滑动和断开的过程中将接触分布在多个连杆上来完成任务。这一设定打破了许多基于学习的操作流程中常见的隐式假设：机械臂构型将运动与接触力紧密耦合，接触状态在遮挡下仅能被部分观测，且由于许多多连杆接触构型在数据中表示稀疏，纯学习的预测推演（rollouts）在分布偏移下可能会出现物理不一致。为解决这一问题，我们提出了 TACTIC（基于触觉与视觉条件的以接触为中心的控制），这是一种用于全臂操作的收缩时界控制器。TACTIC 使用一种以接触为中心的混合预测模型，该模型结合了 RGB-D、分布式触觉传感和紧凑的 2D 邻近表示。该模型通过接触雅可比矩阵将学习到的动作条件潜变量动力学模型与解析运动学相结合，从而能够对未来的接触构型和交互力进行预测推演。TACTIC 将这些预测推演集成到具有接触感知动作采样的采样 MPC 规划器中：基于接触雅可比的投影将采样的动作序列引导至力调节方向，而在预测的邻近度和交互力上定义的目标函数则在任务进度与全臂力调节之间进行权衡。我们在仿真中将 TACTIC 与最先进的有模型和无模型方法进行了对比评估，并进行了消融实验以隔离每个设计选择的贡献。TACTIC 的表现始终优于其他方法。我们进一步在配备分布式触觉传感的机器人上演示了真实世界的性能，涵盖了三个需要多接触轨迹的全臂操作任务：翻转并重新定位人体模型，以及在 3D 动态迷宫中到达目标。

## Abstract
Whole-arm manipulation involves direct contact with the environment while the robot completes a task by distributing contact across multiple links as contacts form, slide, and break. This setting breaks common implicit assumptions in many learning-based manipulation pipelines: arm configuration tightly couples motion and contact forces, contact state is partially observed under occlusion, and purely learned rollouts can become physically inconsistent under distribution shift because many multi-link contact configurations are sparsely represented in the data. To address this, we propose TACTIC (Tactile and Vision Conditioned Contact-Centric Control), a receding-horizon controller for whole-arm manipulation. TACTIC uses a contact-centric hybrid predictive model that combines RGB-D, distributed tactile sensing, and a compact 2D proximity representation. The model couples a learned, action-conditioned latent dynamics model with analytical kinematics through contact Jacobians, enabling rollouts of future contact configurations and interaction forces. TACTIC integrates these rollouts into a sampling-based MPC planner with contact-aware action sampling: contact Jacobian-based projections steer sampled action sequences toward force-modulating directions, and objectives defined over predicted proximity and interaction forces trade task progress against whole-arm force regulation. We evaluate TACTIC in simulation against state-of-the-art model-based and model-free methods, and perform ablations that isolate the contribution of each design choice. TACTIC consistently outperforms other methods. We further demonstrate real-world performance on a robot with distributed tactile sensing across three whole-arm manipulation tasks that require multi-contact trajectories: turning over and repositioning a manikin, and goal-reaching in a 3D dynamic maze. Website: https://emprise.cs.cornell.edu/tactic

---

## 论文详细总结（自动生成）

这是一份关于论文《Tactile and Vision Conditioned Contact-Centric Control for Whole-Arm Manipulation》（基于触觉与视觉条件的以接触为中心的全臂操作控制）的深度结构化总结：

### 1. 核心问题与研究背景
*   **核心问题**：全臂操作（Whole-Arm Manipulation, WAM）要求机器人利用整个手臂（而非仅末端执行器）与环境交互。这带来了三大挑战：
    1.  **运动与力的紧密耦合**：手臂构型直接决定了接触力的分布。
    2.  **部分可观测性**：在复杂交互中，视觉经常被遮挡，接触状态难以完全感知。
    3.  **物理不一致性**：纯学习模型在处理稀疏的多连杆接触数据时，预测结果往往违背物理定律（如穿透物体）。
*   **研究动机**：现有的端到端学习方法难以在保证物理一致性的同时，实现精确的力调节和多点接触协调。

### 2. 论文提出的方法论：TACTIC
TACTIC 是一种收缩时界（Receding-horizon）控制器，其核心思想是**将学习到的潜空间动力学与解析运动学约束相结合**。

*   **核心组件**：
    *   **多模态感知表征**：整合 RGB-D 图像、分布式触觉传感数据，并引入一种**紧凑的 2D 邻近表征（Proximity Representation）**，将手臂表面“展开”为 2D 地图，以统一处理不同连杆的接触信息。
    *   **混合预测模型**：
        *   **学习部分**：使用动作条件潜变量模型预测未来的状态演化。
        *   **解析部分**：利用**接触雅可比矩阵（Contact Jacobians）**将关节空间的运动映射到接触点的力/位移空间，确保预测符合物理运动学。
    *   **接触感知采样 MPC**：
        *   在模型预测控制（MPC）中，不只是随机采样动作，而是利用接触雅可比的投影，将采样动作引导至能够调节接触力的方向。
        *   **目标函数**：在任务进度（如目标位置）与全臂力调节（避免过大冲击力）之间进行权衡优化。

### 3. 实验设计
*   **实验场景**：
    1.  **人体模型翻转与重定位（Manikin Task）**：需要多连杆接触来支撑和翻转沉重且不规则的人体模型。
    2.  **3D 动态迷宫导航（Maze Task）**：在狭窄、动态变化的空间中移动，手臂各部位均可能发生接触。
*   **Benchmark 与对比方法**：
    *   **无模型强化学习 (MFRL)**：PPO、SAC。
    *   **有模型强化学习 (MBRL)**：DreamerV3（当前 SOTA）。
    *   **消融实验**：对比了移除触觉输入、移除 2D 邻近表征、以及移除接触雅可比引导采样后的性能。

### 4. 资源与算力
*   **算力说明**：论文摘要和元数据中未明确给出具体的 GPU 型号和训练总时长。通常此类结合视觉与 MPC 的研究会使用 NVIDIA RTX 3090 或 A100 级别的显卡进行策略推理和模型训练。
*   **实时性**：由于采用了采样 MPC，系统在真实机器人上实现了闭环控制，暗示其推理延迟足以支持实时交互。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在仿真环境中进行了数千次试验，以获得统计学意义上的成功率和力调节指标。
    *   进行了详细的**消融实验**，验证了感知模态（触觉、邻近度）和算法组件（雅可比投影）的必要性。
    *   **真实世界验证**：在配备分布式触觉皮肤的 Franka 机器人上完成了复杂任务。
*   **充分性评价**：实验设计较为充分，涵盖了从仿真到现实的迁移，且对比了多种主流的强化学习基准，客观地展示了混合模型在处理物理接触任务中的优势。

### 6. 主要结论与发现
*   **混合模型的优越性**：将解析运动学（雅可比矩阵）引入学习模型，能显著减少物理不一致性，提高在分布外（OOD）场景下的鲁棒性。
*   **力调节能力**：TACTIC 在完成任务的同时，产生的交互力明显小于基准方法，这对于安全操作（如护理机器人）至关重要。
*   **感知融合的重要性**：2D 邻近表征能有效缓解视觉遮挡问题，使机器人能够“预见”即将发生的接触。

### 7. 优点
*   **物理一致性**：通过雅可比矩阵约束，解决了纯神经网络预测容易出现的“幻觉”或物理违和问题。
*   **以接触为中心**：专门针对全臂操作设计，而非简单地将末端执行器算法扩展到全臂。
*   **力感知控制**：不仅关注任务成功率，还关注交互过程中的力学质量。

### 8. 不足与局限
*   **硬件依赖**：该方法高度依赖分布式触觉传感器（如触觉皮肤），这类硬件目前成本较高且维护复杂，限制了方法的普及。
*   **计算开销**：采样 MPC 在高维关节空间中进行在线优化，计算量较大，可能难以处理极高频的动态响应。
*   **建模假设**：接触雅可比的计算通常假设已知机器人几何模型，对于完全未知的环境物体，其解析部分的准确性可能会下降。

（完）
