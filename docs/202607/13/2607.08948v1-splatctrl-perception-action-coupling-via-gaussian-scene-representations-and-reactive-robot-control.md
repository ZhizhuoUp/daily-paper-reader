---
title: "SplatCtrl: Perception-Action Coupling via Gaussian Scene Representations and Reactive Robot Control"
title_zh: SplatCtrl：通过高斯场景表示和反应式机器人控制实现感知-动作耦合
authors: "Siddarth Jain, Ho Jin Choi"
date: 2026-07-09
pdf: "https://arxiv.org/pdf/2607.08948v1"
tags: ["query:profile-1"]
score: 9.0
evidence: 实时反应式机器人运动生成与无碰撞控制
tldr: 针对机器人在非结构化和动态环境中的避障难题，本文提出SplatCtrl框架。该框架利用3D高斯泼溅技术，通过混合体素滤波和动态重定位策略实现RGB-D流的实时场景重建。同时，研究提出从各向同性高斯中推导连续符号距离函数，并将其集成到控制屏障函数中，实现了感知与动作的深度耦合，确保机器人在未知且变化的环境中能够进行平滑、可靠的实时避障与运动控制。
selection_source: fresh_fetch
motivation: 传统的机器人控制在处理未知且持续变化的动态环境时，难以兼顾实时场景重建与可靠的避障反应。
method: 提出一种基于3D高斯泼溅的混合体素滤波重建方法，并结合连续符号距离函数与控制屏障函数实现反应式运动生成。
result: 实验证明该框架在仿真、物理机器人及人机协作场景中均能实现高效的实时场景感知与安全避障。
conclusion: SplatCtrl成功将现代隐式场景表示与经典控制理论相结合，为动态环境下的机器人感知-动作耦合提供了有效方案。
---

## 摘要
机器人操作臂在结构化环境中表现出色，但在非结构化和动态设置中面临重大挑战。本文提出了 SplatCtrl，这是一个用于实时场景重建和反应式机器人运动生成的统一框架，旨在实现先前未知且持续变化环境中的无碰撞机械臂控制。基于 3D 高斯泼溅（3D-GS），我们引入了一种混合体素滤波和动态高斯重定位策略，支持从 RGB-D 流中进行高效场景重建，同时适应环境变化。为了实现安全且反应式的控制，我们进一步提出了一种从各向同性高斯中推导连续有符号距离函数的方法，提供稳定且可微的碰撞概率估计，从而将经典距离场与现代隐式表示联系起来。这些连续距离度量被纳入控制屏障函数中，从而形成一个统一的感知-动作耦合框架，支持响应场景变化的平滑且可靠的实时运动生成。在仿真、物理机器人以及人机共享工作空间中的实验验证表明了该框架的有效性，在不确定和动态环境中实现了集成的场景重建和反应式控制。

## Abstract
Robotic manipulators excel in structured environments but face substantial challenges in unstructured and dynamic settings. This paper presents SplatCtrl, a unified framework for real-time scene reconstruction and reactive robot motion generation to enable collision-free robotic arm control in previously unseen and continuously changing environments. Building on 3D Gaussian Splatting (3D-GS), we introduce a hybrid voxel-based filtering and dynamic Gaussian relocation strategy that supports efficient scene reconstruction from RGB-D streams while accommodating environmental changes. For safe and reactive control, we further propose a method for deriving continuous signed distance functions from isotropic Gaussians, providing stable and differentiable collision probability estimates that bridge classical distance fields with the modern implicit representation. These continuous distance metrics are incorporated into control barrier functions, resulting in a unified perception-action coupling framework that supports smooth and reliable real-time motion generation in response to scene changes. Experimental validation in simulation, on physical robot, and within shared human-robot workspace demonstrates the framework's effectiveness, achieving integrated scene reconstruction and reactive control in uncertain, and dynamic environments.

---

## 论文详细总结（自动生成）

这是一份关于论文《SplatCtrl: Perception-Action Coupling via Gaussian Scene Representations and Reactive Robot Control》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的机器人操作在结构化环境中表现优异，但在非结构化、动态且未知的环境中，如何实现**实时场景重建**与**反应式避障控制**的无缝耦合（Perception-Action Coupling）是一个重大挑战。
*   **研究动机**：
    *   现有的点云或体素表示在处理遮挡和时间一致性方面存在不足。
    *   神经辐射场（NeRF）虽然精度高，但计算开销大，难以满足实时控制需求。
    *   3D 高斯泼溅（3D-GS）虽渲染快，但缺乏物理一致性，且难以直接用于距离计算和机器人控制。

### 2. 方法论
SplatCtrl 框架通过以下关键技术实现了感知与动作的深度集成：
*   **改进的 3D 高斯场景表示**：
    *   采用**各向同性高斯（Isotropic Gaussians）**以简化距离计算。
    *   引入**混合体素滤波与动态重定位策略**：利用 RGB-D 流实时更新高斯点，通过深度误差掩码（Madd）识别新物体，并利用 GPU 加速的体素占据网格过滤伪影和冗余点。
*   **连续符号距离函数（SDF）推导**：
    *   将高斯过程距离场（GPDF）扩展到带半径的高斯球体。
    *   通过**高斯过程（GP）建模占据场**，推导出可微的连续 SDF 及其梯度。
    *   引入数值稳定性技巧（处理对数项）和质量块矩阵近似（Mass-lumped approximation），将计算复杂度从 $O(n^3)$ 降低到线性复杂度。
*   **反应式机器人控制**：
    *   将推导出的 SDF 作为**控制屏障函数（CBF）**集成到二次规划逆运动学（QP-IK）控制器中。
    *   控制器在保证末端执行器跟踪任务的同时，通过 SDF 梯度实时产生排斥力，确保与动态障碍物保持安全距离。

### 3. 实验设计
*   **实验场景**：
    1.  **场景重建质量评估**：使用 DTU MVS 数据集，对比 3D-GS。
    2.  **仿真机器人实验**：在 Tabletop 和 Cubby 仿真环境中进行 942 次试验，对比 3D-GS + 采样规划器（BiRRT）。
    3.  **物理机器人实验**：使用 Franka Emika Panda FR3 机械臂，在 12 个包含不同几何形状、颜色和布局的未知环境中进行避障抓取任务。
    4.  **人机协作研究**：在动态人机共享空间中进行用户研究（8 名参与者），对比工业标准的“安全监控停止（SMS）”方法。
*   **基准（Benchmark）与对比方法**：
    *   重建基准：PSNR（峰值信噪比）。
    *   控制基准：成功率、路径长度、最小避障距离、任务完成时间。
    *   对比方法：原始 3D-GS、BiRRT 运动规划器、ISO 标准的 SMS 停机策略。

### 4. 资源与算力
*   **算力说明**：文中未明确给出具体的 GPU 型号（如 RTX 4090 等），但强调了其实时性表现。
*   **运行效率**：
    *   单视图高斯拟合迭代频率约为 **240 Hz**。
    *   QP 控制器求解频率 **> 100 Hz**。
    *   通过 GPU 加速的体素网格和哈希表处理，确保了低延迟的感知-动作循环。

### 5. 实验数量与充分性
*   **实验规模**：
    *   仿真实验共计 **942 组**，涵盖了不同相机数量（2-6 个）的消融研究，样本量充足。
    *   物理机器人实验在 **12 个完全不同**的真实场景中重复多次，具有较强的说服力。
    *   用户研究包含 **8 名**无经验志愿者，提供了定性与定量的双重反馈。
*   **充分性评价**：实验设计非常全面，不仅涵盖了静态重建精度，还深入探讨了动态避障、多视角鲁棒性以及真实人类干扰下的安全性，实验结果客观且具有统计意义。

### 6. 主要结论与发现
*   **重建与规划**：SplatCtrl 在有限的高斯预算下，重建质量（PSNR 23.33）略优于 3D-GS，且在运动规划成功率上远超基准（平均成功率 >95% vs 基准 <50%），有效减少了导致误碰撞的伪影。
*   **反应式控制**：相比于离线规划，QP-IK 结合 SDF 能产生更平滑、末端误差更小的轨迹，并能保持更稳定的安全距离。
*   **人机协作**：在动态环境中，SplatCtrl 允许机器人在避开人类的同时继续工作，相比于“进入即停机”的 SMS 策略，显著提高了任务效率并保持了更高的平均安全距离。

### 7. 优点与亮点
*   **无需预训练**：不依赖大规模数据集或离线训练，直接从原始 RGB-D 输入实现零样本（Zero-shot）部署。
*   **理论创新**：成功将高斯过程距离场（GPDF）与 3D 高斯表示结合，解决了隐式表示在机器人控制中难以提取稳定梯度的问题。
*   **端到端实时性**：实现了从感知到动作的闭环，且频率足以应对快速移动的物体。

### 8. 不足与局限
*   **超参数敏感性**：框架性能依赖于高斯数量上限、体素大小等超参数的调优。
*   **几何近似偏差**：将高斯简化为各向同性球体虽然提升了速度，但忽略了高斯分布的完整概率特性，可能在处理极扁平物体时存在精度损失。
*   **抓取物避障**：目前尚未将机器人抓持的物体显式纳入避障模型中（即未考虑物体-环境碰撞）。
*   **场景规模**：目前主要集中在桌面级操作，在大规模环境（如移动机器人导航）中的扩展性有待验证。

（完）
