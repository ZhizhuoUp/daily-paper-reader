---
title: Online Modeling and Sequential Convex Programming for Lunar Landing Trajectory Optimization
title_zh: 月球着陆轨迹优化的在线建模与序列凸规划
authors: Zhenbo Wang
date: 2026-07-02
pdf: "https://arxiv.org/pdf/2607.02750v1"
tags: ["query:profile-1"]
score: 9.0
evidence: 用于月球着陆轨迹优化的实时序列凸规划
tldr: 本文提出了一种结合序列凸规划（SCP）与实时在线模型辨识的月球动力下降着陆制导框架。研究通过变量替换和二阶锥松弛将非凸能量最优问题转化为凸优化序列，并利用带遗忘因子的递归最小二乘法实时估计重力、推力和质量扰动。该方法不仅在理论上证明了松弛无损性和算法收敛性，还通过仿真验证了其在减少着陆误差和精确跟踪燃料消耗方面的优越性，兼顾了实时性与鲁棒性。
selection_source: fresh_fetch
motivation: 针对月球着陆中环境扰动和模型参数不确定性导致的制导精度下降问题，寻求一种实时且鲁棒的轨迹优化方案。
method: 采用序列凸规划处理非凸约束，并集成递归最小二乘滤波器进行在线参数辨识，以实时修正动力学模型中的偏差项。
result: 数值仿真显示，该算法在多种复杂扰动场景下均能有效降低位置与速度误差，并实现更精准的推进剂消耗预测。
conclusion: 该研究成功构建了一个兼具理论收敛保证与工程实用性的在线制导架构，显著提升了月球着陆任务的可靠性与精度。
---

## 摘要
本文提出了一种用于月球动力下降与着陆的制导框架，该框架将序列凸规划（SCP）与实时在线模型辨识相结合。首先建立了一个非凸能量最优着陆问题，随后通过变量代换、逐次线性化以及推力方向约束的无损二阶锥松弛，将其重新表述为一系列凸二阶锥规划（SOCP）。在线辨识层由具有指数遗忘因子的递归最小二乘（RLS）滤波器和指数移动平均（EMA）平滑器构建，用于从含有噪声的导航测量值中估计未知的重力、推力缩放和质量测量扰动，并在每个制导周期将修正后的偏置项注入每个凸子问题的动力学约束中。基于该架构及作者在该领域的先前工作，本文开发了一种基准 SCP 算法和一种具有模型辨识的收缩地平线在线 SCP 算法。此外，本文还探讨了一些理论基础，确立了凸松弛的无损性、辨识滤波器的均方稳定性与收敛性、SCP 迭代的收敛性保证，以及明确的收敛半径和收敛速度结果。在 MATLAB 中使用 YALMIP 和 ECOS 求解器，针对四种复杂度递增的扰动场景进行了数值模拟。结果表明，与未修正的名义轨迹相比，所提出的在线算法能够持续减小着陆位置和速度误差，并能更好地跟踪真实的推进剂消耗，同时保留了凸优化可预测的收敛性和实时计算特性。

## Abstract
This paper presents a guidance framework for lunar powered descent and landing that combines sequential convex programming (SCP) with real-time online model identification. A nonconvex energy-optimal landing problem is developed and then reformulated into a sequence of convex second-order cone programs (SOCPs) through a change of variables, successive linearization, and a lossless second-order cone relaxation of the thrust direction constraint. An online identification layer, built from a recursive least squares (RLS) filter with exponential forgetting and an exponential moving average (EMA) smoother, estimates unknown gravitational, thrust-scale, and mass-gauging perturbations from noisy navigation measurements and injects a corrected bias term into the dynamics constraint of each convex subproblem at every guidance cycle. Building on this architecture and my prior work in this area, a baseline SCP algorithm and a receding-horizon online SCP algorithm with model identification are developed. Also, I try to explore some theoretical foundations, establishing the losslessness of the convex relaxation, the mean-square stability and convergence of the identification filters, the guaranteed convergence of the SCP iteration, and explicit convergence radius and convergence rate results. Numerical simulations across four perturbation scenarios of increasing complexity are implemented in MATLAB using YALMIP and the ECOS solver. The results show that the proposed online algorithm consistently reduces landing position and velocity error and better tracks the true propellant consumption relative to an uncorrected nominal trajectory, while retaining the predictable convergence and real-time computational properties of convex optimization.

---

## 论文详细总结（自动生成）

以下是对论文《Online Modeling and Sequential Convex Programming for Lunar Landing Trajectory Optimization》（月球着陆轨迹优化的在线建模与序列凸规划）的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
月球着陆是航天任务中最具挑战性的环节之一。由于月球缺乏大气，减速完全依赖动力推进，这对燃料效率和制导精度提出了极高要求。
*   **研究动机**：传统的轨迹优化方法（如间接法、直接法）在实时性、收敛性或处理非凸约束方面存在局限。更重要的是，现有的凸优化制导算法大多依赖预先设定的“名义模型”，但在实际飞行中，月球重力异常（质量瘤）、推力器校准偏差和推进剂测量误差会导致实际动力学与名义模型不符，进而引发着陆精度下降甚至任务失败。
*   **核心问题**：如何构建一个既能保证实时收敛和约束满足，又能在线识别并补偿环境/系统扰动的闭环制导框架。

### 2. 方法论
论文提出了一个结合**在线模型辨识层**与**序列凸规划（SCP）层**的双层架构：
*   **核心思想**：通过变量替换和凸松弛将非凸问题转化为二阶锥规划（SOCP）序列，并利用实时导航数据在线估计动力学偏差，将其作为修正项注入优化问题的约束中。
*   **关键技术细节**：
    1.  **凸化处理**：引入对数质量 $w = \ln m$ 和比推力 $\tau = T/m$ 进行变量代换，将动力学方程线性化；对推力方向约束进行二阶锥松弛（证明了其在能量最优准则下是无损的）；对推力-质量比约束进行逐次线性化。
    2.  **在线辨识**：采用带指数遗忘因子的**递归最小二乘（RLS）滤波器**捕捉时变扰动，并配合**指数移动平均（EMA）平滑器**抑制导航噪声。
    3.  **在线 SCP 算法**：采用收缩地平线（Receding-horizon）策略，每隔一定周期（n_replan）重新求解优化问题，初始状态更新为当前测量值，动力学偏置项更新为辨识出的扰动值。
*   **理论保证**：论文严谨地证明了凸松弛的无损性、辨识滤波器的均方稳定性、SCP 迭代的收敛半径以及在扰动估计误差存在下的收敛特性。

### 3. 实验设计
*   **实验环境**：在 MATLAB 环境下使用 YALMIP 建模，并调用 ECOS 求解器进行 SOCP 求解。
*   **对比基准（Benchmark）**：未经过在线修正的“名义 SCP 轨迹”（Nominal Plan）。
*   **测试场景**：设计了四种复杂度递增的扰动场景：
    1.  **场景 1**：恒定重力异常（模拟质量瘤偏置）。
    2.  **场景 2**：正弦时变重力扰动（模拟空间变化的重力场）。
    3.  **场景 3**：质量测量误差与推力缩放误差（状态/控制相关的扰动）。
    4.  **场景 4**：上述所有扰动的综合叠加（极端压力测试）。

### 4. 资源与算力
*   **算力说明**：论文未明确提及具体的硬件型号（如 CPU/GPU 频率），但强调了算法的**实时计算特性**。
*   **计算效率**：由于采用了 SOCP 形式，子问题的求解速度极快。实验中设置了固定的规划节点数（$N_{plan}=30$），确保了在嵌入式航天计算机上的计算时间是可预测且有界的。

### 5. 实验数量与充分性
*   **实验规模**：针对四种主要场景进行了详细的数值仿真，每组仿真均包含了位置、速度、质量、推力剖面以及扰动估计曲线的对比。
*   **充分性评价**：实验设计较为充分。作者不仅验证了算法在简单偏置下的表现，还通过场景 4 验证了算法处理复合、非线性扰动的能力。此外，仿真中加入了符合实际的导航测量噪声（高斯白噪声），增强了实验的客观性和说服力。

### 6. 主要结论与发现
*   **精度提升**：在所有扰动场景下，在线 SCP 算法均显著降低了着陆位置和速度误差。
*   **燃料跟踪**：在线算法能更准确地预测和跟踪真实的推进剂消耗，避免了名义模型过于乐观的估计。
*   **收敛性验证**：SCP 迭代通常在 2-4 次内即可收敛，满足实时制导的需求。
*   **鲁棒性**：即使在扰动模型未知且随时间变化的情况下，RLS+EMA 滤波器也能有效提取扰动趋势，使制导指令及时做出补偿。

### 7. 优点（亮点）
*   **理论与工程结合**：不仅提出了算法，还提供了详尽的数学证明（如收敛半径和无损松弛），这在制导领域至关重要。
*   **架构解耦**：辨识层与优化层相互独立，辨识结果仅作为线性约束的偏置项输入，不改变 SOCP 的凸性结构，保证了求解的可靠性。
*   **适应性强**：无需预知扰动的物理模型（如重力场模型），仅凭残差即可实现黑盒补偿。

### 8. 不足与局限
*   **固定时长限制**：实验中假设飞行时间（Time-of-flight）是固定的，但在实际任务中，最优飞行时间往往也是需要优化的变量。
*   **3-DOF 简化**：虽然是 3D 空间，但模型主要基于质点动力学（3 自由度），未深入探讨姿态动力学（6 自由度）与推力矢量控制的耦合影响。
*   **性能上限**：算法的最终精度受限于导航系统的噪声水平和滤波器的滞后效应，在极高频扰动下的表现尚待验证。

（完）
