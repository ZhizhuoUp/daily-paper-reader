---
title: Anytime Plug-and-Play Control with Contract-Based Distributed MPC
title_zh: 基于契约式分布式 MPC 的随时即插即用控制
authors: "Sabrina Bodmer, Danilo Saccani, Melanie N. Zeilinger, Andrea Carron"
date: 2026-07-05
pdf: "https://arxiv.org/pdf/2607.04215v1"
tags: ["query:q1"]
score: 8.0
evidence: 分布式MPC保证多智能体场景下的避障
tldr: 本研究针对移动多机器人系统中通信拓扑随时间变化的挑战，提出了一种基于合同的分布式模型预测控制（MPC）算法。该方法支持机器人在无需中心化协调的情况下随时加入或退出网络。通过基于距离的邻居定义和预测轨迹生成的合同约束，确保了避障和约束满足。实验在自动驾驶场景中验证了其在高速动态环境下的有效性。
selection_source: fresh_fetch
motivation: 针对多机器人系统中通信拓扑动态变化且缺乏中心化协调的控制难题，寻求一种支持机器人随时加入或离开的方案。
method: 提出一种基于局部通信的分布式MPC算法，利用距离定义邻居，并通过预测轨迹生成的合同约束来保证安全性。
result: 在仿真和真实世界的多车自动驾驶实验中，成功实现了高速动态环境下的有效避障和约束满足。
conclusion: 该方法实现了高效、可扩展的即插即用分布式控制，为动态拓扑下的多机器人协作提供了安全保障。
---

## 摘要
在许多移动多机器人应用中，一个核心挑战是通信拓扑本质上是随时间变化的。智能体可能会进入或退出网络，且此类变化通常无法预先限制。本研究提出了一种基于局部通信的分布式多智能体控制算法，支持智能体在无需集中协调的情况下随时加入和离开通信网络。该方法通过依赖基于距离的邻居定义以及源自预测轨迹的契约，能够随智能体数量高效扩展。由此产生的契约约束保证了避障和约束满足。我们在自动驾驶多智能体场景中验证了所提方法，在仿真和真实实验中展示了在智能体反向运动的高速动态环境下有效的避障性能。

## Abstract
A central challenge in many mobile multi-robot applications is that communication topologies are inherently time-varying. Agents may enter or exit the network and such changes cannot generally be restricted a priori. This work introduces a distributed multi-agent control algorithm based on local communication that supports anytime agent joining and leaving the communication network without centralized coordination. The method scales efficiently with the number of agents by relying on a distance-based neighbor definition and on contracts derived from predicted trajectories. The resulting contract constraints guarantee collision avoidance and constraint satisfaction. We validate the proposed method in an autonomous multi-agent driving scenario, demonstrating effective collision avoidance in high-speed, dynamic environments with agents moving in opposite directions, in both simulated and real-world experiments.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种针对多机器人系统的分布式模型预测控制（DMPC）框架，重点解决了通信拓扑动态变化时的安全协调问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：在现实世界的多机器人应用（如无人机群、自动驾驶车队）中，通信网络是动态变化的。现有的分布式控制方法大多假设网络拓扑固定，或者采用“基于请求”的即插即用（PnP）机制（即智能体加入/退出需经过网络批准）。
*   **核心问题**：如何实现“随时即插即用”（Anytime PnP），允许智能体在没有任何中心化协调或预先批准的情况下，随时自由加入或离开网络，同时在数学上严格保证避障安全性和递归可行性。

### 2. 方法论
该方法的核心是通过**契约（Contract）机制**将耦合的避障约束解耦为局部约束。
*   **核心思想**：每个智能体基于局部邻居的预测轨迹构建契约，契约由“细胞（Cells）”和“安全包络（Safety Envelopes）”组成。
*   **关键技术细节**：
    *   **时变细胞（Cells）**：利用类似 Voronoi 分割的超平面，将当前邻居在预测时域的每一阶段进行空间分割，确保邻居间互不碰撞。
    *   **感知集（Awareness Sets）**：定义一个比通信范围略小的区域。如果非邻居智能体都在各自的感知集内，则数学上可证明它们之间不会发生碰撞。
    *   **安全包络（Safety Envelopes）**：这是保证“随时性”的关键。通过限制预测轨迹中相邻步长间的位移增量，确保即使在拓扑切换时，移位后的轨迹残余部分仍留在感知集内，从而维持递归可行性。
    *   **多轨迹 MPC（mt-MPCC）**：为了兼顾安全与性能，算法同时优化两条轨迹：一条是满足所有契约约束的“安全轨迹”，另一条是追求高速驾驶的“开发轨迹”。两者共享第一个控制输入，以确保实际执行的动作始终安全。

### 3. 实验设计
*   **实验场景**：采用“数字 8 字型”赛道（Figure-eight track）。该场景极具挑战性，因为包含交叉路口、弯道以及频繁的对向会车（Oncoming traffic）。
*   **Benchmark 与对比**：
    *   **对比维度**：论文在讨论部分将该方法与全局协调法（优先级、领导者-跟随者）、基于约束的方法（安全管柱、一致性约束）以及特定交通规则方法进行了定性对比（见表 I）。
    *   **实验对象**：1/28 比例的微型赛车（硬件平台为 CRS 框架）。
*   **实验设置**：仿真中使用了多达 8 辆车，硬件实验使用了 2 到 6 辆车。

### 4. 资源与算力
*   **硬件环境**：计算在配备 Intel Core i9-13900KS CPU（3.20 GHz）和 48 GB RAM 的 PC 上运行。
*   **软件框架**：使用 `acados` 求解器，采用 SQP-RTI 算法。
*   **算力需求**：MPC 运行频率为 20Hz（采样时间 0.05s），预测时域 $N=15$。文中未提到使用 GPU，这符合此类数值优化控制算法通常依赖高性能 CPU 的特征。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **仿真**：展示了 8 辆车在复杂交互（超车、路口避让、对向避障）下的表现。
    *   **硬件**：进行了多组实验，重点展示了 6 辆车在 8 字赛道上的实时运行数据。
*   **充分性评价**：实验设计较为充分。作者不仅提供了定性的视觉轨迹图，还提供了定量的最小两两距离分析和邻居数量变化图，证明了在拓扑频繁变化（邻居数从 0 到 5 波动）时系统的稳定性。实验涵盖了从仿真到真实硬件的迁移，验证了算法的实时性和鲁棒性。

### 6. 主要结论与发现
*   **安全性保证**：理论证明并实验验证了该方法在“随时即插即用”条件下能严格保证避障和局部约束满足。
*   **无需中心化**：系统完全依赖邻居间的局部通信，无需任何全局控制器或预协商过程。
*   **高性能驾驶**：通过多轨迹架构，智能体在保证安全的前提下，能够执行激进的超车动作和高速对向避让。

### 7. 优点
*   **理论严谨**：提供了完整的递归可行性和安全性证明，这在动态拓扑控制中非常难得。
*   **灵活性极高**：真正实现了“随时”加入/退出，不产生额外的协调开销。
*   **硬件验证**：在高速、动态的微型赛车平台上得到了验证，证明了算法在实际物理系统中的可行性。

### 8. 不足与局限
*   **保守性**：安全包络通过限制每一步的位移增量来保证安全，这在一定程度上限制了车辆的最高速度或加速度（即为了安全牺牲了部分性能）。
*   **通信依赖**：虽然不需要中心化协调，但仍依赖邻居间可靠的预测轨迹交换。如果通信存在高延迟或丢包，安全性证明可能会失效。
*   **环境假设**：目前主要针对静态边界和动态智能体，对于复杂的非凸障碍物环境，细胞分割的计算可能会变得复杂。

（完）
