---
title: End-to-End Learning of Safe Optimal Feedback Control in High Dimensions with Control Barrier Function Layers
title_zh: 高维环境下带有控制屏障函数层的安全最优反馈控制端到端学习
authors: "Xingjian Li, Kelvin Kan, Deepanshu Verma, Krishna Kumar, Stanley Osher, Samy Wu Fung"
date: 2026-07-22
pdf: "https://arxiv.org/pdf/2607.20674v1"
tags: ["query:q2"]
score: 8.0
evidence: 带有控制屏障函数层的安全最优反馈控制
tldr: 本研究针对高维非线性系统的安全最优反馈控制问题，提出了一种结合控制屏障函数（CBF）的端到端学习框架。针对传统基于二次规划（QP）的安全性过滤器在处理高维系统时存在的计算和微分瓶颈，本文创新性地结合了算子分裂法与雅可比自由反向传播（JFB）技术，实现了可扩展的端到端训练。该方法在保证硬安全约束的同时，成功应用于状态维度高达1200维的多智能体控制任务，显著提升了安全强化学习在大规模系统中的适用性。
selection_source: fresh_fetch
motivation: 传统的基于CBF的端到端策略训练受限于QP优化层的计算和微分效率，难以扩展到16维以上的复杂高维系统。
method: 提出一种结合算子分裂与雅可比自由反向传播（JFB）的优化层，以实现高效且可微分的高维安全过滤。
result: 在状态维度达1200、控制维度达400的高维多智能体非线性控制问题上验证了该方法的有效性和扩展性。
conclusion: 该研究通过技术创新克服了高维安全控制的学习瓶颈，为大规模复杂系统的安全自主控制提供了理论支撑和实践方案。
---

## 摘要
我们研究了在控制屏障函数（CBF）强制执行的硬安全约束下，学习高维半全局反馈控制器的问题。将 CBF 纳入端到端策略训练需要将基于二次规划的安全过滤器嵌入为优化层，但计算和微分瓶颈在很大程度上限制了以往方法在低维系统中的应用，通常状态维度最高仅为 16 维。我们通过将算子分裂与最近开发的无雅可比反向传播（JFB）方法相结合来解决这一局限性，从而实现可扩展的端到端训练，同时通过 CBF 安全过滤器保留硬安全保证。我们利用非光滑分析技术从理论上证明了该训练方法的合理性，并在状态和控制维度分别高达 1200 和 400 的高维多智能体非线性控制问题上证明了其有效性。

## Abstract
We consider the problem of learning high-dimensional semi-global feedback controllers under hard safety constraints enforced by control barrier functions (CBFs). Incorporating CBFs into end-to-end policy training requires embedding a quadratic-program-based safety filter as an optimization layer, but computational and differentiation bottlenecks have largely restricted prior approaches to low-dimensional systems, typically with at most 16 state dimensions. We address this limitation by combining operator splitting with the recently developed Jacobian-Free Backpropagation (JFB) method to enable scalable end-to-end training while preserving hard safety guarantees through the CBF safety filter. We justify this training methodology theoretically using nonsmooth analysis techniques and demonstrate its effectiveness on high-dimensional multi-agent nonlinear control problems with state and control dimensions up to 1200 and 400, respectively.

---

## 论文详细总结（自动生成）

这篇论文由德克萨斯大学奥斯汀分校、加州大学洛杉矶分校（UCLA）等机构的研究人员合作完成，旨在解决高维非线性系统在硬安全约束下的端到端反馈控制学习问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在保证**硬安全约束**（即绝对不违反约束）的前提下，为**高维**（成百上千个状态变量）非线性系统学习最优反馈控制器。
*   **研究背景**：传统的安全控制通常使用控制屏障函数（CBF）通过二次规划（QP）来过滤控制指令。然而，将 QP 作为神经网络的一个层进行端到端训练时，面临严重的**计算和微分瓶颈**。
*   **动机**：现有的微分优化层方法（如 CVXPY Layers）在状态维度超过 16 维时往往因内存爆炸或计算过慢而失效。本文旨在打破这一维度限制，实现大规模多智能体系统的安全控制学习。

### 2. 方法论
*   **核心思想**：将安全过滤器的求解过程转化为定点迭代，并利用近似梯度技术绕过复杂的隐函数求导。
*   **关键技术细节**：
    *   **Davis–Yin 算子分裂 (DYS)**：将复杂的 CBF-QP 问题分解为两个简单的投影子问题：一个是投影到非负松弛变量集（简单阈值处理），另一个是投影到仿射子空间（线性代数运算）。这使得高维 QP 的求解变得极其高效。
    *   **雅可比自由反向传播 (JFB)**：在反向传播时，JFB 避免了计算和求逆大规模 KKT 系统的雅可比矩阵，而是直接利用定点迭代的最后一步进行梯度近似。这将其计算复杂度从 $O(n^3)$ 降低到 $O(n)$。
    *   **非光滑收敛分析**：利用 Clarke 广义雅可比理论，证明了即使在投影层引入非光滑性的情况下，该训练方案在梯度流下仍能收敛到 Clarke 驻点。
*   **算法流程**：在每个训练步，首先通过 DYS 迭代得到安全控制量，然后在反向传播中使用 JFB 更新策略网络参数，确保控制器在学习过程中就能“感知”到下游安全过滤器的存在。

### 3. 实验设计
*   **实验场景**：
    1.  **三维单积分器 (Single Integrator)**：50 个智能体避障。
    2.  **二维双积分器 (Double Integrator)**：1 个和 6 个智能体在复杂障碍物中穿行。
    3.  **三维四旋翼无人机 (Quadcopter)**：5 个、30 个及 100 个无人机的集群控制与避障。
*   **对比方法 (Benchmarks)**：
    *   **DYS-AD**：使用自动微分（Unrolling）硬拆解 DYS 迭代过程。
    *   **CVXPYLayers (IFT)**：基于隐函数定理的标准微分优化层方法。
    *   **CVXPYLayers (IFT + reg.)**：增加了正则化以提高稳定性的版本。

### 4. 资源与算力
*   **硬件环境**：使用单张 **NVIDIA RTX 5090 GPU**（显存限制在 16GB）和 AMD Ryzen PRO 7975WX CPU。
*   **训练细节**：设置了 50 个时间步的轨迹滚动（Horizon），Batch Size 为 32。
*   **算力表现**：论文强调了其方法的内存优势，例如在 100 个无人机任务中，其他方法均因显存溢出（超过 16GB）而失败，而本文方法能稳定运行。

### 5. 实验数量与充分性
*   **实验规模**：共进行了 6 组主要控制任务实验，涵盖了从低维到极高维（状态维度达 1200，控制维度达 400）的跨度。
*   **充分性评价**：实验设计非常充分。不仅对比了运行成本和终端误差，还详细对比了**峰值 CPU/GPU 内存占用**和**每轮训练时间**。此外，还通过数值实验验证了梯度方向的正确性（Descent Direction）和轨迹的安全性（Barrier Values 始终大于 0）。

### 6. 主要结论与发现
*   **可扩展性**：该方法是目前唯一能处理 1200 维状态空间且带硬安全约束的端到端学习框架。
*   **效率提升**：相比于传统的隐函数微分方法，内存占用降低了多达 **10 倍**，训练速度显著加快。
*   **稳定性**：JFB 避免了 KKT 系统求逆带来的数值不稳定问题，在复杂动力学（如四旋翼）下表现出更强的鲁棒性。
*   **安全性**：训练后的控制器在测试中能完美遵守安全约束，且轨迹比单纯的惩罚项方法更平滑。

### 7. 优点
*   **理论与实践结合**：不仅提出了高效算法，还给出了严谨的非光滑收敛证明。
*   **突破维度瓶颈**：将安全控制的学习规模从“玩具级”提升到了“实用集群级”。
*   **模块化强**：DYS-JFB 框架可以轻松集成到现有的深度学习流水线中，代码改动极小。

### 8. 不足与局限
*   **隐式网络挑战**：尽管 JFB 提高了效率，但隐式神经网络的训练本质上仍比显式网络更敏感，需要精细调节超参数（如松弛变量的惩罚权重）。
*   **架构依赖**：实验主要基于 MLP 和 ResNet，对于更复杂的对称性架构（如处理多智能体置换不变性的 GNN）探讨较少。
*   **局部最优风险**：作为一种基于梯度的非凸优化方法，虽然证明了收敛到驻点，但仍可能陷入局部最优，导致在极复杂障碍物环境下的成功率受限。

（完）
