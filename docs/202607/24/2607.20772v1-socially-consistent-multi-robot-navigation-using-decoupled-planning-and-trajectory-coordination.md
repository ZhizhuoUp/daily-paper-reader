---
title: Socially Consistent Multi-Robot Navigation Using Decoupled Planning and Trajectory Coordination
title_zh: 基于解耦规划与轨迹协调的社会一致性多机器人导航
authors: "Matthew M. Sato, Kincho H. Law"
date: 2026-07-22
pdf: "https://arxiv.org/pdf/2607.20772v1"
tags: ["query:q1"]
score: 8.0
evidence: 采用解耦规划和轨迹协调的多机器人安全导航
tldr: "本研究针对移动机器人在人类环境中的导航问题，提出了一种解耦全局路径规划与轨迹协调的部分去中心化框架。通过在改进的A*算法中嵌入宏观社会规范，并构建共享的社会路径图来增强行为的可预测性和一致性。随后，利用混合整数凸规划实现高效的冲突消解。该方法不仅提高了机器人的社会合规性，还简化了大规模集群的协调复杂度。"
selection_source: fresh_fetch
motivation: 现有的局部避障算法缺乏长期社会一致性，导致机器人行为不可预测且在复杂环境中表现出低效的局部反应性。
method: "提出一种将社会规范嵌入A*代价函数的路径规划方法，并结合混合整数凸规划（MICP）进行多机器人轨迹协调。"
result: 实验证明该框架能生成符合社会规范且高度可预测的路径，并能有效扩展至大规模机器人集群和动态任务分配。
conclusion: 通过在规划阶段引入社会一致性约束，可以显著提升导航的可预测性并简化多机器人协调的复杂难题。
---

## 摘要
移动机器人成功融入以人为中心的环境，要求导航不仅安全高效，而且具有可预测性并符合社会惯例，这是提升人类舒适度和接受度的关键前提。虽然已有大量研究探讨了短期的社会感知规划，但这些方法往往缺乏确保长时域内行为一致性和可预测性的机制。若缺乏具备社会意识的长期规划器，局部规划器将负担过重，导致低效且局部反应式的运动，进而损害可预测性。本文提出了一种部分去中心化的框架，通过将全局路径规划与轨迹协调解耦，生成可预测且符合社会一致性的多机器人运动。首先，我们提出了一种改进的 A* 规划器，将宏观社会规范嵌入到规划器的代价函数中。规划路径在移动机器人之间共享，以协作构建既定路线的社会图，从而强化路径一致性并减少后续规划开销。其次，我们利用社会约束路径的涌现结构，将多机器人轨迹协调问题建模为混合整数凸规划。该凸规划能够高效计算无冲突轨迹，有效扩展至大规模集群并支持动态任务分配。研究结果表明，在路径规划阶段强化社会一致性可以产生可预测、符合社会规范的移动机器人路径，并简化了原本复杂的多机器人协调问题。

## Abstract
The successful integration of mobile robots in human-centric environments requires navigation that is not only safe and efficient, but also predictable and aligned with social conventions, key precursors for human comfort and acceptance. While significant research addresses short-term human-aware planning, these methods often lack mechanisms for ensuring consistent and predictable behaviors across long horizons. Without socially aware long-term planners, local planners are overburdened, resulting in inefficient and locally reactive movements that undermine predictability. This paper introduces a partially decentralized framework that generates predictable and socially consistent multi-robot motion by decoupling global path planning from trajectory coordination. First, we propose a modified A* planner that embeds macroscopic social norms into the planner cost function. Planned paths are shared across mobile robots to collaboratively build a social graph of established routes, which enforces path consistency and reduces future planning effort. Second, we leverage the emergent structure of the socially constrained paths to formulate the multi-robot trajectory coordination problem as a mixed-integer convex program. The convex program enables efficient computation of conflict- free trajectories, scaling effectively to large fleets and supporting dynamic task assignment. Our results demonstrate that enforcing social consistency at the path planning stage produces predictable, socially compliant mobile robot paths and simplifies the otherwise complex problem of multi-robot coordination.

---

## 论文详细总结（自动生成）

以下是对论文《Socially Consistent Multi-Robot Navigation Using Decoupled Planning and Trajectory Coordination》（基于解耦规划与轨迹协调的社会一致性多机器人导航）的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究动机**：随着自主移动机器人在餐厅、医院和机场等人类中心环境的普及，导航不仅要保证安全和高效，还必须具备**可预测性**并符合**社会规范**（如靠右行走）。
*   **核心挑战**：现有的社会感知导航多集中在局部规划（短时避障），缺乏长时域的全局一致性。这导致局部规划器负担过重，产生突兀、不可预测的动作，降低了人类的舒适感。此外，多机器人轨迹协调是一个计算复杂度随机器人数量指数增长的组合难题。
*   **整体含义**：本文提出一种解耦框架，认为社会规范不仅是约束，更是**简化协调问题的工具**。通过让机器人遵循一致的社会路径，可以将复杂的冲突空间压缩为可处理的结构化场景。

### 2. 方法论
该框架分为两个解耦的阶段：

#### A. 全局路径生成（社会感知 A* 与社会图）
*   **改进的 A* 算法**：在代价函数中嵌入宏观社会规范。
    *   **代价函数**：$c(x', x) = \|x - x'\|_2 + \phi(x', x)$。
    *   **社会成本 $\phi$**：包含“靠右行驶惩罚”、“保持与墙壁距离一致性惩罚”以及“在开阔区域预留对向通行缓冲区惩罚”。
*   **学习型社会图（Social Graph）**：
    *   机器人通过通信共享路径，协作构建一个有向子图。
    *   **机制**：通过热力图聚合路径，提取高频路径作为“社会干道”，并进行图剪枝以去除噪声。
    *   **优势**：后续规划会优先选择社会图中的路径，既保证了车队行为的一致性（像公路网一样可预测），又显著降低了搜索空间和计算耗时。

#### B. 去中心化轨迹协调（混合整数凸规划 MICP）
*   **核心思想**：将轨迹协调转化为在预定义路径上分配“到达时间”的问题。
*   **关键技术**：
    *   **事件路标点（Event Waypoints）**：仅在可能发生冲突的位置（交叉口、对向走廊、同向车道）设置优化变量，而非全路径离散化，极大减少了变量规模。
    *   **冲突窗口分类**：将冲突分为交叉（Intersection）、对向（Opposite）和同向（Same Direction）三类。
    *   **Big-M 法**：使用二元变量处理“谁先过”的逻辑决策，将非凸的避障约束转化为线性约束。
    *   **分布式计算**：通过机器人集群并行执行冲突检测，将 $O(R^2)$ 的计算压力分散到各个终端。

### 3. 实验设计
*   **实验场景**：
    1.  两个人工合成环境（包含走廊和房间结构）。
    2.  一个真实环境：斯坦福大学 Y2E2 大楼二层的占用网格图。
*   **对比基准（Benchmarks）**：
    *   **路径规划对比**：Vanilla A*（最短路径）、RRT*（随机采样）。
    *   **轨迹协调对比**：冲突搜索算法（CBS）、优先级规划（PP）。
*   **评估指标**：路径长度、靠右行驶距离、计算时间、成功率、完工时间（Makespan）。
*   **硬件验证**：使用搭载 NVIDIA Jetson Orin Nano 的定制差分驱动机器人进行实地测试。

### 4. 资源与算力
*   **算力设备**：
    *   **车载端**：NVIDIA Jetson Orin Nano (8GB)。
    *   **通信**：基于 Wi-Fi 的数据分发服务 (DDS/ROS2 常用协议)。
*   **训练/计算时长**：
    *   论文未涉及深度学习的大规模训练，但提到社会图是动态构建的。
    *   MICP 求解器使用嵌入式锥优化器（ECOS），在 10 台机器人规模下，求解时间通常在 1 秒左右。
    *   分布式冲突检测相比集中式检测，在硬件上实现了约 50%-80% 的加速。

### 5. 实验数量与充分性
*   **实验规模**：
    *   路径规划部分进行了 **500 组** 随机起终点测试。
    *   轨迹协调部分针对不同机器人数量（最高 10+）进行了 **25 组** 独立试验。
    *   社会图演化分析涵盖了从 0 到 250 条路径的生成过程。
*   **充分性评价**：实验设计较为充分。不仅有大规模的仿真数据支撑（证明统计学意义），还有真实的硬件实验（证明算法在通信延迟、定位噪声下的鲁棒性）。对比实验涵盖了经典的 MAPF 算法，具有较强的说服力。

### 6. 主要结论与发现
*   **社会规范简化了协调**：遵循社会规范的路径虽然比最短路径平均长约 10-15%，但其冲突点更集中、更易预测，使得 MICP 的成功率远高于 CBS 和 PP。
*   **社会图的增益**：一旦社会图形成，路径规划速度甚至超过了基础 A*，因为它将搜索限制在了高效的“干道”上。
*   **可扩展性**：通过分布式约束生成，轨迹协调的计算时间随机器人数量呈线性增长（而非集中式的平方增长），支持大规模车队。

### 7. 优点
*   **解耦设计巧妙**：将复杂的时空耦合问题拆分为“社会路径规划”和“时间分配”，降低了数学处理难度。
*   **实用性强**：考虑了真实世界的社会习俗，生成的路径更符合人类直觉，有利于人机共存。
*   **计算高效**：利用凸优化和分布式架构，使其能够运行在算力受限的嵌入式设备（Jetson Nano）上。

### 8. 不足与局限
*   **静态地图假设**：全局规划假设环境静态，虽然提到了处理临时障碍物的机制，但在极度拥挤、动态变化的环境中，频繁的重规划可能导致社会图失效。
*   **社会规则的局限性**：文中使用的“靠右”等规则是硬编码的（基于美国习惯），缺乏跨文化或特定场景（如左行国家）的自动适应能力。
*   **恒定速度假设**：MICP 假设机器人在路标点间匀速运动，未充分考虑复杂的动力学约束（如加速度限制），在高速运动场景下可能存在安全隐患。

（完）
