---
title: Distributed Model-Based Diffusion For Scalable Multi-Robot Trajectory Optimization
title_zh: 面向可扩展多机器人轨迹优化的分布式基于模型的扩散
authors: "Haejoon Lee, Xinyi Wang, Taekyung Kim, Dimitra Panagou"
date: 2026-07-23
pdf: "https://arxiv.org/pdf/2607.20992v1"
tags: ["query:q3"]
score: 9.0
evidence: 非凸环境下多机器人系统的分布式轨迹优化
tldr: 针对多机器人轨迹优化在复杂环境下的高维数灾难和中心化限制，本文提出分布式基于模型的扩散（DMBD）框架。该方法将全局逆向扩散过程分解为局部条件扩散，使各机器人能在自身控制子空间内独立去噪，并通过服务器聚合信息实现协同。实验证明 DMBD 具有极强的扩展性，能在亚秒级内解决复杂的协调任务，性能显著优于现有基准。
selection_source: fresh_fetch
motivation: 传统的基于模型的扩散方法在多机器人系统中面临维度灾难和中心化计算的局限，难以处理大规模复杂环境下的实时优化。
method: 提出一种分布式服务器-机器人框架，将逆向扩散过程分解为局部条件过程，使机器人能基于其他机器人的轨迹估计独立进行迭代去噪。
result: 在目标交换、多层覆盖和高峰时段等多种挑战性场景中，该方法实现了亚秒级的求解速度，且在扩展性上显著优于现有基准。
conclusion: DMBD 有效解决了多机器人轨迹优化的可扩展性问题，为复杂动态环境下的实时协同提供了高效的分布式采样优化方案。
---

## 摘要
多机器人系统的轨迹优化仍然是一个关键挑战，特别是在高度非凸、非线性和不可微的环境中导航时。虽然基于模型的扩散（MBD）最近已成为单机器人轨迹生成中一种极具前景的基于采样的优化范式，但将其扩展到多机器人系统会导致中心化的高维推理问题，该问题（i）由于维度灾难而面临样本效率低下的困境，且（ii）需要全局访问所有机器人的动力学、约束和目标。为解决这一问题，我们提出了分布式基于模型的扩散（DMBD），这是一种分布式服务器-机器人框架，它将反向扩散过程分解为局部的条件反向扩散过程。这种分解使每个机器人能够在自己的控制子空间内独立地迭代执行去噪，同时以服务器聚合并广播的其他机器人的当前轨迹估计为条件。在目标交换、多层覆盖、停车和高峰时段场景中的广泛仿真表明，DMBD 具有强大的可扩展性，能在亚秒级时间内解决许多具有挑战性的协调任务，并显著优于现有的基准方法。

## Abstract
Trajectory optimization for multi-robot systems remains a critical challenge, particularly when navigating highly non-convex, non-linear, and non-differentiable environments. While Model-Based Diffusion (MBD) has recently emerged as a promising sampling-based optimization paradigm for single-robot trajectory generation, extending it to multi-robot systems results in a centralized, high-dimensional inference problem that (i) suffers from poor sample efficiency due to the curse of dimensionality and (ii) requires global access to all robots' dynamics, constraints, and objectives. To address this, we propose Distributed Model-Based Diffusion (DMBD), a distributed server-robot framework that decomposes the reverse diffusion process into local conditional reverse diffusion processes. This decomposition enables each robot to iteratively perform denoising independently within its own control subspace while conditioning on the current trajectory estimates of the other robots that are aggregated and broadcast by the server. Extensive simulations in goal swapping, multi-floor coverage, parking, and rush-hour scenarios demonstrate that DMBD achieves strong scalability, solving many challenging coordination tasks in sub-seconds and significantly outperforming existing baselines.

---

## 论文详细总结（自动生成）

这是一份关于论文《Distributed Model-Based Diffusion For Scalable Multi-Robot Trajectory Optimization》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是**多机器人系统（MRS）在复杂、非凸且不可微环境下的轨迹优化问题**。
*   **研究动机**：传统的梯度优化方法易陷入局部最优，而采样优化（SBO）如“基于模型的扩散（MBD）”虽能处理多模态分布，但在多机器人场景下存在两大瓶颈：
    1.  **维度灾难**：联合轨迹空间的维度随机器人数量增加而爆炸，导致采样效率极低。
    2.  **中心化限制**：MBD 要求获取所有机器人的动力学、目标和约束，这在异构系统或隐私敏感场景下难以实现。
*   **核心目标**：提出一种分布式的扩散优化框架，在降低计算复杂度的同时，实现大规模机器人的高效协同。

### 2. 方法论：分布式基于模型的扩散（DMBD）
DMBD 采用“服务器-机器人”架构，将全局推理任务分解为并行的局部推理任务。
*   **核心思想**：将全局逆向扩散过程分解为**局部条件逆向扩散（Local Conditional Reverse Diffusion）**。每个机器人仅在自身的控制子空间内进行去噪，但以其他机器人的轨迹估计为“条件”。
*   **关键技术细节**：
    *   **局部代价函数**：每个机器人 $k$ 维护自己的代价函数 $\phi_k$，包含局部目标和与其相关的耦合约束（如避障）。
    *   **条件去噪**：在每一个去噪步 $i$ 中，机器人从服务器接收其他机器人的当前轨迹估计 $\tau_{-k}^{(i)}$，将其作为固定条件，通过采样估计局部得分函数（Score Function）。
    *   **通信机制**：每轮迭代中，机器人仅向服务器发送一条更新后的轨迹，服务器进行聚合后广播。相比其他分布式采样方法（需交换大量样本），DMBD 的通信开销极低。
    *   **理论支撑**：论文推导了全局得分函数与局部条件得分函数之间的差异界限，证明该差异取决于其他机器人轨迹估计的偏差及局部去噪器的 Lipschitz 常数。

### 3. 实验设计
论文设计了四个具有代表性的仿真场景来验证性能：
*   **场景设置**：
    1.  **Goal-Swap（目标交换）**：2-20 个机器人（圆形或矩形）在圆周上对向交换位置，测试扩展性。
    2.  **Multi-Floor Coverage（多层覆盖）**：异构机器人（大小不同）在两层楼间通过电梯协调移动。
    3.  **Parking（停车）**：两车在狭窄空间协调，其中一车需暂时让路，测试非凸协同能力。
    4.  **Rush Hour（高峰时段）**：类似华容道的拼图移动，测试高度受限环境下的逻辑协调。
*   **对比基准（Benchmarks）**：
    *   **CEM**（交叉熵方法）
    *   **MPPI**（模型预测路径积分）
    *   **MBD**（原始中心化扩散模型）
    *   **D4orm**（基于变形的扩散优化）

### 4. 资源与算力
*   **硬件环境**：12th Gen Intel Core i9-12900KF CPU, 64 GB RAM, **Nvidia RTX 3080 Ti GPU**。
*   **软件框架**：使用 **JAX** 进行开发，利用其 GPU 加速和并行处理能力。
*   **算力表现**：论文强调了实时性，DMBD 在处理 20 个机器人的复杂任务时，能在**亚秒级（sub-seconds）**内完成轨迹生成。

### 5. 实验数量与充分性
*   **实验规模**：每个场景和配置均进行了 **50 次独立试验**（使用不同随机种子）。
*   **充分性评价**：实验设计非常充分且具有挑战性。它不仅测试了机器人数量的增量（从 2 到 20），还涵盖了不同的动力学模型（双积分、自行车模型）、不同的几何形状（圆形、矩形）以及异构约束（电梯使用权限）。
*   **客观性**：通过成功率和规划时间两个硬指标进行量化对比，并展示了基准方法在大规模场景下因显存溢出（OOM）而失效的情况，对比客观公平。

### 6. 主要结论与发现
*   **极强的扩展性**：DMBD 的规划时间随机器人数量增长缓慢，而中心化方法呈指数级增长或直接崩溃。
*   **高效的协同**：即使机器人不知道彼此的目标和动力学，仅通过轨迹条件的迭代更新，也能自发产生复杂的协同行为（如主动让路）。
*   **优于基准**：在所有测试场景中，DMBD 的成功率均显著高于 CEM、MPPI 和 D4orm，尤其是在高维度和非光滑约束环境下。

### 7. 优点（亮点）
*   **降维打击**：通过分布式分解，将高维联合空间搜索转化为多个低维空间的并行搜索，解决了 SBO 的核心痛点。
*   **隐私与模块化**：机器人无需共享局部目标函数或动力学模型，符合实际工业部署需求。
*   **零阶优化**：不需要代价函数或动力学可微，能够处理包含 SAT（分离轴定理）碰撞检测等不可微逻辑的复杂任务。

### 8. 不足与局限
*   **理论假设**：理论误差界的推导依赖于局部去噪器的 Lipschitz 连续性假设，在极端非连续环境下可能失效。
*   **通信依赖**：虽然每轮通信量小，但仍需服务器进行同步广播，在完全去中心化（Ad-hoc 网络）环境下的表现尚未探讨。
*   **实机验证缺失**：目前所有实验均在仿真环境中完成，未考虑真实传感器噪声和通信延迟对扩散收敛性的影响。
*   **样本数敏感**：虽然比中心化方法高效，但作为采样方法，其性能仍受限于每步采样的粒子数。

（完）
