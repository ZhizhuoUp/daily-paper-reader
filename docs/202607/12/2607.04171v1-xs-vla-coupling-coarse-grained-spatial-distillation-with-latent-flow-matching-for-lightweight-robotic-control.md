---
title: "XS-VLA: Coupling Coarse-grained Spatial Distillation with Latent Flow Matching for Lightweight Robotic Control"
title_zh: XS-VLA：耦合粗粒度空间蒸馏与潜空间流匹配的轻量化机器人控制
authors: "Lei Iok Tong, Qingchen Xie, Wei Huang, Ying Jie Yap, Yujie Zhang, Qianzhi Li, Xiaolong Liu, Zhidong Deng"
date: 2026-07-05
pdf: "https://arxiv.org/pdf/2607.04171v1"
tags: ["query:q6"]
score: 8.0
evidence: 高效且具有空间感知的机器人操作
tldr: 针对大型视觉语言模型在机器人实时控制中计算开销大，而轻量级模型存在“空间盲区”的问题，本文提出了XS-VLA框架。该框架通过两阶段方法，首先将大模型的空间语义知识蒸馏至0.25B规模的轻量级骨干网络，随后结合潜空间流匹配策略来建模复杂的多模态动作分布。实验表明，XS-VLA在LIBERO基准测试中达到了0.5B参数以下模型的领先水平，显著提升了任务成功率和执行速度。
selection_source: fresh_fetch
motivation: 旨在解决轻量级视觉语言模型在机器人控制中空间感知能力弱以及难以处理复杂多模态动作分布的问题。
method: 采用两阶段框架，先通过粗粒度空间描述蒸馏提升轻量级骨干的空间感知，再利用CVAE与流匹配技术构建生成式动作策略。
result: "在LIBERO基准上，XS-VLA以仅0.25B的参数量超越了2.2B的基准模型，并在长程任务中实现了23%的性能提升。"
conclusion: 空间语义微调与生成式潜空间流控制的结合，能显著增强轻量级VLA模型的性能与实时执行效率。
---

## 摘要
大型视觉语言模型（LVLMs）展现出强大的多模态理解和空间定位能力，但其计算成本限制了实时机器人控制。相比之下，轻量化模型适用于边缘部署，但通常面临“空间盲区”问题，即原生空间预测能力较弱。在混合人类演示数据上训练视觉-语言-动作（VLA）模型，也可能因行为的高度多样性而导致策略性能下降。为了解决这些局限性，我们提出了 XS-VLA，这是一个用于高效且具备空间定位能力的机器人操纵的两阶段框架。首先，我们通过在精选的粗粒度空间描述上进行微调，将 Qwen3-VL-4B 的空间语义知识蒸馏到 SmolVLM2-0.25B 骨干网络中，从而将轻量化模型转变为具备空间定位能力的引擎。其次，我们利用这一增强后的骨干网络作为潜空间流匹配（Latent Flow Matching）策略的条件。与确定性控制器不同，我们的策略结合了条件变分自编码器（CVAE）与流匹配动力学，以建模复杂的多模态动作分布。在 LIBERO 基准测试中，XS-VLA 在参数量少于 0.5B 的模型中实现了最先进的性能。相较于 SmolVLA 0.25B 基线模型，其平均成功率提升了高达 7.2%，其中在 LIBERO-Long 任务上提升了 23%，并优于参数量更大的 2.2B 原始 SmolVLA。消融实验表明，空间微调和生成式潜空间流控制显著提升了轻量化 VLA 的性能，在任务执行速度上比之前的轻量化流匹配策略快了 3.2 倍。

## Abstract
Large Vision-Language Models (LVLMs) have shown strong multimodal understanding and spatial grounding, but their computational cost limits real-time robotic control. In contrast, lightweight models are suitable for edge deployment but often suffer from "spatial blindness", namely weak native spatial prediction ability. Training Vision-Language-Action (VLA) models on mixed human demonstrations can also degrade policy performance due to highly diverse behaviors. To address these limitations, we propose XS-VLA, a two-stage framework for efficient and spatially grounded robotic manipulation. First, we distill spatial semantic knowledge from Qwen3-VL-4B into the SmolVLM2-0.25B backbone by fine-tuning on curated coarse-grained spatial descriptions, turning the lightweight model into a spatially grounded engine. Second, we use this enhanced backbone to condition a Latent Flow Matching policy. Unlike deterministic controllers, our policy combines a Conditional Variational Autoencoder (CVAE) with Flow Matching dynamics to model complex multimodal action distributions. On the LIBERO benchmark, XS-VLA achieves state-of-the-art performance among models with fewer than 0.5B parameters. It improves average success rates by up to 7.2 percent, including a 23 percent gain on LIBERO-Long, over the SmolVLA 0.25B baseline, and outperforms the larger 2.2B vanilla SmolVLA. Ablations show that spatial tuning and generative latent flow control substantially improve lightweight VLA performance, delivering a 3.2 times speedup in mission execution over the previous lightweight flow matching policy.

---

## 论文详细总结（自动生成）

### 论文总结：XS-VLA —— 耦合粗粒度空间蒸馏与潜空间流匹配的轻量化机器人控制

#### 1. 核心问题与研究动机
*   **研究背景**：具身智能领域追求能理解复杂指令并执行任务的机器人。大型视觉语言模型（LVLMs）虽具备强大的空间推理能力，但因参数量巨大导致推理延迟高，无法满足机器人实时控制（10Hz-50Hz）的需求。
*   **核心问题**：
    1.  **空间盲区**：现有的轻量级视觉语言模型（如 <3B 参数）虽然推理快，但缺乏原生空间感知能力，难以精确识别物体坐标。
    2.  **多模态动作分布建模难**：在混合了人类演示的数据集上训练时，传统的确定性策略难以处理高度变异的行为，导致控制不稳定。
*   **研究意义**：旨在开发一种既能保持轻量化推理速度，又具备强空间感知和稳定控制能力的视觉-语言-动作（VLA）模型。

#### 2. 方法论
XS-VLA 采用了一个两阶段框架，将空间知识蒸馏与生成式动作策略相结合：
*   **阶段一：粗粒度空间语义蒸馏**
    *   利用强大的教师模型（Qwen3-VL-4B）对机器人操作场景进行标注，生成抓取关键点坐标。
    *   将坐标转化为 9 个离散方向区域（如“左下”、“中心”等）的文本描述。
    *   通过这些合成数据对 0.25B 的 SmolVLM2 骨干网络进行微调，使其获得空间定位能力。
*   **阶段二：潜空间流匹配策略（Latent Flow Matching）**
    *   **CVAE 潜空间建模**：引入条件变分自编码器（CVAE）来捕捉人类演示中的多模态意图（如不同的抓取角度或速度），将其编码为潜变量 $z$。
    *   **流匹配（Flow Matching）**：结合 CVAE 的输出，利用流匹配动力学生成动作轨迹。相比扩散模型，流匹配推理路径更直观且高效。
    *   **分层架构**：采用交错注意力机制（Interleaved Attention），融合图像、文本、本体状态和潜变量 $z$，通过 Transformer 预测动作块。

#### 3. 实验设计
*   **数据集与基准**：
    *   **LIBERO 仿真基准**：包括 LIBERO-Spatial（空间关系）、LIBERO-Object（物体操作）、LIBERO-Goal（目标适应）和 LIBERO-Long（长程任务）。
    *   **真实世界场景**：在 XLerobot 平台上进行双臂胡萝卜传递任务，并在 OpenARM 和 PiPER 硬件上进行部署验证。
*   **对比方法**：
    *   **外部基线**：Diffusion Policy, Octo (0.1B), OpenVLA (7B), TraceVLA (7B), ThinkAct (7B), SpatialVLA (4B) 等。
    *   **内部消融对比**：Vanilla SmolVLA (0.25B, 0.5B, 2.25B) 以及不同组件组合的变体。

#### 4. 资源与算力
*   **模型规模**：核心骨干网络仅 0.25B 参数。
*   **训练细节**：在 LIBERO 数据集上训练了 160,000 步；真实世界任务训练了 20,000 步。
*   **算力需求**：文中未明确给出训练所用的具体 GPU 型号和数量，但强调了其**极低的推理成本**：推理时仅需 **1600MB GPU 显存**，适合在 NVIDIA Jetson Orin 等边缘设备部署。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了仿真环境下的 4 个任务套件和 3 种不同硬件平台的真实实验，实验设计较为全面。
*   **消融实验**：专门设计了消融实验来验证“空间预训练”和“流匹配策略”各自的贡献，证明了两者结合的协同效应。
*   **客观性**：通过与参数量大其数倍甚至数十倍的模型（如 7B 规模模型）对比，展示了该模型在效率与性能平衡上的优越性，对比过程较为公正。

#### 6. 主要结论与发现
*   **性能突破**：XS-VLA 在 <0.5B 规模的模型中达到了 SOTA。其 0.25B 的版本在 LIBERO 平均成功率上（90%）甚至超过了 2.25B 的 SmolVLA 和 7B 的 OpenVLA。
*   **长程任务优势**：在最难的 LIBERO-Long 任务中，成功率比基线提升了 23%，证明了流匹配策略在维持长时间序列一致性方面的强大能力。
*   **效率提升**：在任务执行速度上，XS-VLA 比之前的轻量化流匹配架构快了 **3.2 倍**，且推理延迟极低。

#### 7. 优点（亮点）
*   **高效的知识转移**：通过“教师-学生”蒸馏模式，成功将大模型的空间感知能力“下载”到了极小规模的模型中。
*   **创新的策略耦合**：首次将 CVAE 的多模态建模能力与流匹配的高效生成能力结合，应用于轻量化 VLA 模型。
*   **极强的实用性**：极低的显存占用和极快的执行速度，使其成为边缘侧机器人实时控制的理想选择。

#### 8. 不足与局限
*   **深度感知局限**：由于依赖 2D 图像教师模型的蒸馏，模型在处理某些具有深度歧义的场景时仍可能出错。
*   **几何理解限制**：在需要极高精度物体几何匹配的目标条件任务中，表现仍有提升空间。
*   **数据依赖**：性能高度依赖于教师模型生成的合成数据质量。

（完）
