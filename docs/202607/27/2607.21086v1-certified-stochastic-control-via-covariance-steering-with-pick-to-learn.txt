Title: Certified Stochastic Control via Covariance Steering with Pick-to-Learn

URL Source: https://arxiv.org/pdf/2607.21086v1

Published Time: Fri, 24 Jul 2026 00:42:39 GMT

Number of Pages: 8

Markdown Content:
# Certified Stochastic Control via Covariance Steering with Pick-to-Learn 

Chun-Wei Kong, Zachary Donovan, Morteza Lahijanian, Jay McMahon 

Abstract — We present CS-P2L, a framework coupling covari-ance steering (CS) with the Pick-to-Learn (P2L) meta-algorithm for certified controller synthesis over high-fidelity stochastic simulators. The method iteratively evaluates policies on sim-ulator rollouts, tightens surrogate constraints using the worst-case violations, and provides compression-based probabilistic guarantees on the true violation probability given a confidence level. On a spacecraft powered-descent problem with uncertain gravity, CS-P2L certifies a violation bound of 4.9% with 600 rollouts, whereas standalone covariance steering underestimates the violation rate by roughly a factor of two. 

I. INTRODUCTION Safety-critical controller design often relies on high-fidelity simulators as the primary means of capturing com-plex dynamics and environmental uncertainty. The richer and more expressive the simulator, the more faithfully it captures deployment conditions; yet this very expressiveness makes it intractable to synthesize controllers with formal guarantees directly over the simulator. In practice, policies are typically designed on a simplified surrogate model and validated by Monte Carlo (MC) simulation, which yields only an empirical estimate of the violation probability and offers no principled mechanism for translating MC failures into policy improvements. To bridge this gap, we propose a surrogate-based, simulator-driven control synthesis framework that provides formal guarantees. Several lines of work focus on this synthesis-validation gap. The scenario approach [1] provides probably approxi-mately correct (PAC) guarantees (probabilistic bounds with confidence) but requires convexity in the decision variables for every scenario. Iterative scenario optimization [2] con-structs worst-case scenario sets for robust control by embed-ding the dynamics model directly in the optimization, but requires solving the complex dynamics within each scenario subproblem. Covariance steering (CS) [3]–[6] synthesizes controllers that steer the state distribution under chance constraints via convex programming, but typically operated on linearized surrogates without accounting for surrogate– simulator mismatch. PAC-Bayes control [7] trains reinforcement learning poli-cies across simulated environments and bounds the ex-pected cost over novel environments. Work [8] extend this framework to safety-aware policy distributions by combin-ing Hamilton-Jacobi reachability shielding with PAC-Bayes generalization, providing lower bounds on both expected performance and safety across unseen environments. In both          

> Authors are with the Dept. of Aerospace Engineering Sciences, University of Colorado Boulder, USA

{firstname.lastname }@colorado.edu 

cases, however, the guarantees bound the expected value of a cost or safety indicator averaged over both the environment distribution and the policy distribution, rather than certifying the violation probability of a single returned policy with respect to the simulator. Moreover, obtaining tight bounds requires careful design of both the prior distribution and the reward structure [9]. Neural supermartingale certificates [10], [11] provide for-mal reach-avoid guarantees for continuous-time SDEs by (jointly) training neural controllers and certificates. They, however, require state space discretization, which scales exponentially with state dimension. Typically, the synthe-sized controllers are continuous-time feedback laws rather than zero-order-hold policies applicable for digital control systems. Recently proposed Pick-to-Learn (P2L) meta-algorithm [12], [13] equips any data-driven algorithm with tight generalization bounds by iteratively selecting informative data points, using all available data for both synthesis and certification [14]. P2L has been demonstrated across reachability, optimal control, safe synthesis, and robust control [13], but its application to simulator-based control synthesis for continuous-time stochastic systems, where the surrogate–simulator gap described above demands a tractable inner solver that can systematically incorporate simulator feedback, has not been explored. In this work, we instantiate P2L for controller design on continuous-time stochastic systems, focusing on stochastic differential equations (SDEs) with partially known drift parameters under reach-avoid specifications. To do so, we pair the outer P2L loop with covariance steering (CS) as a structured inner solver on a linearized surrogate. The re-sulting Covariance-Steering Pick-to-Learn (CS-P2L) method synthesizes a zero-order-hold, feedforward-feedback policy and certifies it with a probabilistic guarantee: with confidence at least 1 − δ, the true violation probability p(π) is at most ¯ε,where ¯ε is determined by the number of simulator rollouts that drive the policy update. At each iteration, CS-P2L evaluates the current policy on a fixed set of simulator rollouts, identifies the most severely violating trajectory, and uses it to tighten the surrogate constraints—state trajectory bounds, control limits, and ter-minal covariance—before re-solving the covariance steering problem. This closed-loop interaction between the high-fidelity simulator and the surrogate is the key mechanism that bridges this surrogate–simulator gap while retaining tractability. Unlike the scenario approach, no convexity in the original problem is required. Unlike iterative scenario optimization, CS-P2L handles continuous-time dynamics of 

> To appear in the 65th IEEE Conference on Decision and Control. arXiv:2607.21086v1 [eess.SY] 23 Jul 2026

arbitrary complexity, since the inner solver operates on a lightweight linearized surrogate while the full nonlinear dynamics are accessed only through simulator rollouts. Un-like neural certificate methods, no state-space discretization is needed and the controller is directly implementable in digital systems; and unlike standalone CS, the outer P2L loop detects and corrects for surrogate–simulator mismatch through iterative rollout evaluation. In summary, the contributions are fourfold: (i) We formalize a class of continuous-time stochastic simulators with uncertain drift parameters over which control policies can be synthesized with certified vio-lation probability bounds. (ii) We develop CS-P2L, a tractable framework that lever-ages covariance steering as an inner solver to synthe-size certified controllers for these simulators. (iii) We propose a tightening strategy that reduces the certified violation probability. (iv) We demonstrate CS-P2L on a spacecraft powered descent problem with uncertain gravitational environ-ments, showing that the method efficiently produces certified zero-order-hold control policies. II. P ROBLEM FORMULATION 

We first formalize the notion of a simulator. 

Definition 1 (Simulator) . Let j, k ∈ N0 and 

t0 < t 1 < · · · < t J = tf ,τ0 < τ 1 < · · · < τ K = tf , {τk}Kk=0 ⊆ { tj }Jj=0 .

Furthermore, let X ⊆ Rnx be the state space, μ0 ∈ P (X) be the initial-state distribution with finite covariance, and Λ ⊂

Rnλ be a compact set of time-invariant uncertain parameters with a probability measure μΛ ∈ P (Λ) . For each rollout, the simulator draws x0 ∼ μ0, λ ∼ μΛ, keeps λ fixed on time interval [t0, t f ], and generates the state process x(·) from the stochastic differential equation (SDE) 

dx t = f (xt, u t, t ; λ) dt + G dw t, t ∈ [t0, t f ],

where wt ∈ Rnw is an nw-vector standard Brownian motion process with E[dw tdw ⊤ 

> t

] = Inw dt , and G ∈ Rnx×nw is a constant diffusion matrix. Control is applied by zero-order hold under a sampled policy π = {πk}K−1 

> k=0

∈ Π:

ut = πk

 x(τk) ∈ Rnu , t ∈ [τk, τ k+1 ), k = 0 , . . . , K −1.

We denote the underlying random realization by 

z = ( x0, λ, {wt}t∈[t0,t f ]),

which is drawn from the joint distribution of μ0, μ Λ and the Brownian motion, and write Sim π (z) for the simulated state-control trajectory 

Sim π (z) :=  x(t0), . . . , x (tJ ) ×  u0, . . . , u K−1

 ∈ T ,

where T := (Rnx )(J+1) × (Rnu )K is the state-control trajectory space. 

One rollout of the simulator samples an initial condition and a time-invariant uncertain parameter, propagates the continuous-time stochastic dynamics under a zero-order-hold controller, and returns the state trajectory on the fine grid {tj }Jj=0 together with the controls on the coarser grid {τk}Kk=0 . The policy is indexed over k = 0 , . . . , K − 1

because a control applied at τK = tf would influence only states beyond the terminal time. We next establish the probability measures induced by Simulator 1. 

Definition 2 (Probability measures) . Let z =(x0, λ, {wt}t∈[t0,t f ]) be a random realization as in Def. 1. Denote by Pz the probability measure induced by z.Given N i.i.d. realizations ZN = {z(1) , . . . , z (N )}, denote by PZN := P⊗Nz the corresponding product measure. 

With these measures, we introduce the quantities needed to state the control design problem. Let φ be a specification on state-control trajectories. In this work, we focus on φ for reach-avoid, but the framework could be extended to other tasks, such as safety-only or temporal logic specifications. 

Definition 3 (Reach-avoid specification) . Given safe set 

Xs := TMm=1 {x | a⊤

> m

x ≤ bm} and terminal set Xtf := {x ∈

X | (x − μtf )⊤Σ−1 

> tf

(x − μtf ) ≤ r2 

> tf

}, with Σtf ∈ Snx 

> ++

and 

rtf > 0 (representing an ellipsoid), and maximum control magnitude umax > 0, the reach-avoid specification φ is satisfied by a realization z of Simulator 1 under controller 

π, denoted as Sim π (z) |= φ, if 

x(tj ) ∈ X s ∧ ∥ uk∥2 ≤ umax ∧ x(tf ) ∈ X tf , ∀j, k. 

By Def. 3, we define the violation probability 

p(π) := Pz

 Sim π (z)̸ |= φ. (1) Intuitively, p(π) is the probability that the specification is violated when the controller is deployed and the simu-lator draws a fresh realization. In general, p(π) cannot be evaluated in closed form nor bounded analytically, because the simulator may involve complex dynamics (e.g., nonlinear SDEs with zero-order-hold control), non-Gaussian distribu-tions, and uncertain parameters whose effects compound over the trajectory. Standard Monte Carlo estimation provides only a point estimate of p(π) with no principled mechanism to feed observed violations back into the policy. We therefore pursue a certification strategy in which the simulator is run N times and the resulting realizations serve a dual purpose: synthesizing a policy π and bounding its violation probability. 

Problem 1 (Control design with probabilistic specification) .

Given the simulator in Def. 1, any set of N i.i.d. realizations 

ZN as in Def. 2, a reach-avoid specification φ (Def. 3), a target violation probability ε ∈ (0 , 1) , and a confidence level 

δ ∈ (0 , 1) , synthesize a policy π ∈ Π such that 

PZN [p(π) ≤ ε] ≥ 1 − δ. (2) Problem 1 is formulated with two layers of probability because the inner probability, p(π), is intractable to bound directly. The outer probability, PZN [ · ] ≥ 1 − δ, certifies the design process itself: over the randomness in the N

simulations from which both π and ε are derived, the event 

p(π) ≤ ε holds with confidence at least 1 − δ.In the next section, we develop a framework to synthesize a control policy and, given any confidence δ, compute an upper bound ¯ε on its violation probability for reach-avoid specifications. We then propose a strategy for tightening ¯ε

below ε in practice. III. C OVARIANCE STEERING WITH THE P2L M ETHOD 

We develop a CS-P2L synthesis framework that replaces the intractable controller design over the simulator with a linearized, discretized surrogate on which covariance steer-ing can be solved using successive convex programming. Because the surrogate enforces chance constraints under a Gaussian assumption at discrete nodes, it cannot certify the original nonlinear dynamics. The P2L loop closes this gap: it evaluates the synthesized policy on the underlying simulator, identifies the most severely violating realization, and recon-figures the surrogate to reduce that violation. The set that accumulates these counterexamples directly determines the violation probability bound ¯ε via compression theory [15]. We begin by formulating the surrogate covariance-steering problem whose configuration parameters are associated with Prob. 1. We then present the CS-P2L algorithm and its probabilistic guarantee, describe the design of violation mea-sure and configuration algorithm, and propose a tightening strategy that seeks to reduce ¯ε by iteratively running CS-P2L on a progressively enlarged dataset. 

A. Surrogate Covariance-steering Formulation 

The surrogate approximates the nonlinear SDE in Simu-lator 1 with a linearized, discretized model at time points 

{τk}Kk=0 . Let S ⊂ Rnx denote the singular set of the Simulator SDE (Def. 1), and let f, g ∈ C 1(Rnx \ S ). Assume there exists a compact set X ⊂ Rnx \ S containing all trajectories over [t0, t f ]. Consequently, f and g are Lipschitz on X , the SDE admits a unique strong solution on [t0, t f ]

[16], and the Jacobians are well-defined at each linearization point. We parameterize the controller over coarse time points τk,indexed by k, via a memoryless, affine control law 

uk = ¯ uk + Kk(xk − μk), (3) where ¯uk ∈ Rnu is the feedforward control, Kk ∈ Rnu×nx is the feedback gain matrix, and μk := E[xk] is the mean state. Eq. (3) defines a zero-order-hold control, with uk constant over [τk, τ k+1 ). Let xref := {xref  

> k

}Kk=0 and uref := {uref  

> k

}K−1

> k=0

be a reference state-control sequence. The SDE is linearized about this state-control sequence, then discretized with the control law in Eq. (3). The discrete linear time-varying (LTV) stochastic system is 

xk+1 = Akxk + Bkuk + ck + Ekνk + Gkwk,

where Ak ∈ Rnx×nx , Bk ∈ Rnx×nu , ck ∈ Rnx , Ek ∈

Rnx×nν , and Gk ∈ Rnx×nw are the system matrices com-puted during the linearization and discretization process. The 

virtual control parameter ν ∈ Rnν is introduced in the linearization step to address artificial infeasiblity of the lin-earized dynamics [17], [18]. The dynamic’s virtual control is penalized using the one-norm as Jν = PK−1 

> k=0

∥Ekνk∆τk∥1

such that the trajectory at convergence is dynamically feasi-ble. The noise of the discrete process wk ∈ Rnw is described by a standard Gaussian process. Under the Brownian process assumption and given the linearized dynamics, the state evolution is decomposed into the evolution of its mean and covariance as 

μk+1 = Akμk + Bk ¯uk + ck + Ekνk, (4a) 

Pk+1 = ( Ak + BkKk)Pk(Ak + BkKk)⊤ + GkG⊤ 

> k

. (4b) The covariance evolution in (4b) is bilinear in the decision variables Pk and Kk but can be transcribed into an affine equality constraint as described in Lemma 1. 

Lemma 1 (Convex state covariance reformulation [19]) .

Consider the change of variables Uk = KkPk and Yk =

UkP −1 

> k

U ⊤ 

> k

where Uk ∈ Rnu×nx and Yk ∈ Snu

> ++

. Eq. (4b) 

can be equivalently expressed as 

Pk+1 = AkPkA⊤ 

> k

+ AkU ⊤ 

> k

B⊤ 

> k

+ BkUkA⊤

> k

+ BkYkB⊤ 

> k

+ GkG⊤ 

> k

, (5) 

where 

Pk U ⊤

> k

Uk Yk



⪰ 0.

The discrete minimum control energy objective can then be written as Ju = PK−1 

> k=0

[¯ u⊤ 

> k

¯uk + tr( Yk)]∆ τk. The discrete stochastic safety constraint for the m-th constraint at the k-th time is 

P[a⊤

> m

xk ≤ bm] ≥ 1 − εm,k 

where 0 ≤ εm,k < 0.5 is the prescribed probability of violat-ing the safety constraint. The nonconvex safety constraint is written as a difference of convex (DC) functions and the convex-concave procedure (CCP) is used to linearize the nonconvex term yielding the convex inequality constraint in Lemma 2 [20], [21]. 

Lemma 2 (Convex safety constraints [22]) . The hyperplane safety constraint is approximated as the convex inequality constraint 

a⊤

> m

μk − bm ≤ 0,

Ψ2a⊤

> m

Pkam −

h

c2 

> m,k

− 2a⊤

> m

cm,k 



μk − xref 

> k

i 

≤ ηm,k ,

(6) 

using sequential convex optimization. Note cm,k = bm −

a⊤

> m

xref  

> k

and Ψ is the inverse cumulative distribution function of a standard Gaussian distribution at the confidence level 

1 − εm,k . The slack variable ηm,k ∈ R+ is added to address artificial unboundedness in the linearization step of the CCP [18]. 

The discrete stochastic control constraint is given by 

P[∥uk∥2 ≤ umax ] ≥ 1 − εu (7) where εu is the prescribed probability of violating the control bound. Lemma 3 (Convex control constraints [23], [24]) . A suffi-cient condition for the stochastic control constraint in Eq. (7) 

is 

∥¯uk∥2 + pλmax (Yk)

q

χ2 

> nu,1−εu

≤ umax 

implemented in the sequential convex optimization problem by linearizing pλmax (Yk) as 

∥¯uk∥2 + ζk

q

χ2 

> nu,1−εu

≤ umax (8a) 

λmax (Yk) − (ζref  

> k

)2 − 2ζref  

> k

(ζk − ζref  

> k

) ≤ νu,k (8b) 

where χ2 is the inverse chi-squared distribution with nu

degrees of freedom and confidence interval 1 − εu. The slack variable νu,k ∈ R+ addresses artificial unboundedness in linearization about the reference ζref := {ζref  

> k

}K−1 

> k=0

[18]. 

The slack variables associated with the safety and control constraints are penalized in the cost function as Jc =PKk=0 

PMm=1 ∥ηm,k ∥1 + PK−1 

> k=0

∥νu,k ∥1. A soft penalized trust region is also included in the cost function as Jtr =PKk=0 ∥xk −xref k ∥22 +PK−1 

> k=0

(∥uk −uref k ∥22 +∥ζk −ζref k ∥22) to prioritize linearization validity. For consistency, linearization and discretization is considered with respect to the complete set of reference trajectories given by ξ = {xref , u ref , ζ ref }.Next, we discuss the initial and terminal constraints for the surrogate problem. 

Lemma 4 (Gaussian terminal chance constraint) .

Let x(tf ) ∈ Rnx be a Gaussian random vector with x(tf ) ∼ N (μtf , P tf ), and let Xtf =n

x ∈ X (x − μtf )⊤Σ−1 

> tf

(x − μtf ) ≤ r2

> tf

o

be the terminal ellipsoid, where Σtf ∈ Snx 

> ++

and rtf > 0. Fix εp ∈ (0 , 1) ,and let χ2 

> nx,1−εp

denote the (1 − εp)-quantile of the chi-square distribution with nx degrees of freedom. If 

Ptf ⪯ r2

> tf

χ2

> nx,1−εp

Σtf , (9) 

then 

P  x(tf ) /∈ X tf

 ≤ εp.

The proof of Lemma 4 is provided in Appendix IV-A. Given the initial distribution of the Simulator 1 (i.e., x0 ∼

μ0), and the terminal ellipsoid specified by Σtf and rtf by Def. 3, the initial and final distributions of the surrogate are constrained by: 

μk=0 = E[x0], Pk=0 = Cov μ0 (x0),μk=K = μtf , Pk=K ⪯ Ptf , (10) where Ptf is computed according to Lemma 4 for any given terminal risk εp ∈ (0 , 1) .In summary, the safety, control, and terminal distribution risks are associated to the target violation probability of Prob. 1 by: 

εx + εu + εp ≤ ε, 

> K

X

> k=0
> M

X

> m=1

εm,k ≤ εx, (11) where the safety constraint risk is prescribed conserva-tively over the summation of K time points and M affine-constraint. Below, we summarize the configuration parame-ters of covariance steering. 

Definition 4 (Configuration parameters of the CS problem) .

Let Θ = RnxM × RM × R++ × Snx 

> ++

× R++ × R++ × R++ 

be the configuration parameters space of the CS problem. We define the initial configuration θ(0) ∈ Θ by associating each component of θ with Prob. 1 under the reach-avoid specification φ in Def. 3: 

θ(0) ≜ ({am}Mm=1 , {bm}Mm=1 , u max , P tf , ε m,k , ε u, ε p) ∈ Θ,

where εm,k , ε u and εp are associated to Prob. 1 by Eq. (11) ,and Ptf is associated to Prob. 1 by Eq. (9) .

The configuration parameters define the surrogate CS problem which is decomposed into two phases: (i) the complete CS problem in Alg. 1 iterates over (ii) the inner convex subproblem in Def. 5 using methods of successive convexification [17]. The hyper-parameters are manually selected to drive the virtual control and trust region below a user-defined, small tolerance δvc and δtr implying dynamic feasiblity and acceptable linearization error [25]. 

Algorithm 1 Solve -CSP( θ)

Require: Configuration θ ∈ Θ, tolerances δvc > 0, δtr > 0 

> 1:

i ← 0; initialize reference ξ(i) 

> 2:

repeat  

> 3:

Linearize/discretize Simulator 1 about ξ(i) →{Ak, B k, c k, E k, G k} 

> 4:

z⋆ ← Solve P(ξ(i), θ ) (Def. 5); ξ(i+1) ← z⋆; i ← i+1  

> 5:

until Jν + Jc ≤ δvc and Jtr ≤ δtr  

> 6:

return μ⋆k, ¯u⋆k, P ⋆k , K ⋆k = U ⋆k (P ⋆k )−1 Kk=0 

Definition 5 (Surrogate CS Subproblem) . Fix λ ∼ μΛ,and configuration θ ∈ Θ. For a given reference ξ(i) =

{xref , u ref , ζ ref }, the i-th surrogate CS subproblem is 

P(ξ(i), θ ) : min  

> z

J (z) s.t. Eqs . (4a) , (5) , (6) , (8) , (10) 

where z = {μk, ¯uk, U k, ν k, ζ k, P k, Y k, η j,k , ν u,k }Kk=0 is the decision variable vector and J (z) = wuJu +wvc (Jν +Jc)+ 

wtr Jtr with hyper-parameters wu, w vc , w tr > 0.

In Alg. 1, a reference ξ(0) is initialized at Line 1 by inter-polation. At Line 3, Simulator 1 is linearized and discretized to generate the system matrices for the convex subproblem (Def. 5). Lines 4-5 solve the convex subproblem, and up-date the reference ξ(i) until the aforementioned convergence criteria are met. See Appd. IV-C for additional details. We note that Alg. 1, denoted by Solve -CSP( θ), inher-ently conceals the non-Gaussian evolution of the initial distribution. Additionally, it does not offer any continuous-time constraint satisfaction guarantees. To overcome these fundamental limitations and deliver provable performance in the nonlinear continuous-time setting, we turn to P2L. B. Algorithm and Probabilistic Guarantee 

We now describe how P2L uses simulator rollouts to itera-tively configure the surrogate CS problem. The construction relies on three objects: a dataset of simulator rollouts under the current policy, a violation measure that ranks the severity of specification violations, and a configuration algorithm that updates the surrogate problem parameters based on the most informative rollouts. Let ZN := {z(1) , . . . , z (N )} denote the set of N i.i.d. realizations drawn as in Def. 2. 

Definition 6 (Rollout dataset) . Given a policy π ∈ Π and the realization set ZN , the rollout dataset is 

DN (π) := 

n

Sim π (z(i))

o

> z(i)∈ZN

.

Definition 7 (Violation measure) . Let φ be the specification in Def. 3, and let T be the state-control trajectory space in Def. 1. A violation measure with respect to specification φ

is a function ℓφ : T → R such that ℓφ(Sim π (z)) ≤ 0 if and only if Sim π (z) |= φ. Larger positive values indicate more severe violations. Since the specification is fixed throughout, we write ℓ for ℓφ when no ambiguity arises. 

Definition 8 (Configuration algorithm) . Let φ be the spec-ification in Def. 3, let θ ∈ Θ denote the configuration parameters of the surrogate CS problem in Def. 5, and let 

T be the state-control trajectory space in Def. 1. A configu-ration algorithm with respect to specification φ is a map Lφ

that takes the current configuration θ ∈ Θ and a finite collection of state-control trajectories {Sim π (z(i))}z(i)∈S for some S ⊆ ZN , and returns an updated configuration θ′ =

Lφ(θ, {Sim π (z(i))}z(i)∈S ) ∈ Θ. Since the specification is fixed throughout, we write L for Lφ.

Algorithm 2 Covariance-Steering Pick-to-Learn (CS-P2L) 

Require: Simulator Sim , reach-avoid specification φ in Def. 3, realization set ZN = {z(1) , . . . , z (N )}, initial configuration θ(0) , configuration algorithm L, violation measure ℓ 

> 1:

Output: Policy π(r), compression set T (r) 

> 2:

π(0) ← Solve -CSP( θ(0) ), evaluate DN (π(0) ) on ZN 

> 3:

T (0) ← ∅ , r ← 0 

> 4:

while ∃ z(i) ∈ ZN \ T (r) such that Sim π(r) (z(i))̸ |= φ

do  

> 5:

¯z ← arg max    

> z(i)∈ZN\T(r)
> Sim π(r)(z(i))̸| =φ

ℓ Sim π(r) (z(i)) 

> 6:

T (r+1) ← T (r) ∪ { ¯z} 

> 7:

θ(r+1) ← L θ(r), {Sim π(r) (z(i))}z(i)∈T (r+1) 

 

> 8:

π(r+1) ← Solve -CSP( θ(r+1) ), re-evaluate 

DN (π(r+1) ) on ZN 

> 9:

r ← r + 1  

> 10:

end while  

> 11:

return π(r), T (r)

We are now ready to present the CS-P2L algorithm. Algorithm 2 proceeds as follows. Line 2 solves the initial surrogate CS problem ( θ(0) per Def. 4) to obtain a start-ing policy π(0) and evaluates it on all realizations in ZN

to form the rollout dataset. The compression set T (0) ⊆

ZN , initialized to the empty set in Line 3, accumulates the realizations that drive policy updates. The while-loop (Lines 4–9) iterates as long as any realization in ZN \ T (r)

violates the specification. In each iteration, Line 5 selects the most severely violating rollout according to the violation measure and Line 6 adds it to the compression set. Line 7 invokes the configuration algorithm to update the surrogate problem parameters, and Line 8 solves the resulting CS problem and re-evaluates the entire dataset under the new policy using the same realization set ZN ; this is essential for the compression-based generalization guarantee, which requires that the data points remain fixed across iterations. The algorithm terminates when every realization in ZN \T (r)

satisfies the specification, and returns the final policy π(r)

together with the compression set T (r).Next we state the probabilistic certificate provided by Alg. 2. The result follows from the compression-based generalization theory [13, Thm. 1], which we specialize to the CS-P2L setting. We first introduce the function needed to state the bound. For i = 0 , 1, . . . , N − 1 and confidence parameter δ ∈ (0 , 1) , define Ψi,δ : (0 , 1) → R by 

Ψi,δ (ε) = δN 

> N−1

X

> q=i

 qi

 Ni

 (1 − ε)−(N −q). (12) 

Theorem 1 (CS-P2L guarantee) . Let (π(r), T (r)) be the output of Alg. 2, and let |T (·)| denote the number of elements in T (·). For any δ ∈ (0 , 1) , it holds that 

PZN

h

p(π(r)) ≤ ¯ε(|T (r)|, δ, N )

i

≥ 1 − δ, 

where PZN and Pz are as defined in Def. 2, and ¯ε(i, δ, N )

is defined as follows: for i = 0 , 1, . . . , N − 1, ¯ε(i, δ, N ) is the unique solution to Ψi,δ (ε) = 1 in the interval [i/N, 1] ;for i = N , ¯ε(N, δ, N ) = 1 .

Remark 1 (Conservative baseline) . The degenerate case 

L(θ, S ) = θ for all S ⊆ ZN (i.e., the configuration algorithm never updates the surrogate problem) reveals the conserva-tive baseline of Alg. 2. In this case θ(0) remains feasible throughout, the policy remains π(r) = π(0) at every iteration, and the rollout dataset DN (π(0) ) never changes. The while-loop simply adds violating realizations to the compression set one by one until T := {z(i) ∈ ZN | Sim π(0) (z(i))̸ |=

φ} is exhausted, and the algorithm terminates normally with output (π(0) , T ). Thm. 1 then holds with compression size |T |.C. Violation Measure and Configuration Algorithm Designs 

The design choices of the configuration algorithm and violation measure affect the compression size at termination and hence the tightness of the bound ¯ε. We now propose a specific instantiation suited to the surrogate CS problem, beginning with the violation measure and then describing the configuration update rules. The violation measure is instantiated as the total number of violations with respect to the original reach-avoid speci-fication φ. Specifically, 

ℓ Sim π (z) =

> M

X

> m=1
> J

X

> j=0

1{(a(0)  

> m

)⊤x(tj ) > b (0)  

> m

}

+

> K−1

X

> k=0

1{∥ uk∥2 > u (0) max } + 1{x(tf ) /∈ X (0)  

> tf

}, (13) where the state and control trajectories are those produced by 

Sim π (z). By construction, ℓ ≤ 0 if and only if Sim π (z) |= φ,satisfying Def. 7. We now turn to the configuration algorithm. Recall that 

θ = ( {am}Mm=1 , {bm}Mm=1 , u max , P tf , ε j,k , ε u, ε p) in Def. 4. For simplicity, we choose to update only three components: the safety constraint bound {bm}Mm=1 , the control bound 

umax , and the terminal covariance Ptf .Let {b(0)  

> m

}Mm=1 = {bm}Mm=1 , u(0) max = umax , and P (0)  

> tf

=

Ptf be the initial configuration parameters associated with 

φ. Given the trajectories {Sim π(r) (z(i))}z(i)∈T (r+1) , the con-figuration algorithm identifies the violations (safety, control, and terminal constraints) from the three corresponding bool 

logic in Def. 3, and updates {b(r) 

> m

}Mm=1 , u(r)max , and P (r)

> tf

independently. For safety constraints, let 

cm = X 

> z(i)∈T(r+1)

{j ∈ { 0, . . . , J } : a⊤

> m

x(i)(tj ) > b (r) 

> m

}

(14) count the state violation time steps across all compression set trajectories. 1 The three update rules are: 

b(r+1)  

> m

← max 



b(r)

> m

 1 − min( γb cm, ¯γb), b min 

> m



, (15a) 

u(r+1) max ← max  γu u(r)max , u min max 

, (15b) 

P (r+1)  

> tf

← min  γP P (r) 

> tf

, P max 

> tf

, (15c) where (15a) tightens the bound on m−th affine constraints, with per-violation fraction γb > 0, capped at rate ¯γb ∈ (0 , 1) ;(15b) shrinks the control bound with factor γu ∈ (0 , 1) ;and (15c) contracts the terminal covariance with factor γP ∈

(0 , 1) . Each rule is applied only when the corresponding violations are present in T .The boundary terms bmin  

> m

, umin max , and P max  

> tf

ensure that the updated configuration never renders Solve -CSP infeasible, and are determined in practice by running Alg. 1 before Alg. 2 to identify the tightest parameters for which Alg. 1 remains feasible. 

D. Tightening Strategy for Violation Probability Bound 

The configuration algorithm design in Sec. III-C deter-mines the compression size for a fixed dataset. We now address the complementary direction: enlarging the dataset to drive ¯ε below the target ε.A key constraint is that Thm. 1 requires the configuration algorithm L, including all tightening factors, to be fixed    

> 1We assume b(0)
> m̸= 0 without loss of generality; this can always be achieved by a coordinate translation.

independently of the realization set used for certification. Tuning L on the same data that enters the bound invalidates the guarantee unless a union-bound correction is applied; see [15, Thm. 4] and [12, Footnote 11]. The following two-phase procedure respects this independence requirement. In the optional calibration phase , if a suitable L is not known in advance, an initial batch Z(0) is generated and CS-P2L is run with various tightening factors to identify a satisfactory configuration algorithm, which is then frozen as the fixed L⋆. This mirrors the pretraining strategy employed in the original P2L experiments [12, Sec. 5]. If domain knowledge already suggests a good L⋆, this phase may be skipped entirely. In the certification phase , independent batches Z(s) are generated (each with a distinct random seed, so that all realizations are i.i.d. draws from Pz ) and CS-P2L is run with the frozen L⋆ on the cumulative dataset Z(1) ∪ Z(2) ∪ · · · ,enlarging it until the bound is acceptable. The guarantee of Thm. 1 holds at each stage because L⋆ was fixed before any of Z(1) , Z (2) , . . . were observed, and the tuning batch Z(0) 

(if used) is excluded from the certification data. Each stage’s guarantee is self-contained; one may therefore stop as soon as ¯ε ≤ ε.2

We formalize this procedure in Alg. 3, which solves Prob. 1 for a target violation probability ε when the returned flag SAT = True. 

Algorithm 3 Tightening Strategy for CS-P2L 

Require: Sim , φ, ε, δ, θ(0) , ℓ, and smax  

> 1:

Output: (π, ¯ε, SAT)  

> 2:

(Calibration phase — optional)  

> 3:

if no suitable L is known then  

> 4:

Sample Z(0) ∼ Pz using a random seed 0 

> 5:

Tune and fix L⋆ via CS-P2L on Z(0)  

> 6:

else  

> 7:

L⋆ ← L 

> 8:

end if  

> 9:

(Certification phase) Zcum ← ∅  

> 10:

for s = 1 to smax do  

> 11:

Generate Z(s) ∼ Pz using random seed s 

> 12:

Zcum ← Zcum ∪ Z(s) 

> 13:

(π(s), T (s)) ← CS-P2L (Sim , Z cum , θ (0) , L ⋆, ℓ ) 

> 14:

Compute ¯ε via Thm. 1  

> 15:

if ¯ε ≤ ε then  

> 16:

return (π(s), ¯ε, SAT = True ) 

> 17:

end if  

> 18:

end for  

> 19:

return (π(s), ¯ε, SAT = False )

Remark 2. Although this work focuses on covariance steer-ing (CS), we note that one can replace the inner CS solver by other uncertainty-aware control methods, then follow the discussed process of (i) associate the inner solver to Prob. 1;      

> 2Selecting the best bound across Sstages, however, requires a union-bound correction δ→δ/S .

(ii) identify the configuration parameters (Def. 4); and (iii) design the configuration algorithm (Def. 8). 

IV. E XPERIMENT 

We demonstrate CS-P2L on a spacecraft powered-descent simulator; the detailed setup is provided in Appendix IV-B. We run Alg. 3 with N = 100 , confidence δ = 10 −3, and target violation probability ε = 0 .05 . The inner CS solver converges to the given 10 −6 tolerance (see Appendix IV-C for more details) at each stage s. At s = 6 , the algorithm terminates, returning a controller πCS -P2L using 600 real-izations with ¯ε = 0 .049 ; for comparison, we also record the initial CS controller πCS (with an associated total 0.05 

chance constraint) and all intermediate CS-P2L controllers 

π(s) computed at Line 14 of Alg. 3. To validate the results, we draw 1000 realizations Zval 1000 

and evaluate each controller on the same set to obtain empirical violation probabilities, shown in Fig. 1. Three observations stand out. First, the CS-P2L bounds ¯ε upper-bound the empirical violation probability, confirming the theoretical guarantee. Second, the gap between the empirical 

ε and the computed ¯ε narrows as more realizations are incorporated. Third, πCS —obtained from the initial surrogate with a total 0.05 chance constraint—exhibits an empirical violation probability of 0.096 , nearly double the prescribed risk and roughly three times that of πCS -P2L (0.031 ); the corresponding state and control trajectories are compared in Figs. 2 and 3; Fig. 4 compares the terminal state samples, showing that most violations occur in the position state. 0 100 200 300 400 500 600 

Number of datapoints used in CS-P2L   

> 0.025
> 0.050
> 0.075
> 0.100
> 0.125
> 0.150
> 0.175
> 0.200
> Violation probability  CS P2L
> empirical
> CS
> target = 0.0500

Fig. 1: Empirical violation probability and CS-P2L upper bound ¯ε vs. increased |Z|, evaluated on 1000 validation realizations. APPENDIX 

A. Proofs 

Proof of Lemma 4 

Proof. Let z ∼ N (0 , I nx ). Since x(tf ) ∼ N (μtf , P tf ), we may write x(tf ) − μtf = P 1/2 

> tf

z. Hence 

(x(tf ) − μtf )⊤Σ−1 

> tf

(x(tf ) − μtf ) = z⊤ P 1/2 

> tf

Σ−1 

> tf

P 1/2

> tf

z. 

(16) Define α ≜ r2

> tf
> χ2
> nx, 1−ϵ

. By (9), Ptf ⪯ αΣtf . Applying the congruence transformation with Σ−1/2 

> tf

gives 1.0 1.2 1.4 1.6 1.8      

> x [-]
> 0.2
> 0.1
> 0.0
> 0.1
> 0.2
> y [-]
> Full View
> 1.35 1.40 1.45 1.50 1.55
> x [-]
> 0.12
> 0.14
> 0.16
> 0.18
> 0.20
> 0.22
> 0.24
> y [-]
> Zoomed View
> CS
> CS P2L
> state constraints
> terminal set

Fig. 2: Position trajectories of πCS and πCS −P2L on 1000 val-idation realizations; the violating trajectories are highlighted in the zoomed view; note that no trajectories of πCS −P2L 

violate the state constraints. 0.0 0.5 1.0 1.5 2.0 2.5 

time [-] 

0.6 

0.8 

1.0    

> u 2 [-]
> CS mean ±3
> CS P2Lmean ±3
> umax

Fig. 3: Control (norm) trajectories of πCS and πCS −P2L on 

1000 validation realizations. 1.00 1.05 1.10 1.15      

> x [-]
> 0.075
> 0.050
> 0.025
> 0.000
> 0.025
> 0.050
> 0.075
> y [-]
> 0.2 0.0 0.2 0.4 0.6
> vx [-]
> 0.2
> 0.0
> 0.2
> 0.4
> 0.6
> vy [-]
> CS
> CS P2L
> terminal set
> violating

Fig. 4: Terminal samples of πCS and πCS −P2L on 1000 

validation realizations. 

Σ−1/2 

> tf

Ptf Σ−1/2 

> tf

⪯ αI nx . Moreover, P 1/2 

> tf

Σ−1 

> tf

P 1/2

> tf

and Σ−1/2 

> tf

Ptf Σ−1/2 

> tf

have the same nonzero eigenvalues; since both matrices are symmetric positive semidefinite, it follows that P 1/2 

> tf

Σ−1 

> tf

P 1/2 

> tf

⪯ αI nx . Substituting this bound into (16) yields (x(tf ) − μtf )⊤Σ−1 

> tf

(x(tf ) −

μtf ) ≤ α z ⊤z. Therefore, P x(tf ) /∈ X tf

 =

P



(x(tf ) − μtf )⊤Σ−1 

> tf

(x(tf ) − μtf ) > r 2

> tf



≤

P



α z ⊤z > r 2

> tf



. Since z⊤z ∼ χ2 

> nx

, P



α z ⊤z > r 2

> tf



=

P



χ2 

> nx

> r2

> tf
> α



= P χ2 

> nx

> χ 2

> nx,1−ϵ

 = ϵ. Thus 

P x(tf ) /∈ X tf

 ≤ ϵ. 

Proof of Theorem 1 Proof. Algorithm 2 is an instance of the P2L meta-algorithm (Alg. 1 of [13]) with the following identifications: the dataset 

D corresponds to the realization set ZN ; the property φ

and violation measure ℓ define the termination condition and selection rule. It remains to verify that the policy construction defines a valid synthesis algorithm in the sense of [13], i.e., a map from a subset of ZN to a policy. At iteration r + 1 , the policy is determined by π(r+1) =Solve -CSP( L(θ(r), T (r+1) )) . Since θ(0) is fixed and each subsequent θ(r) is obtained by applying L along the nested sequence T (1) ⊂ · · · ⊂ T (r+1) , the entire map from T (r+1) 

to π(r+1) is determined by T (r+1) and θ(0) . By construction, the algorithm terminates when Yπ(r) (z(i)) = 1 for all z(i) ∈

ZN \ T (r), so that the compression property is satisfied. The result then follows directly from Theorem 1 of [13]. 

B. Spacecraft Powered-descent Dynamics 

We consider the drift in Simulator (1) as: f (xt, u t, t ; λ) = 

f0(xt, t ; λ) + F (xt)ut, where f0 : Rnx × R 7 → Rnx

is the natural dynamics and F : Rnx 7 → Rnx×2 is a generic influence matrix that maps accelerations to changes in the state space. The natural dynamics are described as: 

f0(xt, t ) = fkep (xt) + F (xt)apert (x, t ), where fkep : Rnx 7 →

Rnx is Keplerian motion and apert : Rnx × R 7 → R2 is a perturbing acceleration due to the rotating reference frame, solar radiation pressure, higher-order gravitational effects, etc. Assuming Keplerian motion in a non-rotating frame, these functions are fkep (xt) =

 vt

−λ(μ/ ∥rt∥32)rt



, F =

02×2

I2



, a pert (x, t ) = 0 2×1 where μ ∈ R is the gravitational parameter of the central body, λ ∈ R is the scaling of the gravitational parameter, and rt = ∥rt∥2.

C. Alg. 1 Setup 

The reference ξ(0) = {xref , ¯uref , ζ ref } for Alg. 1 is initial-ized by the user and updated by the solution from Def. 5. The initial mean state reference linearly interpolates between the initial and terminal means: xref  

> k

= (1 − τk) μt0 + τk μtf ,

where τk = k/K . The feedforward control reference is set to zero, ¯uref  

> k

= 0 . The control covariance slack reference 

ζref  

> k

is initialized as the midpoint of the feasible interval 

[0 , ζ max  

> k

] such that ζref  

> k

= 12 ζmax  

> k

and ζmax  

> k

is the largest value consistent with the nominal control constraint in (8), i.e., ζmax  

> k

= ( umax −∥ ¯uref  

> k

∥2)/

q

χ2  

> nu,1−εu

. The fixed hyper-parameters are set as: wu = 1 .0, w vc = 100 , w tr = 0 .1 and 

δvc = δtr = 10 −6.REFERENCES [1] G. C. Calafiore and M. C. Campi, “The scenario approach to robust control design,” IEEE Transactions on automatic control , vol. 51, no. 5, pp. 742–753, 2006. [2] M. Zagorowska, P. Falugi, E. O’Dwyer, and E. C. Kerrigan, “Au-tomatic scenario generation for efficient solution of robust optimal control problems,” International Journal of Robust and Nonlinear Control , vol. 34, no. 2, pp. 1370–1396, 2024. [3] M. Goldshtein and P. Tsiotras, “Finite-horizon covariance control of linear time-varying systems,” in 2017 IEEE 56th Annual Conference on Decision and Control (CDC) . IEEE, 2017, pp. 3606–3611. [4] K. Okamoto and P. Tsiotras, “Optimal stochastic vehicle path planning using covariance steering,” IEEE Robotics and Automation Letters ,vol. 4, no. 3, pp. 2276–2281, 2019. [5] J. Ridderhof, K. Okamoto, and P. Tsiotras, “Nonlinear uncertainty con-trol with iterative covariance steering,” in 2019 IEEE 58th Conference on Decision and Control (CDC) . IEEE, 2019, pp. 3484–3490. [6] J. Ridderhof and P. Tsiotras, “Chance-constrained covariance steering in a gaussian random field via successive convex programming,” 

Journal of Guidance, Control, and Dynamics , vol. 45, no. 4, pp. 599– 610, 2022. [7] A. Majumdar, A. Farid, and A. Sonar, “Pac-bayes control: learning policies that provably generalize to novel environments,” International Journal of Robotics Research , vol. 40, no. 2–3, pp. 574–593, 2021. [8] K.-C. Hsu, V. Rubies-Royo, C. J. Tomlin, and J. F. Fisac, “Sim-to-lab-to-real: Safe reinforcement learning with shielding and generalization guarantees,” Artificial Intelligence , vol. 314, p. 103811, 2023. [9] M. G. Boroujeni, C. L. Galimberti, A. Krause, and G. Ferrari-Trecate, “A pac-bayesian framework for optimal control with stability guarantees,” in 2024 IEEE 63rd Conference on Decision and Control (CDC) . IEEE, 2024, pp. 8237–8244. [10] G. Neustroev, M. Giacobbe, and A. Lukina, “Neural continuous-time supermartingale certificates,” in Proceedings of the AAAI Conference on Artificial Intelligence , vol. 39, no. 26, 2025, pp. 27 538–27 546. [11] C.-W. Kong, S. Escobar, I. Gracia, J. McMahon, and M. Lahijanian, “Training with hard constraints: Learning neural certificates and con-trollers for sdes,” arXiv preprint arXiv:2602.23526 , 2026. [12] D. Paccagnan, M. Campi, and S. Garatti, “The pick-to-learn algo-rithm: Empowering compression for tight generalization bounds and improved post-training performance,” Advances in Neural Information Processing Systems , vol. 36, pp. 18 165–18 185, 2023. [13] D. Paccagnan, D. Marks, M. C. Campi, and S. Garatti, “Pick-to-learn for systems and control: Data-driven synthesis with state-of-the-art safety guarantees,” arXiv preprint arXiv:2512.04781 , 2025. [14] J. Langford and R. Schapire, “Tutorial on practical prediction theory for classification.” Journal of machine learning research , vol. 6, no. 3, 2005. [15] M. C. Campi and S. Garatti, “Compression, generalization and learn-ing,” Journal of Machine Learning Research , vol. 24, no. 339, pp. 1–74, 2023. [16] B. Oksendal, Stochastic differential equations (3rd ed.): an introduc-tion with applications . Berlin, Heidelberg: Springer-Verlag, 1992. [17] Y. Mao, M. Szmuk, X. Xu, and B. Acikmese, “Successive convexification: A superlinearly convergent algorithm for non-convex optimal control problems,” 2019. [Online]. Available: https://arxiv.org/abs/1804.06539 [18] D. Malyuta, T. P. Reynolds, M. Szmuk, T. Lew, R. Bonalli, M. Pavone, and B. Ac ¸ıkmes ¸e, “Convex optimization for trajectory generation: A tutorial on generating dynamically feasible trajectories reliably and efficiently,” IEEE Control Systems , vol. 42, no. 5, p. 40–113, Oct. 2022. [19] F. Liu, G. Rapakoulias, and P. Tsiotras, “Optimal covariance steering for discrete-time linear stochastic systems,” IEEE Transactions on Automatic Control , no. arXiv:2211.00618, Oct. 2024, arXiv:2211.00618 [eess]. [Online]. Available: http://arxiv.org/abs/2211.00618 [20] T. Lipp and S. Boyd, “Variations and extension of the convex–concave procedure,” Optimization and Engineering , vol. 17, no. 2, pp. 263–287, Jun. 2016. [21] A. L. Yuille and A. Rangarajan, “The concave-convex procedure,” 

Neural Computation , vol. 15, no. 4, pp. 915–936, Apr. 2003. [22] J. Pilipovsky and P. Tsiotras, “Computationally efficient chance con-strained covariance control with output feedback,” in 2024 IEEE 63rd Conference on Decision and Control (CDC) , 2024, pp. 677–682. [23] K. Oguri, “Chance-constrained control for safe spacecraft autonomy: Convex programming approach,” in 2024 American Control Confer-ence (ACC) , 2024, pp. 2318–2324. [24] N. Kumagai and K. Oguri, “Robust cislunar low-thrust trajectory optimization under uncertainties via sequential covariance steering,” 

Journal of Guidance, Control, and Dynamics , p. 1–19, Aug. 2025. [25] M. Szmuk, T. P. Reynolds, and B. Acikmese, “Successive convexifica-tion for real-time 6-dof powered descent guidance with state-triggered constraints,” Journal of Guidance, Control, and Dynamics , vol. 43, no. 8, p. 1399–1413, Aug. 2020.