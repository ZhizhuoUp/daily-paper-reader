---
title: "GRACE: Gradient-Free Robot Action Generation via Combined Diffusion-MPPI Posterior Mean Estimation"
title_zh: GRACE：通过结合扩散-MPPI后验均值估计的无梯度机器人动作生成
authors: "Leesai Park, Jiho HOng, Sanghyun Kim"
date: 2026-07-22
pdf: "https://arxiv.org/pdf/2607.21661v1"
tags: ["query:q3"]
score: 8.0
evidence: 结合 MPPI 控制的无梯度机器人动作生成
tldr: 本研究提出GRACE，一种结合扩散策略与MPPI的无梯度机器人动作生成框架。针对传统扩散策略在处理不可微约束（如碰撞检测、黑盒代价）时的局限性，GRACE通过在扩散反向过程中引入MPPI更新，仅需前向代价评估即可实现引导。实验证明，该方法在仿真和真实7自由度机械臂任务中均优于现有基准，能有效处理部署时的动态约束。
selection_source: fresh_fetch
motivation: 现有的扩散策略引导方法依赖可微代价函数，无法处理碰撞检测等实际应用中的不可微或黑盒约束。
method: 结合扩散模型与MPPI控制，在每个反向扩散步骤中利用MPPI对代价条件下的后验均值进行无梯度估计。
result: 在仿真环境和真实机械臂避障任务中，GRACE的成功率显著高于传统的扩散引导和采样基准方法。
conclusion: GRACE为扩散策略提供了一种通用的无梯度引导方案，增强了机器人在复杂约束环境下的部署能力。
---

## 摘要
扩散策略能够从演示中生成多模态机器人动作序列，但将其引导至满足部署时约束通常依赖于可微的引导代价。这排除了许多实际的安全约束，例如二值碰撞检测、关节限制以及不可微的黑盒展开代价。我们提出了 GRACE（通过结合扩散-MPPI后验均值估计的无梯度机器人动作生成），它仅使用前向代价评估，通过模型预测路径积分（MPPI）控制来引导预训练的扩散策略。基于扩散和 MPPI 共同的分数上升结构，GRACE 在每个反向步骤中构建一个代价条件引导后验，并使用以扩散反向均值为中心的单次 MPPI 更新来估计其均值。对于可微代价，GRACE 在一阶协方差匹配近似下可以还原为传统的梯度引导。在仿真中，GRACE 获得了比基于扩散和基于采样的基准方法更高的成功率。在真实的 7 自由度机械臂上，GRACE 避开了部署时的障碍物，而未经引导的先验策略在每次试验中都会与之发生碰撞。代码和实验视频可在 https://anonymous.4open.science/w/grace-70BB/ 获取。

## Abstract
Diffusion policies generate multimodal robot action sequences from demonstrations, but steering them toward deployment-time constraints typically relies on differentiable guidance costs. This excludes many practical safety constraints, such as binary collision checks, joint limits, and black-box rollout costs that are nondifferentiable. We propose Gradient-free Robot Action generation via Combined diffusion-MPPI posterior mean Estimation (GRACE), which guides a pretrained diffusion policy with Model Predictive Path Integral (MPPI) control using only forward cost evaluations. Building on the common score-ascent structure of diffusion and MPPI, GRACE constructs a cost-conditioned guidance posterior at each reverse step and estimates its mean with a single MPPI update centered at the diffusion reverse mean. For differentiable costs, GRACE recovers conventional gradient guidance under a first-order, matched-covariance approximation. GRACE attains higher success rates than diffusion-based and sampling-based baselines in simulation. On a real 7-DoF manipulator, GRACE avoids a deployment-time obstacle that the unguided prior collides with in every trial. Code and experiment videos are available at https://anonymous.4open.science/w/grace-70BB/.

---

## 论文详细总结（自动生成）

这是一份关于论文《GRACE: Gradient-Free Robot Action Generation via Combined Diffusion-MPPI Posterior Mean Estimation》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：扩散策略（Diffusion Policies）在生成多模态机器人动作方面表现优异，但在部署时如何满足**不可微约束**（如二值碰撞检测、关节限制、黑盒代价函数）仍是一个挑战。
*   **研究背景**：现有的推理时引导方法（如 MPD, EDMP）大多依赖于代价函数的梯度。然而，现实中许多安全约束是不可微的，且基于梯度的优化容易陷入局部最优。
*   **整体含义**：本文提出了一种名为 **GRACE** 的框架，通过结合扩散模型和模型预测路径积分（MPPI）控制，实现了无需梯度的动作生成，使机器人能在包含未知障碍物的复杂环境中安全运行。

### 2. 方法论
*   **核心思想**：利用扩散模型和 MPPI 在数学结构上（均为“分数上升”过程）的相似性，在扩散模型的每一个反向去噪步骤中，嵌入一个 MPPI 更新步骤来估计代价条件下的后验均值。
*   **关键技术细节**：
    1.  **分数上升对应关系**：证明了扩散反向步骤和 MPPI 更新都可以看作是沿着某种分布的分数（Score）方向移动。
    2.  **引导后验构建**：在每个去噪步骤 $i$，以扩散反向均值为中心构建一个探索内核（Exploration Kernel）。
    3.  **MPPI 均值估计**：从探索内核中采样 $K$ 条轨迹，通过前向评估计算它们的代价 $J$，并利用玻尔兹曼加权（Importance Sampling）估计后验均值。
    4.  **投影反向内核**：将估计的后验均值作为新的反向分布均值，同时保留扩散过程的调度协方差，从而采样得到下一个迭代值。
*   **数学特性**：论文证明了在代价函数可微且协方差匹配的一阶近似下，GRACE 可以还原为传统的梯度引导方法。

### 3. 实验设计
*   **实验场景**：
    1.  **2D 点质量导航**：在包含训练时未见的障碍物地图中进行导航。
    2.  **3D 7自由度机械臂（FR3）**：在随机放置障碍物的环境中进行目标触达，涉及自碰撞、地面接触和关节限制。
    3.  **真实世界验证**：使用 FR3 机械臂执行书架放置任务，避开部署时新增的障碍物。
*   **对比方法（Benchmarks）**：
    *   **纯采样方法**：CEM, MPPI, DA-MPPI（无扩散先验）。
    *   **梯度引导扩散**：GG-DP（反向循环内引导）, PO-DP（去噪后优化）。
    *   **无梯度扩散引导**：Diffusion-ES（基于进化策略的外层循环优化）。

### 4. 资源与算力
*   **硬件环境**：所有实验均在一台配备 **NVIDIA GeForce RTX 4060 GPU** 的工作站上完成。
*   **训练数据**：2D 任务使用 10,000 组演示数据；3D 任务使用 20,000 组无障碍物的演示数据。
*   **推理耗时**：GRACE (CNN版本) 在 3D 任务中的总计算时间约为 179ms，其中引导计算耗时约 90ms，表现出较好的实时性潜力。

### 5. 实验数量与充分性
*   **实验规模**：仿真实验中每个任务进行了 **30 组随机试验**；真实世界任务进行了 **10 组对比试验**。
*   **充分性评价**：
    *   **多维度评估**：涵盖了成功率、碰撞率、路径长度、计算时间和多模态保持能力。
    *   **消融与对比**：对比了不同骨干网络（UNet vs. CNN）以及多种主流基准，实验设计较为客观、公平。
    *   **多模态分析**：专门评估了引导过程是否破坏了扩散模型原有的多模态分布特性。

### 6. 主要结论与发现
*   **性能领先**：GRACE 在 2D 和 3D 仿真任务中均取得了最高的成功率（3D 任务达 90%），显著优于梯度引导方法。
*   **安全性高**：相比梯度引导方法容易在窄缝处陷入局部最优导致碰撞，GRACE 通过采样能更好地跳出局部极值，碰撞率极低。
*   **保持多样性**：GRACE 在引导动作满足约束的同时，能够保留扩散先验所学到的多种可行路径模式。
*   **现实迁移**：成功在真实机械臂上实现了对动态新增障碍物的避障，而未经引导的策略则 100% 发生碰撞。

### 7. 优点
*   **无梯度通用性**：能够处理任何形式的代价函数（包括不可微的黑盒函数），极大地扩展了扩散策略的应用范围。
*   **理论完备**：建立了扩散模型与 MPPI 之间的数学联系，并统一了梯度引导作为其特例。
*   **计算效率**：通过前向并行评估代替复杂的反向传播梯度计算，推理速度较快。

### 8. 不足与局限
*   **采样依赖性**：作为一种基于采样的均值估计方法，如果所有采样点都处于高代价区域（如完全被障碍物包围），加权平均后的均值可能仍无法脱离碰撞。
*   **超参数敏感**：引导协方差（探索噪声）和温度参数 $\lambda$ 的设置对结果影响较大，可能需要针对不同任务进行调优。
*   **静态假设**：虽然能处理部署时新增的障碍物，但实验主要针对静态环境，对高速动态障碍物的处理能力尚待验证。

（完）
