Title: Asynchronous Sensitivity-Based Distributed NMPC

URL Source: https://arxiv.org/pdf/2607.03412v1

Published Time: Tue, 07 Jul 2026 01:07:14 GMT

Number of Pages: 8

Markdown Content:
# Asynchronous Sensitivity-Based Distributed NMPC ⋆

# Maximilian Pierer von Esch a, Andreas Völz a, Knut Graichen a

> a

Chair of Automatic Control, Friedrich-Alexander-Universität Erlangen-Nürnberg 

Abstract 

This paper presents a cooperative distributed model predictive control (MPC) scheme for nonlinear continuous-time systems. The centralized optimal control problem is solved asynchronously via a fixed number of sensitivity-based distributed program-ming (SBDP) iterations. The proposed scheme requires only neighbor-to-neighbor communication and no synchronization between agents during optimization. Under nominal MPC stability and bounded information delay, local exponential stability is established for a sufficiently large number of per-agent SBDP iterations. Numerical and hardware-in-the-loop results on both Ethernet and Wi-Fi demonstrate the benefits of an asynchronous execution, reducing execution times by over 60 % while maintaining comparable closed loop performance. 

Key words: model predictive control, distributed and asynchronous optimization, multi-agent systems, distributed control 

1 Introduction 

The distributed operation of large-scale interconnected systems is a central challenge in modern cyber-physical applications, including energy systems [17], process en-gineering [9], and autonomous mobile robots [14]. Model predictive control (MPC) provides a systematic frame-work for their control through the repeated solution of an optimal control problem (OCP) which accounts for cou-pled system dynamics and constraints. However, the cen-tralized solution of the resulting high-dimensional OCPs quickly becomes intractable in real-time MPC applica-tions. Distributed model predictive control (DMPC) ad-dresses this issue by exploiting the structure of large-scale systems and distributing the computational ef-fort among multiple agents. In particular, cooperative DMPC seeks to retain the stability properties and per-formance of centralized MPC while preserving the scal-ability and modularity of distributed implementations. In contrast, non-cooperative schemes require less com-munication but usually do not achieve the same qual-ity as centralized control [10]. For cooperative DMPC, a variety of distributed optimization algorithms have been utilized, such as dual decomposition [6], the alter-nating direction method of multipliers [3], decentralized sequential quadratic programming [15], and distributed sensitivity-based programming (SBDP) [12]. 

⋆ This paper was not presented at any IFAC meeting. Cor-responding author M. Pierer v. Esch. 

Email addresses: maximilian.v.pierer@fau.de 

(Maximilian Pierer von Esch), andreas.voelz@fau.de 

(Andreas Völz), knut.graichen@fau.de (Knut Graichen). 

However, most existing cooperative DMPC schemes implicitly rely on a synchronous execution of these al-gorithms, requiring strict coordination between agents and introducing idle times due to communication de-lays and heterogeneous computational loads. To address these limitations, asynchronous distributed optimiza-tion methods have recently gained attention, allowing agents to update using locally available and potentially outdated information [1,12]. While this reduces waiting times and synchronization overhead, it also introduces additional challenges in terms of convergence and closed loop stability, particularly in real-time MPC implemen-tations where only a finite number of iterations can be performed per sampling step. This paper addresses these challenges and develops a cooperative DMPC scheme where a centralized OCP is solved in an asynchronous fashion via SBDP. Asyn-chronism is restricted to the optimization layer, i.e., SBDP iterations are performed asynchronously within each MPC step while the MPC steps are synchronized between agents. This, for example, formally enables the use of best-effort UDP-based communication proto-cols instead of slower connection-oriented protocols like TCP, representing an important step toward real-time DMPC. Building on R-linear convergence properties of the asynchronous execution of SBDP [12], we derive conditions on the number of per-agent iterations to en-sure local exponential stability of the closed loop system despite inexact optimization. The standing assumptions are that the centralized MPC formulation is stabilizing and that the delay of the information in the network is upper bounded. The stability analysis is inspired by 

Preprint submitted to Automatica 7 July 2026 

> arXiv:2607.03412v1 [math.OC] 3 Jul 2026

recent suboptimal (D)MPC investigations, including MPC frameworks with a fixed number of optimizer it-erations per sampling step [7,8], the class of real-time iterations (RTI) [4,18], and stability analyses for syn-chronous DMPC [2,11,15]. In contrast to these works, we consider asynchronous distributed optimization with an R-linearly convergent algorithm in a continuous-time DMPC framework. Moreover, the optimization error is not required to decrease monotonically, rendering stan-dard RTI arguments based on Q-linear convergence not directly applicable. The rest of the paper is structured as follows: Section 2 introduces the problem statement, while Section 3 developes the DMPC scheme. After-ward, stability of the closed loop is analyzed in Section 4, before a simulative and hardware-in-the-loop valida-tion is given in Section 5. Concluding remarks are made in Section 6. Notation: The stacking of vectors vi ∈ Rni , i ∈ V from a set V ⊂ N is denoted as [vi]i∈V = [ v⊤ 

> 1

, . . . , v⊤|V| ]⊤, while for gi(·) ∈ C([0 , T ]; Rni ) it is g(·) = ( gi(·)) i∈V with 

g(t) = [ g1(t)⊤, . . . , g|V| (t)⊤]⊤, ∀t ∈ [0 , T ]. The block-maximum norm of a vector [vi]i∈V ∈ Rn is given as 

∥v∥b = max i∈V ∥vi∥2, while for vector functions g(·) ∈

C([0 , T ]; Rn) we use the supremum norm ∥g(·)∥b, ∞ =sup t∈[0 ,T ] ∥g(t)∥b. An open neighborhood of a point v0 ∈

Rn is defined as Br (v0) := {v ∈ Rn | ∥ v − v0∥b < r },while for g0(·) ∈ C([0 , T ]; Rn) it is given as Br (g0(·)) := 

{g(·) ∈ C([0 , T ]; Rn) | ∥ g(·) − g0(·)∥b, ∞ < r }.

2 Problem statement 

We consider multi-agent systems described by an undi-rected, connected graph G(V, E) where each vertex i ∈V = 1, . . . , M represents an agent and the edge set 

E ⊂ V × V characterizes the coupling structure between subsystems. For each agent i, the open neighborhood is defined as Ni := {j ∈ V \{ i} | (i, j ) ∈ E} and contains all subsystems directly coupled to agent i, while the closed neighborhood is denoted as ¯N i := Ni ∪ { i}. We con-sider a DMPC scheme for setpoint stabilization where the agents cooperatively solve the centralized OCP 

min 

> ¯u(·)

X

> i∈V

Ji(¯ui(·); ( ¯xj (·)) j∈N i ) (1a) 

s.t. ˙¯xi(τ ) = f i(¯xi(τ ), ¯ui(τ ), [¯xj (τ )] j∈N i ) (1b) 

¯xi(0) = ξi (1c) 

¯ui(τ ) ∈ Ui , τ ∈ [0 , T ] , ∀i ∈ V (1d) in a distributed and possibly asynchronous fashion at each sampling time tk = t0 + k∆t, k ∈ N0 for a fixed sampling period ∆t > 0. The state and input of each agent are denoted as xi(t) ∈ Rni and ui(t) ∈ Rmi , re-spectively. The bar notation denotes optimization vari-ables depending on the prediction time τ ∈ [0 , T ], in contrast to system variables depending on system time 

t > 0. OCP (1) is parametrized by the initial condition 

ξi ∈ Rni , which is instantiated at each sampling time 

t = tk as ξi = xi,k = xi(tk). The local constraint set 

Ui ⊂ Rmi is supposed to be nonempty, compact and convex. The local cost functional is given as 

Ji = Li(¯xi(T ))+ 

Z T

> 0

li(¯xi(τ ), ¯ui(τ ), [¯xj (τ )] j∈N i ) d τ (2) with prediction horizon T ≥ ∆t. Furthermore, we pre-suppose a neighbor-affine structure for costs (2) and dy-namics (1b), i.e., f i = f ii (xi, ui) + P 

> j∈N i

f ij (xi, xj )

and li = lii (xi, ui) + P 

> j∈N i

lij (xi, xj ). The centralized quantities are obtained by stacking all local variables as x(t) = [ xi(t)] i∈V ∈ Rn, u(t) = [ ui(t)] i∈V ∈ Rm,

ξ = [ ξi]i∈V ∈ Rn, xk = [ xi,k ]i∈V ∈ Rn, and U =Πi∈V Ui ⊂ Rm, with n = P 

> i∈V

ni and m = P 

> i∈V

mi. A majority of the subsequent stability analysis is based on an equivalent centralized representation of OCP (1) 

V (ξ) = min  

> ¯u(·)

L(¯x(T )) + 

Z T

> 0

l(¯x(τ ), ¯u(τ )) d τ (3a) 

s.t. ˙¯x(τ ) = f (¯x(τ ), ¯u(τ )) , ¯x(0) = ξ (3b) 

¯u(τ ) ∈ U , τ ∈ [0 , T ] (3c) which is obtained by appropriately stacking the local quantities. Specifically, the centralized dy-namics are given as f : Rn × Rm → Rn with 

f (x, u) := [ f i(xi, ui, [xj ]j∈N i )] i∈V and are supposed to be at least twice continuously differentiable. With-out loss of generality the origin is an equilibrium of the system for u = 0, i.e., f (0, 0) = 0. The objective (3a) consists of the terminal costs L : Rn → R and integral costs l : Rn × Rm → R with L(x) := [ Li(xi)] i∈V as well as l(x, u) := [ li(xi, ui, [xj ]j∈N i )] i∈V and are supposed to be at least twice continuously differentiable. Further-more, we assume ¯u(·) ∈ C([0 , T ]; U). For a time τ ∈

[0 , T ], initial condition ¯x(0) = ξ, and input trajectory 

¯u(·), define Ψ : [0 , T ] × Rn × C([0 , T ]; U) → Rn as the solution map of (3b), i.e., Ψ(τ ; ξ, ¯u(·)) := x(τ ; ξ, ¯u(·)) .We require the existence of optimal control trajectories 

¯u∗(·; ξ) of the central OCP (3) and the corresponding state trajectory ¯x∗(·; ξ) := Ψ(·; ξ, ¯u∗(·; ξ)) . Specifically, we assume the existence of a compact set X0 ⊂ Rn with 

0 ∈ X0 and a set T := (0 , ˆT ] with some ˆT ∈ R>∆t such that for all ξ ∈ X0 and T ∈ T, the OCP (3) has a locally unique minimizer (¯u∗(·; ξ), ¯x∗(·; ξ)) . The value function of OCP (3) is denoted as V : X0 → R.Standard MPC schemes assume that the optimal solu-tion is available at each sampling time tk. Then, the ini-tial segment of ¯u∗(τ ; xk) is applied to the system, i.e., 

u(tk + τ ) = ¯u∗(τ ; xk) , τ ∈ [0 , ∆t) . (4) At the next sampling instant tk+1 , OCP (3) is resolved using the updated initial condition which, in the nominal case, is given by x∗ 

> k+1

= Ψ(∆ t; xk, ¯u∗(·; xk)) . In multi-agent systems with many agents, it increasingly becomes intractable to solve the OCP (1) in a centralized fashion. Therefore, the remainder of this paper is concerned with the distributed and asynchronous solution of OCP (1) 2and the stability analysis of the closed loop resulting from the proposed DMPC control law. 

3 Asynchronous sensitivity-based DMPC 

This section presents the distributed solution of OCP (1) via asynchronous SBDP (a-SBDP) within a real-time DMPC framework. The algorithm exploits sensitivity in-formation between neighboring agents to augment the local cost functional and is based on primal decomposi-tion to achieve a coordinated decoupling between agents. 

3.1 Asynchronous SBDP and DMPC control law 

Each agent i ∈ V constructs a local decoupled OCP, which is solved iteratively in each per-agent SBDP iter-ation qi = 0 , 1, . . . 

min 

> ¯ui(·)

¯Jqi 

> i

(¯ui(·); ξi) := Ji(¯ui(·); ( ¯xqj 

> j

(·)) j∈N i , ξi) (5a) 

+ X

> j∈N i

Z T

> 0

gji (¯xqi 

> i

(τ ), ¯xqj 

> j

(τ ), ¯λqj 

> j

(τ )) ⊤δ ¯xi(τ ) d τ

s.t. ˙¯xi(τ ) = f i(¯xi(τ ), ¯ui(τ ), [¯xqj 

> j

(τ )] j∈N i ) (5b) 

¯xi(0) = ξi, ¯ui(τ ) ∈ Ui , τ ∈ [0 , T ] (5c) with δ ¯xi(τ ) = ¯xi(τ )− ¯xqi 

> i

(τ ) and where gji (·) is given by 

gji (xi, xj , λj ) = ∇xi lji (xj , xi)+ ∇xi fji (xj , xi)⊤λj (6) and can be interpreted as the sensitivity of the cost func-tional Jj (·) w.r.t. a change in the state trajectory ¯xi(·),

i ∈ V . In (6), ¯λj (τ ) ∈ Rnj denotes the adjoint state of the neighbor associated with the local dynamics (5b) which is typically available from the solution of (5). Oth-erwise, ¯λqi+1  

> i

(·) may be computed by solving 

˙¯λqi+1  

> i

(τ ) = −∇ xi Hqi 

> i

(¯xqi+1  

> i

(τ ), ¯uqi+1  

> i

(τ ), ¯λqi+1  

> i

(τ )) (7) with the local Hamiltonian Hqi 

> i

:= li(xi, ui, [xqj 

> j

]j∈N i )+ 

λ⊤ 

> i

f i(xi, ui, [xqj 

> j

]j∈N i )+ P 

> j∈N i

gji (xqi 

> i

, xqj 

> j

, λqj 

> j

)⊤(xi−

xqi ) and terminal condition ¯λi(T ) = ∇xi Li(¯xi(T )) . Let 

pqi 

> i

(τ ; ξi) := [ ¯xqi 

> i

(τ ; ξi)⊤, ¯λqi 

> i

(τ ; ξi)⊤]⊤, τ ∈ [0 , T ] de-note the iterate of agent i ∈ V , then the asynchronous execution of SBDP from a local viewpoint is given in Al-gorithm 1 [11]. Each iteration consists of solving OCP (5) followed by a communication step. Unlike the syn-chronous execution, agents do not wait for updated in-formation from all neighbors and directly proceed with the currently available trajectories. This is reflected in the use of local iteration counters qi, whereby agents may rely on outdated neighbor information indexed by 

qj . Consequently, in the asynchronous case one gener-ally has qi̸ = qj , whereas in a synchronous execution the counters are enforced to satisfy qi = qj .In the context of a real-time DMPC scheme, Algorithm 1 is used to solve the centralized OCP (1) in an asyn-chronous fashion and is terminated after a fixed num-

Algorithm 1 a-SBDP for solving OCP (1) [11] 

0: Initialize and send p0 

> i

(τ ; ξi), τ ∈ [0 , T ] to all neigh-bors j ∈ N i; Set ¯qi, initial condition ξi, and qi ← 0

1: Compute (uqi+1  

> i

(·), pqi+1  

> i

(·)) by solving (5). 

2: Broadcast pqi+1  

> i

(·) to all neighbors j ∈ N i.

3: Receive pqj +1  

> j

(·) from a subset of neighbors. 

4: Stop if qi = ¯ qi − 1 . Otherwise, go to line 1 with 

qi ← qi + 1 .ber of local iterations ¯qi have been executed. The result-ing approximate solution ˆu(τ ; xk) := [ ¯u¯qi 

> i

(τ ; xi,k )] i∈V ,

τ ∈ [0 , T ], is used to define the DMPC control law 

u(tk + τ ) = ˆu(τ ; xk) , τ ∈ [0 , ∆t) (8) which is computed locally by each agent. The central-ized closed loop evolution under (8) is given as xk+1 =

Ψ(∆ t; xk, ˆu(·; xk)) . Furthermore, Algorithm 1 is warm-started with the previous solution, i.e., p0 

> i

(τ ; xi,k +1 ) = 

p¯qi 

> i

(τ ; xi,k ), τ ∈ [0 , T ] which additionally can be shifted by ∆t with a suitable tail approximation if desired. 

3.2 Characterization of the asynchronous process 

While Algorithm 1 is directly suitable for implementa-tion, a global description of the asynchronous execution is required for its convergence analysis. To this end, we introduce an abstract local solution operator associated with each agent. For each agent i ∈ V , we interpret (5) as a parametric OCP, where the parameters are given by the external trajectories (pj (·)) j∈ ¯N i . Under the assump-tion that this OCP admits a locally unique solution, we define the solution operator Φi : Π j∈ ¯N i C([0 , T ]; Rpj ) →

C([0; T ], Rpi ) which maps the external trajectories to the corresponding local primal-dual solution trajectory of agent i, i.e., 

Φi(( pj (·)) j∈ ¯N i ; ξi) := pi(·; ( pj (·)) j∈ ¯N i , ξi) . (9) Equivalently, we obtain (Φi(( pj (·)) j∈ ¯N i ); ξi)i∈V =

Φ(p(·); ξ) = ( ¯x(·; p(·), ξ), ¯λ(·; p(·), ξ)) , p(·) = ( pi(·)) i∈V 

as the stacked solutions of the OCPs (5) and ¯u(·; p(·), ξi)

as the stacked control. To describe the asynchronous process centrally, let S(q) ∈ V denote the set of agents that perform an update during the global update event 

q, where q = 0 , 1, . . . indexes the sequence of update events across the network. Here, we consider the edge case where S(q) = {a(q)} is a singleton, i.e., each global update event corresponds to exactly one agent perform-ing its local update, leading to ¯q := P 

> i∈V

¯qi per-agent updates being performed globally within one MPC step. Then, Algorithm 1 is described by the recursion 

pq+1  

> i

(·; ξi) = 

(

Φi(( pq−dij (q) 

> j

(·; ξj ); ξi)j∈ ¯N i ) , i = a(q)

pqi (·; ξi) , i̸ = a(q)

with dij (q) ∈ N0 denoting the delay, measured in global per-agent updates, of the trajectory pj (·; ξj ), j ∈ N i,3used by agent i ∈ V at update q. Consequently, agent i ∈V performs its update using trajectories generated dij (q)

update events earlier. The delays therefore quantify the age of the information available to agent i ∈ V and are generally unknown a priori. Such delays may arise from heterogeneous solution times of the local OCPs or com-munication latencies during trajectory exchange. Fur-thermore, we assume that either the initial trajectories 

p0 

> i

(·) are transmitted without delay or that each agent initializes p0 

> j

(·) with the corresponding values stored by neighboring agents j ∈ V . To ensure that the asyn-chronous process remains well defined and that informa-tion does not become arbitrarily outdated, the degree of asynchronism is restricted by the following assumption. 

Assumption 1 There exist finite integers d ∈ N0 and 

s ∈ N such that i) the delays are uniformly bounded, i.e., 

0 ≤ dij (q) ≤ d < ∞ , ∀i ∈ V , ∀j ∈ N i. (10) 

and ii) that every agent performs an update at least in every s consecutive iterations, i.e., 

{a(q)} ∪ { a(q + 1) } ∪ · · · ∪ { a(q + s − 1) } = V . (11) This assumption is often referred to as partial asynchro-nism in literature which is in contrast to total asynchro-nism where delays may become unbounded [1]. The first condition ensures that information used by any agent is at most d update events old, while the second ensures that no agent permanently drops out of the process, e.g., hardware failure. A synchronous execution is recovered by setting S(q) = V for all q and enforcing dij (q) = 0 ,i.e., all agents update simultaneously at each event and no information gets lost, leading to s = 1 and d = 0 in Assumption 1. In this case, the event index q coincides with a classical synchronous iteration index. 

3.3 Algorithmic analysis 

In this section, we analyze the convergence of asyn-chronous SBDP as a prerequisite for the subsequent sta-bility analysis of the closed loop system under the con-trol law (8). The analysis mainly follows [11], where con-vergence of asynchronous SBDP was first established. In contrast to [11], however, we avoid convexity assump-tions as well as assumptions directly imposed on solv-ability of the local OCPs (5). Instead, the properties are derived from assumptions imposed solely on the central-ized solutions. To establish existence and local uniqueness of the solu-tions of OCP (5), we employ a coercivity condition which mirrors the standard second-order sufficiency condition from nonlinear programming. To formulate this condi-tion, define Ai(τ ) = ∇xi f ∗ 

> i

(τ ), Bi(τ ) = ∇ui f ∗ 

> i

(τ ),and P i(T ) = ∇2 

> xixi

Li(¯x∗ 

> i

(T )) , Qi(τ ) = ∇2

> xixi

¯H∗ 

> i

(τ ),

Ri(τ ) = ∇2 

> uiu

¯H∗ 

> i

(τ ), Si(τ ) = 2 ∇2

> xiui

¯H∗ 

> i

(τ ), τ ∈ [0 , T ]

where f ∗ 

> i

(τ ) := f i(¯x∗ 

> i

(τ ; ξ), ¯u∗ 

> i

(τ ; ξ), ¯x∗Ni (τ ; ξ)) as well as ¯H∗ 

> i

(τ ) := H∗ 

> i

(¯x∗ 

> i

(τ ; ξ), ¯u∗ 

> i

(τ ; ξ), ¯λ∗ 

> i

(τ ; ξ)) denote the evaluation of dynamics (1b) and Hamiltonian (7) along the solution for each ξ ∈ X0, cf. [5, Sec. 6]. 

Assumption 2 For any i ∈ V , ξ ∈ X0, and T ∈ T there exists a constant ρi ∈ R>0 such that 

∥δxi(T )∥2   

> Pi(T)

+

Z T

> 0



∥δxi(τ )∥2  

> Qi(τ)

+ ∥δui(τ )∥2 

> Ri(τ)

+ δxi(τ )⊤Si(τ )δui(τ ) d τ



≥ ρi

Z T

> 0

∥δui(τ )∥2 dτ (12) 

holds for all perturbations δui(·) ∈ C([0 , T ]; Ui) where 

δxi(τ ) is the solution of the linearized dynamics δ ˙xi(τ ) = 

Ai(τ )δxi(τ ) + Bi(τ )δui(τ ) with δxi(0) = 0, τ ∈ [0 , T ].

The assumption neither implies convexity of the dynam-ics (1b) nor of the costs (2) and can be enforced algorith-mically by adding regularization terms γi 

> 2

(∥xi − xqi 

> i

∥2 +

∥ui − uqi 

> i

∥2) with sufficiency large penalty γi ∈ R≥0 to the integral costs in (5a). The next Lemma utilizes As-sumption 2 and applies a regularity result from paramet-ric optimal control to the local OCPs, cf. [5, Thm. 5]. 

Lemma 3 Let Assumption 2 hold. Then, for every ξ ∈

X0 and T ∈ T there exist constants r′

> 1

, r1, L ∈ R>0

such that for all p(·) ∈ B r1 (p∗(·; ξ)) the local OCPs (5) 

admit a locally unique minimizer (¯u(·; p(·)) , ¯x(·; p(·)) ∈Br′ 

> 1

(( u∗(·; ξ), x∗(·; ξ))) . Moreover, the associated adjoint trajectory ¯λ(·; p(·)) is bounded and locally unique and the mapping Φ(·) is Lipschitz continuous in the sense of 

∥¯u(·; p(·)) − ¯u∗(·; ξ)∥b, ∞ + ∥¯x(·; p(·)) − ¯x∗(·; ξ)∥b, ∞

+∥¯λ(·; p(·)) −¯λ∗(·; ξ)∥b, ∞ ≤ L∥p(·)−p∗(·; ξ)∥b, ∞ . (13) 

PROOF. At first, we observe that for p(·) = p∗(·; ξ) we get that ¯x(τ ; p∗(·; ξ), ξ) = ¯x∗(τ ; ξ), ¯u(τ ; p∗(·; ξ), ξ) = 

u∗(τ ; ξ) and ¯λ(τ ; p∗(·; ξ), ξ) = ¯λ∗(τ ; ξ) are solutions of the concatenated local OCPs (5) since their optimality conditions are structurally identical to OCP (3). The re-sult then follows by a direct application of [5, Thm. 5]. Although the theorem is stated for the functional set-ting ¯u(·) ∈ L∞ and ¯x(·), ¯λ(·) ∈ W 1,θ , θ ∈ [1 , ∞], it also applies in the present setting since, on the bounded in-terval [0 , T ], the continuous embeddings C ⊂ L∞ and 

C ⊂ W 1,θ hold for all θ ∈ [1 , ∞]. Moreover, all assump-tions of [5, Thm. 5] are satisfied: i) the constraint set U is nonempty, closed and convex; ii) all involved functions in the local OCPs (5) are twice continuously differentiable; iii) the coercivity Assumption 2 holds; and iv) we assume existence and local uniqueness of the optimal solutions. Finally, the uniqueness and boundedness of ¯λ(·; p(·)) 

follows from the uniqueness of (¯u(·, ¯p(·)) , ¯x(·; p(·)) and the adjoint dynamics whose right-hand-side is linear in 

λ and continuous w.r.t. τ for all τ ∈ [0 , T ] such that standard arguments imply the existence, uniqueness and boundedness of ¯λ(τ ; p(·)) , τ ∈ [0 , T ] [16, Sec. 2.4.2]. 2

Lemma 3 plays a central role for two reasons. First, it guarantees well-posedness of Algorithm 1, ensuring ex-istence and local uniqueness of solutions to the local OCPs (5). Second, it establishes Lipschitz continuity of 4these solutions w.r.t. the external trajectories. Based on Lemma 3, the following convergence result can be shown. 

Theorem 4 Under Assumptions 1 and 2, there exists a horizon length 0 < ¯T for every ξ ∈ X0 and p0(·) ∈Br1 (p∗(·; ξ)) such that for any T < ¯T there exists a con-stant 0 < C < 1 such that the error eq (ξ) := ∥pq (·; ξ) −

p∗(·; ξ)∥b, ∞ of Algorithm 1 converges R-linearly, i.e., 

eq (ξ) ≤ C⌊ qs+d ⌋e0(ξ) , q = 0 , 1, . . . (14) 

holds and the iterates satisfy pq (·; ξ) ∈ B r1 (p∗(·; ξ)) .

PROOF. The proof follows from a direct application of [11, Thm. 1]. We verify that all requirements are sat-isfied: i) Assumption 1 coincides with [11, Ass. 1]; ii) the assumption of the existence of a locally unique central-ized solution implies [11, Ass. 2] where the uniqueness of (¯u∗(·; ξ), ¯x∗(·; ξ)) together with the adjoint dynamics implies the uniqueness and boundedness of ¯λ∗(·; ξ) as in Lemma 3; iii) Let eqi (ξ) := ∥pqi (·; ξ) − p∗ 

> i

(·; ξ)∥∞, then [11, Thm. 1] requires the estimate 

eq+1  

> i

(ξ)



≤ C max   

> j∈¯Ni

eq−dij (q) 

> j

(ξ) , i = a(q)= eqi (ξ) , i̸ = a(q)

(15) to hold which results by applying [12, Lem. 2] for i = a(q)

and realizing that for i̸ = a(q) the error remains un-changed. The assumptions of [12, Lem. 2] are met since i) Lemma 3 directly implies [12, Ass. 3], ii) optimal and it-erated trajectories are bounded on compact sets which is ensured by the fact that (¯u(·; p(·)) , ¯x(·; p(·)) , ¯λ(·; p(·))) 

are confined to compact sets according to Lemma 3. Finally, the positive invariance of Br1 (p∗(·; ξ)) follows from the contraction estimate (15) and Assumption 1 which ensures uniformly bounded delays dij ≤ d. To this end, taking the maximum over (15) w.r.t. the de-lay interval yields eq+1 (ξ) ≤ max ℓ∈{ q−d,...,q } Ce ℓ(ξ). Be-cause C < 1 for all T < ¯T and e0(ξ) ≤ r1, induc-tion implies eq (ξ) ≤ e0(ξ) ≤ r1, ∀q ≥ 0, and therefore 

pq (·; ξ) ∈ B r1 (p∗(·; ξ)) for all iterations. Consequently, all iterates remain inside the region where Lemma 3 and the contraction estimate remain valid. 2

Theorem 4 establishes R-linear convergence of a-SBDP. This is in contrast to the synchronous case, where Q-linear convergence can be guaranteed under the same assumptions [12] and requires a careful consideration in the following stability analysis. 

4 Stability analysis 

This section is concerned with establishing conditions under which the closed loop system resulting from ap-plying the asynchronous DMPC control law (8) obtained after a limited number of global per-agent iterations 

¯q = P 

> i∈V

¯qi is stable. To this end, we make the follow-ing assumption concerning the value function V (·) asso-ciated with OCP (3). This assumption ensures that V (·)

serves as a Lyapunov function for the closed loop sys-tem under the optimal MPC control law (4) and fulfills certain regularity conditions. 

Assumption 5 Assume that there exist constants α, a1,

a2, a3 ∈ R>0 with a3 < 1 such that for any ξ ∈ Xα := 

{ξ ∈ X0 | V (ξ) ≤ α} the following holds 

a1∥ξ∥2 

> b

≤ V (ξ) ≤ a2∥ξ∥2 

> b

(16a) 

V (Ψ(∆ t; ξ, ¯u∗(·; ξ))) ≤ V (ξ) − a3∥ξ∥2 

> b

. (16b) 

In addition, it is assumed that V (·) is Lipschitz continu-ous on X0 and twice continuously differentiable at ξ = 0

with V (ξ) > 0 for ξ ∈ X0 \ Xα.

This assumption is formulated independently of a spe-cific centralized OCP design to retain flexibility in the choice of the MPC formulation. In the context of DMPC, it can, for example, be enforced through suitable sepa-rable terminal costs [12] or by invoking a cost controlla-bility condition in combination with a sufficiently long prediction horizon [13]. The (exponential) asymptotic stability of the origin with region of attraction Xα under the optimal control law (4) then follows from standard MPC-arguments. In similar spirit, the next assumption requires Lipschitz-like estimates of the change of optimal solutions of OCP (1) w.r.t. perturbations of the initial state ξ ∈ Xα combined with a regularity assumption on the solution map Ψ(·) of (3b). 

Assumption 6 There exist constants Lp, L u ∈ R>0

such that for any ξ ∈ Xα and ξ′ ∈ X0, the following holds 

∥p∗(·; ξ′) − p∗(·; ξ)∥b, ∞ ≤ Lp∥ξ′ − ξ∥b (17a) 

∥u∗(·; ξ′) − u∗(·; ξ)∥b, ∞ ≤ Lu∥ξ′ − ξ∥b . (17b) 

Moreover, we assume Ψ(τ ; ξ, u(·)) ∈ X0 for all ξ ∈ Xα,

u(τ ) ∈ U, and τ ∈ [0 , ∆t).

The next Lemma provides an estimate between the sys-tem states resulting from applying the MPC and DMPC control laws (4) and (8), respectively, as well as a bound on the rate at which a state can change under (8). 

Lemma 7 Let Assumptions 2 and 6 hold. Then, for ξ ∈

Xα and p(·) ∈ B r1 (p∗(·; ξ)) , there exist constants b′

> 1

, b ′ 

> 2

∈

R>0 such that the following estimates hold 

∥ξ+ − ξ∗

> +

∥b ≤ b′

> 1

∥p(·) − p∗(·; ξ)∥b, ∞ (18a) 

∥ξ+ − ξ∥b ≤ b′

> 2

∥ξ∥b + b′

> 1

∥p(·) − p∗(·; ξ)∥b, ∞ (18b) 

with ξ+ = Ψ(∆ t; ξ, ˆu(·; ξ)) and ξ∗ 

> +

= Ψ(∆ t; ξ, u∗(·; ξ)) .

PROOF. To prove (18a), consider the difference of the respective state trajectories generated by (3b) for 

u∗(·; ξ) = u(·; p∗(·), ξ) and ˆu(·; p(·), ξ). Then, we have 

∥ξ+−ξ∗

> +

∥b =∥Ψ(∆ t; ξ,ˆu(·; p(·), ξ)) −Ψ(∆ t; ξ, u∗(·; ξ)) ∥b

≤

Z ∆t

> 0

∥f (x(τ ), ˆu(·; p(·), ξ)) − f (x∗(τ ), u∗(τ ; ξ)) ∥b dτ

≤ ∆tL f,u LeLf,x ∆t∥p(·) − p∗(·; ξ)∥b, ∞ (19) 5where Lf,x , L f,u , L ∈ R>0 follow from the differentiabil-ity of f on X0 and Lemma 3. The last step uses Gron-wall’s lemma, proving (18a) with b′ 

> 1

= ∆ t L f,u L e Lf,x ∆t.For (18b), consider the centralized dynamics (3b) under the DMPC control law (8), i.e., 

∥ξ+ − ξ∥b = ∥Ψ(∆ t; ξ, ˆu(·; p(·), ξ)) − ξ∥b =: ∥r(∆ t)∥b

≤

Z ∆t

> 0

∥f (x(τ ) + r(τ ), ˆu(·; p(·), ξ)) ∥b dτ

≤ ∆teLf,x ∆t(Lf,x ∥ξ∥b + Lf,u ∥ˆu(·; p(·), ξ)∥b, ∞) , (20) where the last inequality again follows from Gronwall’s lemma. Moreover, we have 

∥ˆu(·; p(·), ξ) − u∗(·; ξ) + u∗(·; ξ) − u∗(·; 0)∥b, ∞

≤ L∥p(·) − p∗(·; ξ)∥b, ∞ + Lu∥ξ∥b , (21) using u∗(·; 0) = 0. Substituting (21) into (20) yields (18b) with b′ 

> 2

= ∆ t(Lf,x + Lf,u Lu)e Lf,x ∆t. 2

Lemma 7 and Assumption 5 allow us to quantify the effect of the error ∥p(·) − p∗(·, ξ)∥b, ∞ on the nominal Lyapunov decrease condition (16b). 

Lemma 8 Let Assumptions 2, 5, and 6 hold and let a :=  

> a3
> a2

. Then, there exists a constant b1 ∈ R>0 such that for any ξ ∈ Xα and pq (·; ξ) ∈ B r1 (p∗(·; ξ)) , we have 

V (ξ+) ≤ (1 − a)V (ξ) + b1eq (ξ) . (22) 

PROOF. Under Assumption 5 there exists a constant 

LV ∈ R>0 such that |V (ξ′) − V (ξ)| ≤ LV ∥ξ′ − ξ∥b holds for any ξ′, ξ ∈ X0 which implies 

V (ξ+) ≤ V (ξ∗

> +

)+ LV ∥ξ+ − ξ∗

> +

∥b ≤ (1 − a)V (ξ)+ b1eq (ξ)

with Assumption 5 and (18a) in Lemma 7 being applied. This proves (22) with b1 = LV b′

> 1

. 2

The following result generalizes the convergence re-sult (14) in Theorem 4 to the case where the evolution of the initial state ξ is governed by the DMPC control law (8) and gives an estimate of the error eq (ξ+) in terms of the previous error eq (ξ) and the value function V (ξ).

Lemma 9 Let Assumptions 1, 2, 5, and 6 hold. Then, there exist constants ¯α, r 2, b 2, b 3 ∈ R>0 such that for any 

ξ ∈ X¯α and pq (·; ξ) ∈ B r2 (p∗(·; ξ)) , we have 

eq (ξ+) ≤ C⌊ qs+d ⌋(b2

pV (ξ) + b3eq (ξ)) . (23) 

PROOF. For ¯α := min { r1 

> Lpb′
> 2
> √a1

, α } > 0 and r2 := 

r1 − Lpb′

> 2

√a1 ¯α > 0, we get that e0(ξ+) ∈ B r1 (p∗(·; ξ+)) 

and can apply (14) from Theorem 4 at ξ+, i.e., 

eq (ξ+) ≤ C⌊ qs+d ⌋(eq (ξ) + ∥p∗(·; ξ+) − p∗(·; ξ)∥b, ∞)

≤ C⌊ qs+d ⌋(eq (ξ) + Lp∥ξ+ − ξ∥b) , (24) using the warm start and (17a) in Assumption 6. Com-bining (24) with Lemma 7 and (16a) from Assumption 5 yields (23) with b2 = Lpb′

> 2

√a2 and b3 = 1 + Lpb′

> 3

. 2

The error bounds given in Lemmas 8 and 9 define a discrete-time dynamical system which will be used to infer stability properties of the closed loop system. Let 

w1[k] := V (xk) and w2[k] := eq (xk) denote the value function and optimization error at MPC step k, respec-tively. Then, by (22) and (23), the system 

w1[k + 1] = (1 − a)w1[k] + b1w2[k] (25a) 

w2[k + 1] = C⌊ qs+d ⌋(b2

pw1[k] + b3w2[k]) , (25b) with w1[0] = ¯ α and w2[0] = C⌊ qs+d ⌋r2, provides an up-per bound on the evolution of the quantities V (xk) and 

eq (xk) along the closed loop trajectory. The next lemma establishes a positive invariant set for the closed loop extended state z[k] := ( xk, pq (·; xk)) using (25). 

Lemma 10 Let Assumptions 1, 2, 5 and 6 hold and let 

r := min { ¯αa 3 

> b1a1

, r 2}. Moreover, define the set 

Γ := {ξ ∈ Rn, p(·) ∈ C([0 , T ]; Rp) |

V (ξ) ≤ ¯α, ∥p(·) − p∗(·; ξ)∥b, ∞ ≤ r} . (26) 

If the global number of per-agent iterations satisfies 

q > q 1 := 



1 + log C

 rb2

√¯α + b3r

 

(s + d) , (27) 

then Γ is positive invariant for the closed loop system in the sense of z[k] ∈ Γ implies z[k + 1] ∈ Γ, k = 0 , 1, . . . 

PROOF. Let z[k] ∈ Γ. Then, xk ∈ X¯α and pq (·; xk) ∈Br1 (p∗(·; xk)) which allows us to apply Lemma 8 

V (xk+1 ) ≤ (1 − a)V (xk) + b1eq (xk)

≤ (1 − a)¯ α + ¯ αa = ¯ α (28) which implies xk+1 ∈ X¯α. Moreover, Lemma 9 yields 

eq (xk+1 ) ≤ C⌊ qs+d ⌋(b2

pV (xk) + b3eq (xk)) 

≤ C⌊ qs+d ⌋(b2

√¯α + b3r) ≤ r , (29) where (27) is used, implying eq (xk+1 ) ≤ r. 2

The dynamics (25) are difficult to analyze with standard tools as they are not Lipschitz continuous at the origin. However, we can rewrite Lemma 8 in terms of pV (ξ)

instead of V (ξ) to obtain a system which is linear. 

Lemma 11 Let Assumptions 2, 5, and 6 hold. Then, there exists a constant ¯b1 ∈ R>0 such that for any ξ ∈ Xα

and pq (·; ξ) ∈ B r1 (p∗(·; ξ)) , we have 

q

V (ξ+) ≤ p(1 − a)pV (ξ) + ¯b1eq (ξ) . (30) 6PROOF. Assumption 5 implies that pV (ξ) is Lipschitz on X0 with Lipschitz constant ¯LV =max { LV 

> 2√a1

, LV 

> 2√¯δ

} with ¯δ = min ξ∈X0\Xα V (ξ) > 0 such that |pV (ξ′) − pV (ξ)| ≤ ¯LV ∥ξ′ − ξ∥b holds for any 

ξ′, ξ ∈ X0. The rest of the proof directly follows along the lines of the proof of Lemma 8 with ¯b1 = b′ 

> 1

¯LV . 2

Utilizing the Lyapunov decrease estimate (30) instead of (22), consider the recursion defined by (23) and (30) which can be cast as the linear discrete-time system 

ˆw1[k + 1] = p(1 − a) ˆ w1[k] + ¯b1 ˆw2[k] (31a) 

ˆw2[k + 1] = C⌊ qs+d ⌋(b2 ˆw1[k] + b3 ˆw2[k]) (31b) with states ˆw1[k] = pV (xk) and ˆw2[k] = eq (xk) as well as initial conditions ˆw1[0] = √¯α and ˆw2[0] = C⌊ qs+d ⌋r.The next theorem exploits the linearity of (31) to derive conditions for the closed loop stability. 

Theorem 12 Let Assumptions 1, 2, 5, and 6 hold. Then, for z[0] ∈ Γ and the global number of per-agent iterations satisfying ¯q > max {q1, q 2} with 

q2 := 



1 + log C

 ˜a

˜ab 3 + ¯b1b2

 

(s + d) (32) 

and ˜a := 1 − p(1 − a) ∈ (0 , 1) , the origin of the closed loop system is exponentially asymptotically stable under the control law (8) . Moreover, the value function V (xk)

and optimization error eq (xk) decay exponentially. 

PROOF. We first establish exponential stability of (31) under (32) and then use this result to prove exponential stability of the closed loop under (8). Let A(q) denote the system matrix of (31) and 

p(λ) = λ2−tr( A(q)) λ+det( A(q)) its characteristic poly-nomial. Since all entries of A(q) are strictly positive, the Perron-Frobenius theorem implies that λ1 = spec( A(q)) 

is a simple positive eigenvalue and λ2 < λ 1. Hence, A(q)

is Schur stable if and only if λ1 < 1, which is equivalent to p(1) = (1 − λ1)(1 − λ2) > 0. Since λ2 < λ 1 < 1

implies (1 − λ2) > 0, Schur stability follows from 

1 − tr( A(q)) + det( A(q)) > 0 , which yields (32). There-fore, the origin of (31) is exponentially stable. Conse-quently, there exist constants c0 > 0 and c1 ∈ (0 , 1) such that ∥ ˆw[k]∥ ≤ c0ck 

> 1

∥ ˆw[0] ∥ with ˆw = [ ˆ w1, ˆw2]⊤. Thus, it holds that w1[k] ≤ c20c2k 

> 1

∥[pw1[0] , w 2[0]] ⊤∥2 and 

w2[k] ≤ c0ck 

> 1

∥[pw1[0] , w 2[0]] ⊤∥, showing exponential decay of the value function and optimization error, re-spectively. Consider now the closed loop system ˙x(t) = 

f (x(t), ˆu(tk + τ ; xk)) , τ ∈ [0 , ∆t), t ∈ [tk, t k+1 ). Us-ing (16a) yields ∥xk∥ ≤ √M a −1/21

pw1[k] ≤ c2ck 

> 1

∥x0∥,with c2 = √M a −1/21 c0(1 + ∥w2[0] ∥). Standard sampled-data arguments then imply the existence of γ1, γ 2 > 0

such that ∥x(t)∥ ≤ γ1∥x0∥e−γ2t, t ≥ 0, establishing exponential stability of the closed loop system. 2

Theorem 12 establishes exponential stability of the asyn-chronous DMPC scheme provided that the number of global per-agent iterations ¯q is sufficiently large. Since 

¯q corresponds to the total number of agent activations within one MPC step, there is a flexibility in how these activations are distributed among agents, e.g., ¯qi = ¯qM .

5 Simulative and experimental validation 

To illustrate Theorem 12 and validate the asynchronous DMPC scheme, we consider the stabilization of 

˙xi(t) = ui(t) + X

> j∈N i

sin( xj (t)) , xi(0) = xi, 0 (33) at the (unstable) origin with stage costs li(xi, u i) = x2 

> i

+0.1u2 

> i

, input constraints ui(t) ∈ [−2, 2] , and M = 3 .The analysis consists of i) estimating the constants in Theorem 12 to compute ¯q, and ii) a hardware-in-the-loop (HIL) validation where asynchronism naturally occurs. 

i) Numerical validation: Five representative initial conditions |xi, 0| ≤ 1 are considered and the system (33) is controlled using the optimal MPC law (4) and the inexact DMPC law (8). Without terminal costs, nom-inal stability is ensured via a sufficiently long horizon together with exponential controllability in the sense of [13, Ass. 14]. The required candidate control trajectories are chosen as open loop controls from each initial state over the simulation horizon, verifying the assumption for all cases. The resulting worst-case MPC contraction rate is αmpc = 0 .083 (cf. [13, Thm. 1]) for T = 3 and 

∆t = 0 .1. The constants in Assumptions 5 and 6 are obtained by evaluating V (xk), u∗(·; xk), p∗(·; xk), and 

∥xk∥b along closed-loop trajectories, yielding a1 = 0 .31 ,

a2 = 0 .96 , a3 = 0 .12 , Lp = 1 .62 , Lu = 4 .62 , Lf,u = 1 .00 ,

Lf,x = 2 .00 , ¯LV = 0 .27 . The constants C = 0 .58 (The-orem 4) and L = 16 .65 (Lemma 3) are computed from five synchronous iterations compared against the solu-tion at xk. Assumption 2 holds with, e.g., ρi = 0 .1, since 

Qi(τ ) = 1 , τ ∈ [0 , T ]. Finally, the resulting constants in Theorem 12 are ¯b1 = 0 .55 , b2 = 1 .29 , b3 = 4 .30 , and ˜a =0.06 . Thus, for a synchronous execution (s = 1 , d = 0) ,

¯q = 7 > q 2 = 6 .01 iterations guarantee stability, whereas in the asynchronous case the bound scales with the de-gree of asynchronism and becomes q2 = 6 .01( s + d). We empirically verify that ¯q = 7 > q 1 as z[k] ∈ Γ due to 

V (xk) ≤ V (x0) and eq (xk) ≤ eq (x0).

ii) HIL experiment: The asynchronous SBDP-based DMPC scheme is implemented in C ++ and executed on a hardware network of Raspberry Pi 3B+ connected via Ethernet and Wi-Fi for x0 = [0 .75 , 0.5, 0.25] ⊤. A syn-chronous execution enforces the agents to wait until all neighboring data has arrived, while the asynchronous version directly proceeds with available neighbor data. Communication is implemented over UDP using the 

LCM library based on a publish-subscribe paradigm. This matches the proposed framework, which tolerates message loss and exploits multicast communication in Algorithm 1. The local OCPs (5) are solved via 710 15 20 25 

0

1

2

> median
> median
> async. max
> sync. max

te [ms] 

> Occ.  ×10 3

0 50 100 150 200 

> median
> median
> async. max
> sync. max

te [ms] 

> Sync.
> Async.

Fig. 1. Histogram of the execution time te per MPC step for (a)synchronous SBDP with a fixed number of qi = 7 

per-agent iterations within an Ethernet (left) and Wi-Fi (right) network. For all four combinations, the closed loop cost Jcl ≈ 0.55 is basically identical, illustrating that the a-SBDP achieves the same control performance in less time. 

a damped forward-backward sweep. Figure 1 shows per-agent execution time histograms for ¯qi = 7 per-agent iterations for 10 3 MPC steps. The asynchronous scheme reduces median and maximum execution times by approximately 15 % respective 19 % for Ethernet and 61 % respective 63 % for Wi-Fi, while only increas-ing the closed loop cost Jcl = R Tsim  

> 0

li(xi(t), u i(t)) d t

by around 0.2 % for both Ethernet and Wi-Fi. In ad-dition, approximately only 80 % of the MPC steps in the synchronous Wi-Fi experiment were successful due to package losses combined with UDP-based commu-nication. In case of an unsuccessful MPC step, the simulation is restarted. This indicates that the asyn-chronous DMPC scheme also increases robustness w.r.t. lossy communication. After the execution we estimate 

¯q from the Ethernet data. To reduce conservatism, we use eq (ξ) ≤ C−1 ˆCq e0(ξ), cf. (14), with ˆC = C 1 

> s+d

and determine ˆC directly from the asynchronous execu-tion rather than of estimating the constants s and d.This yields q1 = 20 .43 < ¯q = 21 = P 

> i∈V

¯qi, thereby also verifying Theorem 12 for the asynchronous execu-tion. The code is available at https://github.com/ maximilianpierervonesch/async_sbdp_dmpc .

6 Conclusions 

This paper presents a cooperative DMPC scheme based on a-SBDP for the setpoint stabilization of nonlinear systems. Asynchronous execution enables agents to per-form iterations independently using outdated informa-tion, providing a practical framework for DMPC which is able to deal with, e.g., lossy communication or strag-glers. Exponential stability of the closed loop system is established under a nominally stable MPC scheme and partial asynchronism. The trade-off inherent to asynchronous optimization, i.e., slower convergence vs. reduced execution time, is quantified in the context of MPC with a HIL-experiment, where the execution time is reduced by up to 19 % for Ethernet and 63 % for Wi-Fi with essentially unchanged control performance. Future research concerns more ex-tensive experimental validations and extensions to state constraints. 

References 

[1] D. Bertsekas and J. Tsitsiklis. Parallel and Distributed Computation: Numerical Methods . Athena Scientific, 2015. [2] A. Bestler and K. Graichen. Distributed model predictive control for continuous-time nonlinear systems based on suboptimal ADMM. Optimal Control Applications and Methods , 40(1):1–23, 2019. [3] S. Boyd, N. Parikh, E. Chu, B. Peleato, and J. Eckstein. Distributed optimization and statistical learning via the alternating direction method of multipliers. Foundations and Trends in Machine Learning , 3(1):1–122, 2010. [4] M. Diehl, R. Findeisen, F. Allgöwer, H. G. Bock, and J. P. Schlöder. Nominal stability of real-time iteration scheme for nonlinear model predictive control. IEE Proceedings-Control Theory and Applications , 152(3):296–308, 2005. [5] A. L. Dontchev and W. W. Hager. Lipschitzian stability in nonlinear control and optimization. SIAM Journal on Control and Optimization , 31(3):569–603, 1993. [6] P. Giselsson, M. Doan, T. Keviczky, B. De Schutter, and A. Rantzer. Accelerated gradient methods and dual decomposition in distributed model predictive control. 

Automatica , 49(3):829–833, 2013. [7] K. Graichen and A. Kugi. Stability and incremental improvement of suboptimal MPC without terminal constraints. IEEE Transactions on Automatic Control ,55(11):2576–2580, 2010. [8] D. Liao-McPherson, M. M. Nicotra, and I. Kolmanovsky. Time-distributed optimization for real-time model predictive control: Stability, robustness, and constraint satisfaction. 

Automatica , 117:108973, 2020. [9] J. Liu, D. Muñoz de la Peña, and P. D. Christofides. Distributed model predictive control of nonlinear process systems. AIChE Journal , 55(5):1171–1184, 2009. [10] M. A. Müller and F. Allgöwer. Economic and distributed model predictive control: Recent developments in optimization-based control. Journal of Control, Measurement, and System Integration , 10(2):39–52, 2017. [11] M. Pierer v. Esch, A. Völz, and K. Graichen. Asynchronous sensitivity-based distributed optimal control. In Proc. of the American Control Conf. (ACC) , pages 1110–1118, 2025. [12] M. Pierer v. Esch, A. Völz, and K. Graichen. Sensitivity-based distributed model predictive control for nonlinear systems under inexact minimization. Optimal Control Applications and Methods , 46(4):1538–1558, 2025. [13] M. Reble and F. Allgöwer. Unconstrained model predictive control and suboptimality estimates for nonlinear continuous-time systems. Automatica , 48(8):1812–1817, 2012. [14] M. Rosenfelder, H. Ebel, and P. Eberhard. Cooperative distributed nonlinear model predictive control of a formation of differentially-driven mobile robots. Robotics and Autonomous Systems , 150, 2022. [15] G. Stomberg, A. Engelmann, M. Diehl, and T. Faulwasser. Decentralized real-time iterations for distributed NMPC. 

IEEE Transactions on Automatic Control , 2025. [16] M. Vidyasagar. Nonlinear Systems Analysis . SIAM, 2002. [17] K. Worthmann, C. M. Kellett, P. Braun, L. Grüne, and S. R. Weller. Distributed and decentralized control of residential energy systems incorporating battery storage. 

IEEE Transactions on Smart Grid , 6(4):1914–1923, 2015. [18] A. Zanelli, Q. Tran-Dinh, and M. Diehl. A Lyapunov function for the combined system-optimizer dynamics in inexact model predictive control. Automatica , 134, 2021. 

8