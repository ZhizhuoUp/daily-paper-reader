---
title: "SkillPlug: Unsupervised Skill Mining for Few-Shot Adaptation in Robotic Manipulation"
title_zh: SkillPlug：机器人操作中用于少样本适应的无监督技能挖掘
authors: "Zi-han Ding, Ziwei Wang"
date: 2026-07-09
pdf: "https://arxiv.org/pdf/2607.08354v1"
tags: ["query:q6"]
score: 9.0
evidence: 机器人操纵任务的无监督技能挖掘
tldr: 针对端到端视觉运动策略在任务迁移时数据效率低的问题，本文提出SkillPlug框架。该框架通过自监督目标从原始多任务演示中挖掘出一套共享且可重用的技能库，为组合控制提供先验。在面对新任务时，只需固定技能库并微调轻量级的路由和动作头，即可实现快速适配。实验表明，SkillPlug在仿真和真实机器人场景中均显著提升了多任务性能和少样本迁移效率，为机器人操作提供了一种可扩展的技能重用方案。
selection_source: fresh_fetch
motivation: 传统的端到端策略缺乏显式的行为重用结构，导致在只有少量示范的新任务上迁移效率极低。
method: 通过自监督学习从多任务数据中挖掘紧凑、非冗余的技能库，并采用仅微调轻量级组件的插件式适配策略。
result: 在仿真基准和真实机器人实验中，该方法一致性地提高了多任务学习效果和少样本适配的成功率。
conclusion: SkillPlug提供了一种可扩展的无监督技能挖掘方式，有效增强了机器人操作任务中的数据高效泛化能力。
---

## 摘要
学习可迁移的视觉运动模仿策略，使其能够跨多样化操作任务泛化，并仅凭少量演示快速适应新任务，仍然是一项挑战。大多数现代策略采用端到端训练，将观察结果直接映射到低级动作，这几乎没有为跨任务的行为重用和重组提供显式结构，导致在有限监督下的迁移数据效率低下。我们提出了 SkillPlug，这是一个插件式框架，它通过技能调节模块增强现有的视觉运动策略，并从原始的多任务演示中挖掘共享的可迁移技能库。SkillPlug 通过自监督目标学习技能，这些目标促进了紧凑、可重用且非冗余的行为级原语，从而形成了用于组合控制的任务共享先验。在技能挖掘之后，我们保持学习到的技能固定，并通过仅微调轻量级路由和动作头来专门化到未知任务，从而实现高效适应，而无需进行完整的端到端重新训练。我们在两个仿真基准测试和真实机器人上评估了 SkillPlug，并观察到挖掘出的可迁移技能一致地提高了多任务性能和少样本适应能力。总之，SkillPlug 提供了一种可扩展的方法来挖掘可重用技能，从而提高了机器人操作中数据高效的泛化能力。

## Abstract
Learning transferable visuomotor imitation policies that generalize across diverse manipulation tasks and adapt rapidly to new tasks from only a handful of demonstrations remains challenging. Most modern policies are trained end-to-end to map observations directly to low-level actions, offering little explicit structure for reusing and recombining behaviors across tasks and making transfer data-inefficient under limited supervision. We propose SkillPlug, a plug-in framework that augments an existing visuomotor policy with a skill-conditioning module and mines a shared, transferable skill library from raw multi-task demonstrations. SkillPlug learns skills via self-supervised objectives that promote compact, reusable, and non-redundant behavior-level primitives, forming a task-shared prior for compositional control. After skill mining, we keep the learned skills fixed and specialize to unseen tasks by fine-tuning only lightweight router and action head, enabling efficient adaptation without full end-to-end retraining. We evaluate SkillPlug on two simulation benchmarks and on a real robot, and observe that the mined transferable skills consistently improve both multi-task performance and few-shot adaptation. Overall, SkillPlug offers a scalable way to mine reusable skills that improve data-efficient generalization in robotic manipulation.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **SkillPlug** 的插件式框架，旨在通过无监督方式从多任务演示中挖掘可重用的技能，以解决机器人操作中端到端策略在面对新任务时迁移效率低下的问题。

以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机）
*   **核心问题**：当前的视觉运动策略（Visuomotor Policies）大多采用端到端映射（观察到动作），缺乏显式的行为结构。这导致在面对只有少量演示（Few-shot）的新任务时，策略难以重用已有的行为模式，迁移效率极低。
*   **研究动机**：
    *   **缺乏重用性**：端到端模型将感知与动作紧密耦合，难以提取跨任务共享的“技能原语”（如抓取、移动、放置）。
    *   **标注成本高**：现有的分层学习方法往往依赖人工标注或大模型进行任务分割，难以大规模扩展。
    *   **技能冗余**：无监督方法若缺乏约束，学习到的技能容易与特定的视觉背景纠缠，导致泛化性差。

### 2. 方法论：SkillPlug 框架
SkillPlug 是一个架构无关的插件，其核心思想是在现有策略的编码器和动作头之间插入一个**技能调节模块**。
*   **核心组件**：
    *   **技能库（Skill Library）**：一组可学习的连续向量（Embedding），代表不同的行为原语。
    *   **轨迹-技能后验编码器**：在训练阶段使用，仅输入动作轨迹，通过 VAE 风格的瓶颈约束，迫使技能关注行为而非视觉背景。
    *   **交互器与路由器（Interactor & Router）**：交互器通过交叉注意力机制将技能注入场景特征；路由器根据当前观察预测技能权重，实现技能的组合。
*   **关键技术细节（自监督目标）**：
    1.  **重构损失 ($L_{rec}$)**：确保预测动作与演示一致。
    2.  **KL 正则化 ($L_{KL}$)**：限制技能潜空间容量，促进紧凑性。
    3.  **行为技能对齐 ($L_{BSA}$)**：利用 InfoNCE 风格的对比学习，增强轨迹特征与所选技能之间的互信息。
    4.  **技能解耦 ($L_{SD}$)**：通过惩罚不同技能特征之间的余弦相似度，减少技能库的冗余。
*   **适配策略**：在多任务预训练后，**冻结**主干网络和技能库，仅微调轻量级的路由器和动作头。

### 3. 实验设计
*   **数据集/场景**：
    *   **DISCOVERSE**：高保真仿真环境，包含 7 个训练任务和 4 个测试任务。
    *   **LIBERO**：包含 130 个任务的大规模基准测试，分为四个子集（Long, Object, Goal, Spatial）。
    *   **真实机器人**：使用 Galaxea A1 六自由度机械臂，包含 6 个训练任务和 3 个少样本测试任务。
*   **对比方法**：
    *   **ACT**：作为紧凑型主干网络的基准。
    *   **OpenVLA-OFT**：作为大规模视觉语言动作模型（VLA）的基准。
*   **评估指标**：任务成功率（Success Rate）和推理效率（步数）。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号和数量。
*   **训练细节**：
    *   在 DISCOVERSE 上训练了 6000 个 Epoch，Batch Size 为 16，学习率为 $1 \times 10^{-4}$。
    *   技能数量 $K$ 在不同实验中设置为 4 或 8。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了从紧凑模型（ACT）到大规模模型（OpenVLA）的跨度。
    *   进行了 3 个随机种子的重复实验，并提供了标准差（SE）。
    *   **消融实验**：逐步添加四个损失项（Rec, KL, SD, BSA），验证了每一项对多任务和少样本性能的贡献。
    *   **属性分析**：包括技能的场景无关性可视化、技能组合性测试、技能数量 $K$ 的敏感性分析。
*   **充分性评价**：实验设计较为全面，不仅验证了成功率，还分析了技能的解释性和推理效率，证明了该方法在不同规模模型上的普适性。

### 6. 主要结论与发现
*   **性能提升**：在 DISCOVERSE 上，SkillPlug 将 ACT 的少样本成功率提升了 18.1%；在 LIBERO 上，将 OpenVLA 的跨套件迁移成功率平均提升了 38.3%。
*   **技能特性**：挖掘出的技能具有**场景无关性**（同一技能在不同背景下执行相同动作）和**组合性**（通过调整路由权重可以融合不同技能）。
*   **效率增强**：使用 SkillPlug 的策略在完成任务时所需的推理步数更少，表现出更强的时序先验。
*   **冗余饱和**：当技能数量 $K$ 增加到一定程度后，性能提升边际递减，且技能间出现冗余，说明模型倾向于学习通用的原语而非任务标签。

### 7. 优点（亮点）
*   **架构无关性**：作为插件，可以轻松集成到现有的各种视觉运动策略中。
*   **完全无监督**：无需人工分割轨迹或定义技能标签，降低了数据准备成本。
*   **高效适配**：通过冻结核心技能库，仅微调极少量参数即可实现新任务适配，极具实用价值。
*   **可解释性**：路由权重的变化与任务阶段（如接近、抓取、运输）具有明显的对应关系。

### 8. 不足与局限
*   **技能库固定**：在适配阶段技能库是冻结的。如果新任务需要全新的行为原语（训练集中从未出现），模型可能无法通过组合现有技能来解决。
*   **超参数敏感性**：技能数量 $K$ 需要预先设定，虽然论文讨论了 $K$ 的影响，但在完全未知的超大规模任务集中，如何自动确定最佳技能规模仍是挑战。
*   **算力信息缺失**：未提供训练时长和硬件需求，难以评估其在大规模 VLA 模型上的训练开销。

（完）
