---
title: "Clustering-Embedded Model Predictive Path Integral Control: Avoiding Averaging-Induced Failure and Enabling Efficient Cluster Selection for Dynamic Obstacles"
title_zh: 嵌入聚类的模型预测路径积分控制：避免平均诱导的失效并实现动态障碍物的高效聚类选择
authors: "Zidong Liu, Kaixin Chang, Xu Chen"
date: 2026-07-07
pdf: "https://arxiv.org/pdf/2607.06499v1"
tags: ["query:q1"]
score: 8.0
evidence: 使用MPPI在拥挤环境中进行避障
tldr: 针对模型预测路径积分控制（MPPI）在复杂环境中因样本加权平均导致的决策犹豫和碰撞问题，本文提出CE-MPPI框架。该方法通过引入DBSCAN聚类和几何方向特征，从采样轨迹中分离出可行的模态，并结合智能选择逻辑应对静态和动态障碍物。实验证明，该方法在机械臂避障任务中显著提升了效率和路径质量。
selection_source: fresh_fetch
motivation: 解决标准MPPI在非凸环境中因对互不兼容的采样轨迹进行加权平均而导致的避障失败和决策犹豫问题。
method: 提出CE-MPPI框架，利用DBSCAN聚类和几何方向特征提取可行轨迹模态，并根据环境动态性优化选择逻辑。
result: "在UR5e机械臂实测中，该方法相比标准MPPI缩短了48%的到达时间，并减少了12%的末端执行器路径长度。"
conclusion: CE-MPPI通过架构层面的聚类改进，有效克服了MPPI的平均化缺陷，增强了机器人在复杂动态环境中的规划能力。
---

## 摘要
随着并行计算硬件的普及，基于采样的运动规划方法（如模型预测路径积分（MPPI）控制）在非平滑任务空间中的复杂非线性系统上变得日益强大。然而，MPPI 中的采样和前向模拟流水线在拥挤环境中会遭遇平均诱导的失效。在这种情况下，重要性权重更新会对不相容的轨迹展开（rollouts）进行平均，当障碍物位于正前方时，会导致犹豫甚至碰撞。本文提出了嵌入聚类的 MPPI（CE-MPPI），这是一个从架构上解决标准 MPPI 在非凸环境中固有的平均诱导失效问题的框架。CE-MPPI 不仅仅是减轻干扰，而是通过集成高保真剪枝和聚类阶段重新定义了控制律。通过利用具有噪声的基于密度的空间聚类（DBSCAN）以及从碰撞衍生参考点提取的新型几何方向特征，该系统从不可行展开的噪声中分离出可行的轨迹模态。这与一种智能选择逻辑相结合，该逻辑在静态场景中优化最小成本，而在动态环境中则主动向障碍物通量的反方向转向。在 2D JAX 加速模拟中的实验表明，CE-MPPI 缓解了障碍物前的犹豫，并避免了在动态场景中与移动障碍物的持续耦合。特别是在 Isaac Gym 中使用 CUDA 并行展开对 6 自由度 UR5e 机械臂进行的测试表明，其到达目标的时间减少了 48%，末端执行器路径缩短了 12%。

## Abstract
With the widespread availability of parallel computing hardware, sampling-based motion planning methods such as Model Predictive Path Integral (MPPI) control have become increasingly powerful for complex nonlinear systems in non-smooth task spaces. However, the sampling and forward-simulation pipeline in MPPI suffers from averaging-induced failure in cluttered environments, where the importance-weighted update averages incompatible rollouts and leads to hesitation or even collision when an obstacle lies directly ahead. This paper proposes Clustering-Embedded MPPI (CE-MPPI), a framework that architecturally resolves the averaging-induced failures inherent in standard MPPI within non-convex environments. Rather than simply mitigating interference, CE-MPPI redefines the control law by integrating a high-fidelity pruning and clustering stage. By leveraging density-based spatial clustering of applications with noise (DBSCAN) alongside a novel geometric direction feature that is extracted from collision-derived reference points, the system isolates feasible trajectory modes from the noise of infeasible rollouts. This is paired with an intelligent selection logic that optimizes for minimum cost in static scenes while actively steering opposite to obstacle flux in dynamic environments. Experiments in 2-D JAX-accelerated simulations show that CE-MPPI alleviates obstacle-front hesitation and avoids persistent coupling with moving obstacles in dynamic scenes. In particular, real-world tests on a 6-DoF UR5e manipulator with CUDA-parallel rollouts in Isaac Gym achieve a 48\% reduction in time-to-goal and a 12\% shorter end-effector path.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **CE-MPPI（Clustering-Embedded Model Predictive Path Integral Control）** 的新型运动规划框架，旨在解决标准 MPPI 算法在复杂环境中的固有缺陷。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：标准 MPPI 在拥挤或非凸环境中存在“**平均诱导失效（Averaging-Induced Failure）**”。
*   **研究动机**：MPPI 通过对大量随机采样的轨迹（rollouts）进行重要性加权平均来更新控制序列。当障碍物位于机器人正前方时，左绕和右绕的轨迹可能具有相似的低成本，加权平均后会导致控制指令指向障碍物中心，引发决策犹豫甚至碰撞。此外，在动态场景中，简单的成本最小化逻辑容易使机器人与移动障碍物长时间“耦合”（并排走），导致规划效率低下。

### 2. 方法论
CE-MPPI 重新定义了控制律，在采样和更新之间嵌入了一个高保真的剪枝与聚类阶段：
*   **碰撞剪枝（Pruning）**：首先剔除所有发生碰撞的采样轨迹，确保后续处理仅针对可行解。
*   **DBSCAN 聚类**：利用基于密度的聚类算法（DBSCAN）对可行轨迹进行模态分离。
    *   **创新特征提取**：不直接使用速度或全状态，而是提取一种**几何方向特征**。该特征是基于“碰撞衍生参考点”（所有碰撞轨迹末端位置的均值）到各可行轨迹末端位置的方向向量。这能更有效地分离出如“左绕”和“右绕”这种截然不同的运动模态。
*   **障碍物感知选择逻辑**：
    *   **静态环境**：选择平均成本最低的簇，避免跨模态平均。
    *   **动态环境**：估计障碍物运动方向，选择与障碍物通量方向最相反（点积最小）的簇，实现主动避让。
*   **簇内更新**：仅在选定的单一簇内进行 MPPI 加权平均更新，确保控制指令的模态一致性。

### 3. 实验设计
*   **实验场景**：
    1.  **2D 仿真（JAX 加速）**：包括静态障碍物场景（诱导平均失效）和动态障碍物场景（测试避让效率）。
    2.  **真实世界/Isaac Gym 仿真**：使用 6 自由度 UR5e 机械臂进行桌面避障和目标到达任务。
*   **对比基准（Benchmarks）**：
    *   **Standard MPPI**：标准模型预测路径积分控制。
    *   **CSC-MPPI**：另一种基于聚类的约束 MPPI 方法（对比其在聚类特征和动态处理上的差异）。

### 4. 资源与算力
*   **硬件环境**：PC 配备 Intel Core i7-9750H CPU，**NVIDIA GeForce GTX 1650 GPU**，32 GB RAM。
*   **软件框架**：
    *   2D 仿真使用 **JAX** 及其即时编译（JIT）技术进行加速。
    *   机械臂实验在 **Isaac Gym** 中运行，利用 **CUDA** 进行大规模并行轨迹展开。
*   **实时性**：机械臂控制频率维持在 10 Hz（dt = 0.1s）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   2D 仿真中每个场景的结果均基于 3 次运行的平均值。
    *   机械臂实验对比了到达时间、路径长度等关键指标。
*   **充分性评价**：实验设计较为充分，涵盖了从基础理论失效点（2D 静态）到复杂动态交互（2D 动态），再到高维度实际应用（6-DoF 机械臂）的完整链条。对比实验客观地展示了 CE-MPPI 在解决犹豫和提高效率方面的优势。

### 6. 主要结论与发现
*   **解决犹豫**：CE-MPPI 通过聚类分离模态，彻底消除了标准 MPPI 在障碍物前的犹豫现象。
*   **动态避障更优**：在动态场景中，相比于 CSC-MPPI，CE-MPPI 将到达时间缩短了 **17.4%**，路径长度减少了 **9.1%**。
*   **机械臂性能提升**：在 UR5e 机械臂实测中，CE-MPPI 相比标准 MPPI 减少了 **48%** 的到达时间，并缩短了 **12%** 的末端执行器路径。
*   **计算效率**：虽然增加了聚类步骤，但通过剪枝减少了样本量，且几何特征计算简单，仍能满足实时控制需求。

### 7. 优点
*   **架构创新**：通过在 MPPI 循环中嵌入聚类和特定选择逻辑，从架构上解决了非凸环境下的平均化问题。
*   **特征设计巧妙**：提出的基于碰撞参考点的几何方向特征比传统速度特征更能有效区分避障模态。
*   **动态感知**：引入“反障碍物通量”的选择策略，增强了机器人在动态环境中的主动规划能力。

### 8. 不足与局限
*   **参数敏感性**：DBSCAN 的聚类参数（如 $\epsilon$）可能需要针对不同任务空间进行手动调优。
*   **预测模型简单**：对动态障碍物的运动估计基于简单的历史观测线性外推，在障碍物进行复杂非线性运动时可能失效。
*   **计算开销**：虽然在 10Hz 下可行，但对于更高频（如 100Hz+）的控制任务，DBSCAN 的计算延迟可能成为瓶颈。

（完）
