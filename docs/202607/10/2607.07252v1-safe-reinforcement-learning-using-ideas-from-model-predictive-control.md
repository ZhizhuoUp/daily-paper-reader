---
title: Safe Reinforcement Learning using Ideas from Model Predictive Control
title_zh: 基于模型预测控制思想的安全强化学习
authors: "Georg Schäfer, Jakob Rehrl, Stefan Huber, Simon Hirlaender"
date: 2026-07-08
pdf: "https://arxiv.org/pdf/2607.07252v1"
tags: ["query:profile-1"]
score: 8.0
evidence: 结合DRL与MPC实现具有硬约束的机器人安全控制
tldr: 本研究针对强化学习在物理系统应用中难以保证硬安全约束的问题，提出了一种结合深度强化学习（DRL）与模型预测控制（MPC）的通用框架。该方法利用系统动力学模型，通过离线MPC计算定义可行的状态-动作空间，并在训练和部署过程中利用安全过滤器将RL动作投影到该安全集合内。实验在非线性1自由度实验室平台上验证了该方法能确保探索过程的安全性并实现稳定的策略收敛。
selection_source: fresh_fetch
motivation: 强化学习在处理复杂物理系统时，探索过程中的违规行为可能导致不可逆的机械损坏，因此需要严格的硬安全约束。
method: 结合DRL的高性能与MPC的正式安全保证，通过安全过滤器将智能体动作实时投影到离线计算的全局可行域内。
result: 在非线性1自由度硬件测试平台上成功演示了安全的探索过程和稳定的策略收敛。
conclusion: 该框架为强化学习在具有严格安全要求的机器人和信息物理系统中的应用提供了一种有效的安全保障方案。
---

## 摘要
强化学习 (RL) 能够直接从数据中合成控制策略，这使其对复杂的赛博物理系统 (CPS) 和机器人技术极具吸引力。然而，一个持续存在的挑战是在主动学习阶段确保严格的硬安全约束。在现实世界的物理系统中，违反机械限制可能会导致不可逆的损坏，因此必须确保探索严格保持在安全运行区域内。我们提出了一个通用框架，将深度强化学习 (DRL) 的自适应、高性能特性与模型预测控制 (MPC) 的形式化安全保证相结合。利用系统动力学的数学模型，离线 MPC 计算定义了一个可行状态-动作空间，代表了保证约束满足的所有系统状态和控制输入的安全组合。在训练和部署过程中，强化学习智能体的瞬时动作通过安全过滤器投影到这个经过全局验证的可行集上。我们在一个非线性单自由度 (1-DoF) 实验室测试平台上系统地评估了我们的通用方法，证明了在物理硬件上成功的探索和稳定的策略收敛。

## Abstract
Reinforcement learning (RL) enables the synthesis of control policies directly from data, making it highly appealing for complex cyber-physical systems (CPSs) and robotics. A persistent challenge, however, is ensuring strict, hard safety constraints during the active learning phase. In real-world physical systems, violating mechanical limits can cause irreversible damage, necessitating that exploration remains strictly within safe operational regions. We propose a generalized framework that combines the adaptive, high-performance nature of deep reinforcement learning (DRL) with the formal safety guarantees of model predictive control (MPC). Using a mathematical model of the system dynamics, offline MPC computations define a feasible state-action space, representing all safe combinations of system states and control inputs that guarantee constraint satisfaction. During training and deployment, the RL agent's instantaneous actions are projected onto this globally verified feasible set via a safety filter. We systematically evaluate our generalized approach on a non-linear 1-DoF laboratory testbed, demonstrating successful exploration and stable policy convergence on physical hardware.

---

## 论文详细总结（自动生成）

这篇论文提出了一种结合模型预测控制（MPC）与深度强化学习（DRL）的通用框架，旨在解决强化学习在物理系统（如机器人、工业自动化）中因探索不确定性而导致的安全性问题。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：标准的强化学习依赖于大量的随机探索来寻找最优策略，但在物理系统（CPS）中，这种探索可能导致违反机械极限、热边界或执行器约束，造成不可逆的硬件损坏。
*   **研究动机**：现有的安全强化学习方法（如受限马尔可夫决策过程 CMDP）多提供“软约束”（即期望意义上的安全），无法保证每一步的绝对安全。而在线运行 MPC 或安全屏障函数（CBF）往往计算开销巨大，难以满足高频控制循环的要求。
*   **整体含义**：论文试图寻找一种既能利用 DRL 的高性能自适应能力，又能提供 MPC 式硬安全保证的平衡点，确保智能体在整个学习和部署过程中始终处于“安全状态”。

### 2. 方法论
该方法的核心是将重型计算移至离线，通过安全过滤器实现实时约束。
*   **核心思想**：利用系统的数学模型，通过离线 MPC 计算预先定义一个“可行状态-动作空间”（Feasible State-Action Space, $F$）。
*   **关键技术细节**：
    1.  **离线 MPC 预言机**：对于每一个状态-动作对 $(s, a)$，MPC 模拟未来 $H$ 个步长。如果存在一组未来的控制序列能使系统不违反约束，则该动作被视为安全。
    2.  **递归可行性**：预测时界 $H$ 被设定得足够大，以确保系统有足够的余地在接近极限前停止（避免进入“无法回头点”）。
    3.  **高效映射（二分搜索）**：利用机械系统常见的控制亲和（Control-affine）特性，即安全动作集在给定状态下通常是凸的。通过二分搜索仅寻找安全动作的上下界（$a_{min}$ 和 $a_{max}$），从而避免对整个连续动作空间进行穷举。
    4.  **安全投影过滤器**：在训练和部署时，RL 智能体提出的原始动作 $a_t$ 会被拦截。如果 $a_t$ 不在安全区间内，则将其投影（欧几里得距离最小化）到最近的安全边界上。

### 3. 实验设计
*   **实验场景**：使用 **Quanser Aero 2** 实验室测试平台，配置为非线性 **1 自由度（1-DOF）俯仰角控制**（类似直升机模型）。
*   **约束条件**：机械俯仰角限制在 $\pm 60^\circ$ 之间。
*   **算法与 Benchmark**：
    *   使用 **PPO（Proximal Policy Optimization）** 作为基础强化学习算法。
    *   对比对象主要是未加保护的标准 DRL 探索过程（隐含对比）。
    *   实验涵盖了从数值模拟到物理硬件的“零样本”（Zero-shot）迁移。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或训练时长。
*   **软件栈**：提到了使用 Python 基础的 DRL 库（Stable Baselines3）、Simulink 模型编译成的 DLL 以及 Farama Gymnasium 接口。

### 5. 实验数量与充分性
*   **实验规模**：论文展示了离线生成的安全包络图（Feasible State Space）以及在物理硬件上的训练收敛曲线。
*   **充分性评价**：
    *   **优点**：实验包含了从理论模型到物理硬件的完整闭环验证，证明了方法的实用性。
    *   **不足**：实验仅限于 1-DOF 的简单非线性系统，缺乏在更高维度（如多关节机械臂）或更复杂动态环境下的多组对比实验和消融实验。

### 6. 主要结论与发现
*   **安全性保证**：安全过滤器成功阻止了智能体在探索过程中进入导致碰撞的高速轨迹。
*   **性能收敛**：即使动作被频繁投影（修正），PPO 智能体仍能学习到高效的控制策略，并逐渐学会主动输出符合安全边界的动作。
*   **Sim-to-Real 差距**：在物理硬件上仍出现了极少数的微小违规，这归因于模型不精确、采样延迟和数值求解器的容差，而非框架本身的逻辑错误。

### 7. 优点
*   **硬安全保证**：相比于惩罚函数法，该方法在理论上提供了确定性的递归安全性。
*   **低实时延迟**：通过离线映射安全边界，在线阶段仅需简单的数值投影，极大地降低了实时控制循环的计算负担。
*   **物理可行性**：考虑了系统惯性，能够识别并避开那些虽然当前安全但未来必然违规的“无法回头点”。

### 8. 不足与局限
*   **模型依赖性**：安全性的强弱高度依赖于离线 MPC 所使用的数学模型的准确性。如果模型与实际物理系统偏差较大（Sim-to-Real Gap），安全保证将失效。
*   **维度灾难**：离线网格搜索（Grid Search）在状态空间维度增加时，计算量呈指数级增长，难以直接扩展到高自由度机器人。
*   **凸性假设**：该方法的高效性依赖于动作空间的凸性假设，对于某些高度复杂的非线性约束可能不适用。

（完）
