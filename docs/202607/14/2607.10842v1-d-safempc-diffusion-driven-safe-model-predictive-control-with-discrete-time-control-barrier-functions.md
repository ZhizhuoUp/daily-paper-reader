---
title: "D-SafeMPC: Diffusion-Driven Safe Model Predictive Control with Discrete-Time Control Barrier Functions"
title_zh: D-SafeMPC：基于离散时间控制屏障函数的扩散驱动安全模型预测控制
authors: "Erdi Sayar, Ersin Daş, Joel W. Burdick, Alois Knoll, Erdal Kayacan"
date: 2026-07-12
pdf: "https://arxiv.org/pdf/2607.10842v1"
tags: ["query:profile-1"]
score: 9.0
evidence: 基于扩散驱动的安全模型预测控制及控制屏障函数，用于机器人规划
tldr: 针对扩散模型在机器人规划中难以保证安全性和动力学约束的问题，本文提出D-SafeMPC。该方法通过控制屏障函数（CBF）和控制李雅普诺夫函数（CLF）引导逆向扩散过程，并在每个去噪步骤中利用MPC进行迭代投影优化。实验表明，该方法在Franka机械臂的静态和动态避障任务中显著提升了安全性、成功率和规划效率，并成功实现了从仿真到实机的迁移。
selection_source: fresh_fetch
motivation: 扩散模型生成的轨迹往往缺乏物理可行性和安全性保障，且与MPC结合时易因初始化不佳导致系统不稳定。
method: 利用CBF和CLF引导扩散采样，并在每个去噪步骤中嵌入MPC迭代投影以确保轨迹满足约束并提供可靠的热启动。
result: 在Franka机械臂的多种静态与动态障碍物场景及实机实验中，该方法在安全性、任务成功率和效率上均优于现有基准。
conclusion: D-SafeMPC通过深度融合扩散模型与安全控制理论，为复杂环境下的机器人安全实时规划提供了有效且稳健的方案。
---

## 摘要
在机器人规划中使用扩散模型的一个关键局限是它们无法固有地强制执行安全或动力学约束，这通常导致物理上不可行或不安全的输出。采用模型预测控制（MPC）来解决这一问题的混合方法可能不稳定，因为扩散模型提供的较差轨迹初始化会阻碍 MPC 收敛到安全且可行的解。为了克服这些挑战，我们提出了 D-SafeMPC，它增强了扩散与控制之间的交互。我们的方法利用控制屏障函数（CBF）和控制 Lyapunov 函数（CLF）引导反向扩散过程，并采用一种迭代投影方案，其中 MPC 在每个去噪步骤中对轨迹进行细化。这将采样引导至安全、目标导向的区域，并提供可靠的 MPC 热启动。在 Franka 机械臂的四种场景（一种静态障碍物和三种动态障碍物设置）仿真以及物理 Franka 机器人的 sim-to-real 实验中，D-SafeMPC 相比最先进的基准方法提高了安全性、任务成功率和规划效率。为了便于复现，我们的源代码和实验配置可在以下仓库获取：https://github.com/erdiphd/D-SafeMPC。

## Abstract
A key limitation on the use of diffusion models in robotic planning is their inability to inherently enforce safety or dynamical constraints, which often results in physically infeasible or unsafe outputs. Hybrid approaches that employ model predictive control (MPC) to address this problem can be unstable, as poor trajectory initializations from the diffusion model prevent the MPC from converging to a safe and feasible solution. To overcome these challenges, we propose D-SafeMPC, which enhances the interaction between diffusion and control. Our method guides the reverse diffusion process with control barrier functions (CBFs) and control Lyapunov functions (CLFs) and employs an iterative-projection scheme where an MPC refines the trajectory at each denoising step. This steers sampling toward safe, goal-directed regions and provides reliable MPC warm starts. In simulations on a Franka manipulator across four scenarios (one static-obstacle and three dynamic-obstacle settings) and in a sim-to-real experiment on a physical Franka robot, D-SafeMPC improves safety, task success rates, and planning efficiency over state-of-the-art baselines. To facilitate reproducibility, our source code and experimental configurations are available in a repository at https://github.com/erdiphd/D-SafeMPC

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **D-SafeMPC** 的新型机器人路径规划框架，旨在结合扩散模型（Diffusion Models）的生成能力与模型预测控制（MPC）的严谨约束处理能力。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：扩散模型在生成复杂、多模态的机器人轨迹方面表现出色，但其本质是概率性的，**无法保证生成的轨迹满足物理动力学约束或安全约束（如避障）**。
*   **研究动机**：
    *   现有的混合方法（如将扩散模型作为 MPC 的初始值）存在稳定性问题：如果扩散模型生成的初始轨迹质量太差，MPC 优化器可能无法在有限时间内收敛到可行解。
    *   单纯依靠引导（Guidance）机制只能提供“软约束”，在复杂动态环境下仍可能发生碰撞。
    *   **D-SafeMPC 的目标**：通过引入控制屏障函数（CBF）和控制李雅普诺夫函数（CLF）来引导扩散过程，并结合 MPC 迭代投影，实现既具备生成灵活性又具备硬性安全保障的规划。

### 2. 方法论
D-SafeMPC 的核心思想是在扩散模型的**每一个去噪步骤**中，同时引入“引导”和“投影”机制：
*   **CBF 与 CLF 引导（软引导）**：
    *   利用**离散时间控制屏障函数（DT-CBF）**定义安全集，通过计算安全条件的梯度来调整去噪过程中的均值，使生成的样本向远离障碍物的区域偏移。
    *   利用**控制李雅普诺夫函数（CLF）**提供目标导向的梯度，引导轨迹向目标点收敛。
*   **MPC 迭代投影（硬约束）**：
    *   在去噪过程的后期（跳过高噪声阶段后），将经过 CBF/CLF 引导的轨迹输入 MPC 优化器。
    *   MPC 作为一个投影算子，将轨迹强制投影到满足动力学方程和硬性安全边界的可行域内。
*   **热启动机制**：由于扩散过程已经通过 CBF/CLF 进行了初步的安全引导，MPC 获得了一个高质量的初始猜测（Warm Start），从而显著提高了优化效率和收敛稳定性。

### 3. 实验设计
*   **实验平台**：7 自由度 Franka Emika Panda 机器人。
*   **数据集**：使用 **D3IL 数据集**，包含人类示教的机器人操作轨迹。
*   **实验场景**：
    1.  **静态障碍物场景**：机器人需绕过固定圆柱体到达目标线。
    2.  **动态障碍物场景**：包含三个不同难度的设置，障碍物以不同速度和半径做圆周运动。
*   **对比方法（Benchmarks）**：
    *   **Diffuser**：原始扩散模型规划器。
    *   **CoBL-Diffusion**：仅使用 CBF/CLF 引导的方法。
    *   **DPCC (Diffusion Predictive Control with Constraints)**：现有的扩散+MPC 混合方法（包含 C、R、T 等多个变体）。
    *   **Post-Processing**：仅在扩散完成后进行一次 MPC 修正。
    *   **Model-Free**：无模型基准。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号、显存占用或具体的训练时长。
*   **实现细节**：提到扩散模型使用了 20 个去噪步骤（$N=20$），MPC 投影在后 10 步（$i \le 10$）介入。代码已开源，基于 MuJoCo 物理引擎进行仿真。

### 5. 实验数量与充分性
*   **实验规模**：每个规划器在每个场景下使用 10 个随机种子进行测试，每个种子运行 2 组变体，共计 **20 次评估/场景**。
*   **充分性评价**：
    *   **场景覆盖**：涵盖了从静态到复杂的动态障碍物环境，实验设计较为全面。
    *   **指标客观**：采用了“目标到达率”和“安全合规目标到达率”双重指标，能够清晰区分“完成任务但违规”和“完美完成任务”的情况。
    *   **实机验证**：包含了 Sim-to-Real 的物理机器人实验，证明了算法在现实环境中的可行性。
    *   **消融对比**：对比了多种 DPCC 变体和纯引导方法，充分展示了 D-SafeMPC 在引导+投影结合下的优势。

### 6. 主要结论与发现
*   **安全性显著提升**：在动态障碍物场景中，D-SafeMPC 的安全成功率（Safety-Compliant Goal Reached Rate）显著高于所有基准方法（在某些动态场景下达到 100%）。
*   **计算效率更高**：虽然增加了引导计算，但由于 CBF/CLF 提供了更好的 MPC 初始化，MPC 的收敛速度加快，整体决策时间优于其他基于 MPC 的扩散模型变体。
*   **鲁棒性强**：在障碍物切断专家轨迹路径的挑战性情况下，D-SafeMPC 依然能找到安全路径，而传统方法容易失败。

### 7. 优点与亮点
*   **双层保障**：巧妙地将“概率引导（CBF/CLF）”作为“确定性优化（MPC）”的前置步骤，解决了 MPC 对初始值敏感的痛点。
*   **无需重训**：CBF/CLF 引导属于分类器引导（Classifier Guidance）的一种，可以在不重新训练扩散模型的情况下适配不同的安全约束。
*   **实时性优化**：通过迭代投影而非单次后处理，使轨迹在生成过程中逐步演化至可行域，提高了最终轨迹的质量。

### 8. 不足与局限
*   **高维状态空间扩展性**：目前主要针对状态空间（State-based）规划，对于基于视觉（Visual-based）的高维输入，MPC 的实时优化可能会面临巨大的计算压力。
*   **误差累积风险**：论文提到，如果早期步骤的定位不准，可能会通过扩散模型的条件机制产生递归误差，导致后续预测不稳定。
*   **参数敏感性**：CBF 和 CLF 的权重（$\lambda_c, \lambda_l$）以及 MPC 的惩罚矩阵需要精细调优，以平衡目标达成和避障行为。

（完）
