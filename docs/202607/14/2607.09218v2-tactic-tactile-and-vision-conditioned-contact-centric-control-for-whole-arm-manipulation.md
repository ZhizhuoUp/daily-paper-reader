---
title: "TACTIC: Tactile and Vision Conditioned Contact-Centric Control for Whole-Arm Manipulation"
title_zh: TACTIC：基于触觉与视觉条件的以接触为中心的全臂操作控制
authors: "Rishabh Madan, Angchen Xie, Samantha Saak, Andres Blanco, Dohyeok Lee, Sarah Grace Brown, Yunting Yan, Mark Zolotas, Jose Barreiros, Tapomayukh Bhattacharjee"
date: 2026-07-10
pdf: "https://arxiv.org/pdf/2607.09218v2"
tags: ["query:profile-1"]
score: 8.0
evidence: 全臂操作的收缩时界控制器
tldr: TACTIC是一个针对全臂操作的预测控制框架，旨在解决多接触点操作中运动与力耦合、遮挡及物理一致性问题。它结合了视觉、分布式触觉和2D邻近表示，通过混合预测模型（学习动态+解析运动学）和基于接触雅可比矩阵的MPC采样器，实现了对复杂环境的稳健交互。在仿真和真实世界任务（如人体模型翻转、动态迷宫导航）中表现优异。
selection_source: fresh_fetch
motivation: 传统学习方法在处理全臂多点接触时，难以应对运动与力的紧密耦合以及数据稀疏导致的物理不一致性。
method: 提出一种结合RGB-D、触觉和邻近信息的混合预测模型，并利用接触雅可比矩阵引导MPC进行力调节和动作采样。
result: 在仿真对比中一致优于现有模型驱动和无模型方法，并在真实世界复杂全臂操作任务中验证了有效性。
conclusion: TACTIC通过融合多模态感知与物理约束，显著提升了机器人在复杂接触环境下的全臂操作能力。
---

## 摘要
全臂操作涉及与环境的直接接触，机器人通过在接触形成、滑动和断开的过程中将接触分布在多个连杆上来完成任务。这一设定打破了许多基于学习的操作流程中常见的隐含假设：机械臂构型紧密耦合了运动和接触力，接触状态在遮挡下是部分观测的，且由于许多多连杆接触构型在数据中表示稀疏，纯学习的预测（rollouts）在分布偏移下可能会变得物理不一致。为解决这一问题，我们提出了 TACTIC（基于触觉与视觉条件的以接触为中心的控制），这是一种用于全臂操作的收缩时界（receding-horizon）控制器。TACTIC 使用一种以接触为中心的混合预测模型，该模型结合了 RGB-D、分布式触觉传感和紧凑的二维邻近表示。该模型通过接触雅可比矩阵（contact Jacobians）将学习到的动作条件潜动力学模型与解析运动学相结合，从而能够预测未来的接触构型和交互力。TACTIC 将这些预测集成到具有接触感知动作采样的采样 MPC 规划器中：基于接触雅可比的投影将采样的动作序列引导至力调节方向，而在预测的邻近度和交互力上定义的目标函数则在任务进度与全臂力调节之间进行权衡。我们在仿真中将 TACTIC 与最先进的有模型和无模型方法进行了对比评估，并进行了消融实验以隔离每个设计选择的贡献。TACTIC 的表现始终优于其他方法。我们进一步在配备分布式触觉传感的机器人上演示了其在三项需要多接触轨迹的全臂操作任务中的真实世界性能：翻转并重新定位人体模型，以及在 3D 动态迷宫中到达目标。网站：https://emprise.cs.cornell.edu/tactic

## Abstract
Whole-arm manipulation involves direct contact with the environment while the robot completes a task by distributing contact across multiple links as contacts form, slide, and break. This setting breaks common implicit assumptions in many learning-based manipulation pipelines: arm configuration tightly couples motion and contact forces, contact state is partially observed under occlusion, and purely learned rollouts can become physically inconsistent under distribution shift because many multi-link contact configurations are sparsely represented in the data. To address this, we propose TACTIC (Tactile and Vision Conditioned Contact-Centric Control), a receding-horizon controller for whole-arm manipulation. TACTIC uses a contact-centric hybrid predictive model that combines RGB-D, distributed tactile sensing, and a compact 2D proximity representation. The model couples a learned, action-conditioned latent dynamics model with analytical kinematics through contact Jacobians, enabling rollouts of future contact configurations and interaction forces. TACTIC integrates these rollouts into a sampling-based MPC planner with contact-aware action sampling: contact Jacobian-based projections steer sampled action sequences toward force-modulating directions, and objectives defined over predicted proximity and interaction forces trade task progress against whole-arm force regulation. We evaluate TACTIC in simulation against state-of-the-art model-based and model-free methods, and perform ablations that isolate the contribution of each design choice. TACTIC consistently outperforms other methods. We further demonstrate real-world performance on a robot with distributed tactile sensing across three whole-arm manipulation tasks that require multi-contact trajectories: turning over and repositioning a manikin, and goal-reaching in a 3D dynamic maze. Website: https://emprise.cs.cornell.edu/tactic

---

## 论文详细总结（自动生成）

这是一份关于论文《TACTIC: Tactile and Vision Conditioned Contact-Centric Control for Whole-Arm Manipulation》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
全臂操作（Whole-Arm Manipulation, WAM）要求机器人利用整个机械臂（而非仅限于末端执行器）与环境进行多点接触、滑动和力交互。
*   **核心挑战**：
    *   **运动与力的紧密耦合**：机械臂的姿态直接决定了接触力的分布。
    *   **部分可观测性**：在复杂交互中，视觉传感器常被机械臂自身或物体遮挡。
    *   **物理不一致性**：纯学习模型在处理稀疏的多连杆接触数据时，预测结果往往违背物理定律（如穿透物体）。
*   **研究意义**：该研究旨在开发一种能够感知全身接触、维持物理一致性并能实时调节交互力的控制框架，使机器人能在复杂、动态的环境中完成任务。

### 2. 论文提出的方法论：TACTIC
TACTIC 提出了一种**以接触为中心的收缩时界（MPC）控制框架**，其核心由以下部分组成：
*   **多模态感知融合**：整合 RGB-D 图像、分布式触觉传感数据以及一种紧凑的 2D 邻近表示（Proximity Representation），以应对视觉遮挡。
*   **混合预测模型（Hybrid Predictive Model）**：
    *   **学习部分**：使用动作条件的潜动力学模型（Latent Dynamics）预测环境变化。
    *   **解析部分**：通过**接触雅可比矩阵（Contact Jacobians）**结合解析运动学，将学习到的状态映射到物理空间的接触构型和交互力预测中，确保物理一致性。
*   **接触感知动作采样 MPC**：
    *   **引导采样**：利用接触雅可比矩阵的投影，将随机采样的动作序列引导至能够有效调节力的方向。
    *   **多目标优化**：代价函数在“任务进度”与“全臂力调节”之间进行权衡，防止过大的交互力损坏物体或机器人。

### 3. 实验设计
*   **实验场景**：
    *   **仿真环境**：人体模型翻转（Manikin Flipping）、动态迷宫导航（Maze Navigation）。
    *   **真实世界**：使用配备分布式触觉皮肤的 Franka 机械臂，执行翻转并重新定位人体模型、在 3D 动态迷宫中到达目标等任务。
*   **基准方法（Benchmarks）**：
    *   **无模型强化学习/模仿学习**：如基于视觉和触觉的策略。
    *   **标准有模型方法**：不含接触雅可比引导的传统 MPC 或纯学习的 World Models。
*   **对比维度**：任务成功率、交互力的大小（安全性）、对分布外（OOD）场景的鲁棒性。

### 4. 资源与算力
*   **文中说明**：论文摘要和提取文本中未明确列出具体的 GPU 型号、数量或训练总时长。
*   **推测**：考虑到涉及潜动力学模型训练和基于采样的 MPC（通常需要实时或近实时推理），通常需要高性能工作站（如 NVIDIA RTX 3090/4090 或 A100 级别）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了多项仿真对比实验，验证了 TACTIC 在成功率和力控制上优于 SOTA 方法。
    *   **消融实验**：专门设计了消融组，分别移除触觉输入、移除接触雅可比引导、移除邻近表示，以验证每个模块的必要性。
    *   **真实世界验证**：在三项具有挑战性的全臂操作任务中进行了实机测试。
*   **评价**：实验设计较为充分，涵盖了从虚拟到现实的迁移，且通过消融实验客观地展示了各组件对系统性能的贡献。

### 6. 论文的主要结论与发现
*   **混合模型的优越性**：结合解析运动学（接触雅可比）和学习动力学，能显著提升模型在数据稀疏区域的预测准确性和物理一致性。
*   **触觉的重要性**：在视觉遮挡严重的 WAM 任务中，分布式触觉是维持稳定接触和调节力的关键。
*   **力调节能力**：TACTIC 不仅能完成任务，还能在操作过程中主动降低不必要的交互力，提高了交互的安全性。

### 7. 优点
*   **物理启发式学习**：通过接触雅可比矩阵将物理约束引入学习框架，解决了纯神经网络预测“发散”的问题。
*   **全臂感知**：不同于大多数关注手指触觉的研究，该方法实现了真正的全臂（多连杆）接触管理。
*   **鲁棒性**：在动态环境和存在遮挡的情况下表现稳健。

### 8. 不足与局限
*   **硬件依赖**：需要覆盖全臂的分布式触觉传感器，这类硬件目前成本较高且维护复杂，限制了方法的通用性。
*   **计算开销**：基于采样的 MPC 在高维状态空间下计算量巨大，可能难以处理极高频的实时反馈（如 >1kHz）。
*   **任务局限**：目前的任务主要集中在推、翻转等准静态或低速交互，对于高速动态碰撞或极度复杂的精细操作（如线缆解绕）尚待验证。

（完）
