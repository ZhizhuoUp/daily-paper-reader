---
title: Learning-enabled Acceleration of Scenario-based Model Predictive Control
title_zh: 学习赋能的场景模型预测控制加速
authors: "Trinh Tran, Binh Nguyen, Truong X. Nghiem"
date: 2026-07-14
pdf: "https://arxiv.org/pdf/2607.12775v1"
tags: ["query:q5"]
score: 8.0
evidence: 用于实时MPC的学习加速ADMM算法
tldr: 针对基于场景的模型预测控制（SBMPC）在处理不确定性时计算复杂度高的问题，本文提出一种结合学习加速的ADMM算法。通过将SBMPC重构为共识形式并利用Moreau包络学习加速原对偶更新，实现了跨场景和时间步的并行计算。在微网能量管理实验中，该方法在保持控制性能的同时，显著提升了求解速度。
selection_source: carryover_cache
motivation: SBMPC的计算量随场景数量和预测时域增加而剧增，限制了其在实时规划和控制中的应用。
method: 采用ADMM算法将SBMPC分解为可并行处理的子问题，并利用Moreau包络学习方案加速ADMM的原变量更新过程。
result: 在微网能量管理案例中，该方法相比IPOPT和MadNLP等传统求解器实现了大幅度的计算加速。
conclusion: 结合机器学习与并行优化算法能有效解决大规模SBMPC的实时性难题，且不损失闭环控制的可靠性。
---

## 摘要
场景模型预测控制 (SBMPC) 是模型预测控制 (MPC) 的一种变体，它通过在多个预测场景下优化控制动作来显式地考虑不确定性。然而，其计算复杂度随场景数量和预测时域的增加而迅速增长，限制了其在实时规划与控制中的应用。本文提出了一种学习加速的交替方向乘子法 (ADMM) 算法，通过利用并行计算和 Moreau 包络学习来高效求解 SBMPC 问题，同时保持较高的求解精度。我们将 SBMPC 问题重新表述为可通过 ADMM 分解的一致性形式，将与场景相关的动力学与非预期性约束分离，从而实现跨场景和时间步的并行更新。基于此分解，我们利用现有的学习优化 (learning-to-optimize) 方案，该方案利用代价函数的 Moreau 包络学习来加速 ADMM 中的原始变量更新，从而减少计算时间。所提出的框架在受负荷和可再生能源发电不确定性影响的微电网能量管理问题上进行了评估。与流行的现代非线性规划求解器 IPOPT 和 MadNLP 的对比表明，该方法在保持可靠的闭环控制性能的同时，实现了显著的计算加速。

## Abstract
Scenario-based model predictive control (SBMPC) is a variant of model predictive control (MPC) that explicitly accounts for uncertainty by optimizing control actions over multiple predicted scenarios. However, its computational complexity increases rapidly with the number of scenarios and prediction horizon, limiting is applicability to real-time planning and control. This paper presents a learning-accelerated Alternating Direction Method of Multipliers (ADMM) algorithm for efficiently solving SBMPC problems by leveraging parallel computing and Moreau envelope learning, while maintaining high solution accuracy. We reformulate the SBMPC problems into consensus forms that can be decomposed via ADMM, separating the scenario-dependent dynamics from non-anticipativity constraints and enabling parallel updates across scenarios and time steps. Building on this decomposition, we utilize existing learning-to-optimize schemes, which leverages Moreau envelope learning of the cost function to accelerate the primal update in ADMM, thereby reducing computation time. The proposed framework is evaluated on a microgrid energy management problem subject to load and renewable generation uncertainties. Comparisons with IPOPT and MadNLP, popular and modern nonlinear programming solvers, demonstrate substantial computational speedups while maintaining reliable closed-loop control performance.

---

## 论文详细总结（自动生成）

这篇论文提出了一种结合机器学习与优化算法的框架，旨在加速基于场景的模型预测控制（SBMPC）的实时求解过程。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：SBMPC 通过采样多个不确定性场景来提高控制的鲁棒性，但其计算复杂度随场景数量（$S$）和预测时域（$N$）的增加呈爆炸式增长，难以满足微电网等复杂系统的实时控制需求。
*   **研究背景**：传统的非线性规划求解器（如 IPOPT）在处理大规模场景时耗时过长。虽然 ADMM 算法可以将问题分解，但其内部的原始变量更新（Primal Update）步骤在每次迭代中仍需重复求解大量的子优化问题，构成了主要的计算瓶颈。

### 2. 论文提出的方法论
*   **核心思想**：将 SBMPC 问题重构为**共识形式（Consensus Form）**，利用 ADMM 将其分解为在场景和时间步上均可并行的子问题，并引入 **LEAF（学习赋能的 ADMM 框架）**，用神经网络预测的梯度代替复杂的优化求解。
*   **关键技术细节**：
    1.  **ADMM 分解**：通过引入辅助变量，将场景相关的动力学约束与非预期性约束（Non-anticipativity constraints）分离，使得原始更新步骤在场景 $s$ 和时间 $k$ 上完全解耦。
    2.  **Moreau 包络学习**：利用 Moreau 包络的性质，即近端算子（Proximal Operator）可以通过包络函数的梯度来表示。
    3.  **ICNN 网络**：采用输入凸神经网络（Input Convex Neural Network, ICNN）来近似代价函数的 Moreau 包络，确保学习到的近似函数保持凸性，从而保证 ADMM 的收敛特性。
    4.  **并行加速**：将原本昂贵的局部优化求解替换为神经网络的前向传播和梯度计算，且该模型可在具有相同代价结构的场景间复用。

### 3. 实验设计
*   **应用场景**：微电网能量管理系统（包含可再生能源、电池储能 BESS、可控负荷和电网连接）。
*   **数据生成**：使用高斯过程（GP）对 24 小时真实负荷和光伏发电数据进行建模，生成具有随机扰动的预测场景。
*   **Benchmark（基准）**：
    *   **IPOPT**：经典的工业级非线性规划求解器。
    *   **MadNLP**：现代的高性能非线性编程求解器。
*   **对比设置**：设置了不同的场景数 $S \in \{10, 100\}$ 和预测时域 $N \in \{48, 96\}$。

### 4. 资源与算力
*   **硬件环境**：实验在配备 **Apple M4 Pro 芯片**和 **24 GB RAM** 的设备上运行。
*   **软件环境**：使用 Julia 语言实现。
*   **训练细节**：训练集包含 8000 个实例，测试集 2000 个；使用 AdamW 优化器，训练 5000 个 Epoch；网络结构为 16x16 的隐藏层。

### 5. 实验数量与充分性
*   **实验规模**：进行了 10 步闭环 MPC 仿真，涵盖了四种主要的参数组合（$S$ 和 $N$ 的交叉组合）。
*   **充分性评价**：实验设计较为充分，不仅对比了平均求解时间，还分析了每一步的计算波动、最优性间隙（Optimality Gap）以及闭环总成本。通过与两种主流求解器的对比，客观地展示了该方法在不同规模问题下的扩展性。

### 6. 论文的主要结论与发现
*   **计算加速**：在所有设置下，所提方法（LA-SBMPC）均显著优于基准求解器。在 $S=100, N=96$ 的最大规模设置下，比 IPOPT 快约 **8.5 倍**，比 MadNLP 快约 **20.5 倍**。
*   **高精度保持**：LA-SBMPC 的最优性间隙始终保持在 **0.01% 以下**，闭环控制轨迹与 IPOPT 几乎重合，证明了学习近似并未牺牲控制质量。
*   **扩展性**：随着场景数增加，传统求解器耗时剧增，而 LA-SBMPC 的耗时增长相对平缓，更适合大规模随机优化。

### 7. 优点
*   **理论与学习的结合**：并非简单地用神经网络替代控制器，而是在 ADMM 框架内利用 Moreau 包络理论进行加速，保留了优化算法的结构优势。
*   **凸性保证**：通过 ICNN 确保了近似函数的凸性，这对于优化问题的稳定收敛至关重要。
*   **高度并行化**：分解后的子问题结构非常适合在 GPU 或多核 CPU 上进行大规模并行计算。

### 8. 不足与局限
*   **离线训练开销**：虽然在线求解极快，但需要针对特定的代价函数进行离线训练，若系统参数（如电价结构）发生重大变化，可能需要重新训练。
*   **动力学限制**：目前主要针对线性动力学系统进行验证，对于高度非线性的系统，其分解形式和学习效果尚待进一步研究。
*   **超参数敏感性**：ADMM 的惩罚参数 $\rho$ 以及神经网络的训练权重对最终性能有较大影响，需要精细调优。

（完）
