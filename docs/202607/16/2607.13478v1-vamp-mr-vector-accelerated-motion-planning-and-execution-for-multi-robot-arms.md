---
title: "VAMP-MR: Vector-Accelerated Motion Planning and Execution for Multi-Robot-Arms"
title_zh: VAMP-MR：面向多机械臂的向量加速运动规划与执行
authors: "Philip Huang, Chenrui Gao, Jiaoyang Li"
date: 2026-07-15
pdf: "https://arxiv.org/pdf/2607.13478v1"
tags: ["query:profile-1"]
score: 10.0
evidence: 多机械臂近实时运动生成与碰撞检测
tldr: 针对多机械臂运动规划在工业应用中计算耗时长的挑战，本文提出了VAMP-MR。该方案将经典规划算法与先进的向量化碰撞检测技术相结合，利用CPU SIMD指令大幅加速了规划过程中的核心瓶颈。实验表明，VAMP-MR在多臂操作任务的规划与执行后处理中实现了高达两个数量级的加速，支持近实时运动生成，并开源了代码以降低多臂协作研究的门槛。
selection_source: fresh_fetch
motivation: 现有的多机器人运动规划算法在处理碰撞检测时计算开销巨大，难以满足工业场景对实时性和高质量运动的需求。
method: 提出了一套结合经典规划算法与基于CPU SIMD指令的向量化碰撞检测技术的运动规划器。
result: 在多臂操作任务中，该方法在运动规划和执行后处理方面均实现了高达两个数量级的加速。
conclusion: VAMP-MR通过硬件加速显著提升了多机器人系统的规划效率，为实现近实时、高安全性的工业多臂协作提供了有效工具。
---

## 摘要
多机械臂运动规划是在制造业等工业任务中部署多个操作器的关键挑战。现有的基于搜索和基于采样的求解器通常需要大量的计算时间，才能生成适用于安全现实执行的无碰撞、高质量运动。在这项工作中，我们推出了一套新型多机械臂运动规划器，能够实现近实时的运动生成，它将经典的规划算法与最先进的向量化碰撞检测技术相结合。基于 CPU SIMD 指令，我们的新规划器加速了其主要瓶颈——碰撞检测，并在多臂操作任务的运动规划和执行后处理中实现了高达两个数量级的加速。我们还发布了我们的实现，以降低多机械臂规划和操作问题研发的门槛。代码可在 https://vamp-mr.github.io/vamp-mr 获取。

## Abstract
Multi-robot-arm motion planning is a key challenge in deploying multiple manipulators for industrial tasks such as manufacturing. Existing search-based and sampling-based solvers often require significant computation time to produce collision-free, high-quality motions suitable for safe real-world execution. In this work, we introduce a new suite of multi-robot-arm motion planners capable of near real-time motion generation, combining classical planning algorithms with state-of-the-art vectorized collision-checking techniques. Based on CPU SIMD instructions, our new planners accelerate their primary bottleneck, collision checking, and achieve up to two orders of magnitude speedup in both motion planning and execution postprocessing for multi-arm manipulation tasks. We also release our implementation to lower the barrier for research and development of multi-robot-arm planning and manipulation problems. Code is available at https://vamp-mr.github.io/vamp-mr

---

## 论文详细总结（自动生成）

这篇论文介绍了 **VAMP-MR**，一个专为多机械臂系统设计的向量化加速运动规划与执行框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：多机械臂运动规划（M-RAMP）在工业协作（如组装、制造）中至关重要，但其计算开销巨大。
*   **瓶颈所在**：**碰撞检测（Collision Checking, CC）** 是整个规划流水线（包括路线图构建、采样验证、冲突检测、轨迹捷径化和安全执行协调）中耗时最长的环节（通常占 90% 以上）。
*   **现有局限**：传统的碰撞检测库（如 FCL、Bullet）基于层次包围体（BVH），难以利用现代 CPU 的并行特性，且在 CPU 与 GPU/FPGA 之间传输数据存在显著延迟。

### 2. 方法论：VAMP-MR 核心技术
*   **核心思想**：利用 CPU 的 **SIMD（单指令多数据）** 指令集（如 AVX2），在单个 CPU 核心上并行处理一批（Batch）机器人配置，从而消除通信开销并极大提升吞吐量。
*   **关键技术细节**：
    *   **几何近似**：将机器人链接和附件近似为一组**球体（Spheres）**，将环境障碍物建模为简单基元（球、立方体、圆柱等）。
    *   **向量化内核（FK_CC_MULTI）**：
        1.  **并行正向运动学（FK）**：同时计算一批配置（如 8 个）中所有关节的位置。
        2.  **分层检测**：依次进行自碰撞检测、机器人-环境检测、机器人-机器人检测。
        3.  **早期退出**：一旦批次中某个配置发生碰撞，立即停止计算以节省资源。
    *   **灵活性设计**：支持运行时更新基座变换（Base Transforms）、添加附件（Attachments）和设置允许接触对（Allowed Contacts），无需重新编译内核。
    *   **“耙式”扫描（Rake Strategy）**：在验证运动路径时，将路径离散化后的多个点打包进 SIMD 寄存器同时检测。

### 3. 实验设计
*   **实验场景**：
    *   **Panda Two Rod**：两台 Panda 臂各持一根长杆。
    *   **Panda Four**：四台 Panda 臂在拥挤空间协作。
    *   **Panda Four Bins**：四台臂在带有箱子障碍物的环境中作业。
    *   **LEGO Assembly**：双臂长程乐高组装任务（11 到 258 块砖）。
*   **Benchmark 与对比方法**：
    *   **基准库**：FCL（Flexible Collision Library）。
    *   **规划算法**：复合 RRT-Connect、基于冲突的搜索（CBS-MP）。
    *   **后处理**：动态汤普森捷径化（DTS）。
    *   **执行框架**：异步时序规划图（TPG）。

### 4. 资源与算力
*   **硬件**：AMD 7840HS 笔记本 CPU。
*   **软件/指令集**：C++ 编写，使用 GCC 9 编译，启用 **256位 AVX2** 指令集（单精度浮点数，SIMD 批处理大小为 8）。
*   **说明**：论文强调这是纯 CPU 加速，无需 GPU，从而避免了数据传输延迟。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了 10,000 次随机采样以测试基础碰撞检测速度。
    *   在三个主要场景中各测试了 110-132 个规划实例。
    *   针对长程任务，测试了 4 组不同复杂度的乐高组装任务。
    *   **真实部署**：在 3 台 Kinova 机械臂上进行了实机验证。
*   **充分性评价**：实验覆盖了从底层算子到高层规划器，再到后处理和实际执行的全流程，对比了缓存命中率等底层指标，实验设计客观且充分。

### 6. 主要结论与发现
*   **速度提升**：单次碰撞检测提速 11-27 倍，运动路径验证提速高达 **148 倍**。
*   **规划效率**：RRT-Connect 和 CBS-MP 的规划时间缩短了 1-2 个数量级。
*   **算法扩展性**：加速后的 CBS-MP 在复杂环境中表现优于 RRT，因为它能更快速地处理高层冲突树的数千个节点（以往只能处理几十个）。
*   **执行优化**：在乐高组装任务中，TPG 构建速度提升 20 倍以上，且在相同时间内能生成质量更高（Makespan 更短）的方案。

### 7. 优点
*   **极高性能**：通过 SIMD 实现了近实时的运动生成。
*   **低延迟**：纯 CPU 实现，适合对实时性要求极高的工业控制系统。
*   **通用性强**：作为插件可直接替换 FCL 等传统库，支持多种主流规划算法。
*   **开源贡献**：提供了完整的代码实现和球体近似工具，降低了多臂研究门槛。

### 8. 不足与局限
*   **几何精度**：基于球体的近似虽然高效，但在处理极窄缝隙或精细接触时，精度不如原始三角网格（Mesh）。
*   **硬件依赖**：性能高度依赖于 CPU 对 SIMD 指令集的支持（如 AVX2 或 AVX-512）。
*   **任务分配瓶颈**：实验显示，虽然运动规划变快了，但长程任务中的“任务分配（Task Assignment）”环节（如 ILP 求解）成为了新的计算瓶颈。
*   **静态环境假设**：主要针对静态障碍物环境，对高速动态障碍物的实时响应尚需进一步验证。

（完）
