---
title: "RLMM-Flow: A Flow-based Mobile Manipulation Framework with Latent-Space Reinforcement Learning"
title_zh: RLMM-Flow：一种结合潜空间强化学习的基于流的移动操作框架
authors: "Shuhang Wang, Ziming Li, Hui Cheng"
date: 2026-07-29
pdf: "https://arxiv.org/pdf/2607.26460v1"
tags: ["query:profile-1"]
score: 9.0
evidence: 具有避障和轨迹平滑性的移动操作
tldr: RLMM-Flow是一个结合流生成策略与潜空间强化学习的移动操作框架。针对模仿学习受限于专家数据质量的问题，该框架先通过专家演示预训练流策略以捕获多模态运动先验，随后利用潜空间强化学习引导初始噪声生成更高价值的动作块。通过引入动作空间评论家预热和由粗到精的潜空间转向技术，RLMM-Flow在保持快速推理的同时，显著提升了复杂任务下的成功率和避障性能。
selection_source: fresh_fetch
motivation: 传统的模仿学习流策略受限于专家演示的分布，难以在复杂的移动操作约束下实现性能的进一步突破。
method: 冻结预训练流策略，利用潜空间转向网络优化初始噪声，并结合动作空间评论家预热与由粗到精的转向机制进行强化学习微调。
result: 在移动操作基准测试中，该方法在任务成功率、避障能力和轨迹质量上均显著优于纯模仿学习及现有的强化学习后训练基准。
conclusion: RLMM-Flow证明了在潜空间进行强化学习微调能有效提升生成式运动策略的性能，同时保留了流模型高效推理的优势。
---

## 摘要
移动操作需要生成全身动作块，以共同满足目标到达、避障、底盘运动学约束、机械臂关节限制和轨迹平滑度。基于流的生成式策略为从专家演示中学习多模态且时间一致的运动先验提供了一种高效范式，但仅靠模仿训练无法将策略质量提升到演示分布之外。我们提出了 RLMM-Flow，这是一个基于流的移动操作框架，它将专家流策略预训练与潜空间强化学习后训练相结合。该框架首先学习一个流策略，从专家演示中捕捉多模态全身运动先验。随后冻结预训练的流策略，同时由一个潜引导网络将其初始噪声引导至更高价值的动作块。为了稳定高维潜空间优化，我们在联合训练潜评论家和潜行动者之前，先对动作空间评论家进行预热，并引入了由粗到精的潜引导，将控制从时域共享的潜表示逐步扩展到全维残差表示。在移动操作运动规划基准测试上的实验表明，与仅靠模仿的流策略和现有的强化学习后训练基准相比，RLMM-Flow 在保持快速流推理的同时，显著提高了任务成功率、避障能力和轨迹质量。

## Abstract
Mobile manipulation requires generating whole-body action chunks that jointly satisfy goal reaching, collision avoidance, base kinematic constraints, manipulator joint limits, and trajectory smoothness. Flow-based generative policies provide an efficient paradigm for learning multimodal and temporally consistent motion priors from expert demonstrations, but imitation-only training cannot improve policy quality beyond the demonstration distribution. We propose RLMM-Flow, a flow-based mobile manipulation framework that combines expert flow-policy pretraining with latent-space reinforcement learning post-training. The framework first learns a flow policy that captures a multimodal whole-body motion prior from expert demonstrations. The pretrained flow policy is then frozen, while a latent steering network steers its initial noise toward higher-value action chunks. To stabilize high-dimensional latent optimization, we warm up an action-space critic before jointly training the latent critic and latent actor, and introduce coarse-to-fine latent steering that progressively expands control from a horizon-shared latent representation to a full-dimensional residual representation. Experiments on mobile manipulation motion-planning benchmarks show that RLMM-Flow substantially improves task success, collision avoidance, and trajectory quality over imitation-only flow policies and existing reinforcement learning post-training baselines, while preserving fast flow-based inference.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《RLMM-Flow: A Flow-based Mobile Manipulation Framework with Latent-Space Reinforcement Learning》** 的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
移动操作（Mobile Manipulation）要求机器人底盘与机械臂高度协同，同时满足避障、运动学约束、关节限制及轨迹平滑性。
*   **研究动机**：虽然基于流（Flow-based）的生成式策略能高效学习多模态运动先验，但纯模仿学习（Imitation Learning）受限于专家演示数据的质量，无法超越数据分布。
*   **核心挑战**：直接对多步生成策略进行强化学习（RL）微调非常困难，因为通过复杂的传输过程（ODE/去噪）进行梯度传播和似然评估极不稳定。
*   **整体含义**：本文提出 RLMM-Flow，通过在**潜空间（Latent Space）**进行转向（Steering）来优化预训练的流策略，既保留了专家运动先验，又通过奖励函数提升了规划质量。

### 2. 方法论
RLMM-Flow 采用两阶段训练框架：
*   **阶段一：专家流策略预训练**
    *   使用专家演示数据训练一个基于 Transformer 的条件流匹配（Flow Matching）策略，学习从高斯噪声到全身动作块（Action Chunk）的映射。
    *   引入 **Logit-normal 噪声采样调度**，增强对中间噪声状态的学习，提升高维动作生成的协调性。
*   **阶段二：潜空间强化学习后训练**
    *   **核心思想**：冻结预训练流策略，训练一个“潜转向网络”（Latent Steering Network）来调整初始噪声 $w$，使其解码出的动作块获得更高奖励。
    *   **级联转向架构**：先预测底盘相关的潜变量，再以此为条件预测机械臂潜变量，显式建模全局导航与局部操作的依赖关系。
    *   **动作评论家预热（QA Warm-up）**：先单独训练动作空间评论家 $Q_A$，待其稳定后再训练潜评论家 $Q_W$ 和转向网络，解决“移动目标”导致的训练震荡。
    *   **由粗到精（Coarse-to-Fine）转向**：初期仅优化时域共享的潜变量（全局趋势），后期引入高维残差（局部修正），降低高维搜索难度。

### 3. 实验设计
*   **数据集与场景**：
    *   **仿真**：使用 PhyScene 室内场景，包含 30 个训练场景和 30 个未见（Unseen）测试场景。
    *   **数据量**：9000 条专家轨迹用于预训练；9000 条混合质量轨迹（含 1500 条次优轨迹）用于离线 RL。
*   **Benchmark 与对比方法**：
    *   **生成基准**：M2Diffuser（带/不带测试时优化）、Base Flow。
    *   **RL 基准**：动作空间 AWR、动作空间 IQL、DSRL-NA（原始潜空间 RL 方法）。
*   **评估指标**：成功率（Succ.）、碰撞率（Coll.）、关节违规率、轨迹平滑度、推理时间、路径长度。

### 4. 资源与算力
*   **训练硬件**：使用 **NVIDIA RTX 4090 GPU** 进行模型训练。
*   **部署硬件**：实机使用 NVIDIA Jetson AGX Orin。
*   **训练时长**：预训练 2000 epoch；后训练（RL）进行 100,000 次更新（其中前 20,000 次为预热阶段）。
*   **推理效率**：流策略推理时间约为 0.18s - 0.21s，远快于需要迭代优化的扩散模型（约 3.89s）。

### 5. 实验数量与充分性
*   **实验规模**：仿真测试涵盖了 900 个已知任务和 900 个未见场景任务；实机测试进行了 100 次试验（跨 10 个环境）。
*   **消融实验**：详细对比了“仅 QA 预热”、“仅由粗到精转向”以及完整模型的效果，验证了各组件对稳定性和性能的贡献。
*   **客观性与公平性**：所有离线 RL 方法均基于相同的预训练模型和数据集；实机测试采用零样本（Zero-shot）迁移，未进行实地微调，评估标准严格（需同时满足精度、无碰撞、无违规）。实验设计较为充分且客观。

### 6. 主要结论与发现
*   **性能提升**：RLMM-Flow 在未见场景下的成功率（38%）显著高于 Base Flow（22%）和传统的离线 RL 方法（24%-30%）。
*   **安全性与平滑度**：通过奖励函数引导，碰撞率大幅下降，且生成的轨迹比直接在动作空间优化的方法更平滑。
*   **潜空间优势**：在潜空间进行转向能有效约束动作在“专家流形”内，避免了直接优化动作块时容易出现的时域不一致问题。
*   **实机验证**：成功在真实移动机器人（AgileX 底盘 + Unitree 机械臂）上完成任务，证明了该框架的实用性。

### 7. 优点
*   **稳定性创新**：提出的 $Q_A$ 预热和由粗到精的转向策略有效解决了高维全身动作空间中 RL 训练难收敛的问题。
*   **结构化建模**：级联架构考虑了底盘与机械臂的异构角色，符合移动操作的物理逻辑。
*   **效率平衡**：在不牺牲流模型推理速度的前提下，通过后训练显著提升了规划质量，避开了昂贵的测试时优化（Test-time Optimization）。

### 8. 不足与局限
*   **依赖预训练质量**：如果预训练的流策略未能覆盖基本的运动空间，潜空间转向的上限会受到限制。
*   **奖励函数设计**：虽然引入了归一化，但奖励函数的权重（5 个组件）仍需人工调优，这在不同任务间可能缺乏通用性。
*   **实机泛化风险**：虽然实现了零样本迁移，但在极端复杂的动态环境或光照剧变下的鲁棒性尚未深入探讨。

（完）
