---
title: Self-Adaptive Learning and Model Predictive Control for Tracking Unknown Dynamics with No Regret
title_zh: 用于跟踪未知动力学且具有无悔保证的自适应学习与模型预测控制
authors: "Atharva Navsalkar, Hongyu Zhou, Vasileios Tzoumas"
date: 2026-07-29
pdf: "https://arxiv.org/pdf/2607.26370v1"
tags: ["query:q1"]
score: 8.0
evidence: 用于避障移动物体的模型预测控制
tldr: 本研究针对具有未知且多变动力学特性的目标跟踪问题，提出了一种自适应在线学习控制方法。该方法通过自监督和高效的一步学习同时训练多个预测器，并根据观测到的目标行为动态选择最优预测器。理论证明该方法在期望意义下具有无悔（no-regret）特性，且在动力学切换和学习误差存在时表现稳健。实验验证了其在无人机跟踪结构化、随机及对抗性轨迹时的优越性。
selection_source: fresh_fetch
motivation: 现有的机器人跟踪任务常面临目标动力学未知且可能在结构化、随机或对抗模式间切换的挑战。
method: 提出一种结合模型预测控制的自适应框架，通过并行学习多个预测器并实时切换，以匹配目标的动态行为。
result: 理论上实现了期望无悔保证，并在Crazyflie无人机仿真和硬件实验中优于非随机、核函数及神经网络等基准方法。
conclusion: 该方法为处理复杂多变环境下的目标跟踪提供了具有理论保障且计算高效的自适应控制方案。
---

## 摘要
我们提出了一种用于跟踪未知目标动力学的自适应在线学习控制方法。目标动力学可能表现出切换行为，特别是结构化、随机和/或对抗性运动的混合。这种具有挑战性的目标跟踪场景出现在动态地图构建、交通控制和追逃博弈等应用中，在这些应用中，机器人需要跟踪、追逐或避免与动力学未知的移动地标、物体、人类等发生碰撞。我们的方法通过自监督、单样本且计算高效的学习，从零开始同时学习多个预测器，并自适应地选择最佳预测器以匹配观测到的目标行为。该方法在期望意义上具有有限时间近优性保证，其特征表现为目标动力学学习误差和目标动力学切换频率的函数。在不存在误差和切换的情况下，该方法渐近地匹配预先知道目标动力学的最优非因果控制策略，即该方法在期望意义上实现了无悔（no regret）。在存在学习误差和切换的情况下，该方法性能平稳下降，例如，当存在误差但没有切换时，平均遗憾与平均学习误差和切换次数成正比。为了证明这些保证，与现有的采用基于 RFF（随机傅里叶特征）在线学习的工作相比，需要一种新颖的技术方法。我们在 Crazyflie 仿真和硬件实验中验证了我们的方法，涵盖了从结构化到随机再到对抗性的目标轨迹，并与非随机、基于核以及基于神经网络的在线学习方法进行了对比。

## Abstract
We propose a self-adaptive online learning for control method for tracking unknown target dynamics. The target dynamics can exhibit switching behavior, particularly, a mixture of structured, random, and/or adversarial motion. Such challenging target tracking scenarios arise in applications of dynamic mapping, traffic control, and pursuit evasion, where robots need to track, pursue, or avoid collision with moving landmarks, objects, humans, etc., whose dynamics are unknown. Our method simultaneously learns multiple predictors from scratch, via self-supervised, one-shot, and computationally efficient learning, and adaptively selects the best one to match the observed target behavior. The method enjoys finite-time near-optimality guarantees in expectation, characterized as a function of the learning error of the target dynamics and the frequency that the target dynamics switch. In the absence of both error and switching, the method asymptotically matches the optimal non-causal control policy that knows a priori the target dynamics, i.e., the method enjoys no regret in expectation. In the presence of learning errors and switching, the method degrades gracefully, \eg when there are errors and no switching, the average regret is proportional to the average learning error and switching times. To prove these guarantees, a novel technical approach is required compared to the existing works that employ RFF-based online learning. We validate our method in Crazyflie simulations and hardware experiments, across target trajectories that vary from structured to random to adversarial, in comparison to non-stochastic, kernel-based, and neural-network-based methods for online learning.

---

## 论文详细总结（自动生成）

### 论文总结：用于跟踪未知动力学且具有无悔保证的自适应学习与模型预测控制

#### 1. 核心问题与整体含义（研究动机和背景）
论文关注的核心问题是：**移动机器人在面对动力学特性未知且非平稳（Non-stationary）的目标时，如何实现高精度的实时预测与跟踪。**
*   **研究动机**：在追逃博弈、交通导航和动态地图构建等场景中，目标的运动模式可能在结构化（如匀速）、随机（如布朗运动）或对抗性（故意避开追踪者）之间切换。
*   **现有局限**：传统的鲁棒控制过于保守；离线学习方法（如深度学习）难以泛化到分布外（OOD）的动力学；现有的在线学习方法通常针对固定模式调优，缺乏对切换动力学的自适应能力。
*   **整体含义**：提出一种“自适应专家”框架，通过并行学习多个预测模型并实时切换，使机器人能像拥有“先验知识”一样跟踪复杂目标。

#### 2. 方法论：核心思想与技术细节
该方法由三个相互协作的模块组成：
*   **ISO（独立在线学习模块）**：
    *   **核心思想**：同时维护 $P$ 个独立的预测器，每个预测器采用不同的超参数（如学习率、核参数）。
    *   **技术细节**：使用**随机傅里叶特征（RFF）**将非线性动力学近似为线性组合。通过**在线梯度下降（OGD）**实时更新模型参数 $\alpha$。为了避免递归预测的误差累积，ISO 为 MPC 视野内的每一步都训练了专门的预测器。
*   **AS（自适应选择模块）**：
    *   **核心思想**：利用多臂老虎机（Multi-armed Bandit）中的 **Hedge 算法**。
    *   **技术细节**：根据每个预测器在过去一段时间内的均方根误差（RMSE）计算得分，并维护一个概率分布。引入**遗忘因子（Forgetting Factor）** $\gamma$，使算法能快速丢弃在动力学切换后表现不佳的旧专家，选择当前最匹配的专家。
*   **MPC（模型预测控制模块）**：
    *   **核心思想**：利用 AS 选出的最优预测器，在预测视野 $N$ 内优化控制序列。
    *   **理论保证**：论文证明了该方法具有**动态遗憾（Dynamic Regret）**界限。在理想状态下实现“无悔”（即性能渐近等同于预知未来的最优控制器）；在存在误差和切换时，性能随切换频率和学习误差优雅降级（Graceful Degradation）。

#### 3. 实验设计
*   **实验场景**：
    *   **仿真**：使用 Gazebo 和 ROS2 模拟 Crazyflie 无人机跟踪。轨迹包括双纽线（Lemniscate）、正弦波、星形轨迹、随机行走和随机对抗轨迹。
    *   **硬件**：使用两台物理 Crazyflie 无人机，在 Lighthouse 定位系统下进行实验。设计了混合切换轨迹（如从双纽线突然转为对抗模式）和字母“UM”形状轨迹。
*   **Benchmark 与对比方法**：
    *   **PLOT**：基于线性递归最小二乘法的在线跟踪方法。
    *   **RFF-MPC**：基于单预测器的 RFF 在线学习方法（作者前期工作的变体）。
    *   **MODL**：基于多个神经网络（MLP 和残差网络）的在线深度学习方法。
    *   **Optimal***：预知目标意图的理想非因果控制器（作为性能上限）。

#### 4. 资源与算力
*   **硬件平台**：实验在配备 Intel i9-13900H CPU 和 32GB RAM 的笔记本电脑上运行。
*   **算力消耗**：
    *   该方法主打**计算高效性**，不依赖 GPU 进行大规模训练。
    *   **实时性**：当预测器数量 $P=4$、特征数 $M=50$ 时，ISO 和 AS 模块的单次更新耗时仅为 **9.0 ± 3.2 ms**，完全满足 50Hz 的 MPC 控制频率要求。
    *   **扩展性**：计算复杂度随预测器数量 $P$ 线性增长。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 5 种仿真轨迹和 3 种复杂的硬件切换场景。
*   **消融实验**：作者详细分析了 ISO 模块中超参数（学习率 $\eta$、核标准差 $\sigma$、记忆长度 $\tau$）对不同轨迹的影响，证明了单一参数无法胜任所有轨迹，从而凸显了 AS 模块（自适应选择）的必要性。
*   **客观性**：对比了当前最先进的线性、非线性和深度学习在线算法。实验不仅测量了跟踪误差，还分析了预测准确度和动态遗憾的演变趋势，验证了理论推导的正确性。

#### 6. 主要结论与发现
*   **自适应性**：提出的方法在所有测试轨迹中均表现出最低或次低的跟踪误差，尤其在动力学发生突变时，能比基准方法更快地恢复跟踪精度。
*   **性能优越**：在某些场景下，跟踪误差比经过精细调优的 PLOT 或 RFF-MPC 降低了约 25%-50%。
*   **理论验证**：实验观察到的平均遗憾趋势与理论推导的 $O(T^{3/4})$ 或随切换频率 $\kappa$ 变化的界限高度吻合。
*   **深度学习的局限**：在线深度学习方法（MODL）在小样本实时场景下收敛较慢，且对分布外数据的适应性不如基于 RFF 的浅层在线学习。

#### 7. 优点
*   **理论与实践结合**：不仅提供了严谨的收敛性和遗憾界限证明，还在物理硬件上实现了实时闭环控制。
*   **无需预训练**：实现了真正的“从零开始”（From Scratch）学习，具有极强的单样本（One-shot）适应能力。
*   **模块化设计**：ISO、AS 和 MPC 耦合度低，易于扩展到其他类型的预测模型或控制器。

#### 8. 不足与局限
*   **特征工程依赖**：RFF 的性能仍依赖于基核（Base Kernel）的选择（如文中主要使用高斯核），对于极度不规则的动力学可能需要更复杂的核设计。
*   **状态空间限制**：目前的理论假设机器人和目标状态是有界的，且未深入讨论高维状态空间下的计算瓶颈。
*   **避障与多机协作**：虽然硬件实验通过垂直偏移避免了碰撞，但算法本身尚未集成主动避障逻辑或多机器人间的协同学习。
*   **超参数敏感性**：虽然 AS 模块能选择专家，但“专家库”本身的参数范围仍需人工预设。

（完）
