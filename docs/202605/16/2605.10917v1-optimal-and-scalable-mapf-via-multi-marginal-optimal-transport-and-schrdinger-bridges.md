---
title: Optimal and Scalable MAPF via Multi-Marginal Optimal Transport and Schrödinger Bridges
title_zh: 基于多边缘最优传输与薛定谔桥的最优且可扩展的多智能体路径规划 (MAPF)
authors: "Usman A. Khan, Joseph W. Durham"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10917v1"
tags: ["query:q1"]
score: 8.0
evidence: 具有时空不重叠约束的多智能体路径搜索
tldr: 本研究针对匿名多智能体路径规划（MAPF）问题，提出将其建模为具有马尔可夫结构的多边际最优传输（MMOT）问题。通过将指数级规模的MMOT简化为多项式规模的线性规划（LP），并利用全单模性保证整数解，实现了无碰撞的最优路径规划。此外，引入薛定谔桥框架进行熵正则化，利用Sinkhorn算法提升大规模场景下的计算效率，在保证近乎最优性的同时显著降低了复杂度。
selection_source: fresh_fetch
motivation: 旨在解决匿名多智能体路径规划中计算复杂度随规模指数增长以及难以保证路径最优性和无碰撞性的挑战。
method: 将MAPF转化为具有马尔可夫结构的线性规划问题，并结合薛定谔桥理论与Sinkhorn迭代算法进行规模化求解。
result: 理论证明了该线性规划在特定条件下具有全单模性，实验表明该方法在大规模场景下能以较低复杂度获得近乎最优的整数解。
conclusion: 该研究通过最优传输与概率框架的结合，为实现高效、可扩展且最优的匿名多智能体路径规划提供了坚实的理论基础和算法支撑。
---

## 摘要
我们考虑匿名多智能体路径规划 (MAPF) 问题，其中一组机器人被指派在有限连通图上前往一组目标点。我们证明了 MAPF 可以被建模为一类具有底层马尔可夫结构的特殊多边缘最优传输 (MMOT) 问题，在此结构下，规模呈指数级增长的 MMOT 可以简化为多项式规模的线性规划 (LP)。针对匿名设置，我们确立了相应 LP 可行且全单模 (totally unimodular) 的条件，从而产生在时空上均不重叠的最小成本整数 ({0,1}) 传输。为了使该方法适应大规模问题，我们通过薛定谔桥将 MAPF-MMOT 纳入概率框架。在标准假设下，我们证明了薛定谔桥公式可以简化为相应 MMOT 的熵正则化，从而允许使用迭代 Sinkhorn 型算法求解。薛定谔桥作为一个概率框架，提供了一种影子（分数）传输，我们将其作为模板来求解简化后的 LP，并证明其在显著降低复杂度的同时，能产生近乎最优的整数传输。大量实验突显了所提方法的最优性和可扩展性。

## Abstract
We consider anonymous multi-agent path finding (MAPF) where a set of robots is tasked to travel to a set of targets on a finite, connected graph. We show that MAPF can be cast as a special class of multi-marginal optimal transport (MMOT) problems with an underlying Markovian structure, under which the exponentially large MMOT collapses to a linear program (LP) polynomial in size. Focusing on the anonymous setting, we establish conditions under which the corresponding LP is feasible, totally unimodular, and consequently, yields min-cost, integral $(\{0,1\})$ transports that do not overlap in both space and time. To adapt the approach to large-scale problems, we cast the MAPF-MMOT in a probabilistic framework via Schrödinger bridges. Under standard assumptions, we show that the Schrödinger bridge formulation reduces to an entropic regularization of the corresponding MMOT that admits an iterative Sinkhorn-type solution. The Schrödinger bridge, being a probabilistic framework, provides a shadow (fractional) transport that we use as a template to solve a reduced LP and demonstrate that it results in near-optimal, integral transports at a significant reduction in complexity. Extensive experiments highlight the optimality and scalability of the proposed approaches.

---

## 论文详细总结（自动生成）

这篇论文题为《Optimal and Scalable MAPF via Multi-Marginal Optimal Transport and Schrödinger Bridges》，由亚马逊机器人（Amazon Robotics）和波士顿学院的研究人员撰写。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在大型复杂环境中，为成百上千个机器人规划出既**最优**（成本最低）又**可扩展**（计算快）且**无碰撞**的路径。
*   **研究背景**：多智能体路径规划（MAPF）本质上是组合优化问题。在“匿名”设置下（即任何机器人可以去任何目标点），虽然复杂度有所降低，但随着机器人数量和地图规模增加，传统方法（如搜索算法或整数规划）仍面临计算爆炸或难以保证解的质量的问题。
*   **研究动机**：利用**最优传输（Optimal Transport, OT）**理论的数学优美性，将其引入 MAPF 领域，通过概率框架解决离散路径规划的硬约束问题。

### 2. 方法论
论文提出了一套从理论证明到高效算法的完整框架：
*   **MMOT 建模**：将 MAPF 建模为多边缘最优传输（Multi-Marginal Optimal Transport）问题。通过引入**马尔可夫结构（Markovian structure）**，将原本随时间步长呈指数级增长的变量空间压缩为随时间线性增长的多项式规模。
*   **全单模性（Total Unimodularity, TU）证明**：这是本文的核心理论贡献。作者证明了在匿名 MAPF 设置下，该线性规划（LP）问题的约束矩阵是全单模的。这意味着即使使用连续变量求解 LP，其最优解也必然是整数（0 或 1），从而自然地保证了机器人路径的完整性（不会出现“半个机器人”走两条路的情况）。
*   **薛定谔桥（Schrödinger Bridge）与熵正则化**：为了进一步提升大规模问题的求解速度，引入薛定谔桥框架，对 MMOT 进行熵正则化。这使得问题可以使用高效的 **Sinkhorn 迭代算法** 求解。
*   **影子传输与图剪枝（Shadow Transport & Pruning）**：
    1.  先通过 Sinkhorn 算法快速计算出一个“影子传输”（分数解，代表路径的概率分布）。
    2.  根据影子传输的概率分布，剪掉图中极不可能被选中的边（剪枝率可达 60-80%）。
    3.  在大幅缩减后的图上求解精简版的 LP，以极小的成本损失换取显著的计算加速。

### 3. 实验设计
*   **实验场景**：主要基于网格地图（Grid Maps），规模从 $50 \times 50$ 到 $150 \times 150$（顶点数 $K$ 达 22,500）。
*   **对比基准（Benchmarks）**：
    *   **P1 (Exact LP)**：本文提出的原始线性规划方法。
    *   **CBM (Conflict-Based Min-Cost-Flow)**：一种经典的匿名 MAPF 算法。
    *   **ILP (Integer Linear Programming)**：传统的整数线性规划。
*   **测试变量**：机器人密度（通常为 5%）、地图障碍物分布、时间步长（$T$）、成本函数（均匀成本 vs. 非均匀地形成本）。

### 4. 资源与算力
*   **硬件平台**：2022 款 MacBook（未详细说明具体芯片，推测为 M 系列）。
*   **软件工具**：使用 Python 的 SciPy (HiGHS 求解器) 进行初步实验，大规模实验使用了 **Gurobi** 商业优化求解器。
*   **计算规模**：最大实验涉及约 340 万个 LP 变量。

### 5. 实验数量与充分性
*   **实验规模**：总计进行了超过 **460 组** 独立实验。
    *   162 组用于评估扩展性（Scaling Study）。
    *   260 组用于参数敏感性分析（$\epsilon, \lambda$ 等参数）。
    *   24 组用于非均匀成本测试。
    *   15 组用于与基准算法对比。
*   **充分性评价**：实验设计非常充分且客观。作者不仅验证了算法在理想网格下的表现，还测试了复杂障碍物、非均匀地形成本以及极高机器人密度下的性能，并提供了详细的运行时间缩放曲线（Power-law fits）。

### 6. 主要结论与发现
*   **整数解保证**：理论证明和实验均证实，所提 LP 框架在匿名设置下始终产生 100% 整数解，无需额外的分支定界过程。
*   **计算效率**：相比原始 LP（复杂度约 $O(K^{1.68})$），采用“薛定谔桥剪枝”后的流水线达到了近乎线性的复杂度（约 $O(K^{1.15})$）。
*   **性能权衡**：在剪掉 60-80% 的边后，计算速度提升了 **3.6 到 7.1 倍**，而路径成本的增加（Gap）通常控制在 **10% 以内**。
*   **优越性**：在相同规模下，该方法在处理 300 个机器人时的速度比传统 CBM 算法快一个数量级，且成功率远高于 ILP。

### 7. 优点
*   **理论扎实**：将最优传输理论与全单模性结合，为 MAPF 提供了坚实的数学基础。
*   **创新性强**：首次提出利用薛定谔桥产生的“影子传输”来指导图剪枝，这种概率引导的优化思路非常新颖。
*   **实用性高**：算法不仅保证最优性，还通过剪枝策略解决了大规模部署时的实时性问题。

### 8. 不足与局限
*   **匿名性限制**：目前的方法主要针对“匿名”MAPF（机器人互换无所谓）。虽然文中提到可以扩展到非匿名场景，但未给出具体的实验数据和复杂度分析。
*   **参数依赖**：剪枝阈值 $\eta$ 和正则化参数 $\epsilon$ 需要根据具体场景进行一定程度的调优，否则可能导致 P3 阶段出现不可行解（虽然可以通过调整阈值修复）。
*   **网格依赖**：虽然理论适用于任意图，但实验主要集中在 2D 网格，对于高度动态或连续空间的适应性有待进一步验证。

（完）
