---
title: "TinySDP: Real Time Semidefinite Optimization for Certifiable and Agile Edge Robotics"
title_zh: TinySDP：用于可验证且敏捷的边缘机器人的实时半正定优化
authors: "Ishaan Mahajan, Jon Arrizabalaga, Andrea Grillo, Fausto Vega, James Anderson, Zachary Manchester, Brian Plancher"
date: 2026-05-13
pdf: "https://arxiv.org/pdf/2605.13748v1"
tags: ["query:profile-1"]
score: 9.0
evidence: 针对机器人非凸障碍约束的实时MPC半正定规划求解器
tldr: 针对嵌入式机器人实时运动规划中非凸约束处理难的问题，本文提出了TinySDP，这是首个专为嵌入式系统设计的半正定规划（SDP）求解器。它通过将正定锥投影集成到基于缓存Riccati的ADMM算法中，实现了在微控制器上的实时模型预测控制（MPC）。该方法结合秩-1证书提供几何保证，在复杂避障任务中表现优于现有局部方法，并在Crazyflie无人机上验证了其实时性和敏捷性。
selection_source: fresh_fetch
motivation: 现有的半正定规划求解器计算开销过大，无法满足资源受限的嵌入式系统在实时机器人控制和非凸避障中的需求。
method: 开发了TinySDP求解器，利用基于缓存Riccati结构的ADMM算法处理正定锥投影，并结合后验秩-1证书确保松弛解的几何有效性。
result: "在死胡同和动态避障等挑战性场景中，TinySDP实现了无碰撞导航，且路径长度比现有基准方法缩短了高达73%。"
conclusion: TinySDP证明了在微控制器上实时运行半正定约束优化是可行的，为敏捷边缘机器人提供了具有可认证性的运动规划方案。
---

## 摘要
半正定规划（SDP）为运动规划中非凸几何约束的凸松弛提供了一个原则性框架，但现有的求解器对于实时控制而言计算开销过大，尤其是在资源受限的嵌入式系统上。为了填补这一空白，我们推出了 TinySDP，这是首个专为嵌入式系统设计的半正定规划求解器，能够在微控制器上针对具有非凸障碍物约束的问题实现实时模型预测控制（MPC）。我们的方法将半正定锥投影集成到基于缓存 Riccati 的 ADMM 求解器中，利用计算结构来实现嵌入式设备上的可处理性。我们将该求解器与后验秩-1 证明（a posteriori rank-1 certificate）相结合，在每个时间步将松弛解转换为显式的几何保证。在具有挑战性的基准测试中（例如导致局部方法失败的死胡同和动态避障场景），TinySDP 实现了无碰撞导航，其路径长度比最先进的基准方法缩短了高达 73%。我们在 Crazyflie 四旋翼无人机上验证了我们的方法，证明了半正定约束可以在敏捷嵌入式机器人中以实时速率强制执行。

## Abstract
Semidefinite programming (SDP) provides a principled framework for convex relaxations of nonconvex geometric constraints in motion planning, yet existing solvers are too computationally expensive for real-time control, particularly on resource-constrained embedded systems. To address this gap, we introduce TinySDP, the first semidefinite programming solver designed for embedded systems, enabling real-time model-predictive control (MPC) on microcontrollers for problems with nonconvex obstacle constraints. Our approach integrates positive-semidefinite cone projections into a cached-Riccati-based ADMM solver, leveraging computational structure for embedded tractability. We pair this solver with an a posteriori rank-1 certificate that converts relaxed solutions into explicit geometric guarantees at each timestep. On challenging benchmarks, e.g., cul-de-sac and dynamic obstacle avoidance scenarios that induce failures in local methods, TinySDP achieves collision-free navigation with up to 73% shorter paths than state-of-the-art baselines. We validate our approach on a Crazyflie quadrotor, demonstrating that semidefinite constraints can be enforced at real-time rates for agile embedded robotics.

---

## 论文详细总结（自动生成）

这是一份关于论文《TinySDP: Real Time Semidefinite Optimization for Certifiable and Agile Edge Robotics》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
在机器人运动规划中，避障约束本质上是非凸的。现有的实时模型预测控制（MPC）通常依赖于**局部凸化**（如线性化距离约束）或**控制屏障函数（CBF）**。这些方法在复杂几何环境（如死胡同、窄缝或高速动态障碍物）中容易失败，且缺乏全局几何保证。
**半正定规划（SDP）**通过将非凸二次约束提升（Lifting）到高维正定锥中，提供了更强的凸松弛框架，但其计算开销巨大，传统求解器无法在资源受限的嵌入式微控制器（MCU）上实时运行。本文旨在开发一种能在 MCU 上运行、具备实时性且提供安全证明的 SDP 求解器。

### 2. 论文提出的方法论
*   **核心思想**：将 SDP 引入嵌入式 MPC 框架，利用 **ADMM（交替方向乘子法）** 结合 **缓存 Riccati 递归** 来处理提升后的线性动力学和半正定约束。
*   **关键技术细节**：
    *   **半正定提升（SDP Lifting）**：将位置坐标 $p_k$ 提升为二阶矩矩阵 $X_k = p_k p_k^\top$，从而将非凸的圆形避障约束转化为关于提升变量的线性不等式。
    *   **TinySDP 求解器**：在 ADMM 的 primal 步利用 TinyMPC 的结构，通过预先缓存的 Riccati 矩阵快速求解线性二次调节（LQR）问题；在 slack 步执行小规模对称矩阵的特征值分解（PSD 投影）。
    *   **后验秩-1 证书（A Posteriori Rank-1 Certificate）**：通过计算“迹间隙”（Trace Gap）来评估松弛的紧致性。如果满足特定几何条件，即使松弛不完全精确，也能在数学上证明物理状态是绝对安全的。
    *   **安全监控器**：在每个时间步检查证书，若验证失败则触发“刹车并悬停”的保底策略。

### 3. 实验设计
*   **实验场景**：
    1.  **静态 U 型死胡同**：测试算法是否会陷入局部最优或发生碰撞。
    2.  **动态移动间隙**：三个障碍物（一静两动）周期性关闭通道，测试动态响应。
    3.  **3D 扩展场景**：包括“扫掠屏障”和“垂直门”，验证算法向 3D 空间扩展的能力。
    4.  **真机部署**：在 Crazyflie 2.1 无人机上进行实时避障飞行。
*   **对比基准（Benchmarks）**：
    *   **TinyMPC-LIN**：基于线性化切向半空间的传统方法。
    *   **TinyMPC-HOCBF**：高阶控制屏障函数方法。
    *   **RPCBF**：一种先进的采样策略安全过滤器。

### 4. 资源与算力
*   **硬件平台**：Crazyflie 2.1 无人机，搭载 **STM32F405** 微控制器（168 MHz，192 KB SRAM）。
*   **算力表现**：
    *   **求解频率**：25 Hz（控制周期 40ms）。
    *   **求解耗时**：单次 MPC 求解平均耗时 **14.84 ms**（ADMM 迭代 5 次）。
    *   **内存占用**：Flash 占用 272.4 KB，RAM 占用 139.0 KB。
    *   **计算开销**：PSD 投影通过针对小矩阵优化的特征值分解实现，额外开销极低。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了 4 组不同初始条件的静态实验、1 组复杂的动态模拟、2 组 3D 模拟以及 5 次独立的真机飞行实验。
*   **充分性评价**：实验设计较为充分。作者不仅对比了路径长度和到达目标的精度，还通过消融实验展示了传统方法在零安全裕度下的失效情况。通过真机实验验证了算法在真实噪声和扰动下的鲁棒性，实验结果具有较强的说服力。

### 6. 论文的主要结论与发现
*   **性能优越**：在死胡同场景中，TinySDP 的路径长度比 RPCBF 缩短了 **31%–73%**，且比调优后的线性化方法更接近目标点。
*   **安全性**：传统局部方法（LIN/CBF）在复杂地形中极易碰撞，除非设置巨大的安全裕度（如 1.5m，是无人机尺寸的 17 倍），而 TinySDP 在零裕度下即可实现安全导航。
*   **实时可行性**：证明了 SDP 约束优化可以在低功耗 MCU 上以 25Hz 以上的频率运行，打破了“SDP 太慢无法用于实时控制”的固有印象。

### 7. 优点
*   **首创性**：这是首个专为嵌入式系统设计的实时 SDP 求解器。
*   **理论与实践结合**：不仅提出了凸松弛方法，还配套了高效的后验验证证书，确保了控制指令的可认证安全性。
*   **高效性**：巧妙利用了 Riccati 结构的离线缓存，极大地降低了在线计算负担。

### 8. 不足与局限
*   **递归可行性缺乏证明**：虽然每一步都有后验验证，但论文未能在理论上证明整个时域内的递归可行性（Recursive Feasibility）。
*   **维度扩展瓶颈**：虽然支持 3D，但随着状态维度增加，提升后的矩阵维度会迅速膨胀，特征值分解的计算量将呈立方级增长，可能限制其在更复杂系统上的应用。
*   **保底策略简单**：目前的“刹车悬停”策略是反应式的，在极高动态环境下可能不足以应对所有风险。

（完）
