---
title: Real-time Gaussian Process based Approximate Model Predictive Trajectory Tracking Control for Autonomous Vehicles
title_zh: 自动驾驶车辆中基于高斯过程的实时近似模型预测轨迹跟踪控制
authors: "Alexander Rose, Lukas Theiner, Rolf Findeisen"
date: 2026-05-13
pdf: "https://arxiv.org/pdf/2605.13220v1"
tags: ["query:q5"]
score: 8.0
evidence: 实时高斯过程近似模型预测轨迹跟踪
tldr: 针对嵌入式系统上模型预测控制（MPC）计算开销大的问题，本文提出一种基于高斯过程（GP）的近似MPC轨迹跟踪方法。通过引入曲线坐标系和前馈补偿，显著降低了GP学习所需的数据量。实验证明，该方法在树莓派上运行速度比传统MPC快5倍，且保持了相近的跟踪精度，为自动驾驶车辆提供了高效的实时控制方案。
selection_source: fresh_fetch
motivation: 传统MPC在嵌入式设备上计算成本过高，且轨迹跟踪任务中GP近似方法面临训练数据需求量巨大的挑战。
method: 采用曲线坐标系转换并结合名义前馈组件，使高斯过程仅需学习残差控制输入，从而提高数据效率。
result: 在小型车辆硬件平台上的实验表明，该方法计算速度比实时迭代MPC快约5倍，且跟踪性能相当。
conclusion: 该研究证明了基于高斯过程的近似MPC在资源受限的嵌入式系统中实现高效、实时轨迹跟踪的可行性。
---

## 摘要
由于求解最优控制问题的高计算成本，在嵌入式系统上应用模型预测控制仍然具有挑战性。为了解决这一局限性，可以采用计算效率高的高斯过程来近似隐式模型预测控制律。然而，对于轨迹跟踪应用，在不同参考轨迹上实现成功泛化所需的大量训练数据构成了一个重大挑战。为了提高数据效率，我们建议将模型转换为围绕参考轨迹的曲线坐标系。其次，我们使用标称前馈分量，使高斯过程仅学习残差控制输入，从而使轨迹跟踪控制器的近似变得可行。为了强调该方法的适用性，我们将控制器部署在小型车辆的树莓派（Raspberry Pi）上并进行了实验验证。与使用实时迭代的模型预测控制实现相比，基于高斯过程的近似计算控制输入的速度快约五倍，同时实现了相似的闭环跟踪性能。

## Abstract
Applying model predictive control on embedded systems remains challenging due to the high computational cost of solving optimal control problems. To address this limitation, computationally efficient Gaussian process approximations of the implicit model predictive control law can be employed. However, for trajectory-tracking applications, the large amount of training data required for successful generalization across distinct reference trajectories poses a significant challenge. To improve data efficiency, we propose to transform the model into curvilinear coordinates around the reference trajectory. Secondly, we use a nominal feedforward component, allowing the Gaussian process to learn only the residual control input, making the approximation of a trajectory-tracking controller feasible. To underline the applicability of the approach, we deploy the controller on a Raspberry Pi in a small-scale vehicle and validate it experimentally. Compared to a model predictive control implementation using real-time iterations, the Gaussian process based approximation computes control inputs about five times faster while achieving similar closed-loop tracking performance.

---

## 论文详细总结（自动生成）

这篇论文提出了一种针对自动驾驶车辆的高效实时轨迹跟踪控制方案，通过高斯过程（GP）来近似模型预测控制（MPC）律。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：模型预测控制（MPC）虽然能处理复杂动力学和约束，但其在线求解最优控制问题（OCP）的计算开销巨大，难以在资源受限的嵌入式系统（如树莓派）上实现高频实时更新。
*   **研究动机**：虽然可以使用高斯过程（GP）来近似 MPC 策略以降低计算量，但在“轨迹跟踪”任务中，参考轨迹的不断变化会导致输入维度激增，使得 GP 学习所需的训练数据量过大，难以泛化。

### 2. 方法论
论文提出了两种关键技术来提高数据效率并简化学习任务：
*   **曲线坐标系转换（Curvilinear Coordinates）**：将车辆动力学模型从笛卡尔坐标系（$p_x, p_y, \psi$）转换到相对于参考路径的曲线坐标系（$s, n, \alpha$）。
    *   **效果**：将复杂的二维参考轨迹简化为局部的曲率信息（$\kappa_c$），显著降低了特征空间维度。
*   **残差控制学习（Residual Learning）**：
    *   **核心思想**：将控制输入分解为“名义前馈分量”和“GP 学习的残差分量”。
    *   **公式**：$\hat{u} = u_{ff}(\kappa_c) + \bar{m}_+(\xi)$。其中 $u_{ff}$ 是基于曲率计算的解析前馈输入，GP 仅负责学习 MPC 优化出的控制量与该前馈量之间的差值（$\Delta u$）。
*   **贪婪数据采样（Greedy Sampling）**：为了控制 GP 的在线计算复杂度（复杂度与数据量 $n_D$ 成线性关系），采用贪婪策略从可达集中选择最具代表性的数据点进行训练，而非随机采样。

### 3. 实验设计
*   **实验平台**：小型自动驾驶实验车，车载计算单元为 **Raspberry Pi**。
*   **对比基准（Benchmark）**：使用 **ACADOS** 生成的实时迭代（RTI）MPC 实现。ACADOS 是目前工业界公认的快速嵌入式 MPC 求解工具。
*   **实验场景**：车辆在环形轨道上进行恒定速度（0.5 m/s）的轨迹跟踪。
*   **评估指标**：计算耗时（Computation Time）、均方根误差（RMSE）、闭环轨迹成本（Closed-loop Cost）。

### 4. 资源与算力
*   **离线训练/测试算力**：在一台配备 Intel Core i7-1165G7 CPU（4核，32GB RAM）的桌面电脑上进行。
*   **在线运行算力**：部署在 **Raspberry Pi** 嵌入式系统上。
*   **训练时长**：文中未明确给出具体的训练总时长，但提到通过贪婪算法迭代选择了 2000 个数据点，并最终在嵌入式端使用了 $n_D = 1000$ 的数据规模以平衡精度与速度。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了 3000 个未见测试点的误差分析。
    *   对比了贪婪采样与随机采样在不同数据规模下的 RMSE 表现。
    *   进行了实车闭环运行实验，对比了 GP 近似控制器与原生 MPC 的状态偏差（$n, \alpha$）。
*   **充分性评价**：实验设计较为充分，涵盖了从算法精度分析到硬件实测的完整流程。通过与高性能工具 ACADOS 的对比，客观地证明了该方法在速度上的优势。

### 6. 主要结论与发现
*   **计算速度**：基于 GP 的近似控制器计算速度比 ACADOS RTI-MPC **快约 5 倍**（中位耗时从 $2.7 \times 10^{-4}$s 降至 $5 \times 10^{-5}$s）。
*   **跟踪性能**：近似控制器的闭环成本（0.0092）与原生 MPC（0.0081）非常接近，横向偏差保持在 2cm 以内，证明了近似的有效性。
*   **数据效率**：通过坐标转换和残差学习，仅需 1000 个数据点即可实现高质量的策略近似。

### 7. 优点与亮点
*   **巧妙的坐标转换**：利用曲线坐标系将“全局轨迹跟踪”转化为“局部状态调节”，极大地压缩了 GP 的输入维度。
*   **结构化学习**：结合物理前馈量与机器学习残差，保留了已知的物理结构，降低了学习难度。
*   **硬件落地**：不仅停留在仿真阶段，还在资源受限的树莓派硬件上验证了实时性，具有很强的工程参考价值。

### 8. 不足与局限
*   **场景单一**：实验仅限于恒定速度的环形轨道跟踪，未测试多变速度、紧急避障或复杂动态环境下的表现。
*   **参数依赖**：坐标转换依赖于精确的参考路径曲率信息，如果路径信息存在噪声或突变，可能会影响控制效果。
*   **缺乏安全保证**：虽然 GP 可以提供不确定性估计（方差），但本文仅使用了后验均值，尚未在控制律中显式引入安全约束或稳定性证明。

（完）
