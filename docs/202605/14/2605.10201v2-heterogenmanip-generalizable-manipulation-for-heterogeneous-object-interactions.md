---
title: "HeteroGenManip: Generalizable Manipulation For Heterogeneous Object Interactions"
title_zh: HeteroGenManip：针对异构物体交互的可泛化操作
authors: "Zhenhao Shen, Zeming Yang, Yue Chen, Yuran Wang, Shengqiang Xu, Mingleyang Li, Hao Dong, Ruihai Wu"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10201v2"
tags: ["query:q6"]
score: 8.0
evidence: 通用机器人操作的交互轨迹规划
tldr: 针对机器人处理异构物体交互时的泛化性难题，本文提出 HeteroGenManip 框架。该框架将任务解耦为“在哪操作”和“如何操作”两个阶段。首先利用基础模型引导的抓取模块对齐初始状态，减少位姿不确定性；随后通过多基础模型扩散策略（MFMDP），结合类别专用模型和双流交叉注意力机制，实现精细的几何与部件特征融合。实验证明，该方法在仿真和现实任务中均显著提升了跨类别泛化性能。
selection_source: fresh_fetch
motivation: 现有的端到端模型在处理异构物体交互时，难以应对长程任务中的误差积累以及不同类别物体的多样化特征需求。
method: 提出一种两阶段框架，先通过基础模型引导抓取以对齐初始状态，再利用多基础模型扩散策略（MFMDP）结合类别专用模型进行精细化操作规划。
result: "在仿真和真实世界任务中，该框架分别实现了 31% 和 36.7% 的性能提升，展现出卓越的形状与位姿泛化能力。"
conclusion: 通过解耦抓取与交互阶段并引入多基础模型路由机制，该研究为实现鲁棒且通用的异构物体操作提供了有效方案。
---

## 摘要
涉及跨类型物体交互的可泛化操作是机器人领域中一项关键且具有挑战性的能力。为了可靠地完成此类任务，机器人必须解决两个基本挑战：“在哪里操作”（接触点定位）以及“如何操作”（后续交互轨迹规划）。现有的基于基础模型的方法通常采用端到端学习，模糊了这两个阶段之间的区别，从而加剧了长程任务中的误差累积。此外，它们通常依赖于单一的统一模型，无法捕捉异构物体所需的各种特定类别的特征。为了克服这些局限性，我们提出了 HeteroGenManip，这是一个任务条件的、两阶段的框架，旨在将初始抓取与复杂的交互执行解耦。首先，“基础对应引导抓取”（Foundation-Correspondence-Guided Grasp）模块利用结构先验来对齐初始接触状态，从而显著降低抓取的位姿不确定性。随后，“多基础模型扩散策略”（MFMDP）将物体路由到特定类别的基础模型，通过双流交叉注意力机制将细粒度的几何信息与高度变化的部件特征相结合。实验评估表明，HeteroGenManip 实现了稳健的类内形状和位姿泛化。该框架在具有广泛类型设置的仿真任务中实现了平均 31% 的性能提升，并在四项具有不同交互类型的真实世界任务中获得了 36.7% 的提升。

## Abstract
Generalizable manipulation involving cross-type object interactions is a critical yet challenging capability in robotics. To reliably accomplish such tasks, robots must address two fundamental challenges: "where to manipulate" (contact point localization) and "how to manipulate" (subsequent interaction trajectory planning). Existing foundation-model-based approaches often adopt end-to-end learning that obscures the distinction between these stages, exacerbating error accumulation in long-horizon tasks. Furthermore, they typically rely on a single uniform model, which fails to capture the diverse, category-specific features required for heterogeneous objects. To overcome these limitations, we propose HeteroGenManip, a task-conditioned, two-stage framework designed to decouple initial grasp from complex interaction execution. First, Foundation-Correspondence-Guided Grasp module leverages structural priors to align the initial contact state, thereby significantly reducing the pose uncertainty of grasping. Subsequently, Multi-Foundation-Model Diffusion Policy (MFMDP) routes objects to category-specialized foundation models, integrating fine-grained geometric information with highly-variable part features via a dual-stream cross-attention mechanism. Experimental evaluations demonstrate that HeteroGenManip achieves robust intra-category shape and pose generalization. The framework achieves an average 31% performance improvement in simulation tasks with broad type setting, alongside a 36.7% gain across four real-world tasks with different interaction types.

---

## 论文详细总结（自动生成）

以下是对论文《HeteroGenManip: Generalizable Manipulation For Heterogeneous Object Interactions》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在涉及异构物体（刚体、可变形物体、关节体）及其跨类型交互（如将柔软衣物挂在刚性衣架上）的复杂任务中，实现机器人的可泛化操作。
*   **研究动机**：
    *   **现有方法的局限性**：目前的端到端学习方法模糊了“在哪操作”（接触点定位）和“如何操作”（轨迹规划）的界限，导致长程任务中误差累积严重。
    *   **特征表示不匹配**：现有方法通常依赖单一基础模型，但单一模型难以同时兼顾刚体的几何精确性和可变形物体的拓扑灵活性。
    *   **任务复杂性**：异构物体交互涉及物理属性的剧烈差异，对感知的鲁棒性和控制的精确性提出了极高要求。

### 2. 方法论
HeteroGenManip 采用**任务条件下的两阶段框架**，将操作过程解耦：

*   **第一阶段：基础对应引导抓取 (FCGG)**
    *   **核心思想**：利用预训练基础模型的结构先验来确定最佳接触点。
    *   **技术细节**：通过计算演示点云与目标点云特征之间的**余弦相似度**来定位对应点。对于刚体，引入辅助参考点来估计 3D 旋转方向；对于可变形物体，则采用基于平面状态的固定向量。
*   **第二阶段：多基础模型扩散策略 (MFMDP)**
    *   **类别专用路由**：根据物体类型（刚体/关节体 vs. 可变形物体）分别路由至不同的基础模型（如 Uni3D 用于刚体，GAM 用于衣物）。
    *   **特征处理**：对 Uni3D 的高维特征进行 **PCA 降维**以平衡空间信息；对 GAM 特征计算相似度向量。
    *   **双流交叉注意力机制**：设计了“空间坐标流”和“语义描述符流”，通过交叉注意力融合，确保高维语义特征不会掩盖细粒度的几何位置信息。
    *   **扩散策略**：将融合后的特征输入基于 DDIM 的扩散模型，迭代预测后续交互动作。

### 3. 实验设计
*   **实验场景**：
    *   **仿真环境**：基于 RoboTwin 和 DexGarmentLab 构建了 8 项任务，涵盖“刚体-关节体”、“刚体-变形体”和“变形体-变形体”交互（如叠衣服、挂大衣、放杯子、敲木块等）。
    *   **真实世界**：使用 ARX-X7s 人形机器人完成 4 项任务（挂上衣、插衣架、放马克杯、收纳衣服）。
*   **Benchmark 与对比方法**：
    *   对比了 **DP** (Diffusion Policy), **DP3**, **GenDP**, **3DFA** 以及视觉语言动作模型 **Pi0.5**。
*   **评估设置**：分为 Train（训练集内物体）和 Test（未见过的形状、位姿和空间配置）两个设置，以验证泛化性。

### 4. 资源与算力
*   **硬件**：
    *   主要模型训练：单张 **NVIDIA GeForce RTX 4090 (24GB)** GPU。
    *   基准模型 Pi0.5 微调：使用了 **8 张 NVIDIA A800** GPU。
*   **软件/参数**：使用 PyTorch 2.8；MFMDP 训练 3000 个 epoch；扩散步数设置为 10。

### 5. 实验数量与充分性
*   **实验规模**：
    *   仿真实验：每项任务收集 50 个专家演示，评估时进行 3 次独立运行，每次 50 次尝试（共 150 次/任务）。
    *   真实实验：每项任务 25 个演示，测试阶段针对未见过物体进行 15 次试验。
*   **消融实验**：系统性地移除了抓取模块（w/o CG）、位置增强编码器（w/o PE）和多基础模型架构（w/o MFM），验证了各组件的必要性。
*   **充分性评价**：实验覆盖了多种物理属性组合，对比了当前最先进的基准模型，且包含严格的类外泛化测试，实验设计客观且公平。

### 6. 主要结论与发现
*   **性能提升**：HeteroGenManip 在仿真任务中平均提升了 **31%** 的成功率，在真实世界任务中提升了 **36.7%**。
*   **解耦的优势**：将抓取与后续轨迹解耦显著降低了策略学习的难度，尤其是在处理具有旋转变化的物体（如杯柄）时。
*   **多模型的必要性**：实验证明，单一模型（如仅用 Uni3D 或 DINOv2）在处理跨类型交互时会产生表征冲突，而特定类别的路由策略能显著增强泛化能力。

### 7. 优点
*   **架构创新**：通过两阶段解耦和多模型路由，有效解决了异构物体操作中的特征失真和误差累积问题。
*   **空间感知增强**：双流交叉注意力机制解决了基础模型特征维度过高导致的空间定位偏差问题。
*   **强泛化性**：在未见过的物体形状和复杂交互场景下表现出极高的鲁棒性，sim-to-real 效果良好。

### 8. 不足与局限
*   **视角依赖**：输入依赖单视图点云，语义特征提取受限于相机视角。
*   **感知瓶颈**：严重依赖前端分割模型（如 SAM 3）和深度图质量。对于细长物体（如薄衣架、细绳），分割或深度缺失会导致操作失败。
*   **分类预设**：框架需要预先定义物体类别以进行模型路由，对于完全未知的物体类别可能需要额外的分类模块。

（完）
