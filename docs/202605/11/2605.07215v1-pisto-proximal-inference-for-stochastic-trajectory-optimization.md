---
title: "PISTO: Proximal Inference for Stochastic Trajectory Optimization"
title_zh: PISTO：随机轨迹优化的近端推理
authors: "Hongzhe Yu, Zinuo Chang, Yongxin Chen"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.07215v1"
tags: ["query:q3"]
score: 9.0
evidence: 随机轨迹优化与近端推理
tldr: 本研究揭示了随机轨迹优化方法STOMP背后的变分推理结构，并据此提出了PISTO算法。PISTO通过在连续高斯提议之间引入KL散度正则化，构建了近端推理框架，实现了具有置信域特性的闭式均值更新。该算法无需导数，能处理不可微代价函数。实验表明，PISTO在机器人运动规划和复杂操控任务中，其成功率、路径质量及运行速度均显著优于STOMP、CHOMP、CEM和MPPI等基准方法。
selection_source: fresh_fetch
motivation: 旨在解决现有随机轨迹优化方法在处理非连续或不可微代价函数时存在的稳定性不足和收敛效率低的问题。
method: 提出一种近端推理框架，通过在连续高斯分布间引入KL正则化，并利用重要性采样实现无需梯度的闭式均值更新。
result: "在机器人臂规划任务中取得89%的成功率，且在路径平滑度、运行速度及MuJoCo复杂操控任务的奖励上均优于STOMP、CEM和MPPI等基准。"
conclusion: PISTO通过结合变分推理与近端优化，为处理具有挑战性的非连续代价函数提供了一种高效、稳健且通用的随机轨迹优化方案。
---

## 摘要
STOMP 等随机轨迹优化方法支持使用不可微代价进行规划，相比基于梯度的方法提供了极大的灵活性。我们证明了 STOMP 隐式地最小化了与玻尔兹曼轨迹分布之间的 KL 散度，揭示了其更新过程中蕴含的优雅变分推理（VI）结构。基于这一见解，我们提出了随机轨迹优化的近端推理（PISTO）算法，该算法通过在目标函数中增加连续高斯提议之间的 KL 正则化项来稳定更新。这种近端公式具有置信域解释，并能产生闭式均值更新，这些更新可作为代理分布下的期望进行计算。我们通过重要性权重蒙特卡洛采样来估计这些期望，从而产生了一种简单、无导数的算法，该算法继承了 STOMP 无需修改即可处理不可微和不连续代价的能力。在机械臂运动规划基准测试中，PISTO 实现了 89% 的成功率——优于 CHOMP (63%) 和 STOMP (68%)——同时生成的路径更短、更平滑，且速度是竞争随机方法的两倍。我们进一步在具有丰富接触的 MuJoCo 运动和操纵任务上验证了 PISTO，其在奖励方面始终优于 CEM 和 MPPI 基准。

## Abstract
Stochastic trajectory optimization methods like STOMP enable planning with non-differentiable costs, offering substantial flexibility over gradient-based approaches. We show that STOMP implicitly minimizes the KL divergence from a Boltzmann trajectory distribution, revealing an elegant Variational Inference (VI) structure underlying its updates. Building on this insight, we propose the \textit{Proximal Inference for Stochastic Trajectory Optimization} (PISTO) algorithm that stabilizes the updates by augmenting the objective with a KL regularization between successive Gaussian proposals. This proximal formulation admits a trust-region interpretation and yields closed-form mean updates computable as expectations under a surrogate distribution. We estimate these expectations via importance-weighted Monte Carlo sampling, producing a simple, derivative-free algorithm that inherits STOMP's ability to handle non-differentiable and discontinuous costs without modification. On robot arm motion planning benchmarks, PISTO achieves an 89\% success rate -- outperforming CHOMP (63\%) and STOMP (68\%) -- while producing shorter, smoother paths at twice the speed of competing stochastic methods. We further validate PISTO on contact-rich MuJoCo locomotion and manipulation tasks, where it consistently outperforms both CEM and MPPI baselines in reward.

---

## 论文详细总结（自动生成）

这是一份关于论文《PISTO: Proximal Inference for Stochastic Trajectory Optimization》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：在机器人运动规划中，如何高效且稳定地处理**不可微（Non-differentiable）**和**不连续（Discontinuous）**的代价函数（如碰撞检测、接触力等）。
*   **研究动机**：
    *   传统的基于梯度的优化器（如 CHOMP）依赖代价函数的导数，容易陷入局部最优。
    *   随机优化方法（如 STOMP）虽然灵活，但缺乏清晰的理论目标函数解释，且更新过程可能不稳定。
    *   **理论贡献**：论文揭示了 STOMP 算法本质上是在最小化与玻尔兹曼（Boltzmann）轨迹分布之间的 KL 散度，这为引入更先进的变分推理（VI）技术提供了理论基础。

### 2. 论文提出的方法论：PISTO
*   **核心思想**：将轨迹优化重新表述为**近端变分推理（Proximal Variational Inference）**问题。通过在相邻两次迭代的高斯分布之间引入 KL 散度正则化，构建一个“置信域（Trust-region）”，从而稳定优化过程。
*   **关键技术细节**：
    *   **目标函数重构**：在原始的 KL 散度目标中增加一个近端项（Proximal term），惩罚当前分布与上一步分布之间的偏差。
    *   **代理分布（Surrogate Distribution）**：证明了该近端公式等价于向一个几何插值后的代理分布进行投影。
    *   **闭式更新（Closed-form Update）**：推导出均值更新的解析解，即新轨迹是代理分布下的期望值。
    *   **重要性采样（Importance Sampling）**：利用蒙特卡洛采样和重要性权重来估计上述期望，使得算法完全**无需计算梯度（Derivative-free）**。
    *   **策略优化扩展**：通过控制空间（Action Space）的展开（Rollout），将 PISTO 应用于具有复杂动力学的接触密集型任务。

### 3. 实验设计
*   **场景与数据集**：
    1.  **机械臂运动规划**：使用 MoveIt 和 MotionBenchMaker 数据集，包含 Kitchen、Bookshelf、Table Pick 等 7 个复杂环境。
    2.  **接触密集型任务（MuJoCo）**：包括 PushT（物体推移）、Hopper、Walker2d、HumanoidRun（机器人奔跑）和 HumanoidStandUp（起立）。
*   **基准方法（Benchmarks）**：
    *   运动规划类：CHOMP、STOMP、NGD（自然梯度下降）。
    *   强化学习/控制类：CEM（交叉熵方法）、MPPI（模型预测路径积分控制）。

### 4. 资源与算力
*   **硬件配置**：
    *   CPU：Intel Core i7-12800H。
    *   GPU：NVIDIA RTX 4090（主要用于 MuJoCo 任务的并行仿真）。
*   **软件实现**：运动规划任务采用 C++ 实现，MuJoCo 任务采用 Python 实现。
*   **耗时示例**：17 自由度的人类奔跑任务优化时间约为 142 秒，起立任务约为 168 秒。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在机械臂规划中，针对 300 多个规划任务进行了测试，每个任务重复 20 次（不同随机种子）。
    *   在 MuJoCo 任务中，结果基于 50 次独立运行的平均值。
*   **充分性评价**：实验设计非常充分。涵盖了从低维机械臂到高维（17-DOF）类人机器人的跨度，对比了经典的梯度方法和最先进的随机采样方法，并提供了成功率、路径长度、平滑度和运行时间等多维度指标，客观性较强。

### 6. 论文的主要结论与发现
*   **性能卓越**：PISTO 在机械臂规划中的成功率达到 **89%**，显著高于 CHOMP (63%) 和 STOMP (68%)。
*   **速度优势**：在保持高质量路径的同时，PISTO 的运行速度通常是其他随机方法的 **2 倍**。
*   **鲁棒性强**：在处理具有高度非线性和不连续动力学的 MuJoCo 任务时，PISTO 获得的奖励（Reward）始终优于 CEM 和 MPPI。
*   **路径质量**：生成的轨迹比 STOMP 更短且更平滑，证明了近端正则化在保持平滑性方面的有效性。

### 7. 优点
*   **理论优雅**：为随机轨迹优化提供了坚实的变分推理理论支撑。
*   **无需梯度**：能够直接处理黑盒代价函数，极大地扩展了应用范围。
*   **稳定性高**：通过近端项引入的置信域机制，有效解决了随机优化中常见的步长过大或收敛不稳的问题。
*   **易于并行**：采样结构天然适合 GPU 加速。

### 8. 不足与局限
*   **协方差固定**：目前主要优化轨迹均值，协方差矩阵（探索噪声）多采用预设或简单的退火策略，尚未实现均值与协方差的完全联合变分优化。
*   **局部最优风险**：尽管比梯度方法更强，但在极端复杂的非凸空间中，仍可能受限于初始值和采样效率。
*   **超参数敏感性**：近端参数 $\eta$ 和温度参数 $\tau$ 的选择对性能有影响，文中虽提到了退火策略，但可能仍需针对特定任务调优。

（完）
