---
title: Coordinated Motion Planning is FPT on Discretized Simple Polygons
title_zh: 协同运动规划在离散化简单多边形上是固定参数可解的
authors: "Argyrios Deligkas, Eduard Eiben, Robert Ganian, Iyad Kanj"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.07570v1"
tags: ["query:q4"]
score: 8.0
evidence: 多机器人协同运动规划
tldr: 本研究探讨了在离散化简单多边形图上的协调运动规划问题，旨在最小化k个机器人的总行驶长度。该问题已知为NP难，但在特定图结构下具有固定参数可解性（FPT）。本文提出了一种以机器人数量k为参数的FPT算法，成功将FPT结果从全网格扩展到更复杂的简单多边形离散化图。这一成果不仅具有实际应用价值，还为解决子网格及平面图上的运动规划参数复杂性奠定了重要基础。
selection_source: fresh_fetch
motivation: 旨在解决在更复杂的现实多边形环境离散化图中，如何高效地进行多机器人无碰撞路径规划并最小化总路径长度。
method: 提出了一种针对简单多边形离散化图的固定参数算法，以机器人数量k作为核心参数。
result: 证明了在离散化简单多边形上，以机器人数量为参数的协调运动规划问题是固定参数可解（FPT）的。
conclusion: 该研究将FPT结论从全网格推广到简单多边形，是迈向解决子网格和平面图运动规划难题的关键一步。
---

## 摘要
在协同运动规划问题中，给定一个图以及 $k$ 个机器人的起始顶点和目标顶点。在每个时间步，任何机器人子集都可以移动，每个机器人遍历图的一条边，前提是任意两个机器人不发生碰撞。目标是计算一个调度方案，在最小化某个目标函数的同时，将所有机器人引导至其目的地。在本文中，我们关注一个被广泛研究的目标，即最小化所有机器人的总行驶长度。该问题已知是 NP 难的，并且已被证明在以机器人数量 $k$ 为参数时，在全网格（SoCG 2023）和有界树宽图（ICALP 2024）上是固定参数可解（FPT）的。我们针对由简单多边形离散化生成的图，提出了一种以机器人数量 $k$ 为参数的协同运动规划固定参数算法。此类图在实际应用中具有特殊意义，因为平面运动通常受限于多边形环境的离散化表示。此外，这些图推广了矩形网格；因此，我们的结果在解决子网格以及最终在平面图上的协同运动规划参数复杂性方面迈出了重要一步——这是该领域两个突出的开放问题。

## Abstract
In the coordinated motion planning problem, we are given a graph together with the starting and destination vertices of $k$ robots. At each time step, any subset of robots may move, each traversing an edge of the graph, provided that no two robots collide. The goal is to compute a schedule that routes all robots to their destinations while minimizing some objective function. In this paper, we focus on the well-studied objective of minimizing the total travel length of all robots. This problem is known to be NP-hard, and it has been shown to be fixed-parameter tractable (FPT), when parameterized by the number $k$ of robots, on full grids (SoCG 2023) and on bounded-treewidth graphs (ICALP 2024).   We present a fixed-parameter algorithm for coordinated motion planning, parameterized by the number $k$ of robots, on graphs arising from discretizations of simple polygons. Such graphs are of particular interest in real-world applications, where planar motion is often constrained to discretized representations of polygonal environments. Moreover, these graphs generalize rectangular grids; consequently, our result constitutes a significant step toward resolving the parameterized complexity of coordinated motion planning on subgrids and, ultimately, planar graphs -- two prominent open problems in the field.

---

## 论文详细总结（自动生成）

这是一份关于论文《Coordinated Motion Planning is FPT on Discretized Simple Polygons》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：协同运动规划（Coordinated Motion Planning, CMP），又称多智能体路径规划（MAPF），是机器人学和人工智能领域的核心问题。其目标是在避免碰撞的前提下，为 $k$ 个机器人寻找从起点到终点的最优路径。
*   **核心问题**：本文关注的是**最小化所有机器人总行驶长度（能量）**这一目标。该问题在一般图上是 NP-难的。
*   **研究动机**：虽然已知该问题在“全矩形网格”和“有界树宽图”上是固定参数可解（FPT）的，但在“子网格”和“平面图”上是否为 FPT 仍是悬而未决的难题。本文研究的“离散化简单多边形”介于全网格与一般子网格之间，是解决上述开放问题的关键一步，且更符合现实中机器人在复杂多边形环境（如自动化仓库）中的运动建模。

### 2. 论文提出的方法论
论文提出了一种基于**树宽归约（Treewidth Reduction）**的固定参数算法，核心步骤如下：
*   **预处理与松弛限制**：利用已有研究将问题简化，假设每个机器人的行驶长度与其最短路径之差（松弛量）被限制在 $k$ 的多项式范围内。
*   **扇区分解（Sector Decomposition）**：根据顶点到所有终端（起点/终点）的“转弯距离”（Bend Distance）向量，将复杂的离散多边形图划分为不同的“扇区”（Sectors）。
*   **扇区图分析**：构建扇区图 $\Gamma$，证明其树宽仅由机器人数量 $k$ 决定。扇区被分类为直方图（Histogram）、阶梯形（Staircase）和矩形（Rectangle）三种类型。
*   **归约规则（Reduction Rules）**：
    *   **扁平化**：移除直方图扇区中远离基线的冗余顶点。
    *   **缓冲区修剪**：对阶梯形扇区，证明机器人只需在基线附近的缓冲区内活动。
    *   **编织（Weaving）技术**：这是本文的创新点。对于大型矩形扇区，将其内部替换为一组不相交的“编织路径”（度为2的路径），在保持路径等效性的同时，强制降低图的结构复杂度。
*   **最终求解**：证明经过上述归约后的图，其树宽被限制为 $k$ 的函数，随后调用现有的针对有界树宽图的 FPT 算法进行求解。

### 3. 实验设计
*   **性质说明**：本论文属于**理论计算机科学（算法理论）**论文，主要通过严密的数学证明来确立复杂度上界。
*   **对比基准**：论文的对比对象是该领域之前的理论成果，如 SoCG 2023 关于全网格的 FPT 结论和 ICALP 2024 关于有界树宽图的结论。
*   **场景建模**：研究对象是嵌入在欧几里得平面中的简单多边形（无孔洞）的单位网格离散化表示。

### 4. 资源与算力
*   **算力说明**：文中**未提及**任何具体的 GPU、CPU 型号或训练时长。
*   **原因**：作为理论算法论文，其贡献在于证明了存在运行时间为 $f(k) \cdot |V(G)|^{O(1)}$ 的算法，而非通过实验测量实际运行时间。

### 5. 实验数量与充分性
*   **实验充分性**：由于没有实证实验，其“充分性”体现在数学证明的完备性上。论文长达 37 页，包含了对扇区几何性质、归约规则等价性以及最终树宽上界的详细引理证明。
*   **客观性**：证明过程遵循标准的参数复杂性理论框架，逻辑严密。

### 6. 论文的主要结论与发现
*   **主要结论**：证明了在离散化简单多边形上，以机器人数量 $k$ 为参数的协同运动规划问题（CMP-D）是 **固定参数可解（FPT）** 的。
*   **发现**：即使多边形边界非常复杂，通过合理的几何划分和结构归约，依然可以将全局规划问题转化为在低树宽结构上的搜索问题。

### 7. 优点
*   **理论突破**：成功将 FPT 结果从规则的全网格推广到了具有复杂边界的简单多边形，这是向解决平面图 CMP 问题迈出的重要一步。
*   **创新工具**：引入了“编织（Weaving）”小部件和基于转弯向量的扇区分析方法，这些技术可能被用于解决其他几何图算法问题。
*   **现实意义**：离散化多边形比全网格更贴近实际机器人导航环境。

### 8. 不足与局限
*   **应用限制**：目前仅限于**简单多边形**，即不支持含有“孔洞”（障碍物岛屿）的环境。含有孔洞的环境会显著增加拓扑复杂度。
*   **常数项巨大**：FPT 算法中的函数 $f(k)$ 通常包含极大的常数（如指数级或双指数级），这意味着该算法在 $k$ 较大时在工程实践中可能难以直接运行。
*   **缺乏实证**：论文未提供原型系统实现，无法评估该算法在处理大规模实际地图时的具体效率。

（完）
