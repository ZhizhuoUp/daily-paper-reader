---
title: "IMPACT: An Implicit Active-Set Augmented Lagrangian for Fast Contact-Implicit Trajectory Optimization"
title_zh: IMPACT：一种用于快速接触隐式轨迹优化的隐式有效集增广拉格朗日方法
authors: "Jiayun Li, Dejian Gong, Georgia Chalvatzaki"
date: 2026-05-09
pdf: "https://arxiv.org/pdf/2605.09127v1"
tags: ["query:q3"]
score: 8.0
evidence: 用于操作的快速接触隐式轨迹优化
tldr: 本研究针对接触丰富机器人任务中的接触隐式轨迹优化（CITO）难题，提出了一种名为 IMPACT 的高效求解框架。针对底层互补约束数学规划（MPCC）存在的数值病态问题，IMPACT 采用增广拉格朗日方法，在迭代过程中动态识别隐式接触模式分支，并提供平稳性保证。通过高效的 C++ 实现，该方法在 CITO 基准测试中实现了平均 13.8 倍的加速，并在灵巧操作仿真和真机推物任务中验证了其卓越的控制性能和实时性潜力。
selection_source: fresh_fetch
motivation: 旨在解决接触隐式轨迹优化中互补约束导致的数值病态问题，提高复杂接触任务的求解效率和稳定性。
method: 提出一种基于增广拉格朗日的隐式活动集方法，通过在优化过程中实时识别接触模式分支来高效处理 MPCC 约束。
result: 实验表明 IMPACT 相比现有强基准方法实现了 2.9 至 70 倍的加速，并显著提升了灵巧操作任务的控制质量。
conclusion: 该方法为接触丰富的机器人规划与控制提供了一个兼具速度、鲁棒性与理论保证的高效求解器。
---

## 摘要
接触隐式轨迹优化（CITO）作为一种用于富接触机器人任务规划与控制的统一框架，正受到越来越多的关注。最近的方法在操纵和运动任务中展示了良好的效果，且无需预设接触模式计划。众所周知，其底层的带互补约束的数学规划（MPCCs）在数值上仍然是病态的，而针对 CITO 的系统化、可扩展的求解策略仍是一个活跃的研究领域。因此，能够处理接触约束的更高效且有原则的求解器对于扩大 CITO 的适用性至关重要。在这项工作中，我们开发了一种用于 CITO 的增广拉格朗日方法，旨在求解具有平稳性保证的基于 MPCC 的 CITO 问题。该方法可以被解释为在轨迹优化（TO）迭代过程中实时识别隐式接触模式分支；我们将这种方法称为 IMPACT（隐式接触有效集轨迹优化）。我们提供了一个针对轨迹优化工作负载定制的高效 C++ 实现，并在开源 CITO 和接触隐式模型预测控制（CI-MPC）基准测试中对其进行了评估。在 CITO 上，IMPACT 相比强基准方法实现了 2.9 倍至 70 倍的加速（几何平均值为 13.8 倍）。在 CI-MPC 上，我们在仿真环境下展示了其在灵巧操纵任务中对富接触轨迹控制质量的提升。最后，我们在真实机器人硬件上通过 T 形物体推取任务演示了所提方法。

## Abstract
Contact-implicit trajectory optimization (CITO) has attracted growing attention as a unified framework for planning and control in contact-rich robotic tasks. Recent approaches have demonstrated promising results in manipulation and locomotion without requiring a prescribed contact-mode schedule. It is well known that the underlying mathematical programs with complementarity constraints (MPCCs) remain numerically ill-conditioned, and systematic, scalable solution strategies for CITO remain an active area of research. More efficient and principled solvers that can handle contact constraints are therefore essential to broaden the applicability of CITO. In this work, we develop an augmented-Lagrangian approach to CITO for solving MPCC-based CITO with stationarity guarantees. The method can be interpreted as identifying the implicit contact-mode branches on the fly during the trajectory optimization (TO) iterations; we call this approach IMPACT (IMPlicit contact ACtive-set Trajectory optimization). We provide an efficient C++ implementation tailored to trajectory-optimization workloads and evaluate it on the open-source CITO and contact-implicit model predictive control (CI-MPC) benchmarks. On CITO, IMPACT achieves 2.9x-70x speedups over strong baselines (geometric mean 13.8x). On CI-MPC, we show improved control quality for contact-rich trajectories on dexterous manipulation tasks in simulation. Finally, we demonstrate the proposed method on real robotic hardware on a T-shaped object pushing task.

---

## 论文详细总结（自动生成）

这是一份关于论文《IMPACT: An Implicit Active-Set Augmented Lagrangian for Fast Contact-Implicit Trajectory Optimization》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：接触隐式轨迹优化（CITO）在处理富接触机器人任务（如灵巧操作、足式运动）时，其底层的“带互补约束的数学规划”（MPCC）具有高度的非光滑性和数值病态性。
*   **研究动机**：现有的求解方法（如平滑化、松弛法或惩罚法）往往在数值上不够稳健，或者需要复杂的启发式参数调整，且在逼近原始硬约束时容易出现计算效率低下或不收敛的问题。研究者需要一种既能保证数学平稳性（Stationarity），又能实现实时控制速度的高效求解器。

### 2. 方法论
IMPACT 的核心思想是将增广拉格朗日（AuLa）框架与隐式活动集策略相结合，直接处理非光滑的互补约束。
*   **垂直重构（Vertical Reformulation）**：引入松弛变量 $Y$ 和 $Z$，将非线性的互补约束转化为平滑的等式约束（$G(x)=y, H(x)=z$），而将互补逻辑限制在变量 $y, z$ 之间。
*   **受保护的增广拉格朗日外环（Safeguarded AuLa Outer Loop）**：通过更新惩罚参数和拉格朗日乘子来处理等式和不等式约束，同时保持互补约束作为内层子问题的硬约束，避免了乘子爆炸问题。
*   **块坐标下降内环求解器（BCD Inner Solver）**：
    *   **X 块更新**：固定松弛变量，使用带线搜索的 Gauss-Newton 法更新轨迹变量。
    *   **Y, Z 块更新**：固定轨迹变量，利用互补约束的特殊结构，通过“对偶移位目标投影”实现**闭式解（Closed-form update）**。这一步能动态识别当前的接触分支（接触或不接触），即“隐式活动集”。
*   **平稳性保证**：论文在理论上证明了该算法在标准假设下能够收敛到 MPCC 的一阶平稳点（M-stationarity）。

### 3. 实验设计
实验涵盖了从离线规划到在线控制，再到实机验证的全流程：
*   **长时程 CITO 基准测试（CRISP 框架）**：包含 Push Box（推箱子）、Cart Transport（小车运输）、Push T（推 T 形块）三个任务。
    *   **对比方法**：Scholtes 松弛法 (SR)、平方惩罚法 (PM)、以及最先进的 CRISP 求解器。
*   **高维多接触 CI-MPC 基准测试**：使用 Allegro Hand（灵巧手）对 17 种不同形状的物体进行重定向。
    *   **对比方法**：基于平滑接触模型的 cfree 框架（使用 IPOPT 求解）。
*   **实机演示**：在 Panda 机器人上进行 T 形物体的实时推取任务。

### 4. 资源与算力
*   **硬件环境**：所有实验均在一台配备 **AMD Ryzen 9 7950X3D CPU** 的台式电脑上运行。
*   **算力细节**：
    *   **未使用 GPU 加速**。
    *   算法采用 C++ 实现，利用 Eigen 库进行稀疏线性代数运算和 SIMD 向量化优化。
    *   在灵巧手 MPC 实验中，IMPACT 达到了约 **9.5 Hz** 的控制频率，而对比的平滑化方法（cfree）在相同硬件上约为 50 Hz（但 IMPACT 处理的是更难的非光滑模型）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   CITO 基准测试中，每个任务随机采样 50 个目标/起始点。
    *   灵巧手实验涵盖 17 种物体，每种物体进行 10 次试验，总计 170 次长序列仿真。
    *   实机测试进行了 10 次不同初始配置的试验。
*   **充分性与公平性**：实验对比了成功率、跟踪误差、迭代次数、运行时间以及控制质量（方差、平滑度、能量消耗）。所有方法在相同的目标函数权重和终止条件下运行，确保了对比的客观性。

### 6. 主要结论与发现
*   **速度优势**：在 CITO 任务中，IMPACT 相比强基准方法实现了 **2.9x 至 70x** 的加速（几何平均值 13.8x），相比 CRISP 求解器在特定任务上快了 34 倍。
*   **控制质量**：在灵巧手操作中，尽管 IMPACT 的频率低于平滑化方法，但其产生的控制信号更平滑（控制方差和能量消耗更低），避免了平滑化模型常见的振荡现象。
*   **稳健性**：IMPACT 在不预设接触序列的情况下，能够自主发现复杂的接触模式切换，并保持 100% 的任务成功率（除个别极细长物体外）。

### 7. 优点
*   **高效性**：通过 BCD 和闭式解更新，极大地降低了处理互补约束的计算开销。
*   **理论完备**：提供了明确的平稳性保证，而非纯启发式方法。
*   **无需预设**：不需要预先指定接触时间表或进行复杂的同伦路径设计。
*   **控制性能**：相比平滑化方法，能更好地捕捉接触的“开关”特性，从而获得更高质量的控制轨迹。

### 8. 不足与局限
*   **局部最优风险**：作为一种基于梯度的局部优化方法，在计算预算有限时，对初始值和接触鼓励成本（Contact-encouraging costs）仍有一定依赖。
*   **计算频率限制**：虽然在 CITO 中极快，但在极高维度的 MPC 场景下（如灵巧手），其 10Hz 的频率仍低于某些高度优化的平滑化求解器，可能限制其在极高速动态任务中的应用。
*   **模式切换抖动**：在计算预算极低时，可能会出现接触模式在迭代间的频繁切换（Transient switching）。

（完）
