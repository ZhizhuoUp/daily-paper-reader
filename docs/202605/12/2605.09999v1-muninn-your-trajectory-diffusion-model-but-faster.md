---
title: "Muninn: Your Trajectory Diffusion Model But Faster"
title_zh: Muninn：更快速的轨迹扩散模型
authors: "Gokul Puthumanaillam, Hao Jiang, Ruben Hernandez, Jose Fuentes, Paulo Padrao, Leonardo Bobadilla, Melkior Ornik"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.09999v1"
tags: ["query:q8"]
score: 9.0
evidence: 使用扩散模型的机器人运动实时轨迹优化
tldr: 针对基于扩散模型的轨迹规划器推理速度慢的问题，本文提出了Muninn，一种无需重新训练的缓存封装方案。它通过监控扩散过程中内部表示的变化和误差界限，动态决定是否复用之前的去噪器输出。在保持任务性能和安全性的前提下，Muninn实现了高达4.6倍的加速，并成功应用于实时机器人导航和操作任务。
selection_source: fresh_fetch
motivation: 扩散模型在轨迹规划中表现优异但推理速度极慢，现有加速方法往往牺牲规划质量或需要昂贵的重新训练。
method: 提出一种基于不确定性预算的缓存机制，通过分析去噪器误差对状态更新的影响，动态选择复用缓存或重新计算。
result: 在多个基准测试中实现了高达4.6倍的墙钟时间加速，同时保持了与全量计算相当的任务成功率和安全性。
conclusion: Muninn提供了一种通用、高效且无需训练的加速框架，显著提升了扩散轨迹规划器在实时机器人控制中的实用性。
---

## 摘要
基于扩散的轨迹规划器可以合成丰富的多模态机器人运动，但其迭代去噪过程使得在线规划和控制的速度慢得令人望而却步。现有的加速方法要么修改采样器，要么压缩网络——这往往会牺牲规划质量，或者需要重新训练，且未考虑下游控制风险。我们致力于解决如何使基于扩散的轨迹规划器在不重新训练模型或牺牲轨迹质量的情况下，达到足以用于机器人实时控制的速度，并使其适用于各种状态空间扩散架构。我们的核心见解是，扩散轨迹规划器暴露了两个可供利用的信号：一个是用于探测内部轨迹表示随步数变化的廉价探针，另一个是描述去噪器误差如何影响采样器状态更新的解析系数。通过在离线运行中根据第二个信号校准第一个信号，我们获得了一个每步评分，该评分界定了当我们重用缓存的去噪器输出时，最终轨迹可能偏离的最大程度；我们将此界限视为在去噪过程中可以消耗的不确定性预算。基于这一见解，我们提出了 Muninn，这是一个无需训练的缓存封装器，它在采样过程中跟踪此不确定性预算，并在每个扩散步骤中，根据预测偏差的大小选择是重用缓存的去噪器输出，还是重新计算去噪器。在标准基准测试中，Muninn 通过减少去噪器评估次数，在多种轨迹扩散模型上实现了高达 4.6 倍的实际运行加速，同时保持了任务性能和安全指标。Muninn 进一步证明了缓存的展开轨迹与全计算轨迹保持在指定距离内，我们还在实时闭环导航和操纵硬件部署中验证了这些提升。项目页面：https://github.com/gokulp01/Muninn。

## Abstract
Diffusion-based trajectory planners can synthesize rich, multimodal robot motions, but their iterative denoising makes online planning and control prohibitively slow. Existing accelerations either modify the sampler or compress the network--sacrificing plan quality or requiring retraining without accounting for downstream control risk. We address the problem of making diffusion-based trajectory planners fast enough for real-time robot use without retraining the model or sacrificing trajectory quality, and in a way that works across diverse state-space diffusion architectures. Our key insight is that diffusion trajectory planners expose two signals we can exploit: a cheap probe of how their internal trajectory representation changes across steps, and analytic coefficients that describe how denoiser errors affect the sampler's state update. By calibrating the first signal against the second on offline runs, we obtain a per-step score that upper-bounds how far the final trajectory can deviate when we reuse a cached denoiser output, and we treat this bound as an uncertainty budget that we can spend over the denoising process. Building on this insight, we present Muninn, a training-free caching wrapper that tracks this uncertainty budget during sampling and, at each diffusion step, chooses between reusing a cached denoiser output when the predicted deviation is small and recomputing the denoiser when it is not. Across standard benchmarks Muninn delivers up to 4.6x wall-clock speedups across several trajectory diffusion models by reducing denoiser evaluations, while preserving task performance and safety metrics. Muninn further certifies that cached rollouts remain within a specified distance of their full-compute counterparts, and we validate these gains in real-time closed-loop navigation and manipulation hardware deployments. Project page: https://github.com/gokulp01/Muninn.

---

## 论文详细总结（自动生成）

### 论文总结：Muninn: Your Trajectory Diffusion Model But Faster

#### 1. 论文的核心问题与整体含义
基于扩散模型（Diffusion Models）的轨迹规划器在处理多模态运动和复杂环境时表现卓越，但其核心缺陷在于**推理速度极慢**。由于需要进行数十次迭代去噪，这些模型难以满足机器人实时控制（如高频闭环控制）的需求。现有的加速方案通常涉及模型蒸馏、网络压缩或减少采样步数，但这往往需要昂贵的重新训练，且容易导致规划质量下降或碰撞风险增加。

本文提出了 **Muninn**，一种**无需训练（Training-free）**的通用缓存封装方案。其核心目标是在不改变模型权重、不牺牲轨迹质量的前提下，通过智能复用计算结果，使现有的轨迹扩散模型达到实时运行的速度。

#### 2. 论文提出的方法论
Muninn 的核心思想是利用扩散过程中的两个关键信号来决定是否可以“偷懒”复用之前的计算结果：

*   **核心思想：不确定性预算（Uncertainty Budget）**
    将采样过程视为一个消耗“偏差预算”的过程。如果复用旧的去噪输出对最终轨迹的影响在可控范围内，则跳过当前步的计算。
*   **关键技术细节**：
    1.  **廉价探针（Cheap Probe）**：在去噪器的早期层（Stem）提取中间表示。通过计算相邻步间表示的变化量（Score），评估轨迹生成的稳定性。
    2.  **解析灵敏度系数（Analytic Sensitivity Coefficients）**：基于采样器（如 DDPM/DDIM）的数学形式，推导出局部去噪误差如何随时间传播并放大到最终轨迹的解析界限（利用 Lipschitz 连续性）。
    3.  **共形校准（Conformal Calibration）**：利用少量离线数据，通过共形回归（Conformal Regression）建立“探针变化量”与“实际轨迹偏差”之间的统计映射，从而在运行时提供具有概率保证的误差上界。
*   **算法流程**：
    *   在每一步去噪时，先运行廉价探针计算当前步的评分。
    *   结合解析系数，预测如果复用缓存会产生的轨迹偏差。
    *   如果该偏差在剩余的“偏差预算”内，则复用缓存；否则，重新调用完整的去噪网络并更新缓存。

#### 3. 实验设计
论文进行了极其广泛的实验，涵盖了仿真和真实硬件场景：
*   **数据集/场景**：
    *   **离线强化学习 (D4RL)**：包括 MuJoCo 运动控制、Maze2D/AntMaze 导航、FrankaKitchen 操纵。
    *   **运动规划**：7 自由度机械臂在杂乱环境中的配置空间规划。
    *   **视觉运动策略**：RLBench、Meta-World 以及基于点云的 DP3 任务。
*   **Benchmark 与对比方法**：
    *   **基准模型**：Diffuser, Decision Diffuser, Diffusion-QL, AdaptDiff, CompDiff, DP3 等。
    *   **对比加速方法**：FewSteps（减少步数）、FixedSkip（固定步长跳过）、蒸馏方法（Distill-1step/Kstep）、模型压缩（SmallNet）以及学习停止策略（LearnedExit）。
*   **硬件部署**：SeaRobotics 无人船（ASV）、Crazyflie 无人机（UAV）、SO-ARM100 桌面机械臂。

#### 4. 资源与算力
*   **硬件环境**：实验主要在 **NVIDIA A10 GPU (24GB VRAM)** 上运行。
*   **算力消耗**：
    *   **Muninn 方案**：无需重新训练模型。校准过程仅需 4096 个上下文样本（约数十分钟即可完成）。
    *   **对比方案**：论文提到蒸馏或训练小模型需要大量的算力（图中显示部分任务需要 10 到 200 个 GPU 小时不等）。
*   **推理延迟**：在 A10 上，Muninn 将原本数百毫秒的规划延迟降低了 70%-80%。

#### 5. 实验数量与充分性
*   **实验规模**：每项任务均在 **150 个未见过的测试集数（Episodes）**上进行评估，并取平均值。
*   **充分性**：实验覆盖了从低维状态空间到高维视觉输入、从仿真到真机的多种模态。
*   **客观性与公平性**：论文采用了“配对随机性（Paired Randomness）”评估，即在相同初始噪声下对比 Muninn 和全量计算模型的轨迹偏差，确保了对比的严谨性。此外，还进行了详细的消融实验，验证了灵敏度系数和共形校准的必要性。

#### 6. 论文的主要结论与发现
*   **显著加速**：在保持任务成功率的前提下，实现了高达 **4.6 倍** 的墙钟时间加速，减少了高达 **7.7 倍** 的去噪器调用次数。
*   **性能保持**：在 D4RL 和操纵任务中，Muninn 的得分与全量计算模型几乎一致，远优于简单的固定跳步方案。
*   **难度自适应**：Muninn 会在复杂/危险区域（如狭窄通道）自动增加计算量，而在开阔/稳定区域增加缓存复用。
*   **安全性保证**：通过提供的偏差证书（Certificate），系统可以在偏差过大时触发“运行时升级（Escalation）”，如切换回全量计算或采取保守动作。

#### 7. 优点
*   **即插即用**：不需要修改原始模型架构，不需要重新训练，适用于几乎所有基于状态空间的扩散规划器。
*   **理论支撑**：结合了 Lipschitz 灵敏度分析和共形预测，提供了统计学上的轨迹偏差保证。
*   **高效性**：探针开销极低（仅占总计算量的 6%-8%），却能带来巨大的推理增益。
*   **硬件友好**：在嵌入式设备（如 Jetson Orin）上表现出良好的加速比。

#### 8. 不足与局限
*   **分布偏移风险**：如果部署环境与校准环境差异巨大（如全新的障碍物分布），共形预测给出的误差界限可能会失效。
*   **探针设计的工程性**：虽然方法通用，但针对不同架构（Transformer vs. U-Net）仍需手动指定探针位置（如哪一层作为 Stem）。
*   **不连续约束挑战**：在存在极度不连续约束（如极窄缝隙）的环境中，微小的轨迹偏差仍可能导致碰撞，而 Muninn 的偏差界限是基于范数的，可能无法完全捕捉这种几何上的临界状态。
*   **应用限制**：目前主要针对状态空间扩散，对于直接在像素空间进行全图像生成的扩散模型，其探针设计和误差传播分析会更加复杂。

（完）
