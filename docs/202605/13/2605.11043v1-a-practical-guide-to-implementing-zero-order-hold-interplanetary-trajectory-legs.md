---
title: A practical guide to implementing zero-order-hold interplanetary trajectory legs
title_zh: 实现零阶保持行星际轨道段的实践指南
authors: "Dario Izzo, Harry Holt, Giacomo Acciarini, Laurent Beauregard, Yuri Shimane"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.11043v1"
tags: ["query:q3"]
score: 8.0
evidence: 航天器轨迹优化与基准测试
tldr: 本研究探讨了航天器轨迹优化中零阶保持（ZOH）转录的实际应用，提出了一套无需特定调优即可在多种动力学环境下保持鲁棒性的设计原则。主要贡献包括：深入研究了前向-后向射击构造 ZOH_α；引入冗余四维节流参数化以消除弹道弧段的控制矩阵奇异性；采用 Softmax 时间网格编码解决段时长排序约束；并发布了包含 28 个问题的 TOPS 基准测试集，涵盖二体、三体及太阳帆等多种动力学模型。
selection_source: fresh_fetch
motivation: 旨在解决航天器轨迹优化中 ZOH 转录在不同动力学设置下缺乏鲁棒性且需要繁琐调优的问题。
method: 提出了包含前向-后向射击构造、冗余节流参数化和 Softmax 时间网格编码的综合设计框架。
result: 成功消除了控制矩阵奇异性并简化了时间网格约束，同时建立了一个涵盖多种动力学模型的开源基准测试集 TOPS。
conclusion: 该研究为实现稳健且通用的行星际轨迹优化提供了实用的实施指南和标准化的评估工具。
---

## 摘要
我们研究了用于航天器轨道优化的零阶保持（ZOH）转录方法的实际实现，并确定了一套设计原则，使其在广泛的动力学环境下具有鲁棒性，且无需针对特定问题进行调优。本文的贡献包括四个方面：(i) 对表示为 $\mathrm{ZOH}_α$ 的前向-后向打靶构造进行了深入研究；(ii) 提出了一种冗余四维节流阀参数化方法，消除了沿无动力弧段控制影响矩阵的奇异性；(iii) 提出了一种 softmax 时间网格编码，在保持完全可微性的同时，避免了对分段时长的排序约束；(iv) 发布了 TOPS 基准测试集（航天轨道优化问题集），该套件包含 28 个问题，涵盖了二体笛卡尔坐标、修正春分点根数、圆型限制性三体问题和太阳帆四种动力学模型，并设计为可随时间扩展。

## Abstract
We study the practical implementation of zero-order-hold (ZOH) transcriptions for spacecraft trajectory optimisation, identifying a set of design principles that render them robust across a broad class of dynamical settings without problem-specific tuning. The contributions are fourfold: (i) a thorough study of the forward--backward shooting construction, denoted $\mathrm{ZOH}_α$; (ii) a redundant four-dimensional throttle parameterization that eliminates the singularity of the control influence matrix along ballistic arcs; (iii) a softmax time-grid encoding that avoids ordering constraints on segment durations while preserving full differentiability; and (iv) the TOPS benchmark (Trajectory Optimisation Problems in Space), a suite of 28 problems spanning four dynamical models, two-body Cartesian, modified equinoctial elements, circular restricted three-body, and solar sailing, designed to be extended over time.

---

## 论文详细总结（自动生成）

这是一份关于论文《实现零阶保持行星际轨道段的实践指南》（*A practical guide to implementing zero-order-hold interplanetary trajectory legs*）的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：在航天器低推力轨道优化中，直接法（Direct Methods）常将控制输入参数化为分段常数，即零阶保持（ZOH）。虽然 ZOH 结构简单且符合燃料最优问题的“砰-砰”（Bang-Bang）控制特性，但现有实现往往依赖于特定问题的调优，且在处理无动力（弹道）弧段时存在数学奇异性。
*   **核心问题**：如何构建一个鲁棒、通用且无需针对特定任务进行繁琐调优的 ZOH 轨道优化框架？
*   **研究意义**：旨在将 ZOH 从一种简单的基准方法提升为一种系统化、竞争性强的计算方法，降低对专家经验初始化猜想的依赖。

### 2. 论文提出的方法论
论文提出了名为 **$\mathrm{ZOH}_\alpha$** 的框架，包含以下核心技术细节：
*   **前向-后向射击构造 ($\mathrm{ZOH}_\alpha$)**：将轨道分为前向（从初始点 $x_0$）和后向（从目标点 $x_f$）两部分，在中间点（由参数 $\alpha$ 决定）强制状态连续。这种结构比单向射击更稳定，且比多点射击更简洁。
*   **冗余四维节流参数化**：为了消除推力为零时控制影响矩阵的奇异性，将控制向量 $u$ 扩展为四维：一个标量节流阀 $\tau \in [0, 1]$ 和一个三维方向向量 $d$（满足 $\|d\| \leq 1$）。在动力学方程中，质量消耗仅与 $\tau$ 挂钩，从而在数学上保证了弹道弧段梯度的平滑性。
*   **Softmax 时间网格编码**：利用 Softmax 函数将无约束的权重向量映射为各段时长。这保证了每段时长严格大于零且总时长固定，同时消除了优化算法中对时间排序的显式不等式约束，保持了全流程的可微性。
*   **全微分与代理梯度**：利用现代 Taylor 积分器（Heyoka）计算精确梯度，并对行星历表（Ephemerides）采用基于开普勒动力学的代理梯度，以平衡计算效率与精度。

### 3. 实验设计
*   **TOPS 基准测试集**：论文发布了“航天轨道优化问题集”（TOPS），包含 28 个具有挑战性的案例。
*   **动力学模型**：涵盖了二体笛卡尔坐标（twobody）、修正春分点根数（mee）、圆型限制性三体问题（cr3bp）和太阳帆（ss）。
*   **对比维度**：
    *   **求解器**：对比了 IPOPT（内点法）和 SNOPT（序列二次规划法）。
    *   **网格类型**：均匀网格 vs. Softmax 变步长网格。
    *   **约束形式**：节流阀约束采用等式 vs. 不等式。
    *   **切分点**：$\alpha \in \{0, 0.5, 1\}$（纯后向、中点切分、纯前向）。

### 4. 资源与算力
*   **算力说明**：文中未明确提及具体的 GPU/CPU 硬件型号。
*   **计算规模**：实验运行了超过 **300,000 次** 求解器调用。
*   **软件工具**：使用了高性能 Taylor 积分器 **Heyoka**，并将所有代码开源在 GitLab 上，确保了结果的可重复性。

### 5. 实验数量与充分性
*   **实验规模**：针对每个问题配置，使用了 **128 个独立的随机初始猜想** 进行多起点优化。
*   **充分性**：实验覆盖了从简单的轨道转移到复杂的多圈、高偏心率及三体动力学场景。通过帕累托前沿（Pareto Front）分析 ERT（期望运行时间）与目标函数值（最终质量）的关系。
*   **客观性**：不剔除失败的运行记录，而是将其计入 ERT 指标，这种做法客观地评估了不同配置的鲁棒性。

### 6. 论文的主要结论与发现
*   **$\alpha=0.5$ 最优**：前向-后向结合的构造在所有动力学模型中均优于纯前向或纯后向射击，显著降低了梯度敏感度。
*   **求解器表现**：IPOPT 在处理随机初始化的“冷启动”问题上表现出与行业标准 SNOPT 相当甚至更优的鲁棒性，尤其是在经过良好缩放（Scaling）的情况下。
*   **Softmax 网格的优势**：在太阳帆问题中，Softmax 编码的变步长网格表现极佳，因为它允许优化器自动“收缩”不利于推进的轨道段。
*   **约束建议**：对于大多数低推力问题，将节流阀限制设为不等式约束（$\tau \leq 1$）比等式约束更有效。

### 7. 优点
*   **数学严谨性**：通过冗余参数化巧妙解决了控制矩阵在零推力点的奇异性问题。
*   **工程实用性**：提供了一套无需调优的参数建议（如 $\alpha=0.5$、不等式约束、Softmax 网格），极具参考价值。
*   **开源贡献**：TOPS 基准测试集填补了航天轨道优化领域缺乏标准化、多动力学模型测试集的空白。

### 8. 不足与局限
*   **方法局限**：研究仅限于 ZOH 转录，未与高阶配点法（如 Hermite-Simpson 或伪谱法）在相同基准下进行直接精度对比。
*   **应用范围**：主要针对行星际低推力轨道，对于极短时间尺度或极高精度的近地轨道摄动分析，其表现尚未验证。
*   **计算开销**：虽然 Softmax 增加了灵活性，但也引入了额外的非线性，对于极大规模的分段（如 $N > 100$），其收敛速度可能受到影响。

（完）
