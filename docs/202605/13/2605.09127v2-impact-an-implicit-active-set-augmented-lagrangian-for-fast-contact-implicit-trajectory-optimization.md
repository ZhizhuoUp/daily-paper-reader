---
title: "IMPACT: An Implicit Active-Set Augmented Lagrangian for Fast Contact-Implicit Trajectory Optimization"
title_zh: IMPACT：一种用于快速接触隐式轨迹优化的隐式有效集增广拉格朗日方法
authors: "Jiayun Li, Dejian Gong, Georgia Chalvatzaki"
date: 2026-05-09
pdf: "https://arxiv.org/pdf/2605.09127v2"
tags: ["query:profile-1"]
score: 9.0
evidence: 用于机器人操作的快速接触隐式轨迹优化
tldr: IMPACT是一种针对接触隐式轨迹优化（CITO）的高效增广拉格朗日算法。它通过在优化过程中动态识别隐式接触模式分支，有效解决了互补约束带来的数值病态问题。该方法在CITO和CI-MPC基准测试中表现优异，相比现有强基准算法实现了显著加速（平均13.8倍），并成功应用于复杂操纵任务的实时控制和真机实验。
selection_source: fresh_fetch
motivation: 现有的接触隐式轨迹优化方法受限于互补约束导致的数值病态问题，亟需更高效、更具原则性的求解器以提升其在复杂机器人任务中的适用性。
method: 提出一种名为IMPACT的增广拉格朗日方法，通过在优化迭代中动态且隐式地识别接触模式分支来处理互补约束并保证平稳性。
result: 在基准测试中实现了2.9倍至70倍的加速，并在灵巧操纵仿真和真机推物任务中展现了比基准方法更高的控制质量。
conclusion: IMPACT显著提升了接触密集型任务的轨迹优化效率和鲁棒性，为实现高性能的实时接触隐式模型预测控制提供了有力支持。
---

## 摘要
接触隐式轨迹优化（CITO）作为一种用于富接触机器人任务规划与控制的统一框架，正受到越来越多的关注。最近的方法在操纵和运动任务中展示了良好的效果，且无需预设接触模式计划。众所周知，其底层的带互补约束的数学规划（MPCCs）在数值上仍然是病态的，而针对 CITO 的系统化、可扩展的求解策略仍是一个活跃的研究领域。因此，能够处理接触约束的更高效且更有原则的求解器对于扩大 CITO 的适用性至关重要。在这项工作中，我们开发了一种用于 CITO 的增广拉格朗日方法，以解决具有平稳性保证的基于 MPCC 的 CITO 问题。该方法可以被解释为在轨迹优化（TO）迭代过程中实时识别隐式接触模式分支；我们将这种方法称为 IMPACT（隐式接触有效集轨迹优化）。我们提供了一个针对轨迹优化工作负载定制的高效 C++ 实现，并在开源 CITO 和接触隐式模型预测控制（CI-MPC）基准测试中对其进行了评估。在 CITO 上，IMPACT 相比强基准方法实现了 2.9 倍至 70 倍的加速（几何平均值为 13.8 倍）。在 CI-MPC 上，我们在仿真中展示了其在灵巧操纵任务的富接触轨迹上具有更高的控制质量。最后，我们在真实机器人硬件上的 T 形物体推取任务中演示了所提方法。

## Abstract
Contact-implicit trajectory optimization (CITO) has attracted growing attention as a unified framework for planning and control in contact-rich robotic tasks. Recent approaches have demonstrated promising results in manipulation and locomotion without requiring a prescribed contact-mode schedule. It is well known that the underlying mathematical programs with complementarity constraints (MPCCs) remain numerically ill-conditioned, and systematic, scalable solution strategies for CITO remain an active area of research. More efficient and principled solvers that can handle contact constraints are therefore essential to broaden the applicability of CITO. In this work, we develop an augmented-Lagrangian approach to CITO for solving MPCC-based CITO with stationarity guarantees. The method can be interpreted as identifying the implicit contact-mode branches on the fly during the trajectory optimization (TO) iterations; we call this approach IMPACT (IMPlicit contact ACtive-set Trajectory optimization). We provide an efficient C++ implementation tailored to trajectory-optimization workloads and evaluate it on the open-source CITO and contact-implicit model predictive control (CI-MPC) benchmarks. On CITO, IMPACT achieves 2.9x-70x speedups over strong baselines (geometric mean 13.8x). On CI-MPC, we show improved control quality for contact-rich trajectories on dexterous manipulation tasks in simulation. Finally, we demonstrate the proposed method on real robotic hardware on a T-shaped object pushing task.

---

## 论文详细总结（自动生成）

### IMPACT：一种用于快速接触隐式轨迹优化的隐式有效集增广拉格朗日方法

#### 1. 核心问题与整体含义
接触隐式轨迹优化（CITO）是机器人学中处理富接触任务（如灵巧操纵、足式运动）的统一框架。其核心挑战在于底层的**带互补约束的数学规划（MPCCs）**具有高度的非光滑性和非凸性，导致标准优化求解器在处理接触状态切换（接触/分离、滑动/粘滞）时数值表现脆弱、计算效率低下。论文的研究动机是开发一种既能保证数学上的平稳性（Stationarity），又能实现实时控制速度的高效求解器。

#### 2. 方法论
论文提出了 **IMPACT**（Implicit contact Active-set Trajectory optimization），其核心思想是将 MPCC 问题转化为增广拉格朗日（AuLa）框架下的子问题序列：
*   **垂直重构（Vertical Reformulation）**：引入松弛变量（Slack Variables），将非线性的互补约束转化为简单的变量间互补关系，从而将非线性项转移到光滑的等式约束中。
*   **受保护的增广拉格朗日（Safeguarded AuLa）外环**：通过更新乘子和惩罚参数来处理光滑约束，同时将互补约束作为硬约束保留在子问题中，避免了传统方法中乘子爆炸的问题。
*   **块坐标下降（BCD）内环求解器**：
    *   **X 块更新**：固定松弛变量，使用带线搜索的 Gauss-Newton 法更新轨迹变量（状态、控制、接触力）。
    *   **松弛变量块更新**：固定轨迹变量，利用互补约束的特殊结构，通过**闭式解（Closed-form）**直接映射出最优松弛变量。这种方式能隐式地在迭代中“发现”接触模式，无需预设接触序列。

#### 3. 实验设计
实验涵盖了从长时序规划到实时控制，再到硬件部署的多个维度：
*   **CITO 基准测试（CRISP 框架）**：包含 Push Box（推箱子）、Cart Transport（小车运输）、Push T（推 T 形块）三个长时序任务。对比方法包括 Scholtes 采样松弛法（SR）、平方惩罚法（PM）和 CRISP 求解器。
*   **CI-MPC 基准测试（Allegro Hand）**：在 MuJoCo 环境中对 17 种不同形状的物体进行灵巧操纵重定向。对比基准是基于平滑接触模型的 `cfree` 框架。
*   **真实硬件演示**：使用 Panda 机器人完成 Push-T 任务，验证算法在现实环境中的鲁棒性和重规划能力。

#### 4. 资源与算力
*   **硬件环境**：所有实验均在一台配备 **AMD Ryzen 9 7950X3D CPU** 的台式电脑上运行。
*   **GPU 使用**：明确指出**未使用 GPU 加速**。
*   **软件实现**：使用 C++ 编写，利用 CasADi 进行符号微分，Eigen 进行线性代数运算。在 CI-MPC 任务中实现了约 10Hz 的控制频率。

#### 5. 实验数量与充分性
*   **实验规模**：在 CITO 基准中，每个任务随机采样了 50 组目标/起始位置；在 Allegro Hand 任务中，对 17 种物体各进行了 10 次试验；硬件实验进行了 10 次试验。
*   **充分性与公平性**：实验对比了多种主流的 MPCC 处理策略（松弛、惩罚、专用求解器），并使用了统一的终止准则（如约束违反率 $< 10^{-5}$）。实验设计较为客观，涵盖了成功率、轨迹误差、迭代次数、运行时间及控制质量（平滑度、能耗）等多个指标。

#### 6 主要结论与发现
*   **显著加速**：在 CITO 任务中，IMPACT 相比强基准方法实现了 **2.9 倍至 70 倍**的加速（几何平均值 13.8 倍）。
*   **控制质量提升**：在灵巧操纵任务中，相比于基于平滑模型的方法，IMPACT 在保持相似成功率的同时，显著降低了控制方差、提高了轨迹平滑度并减少了控制能耗。
*   **理论保证**：证明了该算法在标准假设下能够收敛到 MPCC 的平稳点（M-stationarity）。

#### 7. 优点
*   **高效性**：通过 BCD 结构和闭式解更新，极大地降低了处理互补约束的计算开销。
*   **无需预设模式**：能够自动推断复杂的接触序列，适应性强。
*   **原则性强**：结合了现代增广拉格朗日理论，具有明确的收敛性保证，而非纯启发式调参。

#### 8. 不足与局限
*   **局部最优风险**：作为一种基于梯度的局部优化方法，在计算预算有限时（如 MPC 场景），容易陷入局部最优，且对初始值和接触成本函数（Contact-encouraging costs）有一定依赖。
*   **模式切换震荡**：在极短的计算时间内，可能会出现接触模式在相邻迭代间频繁切换的现象（尤其是在处理细长物体如 Stick 时）。
*   **应用限制**：目前主要针对广义坐标系下的动力学，对于极度复杂的非凸几何体接触，可能仍需更精细的碰撞检测支持。

（完）
