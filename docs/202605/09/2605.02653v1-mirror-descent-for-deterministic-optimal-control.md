---
title: Mirror Descent for Deterministic Optimal Control
title_zh: 确定性最优控制的镜像下降法
authors: "Ye Feng, Jianfeng Lu"
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.02653v1"
tags: ["query:q2"]
score: 8.0
evidence: 有限时界确定性最优控制的镜像下降法
tldr: 本研究针对有限时间确定性最优控制问题，提出了一种显式镜像下降方法。该方法受庞特里亚金极大值原理启发，通过求解状态和伴随方程，并利用Bregman散度正则化的哈密顿量一阶近似来更新控制变量。在全局平滑和镜像映射均匀凸的假设下，证明了代价函数的相对平滑性及能量耗散不等式。研究进一步给出了在凸性假设下的O(1/n)收敛率及正则化情况下的几何收敛率，并通过数值算例验证了其在处理高维非线性问题中的有效性。
selection_source: fresh_fetch
motivation: 旨在为有限时间确定性最优控制问题开发一种高效且具有理论收敛保障的显式优化算法。
method: 结合庞特里亚金极大值原理，通过在每轮迭代中求解状态与伴随方程，并利用Bregman散度正则化的一阶哈密顿量近似来更新控制策略。
result: 证明了算法在一般凸情形下具有O(1/n)的收敛速度，而在正正则化参数下可达到几何级收敛。
conclusion: 该镜像下降法为解决线性二次、退化凸及高维非线性最优控制问题提供了一种稳健且理论完备的数值框架。
---

## 摘要
我们研究了一种用于有限时域确定性最优控制问题的显式镜像下降法。该方法的灵感源自庞特里亚金极大值原理（Pontryagin's maximum principle）：在每次迭代中，通过求解状态方程和伴随方程，并最大化受 Bregman 散度惩罚的正规化哈密顿量的一阶近似来更新控制变量。在欧几里得情形下，该更新简化为控制变量上的投影梯度步。在全局光滑性假设和镜像映射的均匀凸性条件下，我们证明了代价泛函的相对光滑性估计，并推导出了在步长足够小时的能量耗散不等式。在未正规化哈密顿量的额外凹性假设和终端代价的凸性条件下，我们建立了正规化目标函数的相对凸性。这些估计在未正规化的凸情形下产生了 $O(1/n)$ 的收敛率，而在控制正规化参数为正时则产生了几何收敛率。数值算例展示了该方法在线性二次、退化凸以及非线性高维场景下的表现。

## Abstract
We study an explicit mirror-descent method for finite-horizon deterministic optimal control problems. The method is motivated by Pontryagin's maximum principle: at each iteration, one solves the state and adjoint equations and updates the control by maximizing a first-order approximation of the regularized Hamiltonian penalized by a Bregman divergence. In the Euclidean case, the update reduces to a projected gradient step in the control variable. Under global smoothness assumptions and uniform convexity of the mirror map, we prove a relative smoothness estimate for the cost functional and derive an energy dissipation inequality for sufficiently small step sizes. Under an additional concavity assumption on the unregularized Hamiltonian and convexity of the terminal cost, we establish relative convexity of the regularized objective. These estimates yield an $O(1/n)$ convergence rate in the unregularized convex case and a geometric rate when the control regularization parameter is positive. Numerical examples illustrate the behavior of the method in linear-quadratic, degenerate convex, and nonlinear high-dimensional settings.

---

## 论文详细总结（自动生成）

这篇论文由杜克大学的 Ye Feng 和 Jianfeng Lu 撰写，探讨了确定性最优控制问题的数值求解方法。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何为有限时界的确定性最优控制问题开发一种具有理论收敛保障的显式迭代算法。
*   **研究动机**：传统的最优控制求解方法（如逐次逼近法 MSA）虽然在实践中广泛应用，但其显式更新规则的系统性收敛分析仍不完善。
*   **整体含义**：论文通过引入优化领域中的**镜像下降（Mirror Descent）**框架，结合**庞特里亚金极大值原理（PMP）**，为确定性控制问题提供了一个统一的变分分析框架，并证明了算法在不同正则化条件下的收敛速率。

### 2. 方法论：核心思想与算法流程
*   **核心思想**：将控制更新步骤视为在控制变量空间上，对正则化哈密顿量的一阶近似进行最大化，并辅以 **Bregman 散度**作为惩罚项。
*   **算法流程（Algorithm 1）**：
    1.  **状态方程求解**：给定当前控制 $u^n$，前向求解状态方程 $\dot{x}^n_t = b(t, x^n_t, u^n_t)$。
    2.  **伴随方程求解**：基于 $x^n$ 和 $u^n$，后向求解伴随方程 $\dot{p}^n_t = -\nabla_x H_0$。
    3.  **镜像更新**：在每个时间点 $t$，通过求解一个局部优化问题来更新控制 $u^{n+1}$。该问题旨在最大化哈密顿量的线性近似，同时限制其偏离当前控制的 Bregman 距离。
*   **关键技术细节**：
    *   引入了**相对光滑性（Relative Smoothness）**和**相对凸性（Relative Convexity）**的概念。
    *   证明了在正则化参数 $\tau > 0$ 时，算法具有几何收敛率（线性收敛）；在 $\tau = 0$ 时，具有 $O(1/n)$ 的次线性收敛率。

### 3. 实验设计
论文设计了三个数值算例来验证理论结果：
*   **一维线性二次（LQ）问题**：作为基准测试（Sanity Check），利用其解析解验证算法的几何收敛特性。
*   **一维四次终端代价问题**：专门用于展示正则化项 $\tau$ 对收敛性质的定性影响（对比 $\tau=0$ 的次线性与 $\tau>0$ 的几何收敛）。
*   **高维非线性耦合动力系统**：状态维度 $d \in \{5, 10, 20\}$，系统包含正弦非线性项，用于测试算法在复杂耦合和高维环境下的鲁棒性与维度依赖性。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件配置（如 GPU 型号或数量）。
*   **实现细节**：实验主要涉及常微分方程（ODE）的数值积分和逐点优化更新，通常在标准 CPU 环境下即可高效运行。时间步长离散化为 $N_t = 500$。

### 5. 实验数量与充分性
*   **实验数量**：共进行了 3 组主要实验，每组实验内部包含了不同参数（如正则化权重 $\tau$、维度 $d$）的对比。
*   **充分性评价**：实验设计较为**充分且具有针对性**。LQ 算例验证了基础正确性，四次项算例验证了收敛率理论的转折点，高维算例则证明了方法在非线性复杂系统中的实用性。实验过程客观，对比了理论预测值与数值观测值。

### 6. 论文的主要结论与发现
*   **收敛性保障**：在镜像映射均匀凸且哈密顿量满足一定光滑性假设下，显式镜像下降法是稳定的。
*   **正则化的重要性**：正则化参数 $\tau$ 不仅能平滑问题，还能将收敛速率从次线性提升至几何级。
*   **高维适用性**：实验表明该方法能有效处理高维非线性耦合系统，尽管维度增加会增加达到相同精度所需的迭代次数，但收敛趋势保持一致。

### 7. 优点
*   **理论完备**：为 PMP 基础的迭代法提供了严谨的收敛率证明，填补了确定性控制领域的部分理论空白。
*   **显式更新**：相比隐式方法，该算法计算开销更低，易于在大规模问题中实现。
*   **框架统一**：利用 Bregman 散度，该方法可以自然地扩展到非欧几里得几何的控制约束集合。

### 8. 不足与局限
*   **假设限制**：理论分析依赖于全局光滑性和哈密顿量的某种凸/凹性假设，这在某些高度非线性的奇异控制问题中可能不成立。
*   **确定性局限**：虽然论文提到了随机控制的相关工作，但本研究仅限于确定性系统，未讨论随机扰动对显式更新稳定性的影响。
*   **步长敏感性**：算法的稳定性依赖于步长参数 $\lambda$ 的选择，而理论上的 $L$（光滑常数）在实际复杂问题中往往难以精确估计。

（完）
