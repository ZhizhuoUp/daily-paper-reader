---
title: "STARRY: Spatial-Temporal Action-Centric World Modeling for Robotic Manipulation"
title_zh: STARRY：面向机器人操作的以动作为中心的时空世界建模
authors: "Yuxuan Tian, Yurun Jin, Bin Yu, Yukun Shi, Hao Wu, Chi Harold Liu, Kai Chen, Cong Huang"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.26848v2"
tags: ["query:profile-1"]
score: 8.0
evidence: 机器人操作和动作生成的时空世界建模
tldr: 针对机器人操作中时空推理与动作执行耦合不足的问题，本文提出STARRY。该模型通过统一的扩散过程联合去噪未来时空潜变量与动作，并引入几何感知选择性注意力调制（GASAM）将深度与末端执行器几何信息转化为权重，实现了2D视觉与3D控制的有效桥接。在RoboTwin 2.0及真实世界实验中表现优异，显著提升了复杂任务的成功率。
selection_source: fresh_fetch
motivation: 现有的视觉-语言-动作策略在预测表示与动作执行之间耦合较弱，导致在需要精确时空协调的任务中表现不佳。
method: 通过统一扩散过程联合去噪未来时空潜变量与动作，并引入GASAM机制将3D几何约束融入2D视觉特征的注意力调制中。
result: "在RoboTwin 2.0仿真中取得超过93%的成功率，并在真实世界实验中将基准模型的成功率从42.5%显著提升至70.8%。"
conclusion: 以动作为中心的时空世界建模对于处理具有高度空间和时间要求的机器人操作任务至关重要且非常有效。
---

## 摘要
机器人操作需要对未来的时空交互和几何约束进行推理，然而现有的视觉-语言-动作（VLA）策略往往使预测表示与动作执行之间的耦合较弱，导致在需要精确时空协调的任务中出现失败。我们提出了 STARRY，这是一种增强了世界模型的动作生成策略，通过统一的扩散过程共同去噪未来的时空潜变量和动作，从而实现时空预测与动作生成的对齐。为了桥接 2D 视觉标记（tokens）与 3D 度量控制，STARRY 引入了几何感知选择性注意力调制（GASAM），该机制将预测的深度和末端执行器几何形状转换为与标记对齐的权重，用于选择性动作注意力调制。在 RoboTwin 2.0 平台上，STARRY 在 50 个双臂任务的清洁（Clean）和随机（Randomized）设置下分别实现了 93.82% 和 93.30% 的平均成功率。真实世界实验表明，与 $π_{0.5}$ 相比，STARRY 将平均成功率从 42.5% 提升至 70.8%。这些结果证明了以动作为中心的时空世界建模在处理对空间和时间要求极高的机器人操作任务中的有效性。

## Abstract
Robotic manipulation requires reasoning about future spatial-temporal interactions and geometric constraints, yet existing Vision-Language-Action (VLA) policies often leave predictive representation weakly coupled with action execution, causing failures in tasks requiring precise spatial-temporal coordination. We propose STARRY, a world-model-enhanced action-generation policy that aligns spatial-temporal prediction and action generation by jointly denoising future spatial-temporal latents and actions through a unified diffusion process. To bridge 2D visual tokens and 3D metric control, STARRY introduces Geometry-Aware Selective Attention Modulation (GASAM), which converts predicted depth and end-effector geometry into token-aligned weights for selective action-attention modulation. On RoboTwin 2.0, STARRY achieves 93.82% / 93.30% average success under Clean and Randomized settings across 50 bimanual tasks. Real-world experiments show that STARRY improves average success from 42.5% to 70.8% compared with $π_{0.5}$. These results demonstrate the effectiveness of action-centric spatial-temporal world modeling for spatially and temporally demanding robotic manipulation.

---

## 论文详细总结（自动生成）

### STARRY: 面向机器人操作的以动作为中心的时空世界建模 论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
现有的视觉-语言-动作（VLA）策略在处理复杂的机器人操作（如挂杯子、双手交接）时面临两大挑战：
*   **反应式控制局限：** 大多数模型仅依赖当前或短期历史观测，缺乏对未来交互状态（如物体几何、接触区域）的显式建模。
*   **预测与控制脱节：** 现有的世界模型虽然能预测未来视频，但其表示往往优化于视觉一致性而非动作相关性，且缺乏 3D 几何约束，导致 2D 视觉标记与 3D 物理控制之间存在鸿沟。
**STARRY** 的提出旨在通过“以动作为中心”且“基于几何接地”的世界模型，将未来时空预测与动作生成深度耦合。

#### 2. 方法论：核心思想与关键技术
STARRY 的核心思想是在统一的扩散过程中联合建模未来时空动态和动作序列。
*   **四模块架构：**
    1.  **理解专家（Understanding Expert）：** 提供视觉-语言语义基础。
    2.  **时空（ST）世界模型：** 预测整合了外观、运动（末端轨迹）和几何的未来时空潜变量。
    3.  **几何专家（Geometry Expert）：** 预测未来的深度图和末端执行器位置。
    4.  **动作专家（Action Expert）：** 生成最终动作。
*   **关键技术 - GASAM（几何感知选择性注意力调制）：**
    *   **流程：** 几何专家预测未来深度 $\hat{D}$ 和末端位置 $\hat{p}$；计算 3D 空间中各点到末端的距离；将距离转换为权重 $w$。
    *   **调制：** 使用这些权重对动作分支的注意力机制进行调制，使模型在生成动作时能“聚焦”于几何关键区域（如把手、开口）。
*   **联合扩散过程：** 通过扩散模型同时对未来时空潜变量 $z$ 和动作序列 $a$ 进行去噪，确保预测信息直接服务于动作生成。

#### 3. 实验设计
*   **仿真环境：** 使用 **RoboTwin 2.0** 基准测试，包含 50 个双臂操作任务，设置了“清洁（Clean）”和“随机（Randomized）”两种模式。
*   **对比方法：** 包括 $\pi_{0.5}$、$\pi_0$、X-VLA、Motus、LingBot-VA 和 GO-1 等前沿 VLA 模型。
*   **真实世界实验：** 在 ARX R5 双臂机器人平台上进行，包含“递送蔬菜”、“整理房间”、“清洗奶瓶”三个代表性双阶段任务。
*   **消融实验：** 验证了时空建模（ST Modeling）和 GASAM 机制各自对性能的贡献。

#### 4. 资源与算力
*   **硬件：** 使用配备 **8 张 NVIDIA Tesla A100-80G GPU** 的计算节点。
*   **训练时长：** 在上述配置下，一个典型的微调运行（Batch Size 256，40k 步）大约需要 **一周时间**。
*   **预训练：** 大规模预训练阶段使用了相同类型的 GPU 节点，并根据数据规模扩展了节点数量。

#### 5. 实验数量与充分性
*   **规模：** 仿真实验涵盖了 50 种不同类型的任务，每种任务在不同设置下进行了大量测试，实验规模较大。
*   **多样性：** 包含了从简单的点击到复杂的双臂协作和几何敏感型任务（如挂杯子）。
*   **客观性：** 采用了统一的基准测试集，并与多种 SOTA 方法在相同步数和 Batch Size 下进行对比，实验设计较为公平、客观。消融实验清晰地展示了各组件的增益。

#### 6. 主要结论与发现
*   **性能领先：** STARRY 在 RoboTwin 2.0 上达到了约 93.8% 的平均成功率，显著优于现有模型。
*   **几何引导的重要性：** 在“挂杯子”等对局部几何敏感的任务中，STARRY 的表现比次优模型高出 30% 以上，证明了 GASAM 的有效性。
*   **真实世界鲁棒性：** 在真实场景中，STARRY 的成功率（70.8%）远高于 $\pi_{0.5}$（42.5%），尤其在多步骤任务的后期阶段表现更稳定。
*   **时空耦合优势：** 实验证明，单纯预测未来外观是不够的，显式的时空潜变量建模能提供更强的物理约束。

#### 7. 优点
*   **创新的 GASAM 机制：** 巧妙地解决了 2D 视觉特征与 3D 物理度量控制之间的对齐问题。
*   **动作中心化：** 不同于传统的“先预测再控制”，STARRY 通过联合扩散让预测与动作生成在同一特征空间内演化。
*   **分层数据策略：** 提出了一套从互联网视频到目标机器人数据的六级分层训练方案，提升了模型的泛化能力。

#### 8. 不足与局限
*   **几何依赖性：** GASAM 的效果高度依赖于深度预测的准确性，在极端光照或透明物体等深度感知困难的场景下，性能可能受限。
*   **计算成本：** 由于引入了大型视频和视觉语言骨干网络，其训练和推理的计算开销高于轻量级的反应式策略。
*   **任务覆盖面：** 虽然在双臂任务上表现优异，但尚未在更广泛的移动操作或更开放的环境中进行大规模验证。

（完）
