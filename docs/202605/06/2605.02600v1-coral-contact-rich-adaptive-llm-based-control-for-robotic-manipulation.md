---
title: "CoRAL: Contact-Rich Adaptive LLM-based Control for Robotic Manipulation"
title_zh: CoRAL：基于大语言模型的机器人操作丰富接触自适应控制
authors: "Berk Çiçek, Mert K. Er, Özgür S. Öğüz"
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.02600v1"
tags: ["query:profile-1"]
score: 8.0
evidence: 基于大语言模型的采样运动规划代价函数设计，用于机器人操作
tldr: 针对大语言模型在处理复杂接触机器人任务时缺乏物理感知和自适应能力的问题，本文提出CoRAL框架。该框架将高层推理与底层控制解耦，利用LLM作为代价函数设计者来驱动采样运动规划器（MPPI）。通过神经符号自适应环路，结合VLM的语义先验与在线系统辨识，实现物理参数的实时修正，并利用检索记忆单元复用成功策略。实验证明，CoRAL在复杂接触任务中表现优异，显著提升了泛化能力和成功率。
selection_source: fresh_fetch
motivation: 旨在解决大语言模型在接触密集型机器人操作中缺乏物理接地和实时自适应控制能力的问题。
method: 提出一种解耦架构，由LLM设计代价函数引导MPPI规划器，并结合VLM先验与在线辨识进行物理参数修正。
result: "在模拟和真实环境的复杂接触任务中，CoRAL比现有基准模型的成功率平均提升了50%以上。"
conclusion: 通过将高层语义推理与底层反应式执行有效结合，CoRAL成功弥合了LLM推理延迟与动态接触控制需求之间的鸿沟。
---

## 摘要
虽然大语言模型 (LLMs) 和视觉语言模型 (VLMs) 在高层推理和语义理解方面表现出卓越的能力，但由于缺乏明确的物理基础且无法执行自适应控制，将它们直接应用于丰富接触 (contact-rich) 的操作任务仍然是一个挑战。为了弥补这一差距，我们提出了 CoRAL (Contact-Rich Adaptive LLM-based control)，这是一个模块化框架，通过将高层推理与底层控制解耦来实现零样本规划。与黑盒策略不同，CoRAL 不将 LLM 用作直接控制器，而是作为代价设计器 (cost designers)，为基于采样的运动规划器 (MPPI) 合成上下文感知的目标函数。为了解决视觉数据中物理参数的歧义，我们引入了一个神经符号自适应循环：VLM 提供环境动力学的语义先验（如质量和摩擦力估计），随后通过在线系统辨识进行实时显式细化，同时 LLM 根据交互反馈迭代调节代价函数结构以纠正策略错误。此外，基于检索的记忆单元允许系统在循环任务中重用成功的策略。这种分层架构通过将高层语义推理与反应式执行解耦，确保了实时控制的稳定性，有效地弥合了慢速 LLM 推理与动态接触需求之间的差距。我们在仿真和真实硬件上验证了 CoRAL，涵盖了具有挑战性的新颖任务，例如利用外部接触将物体靠墙翻转。实验表明，CoRAL 在未见的丰富接触场景中平均成功率提升了 50% 以上，优于最先进的 VLA 和基于基础模型的规划器基准，并通过其自适应物理理解有效地处理了从仿真到现实 (sim-to-real) 的差距。

## Abstract
While Large Language Models (LLMs) and Vision-Language Models (VLMs) demonstrate remarkable capabilities in high-level reasoning and semantic understanding, applying them directly to contact-rich manipulation remains a challenge due to their lack of explicit physical grounding and inability to perform adaptive control. To bridge this gap, we propose CoRAL (Contact-Rich Adaptive LLM-based control), a modular framework that enables zero-shot planning by decoupling high-level reasoning from low-level control. Unlike black-box policies, CoRAL uses LLMs not as direct controllers, but as cost designers that synthesize context-aware objective functions for a sampling-based motion planner (MPPI). To address the ambiguity of physical parameters in visual data, we introduce a neuro-symbolic adaptation loop: a VLM provides semantic priors for environmental dynamics, such as mass and friction estimates, which are then explicitly refined in real time via online system identification, while the LLM iteratively modulates the cost-function structure to correct strategic errors based on interaction feedback. Furthermore, a retrieval-based memory unit allows the system to reuse successful strategies across recurrent tasks. This hierarchical architecture ensures real-time control stability by decoupling high-level semantic reasoning from reactive execution, effectively bridging the gap between slow LLM inference and dynamic contact requirements. We validate CoRAL on both simulation and real-world hardware across challenging and novel tasks, such as flipping objects against walls by leveraging extrinsic contacts. Experiments demonstrate that CoRAL outperforms state-of-the-art VLA and foundation-model-based planner baselines by boosting success rates over 50% on average in unseen contact-rich scenarios, effectively handling sim-to-real gaps through its adaptive physical understanding.

---

## 论文详细总结（自动生成）

这是一份关于论文《CoRAL: Contact-Rich Adaptive LLM-based Control for Robotic Manipulation》的结构化深入分析报告：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的视觉-语言-动作（VLA）模型在处理“丰富接触”（Contact-Rich）任务（如利用墙壁翻转物体、精确推挤等）时表现不佳。这些任务不仅需要几何避障，还需要对力、摩擦力和质量等物理参数进行精确调节。
*   **研究动机**：
    *   **缺乏物理接地**：端到端模型难以理解复杂的接触动力学。
    *   **数据依赖性**：传统方法需要大量示教数据，泛化性差。
    *   **推理延迟**：大语言模型（LLM）推理速度慢，无法直接进行高频闭环控制。
*   **整体含义**：提出一种名为 **CoRAL** 的模块化框架，通过将高层语义推理（慢速）与底层反应式控制（快速）解耦，实现复杂接触任务的零样本（Zero-shot）规划与自适应控制。

### 2. 论文提出的方法论
CoRAL 采用神经符号（Neuro-symbolic）架构，核心流程如下：
*   **感知与世界模型初始化**：
    *   使用 **FoundationPose** 进行 6-DoF 物体位姿跟踪。
    *   利用 **VLM (GPT-4o)** 根据视觉信息推断物理先验（如质量、摩擦系数），构建“模拟规划世界”。
*   **LLM 驱动的任务表述**：
    *   **代价函数设计**：LLM 不直接输出动作，而是编写 Python 代码定义 **MPPI（模型预测路径积分）** 控制器的代价函数（Cost Function）。
    *   **接触策略生成**：LLM 确定物体上的候选接触区域，缩小搜索空间。
*   **分层闭环控制**：
    *   **内环（10Hz-1kHz）**：MPPI 控制器在模拟世界中进行并行采样，寻找最优轨迹，并结合阻抗控制确保物理交互的稳定性。
    *   **外环（~1Hz）**：当任务失败时，LLM 作为“诊断师”分析交互历史，在线修正物理参数（系统辨识）或重写代价函数。
*   **记忆单元**：基于 RAG（检索增强生成）存储成功经验，以便在相似任务中快速复用策略。

### 3. 实验设计
*   **实验场景**：
    *   **仿真环境**：基于 RoboSuite 和 MuJoCo 引擎。
    *   **任务集**：包含 6 类挑战性任务：T1.推挤并抓取切菜板；T2.抓取盒子；T3.杂乱环境抓取；T4.恒力推挤；T5.翻转盒子；T6.利用墙壁翻转物体。
*   **Benchmark 与对比方法**：
    *   **SOTA VLA 模型**：OpenVLA-OFT, $\pi_{0.5}$。
    *   **基于规划的方法**：L2R (Language-to-Rewards)。
    *   **人类专家基准**：专家设计的单阶段代价函数和有限状态机（FSM）。
*   **消融实验**：测试了无位姿跟踪、无记忆单元、无在线细化、统一 VLM（不解耦）等变体。
*   **真实世界验证**：在 Franka Emika Panda 机器人上进行零样本迁移测试。

### 4. 资源与算力
*   **硬件配置**：
    *   **CPU**：Intel Core i9-13900K。
    *   **内存**：64 GB RAM。
    *   **GPU**：单张 **NVIDIA RTX 4060 Ti**。
*   **算力细节**：
    *   MPPI 在 CPU 上并行模拟 256 条轨迹，规划时界（Horizon）为 32 步。
    *   LLM 和 VLM 调用通过 GPT-4o API 实现。
    *   论文未提及具体的训练时长，因为该方法主要基于预训练模型进行零样本推理，无需针对特定任务训练。

### 5. 实验数量与充分性
*   **实验规模**：每个任务进行 10 次随机初始化的试验（包括物体位姿、质量、摩擦力的随机化）。
*   **充分性评价**：
    *   **对比全面**：对比了当前最强的端到端模型（OpenVLA）和代码生成类方法（L2R）。
    *   **消融深入**：5 组消融实验清晰地证明了模块化解耦和在线自适应的必要性。
    *   **跨模态验证**：涵盖了从仿真到现实的迁移，证明了系统的鲁棒性。
    *   **客观性**：通过引入人类专家基准，明确了系统性能的上限。

### 6. 论文的主要结论与发现
*   **性能飞跃**：CoRAL 在复杂接触任务中的成功率比现有 VLA 模型平均高出 **50% 以上**。
*   **解耦的必要性**：将感知（VLM）、推理（LLM）与执行（MPPI）分离，是处理动态接触的关键。直接让 VLM 输出位姿或动作会导致灾难性失败。
*   **在线适配的重要性**：通过 LLM 进行在线系统辨识（修正物理参数），可以有效弥合 Sim-to-Real 的差距。
*   **策略引导**：LLM 提供的符号化接触策略能将计算量巨大的搜索问题转化为可解的优化问题。

### 7. 优点
*   **零样本能力**：无需任何目标任务的示教数据即可执行复杂操作。
*   **高可解释性**：LLM 生成的代码和诊断日志使系统决策过程透明，方便调试。
*   **自适应强**：能够根据物理反馈实时修正对环境的认知（如发现物体比预想的重）。
*   **模块化**：各组件（位姿跟踪、LLM、控制器）可独立升级。

### 8. 不足与局限
*   **依赖内部模型**：系统的表现高度依赖于模拟规划世界的准确性，如果初始视觉估计偏差过大，可能导致规划失败。
*   **推理延迟**：虽然控制是实时的，但 LLM 的策略修正（外环）仍有秒级延迟，不适合极高动态的突发情况。
*   **幻觉风险**：VLM 在估计物理参数时仍可能产生幻觉，虽然有在线修正机制，但会增加尝试次数。
*   **任务复杂度限制**：目前主要针对刚体操作，对于柔性物体或极度精细的装配任务尚未验证。

（完）
