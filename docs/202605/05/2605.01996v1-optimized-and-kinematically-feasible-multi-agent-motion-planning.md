---
title: Optimized and kinematically feasible multi-agent motion planning
title_zh: 优化且运动学可行的多智能体运动规划
authors: "Anja Hellander, Kristoffer Bergman, Daniel Axehill"
date: 2026-05-03
pdf: "https://arxiv.org/pdf/2605.01996v1"
tags: ["query:q3"]
score: 9.0
evidence: 用于运动学可行运动规划的多阶段最优控制问题
tldr: 针对多智能体运动规划（MAMP）问题，本文提出了一种两阶段优化框架。首先利用CBS或PBS等算法寻找初始可行解，随后通过多阶段最优控制问题（OCP）对解进行优化，以确保运动学可行性并提升质量。研究还提出了一种优化运动原语生成的方法，并在牵引车-挂车系统上进行了验证，展示了该框架在复杂动力学系统中的有效性。
selection_source: fresh_fetch
motivation: 旨在解决多智能体系统中寻找既满足运动学约束又具有最优性的路径规划难题。
method: 采用“先搜索后优化”的策略，结合CBS/PBS搜索初始解，并利用热启动的最优控制问题进行轨迹精细化。
result: 在牵引车-挂车系统的实验中，基于格点的规划器优于SIPP-IP，且CBS在成功率和运行时间上表现优于PBS。
conclusion: 该两阶段框架能有效提升多智能体运动规划的质量，且CBS更适合处理具有复杂约束的牵引车系统。
---

## 摘要
多智能体运动规划 (MAMP) 是多智能体自主系统中的一个重要问题。在这项工作中，我们提出了一种两步法，用于寻找 MAMP 问题的优化且运动学可行的解。第一步利用冲突搜索 (CBS) 或基于优先级的搜索 (PBS) 等前沿方法寻找初始可行解；第二步是改进步骤，通过求解多阶段最优控制问题 (OCP) 来优化解，其中初始解被用于热启动求解器。我们还提出了一种在运动基元持续时间均为同一采样时间倍数的约束下，以优化方式生成运动基元的方法。我们在牵引车-挂车系统的 MAMP 问题上评估了所提出的框架。我们扩展了带区间投影的安全区间路径规划 (SIPP-IP) 算法，使其能够处理更通用的代价函数和更大的智能体，但实验结果表明，对于牵引车-挂车系统，简单的基于格网的规划器由于碰撞检查更少保守性而表现更好。我们的实验还表明，CBS 在该系统中的表现优于 PBS，因为它在有障碍物的环境中实现了更高的成功率，且平均运行时间更短，尽管在改进步骤之后，两种规划器得到的解质量相近。

## Abstract
Multi-agent motion planning (MAMP) is an important problem for autonomous systems with multiple agents. In this work we propose a two-step method for finding optimized and kinematically feasible solutions to MAMP problems. The first step finds an initial feasible solution using state-of-the-art methods such as conflict-based search (CBS) or priority-based search (PBS), and the second step is an improvement step which improves the solution by solving a multi-phase optimal control problem (OCP) where the initial solution is used to warm-start the solver. We also propose a method for generating motion primitives in an optimized way under the constraint that the primitive durations are all multiples of the same sample time.   We evaluate our proposed framework on a MAMP problem for tractor-trailer systems. We extend the safe interval path planning with interval projections (SIPP-IP) algorithm so it can handle more general cost functions and larger agents, but our results show that for the tractor-trailer system a simple lattice-based planner performs better due to less conservative collision checks. Our experiments also indicate that CBS performs better than PBS for this system as it achieves a higher success rate in environments with obstacles and had a lower average runtime, although both planners achieve solutions of similar quality after the improvement step.

---

## 论文详细总结（自动生成）

这篇论文《Optimized and kinematically feasible multi-agent motion planning》由瑞典林雪平大学和萨博公司（Saab AB）的研究人员撰写，旨在解决复杂动力学约束下的多智能体运动规划问题。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在包含障碍物的复杂环境中，为多个具有复杂运动学约束（如牵引车-挂车系统）的智能体规划出既**运动学可行**又**局部最优**且**无碰撞**的轨迹。
*   **研究动机**：
    *   传统的多智能体路径规划（MAPF）通常假设智能体在网格上移动，忽略了车辆的动力学限制。
    *   现有的运动学规划方法（如 SIPP-IP）在处理非圆形、长条形智能体（如卡车）时过于保守，导致解的质量不高或无法找到可行解。
    *   直接求解大规模非线性最优控制问题（OCP）计算量巨大且极度依赖初始值。

### 2. 方法论
论文提出了一个**两阶段规划框架**：
*   **第一阶段：寻找初始可行解**
    *   使用**冲突搜索（CBS）**或**基于优先级的搜索（PBS）**作为顶层多智能体协调算法。
    *   底层单智能体规划器采用**基于格点（Lattice-based）的 A* 搜索**或扩展后的 **SIPP-IP**。
    *   **关键技术**：提出了一种生成**时间同步运动原语**的方法，确保所有运动原语的持续时间都是同一采样时间的倍数，便于碰撞检测和多智能体同步。
*   **第二阶段：连续优化改进**
    *   将第一阶段得到的离散路径作为初始猜测（热启动），构建一个**多阶段最优控制问题（OCP）**。
    *   **多阶段设计**：为了处理不同智能体到达目标时间不同的问题，将整个规划时域划分为 $k$ 个阶段（$k$ 为智能体数量）。每个阶段结束时，至少有一个智能体到达其目标点。这种方法允许在优化过程中灵活调整每个智能体的时间进度，同时保持碰撞约束。

### 3. 实验设计
*   **实验场景**：200x200 的地图，分为“无障碍物”和“随机障碍物”两种环境。
*   **智能体模型**：具有复杂运动学的**通用 2-挂车系统**（包含牵引车、小车和半挂车，共 5 个配置状态变量）。
*   **实验设置**：
    *   智能体数量：2、3、4、5 个。
    *   对比方法：CBS vs. PBS；Lattice-based A* vs. 扩展 SIPP-IP。
    *   评价指标：成功率、计算时间、路径代价（Cost）。

### 4. 资源与算力
*   **软件实现**：单智能体规划器和 CBS/PBS 使用 **C++** 编写；运动原语生成和第二阶段优化使用 **Python** 结合 **CasADi** 框架，求解器采用 **IPOPT** 和 **HSL MA57**。
*   **算力说明**：文中**未明确提及**具体的 CPU 或 GPU 硬件型号。实验中为每个规划任务设置了 100 秒的超时限制。

### 5. 实验数量与充分性
*   **实验规模**：针对每种智能体数量（2-5）和每种环境（有/无障碍物），各随机生成了 **100 组**测试用例，总计 800 组多智能体实验。此外还进行了 1000 组单智能体对比实验。
*   **充分性**：实验涵盖了不同复杂度的环境和不同数量的智能体，对比了主流的搜索框架和底层规划器，数据量足以支撑其结论。实验设计客观，通过统计成功率和平均代价来评估性能。

### 6. 主要结论与发现
*   **规划器对比**：对于牵引车-挂车这类长条形智能体，**基于格点的规划器优于 SIPP-IP**。原因是 SIPP-IP 的网格化碰撞检测过于保守，容易将可行空间误判为碰撞。
*   **搜索框架对比**：在有障碍物环境中，**CBS 的成功率和运行效率均优于 PBS**。虽然 PBS 在无障碍物时表现尚可，但 CBS 在处理复杂约束时更具优势。
*   **优化效果**：第二阶段的 OCP 优化能显著降低路径代价（平均降低约 15-20%），并消除离散搜索带来的运动伪影（如不必要的转向动作）。

### 7. 优点
*   **两阶段结合**：巧妙地结合了离散搜索的全局协调能力和连续优化的局部精细化能力。
*   **多阶段 OCP 建模**：解决了多智能体到达时间不一致时，在统一优化框架下难以施加碰撞约束的痛点。
*   **复杂模型验证**：在极具挑战性的牵引车-挂车模型上验证了算法，比常见的质点或圆盘模型更具说服力。

### 8. 不足与局限
*   **计算耗时**：第二阶段优化虽然效果好，但计算开销较大（通常需要数十秒），难以满足实时动态避障的需求。
*   **拓扑固定**：优化步骤无法改变搜索步骤确定的拓扑路径（例如无法从障碍物的左侧跳到右侧），且智能体到达目标的先后顺序被固定。
*   **可扩展性**：随着智能体数量增加（>5个），搜索和优化的成功率均出现明显下降，在大规模集群规划上的适用性受限。

（完）
