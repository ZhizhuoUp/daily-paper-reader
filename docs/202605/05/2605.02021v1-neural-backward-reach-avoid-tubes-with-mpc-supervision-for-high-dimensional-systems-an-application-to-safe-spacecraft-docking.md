---
title: "Neural Backward Reach-Avoid Tubes with MPC Supervision for High-Dimensional Systems: An Application to Safe Spacecraft Docking"
title_zh: 基于 MPC 监督的高维系统神经后向到达-规避管：在航天器安全对接中的应用
authors: "Santiago Thorup, Luca Castelletto, Zeyuan Feng, Somil Bansal"
date: 2026-05-03
pdf: "https://arxiv.org/pdf/2605.02021v1"
tags: ["query:profile-1"]
score: 9.0
evidence: 使用MPC和可达性分析的航天器安全对接与避障
tldr: 针对高维航天器对接中的碰撞规避与目标到达问题，本文提出一种基于MPC监督的神经后向到达-规避管（BRAT）框架。该方法通过将Hamilton-Jacobi（HJ）可达性分析与课程驱动的MPC监督相结合，克服了传统求解器在高维系统中的局限性。在线部署时，利用学习到的价值函数指导控制器，在13维航天器对接任务中实现了比现有方法更高的成功率和计算效率。
selection_source: fresh_fetch
motivation: 传统的Hamilton-Jacobi可达性分析难以处理高维系统，且现有的学习方法在处理紧耦合的到达-规避任务（如航天器对接）时表现不佳。
method: 提出一种结合PDE损失与课程驱动MPC监督的神经价值函数训练方法，并设计了基于价值梯度的实时控制器及增强型终端MPC。
result: 在6维平面对接和13维全系统对接实验中，该方法在任务成功率和实时计算效率上均优于现有的网格法和学习方法。
conclusion: 该框架成功将形式化可达性保证扩展到高维航天器控制任务，证明了MPC监督在稳定神经HJ分析训练中的有效性。
---

## 摘要
自主航天器对接要求控制策略在耦合的高维平移-旋转动力学下，同时确保碰撞规避和目标可达性。Hamilton-Jacobi (HJ) 可达性分析提供了形式化的到达-规避保证，但经典的求解器仅限于低维系统。基于学习的方法已开始扩展 HJ 分析的规模，但在到达-规避设定中仍面临挑战，特别是在目标集和失败集紧密耦合的情况下（如对接任务）。我们提出了一种基于学习的后向到达-规避管 (BRAT) 框架，通过将 HJ 结构与基于 MPC 的监督紧密结合来解决这一挑战。在离线阶段，我们使用基于 PDE 的损失函数并辅以课程驱动的 MPC 监督来训练 HJ 价值函数的神经近似，这提供了信息丰富的价值目标，并在纯基于 PDE 的方法失效的区域稳定了训练。在在线阶段，学习到的价值函数通过两个实时控制器进行部署：(i) 价值梯度驱动控制器，以及 (ii) 在时域终点显式强制执行可达性的价值函数增强终端 MPC。我们在 6 维平面对接问题上针对基于网格的真值评估了所提方法，随后将其扩展到完整的 13 维系统。在这两种设定下，我们的方法在成功率和计算效率方面均优于现有方法。

## Abstract
Autonomous spacecraft docking requires control policies that simultaneously ensure collision avoidance and target reachability under coupled, high-dimensional translational-rotational dynamics. Hamilton-Jacobi (HJ) reachability provides formal reach-avoid guarantees, but classical solvers are limited to low-dimensional systems. Learning-based approaches have begun to scale HJ analysis, yet they struggle in reach-avoid settings, especially where goal and failure sets are tightly coupled, as in docking. We propose a learning-based Backward Reach-Avoid Tube (BRAT) framework that addresses this challenge by tightly integrating HJ structure with MPC-based supervision. In the offline phase, we train a neural approximation of the HJ value function using PDE-based losses augmented with curriculum-driven MPC supervision, which provides informative value targets and stabilizes training in regions where purely PDE-based methods fail. In the online phase, the learned value function is deployed through two real-time controllers: (i) a value gradient-driven controller, and (ii) a value-function-augmented terminal MPC that explicitly enforces reachability at the horizon. We evaluate the proposed method on a 6D planar docking problem against grid-based ground truth and then scale to the full 13D system. Across both settings, our approach outperforms existing methods in success rate and computational efficiency.

---

## 论文详细总结（自动生成）

### 论文结构化深度总结：基于 MPC 监督的神经后向到达-规避管（BRAT）

#### 1. 核心问题与研究动机
*   **核心问题**：如何在保证安全（避障）的同时，实现高维非线性系统（如 13 维航天器对接）的目标到达。
*   **研究背景**：
    *   **Hamilton-Jacobi (HJ) 可达性分析**：能提供形式化的安全与到达保证，但受“维度灾难”限制，传统网格求解器仅能处理 5-6 维系统。
    *   **现有学习方法（如 DeepReach）**：虽然能扩展维度，但在“到达-规避”（Reach-Avoid）任务中表现不佳，尤其是当目标集与障碍集紧密耦合（如对接端口紧邻航天器主体）时，PDE 损失函数梯度消失，导致训练不稳定。
    *   **航天器对接挑战**：涉及平移与旋转的 13 维耦合动力学，对实时性、计算资源和安全性有极高要求。

#### 2. 方法论
该论文提出了一种结合 PDE 约束与 MPC 监督的神经 BRAT 框架，分为离线训练和在线部署两个阶段：
*   **核心思想**：利用 MPC 生成的轨迹成本作为监督信号，辅助神经网络（SIREN）求解 HJ 偏微分方程（PDE），解决纯 PDE 训练在复杂边界处的收敛难题。
*   **关键技术细节**：
    *   **混合损失函数**：结合了 PDE 残差损失（强制符合 HJ 物理结构）和 MPC 监督损失（提供直接的价值函数标签）。
    *   **训练-验证-精炼循环（Train-Verify-Refine）**：引入课程学习，随时间视界（Horizon）逐渐扩大训练范围。在每个阶段，使用当前策略作为 MPC 的热启动（Warm-start）来重新生成标签，形成反馈闭环。
    *   **精确边界条件参数化**：通过数学构造确保网络在 $t=T$ 时自动满足边界条件，减轻学习负担。
*   **在线控制器**：
    *   **BRAT 梯度控制器**：利用价值函数梯度进行 Bang-Bang 控制。
    *   **价值函数增强终端 MPC**：将学习到的价值函数作为 MPC 的终端成本，兼顾短期规划与长期可达性。
    *   **安全过滤器**：引入最少干预的安全层，确保在未收敛区域的安全性。

#### 3. 实验设计
*   **实验场景**：
    1.  **6D 平面对接**：用于与数值网格法的真值（Ground Truth）进行对比验证。
    2.  **13D 全轨道对接**：模拟真实的航天器对接任务，包含位置、速度、四元数姿态和角速率。
*   **Benchmark 与对比方法**：
    *   **数值网格法（仅 6D）**：作为几何精度参考。
    *   **Receding-horizon MPC**：传统的预测控制。
    *   **Vanilla DeepReach**：纯 PDE 驱动的神经可达性方法。
    *   **RL Reach-Avoid**：基于强化学习的可达性方法。
*   **评估指标**：对接成功率、碰撞率、超时率、计算耗时（Time per step）、控制能耗。

#### 4. 资源与算力
*   **硬件环境**：工作站配备 AMD Ryzen Threadripper PRO 5965WX (24核) CPU，128GB RAM，以及 **2 张 NVIDIA RTX 4090 GPU**。
*   **训练时长**：
    *   6D 模型：约 12 小时。
    *   13D 模型：约 28 小时。
    *   数据生成耗时：13D 场景下生成一个视界的 MPC 标签约需 46 分钟。

#### 5. 实验数量与充分性
*   **实验规模**：进行了 **10,000 组随机初始条件的蒙特卡洛模拟**（针对 6D 和 13D 分别进行），样本量巨大，足以证明统计意义上的可靠性。
*   **充分性评价**：实验设计非常充分。不仅对比了成功率，还深入分析了价值函数的几何精度（TPR/FPR）、轨迹最优性以及在线计算效率。通过 6D 场景的真值对比，证明了神经近似的准确性；通过 13D 场景证明了其扩展性。

#### 6. 主要结论与发现
*   **性能卓越**：在 13D 对接任务中，该方法实现了 **99.2% 的成功率**，而传统 MPC 和 Vanilla DeepReach 在此高维紧耦合场景下几乎完全失效。
*   **实时性极强**：在线推理仅需 **10-16 毫秒**，比终端 MPC 快 3 个数量级，完全满足航天器车载计算限制。
*   **精度超越网格**：在 6D 实验中，由于神经网络的连续性，其表现甚至优于受离散化误差影响的网格法（对接速度更快）。

#### 7. 优点
*   **高维扩展性**：成功将形式化保证的可达性分析推向了 13 维系统，突破了传统方法的瓶颈。
*   **训练稳定性**：通过 MPC 监督和课程学习解决了 HJ PDE 在到达-规避任务中梯度消失和训练不稳定的顽疾。
*   **部署友好**：离线重计算、在线轻量化，非常适合算力受限的嵌入式系统。

#### 8. 不足与局限
*   **环境假设**：目前模型假设环境无扰动且目标卫星是静止的（非翻滚状态），未考虑空间环境中的随机干扰。
*   **离线依赖**：虽然在线快，但离线训练和 MPC 标签生成仍需数十小时的高性能计算资源。
*   **泛化风险**：虽然在采样范围内表现良好，但对于极端的、未见过的初始状态，神经网络仍存在失效风险（尽管有安全过滤器兜底）。

（完）
