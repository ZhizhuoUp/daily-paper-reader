---
title: Zero-Shot Signal Temporal Logic Planning with Disjunctive Branch Selection in Dynamic Semantic Maps
title_zh: 动态语义地图中具有析取分支选择的零样本信号时序逻辑规划
authors: "Bowen Ye, Ancheng Hou, Junyue Huang, Ruijia Liu, Xiang Yin"
date: 2026-05-02
pdf: "https://arxiv.org/pdf/2605.01222v1"
tags: ["query:profile-1"]
score: 8.0
evidence: 用于实时轨迹生成的零样本STL规划求解器
tldr: 针对信号时序逻辑（STL）规划中优化方法慢、学习方法泛化性差的问题，本文提出一种零样本STL规划求解器。该方法结合了地图条件Transformer架构与轻量级启发式算法，能有效处理复杂的析取（OR）子公式。通过传递强化学习（TRL）确保子任务间的逻辑一致性，实现了在动态语义地图中无需重新训练即可生成可行轨迹，显著提升了泛化能力和任务覆盖率。
selection_source: fresh_fetch
motivation: 传统的STL规划方法在处理动态环境和复杂逻辑规范时，面临计算效率低或泛化能力不足的挑战。
method: 提出一种集成地图条件Transformer与启发式分支选择的求解器，并利用传递强化学习（TRL）来保证分解任务的时序一致性。
result: 在具有多样化障碍物布局的动态语义地图实验中，该框架展现出优异的零样本泛化性能和广泛的STL任务覆盖能力。
conclusion: 该研究为动态环境下的复杂逻辑任务规划提供了一种高效且具备强泛化能力的零样本解决方案。
---

## 摘要
信号时序逻辑 (STL) 提供了可验证的任务规范，对于安全关键型控制至关重要。然而，STL 规划仍然面临挑战：基于精确优化的方法通常计算速度过慢，而基于学习的方法在不同环境中的泛化能力较差。我们提出了一种针对可变地图环境的零样本 STL 规划求解器，无需重新训练即可生成可行轨迹。通过将地图条件的 Transformer 架构与轻量级启发式方法相结合，我们的方法能够有效处理复杂的析取 (OR) 子公式。此外，我们利用传递强化学习 (TRL) 来确保分解后的子任务之间具有一致的时间落地和逻辑连贯性。在具有多种障碍物布局的动态语义地图上进行的实验表明，该框架取得了持续的性能提升，突显了其在多变环境中卓越的零样本泛化能力以及广泛的 STL 覆盖范围。

## Abstract
Signal Temporal Logic (STL) offers verifiable task specifications and is crucial for safety-critical control. Yet STL planning remains challenging: exact optimization-based methods are often too slow, and learning-based methods struggle to generalize across varying environments. We propose a zero-shot STL planning solver for variable-map environments that generates feasible trajectories without retraining. By integrating a map-conditioned Transformer architecture with a lightweight heuristic, our approach effectively handles complex disjunctive (OR) subformulas. Furthermore, we leverage Transitive Reinforcement Learning (TRL) to ensure consistent temporal grounding and logical coherence across decomposed sub-tasks. Experiments on dynamic semantic maps with diverse obstacle layouts demonstrate consistent gains, highlighting the framework's superior zero-shot generalization to changing environments and broad STL coverage.

---

## 论文详细总结（自动生成）

这篇论文提出了一种针对动态语义地图的零样本信号时序逻辑（STL）规划框架。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：在安全关键型控制任务中，STL 能够提供可验证的逻辑规范。然而，传统的基于优化的方法（如 MILP）在处理长时程、复杂逻辑和复杂地图时计算开销巨大，难以实时应用；而现有的学习方法在面对未见过的地图布局或复杂的析取（OR）逻辑时，泛化能力较差。
*   **核心问题**：如何在无需针对新环境重新训练的情况下（零样本），实现能够处理复杂逻辑（包含“或”分支）且适应多变地图布局的实时轨迹规划。

### 2. 论文提出的方法论
该方法遵循“分解-合成”（Decompose-then-Synthesize）范式，核心组件包括：
*   **析取分支的启发式选择（Heuristic Selection）**：
    *   将复杂的 STL 公式转换为析取范式（DNF），产生多个可选分支。
    *   设计了一个轻量级评分函数，从**时序算子复杂度**（惩罚复杂结构）、**地图可达性成本**（基于图搜索评估目标区域到达难度）和**时间松弛度**（优先选择时间窗口更宽的分支）三个维度筛选最优分支。
*   **基于传递强化学习（TRL）的时间预测器**：
    *   使用 CNN 编码地图，MLP 融合位置特征，预测子任务间的时间步长。
    *   引入 **TRL 损失函数**，通过维护样本间的排序一致性（Transitive Ordering），确保预测的时间分配在逻辑上更连贯，减少噪声干扰。
*   **地图条件 Transformer 轨迹生成器**：
    *   采用自回归 Transformer 架构，将地图特征、区域描述符、时间区间和历史状态序列化为 Token。
    *   通过**计划采样（Scheduled Sampling）**训练，缓解推理时的曝光偏差，使其能在未知动力学模型的情况下，根据地图和逻辑约束生成可行轨迹。

### 3. 实验设计
*   **实验场景**：基于 Maze2D 导航基准，设计了 5x5 (U)、7x7 (M) 和 9x9 (L) 三种不同规模和复杂度的动态迷宫地图。
*   **动力学模型**：测试了双积分器（Double-Integrator）和单轮车（Unicycle）两种动力学系统，验证求解器的动力学无关性。
*   **对比基准（Benchmark）**：主要对比了“不带启发式分支选择”的基础版本（即随机或固定选择逻辑分支），验证启发式机制在复杂逻辑下的有效性。
*   **评估指标**：成功率（Success Rate）、鲁棒性值（Robustness Value）和总规划时间（Total Planning Time）。

### 4. 资源与算力
*   **硬件环境**：
    *   **GPU**：单张 NVIDIA A800 (80GB)。
    *   **CPU**：AMD EPYC 7542 (32核，双路)。
*   **训练与推理**：训练在单 GPU 上完成；推理和闭环仿真测试在 CPU 上执行。文中未详细说明具体的训练时长，但强调了推理过程的实时性。

### 5. 实验数量与充分性
*   **实验规模**：在 3 种地图规模、2 种动力学模型下进行了多组实验，并提供了定性的轨迹可视化图。
*   **充分性评价**：实验涵盖了从简单到复杂的地图和逻辑规范，验证了方法在不同动力学系统下的通用性。
*   **局限性**：对比实验主要集中在自身组件的消融（是否有启发式选择），缺乏与其他最先进（SOTA）零样本 STL 规划器（如基于扩散模型的方法）的直接横向对比。

### 6. 论文的主要结论与发现
*   **零样本泛化成功**：该框架无需针对新地图重新训练，即可在未见过的障碍物布局中保持高成功率。
*   **启发式选择的必要性**：在复杂逻辑中，启发式分支选择显著提升了成功率（尤其在大型复杂地图中），并大幅减少了规划时间（通过剪枝无效分支）。
*   **TRL 的作用**：通过传递强化学习训练的时间预测器提供了更准确的时序落地，增强了轨迹生成的逻辑一致性。
*   **动力学鲁棒性**：尽管 Transformer 未显式建模动力学，但通过离线数据学习到的先验能有效适配不同的执行机构。

### 7. 优点（亮点）
*   **高效处理 OR 逻辑**：通过轻量级启发式算法解决了 STL 规划中常见的组合爆炸问题。
*   **地图感知能力强**：将地图信息直接 Token 化输入 Transformer，使其具备极强的空间泛化能力。
*   **逻辑与几何解耦**：将高层逻辑分解与底层轨迹生成分离，既保证了逻辑严谨性，又兼顾了生成的灵活性。

### 8. 不足与局限
*   **基准对比不足**：未与最新的基于扩散模型（Diffusion-based）或神经预测控制（NPC）的 STL 方法进行性能对比。
*   **动力学依赖数据**：虽然不要求显式模型，但仍高度依赖离线数据集的覆盖范围，若测试时的动力学特性与训练数据差异过大，性能可能下降。
*   **场景单一**：实验主要集中在 2D 导航任务，尚未在 3D 环境或多智能体协同等更复杂的场景中验证。

（完）
