---
title: "Terminal Matters: Kinodynamic Planning with a Terminal Cost and Learned Uncertainty in Belief State-Cost Space"
title_zh: 终端至关重要：置信状态-代价空间中具有终端代价和学习不确定性的动力学规划
authors: "Zhuoyun Zhong, Seyedali Golestaneh, Constantinos Chamzas"
date: 2026-05-09
pdf: "https://arxiv.org/pdf/2605.09046v2"
tags: ["query:q4"]
score: 8.0
evidence: 机器人任务的动力学约束规划
tldr: 针对机器人运动规划中忽略终点状态质量的问题，本文提出了一种结合终端代价和学习不确定性的动力学约束规划框架KiTe。该方法在AO-RRT算法基础上引入终端代价函数，并扩展至信念空间，通过最小化终端信念与目标的Wasserstein距离来提高到达目标的可靠性。实验证明，KiTe在多种复杂任务中显著提升了不确定环境下的规划成功率，并支持从数据中学习动力学模型。
selection_source: fresh_fetch
motivation: 现有的采样动力学规划器通常将到达目标视为可行性检查，缺乏对终点状态质量（如目标偏好和可靠性）的显式优化。
method: 提出了一种包含终端代价的规划公式并扩展至信念空间，利用Wasserstein距离优化目标到达概率，并集成学习到的动力学与不确定性模型。
result: 在Flappy Bird、自动泊车和平面推物等仿真及实物实验中，KiTe在不确定环境下均表现出更高的目标到达成功率。
conclusion: 通过在动力学规划中引入终端代价和信念空间优化，可以显著增强机器人在不确定动力学系统中的规划性能和任务可靠性。
---

## 摘要
在许多现实世界的机器人任务中，机器人必须生成动力学可行的运动，以便在不确定性下可靠地达到预定目标。然而，现有的基于采样的动力学规划器通常优化累积轨迹代价，并将到达目标视为可行性检查，而不是显式地优化终端状态质量，例如目标偏好或到达目标的可靠性。在这项工作中，我们为动力学规划引入了一种终端代价公式，允许在优化累积轨迹代价的同时优化终端状态质量。我们证明了渐近最优动力学规划器 AO-RRT 在这一增强目标下仍保持其渐近最优性。我们进一步将该公式扩展到置信空间，并证明最小化终端置信度与目标之间的 Wasserstein 距离可以提高到达目标区域概率的下界。由此产生的规划器 KiTe 利用这一终端代价目标来编码目标偏好并提高不确定性下的可靠性。为了支持没有解析不确定性模型的系统，我们直接从数据中学习动力学和过程不确定性，并将学习到的置信动力学集成到规划中。在 Flappy Bird、停车场泊车（Car Parking）和平面推动（Planar Pushing）实验中，KiTe 一致地提高了不确定性下到达目标的成功率。现实世界的平面推动实验进一步证明，KiTe 可以利用学习到的动力学和不确定性进行有效规划。源代码可在 https://github.com/elpis-lab/KiTe 获取。

## Abstract
In many real-world robotic tasks, robots must generate dynamically feasible motions that reliably reach desired goals even under uncertainty. Yet existing sampling-based kinodynamic planners typically optimize accumulated trajectory costs and treat goal reaching as a feasibility check, rather than explicitly optimizing terminal-state quality, such as goal preference or goal-reaching reliability. In this work, we introduce a terminal-cost formulation for kinodynamic planning that allows terminal-state quality to be optimized alongside accumulated trajectory cost. We prove that AO-RRT, an asymptotically optimal kinodynamic planner, preserves its asymptotic optimality under this augmented objective. We further extend the formulation to belief space and prove that minimizing the Wasserstein distance between the terminal belief and the goal improves a lower bound on the probability of reaching the goal region. The resulting planner, KiTe, uses this terminal-cost objective to encode goal preferences and improve reliability under uncertainty. To support systems without analytical uncertainty models, we learn dynamics and process uncertainty directly from data and integrate the learned belief dynamics into planning. Experiments on Flappy Bird, Car Parking, and Planar Pushing show that KiTe consistently improves goal-reaching success under uncertainty. Real-world Planar Pushing experiments further demonstrate that KiTe can plan effectively with learned dynamics and uncertainty. Source code is available at https://github.com/elpis-lab/KiTe.

---

## 论文详细总结（自动生成）

这是一份关于论文《Terminal Matters: Kinodynamic Planning with a Terminal Cost and Learned Uncertainty in Belief State-Cost Space》的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：在现实世界的机器人任务中，动力学规划（Kinodynamic Planning）不仅需要满足物理约束，还需应对执行误差和模型不确定性。
*   **核心动机**：现有的基于采样的规划器（如 AO-RRT, SST）主要优化路径上的累积运行代价（如长度、能量），而将“到达目标”仅视为一个可行性检查。这导致规划出的路径虽然在几何上最短，但可能在终点处具有极大的不确定性，或无法体现对特定终端状态的偏好。
*   **研究问题**：如何在保证算法渐近最优性的前提下，显式地在动力学规划中引入并优化终端状态质量（Terminal-state Quality）。

### 2. 方法论
*   **核心思想**：提出 **KiTe** 框架，通过在增广的状态-代价空间中引入终端代价函数，并将其扩展至信念空间（Belief Space），实现对路径效率和终端可靠性的双重优化。
*   **关键技术细节**：
    *   **AO-RRT 扩展**：将 AO-RRT 的目标函数修改为 $C_T = \int_0^T \ell(x, u) dt + \phi(x(T))$，其中 $\phi$ 为终端代价。
    *   **渐近最优性证明**：论文提供了严谨的数学证明，证实了在 Lipschitz 连续性假设下，引入终端代价后的 AO-RRT 依然保持渐近最优性。
    *   **信念空间公式化**：将状态表示为高斯分布 $b = \mathcal{N}(x, P)$。利用 **Wasserstein 距离** 度量信念之间的差异。
    *   **成功率下界理论**：论文证明了终端信念与目标点 Dirac 分布之间的 Wasserstein 距离可以诱导出到达目标区域概率的一个下界。因此，最小化该距离等同于最大化成功率。
    *   **学习置信动力学**：针对难以建模的系统（如推动物体），使用神经网络学习状态转移均值和过程不确定性（方差），并采用负对数似然（NLL）损失函数进行训练。

### 3. 实验设计
*   **实验场景**：
    1.  **Flappy Bird**：验证在确定性环境下对终端位置偏好的优化能力。
    2.  **自动泊车 (Car Parking)**：在 SE(2) 空间处理非完整约束，验证在多个目标区域中选择更优终端状态的能力。
    3.  **平面推物 (Planar Pushing)**：处理接触丰富的复杂动力学，包含仿真和实物机器人实验。
*   **对比方法 (Benchmarks)**：
    *   **Base (AO-RRT/SST)**：仅优化运行代价的标准算法。
    *   **GBT (Gaussian Belief Trees)**：现有的信念空间规划方法。
    *   **AP (Active Pushing)**：利用模型不确定性偏置采样的活跃规划方法。

### 4. 资源与算力
*   **硬件配置**：Intel i7-14700KF CPU, 32GB RAM, NVIDIA RTX 4070 Ti Super GPU。
*   **算力细节**：文中未详细给出模型训练的具体时长，但提到动力学模型是基于 1000 组交互数据训练的 MLP 网络。规划过程在单机环境下运行，规划时间预算设定在 10s 至 30s 之间。

### 5. 实验数量与充分性
*   **实验规模**：每个任务设置了 20 个随机场景或初始位姿，每个问题重复运行 5 次以获取统计意义（实物实验为 1 次）。
*   **充分性评价**：实验设计较为充分。涵盖了从简单离散控制到复杂连续接触动力学的多种任务；包含了仿真验证与实物部署；通过消融实验展示了终端权重 $w_g$ 对结果的影响。对比实验涵盖了当前主流的动力学规划和信念空间规划基准，具有较强的说服力。

### 6. 主要结论与发现
*   **终端代价的重要性**：显式优化终端代价能显著提升机器人在不确定环境下的任务成功率。
*   **性能权衡**：KiTe 往往会选择几何上稍长但更“稳健”的路径（例如从物体更稳定的侧面进行推动），以换取更小的终端不确定性。
*   **理论有效性**：Wasserstein 距离被证明是优化目标达成概率的有效代理指标，能够指导规划器在信念空间中找到更高质量的解。

### 7. 优点与亮点
*   **理论与实践结合**：不仅提供了严谨的渐近最优性证明，还通过实物实验验证了算法的实用性。
*   **解决阈值难题**：通过将成功率转化为终端代价优化，避免了传统机会约束规划中难以设定硬性概率阈值（Chance Constraints Threshold）的问题。
*   **通用性**：学习到的动力学模型可以无缝集成到基于采样的规划框架中，适用于多种复杂动力学系统。

### 8. 不足与局限
*   **开环规划**：目前 KiTe 主要关注开环轨迹生成，虽然考虑了不确定性传播，但未在规划层面引入闭环反馈控制。
*   **权重调优**：终端代价与运行代价之间的权重平衡（$w_g$）仍需要一定的人工调参。
*   **高斯假设**：信念空间传播依赖于高斯分布假设，对于具有高度非线性或多峰分布的不确定性场景，表现可能会受限。

（完）
