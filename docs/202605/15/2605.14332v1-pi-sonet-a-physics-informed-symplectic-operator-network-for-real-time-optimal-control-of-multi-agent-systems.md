---
title: "PI-SONet: A Physics-Informed Symplectic Operator Network for Real-Time Optimal Control of Multi-Agent Systems"
title_zh: PI-SONet：一种用于多智能体系统实时最优控制的物理信息辛算子网络
authors: "Alan John Varghese, Shanqing Liu, Paula Chen, Yaochen Zhu, Jérôme Darbon, George Em Karniadakis"
date: 2026-05-14
pdf: "https://arxiv.org/pdf/2605.14332v1"
tags: ["query:profile-1"]
score: 8.0
evidence: 高维系统的实时最优控制
tldr: 针对高维多智能体系统实时最优控制中的维度灾难和重复计算问题，本文提出PI-SONet。这是一种物理信息引导的辛算子网络，通过结合潜在空间求解器与条件辛算子，学习庞特里亚金极大值原理（PMP）的解映射。该方法在保持哈密顿结构的同时，实现了对未见问题配置的快速泛化，推理速度比传统基准快达一万倍，为高维实时最优控制提供了高效的代理模型。
selection_source: fresh_fetch
motivation: 传统最优控制求解器在处理高维非凸非线性多智能体系统时面临维度灾难，且无法在不同问题设置间复用。
method: 提出一种结构保持的算子学习框架，利用潜在空间求解器和条件辛算子在辅助空间生成哈密顿轨迹并映射回物理空间。
result: "在数百维的问题上实现了亚秒级推理，相比代表性基准算法提速高达10,000倍，并展现出强大的泛化能力。"
conclusion: 结构保持的神经算子是实现高维最优控制实时、可重用代理模型的有效途径。
---

## 摘要
许多现实应用涉及对高维多智能体系统的实时控制。现有的最优控制求解器通常受限于“维数灾难”，且针对每个新问题设置都需要重新运行。我们针对数百维的非凸、非线性问题，引入了 PI-SONet（物理信息辛算子网络），这是一种保结构算子学习框架，用于求解参数化最优控制问题族及其庞特里亚金极大值原理（PMP）系统。PI-SONet 将潜在右空间求解器与条件辛算子相结合，在计算高效的辅助空间中生成可处理的哈密顿轨迹，并将其转换回物理空间。这种分解产生了一个经过训练的单一算子，它能够近似 PMP 解映射，固有地保持哈密顿结构，并能推广到未见过的问题配置。与本质上是单实例求解器的现有方法不同，PI-SONet 在新问题实例上实现了亚秒级推理，与代表性基准相比，加速比高达 10,000 倍。这些结果表明，保结构神经算子为实现高维最优控制的可重用、实时代理模型提供了一条切实可行的途径。

## Abstract
Many real-life applications involve controlling high-dimensional multi-agent systems in real-time. Existing optimal control solvers often suffer from the curse-of-dimensionality and require complete rerunning for each new problem setting. We target nonconvex, nonlinear problems in 100s of dimensions by introducing PI-SONet (Physics-Informed Symplectic Operator Network), a structure-preserving operator learning framework for solving parameterized families of optimal control problems and their Pontraygin Maximum Principle (PMP) systems. PI-SONet combines a latent right-space solver with a conditional symplectic operator to produce tractable Hamiltonian trajectories in a computationally efficient auxiliary space and transform them back to physical space. This decomposition yields a \textit{single} trained operator that approximates the PMP solution map, inherently preserves Hamiltonian structure, and generalizes across unseen problem configurations. Unlike existing methods, which are fundamentally single-instance solvers, PI-SONet achieves sub-second inferences on new problem instances, equating to up to 10,000x speedup over representative baselines. These results suggest that structure-preserving neural operators provide a practical route toward reusable, real-time surrogates for high-dimensional optimal control.

---

## 论文详细总结（自动生成）

### PI-SONet：用于多智能体系统实时最优控制的物理信息辛算子网络总结

#### 1. 核心问题与整体含义（研究动机和背景）
在机器人、自动驾驶和空中交通管理等领域，高维多智能体系统（MAS）的实时最优控制至关重要。然而，传统方法面临两大挑战：
*   **维数灾难**：随着智能体数量增加，状态空间维度剧增，计算成本呈指数级增长。
*   **单实例限制**：现有的数值求解器（如直接法或基于PMP的间接法）通常是“单次使用”的，即环境、初始条件或智能体参数稍有变化，就必须从头开始重新计算，无法满足动态环境下的实时响应需求。
**PI-SONet 的核心目标**是开发一种能够学习“解映射”的算子网络，使其不仅能保持物理结构的严谨性，还能在不同问题配置间实现亚秒级的快速泛化。

#### 2. 方法论：核心思想与技术细节
PI-SONet 采用了一种**保结构算子学习框架**，将最优控制问题的求解分解为两个阶段：
*   **阶段 1：潜在右空间求解器（Latent Right-Space Solver）**：
    *   首先构建一个计算廉价的辅助（潜在）哈密顿系统（如线性二次型调节器 LQR、带旋转的 LQR 或 Eikonal 方程）。
    *   该求解器快速生成一个粗略但具有基本拓扑特征的潜在状态-协态轨迹。
*   **阶段 2：条件辛算子（Conditional Symplectic Operator）**：
    *   使用一种特殊的神经网络架构（SONet），通过一系列交替的“辛剪切（Symplectic Shears）”层组成。
    *   **关键技术**：该算子是“条件化”的，其参数由当前问题的配置（如初始状态、障碍物几何、智能体能力）决定。
    *   **保结构性**：通过架构设计强制满足辛对称性（Symplecticity），从而在数学上保证映射后的轨迹依然遵循哈密顿动力学，维持最优性。
*   **训练流程**：采用**物理信息（Physics-Informed）**方式，通过最小化庞特里亚金极大值原理（PMP）系统的残差、初始条件损失和终端约束损失来训练网络，无需大量标注的专家轨迹数据。

#### 3. 实验设计
论文在多种复杂度的多智能体路径规划场景中进行了验证：
*   **实验场景**：
    *   **自由空间穿越**：4 到 64 个智能体（最高 256 维）的位置交换。
    *   **障碍物规避**：绕过圆形障碍物或在非凸迷宫环境中导航。
    *   **几何泛化**：改变障碍物半径。
    *   **异构智能体**：不同半径、质量和阻力系数的智能体组合。
    *   **高维 3D 场景**：100 个异构智能体在 3D 空间规避多个长方体障碍物（600 维）。
*   **基准方法（Benchmarks）**：
    *   **CasADi (Multiple Shooting)**：传统的直接多重打靶法。
    *   **GPOPS-II (Pseudospectral)**：伪谱最优控制方法。
    *   **TSympOCNet**：一种单实例的辛物理信息神经网络（PINN）。

#### 4. 资源与算力
*   **硬件使用**：
    *   **推理测试**：在 NVIDIA H100 GPU 上进行，强调了亚秒级的实时性能。
    *   **基准对比**：在配备 Intel Xeon E5-2620 v4 CPU 和 NVIDIA GeForce RTX 2080 Ti GPU 的工作站上运行。
*   **训练时长**：文中提到了离线训练时间（通常在数百秒量级，如 178s 到 623s 不等），但强调一旦训练完成，在线推理仅需约 0.02s。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 4 智能体到 100 智能体的不同规模，测试样本通常为 50-100 个未见过的随机扰动实例。
*   **消融实验**：
    *   对比了不同潜在求解器（标准 LQR vs 带旋转 LQR）对收敛性的影响。
    *   对比了辛架构（SONet）与标准多层感知机（MLP）的性能，证明了保结构设计的必要性。
*   **充分性评价**：实验设计较为全面，不仅覆盖了维度扩展，还验证了对环境几何和智能体异构性的泛化能力，对比基准具有代表性，结果客观。

#### 6. 主要结论与发现
*   **极速推理**：PI-SONet 实现了亚秒级（约 20ms）的在线推理，相比传统基准方法（如 GPOPS-II 或多重打靶法）提速 **21 倍至 10,868 倍**。
*   **强泛化性**：单个训练好的算子可以成功处理未见过的初始位置、障碍物尺寸和智能体参数变化。
*   **高质量解**：在保持极高可行性（碰撞规避成功率接近 100%）的同时，获得的控制成本与传统高精度求解器非常接近。
*   **突破维度限制**：成功解决了高达 600 维的非线性最优控制问题，展示了良好的扩展性。

#### 7. 优点
*   **算子化思维**：将最优控制从“逐次求解”转变为“一次学习，多次复用”，解决了实时性痛点。
*   **物理结构诱导偏差**：通过辛神经网络强制保持哈密顿结构，比纯数据驱动方法更具鲁棒性和数学解释性。
*   **冷启动能力**：利用潜在空间求解器提供拓扑先验，有效处理了非凸环境（如迷宫）中的局部最优问题。

#### 8. 不足与局限
*   **局部最优风险**：基于 PMP 的方法本质上寻找的是局部最优解，在极度复杂的非凸空间中可能无法保证全局最优。
*   **对潜在求解器的依赖**：算子的性能在一定程度上取决于潜在求解器（如 LQR）提供的先验轨迹质量，若先验与实际物理空间差异过大，训练难度会增加。
*   **测试时微调**：虽然推理很快，但在极少数极端碰撞情况下，仍可能需要额外的测试时微调（如 L-BFGS 优化）来确保 100% 的安全性。

（完）
