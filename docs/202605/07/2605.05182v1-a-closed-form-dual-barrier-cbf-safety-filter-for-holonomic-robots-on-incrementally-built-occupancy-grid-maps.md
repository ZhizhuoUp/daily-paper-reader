---
title: A Closed-Form Dual-Barrier CBF Safety Filter for Holonomic Robots on Incrementally Built Occupancy Grid Maps
title_zh: 一种用于增量构建占据网格地图上全向机器人的闭式双障碍 CBF 安全过滤器
authors: "Himanshu Paudel, Basanta Joshi, Dhirendra Raj Madai, Alina Bartaula, Biman Rimal, Sanjay Neupane"
date: 2026-05-06
pdf: "https://arxiv.org/pdf/2605.05182v1"
tags: ["query:q5"]
score: 8.0
evidence: 使用控制屏障函数的实时安全关键速度控制
tldr: 本文提出一种针对全向机器人在增量占据网格地图中运行的双屏障控制屏障函数（CBF）安全过滤器。该方法通过解析推导，同时强制执行避障和限制进入未探索区域两项约束，并提供闭式解，计算开销极低。结合自适应增益调度，在保证安全的同时提升了探索效率，特别适用于资源受限的嵌入式平台。
selection_source: fresh_fetch
motivation: 在未知环境中，未探测区域的不确定性是碰撞的主要风险源，现有方法难以在低算力平台上兼顾已知障碍避让与未知区域限制。
method: 利用占据网格的有向距离场解析推导双屏障CBF，通过求解小型线性系统实现闭式安全过滤，并采用自适应增益调节边界约束。
result: 在树莓派等低功耗平台上运行流畅，并在PX4四旋翼无人机的实际室内飞行实验中成功实现了零碰撞。
conclusion: 该研究提供了一种高效、微创的安全过滤方案，能够与任意名义控制器结合，显著提升了机器人在未知环境中的自主导航安全性。
---

## 摘要
我们提出了一种双障碍控制障碍函数 (CBF) 安全过滤器，用于在增量构建的占据网格地图中运行的全向机器人的实时、安全关键型速度控制。当机器人探索未知环境时，未建图区域会引入不可消除的不确定性，因为探索边界之外的障碍物几何形状是未知的，这使得进入此类区域成为碰撞风险的来源，尤其是在使用前向传感器的情况下。为了解决这个问题，我们实施了两个约束：避开已建图的障碍物和限制进入未探索区域。这两个约束都是从占据网格的有符号距离场中解析推导出来的，从而产生了一个闭式安全过滤器，每个周期仅需求解一个小规模线性系统。在 Raspberry Pi 等资源受限的平台上，SLAM 和规划已经消耗了大量计算资源，所提过滤器较低的开销节省了资源。一种自适应增益调度在信息丰富的区域放宽边界约束，并在建图良好的区域收紧约束，在保持安全的同时提高了探索效率。该过滤器在速度空间中作为一种最小侵入式修正运行，并可与包括基于学习的方法在内的任意名义控制器组合。在受 PX4 控制的四旋翼飞行器上进行的硬件飞行实验表明，在多次室内运行中实现了零碰撞。

## Abstract
We present a dual-barrier control barrier function (CBF) safety filter for real-time, safety-critical velocity control of holonomic robots operating in incrementally built occupancy grid maps. As a robot explores an unknown environment, unmapped regions introduce irreducible uncertainty, since obstacle geometry beyond the explored frontier is unknown, making entry into such regions a source of collision risk, especially with front-facing sensors. To address this, we enforce two constraints: avoidance of mapped obstacles and restriction from unexplored regions. Both constraints are derived analytically from the occupancy grid's signed distance field, yielding a closed-form safety filter that requires only a small linear system solve per cycle. On resource-constrained platforms such as the Raspberry Pi, where SLAM and planning already consume significant compute, the low overhead of the proposed filter preserves resources. An adaptive gain schedule relaxes the frontier constraint in information-rich regions and tightens it in well-mapped areas, improving exploration efficiency while maintaining safety. The filter operates in velocity space as a minimally invasive correction and composes with arbitrary nominal controllers, including learning-based methods. Hardware flight experiments on a PX4-controlled quadrotor demonstrate zero collisions across multiple indoor runs.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种针对全向机器人（如四旋翼无人机）在未知环境中自主探索时的安全过滤机制。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：机器人在探索未知环境时面临双重风险：一是碰撞已探测到的障碍物；二是由于传感器视野有限，盲目进入未探测区域（Frontier）可能导致不可预知的碰撞。
*   **研究动机**：
    *   **现有方法局限**：人工势场法（APF）缺乏正式的安全保证且易陷入局部最优；模型预测控制（MPC）计算开销过大，难以在树莓派等资源受限的嵌入式平台上实时运行。
    *   **环境动态性**：在增量建图（SLAM）过程中，地图是不断变化的，安全层必须能够实时处理动态更新的占据网格地图（OGM）。

### 2. 方法论
该论文提出了一种基于**双障碍控制障碍函数（Dual-Barrier CBF）**的闭式安全过滤器：
*   **核心思想**：将安全问题转化为一个最小二乘优化问题，即在满足安全约束的前提下，对名义控制器输出的速度指令进行最小程度的修正。
*   **关键技术细节**：
    *   **双障碍构建**：利用占据网格的有符号距离场（SDF）构建两个屏障函数：$h_1$（针对已建图障碍物）和 $h_2$（针对未探索区域边界）。
    *   **可容许塑造类（Admissible Shaping Class）**：引入一类单调、Lipschitz 连续且有界的函数（如 `tanh`），将原始 SDF 映射为 CBF，确保梯度的有界性和数值稳定性。
    *   **闭式双投影求解**：不同于通用的二次规划（QP）求解器，论文通过解析推导 KKT 条件，将优化问题简化为四种活跃集情况的分类讨论。在最复杂的情况下也只需解一个 2x2 的线性系统，极大地降低了计算量。
    *   **自适应增益调度**：根据局部不确定性密度动态调整边界约束的增益 $\gamma_2$。在信息丰富区域放宽约束以促进探索，在建图完善区域收紧约束以确保安全。
    *   **软 QP 回退机制**：当两个约束方向冲突（如狭窄通道）导致硬约束无解时，通过二分法求解软化后的优化问题，寻找“最不违反安全”的速度。

### 3. 实验设计
*   **实验场景**：室内自主探索任务。
*   **硬件平台**：搭载 Raspberry Pi 4B（4GB）作为上位机，CUAV Nano 7（运行 PX4 v1.16）作为飞行控制器的四旋翼无人机。
*   **对比基准（Benchmark）**：RRT*-APF 名义控制器（仅使用人工势场法进行避障，不带 CBF 过滤）。
*   **评估指标**：总探索时间、覆盖率、探索面积、路径长度、平均速度、最小间距、CBF 干预频率及计算耗时。

### 4. 资源与算力
*   **计算平台**：主要计算在 **Raspberry Pi 4B** 上完成。
*   **算力需求**：论文强调了该方法的**极低计算开销**。由于采用了闭式解而非通用 QP 求解器，它为板载 SLAM（RTAB-Map）和路径规划留出了充足的 CPU 资源。
*   **GPU 使用**：文中**未提到使用 GPU**，所有计算均在 CPU 上实时完成（控制频率为 10Hz）。

### 5. 实验数量与充分性
*   **实验组成**：
    *   **仿真实验**：对比了开启和关闭 CBF 时的探索表现。
    *   **硬件实验**：进行了 3 组完整的室内硬件飞行测试（Run 1-3）。
*   **充分性评价**：实验涵盖了从理论验证到实机部署的全过程。虽然硬件实验次数（3次）不算多，但提供了详细的时序数据（如屏障函数演化、速度修正量、SDF 距离等），足以证明该算法在真实物理环境中的有效性和鲁棒性。

### 6. 主要结论与发现
*   **零碰撞保证**：在所有硬件实验中，无人机均实现了零碰撞。
*   **探索效率提升**：相比于保守的基准控制器（APF），带有 CBF 的系统允许机器人更“激进”地接近边界，仿真显示探索面积从 56.35 $m^2$ 提升至 81.68 $m^2$。
*   **实时性强**：闭式解确保了在低功耗嵌入式设备上的实时运行，干预仅在必要时发生，对名义控制器的侵入性极小。

### 7. 优点
*   **计算高效**：通过解析解取代数值优化，非常适合算力受限的无人机或移动机器人。
*   **双重保障**：同时考虑了“已知障碍”和“未知区域”，解决了探索任务中常见的“盲目冒进”问题。
*   **通用性强**：作为速度空间的安全过滤器，它可以透明地包装任何名义控制器（包括基于强化学习的策略），无需修改底层算法。

### 8. 不足与局限
*   **维度限制**：目前仅在 **2D 平面**（固定高度）上实现，扩展到 3D 体积 SDF 会显著增加计算复杂度。
*   **静态环境假设**：算法假设环境是静态的，未考虑动态障碍物（如行人）的速度预测。
*   **感知依赖**：安全保证高度依赖于 SLAM 的准确性。如果 SLAM 发生漂移或地图更新延迟，CBF 可能会基于错误的信息做出决策。
*   **参数调优**：类-K 函数（增益 $\gamma$）的选取仍需要一定的经验调试，以平衡安全性和任务性能。

（完）
