---
title: "BarrierFormer: Transformer-Guided Predictive Barrier Enforcement for Safe Robot Control"
title_zh: BarrierFormer：用于安全机器人控制的 Transformer 引导预测性屏障约束实施
authors: "Anandsingh Chauhan, Kunal Garg"
date: 2026-09-20
pdf: "https://arxiv.org/pdf/2609.23896v1"
tags: ["query:profile-1"]
score: 9.0
evidence: 使用预测屏障函数的实时安全机器人控制
tldr: 针对标准控制屏障函数（CBF）的短视性以及模型预测控制（MPC）实时计算成本高的问题，本文提出了BarrierFormer框架。该框架利用因果Transformer预测未来轨迹，结合屏障批评器与安全教师监督，将时域CBF约束融入无模型策略的学习中，实现了无需在线优化的高效实时预测安全控制。
selection_source: fresh_fetch
motivation: 传统CBF方法因仅考虑当前时步而存在短视性，而基于MPC的方法计算昂贵且依赖已知模型，难以满足实时部署需求。
method: 提出BarrierFormer框架，结合因果Transformer预测未来轨迹并生成控制残差，利用屏障批评器评估时域内CBF违规情况，并通过安全教师提供屏障一致性动作进行直接监督。
result: 在2D和3D线性及非线性系统的安全导航评估中，BarrierFormer在安全率和推理延迟上均优于现有的RL、Diffusion、MPC及Transformer基线方法。
conclusion: BarrierFormer实现了无需已知模型和在线优化的预测性安全控制，显著提升了安全关键型机器人系统的实时控制性能。
---

## 摘要
控制屏障函数（CBF）已成为安全攸关的机器人技术中编码和实施状态约束最受欢迎的工具之一。标准的 CBF 方法本质上具有短视性，因为它们仅在当前时间步实施安全约束。因此，系统可能会被推向安全集的边界，导致在未来时间步中不存在可行的安全控制。基于模型预测控制（MPC）的方法通过在滚动时域内实施状态约束来解决这一问题。然而，此类方法通常需要已知模型，以便在每一步求解带约束的优化问题，这对于实时部署而言计算成本高昂。我们提出了 BarrierFormer，这是一种由屏障监督的 Transformer 框架，通过在学习无模型安全策略时引入轨迹展开（rollout）层级的 CBF 约束来解决上述局限性。因果 Transformer 编码观察-动作历史，通过动力学头自回归地生成预测性轨迹展开以替代模型，并通过动作头向标称控制器提供残差修正以替代在线计算。在局部观察上运行的屏障 Critic 评估该展开轨迹上的 CBF 约束违反情况，而安全指导者计算满足这些约束的屏障一致性动作，作为所学控制策略的直接监督目标。在推理阶段，该策略将观察-动作历史直接映射为控制动作，无需任何在线优化或模型知识，从而实现了实时的无模型预测性安全约束实施。在用于安全目标导向导航的线性与非线性、二维与三维动力学系统上的评估表明，BarrierFormer 在安全率和推理延迟方面均优于现有的基于强化学习（RL）、基于扩散模型、基于 MPC 以及基于 Transformer 的方法。

## Abstract
Control barrier functions (CBFs) have become one of the most popular tools for encoding and enforcing state constraints in safety-critical robotics. Standard CBF approaches are inherently myopic in nature as they enforce safety only at the current time step. Consequently, the system can be driven toward the boundary of the safe set where no feasible safe control exists at a future timestep. Model predictive control (MPC) based approaches address this by enforcing state constraints over a receding horizon. However, such approaches generally require the model to be known for solving a constrained optimization problem at every step, which is computationally expensive for real-time deployment. We propose BarrierFormer, a barrier-supervised transformer framework that addresses these limitations by encoding rollout-level CBF constraints in learning a model-free safe policy. A causal transformer encodes observation-action history, autoregressively generates a predictive rollout through the dynamics head to replace the model, and provides a residual correction to a nominal controller through the action head to replace the online computation. A barrier critic operating on local observations evaluates CBF constraint violations along this rollout, and a safety teacher computes barrier-consistent actions satisfying these constraints as direct supervision targets for the learned control policy. During inference, the policy maps observation-action history to control actions without any online optimization or model knowledge, enabling real-time model-free predictive safety enforcement. Evaluations across linear and nonlinear, 2D and 3D dynamical systems for safe goal-directed navigation demonstrate that BarrierFormer outperforms existing reinforcement learning (RL)-based, diffusion-based, MPC-based, and transformer-based approaches in safety rate and inference latency.

---

## 论文详细总结（自动生成）

以下是对论文 **《BarrierFormer: Transformer-Guided Predictive Barrier Enforcement for Safe Robot Control》** 的结构化深入总结：

---

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：在安全攸关的机器人导航中，控制屏障函数（CBF）常用于保证安全。但传统 CBF 属于**单步/逐点（pointwise）短视方法**，仅约束当前时间步，可能导致系统驶入未来无解的安全集边界；而基于模型预测控制（MPC-CBF）虽能进行多步预测，却依赖已知动力学模型，且需在线求解高复杂度优化问题，无法满足实时部署要求。此外，在局部感知（如有限距离 LiDAR）下，单帧观察难以评估轨迹长远的安全性。
*   **核心问题**：如何在**部分可观测**且**无已知模型**的前提下，实现类似 MPC 的**时域预测性安全约束**，同时做到**推理阶段零在线优化**，以满足高频实时控制需求？

---

### 2. 核心方法论（BarrierFormer 框架）
BarrierFormer 采用“训练时屏障监督、推理时纯前馈”的设计思想，将预测时域 CBF 约束直接内化到 Transformer 策略参数中。

*   **核心架构与流程**：
    1.  **输入与编码**：策略输入为长为 $T$ 的历史观察-动作序列 $H_t = (o_{t-T+1}, u_{t-T+1}, \dots, o_t)$，其中观察包含状态、目标相对位置和局部 LiDAR 点云。采用 GPT-2 风格的因果 Transformer 抽取时序特征 $z_t$。
    2.  **双头预测与自回归展开（Step 1）**：
        *   **动作头（Actor Head）**：输出对标称控制器 $\mu_{\text{nom}}$ 的残差修正 $\Delta u_t$，最终控制量 $u_t = \mu_{\text{nom}}(x_t) + \Delta u_t$。
        *   **动力学头（Dynamics Head）**：预测状态增量 $\Delta \hat{x}_t$，配合动作自回归地生成未来 $H$ 步的预测轨迹展开 $\{\hat{o}_{t+k|t}\}_{k=0}^H$。
    3.  **屏障评估与安全指导者（Step 2，仅训练阶段）**：
        *   **屏障 Critic ($h_\phi$)**：在局部观察上评估预测展开轨迹的离散时间 CBF（DTCBF）违反程度，利用 Log-Sum-Exp 导出平滑的时域最大违规量 $V(o_t)$。
        *   **SQP 安全教师（Safety Teacher）**：在训练期，通过序列二次规划（SQP）求解带时域 CBF 约束的优化问题，生成屏障一致的动作修正量 $\Delta u_t^*$，作为动作头的直接监督目标。
    4.  **损失函数设计**：
        策略由三部分联合训练：动作损失 $L_{\text{act}}$（监督残差动作）、动力学损失 $L_{\text{dyn}}$（监督状态增量）和屏障损失 $L_{\text{bar}}$（惩罚预测轨迹的违规量并进行安全/不安全分类）。
*   **部署推理**：推理阶段移除 SQP 教师和屏障 Critic， Transformer 直接从观察历史映射到控制动作，无需任何在线优化或动力学模型查询。

---

### 3. 实验设计
*   **测试环境/动力学系统**：
    1.  **Double Integrator**（2D 线性系统，4维状态，2维控制）
    2.  **Dubins Car**（2D 非线性非完整约束系统，4维状态，2维控制）
    3.  **Crazyflie Drone**（3D 非线性四旋翼无人机，12维状态，4维控制）
*   **评估指标**：安全率（Safe Rate）、到达目标率（Reach Rate）、成功率（Success Rate = 安全且到达）、单步推理延迟（Time/$\mu s$）。
*   **对比基线（Baselines）**：
    *   *基于强化学习*：**CBF-RL**（在训练中加入 CBF 滤波与奖励惩罚）
    *   *基于扩散模型*：**CoBL-Diffusion**（在扩散去噪过程中通过梯度投影注入 CBF）
    *   *基于 Transformer*：**ConBaT**（预测未来状态并在推理时执行轻量级梯度修正）
    *   *基于模型*：**MPC**（基于 CasADi/IPOPT 的在线 receding-horizon 优化）
    *   *本文变体*：**Ours (Sim)**（使用真实模拟器展开训练 Critic）与 **Ours (Dyn-Head)**（使用自研动力学头展开训练 Critic）。

---

### 4. 资源与算力
*   **硬件配置**：单台工作站，配备 **NVIDIA RTX 4000 Ada Generation GPU (20 GB)**，Intel Xeon w5-3423 CPU (12核, 4.2 GHz)，62 GiB 内存，系统为 Ubuntu 22.04。
*   **软件框架**：基于 **JAX** 和 Flax 神经网络库开发，优化器使用 AdamW。
*   **算力消耗**：文中详细记录了推理延迟评估方式（在 32 个并行环境中对 256 步轨迹取平均，包含 JAX JIT 预热），但未明确给出总体训练所消耗的总 GPU 小时数。

---

### 5. 实验数量与充分性
论文设计了极为全面且严谨的实验体系：
*   **主实验规模**：每个动力学系统在 3 个随机种子、32 个独立随机环境中评估，每个 Episode 256 步，统计均值与标准差。
*   **跨分布分布偏移 / 零样本泛化实验**：改变障碍物密度 $\rho \in \{0.50, 0.75, 1.00\}$ 及地图尺寸（2D 变至 $l=6$，3D 变至 $l=4$），评估出域（OOD）泛化性能。
*   **消融实验**：
    1.  预测时域长度 $H \in \{1, 3, 6, 9, 12\}$ 对安全率的影响（证明多步预测优于单步短视约束）。
    2.  动力学头预测误差（RMSE）评估：在 11 种不同输入信号（如 LQR、CBF、正弦、多项式、高斯噪声等）下，评估长达 32 步的自回归误差。
    3.  冻结主干网络消融：对比“预训练后冻结主干”与“全联合在线训练”的性能差异（验证了抗分布偏移的必要性）。
    4.  鲁棒性评估：评估策略在未见过的速度不匹配（$\alpha_{\text{vel}}$）和过程噪声（$\sigma$）下的表现。

---

### 6. 主要结论与发现
1.  **超越无模型基线**：BarrierFormer 的成功率和安全率在所有测试环境中均显著高于 CBF-RL、CoBL-Diffusion 和 ConBaT。
2.  **极低的推理延迟**：单步推理耗时仅约为 **$4.8 \sim 5.1 \space \mu s$**，相比基于 IPOPT 的经典 MPC（耗时 $2.7 \sim 9.2 \space ms$），推理速度提高了 **3 个数量级（>1000倍）**，且性能与 MPC 相当。
3.  **预测时域的作用**：消融实验证明，将约束引入 $H=6$ 的预测轨迹显著优于仅考虑 $H=1$ 单步 CBF 约束。
4.  **出色的零样本泛化**：仅在低密度、小地图训练的模型，直接部署到高密度、大范围未见过场景中仍保持了较高的安全率。

---

### 7. 优点（亮点）
*   **思想创新**：首次将 Transformer 自

回归状态-动作预测能力与控制屏障函数（CBF）在训练期深度融合，实现了“预测性安全约束”与“毫秒级纯前馈推理”的结合。
*   **端到端局部感知解法**：直接处理历史观察与局部 LiDAR 点云，消除了传统 MPC 依赖全局完美状态已知和精确已知物理模型的硬性假设。
*   **推理性能极佳**：通过“训练期 SQP 教师指导 + 运行期零在线优化”的机制，将计算重负转移到训练阶段，在微秒级时间内实现了接近 MPC 的安全控制表现。
*   **出色的域外泛化力**：在高度未知的场景密度、尺寸偏移以及过程噪声下，依然能保持远超传统 RL 滤波方法的鲁棒性。

---

### 8. 局限性与短板
*   **缺乏确定性的安全保证**：与数学上严谨的传统 CBF（拥有形式化安全证明）不同，BarrierFormer 的约束由神经网络拟合与评估，在极度罕见的分布外状态下存在非零的违反安全屏障风险（近似安全而非绝对保证）。
*   **离线/训练计算成本较高**：在训练阶段，需要通过自回归展开预测轨迹并调用 SQP 优化器生成安全动作监督，这大幅增加了训练阶段的算力和系统构建开销。
*   **缺少真机（Sim-to-Real）实测验证**：论文实验虽然涵盖了多自由度的 3D 无人机仿真，但所有评估均在 JAX 仿真环境中完成，未在物理机器人（如真实 Crazyflie 或移动机器人）上验证传感器噪声、非建模动力学及计算延迟的影响。
*   **长时域自回归漂移**：动力学头（Dynamics Head）虽然在 11 种信号测试中表现优异，但在时域视界 $H$ 设得过大时（如 $H > 12$），自回归误差的累积仍可能影响屏障评估的准确度。

---

### 9. 实际应用价值与行业影响
*   **高频边缘嵌入式部署**：微秒级（$\sim 5 \mu s$）的推理延迟使其非常适合部署在高频控制系统（如 $>200 \text{ Hz}$ 的四旋翼无人机飞控系统、高速自动驾驶避障）以及算力极其有限的边缘嵌入式芯片（如 Raspberry Pi、Jetson Nano 等）上。
*   **桥接控制理论与深度学习**：展示了一种通过“控制理论引导”训练 Transformer 的通用范式，即用复杂的优化控制器（如 MPC-CBF）作为离线/训练期教师，将其预测能力与安全特性蒸馏到大容量时序 Transformer 模型中。

---

### 10. 综合评价
**BarrierFormer** 为安全攸关的机器人控制开辟了新的思路。它成功突破了“单步 CBF 短视无远见”与“预测型 MPC 无法在线实时计算”的双重壁垒。通过巧妙的双头架构、预测展开屏障评估以及 SQP 教师蒸馏，该论文在保持高安全率和强泛化性的同时，将控制延迟降至极低的微秒量级，是端到端安全机器人控制领域一项极具实用前景的重要突破。

（完）
