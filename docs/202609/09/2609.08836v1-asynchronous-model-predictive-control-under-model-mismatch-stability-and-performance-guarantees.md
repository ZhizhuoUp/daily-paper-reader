---
title: "Asynchronous Model Predictive Control Under Model Mismatch: Stability and Performance Guarantees"
title_zh: 模型失配下的异步模型预测控制：稳定性与性能保证
authors: "Changrui Liu, Anil Alan, Shengling Shi, Bart De Schutter"
date: 2026-09-08
pdf: "https://arxiv.org/pdf/2609.08836v1"
tags: ["query:q2"]
score: 9.0
evidence: 非线性系统的异步模型预测控制
tldr: 本文研究了存在模型失配的非线性系统在异步反馈下的确定性等价模型预测控制（CE-MPC）。通过引入区间分析与价值函数扰动理论，推导了异步执行下的稳定性条件与竞争比界限，量化了预测时域、反馈频率与模型精度之间的权衡，为异步MPC的设计提供了理论支撑。
selection_source: fresh_fetch
motivation: 现有异步反馈下的CE-MPC缺乏针对模型失配的系统性稳定性与性能理论保障。
method: 通过价值函数扰动分析，结合非平滑模型失配与异步执行间隔，推导了通用的稳定性判据与竞争比界限。
result: 建立了预测时域、执行间隔与不确定性幅度之间的定量关系，明确了性能退化边界。
conclusion: 该研究为利用近似或学习模型进行异步MPC设计提供了明确的性能权衡指导。
---

## 摘要
确定性等价模型预测控制（CE-MPC）因其简单高效而被广泛应用，但其在异步反馈下的理论保证仍然有限。本文为输入受限非线性系统的异步 CE-MPC 建立了稳定性与性能保证。我们首先推导了一个名义稳定性条件和竞争比界限，在不预设反馈机制的情况下明确考虑了执行间隔。随后，针对二次阶段代价的值函数扰动分析，在无需约束规范条件的情况下，处理了加性且可能非光滑的模型失配问题。结合这些结果，我们得出了通用异步反馈（包括事件触发/自触发和多步 MPC）下 CE-MPC 的稳定性准则和竞争比界限。这些保证明确了预测时域、执行间隔时间和不确定性幅度之间的关系，量化了相对于理想无限时域控制器的性能退化。这些结果阐明了反馈频率、模型精度和时域长度之间的权衡，为使用近似或学习模型进行异步 MPC 设计提供了指导。

## Abstract
Certainty-equivalence model predictive control (CE-MPC) is widely used for its simplicity and efficiency, but theoretical guarantees under asynchronous feedback remain limited. This paper establishes stability and performance guarantees for asynchronous CE-MPC of input-constrained nonlinear systems. We first derive a nominal stability condition and competitive-ratio bound that explicitly account for inter-execution intervals without prescribing a feedback mechanism. A value-function perturbation analysis for quadratic stage costs then accommodates additive, potentially non-smooth model mismatch without constraint qualification conditions. Combining these results yields stability criteria and competitive-ratio bounds for CE-MPC under general asynchronous feedback, including event/self-triggered and multi-step MPC. The guarantees explicitly relate prediction horizon, inter-execution time, and uncertainty magnitude, quantifying performance degradation relative to an ideal infinite-horizon controller. These results clarify tradeoffs between feedback frequency, model accuracy, and horizon length, guiding asynchronous MPC design using approximate or learned models.

---

## 论文详细总结（自动生成）

这是一份关于论文《Asynchronous Model Predictive Control Under Model Mismatch: Stability and Performance Guarantees》的结构化总结：

### 1. 核心问题与研究动机
*   **背景**：模型预测控制（MPC）在机器人、电力电子等领域应用广泛，但传统的高频时间触发反馈计算成本高。异步反馈（如事件触发、自触发、多步MPC）能有效降低计算与通信开销。
*   **痛点**：在实际应用中，由于模型失配（Plant-Model Mismatch）不可避免，且异步反馈的非规则性进一步加剧了系统稳定性和性能分析的难度。现有研究多针对完美模型或特定触发规则，缺乏一个统一的、能明确量化预测时域、反馈频率与模型精度之间权衡的理论框架。

### 2. 方法论
*   **核心思想**：采用确定性等价（CE-MPC）框架，通过价值函数扰动分析（Value-function Perturbation Analysis），将异步执行间隔与模型失配的影响解耦并量化。
*   **关键技术**：
    *   **名义稳定性分析**：定义了稳定性指标 $\delta_{N,M}$，量化了预测时域 $N$ 和最大执行间隔 $M$ 对稳定性的影响。
    *   **扰动分析**：利用“一致误差匹配”（Consistent Error Matching）属性，处理非光滑模型失配。
    *   **竞争比界限（Competitive-ratio Bound）**：推导了 $R^* \ge \frac{\text{异步CE-MPC代价}}{\text{理想无限时域最优代价}}$，该界限显式依赖于模型失配水平 $\epsilon_f$、预测时域 $N$ 和最大执行间隔 $M$。
    *   **分析流水线**：构建了从基础引理（状态扰动、误差匹配）到扰动分析（值函数扰动），再到最终稳定性与性能保证的完整理论框架。

### 3. 实验设计
*   **场景**：针对一个二阶离散非线性系统（包含正弦项和非光滑项），模拟存在时间变化模型失配的情况。
*   **Benchmark**：对比了理想无限时域最优控制（IHOPC，通过混合整数规划求解）。
*   **方法**：评估了两种异步执行场景：(1) 随机执行间隔；(2) 固定执行间隔。

### 4. 资源与算力
*   **说明**：论文未明确提及具体的算力消耗（如 GPU 型号或训练时长）。仅提到仿真在 Python 3.13.11 环境下运行，优化问题分别使用 Gurobi 和 CasADi (IPOPT) 求解。

### 5. 实验数量与充分性
*   **实验量**：进行了两组主要实验：(1) 改变最大执行间隔 $M$；(2) 改变模型失配水平 $\epsilon_f$。每组实验包含 100 次蒙特卡洛模拟，并覆盖了 4 个不同的初始状态。
*   **评价**：实验设计客观，通过对比理论界限与实际闭环性能，验证了理论结论的有效性。实验充分性较好，涵盖了随机与固定两种异步模式，能够支撑论文的理论主张。

### 6. 主要结论与发现
*   **性能权衡**：更长的预测时域和更频繁的反馈（较小的 $M$）能有效减小次优性差距。
*   **稳定性边界**：模型失配会显著缩小稳定性裕度，且随着 $M$ 的增大，系统对模型精度的要求更高。
*   **理论一致性**：当模型失配为零且 $M=1$ 时，该理论框架能退化回标准的同步 MPC 结果，证明了其通用性。

### 7. 优点
*   **通用性强**：不依赖于特定的触发机制（如特定的事件触发规则），适用于多种异步反馈设计。
*   **无需约束规范**：在处理非光滑模型失配时，避开了复杂的约束规范条件（Constraint Qualification）。
*   **指导意义**：提供了一个可计算的竞争比界限，为工程实践中选择预测时域和反馈频率提供了量化依据。

### 8. 不足与局限
*   **保守性**：作为一种最坏情况（Worst-case）的理论分析，其性能界限通常比实际观测到的性能更为保守。
*   **稳定性条件**：所给出的稳定性条件 $\rho_{N,M}(\epsilon_f) < 1$ 是充分条件而非必要条件，可能在某些场景下过于严苛。
*   **应用限制**：假设模型失配不改变系统的平衡点，这在某些强非线性或复杂动力学系统中可能难以满足。

（完）
