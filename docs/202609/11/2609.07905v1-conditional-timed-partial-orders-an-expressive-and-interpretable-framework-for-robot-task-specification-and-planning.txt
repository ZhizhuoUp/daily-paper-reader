Title: Conditional Timed Partial Orders: An Expressive and Interpretable Framework for Robot Task Specification and Planning

URL Source: https://arxiv.org/pdf/2609.07905v1

Published Time: Wed, 09 Sep 2026 02:45:56 GMT

Number of Pages: 9

Markdown Content:
# Conditional Timed Partial Orders: An Expressive and Interpretable Framework for Robot Task Specification and Planning 

Sebastian Escobar and Morteza Lahijanian 

Abstract — Timed Partial Orders (TPOs), originally pro-posed for workflows, provide an interpretable framework for robot task specification with planning algorithms based on mixed-integer linear programming (MILP). However, TPOs are limited in expressivity, capturing only partial-order events with simple timing constraints. In this paper, we introduce Conditional TPOs (cTPOs), which extend TPOs with richer relative-timing constraints and conditional event activations based on environmental conditions. We show that planning for cTPOs also reduces to an MILP problem; however, the added expressivity results in significantly larger MILPs that can become computationally intractable. To address this challenge, we propose a decomposition algorithm that partitions a cTPO into smaller sub-TPOs, yielding a sequence of smaller MILP problems. We prove that this decomposition is complete and preserves plan optimality while improving the interpretability of complex tasks. Experimental results demonstrate the effec-tiveness of cTPOs as a task specification framework and the efficiency of our decomposition approach, achieving up to four orders of magnitude speedup over the monolithic MILP. 

I. I NTRODUCTION 

Robotic autonomy increasingly requires reasoning at the mission level: robots must decide not only how to move, but 

what to do, when to do it, and under which conditions tasks become required. Missions in domains such as healthcare, aerospace, and field robotics are structured by precedence, deadlines, and conditional obligations. However, specifica-tion languages capable of representing such requirements must balance competing goals of expressivity , human inter-pretability , formal rigor , and computational tractability [1], [2]. In this work, we seek to improve this balance by extending an existing interpretable specification language while retaining formal guarantees and efficient planning. In recent years, there has been growing interest in using natural language to specify robotic tasks and missions [3], [4]. Advances in LLMs have further enabled the generation of plans directly from such specifications. These approaches offer substantial expressivity and human interpretability. However, LLM-based methods generally lack explicit formal semantics and, when used directly for planning, provide no guarantees of correctness and completeness [5]. A widely used class of formal specification languages in robotics is temporal logic (TL) [1], including timed variants Metric/Signal TL (M/S-TL), which explicitly capture timing requirements [6], [7]. These specifications admit rigorous planning methods based on timed-automata-theoretic [8], [9] or optimization-based reasoning [1], namely Mixed Integer Linear Program (MILP) [10]. While expressive and formal,  

> Authors are with the Aerospace Eng. Sci. Dept. at University of Colorado Boulder, CO, USA

{firstname.lastname }@colorado.edu 

these specifications require domain expertise and familiarity with logical formulas, limiting their interpretability for un-trained users. Also, the expressiveness of M/S-TL comes at a significant computational cost, making planning challenging. A recent specification language, originally introduced for workflows, is Timed Partial Orders (TPOs) [11], which provide a better balance among the aforementioned four competing objectives. TPOs represent missions as directed acyclic graphs (DAGs), where edges encode precedence relations and clocks impose timing constraints between events (e.g., see Fig. 1). This graphical representation allows designers to interpret and modify task requirements directly, while retaining precise formal semantics. Moreover, planning under a TPO specification can be formulated as a variation of Generalized Traveling Salesman Problem [12]–[14] and solved using MILP [15], avoiding expensive construction of timed automata [8]. Nevertheless, existing TPOs have two key expressiveness limitations: (i) timing constraints are re-stricted to precedence-ordered events, and (ii) lack of support for conditional subtasks that depend on environmental events. In this work, we address both gaps with conditional TPOs (cTPOs). cTPOs extend TPOs with generalized clock-difference guards relating arbitrary pairs of events and propo-sitional activation conditions that conditionally enforce tasks and timing constraints based on the realized trajectory, in-creasing expressivity. Then, we show that the MILP formula-tion of [15] can be extended to plan for cTPO specifications. To mitigate the increased computational cost of this added expressivity, we introduce sub-TPO decomposition , which partitions a cTPO into smaller sub-TPOs, yielding a sequence of smaller MILP problems. We prove that our decomposition algorithm is complete and preserves plan optimality, while substantially reducing computation time and improving inter-pretability by exposing the modular structure of the mission specification. Our case studies demonstrate the expressivity and interpretability of cTPOs and benchmarks show that decomposition-based planning achieves speedups of up to four orders of magnitude. In short, our contributions are fourfold: (i) we introduce cTPOs, extending TPOs with timing constraints between arbitrary events and conditional tasks; (ii) we formulate cTPO planning as an MILP; (iii) we develop a complete, optimality-preserving decomposition into sequentially solved sub-TPOs; and (iv) we demonstrate cTPO expressivity and interpretability on robotic missions and show up to four orders of magnitude speedup over the monolithic MILP. 1arXiv:2609.07905v1 [cs.RO] 7 Sep 2026 II. TPO S & P LANNING PROBLEM 

We consider complex robotic tasks defined over a set of events with temporal requirements. A formalism that provides easily interpretable visual representations of such tasks is TPOs [11]. Here, we first formally define TPOs and discuss their limitations in expressing complex tasks. We then introduce Conditional TPOs (cTPOs) to address these limitations and finally formulate the cTPO planning problem. 

A. Timed Partial Order (TPO) 

TPOs use partial-order relations together with clocks to capture temporal requirements over events. 

Definition 1 (TPO [11]) . A Timed Partial Order (TPO) is a tuple ϕ = (Π , ≺, C, GC , G, R ), where  

> •

Π = {e1, . . . , e n} is a finite set of events;  

> •

≺⊆ Π × Π is a strict partial order over events in Π; 

> •

C = {c1, . . . , c m} is a finite set of clocks;  

> •

GC =  V  

> i∈I

(ci ▷◁ t i) | I ⊆ { 1, . . . , |C|} , ▷◁ ∈ { <, ≤, ≥, > }, t i ∈ R≥0 is the set of timing constraints (guards), expressed as conjunctions of clock bounds;  

> •

G : Π → G C assigns to an event e ∈ Π a timing constraint (guard condition) G(e) ∈ G C ; and  

> •

R : Π → 2C is the reset map that, upon execution of an event e, resets clocks R(e) ⊆ C.

A TPO can be represented as a Directed Acyclic Graph (DAG) whose edges encode the cover relation of ≺.

Example 1 (Hospital Robot Workflow) . Consider a hospital robot tasked with collecting diagnostic results and delivering them to medical staff. The task is: first collect the triage report ( e1); then perform the blood draw ( e2) and imaging scan (e3) in any order; finally deliver the results to aphysician ( e4) and the patient file to a social worker ( e5)in any order. The blood draw e2 and physician delivery e4

must occur within 15 and 60 minutes, respectively, of e1.The precedence relations are e1 ≺ e2, e1 ≺ e3, e2 ≺

e4, e3 ≺ e4, e2 ≺ e5, and e3 ≺ e5. Timing constraints use one clock C = {c1}, reset at e1 (R(e1) = {c1}), with guards G(e2) = ( c1 ≤ 15) and G(e4) = ( c1 ≤ 60) . This TPO is shown as the black DAG with black clock and guard annotations in Fig. 1. 

A timed trace τ = ( σ1, t (1) ) . . . (σn, t (n)) is a sequence of event-timestamp pairs, where each σi ∈ Π is distinct and 

t(i) ∈ R≥0. A valid execution of a TPO is a timed trace that respects ≺ and timing constraints. To reason about the latter, we need to track clock readings, which we do via a 

valuation function ν : C → R≥0. This is formalized below. 

Definition 2 (TPO Semantics [15]) . A run of TPO ϕ is a sequence ρ = ( σ1, t (1) , ν 1) . . . (σn, t (n), ν n), where each 

σi ∈ Π is distinct and each νj is a clock valuation, satisfying:  

> •

timestamps are non-decreasing, i.e., t(1) ≤ · · · ≤ t(n); 

> •

σ1, . . . , σ n is a linearization of ≺; 

> •

just before σj fires (is executed), time duration ∆t(j) =

t(j) − t(j−1) elapses, advancing each clock c ∈ C by 

e1: triage 

e2: blood draw 

e3: imaging e4: physician 

e5: social worker 

e6: ECG pickup e7: cardiology delivery              

> pc
> pc
> c1:= 0
> c2:= 0
> c3:= 0
> c6:= 0
> c1≤15
> c1≤60
> ∧ | c2−c3| ≤ 20
> c6≤15

Fig. 1: TPO and cTPO specifications for the Hospital Robot exam-ples. The TPO shown in black captures the (original) requirements in Example 1. It is extended to a cTPO by adding the purple clocks and guards to represent the additional requirement R1, and the bottom orange branch to capture requirement R2. 

∆t(j). Then, the resulting valuation (ν ⊕ ∆t)( c) =

ν(c) + ∆ t must satisfy the guard G(σj ); and  

> •

after σj fires, the clocks in R(σj ) are reset to zero while all others are unchanged, giving νj = reset (νj−1 ⊕

∆t(j), R (σj )) , where reset (ν, ˆC)( c) = 0 if c ∈ ˆC,otherwise ν(c).A timed trace τ = ( σ1, t (1) ) . . . (σn, t (n)) is compatible with 

ϕ, denoted τ |= ϕ, if there exists a corresponding run. 

An example of a compatible timed trace with the TPO in Fig. 1 is τ0 = ( e1, 0)( e2, 10)( e3, 20)( e4, 30)( e5, 40) .While expressive and interpretable, TPOs in Def. 1 have two major limitations: restricted guard conditions and lack of support for environmental conditions. 

B. Conditional TPOs with Complex Guards 

TPOs in Def. 1 only allow guard conditions with single-clock bounds (see GC in Def. 1) and cannot express events triggered by external environmental conditions. For instance, in Example 1, additional requirements such as 

R1: “Blood draw e2 and imaging e3 must be done within 20 minutes of each other. ”

R2: “If the robot happens to pass near the Cardiac Wing, it should also pick up ECG report ( e6) and deliver it to cardiologist ( e7) within 15 minutes. ”cannot be expressed. We extend TPOs to support such expressions, enriching the properties they can represent while maintaining their strong interpretability. These extensions, however, have consequences in computation, on which we elaborate in Sec. III-B and address in Sec. III-C. First, we generalize the guard condition GC to allow richer, 

precedence-free constraints. Specifically, each guard may also be a conjunction of sums of clock differences, i.e., 

G′ 

> C

= GC ∪

n N^

> k=1

X  

> ik̸=jk∈Ik

(cik − cjk ) ▷◁ t k | N ≤ 2|C|,Ik ⊆ { 1, . . . , |C|} , t k ∈ R≥0

o

. (1) This enables timing constraints between arbitrary pairs of events, regardless of their precedence relation. Note that G′

> C

allows for conditions such as |c2 − c3| ≤ 20 , which precisely captures the additional requirement R1 for Example 1 by 2extending clocks to C = {c1, c 2, c 3}, as shown in Fig. 1 (purple clocks and guards). The second extension is motivated by the fact that real robotic tasks rarely unfold as a single fixed task structure. Environmental features encountered along the way (e.g., a geologically significant outcrop, or an area triggering a safety protocol) may impose additional tasks with their own precedence and timing requirements, e.g., R2 for Example 1. To capture such behavior, we associate events with logical conditions over the environment. Let AP = {p1, . . . , p r } be a finite set of atomic propositions (predicates), where each pi

evaluates to true when the robot’s trajectory satisfies an as-sociated environmental condition (e.g., entering a designated area). Furthermore, let F(AP ) denote the set of all Boolean (propositional) formulas over AP formed using ¬ (negation), 

∧ (conjunction), and ∨ (disjunction). We allow each event in Π to be triggered by a formula in F(AP ). Intuitively, an event and its timing and precedence constraints are triggered in the task structure of TPO only when its associated formula evaluates to true. Otherwise, the event and its precedence and temporal constraints are ignored. We formalize this extension as a Conditional TPO (cTPO). 

Definition 3 (cTPOs) . A Conditional TPO (cTPO) is a tuple 

ϕc = ( ϕ, AP, α ) where ϕ is a TPO in Def. 1 with guard condition G′ 

> C

in (1) , AP is a finite set of atomic propositions, and α : Π → F (AP ) is an activation function that assigns, to each event e ∈ Π, a propositional condition (Boolean formula) α(e) ∈ F (AP ) over AP .

When α(e) evaluates to true, i.e., α(e) ≡ ⊤ , event e and its associated precedence constraints, guard conditions, and clock resets are enforced; otherwise, e is not required. Note that cTPO ϕc recovers TPO ϕ if α(e) = ⊤ for all e ∈ Π.The TPO in Fig. 1 with the bottom orange branch is a cTPO for Example 1 with R1 and R2 requirements, where 

Π = {e1, . . . , e 7}, AP = {pc}, and α(ei) = pc if i ∈ { 6, 7}

and α(ei) = ⊤ if i ∈ { 1, . . . , 5} (not shown in the figure). For each evaluation of the propositions in AP , a cTPO induces a TPO obtained by restricting to the events whose activation conditions evaluate to true. Hence, cTPO seman-tics are inherited from Def. 2 on this induced substructure. 

Definition 4 (cTPO Semantics) . Let ω : AP → {⊥ , ⊤} 

be a valuation of atomic propositions in AP . The active event set induced by ω is Πω := {e ∈ Π | α(e) ≡⊤ under ω}, and active suborder induced by ω is ≺ω := 

≺ ∩ (Π ω × Πω ). A run of a cTPO ϕc under ω is a sequence 

ρ = (σ1, t (1) , ν 1) . . . (σk, t (k), ν k), where k = |Πω | and 

σi ∈ Πω , satisfying the conditions in Def. 2 with ≺ replaced by ≺ω . A timed trace τ is compatible with ϕc under ω,denoted τ |= ϕc, if there exists a corresponding run. 

An example of a compatible timed trace that satisfies the cTPO in Fig. 1 (Example 1) under valuation ω(pc) = ⊤ is 

τ1 = ( e1, 0)( e2, 10)( e3, 20)( e4, 30)( e5, 40)( e6, 50)( e7, 60) .

C. Robot Model 

Similar to [15], we consider a robot whose evolution in the environment is modeled as a Deterministic Transition System (DTS), which is a common abstraction in the literature [1], [16], [17]. 

Definition 5 (DTS) . A deterministic transition system (DTS) is a tuple T = ( X, x 0, A, δ, ∆, Π, L Π, AP, L AP ), where  

> •

X is a finite set of states,  

> •

x0 ∈ X is the initial state,  

> •

A is a finite set of actions,  

> •

δ : X × A → X is the (partial) transition function,  

> •

∆ : X × A → R≥0 is the transition duration function,  

> •

Π is a finite set of events,  

> •

LΠ : X → Π ∪ {∅} is an event labeling function that maps each state to an event with L(x0) = ∅, 

> •

AP = {p1, . . . , p r } is a finite set of atomic proposi-tions, and  

> •

LAP : X → 2AP is a proposition labeling function that maps each state to the atomic propositions it satisfies. 

A plan γ = γ0γ1 . . . γ n−1 with γi ∈ A is a sequence of actions. A plan γ is valid if it generates a trajectory sγ =

sγ 

> 0

sγ 

> 1

. . . s γn such that sγ 

> 0

= x0 and sγi+1 = δ(sγi , γ i). The set of all valid plans is denoted Γ. The duration of sγ is defined as the sum of the transition durations along the trajectory, i.e., D(sγ ) = Pn−1 

> i=0

∆( sγi , γ i).A valid plan γ induces a timed trace τ γ =(L(sγ 

> 0

), t 0) . . . (L(sγn), t n), where ti = D(sγ 

> 0

. . . s γi ). We say plan γ satisfies a TPO ϕ defined over events Π if τ γ |= ϕ

per Def. 2. For a cTPO ϕc = ( ϕ, AP, α ), we first define valuation ω for every pk ∈ AP to be 

ω(pk) := 

(

⊤ if pk ∈ LAP (sγi ) for some i

⊥ otherwise. (2) Then, we say γ satisfies ϕc if τ γ is compatible with ϕc under 

ω per Def. 4. Our goal is to compute a minimum-duration plan that satisfies a given cTPO specification, as formalized below. 

Problem 1 (cTPO Plan Synthesis) . Given a robot as a DTS T in Def. 5 and a cTPO task ϕc as in Def. 3, synthesize a valid plan γ∗ ∈ Γ that generates a minimum-duration trajectory satisfying ϕc, i.e., γ∗ =arg min γ∈Γ D(sγ ) subject to τ γ |= ϕc.

III. A PPROACH 

Our approach to Problem 1 proceeds in three stages. First, we express cTPO compatibility in terms of event timestamps, reducing the problem to a Generalized Traveling Salesman Problem with Time Windows and Precedence Re-lations (GTSP-TWPR) [11], [15], [18] (Sec. III-A). Second, we solve the resulting problem exactly via an MILP that extends [15] with proposition tracking and conditional acti-vation constraints (Sec. III-B). Third, we improve scalability through a sub-TPO decomposition that solves self-contained cTPO substructures as smaller MILPs and composes their solutions into a globally optimal plan (Sec. III-C). 3A. Reduction to GTSP-TWPR 

Whether a plan satisfies a cTPO depends only on which events are active, which event-labeled states are visited, in what order, and at what times. Problem 1 thus reduces to a routing problem: select one location per required event, order the visits, and schedule each to satisfy the timing constraints. To formulate these scheduling bounds, the timing constraints in G are expressed directly over visit timestamps. Since clocks reset only at events, the value of any clock when an event fires is simply the elapsed time since its most recent reset, i.e., the difference of two event timestamps. Substituting this into every guard conjunct rewrites each tim-ing requirement as a linear inequality over visit timestamps, yielding the following characterization. 

Proposition 1 (Timestamp Characterization) . Let ϕc be a cTPO and ω : AP → {⊥ , ⊤} a fixed valuation. The guard conditions of a run of ϕc under ω are satisfied iff there exist constants ai, b i, ℓ ij , u ij , f kl , w kl ∈ R≥0 ∪ {∞} determined by G and R such that 

ti ∈ [ai, b i] ∀ei ∈ Πω , (3a) 

tj − ti ∈ [ℓij , u ij ] ∀ei, e j ∈ Πω s.t. ei ≺ω ej , (3b) 

tl − tk ∈ [fkl , w kl ] ∀(ek, e l) ∈ Πω×Πω coupled by a clock-difference of G′ 

> C

in (1) (3c) 

where ti ∈ R≥0 is the timestamp at which event ei fires. 

Condition (3a) is an absolute time window per event, derived from guards whose clocks reset at mission start; (3b) is a relative time window between precedence-ordered pairs, derived from guards whose clocks reset at the earlier event; and (3c) is a precedence-free relative time window between clock-coupled unordered pairs, the novel case admitted by 

G′ 

> C

in (1). These three families correspond one-to-one to the scheduling constraints of a GTSP-TWPR. With this reduction, the objective becomes finding a tour of the GTSP-TWPR graph that visits one vertex per required event and satisfies the ordering given by ≺ as well as the conditions in (3a)-(3c). Below, we first formulate this GTSP-TWPR by extending the one in [15], and then show its construction from the robot DTS and cTPO ϕc.

Definition 6 (GTSP-TWPR) . Let G = ( V, E ) be a weighted directed graph with depot v0 ∈ V , and denote by di ∈ R≥0

the dwell cost of (time spent at) vi ∈ V and by dij ∈ R≥0 the travel cost (time) of edge (vi, v j ) ∈ E. The nodes in V \{ v0}

are partitioned into disjoint sets (groups) V1, . . . , V m, each designated active or inactive . GTSP-TWPR imposes a partial order over the groups, a time window [ai, b i] constraint over each Vi, and relative time window [ℓij , u ij ] over coupled pairs (Vi, V j ). Let ti denote the visit time of a node in the active set Vi. Then, a satisfying tour starts and ends at v0,visits exactly one node from each active group, and minimizes the makespan subject to:  

> •

time windows: ti ∈ [ai, b i] for each active group Vi; 

> •

precedence constraints : ti ≤ tj for each ordered pair of active groups (Vi, V j ); and     

> v0v1
> v2
> v3
> v4
> v5
> v6v7vpcsub-TPO ϕs

Fig. 2: GTSP-TWPR G for the Hospital Robot in Example 1 with cTPO in Fig. 1. v0 is the depot, v1, . . . , v 7 are event nodes, and 

vpc is the (cardiac-wing) proposition node.  

> •

relative time windows : tj − ti ∈ [ℓij , u ij ] for each coupled pair of active groups (Vi, V j ).

An example of G for the robot in Example 1 is shown in Fig. 2. We now show how to construct a GTSP-TWPR graph 

G for a given ϕc using DTS T . Nodes in V and edges in E

correspond to states and transitions of T that are relevant to 

ϕc, and each node groups Vi is associated with an event in 

Π or proposition in AP , as detailed below. 

Nodes: We associate each v ∈ V with x ∈ X whose label is non-empty, and the depot v0 = x0, i.e., 

V = {x ∈ X | x = x0 or LΠ(x)̸ = ∅ or LAP (x)̸ = ∅} . (4) Event group Vli = {x ∈ X | LΠ(x) = ei} is a set of states with label ei. Proposition group Vℓk = {x ∈ X |

pk ∈ LAP (x), L Π(x) = ∅} is the set of states whose proposition label includes pk and event label is empty. Note that Vl1 , . . . , V l|Π| , ∪|AP | 

> k=1

Vℓk constitute a partition of V \{ v0}.

Edges: Edges E encode which transitions between nodes are permitted, and are determined jointly by the precedence structure of ϕc and the reachability in T . Let ΠI

> ≺

, ΠF 

> ≺

⊆

Π denote the sets of events with no predecessors and no successors in ≺, respectively. Then, E is defined as follows.                                                               

> Source Edge Target Condition
> 1v0→vj∈Vliei∈ΠI
> ≺
> 2vi∈Vli→v0ei∈ΠF
> ≺
> 3vi∈Vli→vj∈Vljei≺ej
> 4vi∈Vli↔vj∈Vljei̸≺ej, e j̸≺ei
> 5vi∈Vℓk↔vj∈Vlj∀k, j
> 6vi∈Vℓk↔vj∈Vℓk′∀k̸=k′

Rows 1-2 restrict the depot to events in ΠI 

> ≺

and ΠF

> ≺

: the tour must begin at an event with no required predecessors and end at one with no required successors. Row 3 adds a directed edge for each precedence-ordered pair, carrying a relative time window from (3b) when applicable. Row 4 adds bidirectional edges between events unordered under 

≺, carrying a time window from (3c) when applicable. Rows 5-6 connect proposition nodes to all event and propo-sition groups, enabling proposition detection along any path through the graph. Note that, with this construction, directed edges enforce precedence order ≺ of ϕc.4Costs: Travel costs dij between any two nodes vi, v j ∈ V

are the shortest-path durations computed via graph search (e.g., A* and Floyd-Warshall algorithm) over all states in X

of T . The dwell cost is di = ∆( xi, a ) if vi has a self-loop action a ∈ A, else di = 0 .This construction of G encodes precedence order of ϕc,but the timing windows from Proposition 1 and proposition tracking must still be enforced. We do this through an MILP. 

B. MILP Formulation 

With the GTSP-TWPR instance G in hand, we solve it ex-actly via an MILP, which simultaneously selects a minimum-makespan tour, infers which propositions hold along it, and enforces timing and coverage constraints only for the resulting active events. The complete formulation is given in Fig. 3. Constraints (5a)–(5f) and (5h)–(5i), together with the routing variables yij and visit-time variables ti, extend the MILP of [15]; the novel contributions of this work are the precedence-free timing constraints (5g), the proposition and activation variables zk, χ e, and the proposition-tracking and conditional-activation constraints (5j)–(5o), described below. 

Variables. Let n = |Π|, N = |V |, and r = |AP |. Further, for any positive integer k, let [k] = {1, . . . , k }. Then, [N ]

indexes all non-depot nodes (including proposition nodes), with P ⊆ [N ] the subset of proposition node indices. The routing variables are yij ∈ { 0, 1}, indicating whether edge 

(vi, v j ) ∈ E is traversed, and ti ≥ 0, the visit time at node 

vi, making tN +1 ≥ 0 the makespan. 

Proposition and activation variables. For each proposi-tion pk ∈ AP , the binary variable zk ∈ { 0, 1} is inferred from the tour: zk = 1 iff the tour passes through some node in Vℓk . Let Z = {z1, . . . , z r }, and Πc ⊆ Π denote the conditional events . For each e ∈ Πc, we introduce a single binary variable χe ∈ { 0, 1}, obtained by linearizing 

α(e) over Z such that χe = 1 iff α(e) ≡ ⊤ (see Table I). In Example 1, if the tour passes through the cardiac wing (Vℓpc ), then zpc = 1 , which sets χe6 = χe7 = 1 and activates the ECG pickup e6 and delivery e7.

Constraints. Flow conservation (5c) enforces tour con-tinuity. Each active event group must be visited exactly once (5d)-(5e), while proposition nodes carry no coverage constraint and may be traversed freely. The timing windows from Proposition 1 are enforced as linear inequalities on visit times: (5f) encodes bounds from (3b) and (5g) those from (3c). Constraint (5h) ensures that if edge (vi, v j ) is traversed, the visit time at vj accounts for travel and dwell at 

vi. The makespan is bounded by (5i), which may be omitted if the robot need not return to the depot, relaxing (5c) at v0.The novel constraints and variables couple routing and activation: the tour determines which propositions hold; this in turn determines the conditional events that are active and what timing they must satisfy, all resolved within a single solve. Specifically, proposition tracking (5k)-(5l) infers zk

from the tour; conditional coverage (5m) ties event group visitation to χel ; and conditional timing (5n)-(5o) enforces timing windows only for active events. Table I details how χe                                                                                                                                                                  

> min tN+1
> yij ∈ { 0,1}∀(i, j )∈E(5a)
> ti≥0∀i∈ { 0} ∪ [N+ 1] (5b)
> X
> i
> yij −X
> k
> yjk = 0 ∀j∈ { 0} ∪ [N](5c)
> X
> vlj∈Vl
> X
> i
> yij = 1 ∀l∈[n], e l/∈Πc, j ̸∈ P (5d)
> X
> vlj∈Vl
> X
> k
> yjk = 1 ∀l∈[n], e l/∈Πc, j ̸∈ P (5e)
> tj−ti∈[ℓij , u ij ]∀(ei, e j)∈(3b) , e i, e j/∈Πc(5f)
> tl−tk∈[fkl , w kl ]∀(ek, e l)∈(3c) , e k, e l/∈Πc(5g)
> yij =1 ⇒tj−ti≥dij +dj∀i∈ { 0} ∪ [N],∀j∈[N]\ P (5h)
> yij = 1 ⇒tN+1 −ti≥dij ∀i∈[N], j = 0 (5i)
> zk, χ el∈ { 0,1}∀k∈[r],∀el∈Πc(5j)
> zk≥yij ∀k∈[r],(i, j )∈Ek(5k)
> zk≤P
> (i,j )∈Ekyij ∀k∈[r](5l)
> X
> vlj∈Vl
> X
> i
> yij =χel∀el∈Πc(5m)
> χei= 1 ⇒tj−ti∈[ℓij , u ij ]∀(ei, e j)∈(3b) , e i∈Πc(5n)
> χek= 1 ⇒tl−tk∈[fkl , w kl ]∀(ek, e l)∈(3c) , e k∈Πc(5o)

Fig. 3: MILP formulation, where el is an event of group Vl, with activation indicator χel when el ∈ Πc, Vℓk is the proposition group for pk , and Ek = {(i, j ) ∈ E | pk ∈ LAP (xi) or pk ∈ LAP (xj )}.

TABLE I: Linearization of formula α(e) into binary constraints relating the activation indicator χe to prop. variables zki , z kj ∈ Z.

Formula α(e) Linearization of χe

pki χe = zki

¬pki χe = 1 − zki

pki ∧ pkj χe ≤ zki , χ e ≤ zkj , χ e ≥ zki + zkj − 1

pki ∨ pkj χe ≥ zki , χ e ≥ zkj , χ e ≤ zki + zkj

is computed from Z following standard MILP linearization of Boolean formulas [10] for each Boolean connective. Nested formulas are handled recursively by applying these rules to each subformula. 

1) Complexity: GTSP-TWPR is NP-hard [19]; solving it via MILP has exponential worst-case time complexity in the number of binary variables and, per branch-and-bound node, polynomial in the number of constraints. Our MILP has O(N 2 +n·r) binary variables and O(r ·N 2) constraints. Timing constraints have a dual effect: each additional constraint tightens the LP relaxation and strengthens the branch-and-bound bound, but also increases solve time per node. Consequently, sparser timing constraints can increase solve time in practice, as confirmed in Sec. IV. As N and r

grow with ϕc complexity, solving a single MILP over a larger event set can become intractable. We address this issue next. 

C. Sub-TPO Decomposition 

Rather than solving a single large MILP over all events (the monolithic formulation), we observe that many task structures contain subsets of events that are internally cou-pled, through both precedence and timing constraints, but interact with the rest of the plan only through a single entry 5and exit event. Such substructures can be identified, solved independently as smaller MILP instances, and composed back into the global plan, which we call the decomposition 

approach. Since each sub-problem is exponential only in its own size, and the sub-problem sizes are small relative to the full problem, the overall computational cost becomes a sum of small exponential problems rather than one large one. We formalize these substructures as sub-TPOs. 

Definition 7 (Sub-TPO) . Let ϕc be a cTPO with event set 

Π and G = ( V, E ) the corresponding GTSP-TWPR graph. Let Πs ⊆ Π and let ϕs be a cTPO with event set Πs, whose precedence, timing, and activation components are defined over events in Πs. We say ϕs is a sub-TPO of ϕc if: 

1) it has a unique entry ein and exit eout in Πs, i.e., ein 

is the unique event in Πs that is preceded by some event in Π \ Πs, and eout is the unique event in Πs

that precedes some event in Π \ Πs, and 

2) each internal event ei ∈ Πs \ { ein , e out } satisfies: 

a) GTSP-TWPR isolation: every node in Vli is con-nected in E only to nodes belonging to events in 

Πs, with no edges to proposition nodes; further, 

Vlout has no incoming edge from a node of an event in Π \ Πs.

b) Sub-TPO isolation: ei is unordered under ≺ with respect to every ej /∈ Πs.

c) Clock isolation: the clocks appearing in G(ei)

and R(ei) are not used by G(ej ) or R(ej ) for any ej /∈ Πs.

d) Activation consistency: α(ei) ≡ α(ein ) ≡ α(eout ).

The cTPO in Fig. 1 with G in Fig. 2 contains one sub-TPO, which happens to be exactly the part shown in orange. Among all valid sub-TPOs rooted at a given entry event, we seek a largest one to maximize the computational benefit of decomposition. The existence and structure of sub-TPOs is determined by the topology of G, which depends on T .

Definition 8 (Locally Maximal Sub-TPO) . A sub-TPO ϕs of 

ϕc with entry ein is locally maximal if no other sub-TPO of 

ϕc with the same entry ein has a strictly larger event set. 

Locally maximal sub-TPOs are not only well-defined but also pairwise disjoint. 

Proposition 2 (Disjointness of Locally Maximal Sub-TPOs) .

For any two distinct locally maximal sub-TPOs ϕs and ϕ′

> s

,

Πs ∩ Πs′ = ∅.Proof. Suppose Πs ∩ Πs′̸ = ∅. If Πs ⊆ Πs′ or Πs′ ⊆ Πs,local maximality is violated at the shared or inner entry. If they partially overlap, any shared event forces edges or precedence relations across a sub-TPO boundary, violating GTSP-TWPR or sub-TPO isolation, or else the two sub-TPOs concatenate into a strictly larger valid sub-TPO, violat-ing local maximality. Every case yields a contradiction. We now present Algorithm 1 to compute the set of locally maximal sub-TPOs. We define the set of articulation events A(G) = {ei ∈ Π | ∃v ∈

Algorithm 1: Maximal Sub-TPO Decomposition                                                

> Input: ϕc,G= ( V, E )
> Output: M: set of maximal sub-TPOs of ϕc
> 1M ← ∅ ;
> 2classified ← ∅ ;
> 3C ← topological order of A(G)∪ I (G)induced by ≺;
> 4for e∈ C do
> 5if e / ∈classified then
> 6{B1, . . . , B m} ← DESCENDANTS (e, Π,≺);
> 7B←Smi=1 Bi;
> 8if ISSUB TPO (B∪ { e}, e, G, ϕ c)then
> 9M ← M ∪ { ϕsinduced by B∪ { e}} ;
> 10 classified ←classified ∪B∪ { e};
> 11 return M;

Vli s.t. v is a strong articulation point of G} as events whose removal from G increases the number of strongly connected components, and the initial events 

I(G) := {ei ∈ Π | ∃ v ∈ Vli , (v0, v ) ∈ E} as the set of events that are reachable directly from the depot v0. In the construction of G, each event node carries a self-loop to enable correct articulation point identification. A valid entry event ein must be either an initial event or an articulation event: if it were neither, its internal events would remain reachable from the rest of G after its removal, violating GTSP-TWPR isolation. This restricts the candidate set to 

C = A(G) ∪ I (G) ⊆ Π.Algorithm 1 iterates over C in topological order and, for each unclassified candidate entry e, partitions the events reachable from e under ≺ into descendant sets {Bi} by 

e’s direct successors. It then applies the pruning procedure ISSUB TPO (described below) to the full union B = ∪iBi,which returns the largest valid subset of B ∪ { e} satisfying Def. 7, if one exists. Once a sub-TPO is found, all its events are marked and excluded from any subsequent sub-TPO. ISSUB TPO takes the candidate set B ∪ e and iteratively removes any event that violates one of the isolation condi-tions of Def. 7 relative to the current set. This process repeats until the candidate set contains no violations. The procedure returns the resulting set as a valid sub-TPO if it contains at least two events and has exactly one exit (an event with an edge to an event outside the set); otherwise, it rejects the set. 

Theorem 1 (Completeness) . Algorithm 1 is complete, i.e., it returns exactly the set of all locally maximal sub-TPOs of ϕc.Proof. Any locally maximal sub-TPO’s entry ein is either an initial event or, by GTSP-TWPR isolation, an articulation event, so ein ∈ C . Topological processing of C guarantees ein 

is considered before any of its descendants can be classified by another sub-TPO. Since every event of a valid sub-TPO satisfies Def. 7, none are pruned by I SSUB TPO, which therefore returns the largest valid subset, ensuring local maximality. Proposition 2 ensures no two locally maximal sub-TPOs compete for the same events. Once all maximal sub-TPOs are identified, we solve each 

ϕsi ∈ M independently, obtaining a local timed trace τ ∗

> i

with duration d∗ 

> i

. We then solve a reduced high-level TPO 6e1: charge e3: shelf B            

> e2: shelf A
> e4: shelf C
> e5: dock
> e7: clean 2 e6: clean 1 e8: clean 3
> ph
> phph
> ph
> c1:= 0 5≤c1≤15
> c1≥5
> c1≥5

(a) Task cTPO ϕinsp  

> c

. Black edges and guards apply unconditionally. Orange denotes the conditional events. 

(b) The optimal trajectory avoids the spill zone and completes only the mandatory, unconditional events of ϕinsp  

> c

.

(c) The optimal traj. passes through the spill zone, activating ph = ⊤ and extending the plan with the cleaning sequence e6, e 7, e 8.                

> e1: arrive e4: sci. B e5: return
> e2: heat soil e3: sci. A
> e6: nav e7: collect
> po
> po
> po
> c1:= 0
> c3:= 0
> c4:= 0
> c6:= 0 c6≤30
> |c3−c4| ≤ 20 ∧
> c1≤50

(d) Task cTPO ϕrover  

> c

. Black edges and guards apply unconditionally. Orange de-notes the conditional requirements. 

(e) The outcrop is away from the rover. The optimal trajectory avoids it, yielding po = ⊥

and a plan over five events only. 

(f) The outcrop surrounds the rover. The op-timal trajectory passes through it, activating 

po = ⊤ and extending the plan with e6, e7.

Fig. 4: Illustrative examples. Task cTPOs and plans for the Warehouse (a)-(c) and Mars Rover (d)-(f) examples (video: https:// youtu.be/9tdEdERKRE4 ). 

ϕH , which replaces each sub-TPO with its entry event ei

> in

and adjusts the travel cost and timing bounds around ei

> in

by d∗ 

> i

to account for the time spent inside it. The full trace is recovered from the resulting high-level trace τ ∗ 

> H

by substituting each local τ ∗ 

> i

back in at the time ei 

> in

is reached. 

Theorem 2 (Optimality) . The timed trace τ ∗ 

> H

returned by the decomposition procedure above is optimal for Problem 1. Proof. By Conditions 2a–2d of Def. 7, each ϕsi is isolated from ϕc beyond ei

> in

, e i

> out

, so the cost of traversing Πi is independent of the rest of the plan and d∗ 

> i

equals what any globally optimal solution would incur on Πi. Substituting 

d∗ 

> i

into the high-level problem is thus exact, so solving ϕH

optimally yields τ ∗ 

> H

with makespan equal to the monolithic optimum. IV. E XPERIMENTS 

Here, we provide empirical evaluations of our cTPO framework in two stages: illustrative examples and bench-marks. The examples are designed to illustrate interpretabil-ity of cTPOs, correctness of synthesized plans, and how a change in the environment changes optimal plans depending on proposition evaluations. The benchmarks compare the decomposition approach against the monolithic MILP on randomly generated problems. Our tool is implemented in Python, uses Gurobi [20] as the MILP solver, and integrates with ROS for robot deployment. All benchmarks were conducted on a MacBook Air M3 with 16 GB of RAM and robot experiments performed in ROS with RViz visualization. 

A. Illustrative Examples 1) Warehouse Inspection: We consider a robot inspecting three warehouse stations, Shelf A ( e2), Shelf B ( e3), and Shelf C ( e4), and returning to its loading dock ( e5) after a mandatory charging event ( e1). The aisle layout contains an oil spill zone. If the robot traverses the spill zone, it must complete a cleaning sequence ( e6, e 7, e 8) before proceeding TABLE II: Robot’s DTS T and the induced GTSP-TWPR graph 

G sizes and MILP solve time (monolithic) for the illustrative examples. Synthesis times are averaged over 3 trials. 

T G Synth. 

|X| |δ| |V | |E| Time (s) Warehouse Fig. 4b 108 728 10 31 0.010 Warehouse Fig. 4c 108 728 10 31 0.008 Rover Fig. 4e 70 280 15 165 77.5 Rover Fig. 4f 70 280 14 139 575.6 

to the loading dock. The cTPO ϕinsp  

> c

is shown in Fig. 4a, and the environments are shown in Fig. 4b-4c, which differ only in the position of Shelf B relative to the spill zone. The size of the robot’s DTS and induced GTSP-TWPR graph, along with the MILP planning times are shown in Table II. Notice that the environment topology influences the op-timal trajectories and how they satisfy ϕinsp  

> c

. Specifically, in Fig. 4b, the location of Shelf B ( e3) enables the robot to optimally avoid the spill zone and satisfy ϕinsp  

> c

without triggering ph, whereas in Fig. 4c, the optimal behavior is to pass through the spill zone (triggering ph) and go through the three cleaning areas. 

2) Mars Rover Atmospheric Science Mission: We con-sider a Mars rover mission in which the rover must, after arriving at the landing site ( e1), heat and analyze a soil sample ( e2 ≺ e3) and, independently, take a measurement with instrument B ( e4), with both branches completing within 20 time units of each other. Then, it must return to its lander (e5) within 50 time units of arrival. If the rover ever passes through an outcrop, before returning, it must ensure to have navigated to e6 and collected a sample from e7 within 30 time units. The cTPO representation of the task ϕrover  

> c

is shown in Fig. 4d. For plan synthesis, we consider the environments in Figs. 4e-4f, differing in the placement and number of the outcrop regions relative to the lander. Table II summarizes the problem size and plan synthesis times. In Fig. 4e, the optimal trajectory avoids the outcrop ( po = ⊥) and visits 710 20 30 40 50  

> Run Number
> 10 1
> 10 0
> 10 1
> 10 2
> Solve Time (s)

Monolithic vs Decomposition Solve Time per Run 

> Monolithic Decomposition

(a) TPOs solve times across 50 runs. 10 20 30 40 50 60 70   

> Run Number
> 10 2
> 10 1
> 10 0
> 10 1
> 10 2
> 10 3
> Solve Time (s)

Monolithic vs Decomposition Solve Time per Run 

> Monolithic Decomposition

(b) cTPO solve times across 76 runs. 

Fig. 5: Benchmark results of monolithic MILP vs decomposition-based MILP. Each data point is averaged over 3 trials. 

unconditional events only. In Fig. 4f, it is optimal for the rover to pass through the outcrop, setting po = ⊤ and executing e6 and e7 rather than avoiding it. In fact, it cleverly executes e6 and e7 before triggering po, anticipating its eventual activation along the trajectory. 

B. Benchmarks 1) Sub-TPO Decomposition on Random TPOs: This benchmark isolates the effect of decomposition from con-ditional structure by considering TPOs with no proposition nodes or conditional sub-TPOs. We randomly generated TPOs, each with 50-60 events, until 50 feasible TPOs were obtained. We then solved each feasible instance using both the monolithic MILP and decomposition-based approaches. The results (averaged over 3 trials) are reported in Fig. 5a, with instances sorted by increasing monolithic solve time along the x-axis. Note that as instance difficulty increases, the monolithic solve time grows by several orders of magnitude, while the decomposition-based solve time remains stable at roughly below 1s. Consequently, the speedup increases with problem difficulty, exceeding 3 orders of magnitude for the most com-plex instances. The sharpest increases in monolithic solve time correspond to instances with sparser timing constraints, which reduce MILP pruning and enlarge the feasible region, consistent with the complexity analysis in Sec. III-B. 

2) Sub-TPO Decomposition on Random cTPOs: The second benchmark extends the evaluation to cTPOs with conditional sub-TPOs, totaling 13-22 events and 4-13 atomic propositions. Fig. 5b shows the results across 76 feasible instances sorted by increasing monolithic solve time. Note that decomposition remains advantageous in over 90% of instances, with speedups reaching 4 orders of magnitude on the hardest instance. Unlike the TPO case, decomposition times also grow with problem difficulty, as the high-level MILP must jointly resolve proposition values and tour struc-ture. The variability in speedup reflects the difficulty of this high-level MILP, which depends on the randomly generated proposition formulas and activation structure. We finally note that, in all TPO and cTPO benchmarks, the decomposition-based method matched the monolithic solution cost (plan makespan), validating Theorem 2. V. C ONCLUSION 

In this work, we introduced Conditional TPOs to address the expressiveness limitations of existing TPOs in robotic task planning. By incorporating generalized clock-difference guards and environment-dependent activation conditions, we enabled the specification of complex timing constraints be-tween unordered events alongside conditionally activated task structures. To efficiently solve the resulting plan syn-thesis problem, we extended existing MILP formulations and introduced a novel sub-TPO decomposition algorithm. Our benchmarks and case studies demonstrate that this de-composition preserves plan optimality, yields computational speedups of up to 4 orders of magnitude for complex instances. Future work includes extension of cTPOs multi-agent planning. REFERENCES [1] H. Kress-Gazit, M. Lahijanian, and V. Raman, “Synthesis for robots: Guarantees and feedback for robot behavior,” Annual Review of Control, Robotics, and Autonomous Systems , vol. 1, 2018. [2] H. Guo, F. Wu, Y. Qin, R. Li, K. Li, and K. Li, “Recent trends in task and motion planning for robotics: A survey,” ACM Computing Surveys , 2023. [3] I. Singh, V. Blukis, A. Mousavian, A. Goyal, D. Xu, J. Tremblay, D. Fox, J. Thomason, and A. Garg, “Progprompt: Program genera-tion for situated robot task planning using large language models,” 

Autonomous Robots , vol. 47, no. 8, pp. 999–1012, 2023. [4] Y. Kim, D. Kim, J. Choi, J. Park, N. Oh, and D. Park, “A survey on integration of large language models with intelligent robots,” 

Intelligent Service Robotics , vol. 17, no. 5, pp. 1091–1107, 2024. [5] S. Kambhampati, K. Valmeekam, L. Guan, M. Verma, K. Stechly, S. Bhambri, L. P. Saldyt, and A. B Murthy, “Position: LLMs can’t plan, but can help planning in LLM-modulo frameworks,” in ICML , ser. Pro-ceedings of Machine Learning Research, R. Salakhutdinov, Z. Kolter, K. Heller, A. Weller, N. Oliver, J. Scarlett, and F. Berkenkamp, Eds., vol. 235. PMLR, 21–27 Jul 2024. [6] R. Koymans, “Specifying real-time properties with metric temporal logic,” Real-Time Systems , vol. 2, no. 4, pp. 255–299, 1990. [7] O. Maler and D. Nickovic, “Monitoring temporal properties of con-tinuous signals,” in International symposium on formal techniques in real-time and fault-tolerant systems . Springer, 2004, pp. 152–166. [8] R. Alur and D. L. Dill, “A theory of timed automata,” Theoretical computer science , vol. 126, no. 2, pp. 183–235, 1994. [9] O. Maler, D. Nickovic, and A. Pnueli, “From mitl to timed automata,” in International conference on formal modeling and analysis of timed systems . Springer, 2006, pp. 274–289. [10] G. A. Cardona, D. Kamale, and C.-I. Vasile, “Mixed integer linear programming approach for control synthesis with weighted signal temporal logic,” in HSCC . ACM, 2023. [11] K. Watanabe, G. Fainekos, B. Hoxha, M. Lahijanian, D. Prokhorov, S. Sankaranarayanan, and T. Yamaguchi, “Timed partial order infer-ence algorithm,” in ICAPS , vol. 33, no. 1, 2023. [12] C. E. Noon, The generalized traveling salesman problem . University of Michigan, 1988. [13] A. Mingozzi, L. Bianco, and S. Ricciardelli, “Dynamic programming strategies for the traveling salesman problem with time window and precedence constraints,” Operations research , vol. 45, no. 3, 1997. 

8[14] P. C. Pop, O. Cosma, C. Sabo, and C. P. Sitar, “A comprehensive survey on the generalized traveling salesman problem,” European Journal of Operational Research , 2023. [15] K. Watanabe, G. Fainekos, B. Hoxha, M. Lahijanian, H. Okamoto, and S. Sankaranarayanan, “Optimal planning for timed partial order specifications,” in Int. Conf. on Robotics and Auto. IEEE, May 2024. [16] H. Kress-Gazit, G. E. Fainekos, and G. J. Pappas, “Where’s waldo? sensor-based temporal logic motion planning,” in Int. Conf. on Robotics and Auto. IEEE, 2007, pp. 3116–3121. [17] M. Lahijanian, M. Kloetzer, S. Itani, C. Belta, and S. B. Andersson, “Automatic deployment of autonomous cars in a robotic urban-like environment (rule),” in Int Conf on Robotics and Auto , 2009. [18] S. Edelkamp, M. Lahijanian, D. Magazzeni, and E. Plaku, “Integrating temporal reasoning and sampling-based motion planning for multigoal problems with dynamics and time windows,” IEEE Robotics and Automation Letters , vol. 3, no. 4, pp. 3473–3480, 2018. [19] J. Hartmanis, Computers and intractability: a guide to the theory of np-completeness . SIAM, 1982, vol. 24, no. 1. [20] Gurobi Optimization, LLC, “Gurobi Optimizer Reference Manual,” 2026. [Online]. Available: https://www.gurobi.com 

9