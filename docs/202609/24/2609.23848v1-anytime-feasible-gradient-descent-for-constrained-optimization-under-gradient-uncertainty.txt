Title: Anytime-Feasible Gradient Descent for Constrained Optimization Under Gradient Uncertainty

URL Source: https://arxiv.org/pdf/2609.23848v1

Published Time: Tue, 22 Sep 2026 01:50:12 GMT

Number of Pages: 6

Markdown Content:
# Anytime-Feasible Gradient Descent for Constrained Optimization Under Gradient Uncertainty 

Sina Sharifi, Jiarui Wang, and Mahyar Fazlyab 

Abstract — Constrained optimization is central to many en-gineering systems in which decisions must satisfy strict safety and operational requirements, especially in real-time settings with limited computational budgets. In such scenarios, opti-mization algorithms are often terminated before full conver-gence, making anytime feasibility essential for safe deployment. Existing methods that guarantee feasibility at every iterate typically rely on exact gradient information, an assumption that is often violated in practice due to measurement noise, stochastic approximations, or model mismatch. We develop an anytime-feasible first-order method for nonlinear constrained optimization under norm-bounded errors in the objective and constraint gradients. The method computes a robust search direction by solving a second-order cone program and selects a step size through safeguarded backtracking. Assuming exact function evaluations and a strictly feasible initialization, the method preserves strict feasibility and guarantees sufficient objective decrease whenever the computed search direction is nonzero. We establish a uniform positive lower bound on the accepted step sizes, an O(1 /K ) bound on the average squared search direction norm, and convergence of the search directions to zero. We also show that a zero search direction at a strictly feasible point certifies approximate first-order stationarity. We validate the proposed method on a multi-agent navigation task in cluttered environments and show that it maintains collision-free trajectories despite noisy gradient information. 

I. INTRODUCTION Constrained optimization underlies many modern engi-neering systems, where decisions must be made while sat-isfying strict operational and safety requirements [1], [2]. This is particularly critical in real-time and high-frequency settings, where solutions must be computed rapidly while remaining robust to uncertainty and model inaccuracies. A representative example arises in trajectory optimization for autonomous systems operating in cluttered environments [3], where a robot must compute collision-free trajectories while minimizing energy or travel time using approximate gradient information. Limited computational budgets often require terminating the optimization before convergence. Ensuring anytime feasibility, i.e., satisfying the constraints at every iteration, is therefore essential for safely deploying interme-diate solutions. Motivated by these challenges, a number of methods have been proposed to guarantee anytime feasibility. Most existing approaches, however, rely on access to exact gradients of the objective and constraint functions [4], [5]. This assumption          

> The authors are with the Department of Electrical and Computer Engineering at Johns Hopkins University, Baltimore, MD 21218, USA.

{sshari12, jwang486, mahyarfazlyab }@jhu.edu .This work was supported by the National Science Foundation (NSF) under Grant 2515978 and by the DOE Office of Science (ASCR) under Award No. 826565. 

is often unrealistic in practice, where gradients are corrupted by noise due to measurement errors, stochastic approxima-tions, or model mismatch. A complementary line of work studies constrained optimization in the presence of noisy or stochastic gradient information [6], [7], [8]. However, these methods do not guarantee anytime feasibility. In this work, we bridge these two directions by study-ing constrained optimization problems under noisy gra-dient information and developing a method that ensures the iterates remain feasible at all times. Specifically, we first formulate the problem as a sequence of quadratically constrained quadratic programs (QCQPs) and show that, when accounting for worst-case noise, each subproblem reduces to a second-order cone program (SOCP). We then demonstrate that solving this SOCP, combined with a line search procedure, yields safe update directions and guaran-tees convergence at an ergodic rate of O(1 /K ) in terms of the average squared search-direction norm. 

A. Related Work 

Sequential Quadratic Programming (SQP) is a widely used class of methods for solving nonconvex constrained opti-mization problems. These methods iteratively approximate the original nonlinear program by solving a sequence of quadratic subproblems, obtained via a second-order expan-sion of the Lagrangian and linearization of the constraints [9], [10]. Standard SQP iterations do not, in general, preserve feasibility of the original nonlinear constraints, hindering their deployment in online settings where intermediate iter-ates must remain feasible. Assuming knowledge of the Lip-schitz constants of the gradients, [4] constructs conservative local approximations, thereby ensuring anytime feasibility. In contrast, [11], [5] incorporate a tilting quadratic term into the linearized constraints, yielding a QCQP subproblem, and use a line search procedure to ensure anytime feasibility without requiring prior knowledge of the gradient Lipschitz constants. The methods discussed so far all assume access to exact gradients when needed. However, a complementary line of work considers the optimization problem in the presence of noise. The authors of [6] propose a noise-tolerant version of SQP and provide guarantees that the iterates converge to a neighborhood of the solution. Other methods change the classical Armijo condition and adapt it to the stochastic setup [7], [8]. In the context of control synthesis for dynamical systems with uncertainty, [12] uses control barrier functions (CBFs) [13] to enforce safety under uncertainty, leading to a reformulation as a second-order cone program (SOCP) arXiv:2609.23848v1 [math.OC] 20 Sep 2026 that can be solved efficiently online. In the context of reinforcement learning, [14] considers stochastic gradient uncertainty arising from finite-sample estimation and pro-vides probabilistic safety guarantees. In contrast, we consider worst-case (deterministic) gradient perturbations and enforce feasibility for all realizations within a prescribed uncertainty set. II. B ACKGROUND AND PROBLEM STATEMENT 

A. Problem Statement 

We consider the inequality-constrained optimization prob-lem 

f ⋆ = min  

> x

{f0(x) s.t. fi(x) ≤ 0, i ∈ [m]}, (1) where fi : Rn → R, i ∈ [0 , m ] := {0, · · · , m }, are continuously differentiable. For brevity, we denote [1 , m ] by 

[m]. We assume that the optimal value f ⋆ > −∞ is attained at some feasible point x⋆. We denote the feasible set by 

F = {x ∈ Rn | fi(x) ≤ 0, ∀i ∈ [m]}.

We assume that the objective and constraint values can be evaluated exactly, while their gradients are available through estimates satisfying 

∇fi(x) = c∇f i(x)+ di, ∥di∥ ≤ ϵi, i ∈ { 0, . . . , m }, (2) where ϵi > 0 are known error bounds. The estimates may vary between iterations; dependence on the current estimates is suppressed in the notation u(x). We make the following assumptions throughout the paper. 

Assumption 1 (Smoothness): For each i ∈ [0 , m ], the function fi is continuously differentiable and has Li-Lipschitz continuous gradient, i.e., 

∥∇ fi(x) − ∇ fi(y)∥ ≤ Li∥x − y∥, ∀x, y ∈ Rn.

Assumption 2 (MFCQ): The Mangasarian–Fromovitz constraint qualification [15] holds at every feasible point 

x ∈ F ; that is, for every x ∈ F , there exists a direction 

d ∈ Rn such that ∇fi(x)⊤d < 0, ∀i ∈ { i | fi(x) = 0 }.

Definition 1 ( ε-KKT point): A point x ∈ F is called an 

ε-KKT point if there exists λ ∈ Rm 

> +

such that 

∇f0(x) + 

> m

X

> i=1

λi∇fi(x) ≤ ε, λi ≥ 0, f i(x) ≤ 0, λ ifi(x) ≥ − ε, ∀i ∈ [m].

B. Deterministic Safe Sequential QCQP 

To motivate our development, we first recall the deter-ministic safe gradient method proposed in [5]. At a current feasible iterate x, the search direction ˜u(x) is obtained as 

˜u(x) =arg min  

> u
> 12

∥u + ∇f0(x)∥2 (3) s.t. ∇fi(x)⊤u + αf i(x) + wi∥u∥2 ≤ 0, ∀i ∈ [m].

where α > 0, wi > 0. The objective chooses a search direction that deviates minimally from the unconstrained gradient-descent direction, whereas the constraints enforce barrier-type invariance conditions associated with the feasible z₁                 

> z₂
> u₁
> u₂
> ℱ
> x
> ∇f1
> 𝒰 nom
> 𝒰 rob
> −2 0
> (a) State space (b) Search-direction space
> Fig. 1: Geometry of the nominal and robust search-direction con-straints. (a) Feasible set F, boundary point x, and estimated gradient direction c∇f1.(b) Gradient uncertainty contracts the nominal admissible set Unom to Urob , excluding directions insufficiently aligned with the inward normal.

set. In particular, the inequality ∇fi(x)⊤ ˜u(x) + αf i(x) ≤

0 is the nominal continuous-time CBF condition for the constraint fi(x) ≤ 0, and the additional quadratic term 

wi∥˜u(x)∥2 introduces a robustness margin that accounts for discretization error in the subsequent line-search-based im-plementation. Specifically, after computing ˜u(x), the method in [5] employs a backtracking line search to select a step size 

η and update the iterate as x+ = x + η˜u(x). The line search is designed to guarantee two properties simultaneously: i) 

anytime feasibility , meaning that every iterate remains in F,and ii) monotonic descent of the objective. A key feature of (3) is the quadratic safeguard term wi∥˜u(x)∥2, which ensures that the accepted step size is bounded away from zero under suitable smoothness assumptions. This avoids the vanishing-step-size pathology that can arise when discretiz-ing continuous-time QP-based safe-gradient dynamics; see, e.g., [16], [17], [18]. III. S EQUENTIAL SOCP 

A. Robust Search Direction via SOCP 

We now revisit the deterministic QCQP (3) with uncertain gradients. Recall that, instead of the exact gradients, we only have access to inexact estimates satisfying (2). To preserve descent and anytime feasibility under gradient uncertainty, we enforce each constraint robustly over all admissible perturbations. Specifically, for each i ∈ [m], we replace the nominal constraint in (3) by 

sup 

> ∥di∥≤ ϵi

h

( c∇f i(x) + di)⊤u

i

+ αf i(x) + wi∥u∥2 ≤ 0. (4) We have sup ∥di∥≤ ϵi d⊤ 

> i

u = ϵi∥u∥, with maximizer d⋆i =

ϵiu/ ∥u∥ whenever u̸ = 0 . Hence, (4) is equivalent to 

c∇f i(x)⊤u+ϵi∥u∥+αf i(x)+ wi∥u∥2 ≤ 0, ∀i ∈ [m]. (5) We now turn to the objective. In the deterministic QCQP (3), the objective can be written as 

12 ∥u + ∇f0(x)∥2 = 12 ∥u∥2 + ∇f0(x)⊤u + 12 ∥∇ f0(x)∥2.

Since x is fixed when solving the subproblem, the last term is independent of u and can be omitted without affecting the minimizer. Motivated by this equivalent reduced form, we robustify the reduced objective model 12 ∥u∥2 + ∇f0(x)⊤u

under the uncertainty model (2). This leads to the worst-case objective 

sup 

> ∥d0∥≤ ϵ0

 12 ∥u∥2 + u⊤( c∇f 0(x) + d0)



= 12 ∥u∥2 + c∇f 0(x)⊤u + ϵ0∥u∥. (6) Combining (5) and (6), we obtain the robust search direction 

u(x) as the solution of 

u(x) = argmin 12 ∥u∥2 + c∇f 0(x)⊤u + ϵ0∥u∥ (SS-SOCP) 

s.t. c∇f i(x)⊤u+ϵi∥u∥+αf i(x)+ wi∥u∥2 ≤ 0, ∀i ∈ [m].

This problem is a robust counterpart of (3), where both descent and the forward invariance constraints are enforced against worst-case gradient perturbations. Figure 1 visual-izes the differences between the feasible set of (3) and (SS-SOCP). The resulting problem (SS-SOCP) admits an equivalent SOCP representation as follows, 

min  

> u,r,s
> 12

s + c∇f 0(x)⊤u + ϵ0r (7) s.t. c∇f i(x)⊤u + ϵir + αf i(x) + wis ≤ 0, ∀i ∈ [m],

∥u∥ ≤ r, ∥u∥2 ≤ s, 

An optimal solution can be chosen with r = ∥u∥ and s =

r2 = ∥u∥2, so this formulation is equivalent to (SS-SOCP). Moreover, ∥u∥ ≤ r is a standard second-order cone con-straint, while ∥u∥2 ≤ s admits a rotated second-order cone representation. Hence, the problem can be efficiently solved using off-the-shelf SOCP solvers. In the following result, we show that (SS-SOCP) yields a descent direction for the objective despite noisy gradient information. To this end, we first establish strong duality for (SS-SOCP). 

Lemma 1 (Slater’s Condition): Suppose x is strictly fea-sible for (1), i.e., fi(x) < 0, ∀i ∈ [m]. Then the robust subproblem (SS-SOCP) is strictly feasible. Therefore, Slater’s condition holds for (SS-SOCP). 

Proof: Setting u = 0 yields αf i(x) < 0 for all i ∈ [m],hence all constraints are strictly satisfied. 

Theorem 1 (Robustness of Descent Direction): Let x be a strictly feasible point for (1), and let u(x) be the optimizer of (SS-SOCP). Then ∇f0(x)⊤u(x) ≤ −∥ u(x)∥2.

Proof: The proof can be found in Section VI-C 

B. Sequential SOCP with Safeguarded Backtracking 

We now use the search direction obtained by solving (SS-SOCP) to generate the iterates 

xk+1 = xk + tku(xk), (8) where tk > 0 denotes the step size. To select tk adaptively, we employ a safeguarded backtracking line search. Specifi-cally, given fixed parameters γ1, γ 2 ∈ (0 , 1) , we choose tk

such that 

f0(xk + tku(xk)) ≤ f0(xk) − γ1tk∥u(xk)∥2, (9) 

fi(xk + tku(xk)) ≤ (1 − γ2)fi(xk), ∀i ∈ [m]. (10) Condition (9) imposes an Armijo-type sufficient decrease on the objective, while (10) permits the constraint to approach its boundary while remaining strictly satisfied. In Proposi-tion 1 we prove the existence of such a step size tk > 0.The next result shows that if the subproblem (SS-SOCP) returns the zero direction at a strictly feasible point, then that point satisfies the ε-KKT conditions of the original problem. Hence, the condition u(x) = 0 serves as a natural first-order stationarity certificate for the proposed method. 

Theorem 2 ( ε-KKT conditions): Assume that x is strictly feasible for (1), i.e., fi(x) < 0, ∀i ∈ [m]. Let u(x) be an optimal solution of (SS-SOCP). If u(x) = 0 , then x is an 

ε-KKT point of (1) with ε = 2 ϵ0.

Proof: The proof can be found in Section VI-D. 

Proposition 1 (Lower bound on step size): Let Assump-tion 1 hold, and suppose xk is strictly feasible for (1), then condition (9) and (10) hold for all t ∈ [0 , t min ] where 

tmin = βmin 

 2(1 − γ1)

L0

, min 

> i

 2wi

Li



, γ2

α , t init 



,

and tinit is the initial step size in Algorithm 1. Consequently, the safeguarded backtracking line search returns tk ≥ tmin .

Proof: The proof can be found in Section VI-E. 

Theorem 3 (Strict feasibility and convergence): Suppose Assumption 1 and the gradient uncertainty model (2) hold. Let {xk}k≥0 be generated by Algorithm 1 from a strictly feasible point x0. Assume α > 0, wi > 0 for all i ∈ [m],

β, γ 1, γ 2 ∈ (0 , 1) . Then every iterate is strictly feasible, and 

fi(xK ) ≤ (1 −γ2)K fi(x0) < 0, i ∈ [m], K ≥ 0. (11) Moreover, for every K ≥ 1,

1

K

> K−1

X

> k=0

∥u(xk)∥2 ≤ f0(x0) − f ⋆

γ1tmin K . (12) where tmin is defined in Proposition 1. In particular, 

P∞ 

> k=0

∥u(xk)∥2 ≤ f0(x0)−f ⋆ 

> γ1tmin

< ∞, and hence 

lim  

> k→∞

∥u(xk)∥ = 0 . (13) IV. E XPERIMENTS 

In this section, we evaluate the proposed method on a multi-agent optimal control task involving collision-free nav-igation. The objective is to steer nv vehicles from their initial configurations to designated goal states while avoiding both static obstacles and inter-agent collisions. The experimental Algorithm 1 Safe Sequential SOCP with Line Search 

Require: Initialization x0 ∈ Rn, maximum iteration K > 

0, stopping tolerance ϵtol > 0, line search parameters 

γ1, γ 2, β ∈ (0 , 1) , α > 0, wi > 0, gradient-error bound 

ϵi > 0, and initial step size tinit > 0

1: Ensure fi(x0) < 0, i ∈ [m]

2: for k = 0 , 1, 2, ..., K − 1 do 

3: Compute u(xk) according to (SS-SOCP) 

4: if ∥u(xk)∥2 ≤ ϵtol then return xk

5: end if 

6: t ← tinit 

7: while either (9) or (10) fails do 

8: t ← βt 

9: end while 

10: tk ← t, xk+1 ← xk + tku(xk)

11: end for 

12: Return xK

setup follows [19], [5]. Formally, the problem is 

min  

> X,U N−1

X

> t=0

l(X(t), U (t)) + V (X(N )) (14) s.t. Ui(t) ∈ U , ∀t ∈ [0 , N − 1] , i ∈ [nv ],Xi(t) ∈ X , ∀t ∈ [0 , N ], i ∈ [nv ],Xi(t + 1) = f (Xi(t), U i(t)) , ∀t ∈ [0 , N − 1] , i ∈ [nv ],Xi(0) = Xsi , ∀i ∈ [nv ],gj (Xi(t)) ≤ 0, ∀i ∈ [nv ], j ∈ [3] , t ∈ [0 , N ],

∥pi(t) − pj (t)∥22 ≥ 1, 1 ≤ i < j ≤ nv , t ∈ [0 , N ].

Each vehicle follows Dubins’ car dynamics: 

f (Xi(t), U i(t)) = 



xi(t) + vi(t)∆ t cos θi(t) − ∆x∆tyi(t) + vi(t)∆ t sin θi(t)

θi(t) + wi(t)∆ t

 .

where ∆t is the discretization step size, Xi(t) =[xi(t), y i(t), θ i(t)] ⊤ consists of xy coordinate and orienta-tion, and Ui(t) = [ vi(t), w i(t)] ⊤ consists linear and angular velocity. The constant ∆x = 1 represents a drift velocity in the negative x-direction. We write pi(t) = [ xi(t), y i(t)] ⊤.The desired steady-state input is U di = [1 , 0] ⊤ for all 

i ∈ [nv ]. The stage cost is 

l(X(t), U (t)) = X 

> i∈[nv]

 ∥Xi(t)−Xdi ∥22 +0 .01 ∥Ui(t)−U di ∥22

,

and the terminal cost is V (X(N )) = P  

> i∈[nv]

∥Xi(N ) −

Xdi ∥2 

> Pi

, where each Pi is obtained from the discrete-time algebraic Riccati Equation 

A⊤ 

> i

PiAi − Pi − (A⊤ 

> i

PiBi)( R + B⊤ 

> i

PiBi)−1(B⊤ 

> i

PiAi) + Q = 0 ,

with Ai = ∇1f (Xdi , U di ), Bi = ∇2f (Xdi , U di ), Q = I and 

R = 0 .01 I. The state and input constraints are defined as 

X = {(x, y, θ ) : |x| ≤ 3.2, |y| ≤ 3.2, |θ| ≤ π},

U = {(v, w ) : −5 ≤ v ≤ 12 , |w| ≤ 32 π}.3 2 1 0 1 2 3X (m) 3                           

> 2
> 1
> 0123Y (m)
> Environment 3210123X (m) 3
> 2
> 1
> 0123Y (m)
> Environment
> SS-QCQP Robust SS-QCQP 020 40 60 80 100 120 Iteration 10 3
> Objective value SS-QCQP Robust SS-QCQP 020 40 60 80 100 120 Iteration 10 1
> 10 2
> 10 3
> 10 4
> 10 5
> 010 5
> 10 4
> 10 3
> 10 2
> Max constraint SS-QCQP Robust SS-QCQP Feasible

Fig. 2: The top-left figure shows the navigation performance of our method with nv = 3 . The top-right figure compares our method with SS-QCQP under a noisy setup. The bottom plots compare the objective value and maximum constraint value across iterations. 

Obstacle avoidance is enforced through the constraint 

gj (Xi(t)) = r2 

> j

− ∥ pi(t) − cj ∥22 ≤ 0, where obstacle centers are c1 = [ −1, −1] ⊤, c 2 = [0 , 1] ⊤, c 3 = [1 , 0] ⊤ and obstacles radii are r1 = 1 , r 2 = 0 .5, r 3 = 0 .5. We set the planning horizon to N = 30 and ∆t = 0 .03 .

a) Multi-agent scenario: We first consider a three-vehicle navigation task (top-left of Figure 2). The initial states are Xs 

> 1

= [ −3, −1, 0] ⊤, X s 

> 2

= [ −3, −3, 0] ⊤, X s 

> 3

=[−1, −3, 0] ⊤ and the goal states are Xd 

> 1

= [2 , 1, 0] ⊤, X d 

> 2

=[2 , 2, 0] ⊤, X d 

> 3

= [1 , 2, 0] ⊤. Dotted curves in the figure rep-resent vehicle trajectories. To model gradient uncertainty, we perturb the gradient using noise di whose direction is sampled uniformly from the unit sphere and whose magnitude is uniformly sampled from [0 , ϵ ]. Due to the worst-case treatment of gradient perturbations in (SS-SOCP), the proposed method maintains feasibility throughout the optimization process despite noisy gradients. However, the final state does not exactly reach the goal due to the noise in the objective gradient. 

b) Single-agent scenario: We next consider a single-vehicle task (top-right of Figure 2) with Xs 

> 1

=[−1, −3, 0] ⊤, X d 

> 1

= [1 , 2, 0] ⊤. Here, the gradient noise has a fixed all-ones direction with magnitude uniformly sampled from [0 , ϵ ]. We compare SS-QCQP from [5] with the proposed method. Since SS-QCQP does not account for gradient noise, the perturbations bias the search direction (e.g., inducing a systematic left turn). Moreover, its line search may fail to terminate near the boundary of feasibility, leading to premature failure of the algorithm. In contrast, our proposed method maintains feasibility throughout the optimization process and proceeds to convergence. The evo-lution of the objective value and maximum constraint value is shown in the bottom row of Figure 2. V. C ONCLUSION 

We developed an anytime-feasible first-order method for constrained optimization under norm-bounded gradient un-certainty. The method computes robust search directions by solving an SOCP and uses a safeguarded backtracking line search to preserve strict feasibility and ensure sufficient objective decrease. We established a uniform positive lower bound on the accepted step sizes, an O(1 /K ) bound on the average squared search-direction norm, and convergence of the search directions to zero. Numerical experiments on vehicle navigation illustrate the method’s ability to maintain feasibility despite gradient perturbations. The guarantees rely on known gradient-error bounds, exact function evaluations, and a strictly feasible initialization. Extending the framework to uncertain function values and unbounded gradient noise is a direction for future work. VI. A PPENDIX 

A. KKT Conditions of the Original Problem (1) Let x⋆ be a local minimizer of (1) at which MFCQ holds. Then there exist multipliers λ⋆ ∈ Rm 

> +

satisfying 

∇f0(x⋆) + 

> m

X

> i=1

λ⋆i ∇fi(x⋆) = 0 , (15a) 

fi(x⋆) ≤ 0, λ⋆i ≥ 0, λ⋆i fi(x⋆) = 0 , i ∈ [m], (15b) A feasible point satisfying these conditions is called a KKT point of (1). 

B. KKT conditions of (SS-SOCP) Fix a strictly feasible point x, and let u(x) be the unique minimizer of (SS-SOCP). By Lemma 1, the subproblem satisfies Slater’s condition. Since it is convex, its KKT conditions are necessary and sufficient for optimality. Thus, there exist multipliers λi(x) ≥ 0, i ∈ [m], such that 

0 ∈ u(x)+ c∇f 0(x)+ ϵ0∂∥u(x)∥+

> m

X

> i=1

λi(x)∂uhi(u(x)) ,hi(u(x)) ≤ 0, λi(x)hi(u(x)) = 0 i ∈ [m], (16) where hi(u) = c∇f i(x)⊤u+ϵi∥u∥+αf i(x)+ wi∥u∥2. Here, 

∂∥u∥ denotes the subdifferential of the Euclidean norm at u

and ∂h i(u) = c∇f i(x) + ϵi∂∥u∥ + 2 wiu. Since ∥u∥ is not differentiable at u = 0 , the stationarity condition must be characterized separately for the cases u(x) = 0 and u(x)̸ =0. If u(x)̸ = 0 , then the stationarity condition becomes 

u(x) + c∇f 0(x) + ϵ0

u(x)

∥u(x)∥ (17) 

+

> m

X

> i=1

λi(x)

c∇f i(x) + ϵi

u(x)

∥u(x)∥ + 2 wiu(x)



= 0 .

If u(x) = 0 , there exist vectors v0, . . . , v m with ∥vi∥ ≤ 1

such that 

c∇f 0(x) + ϵ0v0 +

> m

X

> i=1

λi(x)( c∇f i(x) + ϵivi) = 0 . (18) 

C. Proof of Theorem 1 Proof: If u(x) = 0 , the claim is immediate. Consider now the case u(x)̸ = 0 . Since x is strictly feasible for (1), Lemma 1 implies that the robust subproblem (SS-SOCP) satisfies Slater’s condition. Moreover, (SS-SOCP) is convex. Hence, the KKT conditions are necessary and sufficient for optimality. Let λi(x) be the associated KKT multipliers. From the stationarity condition in (17), we have 

c∇f 0(x) = − u(x)−ϵ0

u(x)

∥u(x)∥−

> m

X

> i=1

λi(x)

c∇f i(x)+ ϵi

u(x)

∥u(x)∥ +2 wiu(x)



.

Taking the inner product with u(x) yields 

c∇f 0(x)⊤u(x) = −∥ u(x)∥2 −ϵ0∥u(x)∥−

> m

X

> i=1

λi(x)

 c∇f i(x)⊤u(x) + ϵi∥u(x)∥ + 2 wi∥u(x)∥2

= −∥ u(x)∥2 −ϵ0∥u(x)∥+

> m

X

> i=1

λi(x)( αf i(x)−wi∥u(x)∥2),

where we used complementary slackness (16). Since fi(x) ≤

0 and λi ≥ 0, we have 

c∇f 0(x)⊤u(x) + ϵ0∥u(x)∥ ≤ −∥ u(x)∥2. (19) Finally, since ∥di∥ ≤ ϵi,

∇f0(x)⊤u(x) ≤ sup 

> ∥d0∥≤ ϵ0

( c∇f 0(x) + d0)⊤u(x)

≤ c∇f 0(x)⊤u(x) + ϵ0∥u(x)∥,

which coupled with (19), concludes the proof. 

D. Proof of Theorem 2 Proof: Since u(x) = 0 , the SOCP constraints reduce to 

αf i(x) ≤ 0, ∀i ∈ [m]. Because x is strictly feasible, we have αf i(x) < 0, ∀i ∈ [m]. Hence all SOCP constraints are strictly inactive at u(x) = 0 . By complementary slack-ness of (SS-SOCP), every associated KKT multiplier must therefore vanish: λi(x) = 0 , ∀i ∈ [m]. Using the uncertainty model (2) and that ∂∥u∥ u=0 = {q ∈ Rn : ∥q∥ ≤ 1}, the stationarity condition of (SS-SOCP) reduce to 

∇f0(x)+ d0 +ϵ0q+

> m

X

> i=1

λi(x)∇fi(x)+ di +ϵiq = 0 ,

where ∥q∥ ≤ 1, ∥di∥ ≤ ϵi. Since λi(x) = 0 ∀i ∈ [m], we have 

∇f0(x) + 

> m

X

> i=1

λi(x)∇fi(x) = ∥d0 + ϵ0q∥ ≤ 2ϵ0.

Finally, since x ∈ F , primal feasibility holds. Moreover, because λi(x) = 0 , λi(x)fi(x) = 0 for all i. Hence x is a 

2ϵ0-KKT point of (1) with dual variable λi = λi(x).E. Proof of Proposition 1 Proof: We show that both conditions (9)–(10) hold for all sufficiently small t > 0, and then extract an explicit bound. Following Lemma 1, since xk is strictly feasible for (1), KKT conditions hold for (SS-SOCP). 

Descent condition: By Lipschitz continuity of ∇f0, we have 

f0(xk+tu (xk)) 

≤f0(xk)+ t∇f0(xk)⊤u(xk)+ L0

2 t2∥u(xk)∥2

≤f0(xk)−(t − t2L0

2 )∥u(xk)∥2,

where we used Theorem 1. Thus, (9) holds if 

f0(xk) − (t − t2L0

2 )∥u(xk)∥2 ≤ f0(xk) − γ1t∥u(xk)∥2,

which simplifies to t ≤ 2(1 −γ1) 

> L0

.

Feasibility condition: By Lipschitz continuity of ∇fi,

fi(xk + tu (xk)) 

≤fi(xk) + t∇fi(xk)⊤u(xk) + Li

2 t2∥u(xk)∥2

≤fi(xk)−tαf i(xk) − tw i∥u(xk)∥2 + Li

2 t2∥u(xk)∥2

=(1 − tα )fi(xk) + 

 Li

2 t2 − tw i



∥u(xk)∥2,

where we used the feasibility of u(xk), i.e., 

∇fi(xk)⊤u(xk) ≤ ∇ ˜fi(xk)⊤u(xk)+ ϵi∥u(xk)∥≤ − αf i(xk)−wi∥u(xk)∥2.

Thus, (10) holds if 

(1 −αt )fi(xk) + 

 Li

2 t2 −tw i



∥u(xk)∥2 ≤ (1 −γ2)fi(xk).

Since fi(xk) < 0, it suffices that both t ≤ γ2 

> α

and Li 

> 2

t2 −

tw i ≤ 0, which holds whenever t ≤ min 

n γ2 

> α

, 2wi

> Li

o

. Thus, both conditions are satisfied for 

t ≤ min 

 2(1 − γ1)

L0

, min 

> i

 2wi

Li



, γ2

α



.

Therefore, the resulting step size satisfies tk ≥ tmin .

F. Proof of Theorem 3 Proof: We first establish strict feasibility by induction. The initial point x0 is strictly feasible by assumption. If 

xk is strictly feasible, Proposition 1 guarantees that the backtracking line search terminates with an accepted step size tk ≥ tmin > 0. The feasibility condition (10) then gives 

fi(xk+1 ) ≤ (1 − γ2)fi(xk) < 0, i ∈ [m],

because 0 < γ 2 < 1. Thus, every iterate remains strictly feasible. Applying this inequality recursively proves (11). Next, the sufficient decrease condition (9) and the uniform step-size bound imply 

f0(xk+1 ) ≤ f0(xk)−γ1tk∥u(xk)∥2 ≤ f0(xk)−γ1tmin ∥u(xk)∥2.

Summing from k = 0 to K − 1 yields 

γ1tmin 

> K−1

X

> k=0

∥u(xk)∥2 ≤ f0(x0) − f0(xK ).

Since xK is feasible, f0(xK ) ≥ f ⋆. Dividing by γ1tmin K

therefore establishes (12). The same inequality shows that 

> K−1

X

> k=0

∥u(xk)∥2 ≤ f0(x0) − f ⋆

γ1tmin 

∀K ≥ 1.

These partial sums are nondecreasing and uniformly bounded. As K → ∞ , we conclude that the series converges, which implies ∥u(xk)∥2 → 0.REFERENCES [1] T. Erseghe, “Distributed optimal power flow using admm,” IEEE transactions on power systems , vol. 29, no. 5, pp. 2370–2380, 2014. [2] P. M. Wensing, M. Posa, Y. Hu, A. Escande, N. Mansard, and A. Del Prete, “Optimization-based control for dynamic legged robots,” 

IEEE Transactions on Robotics , vol. 40, pp. 43–63, 2023. [3] G. Williams, P. Drews, B. Goldfain, J. M. Rehg, and E. A. Theodorou, “Aggressive driving with model predictive path integral control,” in 2016 IEEE international conference on robotics and automation (ICRA) , pp. 1433–1440, IEEE, 2016. [4] A. Auslender, R. Shefi, and M. Teboulle, “A moving balls approxima-tion method for a class of smooth constrained minimization problems,” 

SIAM Journal on Optimization , vol. 20, no. 6, pp. 3232–3259, 2010. [5] J. Wang and M. Fazlyab, “Anytime-feasible first-order optimization via safe sequential qcqp,” arXiv preprint arXiv:2511.19675 , 2025. [6] F. Oztoprak, R. Byrd, and J. Nocedal, “Constrained optimization in the presence of noise,” SIAM Journal on Optimization , vol. 33, no. 3, pp. 2118–2136, 2023. [7] A. S. Berahas, M. J. O’Neill, and C. W. Royer, “A line search frame-work with restarting for noisy optimization problems,” Computational Optimization and Applications , pp. 1–22, 2025. [8] C. Paquette and K. Scheinberg, “A stochastic line search method with expected complexity analysis,” SIAM Journal on Optimization , vol. 30, no. 1, pp. 349–376, 2020. [9] P. E. Gill, W. Murray, M. A. Saunders, and M. H. Wright, “Some theoretical properties of an augmented lagrangian merit function.,” tech. rep., 1986. [10] D. Q. Mayne and E. Polak, “A surperlinearly convergent algorithm for constrained optimization problems,” in Algorithms for constrained minimization of smooth nonlinear functions , pp. 45–61, Springer, 2009. [11] S. Sharifi, E. Y. Hamedani, and M. Fazlyab, “Sequential qcqp for bilevel optimization with line search,” IEEE Control Systems Letters ,2025. [12] K. Long, V. Dhiman, M. Leok, J. Cort´ es, and N. Atanasov, “Safe control synthesis with uncertain dynamics and constraints,” IEEE Robotics and Automation Letters , vol. 7, no. 3, pp. 7295–7302, 2022. [13] A. D. Ames, X. Xu, J. W. Grizzle, and P. Tabuada, “Control barrier function based quadratic programs for safety critical systems,” IEEE Transactions on Automatic Control , vol. 62, no. 8, pp. 3861–3876, 2016. [14] P. Mestres, A. Marzabal, and J. Cort´ es, “Off-policy reinforcement learning with anytime safety guarantees via robust safe gradient flow,” 

arXiv preprint arXiv:2510.01492 , 2025. [15] J. Nocedal and S. J. Wright, Numerical optimization . Springer, 2006. [16] M. Muehlebach and M. I. Jordan, “On constraints in first-order optimization: A view from non-smooth dynamical systems,” arXiv preprint arXiv:2107.08225 , 2021. [17] A. Allibhoy and J. Cort´ es, “Control-barrier-function-based design of gradient flows for constrained nonlinear programming,” IEEE Trans-actions on Automatic Control , vol. 69, no. 6, pp. 3499–3514, 2023. [18] S. Sharifi, N. Abolfazli, E. Y. Hamedani, and M. Fazlyab, “Safe gradient flow for bilevel optimization,” in 2025 American Control Conference (ACC) , pp. 1675–1680, IEEE, 2025. [19] J. Wu, L. Dai, S. Dou, and Y. Xia, “Accelerated successive convex approximation for nonlinear optimization-based control,” IEEE Trans-actions on Automatic Control , 2025.