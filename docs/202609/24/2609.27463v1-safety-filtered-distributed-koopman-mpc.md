---
title: Safety-Filtered Distributed Koopman-MPC
title_zh: 安全过滤的分布式 Koopman 模型预测控制
authors: "Shengjun Zhang, Wenhao Li, Zhenxin Lin, Zhenglong Sun"
date: 2026-09-23
pdf: "https://arxiv.org/pdf/2609.27463v1"
tags: ["query:q2"]
score: 8.0
evidence: 带有碰撞约束的分布式模型预测控制
tldr: 针对分布式模型预测控制（DMPC）中因通信丢包导致预测与安全约束同时失效的问题，本文提出了一种安全过滤的分布式Koopman-MPC方法。该方法将轨迹预测与安全约束解耦，利用Koopman算子进行轨迹预测，并通过基于局部感知和货架几何的硬约束二次规划（QP）对输入进行投影。实验表明，该方法在存在丢包和非线性漂移的仓库机器人场景中，实现了极高的避障成功率与在线可行性，显著优于传统预测控制。
selection_source: fresh_fetch
motivation: 解决分布式MPC中通信丢包导致预测与安全约束同时失效的鲁棒性问题。
method: 将Koopman预测与基于局部感知和几何约束的硬约束QP投影解耦，通过投影确保输入满足安全要求。
result: 在8至20个机器人的仓库仿真中，该方法在所有测试中均保持无碰撞且在线可行，远超未加投影的基准方法。
conclusion: 该方法通过解耦预测与安全决策，在通信不可靠环境下实现了鲁棒的分布式多机器人协同控制。
---

## 摘要
分布式模型预测控制（DMPC）通常基于邻居轨迹构建预测和碰撞约束，因此数据包丢失可能导致两者同时失效。我们将这些角色分离：接收到的轨迹驱动 Koopman-MPC，而局部感知和货架几何结构定义了一个硬约束二次规划（QP），用于投影所施加的输入。其径向需求是使支撑平面间隙在一个零阶保持间隔内保持非负的最小恒定加速度。互补对行在不交换安全决策的情况下恢复耦合需求。我们给出了在有界快照和定向工厂误差下的采样间间隔定理、用于同时局部可行性的精确最大-最小测试，以及用于切换交互图的感知半径条件。预期的更高阶行可以为了性能而放宽，但有限保持行不包含安全余量。匹配的八机器人仓库仿真使用了冻结的 Koopman 模型、非线性漂移、有界输入和速度、货架约束、120 毫秒控制周期以及数据包丢失。完整控制器在 20/20 次匹配试验中均无碰撞，并达到 160/160 个机器人目标；而没有最终投影的预测性 Koopman-MPC 在 20 次试验中仅有 1 次无碰撞。所有 38,400 个完整方法的硬行集均通过了在线可行性测试，且每个局部 QP 均有解。五流车队扫描在 16 个机器人规模下均无碰撞且硬行可行；20 个机器人的边界仅在在线余量变为负值后失效，而重构的每个代理的关键路径仍保持在采样周期以下。有界感知和差速驱动测试提供了额外的部署压力测试。

## Abstract
Distributed model predictive control (DMPC) often constructs both predictions and collision constraints from neighbor trajectories, so packet loss can remove both. We separate these roles: received trajectories drive Koopman-MPC, while local sensing and shelf geometry define a hard-constrained quadratic program (QP) that projects the applied input. Its radial demand is the least constant acceleration that keeps a supporting-plane clearance nonnegative throughout one zero-order-hold interval. Complementary pair rows recover the coupled demand without exchanging safety decisions. We give an intersample separation theorem under bounded snapshot and directional plant errors, an exact max-min test for simultaneous local feasibility, and a sensing-radius condition for switching interaction graphs. Anticipatory high-order rows may be relaxed for performance, but the finite-hold rows contain no safety slack. Matched eight-robot warehouse simulations use a frozen Koopman model, nonlinear drift, bounded inputs and speed, shelf constraints, a 120 ms control period, and packet dropout. The full controller is collision-free in 20/20 matched trials and reaches 160/160 robot goals; predictive Koopman-MPC without the final projection is collision-free in 1/20 trials. All 38,400 full-method hard-row sets pass the online feasibility test, and every local QP solves. Five-stream fleet sweeps are collision-free and hard-row feasible through 16 robots; the 20-robot boundary fails only after the online margin turns negative, while the reconstructed per-agent critical path remains below the sampling period. Bounded-sensing and differential-drive tests provide additional deployment stress.

---

## 论文详细总结（自动生成）

这是一份关于论文《Safety-Filtered Distributed Koopman-MPC》的结构化总结：

### 1. 核心问题与研究动机
在分布式模型预测控制（DMPC）中，机器人通常依赖邻居发送的轨迹信息来同时进行“路径规划”和“碰撞约束构建”。这种耦合机制存在严重缺陷：一旦发生通信丢包，机器人不仅会失去预测信息，还会同时丢失碰撞约束，导致系统在通信不可靠环境下极易发生碰撞。本文旨在解决这一鲁棒性问题，通过解耦预测与安全决策，确保即使在丢包情况下，机器人仍能通过局部感知维持安全。

### 2. 方法论：安全过滤的分布式 Koopman-MPC
*   **核心思想**：将控制架构分为两层：上层为基于 Koopman 算子的预测控制（负责任务目标），下层为基于局部感知的硬约束安全投影（负责避障）。
*   **关键技术**：
    *   **Koopman 预测**：利用离线训练的 EDMDc（带控制的扩展动态模态分解）模型进行线性化预测，该模型在评估阶段保持冻结。
    *   **硬约束投影（Safety Filter）**：在每个采样周期，机器人利用局部传感器获取邻居和障碍物的快照，构建一个“硬约束二次规划（QP）”。该 QP 计算出满足安全条件的最小径向加速度，对 MPC 输出的标称控制量进行投影。
    *   **互补分配**：对于机器人对，通过互补权重分配安全责任，无需交换安全决策信息，仅依赖局部快照即可实现协同避障。
    *   **可行性审计**：引入在线可行性测试（$\mu_i \ge 0$），在执行前预判当前约束是否冲突。

### 3. 实验设计
*   **场景**：模拟八机器人仓库环境，包含货架障碍物、非线性漂移、输入/速度限制及通信丢包。
*   **对比方法**：
    1.  **ORCA**（基于速度障碍的反应式避障）。
    2.  **仅目标反馈 + 安全投影**（无预测）。
    3.  **预测性 Koopman-MPC**（无安全投影，依赖通信）。
    4.  **本文方法**（完整架构）。
*   **Benchmark**：碰撞次数、目标到达率、最终位置误差、最小对间距离、在线可行性余量。

### 4. 资源与算力
*   **算力说明**：论文未明确提及具体的 GPU 型号或训练时长。文中仅提到使用 Python 进行仿真，且在 120ms 的控制周期内，单机即可完成所有机器人的规划与 QP 求解，显示了该方法极高的计算效率。

### 5. 实验数量与充分性
*   **实验规模**：进行了 20 次匹配试验（针对主仓库场景），并进行了多组压力测试（包括 16-20 个机器人的扩展规模、不同丢包率、感知误差、差速驱动模型等）。
*   **客观性**：实验设计了“匹配试验”，即所有对比方法在相同的随机丢包流和初始条件下运行，保证了对比的公平性。实验覆盖了从基础避障到大规模车队扩展的多个维度，具有较强的说服力。

### 6. 主要结论与发现
*   **鲁棒性显著提升**：在 15% 丢包率下，本文方法在 20/20 次试验中均保持无碰撞，而对比的预测性 MPC 仅 1/20 次无碰撞。
*   **性能与安全兼顾**：该方法在保证安全的同时，保持了与预测性 MPC 相当的目标到达率和轨迹精度。
*   **可扩展性**：在 16 机器人规模下表现稳定，20 机器人规模下的失效主要源于物理空间限制导致的在线可行性余量耗尽，而非算法逻辑错误。

### 7. 优点
*   **解耦设计**：成功将“任务规划”与“安全保障”分离，使得安全约束不再依赖于不可靠的通信链路。
*   **理论严谨**：提供了 intersample（采样间）安全证明，确保在零阶保持（ZOH）期间系统依然安全。
*   **计算高效**：QP 投影的规模与 MPC 预测时域解耦，非常适合实时分布式部署。

### 8. 不足与局限
*   **局部最优性**：由于安全投影是基于当前快照的局部优化，可能在极端拥挤环境下导致“死锁”或无法到达目标（虽然论文强调了安全优先，但未深入探讨全局死锁规避）。
*   **假设依赖**：理论证明依赖于对工厂漂移（plant drift）和感知误差的精确有界假设，若实际环境噪声超出预设范围，安全保证可能失效。
*   **应用限制**：主要针对仓库等结构化环境，对于高度动态、非结构化的复杂人群环境，其感知半径和几何假设可能需要进一步调整。

（完）
