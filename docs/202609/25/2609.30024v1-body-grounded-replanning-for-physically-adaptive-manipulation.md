---
title: Body-Grounded Replanning for Physically Adaptive Manipulation
title_zh: 面向物理自适应操作的身体基础重规划
authors: "Namiko Saito, Hiroshi Kera"
date: 2026-09-24
pdf: "https://arxiv.org/pdf/2609.30024v1"
tags: ["query:profile-1"]
score: 8.0
evidence: 利用内部物理状态进行自适应操作的高层重规划
tldr: 本文提出了一种“身体感知”的高层重规划框架，旨在解决机器人操作中物理状态（如关节负载、移动受限）对任务执行的影响。该方法利用大语言模型（LLM）实时分析内部物理状态与执行历史，在不改变任务目标的前提下动态调整操作策略。实验证明，该框架在模拟环境及真实机器人上均能有效提升任务成功率，同时降低物理损耗并实现更高效的策略适应。
selection_source: fresh_fetch
motivation: 现有操作策略多关注外部环境，忽略了机器人自身物理状态（如关节负载）对任务执行的制约。
method: 引入身体感知重规划机制，利用LLM结合关节状态、执行统计和历史数据，在任务执行过程中动态触发并选择替代策略。
result: 实验表明该方法在保持高任务成功率的同时，显著降低了物理努力并提升了策略适应效率。
conclusion: 内部物理状态不仅适用于底层控制，还应作为高层决策的关键依据，以实现更具适应性的机器人操作。
---

## 摘要
操作任务不仅需要对外部环境进行推理，还需要考虑机器人的物理状态。某种策略在几何上可能是可行的，但由于关节负载增加或移动能力受限，在物理上可能变得不再适用，然而内部物理状态通常仅用于底层控制。我们提出了身体基础的高层重规划方法，利用内部物理状态在执行过程中调整操作策略。身体状态事件会触发策略重规划，大语言模型（LLM）通过解读底层的关节状态、近期执行统计数据和执行历史来选择上下文相关的替代方案，同时保持任务目标和底层控制器不变。我们在仿真环境和真实机器人上，针对受控负载和非对称移动约束下的到达任务对该框架进行了评估。实验表明，身体基础重规划在保持高任务成功率的同时，降低了物理损耗并实现了更高效的策略调整。额外的接触丰富型操作实验证明了该重规划接口在到达任务之外的适用性。这些结果表明，内部物理状态不仅可以为底层控制提供信息，还可以为如何执行操作任务的高层决策提供依据。

## Abstract
Manipulation requires not only reasoning about the external environment, but also about the robot's physical condition. A strategy may remain geometrically feasible while becoming physically unsuitable due to increased joint load or limited mobility, yet internal physical state is typically used only for low-level control. We propose body-grounded high-level replanning, which uses internal physical state to adapt manipulation strategies during execution. Body-state events trigger strategy replanning, and an LLM interprets the underlying joint-level state, recent execution statistics, and execution history to select a context-dependent alternative, while leaving the task objective and low-level controller unchanged. We evaluate the framework on a reaching task under controlled load and asymmetric mobility constraints in simulation and on a real robot. Our experiments show that body-grounded replanning maintains high task success while reducing physical effort and enabling more efficient strategy adaptation. Additional contact-rich manipulation experiments demonstrate the applicability of the same replanning interface beyond reaching. These results show that internal physical state can inform not only low-level control, but also high-level decisions about how a manipulation task should be performed.

---

## 论文详细总结（自动生成）

这是一份关于论文《Body-Grounded Replanning for Physically Adaptive Manipulation》的结构化总结：

### 1. 核心问题与研究背景
*   **研究动机**：现有的机器人操作规划主要关注外部环境（如物体几何、场景语义），而将机器人的内部物理状态（如关节负载、关节极限、电机状态）仅局限于底层控制。
*   **核心矛盾**：一个在几何上可行的操作策略，可能因机器人自身的物理限制（如关节过载或移动受限）而变得“物理上不适用”。机器人缺乏一种机制，在执行过程中根据自身物理反馈动态调整高层策略。

### 2. 方法论：身体基础重规划 (Body-Grounded Replanning)
*   **核心思想**：建立一个“身体感知”的反馈闭环，将内部物理状态作为高层决策的输入，在不改变任务目标和底层控制器的情况下，动态切换操作策略。
*   **关键流程**：
    1.  **状态监测与预测**：通过滚动窗口统计（如关节力矩、任务进度、关节极限余量）监测身体状态。引入短时预测模型（Ridge回归）提前预判潜在的物理困难。
    2.  **事件触发**：定义了四类事件（HighEffort, LowProgress, NearJointLimit, EarlyContact），当事件发生时触发重规划。
    3.  **LLM 决策**：利用 GPT-4o-mini 作为决策核心，输入当前策略、物理状态、执行历史和事件描述，输出“原因-解决方案-策略选择”的结构化指令，从预定义的策略集（如侧向接近、顶部接近、重试等）中选择最优解。

### 3. 实验设计
*   **场景与平台**：
    *   **仿真**：Franka Panda 7-DoF 机械臂（robosuite/MuJoCo）。
    *   **真实机器人**：CRANE-X7 7-DoF 机械臂（MoveIt）。
*   **Benchmark**：受控负载（增加配重）和非对称移动约束（限制关节活动范围）。
*   **对比方法**：
    *   **Fixed**：固定策略，不重规划。
    *   **Env.**：环境驱动的身体无关重规划（周期性或失败后切换）。
    *   **Rule**：基于规则的身体重规划（手动设定优先级，不考虑连续物理状态）。
    *   **Ours**：本文提出的 LLM 驱动的身体基础重规划。

### 4. 资源与算力
*   **算力说明**：论文未明确提及训练或推理所需的具体 GPU 型号、数量或总训练时长。仅提到 LLM 使用了 GPT-4o-mini，且在实验中排除了 LLM 推理延迟对执行时间的影响。

### 5. 实验数量与充分性
*   **实验规模**：仿真实验包含 10 种物理条件（3 种负载 + 7 种关节约束），每种条件 100 次配对实验；真实机器人实验包含 10 种条件，每种 8 次试验。
*   **充分性与公平性**：实验设计了配对比较（Paired Comparison），确保不同方法在相同的初始条件和目标下进行测试，统计分析使用了 Wilcoxon 符号秩检验并进行了 Holm 校正，实验设计客观且具有统计学意义。

### 6. 主要结论与发现
*   **性能提升**：该方法在保持高成功率的同时，显著降低了平均关节力矩（物理损耗）并缩短了受限完成时间。
*   **身体感知的必要性**：对比 Rule 方法证明，仅检测物理事件是不够的，结合连续的物理状态进行上下文推理对于选择正确的替代策略至关重要。
*   **通用性**：该框架成功迁移至按钮按下、开盒盖、提取 lint-roller 等接触密集型任务，证明了接口的通用性。

### 7. 优点
*   **解耦设计**：将高层策略选择与底层控制器解耦，无需重新训练底层策略即可增强系统的鲁棒性。
*   **可解释性**：LLM 输出的“REASON”和“SOLUTION”字段提供了决策的可解释性，便于调试。
*   **高效适应**：相比于盲目尝试的重规划，该方法能更精准地根据物理状态选择策略，减少了无效尝试。

### 8. 不足与局限
*   **策略空间受限**：目前依赖于手动定义的离散策略集，缺乏对复杂、长程任务的泛化能力。
*   **推理延迟**：虽然实验排除了推理时间，但在实时性要求极高的场景下，LLM 的推理延迟可能成为瓶颈。
*   **依赖性**：高度依赖于 LLM 对物理状态的理解能力，若物理状态特征与策略之间的映射过于复杂，LLM 可能出现决策偏差。

（完）
