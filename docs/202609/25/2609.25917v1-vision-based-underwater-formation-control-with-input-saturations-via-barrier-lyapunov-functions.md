---
title: Vision-based Underwater Formation Control With Input Saturations via Barrier Lyapunov Functions
title_zh: 基于视觉的欠驱动水下机器人编队控制：利用障碍李雅普诺夫函数处理输入饱和
authors: "Nicola De Carli, João Zenário, Victor Nan Fernandez-Ayala, Dimos V. Dimarogonas"
date: 2026-09-22
pdf: "https://arxiv.org/pdf/2609.25917v1"
tags: ["query:q2"]
score: 8.0
evidence: 屏障李雅普诺夫函数与二次规划控制
tldr: 本文提出了一种无需通信的视觉水下机器人编队控制框架。该方法针对全驱动水下机器人在感知约束、避障需求及输入饱和下的控制难题，利用重中心化障碍李雅普诺夫函数（RBLF）处理约束，结合指令滤波反步法处理二阶动力学，并通过二次规划（QP）显式处理执行器限制。此外，通过自适应放宽保守感知域，在保证安全的同时提升了灵活性，并通过Gazebo仿真验证了其有效性。
selection_source: fresh_fetch
motivation: 解决水下机器人在复杂环境下的编队控制中，感知受限、避障需求及执行器输入饱和带来的挑战。
method: 采用重中心化障碍李雅普诺夫函数编码约束，结合指令滤波反步法与二次规划技术实现受限下的最优控制。
result: 在Gazebo仿真环境中验证了该方法在处理感知约束和输入饱和时的有效性与鲁棒性。
conclusion: 该框架成功实现了无需通信的视觉编队控制，为受限水下环境下的多机器人协同提供了可靠的解决方案。
---

## 摘要
本文提出了一种无需通信的视觉编队控制框架，用于解决受感知约束、避障需求及输入饱和限制的全驱动水下机器人编队控制问题。通过重中心化障碍李雅普诺夫函数（Recentered Barrier Lyapunov Functions）对感知和避障约束进行编码，并利用指令滤波反步法（Command-filtered Backstepping）将设计扩展至二阶车辆动力学。最终的控制目标通过二次规划（Quadratic Program）实现，该规划明确考虑了执行器的限制。保守的感知域为物理极限提供了裕度，并在必要时进行自适应放宽，允许对保守边界的暂时性突破。所提方法通过 Gazebo 中的真实软件在环（SITL）仿真得到了验证。

## Abstract
In this work, we propose a communication-free framework for vision-based formation control of fully actuated underwater robots subject to sensing constraints, collision-avoidance requirements, and input saturations. Recentered barrier Lyapunov functions encode sensing and collision-avoidance constraints, while command-filtered backstepping extends the design to the second-order vehicle dynamics. The resulting control objective is enforced through a quadratic program that explicitly accounts for actuator limits. Conservative sensing domains provide margins from the physical limits and are adaptively relaxed when necessary, allowing temporary violation of the conservative bounds. The proposed approach is validated through realistic Software-in-the-Loop (SITL) simulations in Gazebo.

---

## 论文详细总结（自动生成）

这是一份关于论文《Vision-based Underwater Formation Control With Input Saturations via Barrier Lyapunov Functions》的结构化总结：

### 1. 核心问题与研究背景
水下机器人（AUV）在执行多机协同任务时面临严峻挑战：GPS信号缺失、水下通信带宽受限且不可靠、动力学模型复杂且存在非线性。现有的编队控制方法往往难以同时处理**感知范围受限**（如摄像头的视场角和探测距离）、**避障需求**以及**执行器输入饱和**（推力限制）这三类约束。本文旨在提出一种无需机器人间显式通信的视觉编队控制框架，通过仅依赖机载视觉传感器获取相对位置信息，实现全驱动水下机器人的稳定编队。

### 2. 方法论：核心思想与关键技术
*   **重中心化障碍李雅普诺夫函数 (RBLF)**：利用 RBLF 将感知约束（距离、视场角）和避障需求编码为势函数。RBLF 在目标位置处梯度为零，在接近约束边界时趋于无穷大，从而提供强排斥力。
*   **指令滤波反步法 (Command-filtered Backstepping)**：针对二阶车辆动力学，通过指令滤波避免对虚拟控制律求导，从而简化了控制器设计，并处理了由于缺乏父节点速度信息而导致的互联项问题。
*   **二次规划 (QP) 控制分配**：将控制目标转化为一个带约束的二次规划问题。该 QP 显式地将执行器推力饱和限制作为硬约束，在满足动力学要求的同时，通过松弛变量处理潜在的不可行性。
*   **自适应边界放宽机制**：引入动态松弛状态，当系统在保守边界内无法满足控制要求（如推力饱和）时，自适应地扩大保守感知域，并在条件允许时恢复至标称状态，从而在安全与性能之间取得平衡。

### 3. 实验设计
*   **场景设置**：使用 Gazebo 仿真环境，结合 PX4 自动驾驶仪和 BlueROV2 Heavy 水下机器人模型。
*   **实验内容**：
    *   **五机编队实验**：在模拟水箱环境中，验证了在存在模型失配（浮力、附加质量、阻尼参数不准）和未建模动力学的情况下，系统的编队保持能力。
    *   **两机压力测试**：通过人为降低推力限制（仅为标称值的 20%），测试在极端受限条件下，自适应边界放宽机制如何防止系统崩溃并维持物理约束。

### 4. 资源与算力
论文未明确提及具体的计算资源消耗（如 GPU 型号、训练时长等）。由于该方法主要基于控制理论的实时优化（QP求解），其计算开销主要取决于机载处理器的实时计算能力，而非大规模离线训练。

### 5. 实验数量与充分性
*   **实验覆盖**：包含了一个多机编队场景和一个针对极端饱和情况的压力测试。
*   **充分性评价**：实验设计较为客观，通过引入模型失配和未建模动力学，模拟了真实水下环境的复杂性。虽然缺乏与现有其他先进算法的直接对比（Benchmark），但通过对关键约束（感知、推力）的详细演化曲线分析，验证了所提方法的有效性。

### 6. 主要结论与发现
*   所提框架成功实现了在无通信条件下的多机器人编队，且对感知约束和执行器饱和具有鲁棒性。
*   自适应边界放宽机制有效地解决了“保守约束”与“控制可行性”之间的冲突，在极端情况下通过暂时牺牲保守裕度保证了系统的物理安全性。

### 7. 优点
*   **无需通信**：完全依赖机载视觉，降低了对水下通信基础设施的依赖，提升了系统的自主性。
*   **显式约束处理**：通过 QP 显式处理推力饱和，避免了传统饱和处理方法（如截断）带来的性能损失。
*   **理论完备性**：通过李雅普诺夫稳定性分析，证明了系统在特定条件下的几乎全局吸引性和输入到状态稳定性（ISS）。

### 8. 不足与局限
*   **应用限制**：目前假设每个跟随者仅观察一个父节点（树状拓扑），对于更复杂的循环图拓扑的扩展性未做深入探讨。
*   **环境假设**：假设水下环境足够清晰以支持视觉探测，未考虑高浊度环境下的视觉失效风险。
*   **实验局限**：目前主要基于仿真（SITL），虽然使用了真实机器人模型，但尚未在真实水下环境中进行实地部署验证。

（完）
