---
title: Koopman-Accelerated Model-Based Diffusion for Real-Time Robot Control
title_zh: 用于实时机器人控制的 Koopman 加速模型预测扩散
authors: "Bohyeong Pak, Kangmin Lee, Sanghyun Kim"
date: 2026-09-24
pdf: "https://arxiv.org/pdf/2609.28920v1"
tags: ["query:profile-1"]
score: 9.0
evidence: 利用 Koopman 加速扩散进行实时机器人控制和轨迹优化
tldr: 针对传统基于模型的扩散模型（MBD）在机器人控制中计算成本过高的问题，本文提出了双线性Koopman模型驱动的扩散方法（BK-MBD）。该方法通过将机器人状态提升至高维空间并利用双线性动力学进行快速传播，将复杂的滚动预测简化为矩阵乘法，实现了实时轨迹优化。实验表明，BK-MBD在仿真和物理机械臂上均能满足实时控制的时效性要求，并展现出优于线性模型和传统凸优化方法的任务成功率与鲁棒性。
selection_source: fresh_fetch
motivation: 传统基于模型的扩散控制方法因动力学滚动计算开销巨大，难以满足机器人实时控制的需求。
method: 利用双线性Koopman算子将机器人状态提升至高维空间，通过矩阵乘法替代复杂的动力学模拟，实现高效的轨迹预测。
result: BK-MBD在仿真中实现了14.7ms以内的快速规划，并在物理机械臂实验中成功完成了动态目标追踪任务。
conclusion: BK-MBD通过结合Koopman算子与扩散模型，有效解决了机器人实时控制中的计算瓶颈，提升了复杂环境下的控制精度与鲁棒性。
---

## 摘要
传统的模型预测扩散（MBD）通过利用噪声退火实现了有效的轨迹优化。然而，其高昂的计算成本（主要源于对系统动力学的重复推演）在很大程度上将其应用限制在离线场景中。为了解决这一局限性，本文提出了双线性 Koopman 模型预测扩散（BK-MBD）。该方法在每个控制步长中仅将机器人状态提升至高维空间一次，此后在提升空间中传播所有候选轨迹，从而将每次推演简化为固定次数的矩阵向量乘法。提升后的动力学模型为双线性，允许预测的输入增益随机器人配置变化，这是线性提升模型无法表示的。在仿真实验中，BK-MBD 在 50 毫秒的控制周期内，每次规划更新最多耗时 14.7 毫秒，且在每次试验中均成功到达目标，而线性提升模型几乎从未成功。在学习到的推演模型下，退火调度比固定噪声调度提高了闭环精度。在精确推演下，退火调度和固定窄范围调度均能到达所有目标，这表明退火降低了对代理模型误差的敏感性。BK-MBD 还成功穿过了没有任何单一凸区域覆盖的通道，而凸优化双线性控制器则很少成功。在物理机械臂上，BK-MBD 在控制周期内跟踪了初始未知的移动目标，是唯一同时满足跟踪任务和实时性要求的方法。项目主页见 https://rcilab.khu.ac.kr/bkmbd/。

## Abstract
Conventional model-based diffusion (MBD) achieves effective trajectory optimization by leveraging noise annealing. However, its high computational cost, primarily arising from repeated rollouts of the plant dynamics, has largely confined its use to offline settings. To address this limitation, this paper proposes bilinear Koopman model-based diffusion (BK-MBD). The proposed method lifts the robot's state into a high-dimensional space only once per control step and propagates all candidates in the lifted space thereafter, so each rollout reduces to a fixed number of matrix-vector multiplications. The lifted dynamics are bilinear, allowing the predicted input gain to vary with the robot's configuration, which a linear lifted model cannot represent. In simulation, BK-MBD completed each planning update in at most 14.7 ms within a 50 ms control period and reached the goal on every trial, whereas a linear lift almost never did. The annealed schedule improves closed-loop accuracy over fixed-noise schedules under the learned rollout. Under the exact rollout, both the annealed and fixed-narrow schedules reach every goal, indicating that annealing reduces sensitivity to surrogate-model error. BK-MBD also threaded a passage that no single convex region covers, whereas a convexified bilinear controller rarely succeeded. On a physical manipulator, BK-MBD tracked an initially unknown moving target within the control period and was the only method that met both the tracking task and the deadline. The project page is available at https://rcilab.khu.ac.kr/bkmbd/.

---

## 论文详细总结（自动生成）

这是一份关于论文《Koopman-Accelerated Model-Based Diffusion for Real-Time Robot Control》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的基于模型的扩散（MBD）控制方法虽然在轨迹优化方面表现出色，但由于需要对系统动力学进行大量的重复推演（Rollout），计算开销巨大，导致其难以应用于需要高频响应的实时机器人控制场景。
*   **研究动机**：为了在保持扩散模型优化能力的同时，实现实时闭环控制，作者旨在寻找一种既能捕捉机器人非线性动力学（特别是状态相关的输入增益），又能将计算复杂度降低到毫秒级的替代方案。

### 2. 方法论：BK-MBD
*   **核心思想**：引入双线性 Koopman 算子作为动力学代理模型，将非线性系统提升至高维线性空间，从而将复杂的动力学推演简化为高效的矩阵-向量乘法。
*   **关键技术**：
    *   **双线性提升**：不同于线性 Koopman 模型（假设输入增益为常数），双线性模型通过状态与输入的乘积项，能够准确表示随机器人配置变化的输入增益。
    *   **一次性提升**：在每个控制周期内，仅对当前状态进行一次提升（Encoder），后续所有候选轨迹的传播均在提升空间内完成。
    *   **退火采样优化**：结合 MBD 的噪声退火机制，通过在不同噪声水平下进行采样，平滑代价函数景观，从而增强对代理模型误差的鲁棒性。

### 3. 实验设计
*   **场景与平台**：
    *   **仿真**：机械臂（FR3）到达任务、无人机穿过非凸通道任务。
    *   **物理实验**：FR3 机械臂对未知移动目标的实时跟踪任务。
*   **对比方法**：包括线性 Koopman 模型（DK-MBD）、MLP 代理模型、基于真实动力学的 Oracle（MBD-TRUE）、以及凸优化控制器（QP-MPC）。
*   **Benchmark**：以任务成功率（到达目标精度）、实时性（是否在 50ms 内返回指令）以及轨迹跟踪误差为核心指标。

### 4. 资源与算力
*   **硬件环境**：实验在 Intel Core Ultra 5 225F CPU 上进行。
*   **算力消耗**：仿真使用 10 个 PyTorch CPU 线程，物理实验限制为 4 个线程。文中明确指出该方法**未依赖 GPU**，仅通过 CPU 即可实现实时控制，这体现了该方法在计算效率上的巨大优势。

### 5. 实验数量与充分性
*   **实验覆盖**：论文进行了多维度实验，包括：
    *   **消融实验**：对比了不同 rollout 模型（线性 vs 双线性）、不同噪声调度（固定 vs 退火）。
    *   **鲁棒性测试**：在精确动力学与学习动力学下分别测试，验证了退火机制对模型误差的补偿作用。
    *   **实时性验证**：在物理机器人上进行了 10 次连续试验，统计了规划延迟。
*   **评价**：实验设计非常严谨且客观，通过控制变量法（如固定优化器换模型、固定模型换优化器）清晰地剥离了各组件的贡献，具有很高的说服力。

### 6. 主要结论与发现
*   **性能突破**：BK-MBD 是唯一在物理机械臂上既能完成跟踪任务又能满足 50ms 实时性要求的算法。
*   **结构重要性**：双线性结构对于捕捉状态相关的输入增益至关重要，线性模型在复杂任务中几乎无法成功。
*   **退火的价值**：退火调度不仅能提升优化精度，更重要的是它能显著降低对代理模型（Surrogate Model）误差的敏感性。

### 7. 优点
*   **计算高效**：将复杂的非线性推演转化为矩阵乘法，极大地降低了实时控制的门槛。
*   **表达能力强**：双线性 Koopman 结构在保持计算效率的同时，具备了处理状态相关输入增益的能力。
*   **鲁棒性高**：退火机制有效缓解了学习模型带来的局部极小值问题。

### 8. 不足与局限
*   **接触动力学限制**：文中明确提到，当前方法主要针对无接触任务。对于涉及复杂接触（Contact-rich）的场景，简单的提升模型可能无法捕捉接触瞬间的动力学突变。
*   **模型依赖**：虽然比传统 MBD 鲁棒，但仍依赖于离线收集的数据进行模型训练，对于训练数据分布之外的极端状态，其泛化能力有待进一步验证。
*   **适用范围**：该方法目前主要针对控制仿射（Control-affine）系统，对于更广泛的非线性系统，其适用性可能受限。

（完）
