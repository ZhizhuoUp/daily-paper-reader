---
title: "Predict Before You Step: Auditable Occupancy Forecasting for Dynamic Obstacle Avoidance under Sparse Guidance"
title_zh: 先行一步：面向稀疏引导下动态避障的可审计占用预测
authors: "Yuhui Mao, Fen Liu, Shenghai Yuan, Tianxin Hu, Ruimeng Liu, Rong Su"
date: 2026-09-22
pdf: "https://arxiv.org/pdf/2609.25969v1"
tags: ["query:q1"]
score: 8.0
evidence: 50Hz频率下的动态障碍物局部避障策略
tldr: 针对足式机器人在稀疏路点引导下避障困难的问题，本文提出了LOOP（潜空间循环占用预测策略）。该方法通过循环预测器对未来1秒的占用情况进行建模，结合流场与可见性门控机制，实现对动态障碍物的精准预测与规避。LOOP在Isaac Lab仿真及Unitree Go2实机测试中表现优异，显著提升了高速避障的成功率与系统鲁棒性，且具备良好的可审计性。
selection_source: fresh_fetch
motivation: 解决足式机器人在稀疏路点引导下，面对动态障碍物时因观测稀疏和环境快速变化导致的避障难题。
method: 提出一种基于潜空间循环预测的占用滚动策略（LOOP），通过历史观测预测未来占用图，并以此指导速度选择。
result: "在仿真中实现57.1%的正面避障成功率，较基线提升8.2个百分点，并在实机测试中完成16次无碰撞穿越。"
conclusion: LOOP通过显式的预测接口实现了高效、可审计的动态避障，证明了其在复杂动态环境下的部署可行性。
---

## 摘要
在稀疏航点引导下，足式机器人必须利用局部且快速变化的激光雷达观测结果来规避移动障碍物。我们提出了 LOOP（潜空间循环占用展开策略），这是一种将稀疏航点引导与固定运动控制器连接起来的局部避障策略，运行频率为 50 Hz。通过占用情况和自身速度的历史数据，循环预测器利用学习到的流场和可见性门控对当前地图进行变形，从而预测未来 1 秒内的占用情况。这些地图通过地图派生特征和几何风险估计来指导速度选择，为检查和替换预测结果提供了明确的接口。在同步遭遇场景的 Isaac Lab 评估中，LOOP 在 2.5-3.2 m/s 的障碍物速度下实现了 57.1% 的正面碰撞规避成功率，比重新训练的反应式基准高出 8.2 个百分点。与无展开过程的 BEV 策略相比，预测分支在特定场景下表现出更小的增益，包括在最高正面速度下提高了穿越成功率并降低了不同训练种子间的变异性。该适配器在 Unitree Go2 上以每步 14.5 毫秒的速度运行，并完成了全部 16 次真实世界穿越试验且未发生碰撞，证明了其部署的可行性。

## Abstract
Legged robots under sparse waypoint guidance must avoid moving obstacles using partial, rapidly changing LiDAR observations. We present LOOP (Latent-recurrent Occupancy rollOut Policy), a local avoidance policy that connects sparse waypoint guidance to a frozen locomotion controller at 50 Hz. From occupancy and ego-velocity histories, a recurrent predictor forecasts future occupancy over a 1 s horizon by warping the current map with learned flow and visibility gates. These maps guide velocity selection through map-derived features and geometric risk estimates, providing an explicit interface for inspecting and replacing predictions. In encounter-synchronised Isaac Lab evaluations, LOOP achieves 57.1% head-on success at obstacle speeds of 2.5-3.2 m/s, exceeding a retrained reactive baseline by 8.2 percentage points. Comparisons with a rollout-free BEV policy show smaller, scenario-dependent gains from the prediction branch, including improved crossing success and reduced variability across training seeds at the highest head-on speeds. The adapter runs onboard a Unitree Go2 in 14.5 ms per step and completes all 16 real-world crossing trials without collision, demonstrating deployment feasibility.

---

## 论文详细总结（自动生成）

你好！我是 Antigravity。很高兴能为你分析这篇关于足式机器人动态避障的论文。以下是对《Predict Before You Step: Auditable Occupancy Forecasting for Dynamic Obstacle Avoidance under Sparse Guidance》的结构化总结：

### 1. 核心问题与研究动机
*   **背景**：足式机器人在人类共享环境（如巡检、配送）中，通常依赖稀疏的路点（Waypoint）引导。
*   **痛点**：在路点更新的间隙（≤1 Hz），机器人需应对快速移动的障碍物。仅靠激光雷达的局部观测，难以在 50 Hz 的高频控制循环中实现对动态障碍物速度的显式建模，导致避障决策滞后或不安全。
*   **目标**：开发一种轻量级、可审计的局部避障策略（LOOP），在不改变底层运动控制器的情况下，实现对未来 1 秒占用情况的预测，从而提升动态避障的安全性。

### 2. 方法论：LOOP (Latent-recurrent Occupancy rollOut Policy)
*   **核心思想**：将“预测”与“决策”解耦，通过显式的地图空间接口（Map-space interface）连接。
*   **关键技术**：
    *   **历史编码**：利用 LSTM 总结 12 帧的占用图（BEV）和自身速度历史。
    *   **循环预测器**：通过“自展开”（Self-unrolled）机制，利用学习到的流场（Flow）和可见性门控（Visibility gates）对当前地图进行变形（Warping），预测未来 1 秒（5 个步长）的占用情况。
    *   **地图空间接口**：预测的未来地图是预测分支与决策模块的唯一通道。决策模块通过“风险池化”（Risk pooling）计算不同速度模式下的碰撞风险，并结合注意力机制输出最终控制指令。
    *   **可审计性**：由于接口是显式的地图，推理时可直接将预测图替换为零或真值（Ground Truth），从而量化预测分支对决策的贡献。

### 3. 实验设计
*   **场景**：Isaac Lab 仿真环境，包含三种家族场景：对角线穿越（分布内）、正面迎面（分布外）、曲线穿越（分布外）。
*   **Benchmark**：对比了反应式基准（REASAN）、基于 reach-avoid 的屏蔽策略（ABS-style shield）、以及理想情况下的经典规划器（APF, VO）。
*   **协议**：引入“遭遇同步协议”（Encounter-synchronised protocol），通过控制障碍物释放时间，消除障碍物速度变化带来的难度偏差，确保对比的公平性。

### 4. 资源与算力
*   **训练**：使用 1024 个并行环境，在单张 RTX 3090 上训练约 15 小时。
*   **推理**：在 Jetson AGX Orin 上，单步推理耗时 14.5 ms，满足 50 Hz（20 ms）的实时控制预算。

### 5. 实验数量与充分性
*   **规模**：每组实验聚合了超过 3,000 次仿真剧集，并使用 5 个不同的训练种子进行统计显著性检验（Welch's t-test）。
*   **充分性**：实验设计非常严谨，包含消融实验（移除预测分支、移除风险池化等）和推理时的审计实验（替换预测图），能够清晰地归因性能提升的来源。

### 6. 主要结论与发现
*   **性能提升**：在高速（2.5–3.2 m/s）正面迎面场景中，LOOP 成功率达 57.1%，比反应式基准高出 8.2 个百分点。
*   **鲁棒性**：LOOP 在超出训练速度范围的场景下表现出更强的稳定性，且不同训练种子间的性能波动更小。
*   **预测价值**：预测分支在处理需要“外推”运动轨迹的场景（如曲线穿越）时贡献最大；而在简单的正面迎面场景中，性能提升主要源于对时序信息的利用。

### 7. 优点
*   **可审计性（Auditable）**：通过地图接口，设计者可以直观地看到机器人“认为”未来会发生什么，并能通过替换预测图进行故障诊断。
*   **实时性**：在嵌入式设备上实现了高效的 50 Hz 闭环控制。
*   **公平的评估协议**：通过同步遭遇协议，有效排除了传统评估中“障碍物跑得快反而更容易避开”的幸存者偏差。

### 8. 不足与局限
*   **低速性能下降**：在低于训练速度范围（< 0.9 m/s）时，预测器可能产生错误的先验，导致性能反而不如反应式策略。
*   **多智能体局限**：当前仅针对单个移动障碍物，未测试复杂的群体避障场景。
*   **预测准确性非唯一指标**：实验发现，即使预测图的 IoU 较低，只要提供了“地图空间”的未来信息，策略依然能表现良好，说明预测的准确性与控制效果之间存在复杂的非线性关系。

（完）
