---
title: "VertexCBF: Improving Neural Control Barrier Functions via Vertex-Restricted Control Search"
title_zh: VertexCBF：通过顶点受限控制搜索改进神经控制障碍函数
authors: "Bojan Derajić, Sebastian Bernhard, Wolfgang Hönig"
date: 2026-09-11
pdf: "https://arxiv.org/pdf/2609.12831v1"
tags: ["query:q1"]
score: 8.0
evidence: 用于安全和避障的神经控制屏障函数
tldr: 针对机器人安全控制中神经控制障碍函数（CBF）设计困难及保守性问题，本文提出了VertexCBF框架。该方法通过物理信息与稀疏监督学习近似Hamilton-Jacobi值函数，利用控制仿射动力学和顶点受限搜索高效生成监督点，并结合残差架构确保安全约束。实验证明，该方法在15个系统上均能有效扩大安全集，并在移动机器人避障任务中表现优异。
selection_source: fresh_fetch
motivation: 现有神经CBF设计方法在可扩展性、可解释性及安全集保守性方面存在局限。
method: 利用GPU并行顶点受限树搜索生成监督点，并通过残差神经网络学习满足约束的CBF。
result: 在15个基准系统上显著优于现有方法，并成功在真实移动机器人上实现安全避障。
conclusion: VertexCBF提供了一种可扩展、系统化且具备可解释性的神经CBF学习方案，有效提升了机器人系统的安全性能。
---

## 摘要
随着自主机器人数量的不断增加，安全性变得愈发重要。控制障碍函数（CBFs）为确保安全性提供了一个理论基础框架，但现有的设计方法往往在有效性、可扩展性或可解释性方面面临局限，且可能导致过于保守的安全集。在本文中，我们提出了 VertexCBF，这是一个以可扩展、系统化且可解释的方式学习神经 CBF 的框架。我们利用结合了物理信息和稀疏监督学习的方法训练神经网络，以逼近平稳 Hamilton-Jacobi 值函数。通过利用控制仿射动力学和凸多面体控制集（在该条件下，哈密顿量在控制顶点处达到最大值），我们利用 GPU 并行顶点受限树搜索高效生成监督点，同时通过残差架构确保所学习的 CBF 始终不大于指定的约束函数。我们在 15 个系统上对该方法进行了评估，并与相关基准进行了比较，结果表明，在基准方法表现保守或完全失效的情况下，该方法能够可靠地恢复出较大的安全集。此外，我们还进行了硬件实验，展示了一台移动机器人在使用通过该方法训练的神经 CBF 时能够安全地避开行人。

## Abstract
As the number of autonomous robots continues to grow, safety becomes increasingly important. Control barrier functions (CBFs) provide a theoretically grounded framework for ensuring safety, but existing design methods often face limitations in effectiveness, scalability, or interpretability, and may result in overly conservative safe sets. In this paper, we propose \emph{VertexCBF}, a framework for learning neural CBFs in a scalable, systematic, and explainable way. We approximate the stationary Hamilton--Jacobi value function using a neural network trained via a combination of physics-informed and sparsely supervised learning. By exploiting control-affine dynamics and a convex polytope control set, under which the Hamiltonian is maximized at the control vertices, we efficiently generate supervision points via GPU-parallel vertex-restricted tree search, while a residual architecture guarantees that the learned CBF is never larger than the specified constraint function. We evaluate the method on 15 systems and compare it against relevant baselines, showing that it reliably recovers large safe sets where the baselines are conservative or fail completely. In addition, we perform a hardware experiment in which a mobile robot safely avoids pedestrians using a neural CBF trained with our method.

---

## 论文详细总结（自动生成）

这是一份关于论文《VertexCBF: Improving Neural Control Barrier Functions via Vertex-Restricted Control Search》的结构化总结：

### 1. 核心问题与研究动机
在自主机器人领域，**控制障碍函数（CBF）**是确保系统安全性的重要工具。然而，现有的神经 CBF 设计方法存在以下痛点：
*   **保守性强**：往往导致安全集过小，限制了机器人的性能。
*   **可扩展性差**：在高维系统或复杂约束下，求解 Hamilton-Jacobi (HJ) 可达性方程极其困难。
*   **训练不稳定**：仅依赖物理信息（PDE）损失函数容易陷入平凡解或局部最优。
本文旨在构建一种系统化、可扩展且具备可解释性的框架，通过更有效的监督信号来学习逼近最优安全集的神经 CBF。

### 2. 方法论：VertexCBF
*   **核心思想**：利用控制仿射动力学和凸多面体控制集的特性，即哈密顿量（Hamiltonian）在控制顶点处达到最大值。通过将搜索空间限制在控制顶点上，实现高效的监督数据生成。
*   **关键技术**：
    *   **残差架构**：将 CBF 定义为 $V_\Theta(x) = c(x) - r_\Theta(x)$，其中 $r_\Theta \ge 0$，确保学习到的安全集始终包含在原始约束集内。
    *   **顶点受限搜索**：使用 GPU 并行化的树搜索（如 Beam Search、Branch-and-Bound）在有限时域内寻找最优控制序列，生成高质量的监督标签。
    *   **混合损失函数**：结合物理信息损失（PDE Loss，基于 stationary HJB-VI）和监督损失（Data Loss，基于顶点搜索结果），既保证了局部一致性，又避免了训练发散。

### 3. 实验设计
*   **场景与系统**：涵盖了 15 个不同维度的动力学系统，包括倒立摆、无人机、Dubins 小车、四足机器人、6-DoF 水下航行器及四旋翼等。
*   **对比基准**：
    *   **PDE-only (ND)**：仅使用物理信息损失（无监督数据）。
    *   **Full-Control Data (FCD)**：使用基于 MPPI（模型预测路径积分）的全控制空间采样数据进行监督。
*   **验证方法**：通过大规模闭环仿真轨迹回放，计算假安全率（$\rho_{FS}$）、假不安全率（$\rho_{FU}$）和有效安全体积（$\eta_{eff}$）。

### 4. 资源与算力
*   **硬件**：所有数值实验均在单块 NVIDIA RTX 3090 (24GB) GPU 上完成。
*   **训练时长**：文中详细列出了各系统的训练时间，从几分钟到几十分钟不等（如 6-DoF 水下航行器训练耗时约 5 分钟，四足机器人约 5 分钟）。

### 5. 实验数量与充分性
*   **实验规模**：共测试了 15 个系统，并进行了多组消融实验（如残差架构消融、PDE 损失权重敏感性分析、步长 $\Delta t$ 影响分析）。
*   **客观性**：采用了统一的验证协议，并对每个配置运行了 5 个随机种子以计算均值和标准差，实验设计严谨且具有较强的说服力。

### 6. 主要结论与发现
*   **性能优势**：VertexCBF 在绝大多数系统中均能恢复出比基准方法更大、更可靠的安全集。
*   **监督有效性**：相比于全控制空间采样（FCD），顶点受限搜索生成的监督信号更能引导模型收敛到最优解，避免了保守性。
*   **硬件可行性**：在真实移动机器人避障实验中，该方法表现出良好的实时性和鲁棒性，能够处理非理想动力学带来的偏差。

### 7. 优点
*   **计算高效**：通过顶点受限将搜索空间降维，极大提升了监督数据生成的效率。
*   **理论支撑**：基于 HJ 可达性分析，具有明确的数学意义。
*   **工程实用**：残差架构和软约束处理使得该方法在真实机器人部署时具有较好的容错性。

### 8. 不足与局限
*   **缺乏形式化保证**：目前尚无严格的收敛性证明或确定性误差界，主要依赖后验验证。
*   **扩展性边界**：虽然在 13 维系统上表现良好，但对于极高维的全身动力学模型（如人形机器人），树搜索的计算开销仍可能成为瓶颈。
*   **超参数敏感性**：搜索深度 $K$ 和步长 $\Delta t$ 的选择需要针对特定系统进行调优，以平衡计算精度与覆盖范围。

（完）
