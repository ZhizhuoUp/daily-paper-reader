---
title: Real-Time Shape Control of Multi-Segment Soft Robotic Arms Using Koopman Operators with Global and Local Observables
title_zh: 结合全局与局部可观测量的基于 Koopman 算子的多段软体机械臂实时形状控制
authors: "Jiahe Wang, Eron Ristich, Sultan Haidar Ali, Eric weissman, Lei Zhang, Wanxin Jin, Yi Ren, Jiefeng Sun"
date: 2026-09-02
pdf: "https://arxiv.org/pdf/2609.03175v1"
tags: ["query:q8"]
score: 9.0
evidence: 多段机器人手臂的实时形状控制
tldr: 针对多段软体机械臂高维非线性动力学导致的形状控制难题，本文提出了结合全局与局部可观测量的库普曼（Koopman）模型预测控制框架。该框架克服了段间耦合与惯性干扰，实现了最高10段机械臂的高效扩展，在物理实验中展现出极佳的实时性、强鲁棒性及受限空间应用潜力。
selection_source: fresh_fetch
motivation: 多段软体机械臂在受限空间中仅凭末端控制或全局形状误差难以克服段间耦合与非线性动力学挑战。
method: 提出一种结合全局与局部可观测量的 Koopman 模型预测控制框架，以精确建模并线性化软体臂的复杂动力学。
result: 数值仿真实现多达10段的形态控制，物理实验验证了3段和5段臂的高速实时控制（达0.6 m/s）及强抗干扰能力。
conclusion: 该框架为多段软体机械臂提供了高效、可扩展且高精度的实时形状控制方案，具备广阔的受限空间应用前景。
---

## 摘要
多段软体机械臂能够连续重构其形体形状以实现安全交互，但仅进行末端控制对于受限空间任务而言是不够的。因此，相比于末端控制，形状控制对于多段软体机械臂是更为关键的任务，但由于连续体变形的高维特性与非线性动力学，形状控制仍极具挑战性。在现有工作中，形状控制精度通常由全局坐标系下的误差（全局形状误差）来定义。对于多段软体机械臂而言，仅将全局形状误差作为控制目标是不够的，因为段间耦合、重力诱导载荷以及惯性效应会变得更加显著，且这一难度随着段数的增加而增加。在本文中，我们提出了一种结合全局与局部可观测量的基于 Koopman 算子的模型预测控制框架，实现了多段软体机械臂的实时形状控制。该框架通过数值实验和物理实验进行了评估。数值实验表明，所提出的控制器在包含多达 10 个独立驱动段的机器人上实现了形状控制，证明了其可扩展性。物理实验表明，该控制器具备以下能力：（1）实现末端速度高达 0.6 m/s 的 3 段和 5 段机械臂的实时形状控制；（2）在无需重新训练的情况下实现鲁棒跟踪，包括承受高达 400 g 的末端负载以及从 7 N 侧向干扰中恢复；（3）通过受限空间演示展示了未来用于检测应用的潜力。这些结果表明，所提出的框架实现了多段软体机械臂动态、可扩展且精确的实时形状控制。

## Abstract
Multi-segment soft robotic arms can continuously reconfigure their body shapes for safe interaction, but tip control alone is insufficient for constrained-space tasks. Therefore, shape control is a more important task for multi-segment soft arms than tip control, but remains challenging due to the high dimensionality and nonlinear dynamics of continuum deformation. In existing work, shape control accuracy is defined by the error in the global frame (global shape error). For multi-segment soft arms, using only global shape error as the control objective is insufficient, as segment coupling, gravity-induced loading, and inertial effects become more significant. This difficulty increases with the number of segments. In this paper, we present a Koopman-based model predictive control framework that combines global and local observables, enabling real-time shape control on multi-segment soft robotic arms. The framework is evaluated through numerical and physical experiments. Numerical experiments demonstrate the scalability of the proposed controller by achieving shape control on robots with up to 10 independently actuated segments. The physical experiments demonstrate that the controller is capable of (1) real-time shape control of 3- and 5-segment robotic arms with tip speeds up to 0.6 m/s, (2) robust tracking without retraining, including distal payloads up to 400~g and recovery from a 7~N lateral disturbance, and (3) the potential for future inspection applications through a confined-space demonstration. These results demonstrate that the proposed framework enables dynamic, scalable, and accurate real-time shape control on multi-segment soft robotic arms.

---

## 论文详细总结（自动生成）

以下是对论文《Real-Time Shape Control of Multi-Segment Soft Robotic Arms Using Koopman Operators with Global and Local Observables》的详细结构化中文总结：

---

### 1. 核心问题与整体含义（研究动机和背景）

*   **研究背景**：多段软体机械臂具有连续变形和本征柔顺性，在受限空间（如微创手术、复杂狭窄环境探索）中具备独特的安全交互优势。
*   **核心痛点**：
    1.  **形状控制 vs. 末端控制**：传统的末端（Tip）控制无法约束软体臂无限维度的躯体变形。在狭窄或多障碍物环境中，必须对软体臂的**全局全形（Full-body shape）**进行精确控制。
    2.  **高维非线性与段间耦合**：随着段数增加，近端结构需承受远端结构的重力、动力学载荷与惯性效应，高维连续体变形极难实时建模与控制。
    3.  **传统观测量的局限（重叠投影歧义）**：现有基于 Koopman 算子的控制方法大多仅使用全局坐标系下的观测量（Global Observables）。在多段软体臂大变形情况下，不同躯体姿态在全局投影下可能出现重叠（Overlap projection），导致状态映射非唯一，从而引发局部变形欠拟合或控制不稳定。

---

### 2. 论文提出的方法论

#### 核心思想
提出了结合**全局观测量（Global Observables）**与**局部观测量（Local Observables）**的库普曼模型预测控制（Koopman-based MPC）框架。通过在 Koopman 提升函数（Lifting Function）中同时引入全局任务空间位置与局部段坐标系下的变形特征，使系统既能保持全局任务空间的位置一致性，又能精确约束局部段间变形。

#### 关键技术细节与流程
1.  **观测量映射映射设计（Observable Mapping Design）**：
    *   **全局观测量 ($h_{\text{Global}}$)**：将骨架采样点映射到全局基坐标系 $\{F\}$ 的 x-z 平面上，消除难以精确建模的伸缩方向非线性，捕获全局任务空间位置。
    *   **局部观测量 ($h_{\text{Local}}$)**：将采样点映射到其所在段的局部基坐标系 $\{B_{\lambda(i)}\}$ 的 x-z 平面上，避免全局投影的模糊性，精准捕捉段内局部弯曲变形。
    *   **组合观测量 ($h_{\text{Combined}}$)**：拼接全局与局部观测量。
2.  **Koopman 提升函数与模型辨识**：
    *   提升状态定义为：$z_k = \theta_x(x_k) = \Psi(h(x_k))$，其中 $\Psi$ 为结合历史状态的时延坐标（Delay coordinates），用于捕捉隐式动力学（速度、内力等）。
    *   使用带控制的扩展动态模式分解（EDMDc）结合 **LASSO（L1 正则化）** 求解 Koopman 算子矩阵，抑制噪声并提高矩阵稀疏性与系统稳定性。
3.  **稠密模型预测控制（Dense MPC）**：
    *   采用控制增量 $\Delta u$ 作为控制输入，避免过快驱动引发未建模的剧烈惯性效应。
    *   利用 Dense MPC 形式消去中间状态变量，直接将标准 MPC 转化为仅针对控制输入序列二次规划（QP）问题。
    *   在目标函数中设置对角块权重矩阵 $Q_{\text{Combined}} = \text{diag}(Q_{\text{Global}}, Q_{\text{Local}})$，在求解 QP 时同时惩罚全局和局部误差。

---

### 3. 实验设计

#### 实验场景与硬件
*   **仿真环境**：基于 Kirchhoff 杆模型与 BDF-$\alpha$ 动态求解器构建高精度数值仿真，用于评估段数扩展性（3段、5段、8段、10段）。
*   **物理硬件平台**：
    *   构建了 3 段（长 0.6 m）和 5 段（长 1.0 m）线驱动软体机械臂。
    *   每段采用超弹性 NiTi 合金作为主骨架，外侧为 TPU 3D 打印的柔顺波纹管结构，电机独立安装于每段基座。
    *   使用 OptiTrack 光学动作捕捉系统（100 Hz）进行实时骨架姿态重建。

#### Baseline 与对比方法
*   **Global Observables K-MPC**（基于全局可观测量的现有主流 Koopman 控制 Baseline）。
*   **Local Observables K-MPC**（消融对比）。
*   **Combined Observables K-MPC**（本文提出的组合观测量方法）。

#### 验证任务
1.  **扩展性验证（仿真）**：在 3、5、8、10 段机器人上测试静态/动态形状跟踪能力。
2.  **物理静态重复性与动态不确定性基准测试**。
3.  **实时形状跟踪（物理）**：包括慢速（周期 25s）与高速（周期 6s，末端速度达 0.6 m/s）跟踪。
4.  **多姿态形状跟踪（物理）**：在 5 段臂上连续跟踪 6 种包含三维双重弯曲、非对称变形及螺旋状的复杂几何形状。
5.  **抗干扰与鲁棒性（物理，未重新训练）**：
    *   末端挂载附加负载（200 g 及 400 g，约占臂重 26.1%）。
    *   连续侧向风场干扰。
    *   突发脉冲式侧向拉力干扰（峰值约 7 N）。
6.  **受限空间穿越与检测演示（物理）**：引导 5 段臂穿过圆形孔洞并用末端相机观察目标。

---

### 4. 资源与算力

*   **计算资源**：Dense MPC 优化问题在标准 CPU（2.2 GHz）上使用 OSQP 求解器求解，控制更新频率达到 **300 Hz 以上**（物理实验 loop 运行在 100 Hz）。
*   **数据集规模与训练时间**：
    *   仿真数据集：采集 200,000 个快照（Snapshots）。
    *   物理数据集：采集 600,000 个快照（约 1.6 小时实际运行数据）。
*   **GPU 使用**：论文中未特别指出依赖 GPU 进行实时推理或训练，主要依赖 CPU 上的线性代数与 QP 优化求解。

---

### 5. 实验数量与充分性

*   **仿真统计充分性**：针对 3、5、8、10 段模型各运行了 200 组独立仿真任务（每组 200 个时间步），绘制了 25%-75% 分位数误差区间，具有极强的统计学说服力。
*   **物理实验重复性**：
    *   静态重复性：使用拉丁超立方采样选取 20 个目标姿态，每个重复 20 次实验。
    *   动态不确定性：对 15s 轨迹进行 20 次连续重复循环运行。
*   **消融与对比充分**：全方位对比了 Global、Local 和 Combined 三种可观测量在不同段数、不同速度、不同干扰条件下的全局误差
