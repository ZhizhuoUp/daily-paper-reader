---
title: "MR-SPITE: Accelerating Multi-Robot Conflict Scans via Hierarchical Swept-Volume Approximations"
title_zh: MR-SPITE：通过分层扫掠体近似加速多机器人冲突扫描
authors: "Marta Markowicz, James Motes, Marco Morales, Nancy Amato"
date: 2026-09-20
pdf: "https://arxiv.org/pdf/2609.23928v1"
tags: ["query:q1"]
score: 8.0
evidence: 加速多机器人冲突扫描与碰撞检测
tldr: MR-SPITE 是一种用于加速多机器人路径冲突检测的层次化扫描过滤方法。该方法通过将机器人路径划分为时间片段并建立保守边界，在进行详细碰撞检测前快速剔除无冲突的时间窗口。实验表明，该方法在 16 个 Fetch 机器人场景下显著提升了冲突扫描速度并大幅缩短了规划时间，有效优化了多机器人路径规划的计算效率。
selection_source: fresh_fetch
motivation: 多机器人路径规划中频繁的冲突检测计算开销巨大，亟需一种高效的过滤机制来减少冗余的碰撞检查。
method: 通过将路径划分为时间片段并利用保守边界进行预筛选，仅对无法排除冲突的时间窗口执行底层碰撞检测。
result: "在 16 个 Fetch 机器人的测试中，冲突扫描速度提升了 7.18 倍，整体规划时间缩短了 57%。"
conclusion: MR-SPITE 证明了基于运动片段的边界过滤能与配置级碰撞检测互补，是提升多机器人系统规划效率的有效手段。
---

## 摘要
针对同步机器人路径的冲突扫描需要进行详细的碰撞检测，这可能涉及每个时间步长下所有机器人对的检查，且在冲突修复过程中需多次重复。我们提出了多机器人 SPITE (MR-SPITE)，这是一种基于运动片段的保守型过滤器，旨在加速此类扫描。MR-SPITE 将每条路径划分为时间间隔，并为每个片段分配保守边界。区间调度器会对时间上重叠的运动边界进行比较：不相交的边界可证明该共享窗口无冲突，而未解决的窗口则传递给底层的碰撞检测器。我们将 MR-SPITE 集成到 ARC 中，并结合了基于 VAMP 的碰撞检测。对于 16 个 Fetch 机器人，与基于 PRM+VAMP 的基准 ARC 实现相比，集成 MR-SPITE 的 ARC 在成对中位数冲突扫描速度上提升了 7.18 倍，并将中位数规划时间缩短了 57%。这些结果表明，运动片段边界能够与配置级碰撞加速形成互补，同时保持底层离散扫描器的行为特性。

## Abstract
Conflict scanning over synchronized robot paths requires detailed collision checking, potentially across every robot pair at every timestep, and may be repeated many times as conflicts are repaired. We present Multi-Robot SPITE (MR-SPITE), a conservative, motion-segment-based filter for accelerating these scans. MR-SPITE partitions each path into temporal intervals and assigns conservative bounds to each segment. An interval scheduler compares bounds for temporally overlapping motions: disjoint bounds certify the shared window as conflict-free, while unresolved windows are passed to the underlying collision checker. We integrate MR-SPITE into ARC and combine it with VAMP-based collision checking. For 16 Fetch robots, ARC with MR-SPITE achieves a paired median conflict scan speedup of 7.18x and reduces median planning time by 57% relative to the baseline ARC implementation with PRM+VAMP. These results demonstrate that motion-segment bounds complement configuration-level collision acceleration while preserving the behavior of the underlying discretized scanner.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《MR-SPITE: Accelerating Multi-Robot Conflict Scans via Hierarchical Swept-Volume Approximations》** 的结构化总结：

### 1. 核心问题与研究动机
*   **背景**：多机器人运动规划（MRMP）中，冲突检测是计算瓶颈。随着机器人数量增加，机器人对的组合呈二次方增长，且冲突驱动的规划器（如 ARC）在路径修复过程中需频繁重复执行“寻找首个冲突”（FFC）操作。
*   **动机**：现有的加速方法多集中在单配置的碰撞检测（如 VAMP），但缺乏针对“整个运动片段”的过滤机制。MR-SPITE 旨在通过保守的扫掠体近似，在详细碰撞检测前剔除无冲突的时间窗口，从而减少冗余计算。

### 2. 方法论
*   **核心思想**：将同步路径划分为时间片段，利用预计算的保守边界（如 OBB，有向包围盒）进行快速过滤。
*   **关键技术**：
    *   **路径分区**：将路径划分为 PRM 边、查询连接器、目标保持等区间。
    *   **区间调度器**：维护一个优先级队列，按时间顺序处理机器人对。若两个运动片段的保守边界不相交，则直接判定该时间窗口无冲突，跳过详细检查。
    *   **层次化过滤**：先进行全机器人边界测试，若重叠，再进行细粒度的“每链接（per-link）”边界测试。
    *   **兼容性**：该方法作为“过滤器”集成在现有规划器（如 ARC）中，不改变底层碰撞检测器的逻辑，确保结果的一致性。

### 3. 实验设计
*   **场景**：
    *   **Fetch 机器人**：移动交叉场景（Mobile Crossing），测试多机器人协作。
    *   **Franka Emika Panda**：笼子场景（Cage），测试高维关节机器人的复杂碰撞。
*   **基准（Benchmark）**：以 PRM + VAMP 作为基准线。
*   **对比方法**：对比了不同组合，包括 MR-SPITE、MR-SPITE-H（层次化）、VAMP、VAMP-H 以及 RRT-C 等。

### 4. 资源与算力
*   **硬件环境**：Linux 工作站，配备 Intel Core i7-14700F CPU 和 62 GiB 内存。
*   **说明**：论文未提及使用 GPU 进行训练或推理，主要依赖 CPU 进行向量化碰撞检测（VAMP）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4、8、16、32 个机器人等不同规模，并测试了不同复杂度的碰撞模型（球体数量从 1 到 128）。
*   **客观性**：通过 100 组随机路径进行统计，并对相同初始路径进行成对比较（Paired Comparison），实验设计严谨且具有统计学意义。

### 6. 主要结论与发现
*   **性能提升**：在 16 个 Fetch 机器人场景下，冲突扫描速度提升了 7.18 倍，整体规划时间缩短了 57%。
*   **可扩展性**：随着机器人数量和碰撞模型复杂度的增加，MR-SPITE 的优势愈发明显。
*   **互补性**：运动片段过滤（MR-SPITE）与配置级加速（VAMP-H）可以有效结合，提供比单一方法更好的性能。

### 7. 优点
*   **高效过滤**：通过“先粗后细”的层次化过滤，大幅减少了进入底层碰撞检测器的查询次数。
*   **通用性**：作为插件式模块，可轻松集成到现有的冲突驱动规划器中，无需重构底层逻辑。
*   **保守性**：保证了结果的正确性，不会漏掉任何潜在冲突。

### 8. 不足与局限
*   **预处理开销**：对于小型或简单的规划任务，构建保守边界的额外开销可能无法被扫描时间的节省所抵消。
*   **适用范围**：目前主要针对基于 roadmap 的规划器，对于其他类型的规划器（如基于优化的方法）的适配性有待验证。
*   **边界紧致度**：对于某些细长形状的机器人（如 Panda 的某些链接），简单的 OBB 可能过于保守，导致过滤效果不如预期。

（完）
