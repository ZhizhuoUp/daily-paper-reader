---
title: Coverage Path Planning for Redundant Manipulators using Generalized Spanning Trees
title_zh: 基于广义生成树的冗余机械臂覆盖路径规划
authors: "Raksi Kopo, Kostas J. Kyriakopoulos"
date: 2026-09-08
pdf: "https://arxiv.org/pdf/2609.08409v1"
tags: ["query:profile-1"]
score: 8.0
evidence: 冗余机械臂的覆盖路径规划及可行性评估
tldr: 本文针对冗余机械臂的表面覆盖路径规划问题，提出了一种基于广义生成树（JSTC）的覆盖算法。通过将每个网格单元的多个逆运动学解建模为广义最小生成树问题，该方法在离线和在线场景下均能有效选择最优配置，从而在保证无重复覆盖的同时，显著降低了机械臂的重构频率和关节运动成本。
selection_source: fresh_fetch
motivation: 冗余机械臂在表面覆盖任务中存在多逆运动学解选择困难，且配置直接影响运动质量。
method: 提出离线与在线联合生成树覆盖（JSTC）算法，将覆盖路径规划转化为广义最小生成树问题进行求解。
result: 仿真表明该方法相比现有技术显著减少了计算时间、重构次数及关节运动量。
conclusion: JSTC算法为冗余机械臂在静态与动态环境下的高效表面覆盖提供了鲁棒且优化的解决方案。
---

## 摘要
利用任务冗余机械臂进行表面覆盖具有挑战性，因为每个表面点可能对应多个逆运动学（IK）解，且构型选择会显著影响运动质量。本文通过离线和在线联合生成树覆盖（JSTC）算法，将经典的生成树覆盖（STC）方法扩展至冗余机械臂。离线 JSTC 为每个网格单元采样多个逆运动学解，并将问题建模为广义最小生成树（GMST）问题，通过为每个单元选择一种构型并遍历生成的树，从而获得一条不重复的覆盖路径。在线 JSTC 则在处理动态网格更新的同时，通过可行性和成本评估，以增量方式扩展和回溯生成树。仿真结果表明，与其他方法相比，离线 JSTC 减少了计算时间、重构次数和关节运动量，而在线 JSTC 在动态场景下实现了快速的单步规划。

## Abstract
Surface coverage with task-redundant manipulators is challenging because each surface point may admit multiple inverse kinematics (IK) solutions, and configuration choices strongly affect motion quality. This paper extends the classical Spanning Tree Coverage (STC) method to redundant manipulators through offline and online Joint Spanning Tree Coverage (JSTC) algorithms. Offline JSTC samples multiple Inverse Kinematics (IK) solutions per grid cell and formulates the problem as a Generalized Minimum Spanning Tree (GMST), selecting one configuration per cell and tracing the resulting tree to obtain a non-revisiting coverage path. Online JSTC incrementally expands and backtracks a spanning tree with feasibility and cost evaluation while handling dynamic grid updates. Simulation results show that offline JSTC reduces computation time, reconfigurations, and joint motion compared to other methods, while online JSTC achieves fast per-step planning in dynamic scenarios.

---

## 论文详细总结（自动生成）

你好！我是 Antigravity，很高兴为你分析这篇关于冗余机械臂覆盖路径规划的论文。以下是该论文的结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：在表面覆盖任务（如清洁、打磨、喷涂）中，冗余机械臂（自由度 > 5）因其运动学冗余，每个表面点对应多个逆运动学（IK）解。
*   **核心挑战**：如何从海量 IK 解中选择最优构型，以最小化关节运动成本、减少重构（reconfiguration）次数，并确保在静态或动态环境下实现高效、无重复的覆盖路径规划。

### 2. 方法论：联合生成树覆盖 (JSTC)
*   **核心思想**：将覆盖区域划分为网格（mega-cells），通过构建“广义最小生成树”（GMST）来选择最优 IK 解序列，并利用 STC（生成树覆盖）算法进行路径追踪。
*   **关键技术细节**：
    *   **离线 JSTC**：
        1.  **IK 采样与传播**：为每个网格采样多个 IK 解，并利用 DBSCAN 聚类合并相似解，确保解能覆盖子单元。
        2.  **图构建**：构建一个以网格为簇的广义图，边权重基于关节运动距离及重构惩罚。
        3.  **GMST 求解**：使用启发式算法求解 GMST，选出最优构型路径。
        4.  **子单元追踪**：遵循 STC 规则，在不跨越树边的情况下遍历所有子单元。
    *   **在线 JSTC**：
        1.  **增量式构建**：通过“扩展（Extension）”和“回溯（Backtracking）”操作动态更新生成树。
        2.  **动态更新**：支持网格的添加与移除，当环境变化导致树断裂时，将剩余部分视为独立组件处理。

### 3. 实验设计
*   **场景与 Benchmark**：
    *   **离线**：使用“Scan Floor”基准测试，模拟 7-DoF Franka Emika Panda 机械臂进行表面扫描。
    *   **在线**：使用 7-DoF KUKA LBR iiwa 机械臂进行桌面清洁模拟，涵盖自由网格、外部障碍物、内部障碍物、断开区域及动态更新等 5 种场景。
*   **对比方法**：与 JGTSP（广义旅行商问题）和 HJGTSP（分层加速版）进行对比。

### 4. 资源与算力
*   **硬件配置**：实验在配备 Intel Core i9-11900H 处理器（2.50 GHz）和 32 GB RAM 的笔记本电脑上完成。
*   **算力说明**：文中未提及使用 GPU 进行加速，主要依赖 CPU 进行优化求解（如 CasADi 的 IPOPT 求解器）。

### 5. 实验数量与充分性
*   **实验规模**：离线实验重复 10 次以消除采样随机性；在线实验涵盖了 5 种典型动态场景。
*   **评价**：实验设计较为充分，通过计算时间、重构次数、关节运动量及路径误差等多个维度进行量化评估，对比基准明确，具有较好的客观性。

### 6. 主要结论与发现
*   **性能提升**：离线 JSTC 在计算效率、重构次数和关节运动平滑度上均优于 JGTSP 和 HJGTSP。
*   **在线鲁棒性**：在线 JSTC 能够以极低的单步计算时间（< 70ms）处理动态环境更新，证明了其在实时应用中的可行性。

### 7. 优点
*   **计算效率高**：通过将问题转化为 GMST 而非复杂的 GTSP，显著降低了组合优化问题的复杂度。
*   **灵活性强**：在线算法能够处理动态障碍物和网格更新，适应性优于传统的离线规划方法。
*   **运动质量优化**：通过显式惩罚重构行为，有效提升了机械臂的运动平滑度。

### 8. 不足与局限
*   **局部最优风险**：在线算法主要进行局部优化，相比离线全局优化，在某些复杂场景下可能无法达到全局最优解。
*   **应用限制**：目前主要针对平面或类平面网格，尚未在复杂曲面或真实物理硬件上进行实验验证。
*   **依赖性**：算法性能在很大程度上依赖于 IK 采样的质量和密度，若采样不足可能导致覆盖失败。

（完）
