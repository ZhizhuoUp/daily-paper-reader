---
title: "Online, Reachability-Aware, Sampling-Based Motion Planning"
title_zh: 在线、具备可达性感知能力的基于采样的运动规划
authors: "Brendan Gould, Zhiyuan Zhang, Panagiotis Tsiotras, Samuel Coogan"
date: 2026-09-08
pdf: "https://arxiv.org/pdf/2609.09073v1"
tags: ["query:profile-1"]
score: 9.0
evidence: 在线、可达性感知、基于采样的运动规划
tldr: 该论文提出了一种在线、感知可达性的采样模型预测控制（MPC）算法，旨在解决机器人导航中缺乏硬安全保证的问题。通过引入快速的区间计算流水线，该方法能在运行过程中实时生成可达集过近似，从而在无需昂贵预计算的前提下，显著提升了复杂系统的安全性与可扩展性，并在仿真与真实赛车实验中验证了其卓越的防撞性能。
selection_source: fresh_fetch
motivation: 传统采样MPC算法在机器人导航中缺乏硬性安全保障，且现有可达性规划方法依赖昂贵的预计算。
method: 提出了一种基于区间计算的在线可达集过近似流水线，将其集成到采样MPC框架中以实现实时安全规划。
result: "该方法在赛车仿真中将安全违规率降低了99%以上，并成功在真实硬件上实现了无碰撞控制。"
conclusion: 该研究证明了在线可达性分析能够以低计算成本为复杂机器人系统提供高效且可靠的安全保障。
---

## 摘要
基于采样的模型预测控制（MPC）算法是一类灵活的控制器，广泛应用于各类机器人系统的导航任务。传统上，此类方法缺乏严格的安全保证；我们在本研究中通过一种基于区间的快速流水线，在线计算有保证的可达集上近似，从而弥补了这一缺陷。实验表明，我们的方法在无需昂贵预计算步骤的前提下，达到了与现有最先进的可达性规划器相当的性能，并能扩展至现有方法无法处理的复杂系统。最后，我们在赛车仿真中证明该技术将安全违规率降低了 99% 以上，并在真实硬件实验中成功控制模型赛车完成了行驶，且未发生任何碰撞。

## Abstract
Sampling-Based Model-Predictive Control (MPC) algorithms are a flexible class of controllers used for navigation on a wide range of robotic systems. Historically, such approaches have lacked hard safety guarantees, a shortcoming which we remedy in this work by computing guaranteed reachable-set overapproximations online with a fast, interval-based pipeline. We show that our method achieves similar performance to a state-of-the-art reachability-based planner without the need for the expensive pre-computation step, and can be scaled to systems that are infeasible using existing approaches. Finally, we demonstrate that our technique reduces safety violations by over 99% in a racing simulation and successfully controls a model racecar on real hardware experiments without crashes.

---

## 论文详细总结（自动生成）

这是一份关于论文《Online, Reachability-Aware, Sampling-Based Motion Planning》的结构化总结：

### 1. 核心问题与研究背景
*   **研究动机**：基于采样的模型预测控制（MPC，如 MPPI）在机器人导航中非常灵活，但缺乏硬性的安全保证。
*   **背景痛点**：现有的安全增强方法（如控制障碍函数 CBF）设计困难，而基于 Hamilton-Jacobi (HJ) 可达性分析的方法虽然能提供安全保证，但需要昂贵的离线预计算，且随着系统维度增加会遭遇“维度灾难”，难以扩展到复杂系统。

### 2. 方法论
*   **核心思想**：将“区间可达性分析”与“采样 MPC”结合，在运行过程中实时计算可达集过近似，从而剔除不安全的采样轨迹。
*   **关键技术**：
    *   **区间算术与嵌入系统**：利用区间包含函数（Inclusion Function）构建嵌入系统，通过求解嵌入系统的轨迹来包围原系统的所有可能状态（考虑了扰动）。
    *   **实时流水线**：利用 `immrax` 工具箱，结合 JAX 的高性能计算能力，在 GPU 上并行处理所有采样轨迹的区间演化。
    *   **算法流程**：在采样阶段，对每个候选控制序列计算其对应的区间轨迹；在选择阶段，仅从满足安全约束（即区间轨迹完全位于安全集内）的样本中选择最优控制。

### 3. 实验设计
*   **场景**：使用 6 自由度动态自行车模型（Dynamic Bicycle Model）的赛车在赛道上行驶。
*   **Benchmark**：对比了传统的采样 MPC（无安全过滤）以及最先进的基于 HJ 可达性的规划器（[20]）。
*   **对比维度**：安全性（碰撞次数）、性能（单圈时间）、计算开销（预计算需求）。

### 4. 资源与算力
*   **仿真环境**：使用了 Intel Xeon Gold 6230 CPU 和 NVIDIA Quadro RTX 8000 GPU。
*   **对比组算力**：HJ 可达性方法在 6D 模型上计算耗时超过 115 小时，生成文件达 2.4 GB。
*   **本文方法**：无需预计算，利用 GPU 并行化实现实时控制（60Hz）。
*   **硬件实验**：使用 Intel i9-10920X CPU 和 NVIDIA RTX 3090 GPU。

### 5. 实验数量与充分性
*   **规模**：在仿真中进行了 1080 次运行的参数扫描（包含 30 个初始状态、9 种权重组合、2 种扰动类型）。
*   **客观性**：实验设计了双盲对比，并明确了“碰撞”与“停滞”的终止条件，覆盖了多种参数变化，具有较好的统计学意义和公平性。

### 6. 主要结论与发现
*   **安全性提升**：在仿真中，该方法将安全违规率降低了 99.1% 以上。
*   **性能优势**：在无需预计算的情况下，达到了与 HJ 方法相当的安全性，且在 6D 复杂系统上表现出极佳的扩展性。
*   **鲁棒性**：该方法将安全保证与性能优化解耦，使得控制器对参数变化和扰动具有更强的鲁棒性。

### 7. 优点
*   **实时性强**：完全在线计算，消除了离线预计算的瓶颈。
*   **可扩展性**：能够处理高维系统（如 6D 车辆模型），克服了传统 HJ 方法的维度限制。
*   **理论支撑**：提供了明确的安全性证明（Theorem 1），确保在扰动边界内系统轨迹的安全性。

### 8. 不足与局限
*   **保守性**：由于区间分析的特性，可能存在过度保守的情况，导致某些实际上安全的控制被剔除。
*   **递归可行性**：缺乏递归可行性保证，即在某些极端情况下，可能无法采样到任何安全控制序列。
*   **模型依赖**：安全性高度依赖于对扰动边界的准确建模；如果真实扰动超出预设范围，安全保证将失效。
*   **采样限制**：由于是基于采样的，无法保证找到全局最优解，且在采样空间较小时可能找不到安全解。

（完）
