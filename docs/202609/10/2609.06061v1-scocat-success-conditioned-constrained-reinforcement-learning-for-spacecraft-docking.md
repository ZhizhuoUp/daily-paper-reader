---
title: "SCoCaT: Success Conditioned Constrained Reinforcement Learning for Spacecraft Docking"
title_zh: SCoCaT：用于航天器对接的成功条件约束强化学习
authors: "Aman Arora, Ricard Marsal I Castan, Matteo El-Hariry, Miguel Olivares-Mendez"
date: 2026-09-05
pdf: "https://arxiv.org/pdf/2609.06061v1"
tags: ["query:profile-1"]
score: 8.0
evidence: 用于航天器对接的受限强化学习与安全约束
tldr: 本文针对航天器对接等安全关键任务，指出基于终止条件的约束强化学习在处理终端导航任务时存在“可行性崩溃”问题，即智能体为规避约束而倾向于不完成任务。为此，研究提出了一种通过辅助价值评价器引入密集成功信号的改进方法，有效提升了任务完成率并保持了高安全性，并在CubeSat仿真及实验室浮动平台实验中验证了其有效性。
selection_source: fresh_fetch
motivation: 现有基于终止条件的约束强化学习在处理终端导航任务时，因生存加权目标导致智能体倾向于规避目标区域以避免约束违规，从而引发“可行性崩溃”。
method: 通过引入一个辅助价值评价器，为强化学习算法提供密集的每步成功信号，从而引导智能体在满足安全约束的同时完成任务目标。
result: 该方法在6U-CubeSat仿真平台及实验室零样本模拟到现实（sim-to-real）迁移测试中，显著提升了任务完成率并维持了严格的安全性。
conclusion: 该研究提出的简单且通用的增强方案有效解决了约束强化学习在终端导航任务中的性能瓶颈，为安全关键型机器人部署提供了可靠方案。
---

## 摘要
基于终止的约束强化学习在安全关键型机器人部署中极具吸引力：它避免了推理时的在线优化，通过每个约束的单一标量即可轻松扩展至多个约束，且比常用的拉格朗日方法更易于实现。该方法并非通过累加成本惩罚来衡量违规行为，而是通过缩短每次违规的有效视界，使违规行为在结构上变得“无利可图”。我们发现该类方法在终端导航任务中存在一种结构性失效模式：即在满足随最终接近而收紧的安全约束的同时，达到精确的目标构型。当目标位于约束开始生效的区域附近时，生存加权目标函数会使停留在目标区域外变得严格优于进入目标区域，从而导致高约束合规性但低任务完成率。我们对这种病态现象进行了形式化描述，并证明了对现成强化学习算法（如 PPO）进行最小化增强即可解决这种“可行性崩溃”。我们通过实验证明，通过辅助价值评价器添加密集的每步成功信号，可以在保持安全关键约束合规性的同时提高任务完成率。在两个代表性航天器平台上的验证——涵盖了操作性近距离作业的质量和自由度包络的 6U-CubeSat，以及用于实验室零样本仿真到现实迁移的浮动平台测试台——支持了这些发现的普适性。

## Abstract
Termination-based constrained reinforcement learning is attractive for safety-critical robotic deployments: it avoids online optimization at inference, scales easily to many constraints via a single scalar per constraint, and is simpler to implement than commonly used Lagrangian methods. Instead of pricing violations through summed cost penalties, this approach makes violations structurally unprofitable by shortening the effective horizon for each violation. We identify a structural failure mode of this method class on terminal-navigation tasks: reaching a precise goal configuration while satisfying safety constraints that tighten along the final approach. When the goal sits inside the region close to where the constraints become active, the survival-weighted objective makes dwelling outside the goal region strictly preferable to entering, producing high constraint compliance with low task completion. We formalize this pathology and show that a minimal augmentation to off-the-shelf RL algorithms like PPO resolves this ``feasibility collapse''. We empirically demonstrate that adding a dense per-step success signal via an auxiliary value critic improves the task completion rate while maintaining safety-critical constraint compliance. Validation across two representative spacecraft platforms: a 6U-CubeSat spanning the mass and degree-of-freedom envelope of operational proximity operations, and a floating platform testbed for zero-shot sim-to-real transfer in our laboratory, supports the generality of these findings.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《SCoCaT: Success Conditioned Constrained Reinforcement Learning for Spacecraft Docking》** 的结构化总结：

### 1. 核心问题与研究动机
*   **背景**：在航天器对接等安全关键型任务中，强化学习（RL）因其离线训练、在线推理高效（仅需神经网络前向传播）的特性，非常适合资源受限（SWaP）的平台。
*   **问题**：现有的“约束即终止”（CaT）方法通过将约束违规转化为随机终止来强制合规。然而，在终端导航任务中，由于目标区域往往位于约束生效的边缘，智能体为了避免违规（即避免触发终止），会倾向于在目标区域外“徘徊”，从而导致“可行性崩溃”（Feasibility Collapse）——即高安全性但任务完成率极低。

### 2. 方法论：SCoCaT
*   **核心思想**：在保留 CaT 约束机制的同时，引入一个与生存加权目标解耦的“成功信号”，通过辅助价值评价器（Success Critic）提供密集的梯度，引导智能体进入目标区域。
*   **关键技术**：
    *   **双价值流架构**：维护两个独立的价值评价器：$V_r$（基于 CaT 终止机制，处理安全约束）和 $V_s$（基于环境真实终止，处理任务成功）。
    *   **成功指标**：定义了每步成功指示器 $I_{succ,t}$，当智能体处于目标容差范围内时触发。
    *   **优势函数标量化**：将两个优势函数 $A_r$ 和 $A_s$ 进行标准化并求和，作为策略更新的统一信号，避免了引入额外的权重超参数。
    *   **连续成功信号**：针对稀疏奖励场景，提出了平滑的指数衰减函数作为连续成功信号，以提供更平滑的梯度。

### 3. 实验设计
*   **场景**：
    *   **3-DoF 浮动平台**：实验室空气轴承测试台，用于零样本（Zero-shot）模拟到现实（Sim-to-Real）迁移。
    *   **6U-CubeSat**：6自由度刚体仿真，模拟真实的航天器动力学。
*   **对比方法**：
    *   **PPO**：无约束基准（上限成功率，下限安全性）。
    *   **CaT-PPO**：直接前身（高安全性，但存在可行性崩溃）。
    *   **梯度投影法（PEGrad-CaT）**：对比了将成功梯度投影到奖励梯度零空间的方案。

### 4. 资源与算力
*   **算力说明**：论文使用了 NVIDIA Isaac Lab 进行仿真训练。文中未明确列出具体的 GPU 型号、数量或总训练时长，仅提到使用了 4,096 个并行环境进行训练。

### 5. 实验数量与充分性
*   **实验覆盖**：进行了多维度实验，包括：
    *   **基准对比**：在两个不同自由度平台上的性能对比。
    *   **消融实验**：针对奖励密度（密集/稀疏）、成功信号类型（二进制/连续）、以及是否使用双评价器架构进行了详尽消融。
    *   **Sim-to-Real**：在真实物理硬件上进行了零样本迁移验证。
*   **评价**：实验设计非常充分且客观，通过多组随机种子（5 seeds）保证了结果的统计显著性，并详细分析了不同约束等级（严格/中等/软约束）下的表现。

### 6. 主要结论与发现
*   **可行性崩溃的本质**：证明了该病态现象是由约束终止通道引起的，而非奖励塑造问题。
*   **有效性**：SCoCaT 在保持与 CaT 相当的极高安全合规性的同时，将任务完成率（TCR）提升了数百倍（在浮动平台上从 0.4% 提升至 100%）。
*   **普适性**：该方法在 3-DoF 和 6-DoF 平台上均表现出良好的泛化能力。

### 7. 优点
*   **简洁性**：架构改动极小，无需复杂的在线优化器，完全符合 SWaP 约束。
*   **解耦设计**：通过双评价器解耦安全与成功目标，巧妙规避了奖励函数与约束惩罚之间的冲突。
*   **理论支撑**：对“可行性崩溃”进行了数学形式化证明，并推导了 SCoCaT 能够打破该平衡的条件。

### 8. 不足与局限
*   **超参数依赖**：虽然避免了权重超参数，但最优的安全-成功权衡仍是任务相关的，缺乏自适应调整机制。
*   **Sim-to-Real 差距**：在真实硬件上观察到了角速度的系统性偏差，表明仿真环境在动力学建模上仍有提升空间。
*   **鲁棒性测试**：未在观测噪声较大的环境下测试算法的鲁棒性。
*   **双峰分布**：在 6-DoF 任务中，部分种子表现出双峰分布，说明训练过程在复杂动力学下仍存在一定的不稳定性。

（完）
