---
title: Networked Admissibility-Preserving Control for Directed Safe Coordination
title_zh: 面向有向安全协同的网络化容许性保持控制
authors: "Abhinav Sinha, Lohitvel Gopikannan, Shashi Ranjan Kumar"
date: 2026-09-08
pdf: "https://arxiv.org/pdf/2609.09384v1"
tags: ["query:q2"]
score: 8.0
evidence: 针对受限动力学的安全性关键协作与容许性保持控制。
tldr: 本文研究了受限物理输入下多智能体系统的安全协同控制问题。针对有向通信拓扑，提出了一种网络化容许性保持控制（APC）架构，通过引入对数障碍坐标和输入实现机制，确保智能体在满足异构非对称输入约束的同时，始终保持在动态安全走廊内，并实现指数级一致性。该研究为复杂约束下的分布式协同提供了理论保障。
selection_source: fresh_fetch
motivation: 解决多智能体系统在物理输入受限及动态安全约束下的协同控制难题。
method: 提出了一种结合对数障碍坐标与容许性保持输入实现（APIR）的网络化控制架构。
result: 系统实现了对安全走廊和执行器边界的前向不变性，并保证了闭环系统的指数级一致性。
conclusion: 该方法通过级联控制结构有效处理了非对称约束，为有向网络下的安全协同提供了闭式解与理论证明。
---

## 摘要
本文研究了通过受限物理输入动力学实现分布式指令的标量智能体的安全关键协同问题。智能体在具有有向生成树的固定加权有向图上进行通信，其输出必须保持在共同的移动安全走廊内，且其实际输入必须满足异构的非对称边界。我们提出了一种网络化容许性保持控制（APC）架构，其中容许性保持输入实现（APIR）控制物理输入，并利用对数障碍坐标表示安全走廊。该综合方法产生了一个精确的级联结构，其中呈指数衰减的实现误差驱动非对称一致性动力学。对于每一个相容的紧致初始集，闭环系统均存在唯一的完整解，使移动走廊和执行器区间保持前向不变且具有均匀裕度，确保指令有界，并实现指数一致性。我们推导了方向特定的充分条件，以确保正向和负向控制需求保持在相应的执行器限制范围内。分析得出了由左Perron向量和初始APIR失配决定的闭式障碍坐标极限。在强连通性以及所述增益和相容性条件下，部分钉扎（partial pinning）将恒定的障碍参考从非空的受控子集传播出去，并分配诱导的安全走廊轨迹。一个非权重平衡的算例展示了方向性证书和预测的集体运动。

## Abstract
This paper addresses safety-critical coordination for scalar agents whose distributed commands are implemented through constrained physical-input dynamics. Agents communicate over a fixed weighted digraph with a directed spanning tree, while their outputs must remain inside a common moving safety corridor and their realized inputs must satisfy heterogeneous asymmetric bounds. We propose a networked Admissibility-Preserving Control (APC) architecture in which an Admissibility-Preserving Input Realization (APIR) governs physical inputs and a logarithmic barrier coordinate represents the safety corridor. The synthesis yields an exact cascade in which exponentially decaying realization errors drive nonsymmetric consensus dynamics. For every compatible compact initial set, the closed-loop system admits a unique complete solution, renders the moving corridor and actuator intervals forward invariant with uniform margins, keeps commands bounded, and achieves exponential consensus. We derive direction-specific sufficient conditions under which positive and negative control demands remain within their corresponding actuator limits. The analysis yields a closed-form barrier-coordinate limit determined by the left Perron vector and initial APIR mismatch. Under strong connectivity and the stated gain and compatibility conditions, partial pinning propagates a constant barrier reference from a nonempty informed subset and assigns the induced safety corridor trajectory. A non-weight-balanced example illustrates the directional certificate and predicted collective motion.

---

## 论文详细总结（自动生成）

这是一份关于论文《面向有向安全协同的网络化容许性保持控制》（Networked Admissibility-Preserving Control for Directed Safe Coordination）的结构化总结：

### 1. 核心问题与研究动机
*   **背景**：在多智能体系统（MAS）中，安全协同要求智能体在满足输出约束（安全走廊）的同时，还要处理物理执行器的异构非对称约束。
*   **挑战**：现有的障碍函数方法多处理代数输入或静态饱和，忽略了执行器动力学（即从指令到物理输入的瞬态过程）。在有向图（尤其是非权重平衡图）中，执行器瞬态会通过拓扑结构影响最终的一致性值，导致系统可能偏离安全区域。
*   **目标**：提出一种网络化容许性保持控制（APC）架构，将执行器实现视为动态状态，确保在有向通信拓扑下，系统能同时满足输出安全走廊和执行器边界的前向不变性。

### 2. 方法论
*   **核心思想**：构建一个“精确级联”结构，将物理输入实现（APIR）与网络一致性动力学解耦。
*   **关键技术**：
    *   **对数障碍坐标**：引入障碍坐标 $z_i$ 将输出约束映射到实数域，从而将受限协同问题转化为无约束的一致性问题。
    *   **APIR 机制**：将物理输入 $u_i$ 建模为具有独立初始条件的动态状态，通过设计的指令 $v_i$ 驱动 $u_i$ 保持在执行器区间内。
    *   **级联控制**：通过设计反馈控制律，使得 APIR 误差呈指数衰减，并驱动网络一致性动力学。
    *   **相容性证书**：推导了方向特定的充分条件，用于验证给定的初始状态是否能在有限的执行器权限下实现安全协同。

### 3. 实验设计
*   **场景设置**：论文设计了三个代表性场景，均基于 5 个单积分器智能体：
    1.  **强连通有向图（G1）**：验证在复杂拓扑下的安全协同能力。
    2.  **仅含生成树的有向图（G2）**：验证非强连通拓扑下的鲁棒性。
    3.  **部分钉扎（Partial Pinning）**：验证通过部分智能体引入参考轨迹，实现全网协同的能力。
*   **Benchmark**：论文主要通过对比理论预测的边界与实际仿真轨迹，验证所提方法的有效性，而非与特定算法进行性能对比。

### 4. 资源与算力
*   **说明**：论文未明确提及实验所使用的计算资源（如 GPU 型号、数量或训练时长）。考虑到该研究属于控制理论范畴，其仿真主要基于数值微分方程求解（如 MATLAB/Simulink），对算力要求较低。

### 5. 实验数量与充分性
*   **实验数量**：共进行了三组主要仿真实验，涵盖了不同的拓扑结构和控制模式。
*   **充分性与客观性**：实验设计涵盖了理论中提到的核心假设（强连通、生成树、钉扎），且通过数值仿真直观展示了状态轨迹、指令信号和输入边界，验证了理论结论的正确性。实验设计客观，能够充分支撑论文提出的理论框架。

### 6. 主要结论与发现
*   **前向不变性**：证明了在满足相容性条件下，系统能确保输出始终在安全走廊内，且输入始终在执行器区间内。
*   **一致性收敛**：实现了指数级一致性，并给出了由初始 APIR 失配决定的闭式一致性值。
*   **方向性优势**：证明了所提出的单侧相容性测试比传统的对称测试更具包容性，能处理更广泛的非对称执行器约束。

### 7. 优点
*   **理论严谨**：将执行器动力学显式纳入闭环系统，弥补了传统静态饱和模型的不足。
*   **通用性强**：不依赖于图的权重平衡性，适用于更广泛的有向图拓扑。
*   **闭式解**：提供了明确的相容性条件和一致性值计算公式，便于工程应用中的参数整定。

### 8. 不足与局限
*   **应用限制**：目前仅针对标量智能体，未扩展至高阶动力学系统。
*   **鲁棒性待验证**：论文未深入讨论系统参数不确定性或外部扰动下的鲁棒性（作者在结论中将其列为未来工作）。
*   **切换拓扑**：当前研究基于固定拓扑，对于切换拓扑下的安全协同尚未给出完整证明。

（完）
