---
title: "MM-LMPC: Multi-Modal Learning Model Predictive Control via Mode-Specific Terminal Design and Bandit-Based Exploration"
title_zh: MM-LMPC：基于模态特定终端设计与多臂老虎机探索的多模态学习模型预测控制
authors: "Wataru Hashimoto, Kazumune Hashimoto, Masako Kishida"
date: 2026-09-14
pdf: "https://arxiv.org/pdf/2609.15623v1"
tags: ["query:q3"]
score: 8.0
evidence: 用于迭代控制任务的学习模型预测控制
tldr: 针对传统学习模型预测控制（LMPC）在迭代任务中过度依赖初始数据、缺乏探索性的问题，本文提出了多模态LMPC（MM-LMPC）框架。该方法通过将历史轨迹聚类为不同运动模式，构建模式特定的终端约束与代价函数，并结合基于置信下界（LCB）的元控制器进行模式选择，在提升探索效率的同时，有效解决了单一终端记忆带来的偏差问题，并从理论上保证了递归可行性与稳定性。
selection_source: carryover_cache
motivation: 传统LMPC容易陷入局部最优，难以充分探索可能带来更低成本的替代路径。
method: 通过轨迹聚类划分运动模式，并设计模式特定的终端约束与代价函数，结合LCB元控制器实现自适应模式选择。
result: 在多路径避障任务中，MM-LMPC展现出更强的探索能力，并显著降低了控制成本。
conclusion: 该方法在保持系统稳定性和递归可行性的前提下，实现了更优的收敛性能和多模态任务适应性。
---

## 摘要
学习模型预测控制（LMPC）通过利用先前的执行记录来构建MPC问题的终端约束和终端代价，从而提升迭代控制任务的性能。尽管该方法行之有效，但对历史轨迹的复用使得LMPC对初始数据较为敏感。具体而言，LMPC可能会反复利用具有较优剩余代价（cost-to-go）的存储轨迹，而对那些经过进一步改进后可能产生更低代价的替代路径模式探索不足。为解决这一问题，我们提出了多模态LMPC（MM-LMPC）。该框架将历史轨迹聚类为不同的运动模态，为每个模态构建特定的LMPC控制器，并利用基于下置信界（LCB）的元控制器来选择在每次迭代中执行的模态控制器。模态信息通过两种设计被纳入终端约束和终端代价中：硬约束设计使用由各模态相关数据构建的模态特定终端约束和终端代价；软正则化设计则保留共享的终端约束，同时在终端代价中加入基于隶属度的惩罚项。这些设计在保留LMPC递归可行性和稳定性结构的同时，减少了因将所有轨迹汇集到单一终端内存中而产生的偏差。理论分析表明，两种设计均能保持递归可行性和闭环稳定性。对于硬约束设计，我们进一步证明了模态代价收敛性、渐近最优模态性能，以及在LCB规则下的对数累积遗憾界。在多路径避障任务中的仿真结果表明，MM-LMPC相较于标准LMPC提升了探索能力并实现了更低的代价。

## Abstract
Learning Model Predictive Control (LMPC) improves iterative control tasks by using previous executions to construct the terminal constraint and terminal cost of the MPC problem. Although effective, this reuse of past trajectories can make LMPC sensitive to the initial data. In particular, LMPC may repeatedly exploit stored trajectories with favorable cost-to-go values while insufficiently exploring alternative route patterns that could yield lower cost after further improvement. To address this issue, we propose Multi-Modal LMPC (MM-LMPC). The proposed framework clusters past trajectories into motion modes, constructs a mode-specific LMPC controller for each mode, and uses an LCB-based meta-controller to select which mode-specific controller to execute at each iteration. Mode information is incorporated into the terminal constraint and terminal cost through two designs. The hard-constrained design uses mode-specific terminal constraints and terminal costs constructed from the data associated with each mode. The soft-regularized design retains a shared terminal constraint while adding membership-based penalties to the terminal cost. These designs reduce the bias caused by pooling all trajectories into a single terminal memory while retaining the recursive feasibility and stability structure of LMPC. Our theoretical analysis shows that both designs preserve recursive feasibility and closed-loop stability. For the hard-constrained design, we further establish mode-wise cost convergence, asymptotic best-mode performance, and a logarithmic cumulative regret bound under the LCB rule. Simulations on multi-route obstacle-avoidance tasks show that MM-LMPC improves exploration and achieves lower costs than standard LMPC.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《MM-LMPC: Multi-Modal Learning Model Predictive Control via Mode-Specific Terminal Design and Bandit-Based Exploration》** 的结构化总结：

### 1. 核心问题与研究动机
*   **背景**：学习模型预测控制（LMPC）通过复用历史轨迹数据来优化迭代任务的终端约束和代价，从而提升控制性能。
*   **问题**：标准 LMPC 存在严重的“路径依赖”问题。由于所有历史轨迹被汇集到单一的终端内存中，优化器倾向于反复利用初始阶段表现较好（代价低）的路径，导致其他潜在的、可能在经过进一步优化后表现更优的路径模式被忽视（探索不足）。

### 2. 方法论
*   **核心思想**：引入“多模态”概念，将任务分解为多个 qualitatively distinct（定性不同）的运动模态，并为每个模态维护独立的终端信息。
*   **关键技术**：
    *   **模态识别**：通过聚类或分类方法将轨迹关联到特定模态。
    *   **双层控制架构**：
        *   **上层（元控制器）**：基于多臂老虎机（MAB）的 LCB（Lower Confidence Bound）规则，在“利用”已知最优模态和“探索”未充分测试模态之间进行平衡。
        *   **下层（模态特定 LMPC）**：
            *   **硬约束设计**：严格隔离不同模态的终端约束和代价，确保模态间的纯粹性。
            *   **软正则化设计**：保留共享的终端安全集，但在终端代价中加入基于模态隶属度的惩罚项，既允许跨模态参考，又引导优化器偏向当前选定模态。

### 3. 实验设计
*   **场景**：Dubins 车辆在包含三个椭圆障碍物的环境中的“到达-避障”（reach-avoid）任务。
*   **Benchmark**：标准 LMPC。
*   **对比方法**：硬约束 MM-LMPC 与软正则化 MM-LMPC。
*   **评价指标**：闭环控制代价（时间）、首次达到最优代价的迭代次数、模态一致性（Mode Agreement）。

### 4. 资源与算力
*   **说明**：论文未明确提及具体的硬件算力配置（如 GPU 型号、训练时长等）。仅提到 MPC 问题使用 CasADi 框架实现，并基于公开的 LMPC 代码库进行扩展。

### 5. 实验数量与充分性
*   **实验规模**：进行了 30 次全局迭代的对比实验，并对超参数（探索权重 $\kappa$、惩罚权重 $\rho$）进行了网格搜索（Grid Search）。
*   **充分性**：实验设计较为充分，通过对比不同参数下的表现，验证了方法在探索能力和收敛速度上的优势。实验设置公平，所有方法在相同的动力学模型、约束和初始数据集下进行对比。

### 6. 主要结论与发现
*   **性能提升**：MM-LMPC 能够有效发现并优化初始代价较高的路径，最终达到的最优代价低于标准 LMPC。
*   **理论保证**：证明了两种设计均能保持递归可行性和闭环稳定性；对于硬约束设计，证明了模态代价的收敛性及对数级的累积遗憾界。
*   **参数影响**：软正则化设计中的惩罚权重 $\rho$ 是平衡模态一致性与探索效率的关键。

### 7. 优点
*   **创新性**：将多模态学习与 LMPC 结合，巧妙地解决了迭代控制中的探索-利用困境。
*   **理论严谨**：提供了完整的递归可行性与稳定性证明，并给出了遗憾界分析。
*   **灵活性**：提供了硬约束和软正则化两种方案，适应不同程度的模态隔离需求。

### 8. 不足与局限
*   **系统假设**：目前的理论分析仅限于无扰动的标称系统，未考虑实际环境中的随机干扰或模型不确定性。
*   **模态定义**：依赖于预定义的模态识别方法（如聚类），若任务模态难以通过几何特征区分，该方法的有效性可能受限。
*   **超参数敏感性**：软正则化设计中 $\rho$ 的选择对性能影响较大，缺乏自动调节机制。

（完）
