---
title: "SafePG: Safe and Globally Optimal Reinforcement Learning with Hard Constraints"
title_zh: SafePG：具有硬约束的安全且全局最优的强化学习
authors: "Vipul K. Sharma, Wesley A. Suttle, S. Sivaranjani"
date: 2026-09-12
pdf: "https://arxiv.org/pdf/2609.14076v1"
tags: ["query:q2"]
score: 8.0
evidence: 硬安全约束下的最优控制
tldr: 本文提出了一种名为SafePG的强化学习框架，旨在解决非线性动力系统在硬约束下的安全控制问题。通过构建基于确定性控制器的随机包装策略，并将其截断以满足硬安全约束，该方法在保证探索性的同时实现了安全保障。研究通过测度论证明了目标函数的良定义性，并利用梯度主导性质确保了无模型策略梯度算法的收敛性与全局最优性，在四旋翼导航任务中验证了其有效性。
selection_source: fresh_fetch
motivation: 现有强化学习在处理非线性动力系统的硬安全约束时，往往难以同时兼顾探索性、收敛性与全局最优性。
method: 提出了一种基于截断随机包装策略的无模型策略梯度算法，通过将策略投影至硬约束空间并利用梯度主导性质进行优化。
result: 在四旋翼导航仿真实验中，该框架成功实现了在满足硬安全约束的前提下进行高效且稳定的控制。
conclusion: SafePG为受限非线性系统的强化学习提供了一种具备理论收敛保证和全局最优性的安全控制解决方案。
---

## 摘要
我们提出了一种用于在硬安全约束下控制非线性动力系统的最优且收敛的无模型策略梯度（PG）强化学习（RL）框架。我们首先构建了一类以确定性控制器为中心的随机包装策略，从而在保留底层确定性控制结构的同时，实现未知环境下的探索。接着，我们通过将这些随机策略截断至硬安全约束上，定义了一类参数化的“构造性安全”控制策略。随后，我们通过测度论论证，确立了在截断策略类下潜在的非凸强化学习目标及其策略梯度的良定义性。在此基础上，我们开发了一种基于随机梯度上升的无模型策略梯度算法，该算法直接在这些截断策略上进行搜索，并利用梯度主导性建立了收敛性和最优性保证。最后，我们通过安全四旋翼导航问题的仿真验证了该框架的有效性。

## Abstract
We present an optimal and convergent model-free policy gradient (PG) reinforcement learning (RL) framework for controlling nonlinear dynamical systems under hard safety constraints. We first construct a class of stochastic wrapper policies centered around a deterministic controller, thereby enabling exploration in unknown environments while preserving the underlying deterministic control structure. We then define a class of parameterized safe-by-construction control policies by truncating these stochastic policies onto hard safety constraints. We next establish, via measure-theoretic arguments, that the potentially nonconvex RL objective under the truncated policy class, as well as its policy gradients, are well-defined. We then develop a model-free PG algorithm based on stochastic gradient ascent that directly searches over these truncated policies and leverage gradient dominance to establish convergence and optimality guarantees. Finally, we validate this framework through simulations on a safe quadrotor navigation problem.

---

## 论文详细总结（自动生成）

这是一份关于论文《SafePG: Safe and Globally Optimal Reinforcement Learning with Hard Constraints》的结构化总结：

### 1. 核心问题与研究动机
在安全关键型应用（如自动驾驶、机器人控制）中，强化学习（RL）面临的核心挑战是：如何在未知环境下进行有效探索的同时，**始终满足硬性安全约束**（即在学习过程中的每一步都不能违反约束）。现有的方法（如基于惩罚的约束RL或基于投影的CBF安全滤波器）往往要么无法保证点对点的硬约束满足，要么因为投影操作破坏了优化景观，导致无法保证收敛性或全局最优性。本文旨在填补这一空白，提出一种既能保证硬约束，又能实现全局最优收敛的无模型RL框架。

### 2. 方法论：SafePG
*   **核心思想**：通过“构造性安全”策略（Safe-by-construction）实现安全探索。
*   **关键技术细节**：
    *   **随机包装策略（Stochastic Wrapper Policies）**：以确定性控制器（如PID）为中心，添加高斯噪声以实现探索。
    *   **策略截断（Truncation）**：将上述随机策略的支撑集直接截断至硬安全控制集 $C(x_t)$ 内。这意味着采样出的每一个动作 $u_t$ 在物理上必然满足安全约束，无需后续的投影步骤。
    *   **理论支撑**：利用测度论证明了在截断策略下，RL目标函数及其梯度是良定义的。
    *   **算法流程**：SafePG 算法通过蒙特卡洛采样估计截断策略的得分函数（Score Function），并利用随机梯度上升直接在截断策略空间内进行优化。
    *   **收敛性保证**：引入梯度主导（Polyak-Łojasiewicz, PL）条件，证明了算法能够收敛至全局最优解的邻域内。

### 3. 实验设计
*   **场景**：安全四旋翼导航问题。目标是在避开障碍物的同时到达指定目标点。
*   **Benchmark**：对比了基于投影的“安全滤波器”方法（即先学习一个无约束策略，再通过二次规划QP进行投影）。
*   **对比指标**：是否能到达目标点、是否能始终保持安全、训练收敛性。

### 4. 资源与算力
*   **说明**：论文中未明确提及具体的硬件配置（如 GPU 型号、数量）或具体的训练时长。仅提到了实验中使用的超参数（如蒙特卡洛采样数 $M=200$，回放长度 $N=200$ 等）。

### 5. 实验数量与充分性
*   **实验组**：主要针对四旋翼导航任务进行了对比实验，并测试了不同探索噪声方差（$\Sigma_0$ 从 $10I_2$ 到 $100I_2$）下的鲁棒性。
*   **评价**：实验设计针对性强，通过对比基准方法清晰地展示了 SafePG 在“既能避障又能到达目标”方面的优势。但实验仅限于单一的四旋翼场景，缺乏在更复杂、高维或多智能体环境下的验证，实验覆盖面相对有限。

### 6. 主要结论与发现
*   SafePG 成功实现了在模型未知的情况下，通过直接优化截断策略来满足硬安全约束。
*   与传统的投影法相比，SafePG 不会破坏优化景观，因此能够引导智能体在保证安全的同时成功到达目标，而投影法往往会导致智能体陷入局部最优或无法完成任务。
*   理论上证明了该方法在满足梯度主导条件下具有全局最优性保证。

### 7. 优点
*   **理论严谨**：通过测度论和梯度主导条件，为安全RL提供了坚实的理论基础。
*   **设计巧妙**：通过截断策略直接规避了投影带来的优化干扰，保持了策略梯度的有效性。
*   **安全性高**：实现了“构造性安全”，即探索过程中的每一步动作都由安全集定义，安全性是内生的。

### 8. 不足与局限
*   **应用限制**：该方法依赖于对安全集 $C(x_t)$ 的显式定义（如CBF），在安全集难以解析表达或需要从数据中学习安全边界的场景下，应用难度较大。
*   **计算开销**：在每一步进行策略截断和蒙特卡洛采样估计得分函数，可能在实时性要求极高的系统中带来计算压力。
*   **实验单一**：缺乏在更广泛的非线性动力系统或高维状态空间中的验证，其扩展性有待进一步研究。

（完）
