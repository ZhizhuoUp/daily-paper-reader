---
title: Contact-Rich Motion Planning via GPU-Parallel Mode Evaluation
title_zh: 基于 GPU 并行模式评估的接触丰富运动规划
authors: "Jiayun Li, Georgia Chalvatzaki"
date: 2026-09-18
pdf: "https://arxiv.org/pdf/2609.21803v1"
tags: ["query:profile-1"]
score: 9.0
evidence: 用于操作的 GPU 并行轨迹优化
tldr: 针对机器人接触丰富运动规划（CRMP）中组合接触决策带来的计算难题，本文提出了CoMET框架。该方法利用现代GPU的高并行计算能力，通过并行轨迹评估与贪婪接触模式扩展相结合，实现了对接触模式序列的广泛评估。实验表明，CoMET在平面推移和双臂操作任务中表现优异，在保证解质量的同时显著提升了规划效率，证明了显式模式评估在复杂接触规划中的有效性。
selection_source: fresh_fetch
motivation: 现有CRMP方法多依赖启发式搜索或优化重构来规避接触模式的广泛评估，难以应对复杂的组合决策。
method: 提出CoMET框架，结合GPU并行轨迹评估与贪婪接触模式扩展，实现对接触模式空间的高效搜索。
result: 在平面推移和双臂非抓取操作基准测试中，CoMET在规划成功率和效率上均优于或持平现有主流算法。
conclusion: 利用GPU的高吞吐量进行显式模式评估，是解决接触丰富运动规划问题的一种简单且高效的替代方案。
---

## 摘要
接触丰富运动规划（CRMP）对于机器人操作和运动至关重要，但由于接触决策的组合性，其计算难度依然很大。现有方法通常通过搜索启发式算法或优化重构来避免对接触模式序列进行广泛评估。鉴于现代 GPU 硬件的发展，我们重新审视了广泛评估的可行性，并引入了结合 GPU 并行轨迹评估与贪婪接触模式扩展的“接触模式扩展与并行轨迹优化”（CoMET）方法。在平面推移基准测试中，CoMET 在解的质量和规划时间上与基于优化、采样和树搜索的基准方法具有竞争力，在几乎所有实例中均能匹配全枚举参考结果，且评估次数更少、规划时间更短。消融实验表明，大部分性能提升源于高吞吐量的轨迹评估器。在双臂非抓取式操作任务中，随着模式空间的增长，GPU 友好的局部模式扩展比所测试的自适应树搜索实现了更高的规划成功率。这些结果表明，广泛的显式模式评估为 CRMP 提供了一种简单而有效的替代方案。

## Abstract
Contact-rich motion planning (CRMP) is essential for robotic manipulation and locomotion, yet remains computationally challenging due to combinatorial contact decisions. Existing methods typically avoid broad evaluation of contact-mode sequences through search heuristics or optimization reformulations. We revisit broad evaluation in light of modern GPU hardware and introduce Contact-Mode Expansion with parallel Trajectory optimization (CoMET), which combines GPU-parallel trajectory evaluation with greedy contact-mode expansion. On planar pushing benchmarks, CoMET is competitive with optimization-based, sampling, and tree-search baselines in solution quality and planning time, matching the full-enumeration reference on nearly all instances with fewer evaluations and shorter planning times. Ablations suggest that much of the performance gain comes from the high-throughput trajectory evaluator. In bimanual nonprehensile manipulation, GPU-friendly local mode expansion achieves higher planning success than the tested adaptive tree search as the mode space grows. These results demonstrate that broad explicit mode evaluation provides a simple yet effective alternative for CRMP.

---

## 论文详细总结（自动生成）

这是一份关于论文《Contact-Rich Motion Planning via GPU-Parallel Mode Evaluation》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：接触丰富运动规划（CRMP）涉及复杂的组合接触决策（即何时、何地、如何接触），这导致了混合整数非线性规划（MINLP）问题，计算难度极大。
*   **研究动机**：现有方法（如启发式搜索、凸松弛、接触隐式优化）多致力于“规避”对接触模式序列的广泛评估。随着现代 GPU 并行计算能力的提升，作者提出重新审视“显式模式评估”的可行性，旨在通过高吞吐量的并行计算来解决组合搜索难题。

### 2. 方法论：CoMET 框架
*   **核心思想**：将 CRMP 视为一个在接触模式空间上的搜索问题，利用 GPU 并行处理能力，对大量候选模式序列进行轨迹优化（TO）评估，并采用贪婪策略扩展最优模式的邻域。
*   **关键技术**：
    *   **模式条件化轨迹优化**：将不同接触模式映射为共享的固定结构，通过掩码（Mask）处理不同模式下的约束，使所有候选者在 GPU 上拥有相同的内存布局和内核执行逻辑。
    *   **固定工作空间局部求解**：利用 primal-dual 增强拉格朗日 DDP 方法，确保每个候选者的求解维度独立于接触约束数量，实现高效的并行化。
    *   **贪婪模式扩展**：从种子批次开始，根据目标函数值和约束违反程度（Merit Function）对模式进行排序，并对最优模式进行邻域扩展（插入、删除、替换模式段），实现对搜索空间的有效覆盖。

### 3. 实验设计
*   **场景与基准**：
    *   **平面推移（Planar Pushing）**：基于 Graesdal 等人提出的基准，包含 8 面体和 4 面体滑块，涉及数千至数万种模式序列。
    *   **双臂非抓取式操作（Bimanual Flipping）**：L 型物体翻转任务，模式空间超过 50 万种，测试算法在超大规模空间下的扩展能力。
*   **对比方法**：包括 GCS-SDP（凸松弛）、IMPACT（接触隐式）、CMA-ES（采样）、MCTS（树搜索）以及全枚举（Full Enumeration）参考基准。

### 4. 资源与算力
*   **硬件配置**：单台工作站，配备 NVIDIA RTX 4080 GPU、AMD Ryzen 9 7950X3D CPU 和 30 GB RAM。
*   **算力利用**：利用 GPU 的 CUDA Warp 并行处理能力，通过可重用的 resident pool 动态调度任务，避免了异构求解时间导致的同步瓶颈。

### 5. 实验数量与充分性
*   **实验覆盖**：进行了 50 组平面推移实例测试和 30 组双臂翻转任务测试。
*   **客观性与公平性**：通过消融实验（如 CPU vs GPU、不同搜索策略、不同池容量）对比，严格控制了变量（如相同的轨迹评估器、相同的计算预算），实验设计严谨且具有较强的说服力。

### 6. 主要结论与发现
*   **性能优势**：CoMET 在平面推移任务中，规划速度比最强优化基准快 4.4 倍，且在几乎所有实例中都能匹配全枚举结果。
*   **扩展性**：在模式空间巨大的双臂任务中，CoMET 的局部模式扩展策略显著优于自适应树搜索和均匀采样，成功率更高。
*   **核心驱动力**：消融实验证明，性能提升主要归功于高吞吐量的 GPU 轨迹评估器，而非复杂的搜索启发式算法。

### 7. 优点
*   **简单高效**：证明了“暴力”但并行的显式评估在现代硬件下比复杂的启发式规避策略更有效。
*   **通用性强**：通过共享结构设计，将异构的接触模式问题转化为统一的 GPU 计算任务，架构设计优雅。
*   **物理可行性**：通过真实机器人实验验证了算法生成的规划在物理世界中的成功率（80%）。

### 8. 不足与局限
*   **模型依赖**：依赖于预定义的接触模式规则，对于未知的复杂环境或非结构化接触可能需要额外的几何推理。
*   **实时性限制**：虽然比传统方法快，但对于极高频的动态反馈控制，仍需结合局部重规划或力反馈机制。
*   **物理偏差**：在真实机器人实验中，由于规划模型与真实物理环境的微小偏差，导致了部分失败，说明纯轨迹优化在处理复杂接触动力学时仍存在局限。

（完）
