---
title: "SkipVLA: Skipping VLA Steps with Classical Planning for Fast Robot Manipulation"
title_zh: SkipVLA：利用经典规划跳过 VLA 步骤以实现快速机器人操作
authors: "Kaivalya Agrawal, Md Ashiqur Rahman, Raymond A. Yeh, Zachary Kingston"
date: 2026-09-17
pdf: "https://arxiv.org/pdf/2609.20648v2"
tags: ["query:profile-1"]
score: 8.0
evidence: 结合VLA与经典运动规划器，实现快速无碰撞机器人操作
tldr: SkipVLA是一种结合视觉语言动作模型（VLA）与经典运动规划器的混合机器人策略。针对VLA在长程任务中推理速度慢的问题，该方法利用经典规划器处理自由空间运动，仅在接触密集型任务（如抓取）时调用VLA。通过复用VLA的视觉语言骨干网络预测目标位姿，SkipVLA在保持成功率的同时，显著提升了任务执行速度并降低了能耗。
selection_source: fresh_fetch
motivation: VLA模型在长程机器人任务中推理延迟高且计算开销大，难以满足实时性需求。
method: 提出一种混合架构，利用经典运动规划器处理非接触运动，仅在关键交互环节调用VLA，并训练一个轻量级预测器来衔接两者。
result: 在LIBERO模拟任务和物理机器人实验中，任务完成速度提升至原来的2.5倍，且能耗显著降低。
conclusion: 通过将语义理解与经典规划相结合，SkipVLA为实现高效、低能耗的机器人长程操作提供了一种可扩展的解决方案。
---

## 摘要
视觉-语言-动作（VLA）模型是一类通用的机器人策略，能够将摄像机图像和语言指令直接映射为机器人动作。尽管前景广阔，但这些模型在测试时的运行速度仍然较慢，特别是在需要多次调用策略的长程任务中。近期的研究通过蒸馏更小的模型、重叠异步动作块或将 VLA 与快速底层策略配对来降低 VLA 延迟，但仍需在整个任务过程中运行学习到的策略。与 VLA 相比，经典运动规划器能快速找到无碰撞的运动路径，但需要明确的目标，且缺乏对任务的语义理解。在本文中，我们提出了 SkipVLA，这是一种结合了预训练 VLA 与经典运动规划器的混合策略。它利用规划器处理自由空间运动，仅在抓取和放置等需要接触的技能上调用 VLA。SkipVLA 重用了 VLA 冻结的视觉-语言主干网络来预测每次规划运动的目标位姿，并利用大型 VLA 已习得的知识来学习该预测器，无需引入额外的演示数据。我们在仿真环境中的 13 项 LIBERO 任务以及物理 6 自由度 YAM 机械臂上的 3 项抓取放置任务中对 SkipVLA 进行了评估。结果表明，在保持相同任务成功率的同时，该方法可将任务完成速度提升至原来的 2.5 倍，并显著降低了能耗。

## Abstract
Vision-Language-Action (VLA) models are a class of generalist robot policies that map camera images and language instructions directly to robot actions. While promising, these models remain slow at test time, particularly for long-horizon tasks that require many queries to the policy. Recent efforts reduce VLA latency by distilling smaller models, overlapping asynchronous action chunks, or pairing the VLA with a fast low-level policy, but still run a learned policy for the entire task. In contrast to VLA, classical motion planners quickly find collision-free motions, but require an explicit goal and have no semantic understanding of the task. In this work, we present SkipVLA, a hybrid policy that combines a pretrained VLA with a classical motion planner, using the planner for free-space motion and querying the VLA only for contact-rich skills such as grasping and placing. SkipVLA reuses the frozen vision-language backbone of the VLA to predict a target pose for each planned motion, and learns this predictor without additional demonstrations introduced into the system by using what was already learnt by the large VLA. We evaluate SkipVLA with three VLAs on 13 LIBERO tasks in simulation and three pick-and-place tasks on a physical 6-DoF YAM arm, demonstrating up to 2.5x faster task completion and significantly lower energy consumption while achieving the same task success rate.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《SkipVLA: Skipping VLA Steps with Classical Planning for Fast Robot Manipulation》** 的结构化总结：

### 1. 核心问题与研究动机
*   **背景**：视觉-语言-动作（VLA）模型作为通用机器人策略，在处理复杂语义任务时表现出色，但其巨大的参数量导致推理延迟极高，难以满足机器人实时控制的需求。
*   **动机**：现有的加速方案（如模型蒸馏、动作分块）往往仍需在整个任务周期内运行神经网络。然而，机器人操作具有物理分解性：自由空间运动（Transit）是几何问题，而接触密集型操作（Manipulation）是语义问题。论文旨在通过“混合策略”将非接触运动卸载给快速的经典规划器，仅在关键交互环节调用 VLA，从而解决长程任务中的高延迟和高能耗问题。

### 2. 方法论：SkipVLA
*   **核心思想**：将任务拆解为“自由空间运动”和“接触密集型操作”。前者由经典运动规划器（如 VAMP, cuRoboV2）执行，后者由预训练的 VLA 执行。
*   **关键技术**：
    *   **目标位姿预测器 ($f_{tgt}$)**：利用 VLA 冻结的视觉-语言主干网络提取特征，通过轻量级 MLP 预测目标物体的 3D 边界框及悬停位姿。
    *   **策略指示器 ($I$)**：基于启发式规则（如抓取/释放时的夹爪状态切换）动态切换控制权。
    *   **训练策略**：无需额外标注，直接从现有的专家演示数据中提取 gripper 事件作为监督信号，通过交叉熵损失训练目标预测器，无需对 VLA 进行任何微调。
*   **流程**：系统首先预测目标位姿 $\rightarrow$ 规划器生成无碰撞路径 $\rightarrow$ 机器人移动至目标 $\rightarrow$ 切换至 VLA 执行抓取/放置 $\rightarrow$ 循环往复。

### 3. 实验设计
*   **数据集与场景**：
    *   **仿真**：LIBERO-Object（短程）和 LIBERO-10（长程）基准测试。
    *   **物理实验**：6 自由度 YAM 机械臂，包含单物体抓取、多物体抓取、堆叠任务。
*   **对比方法**：对比了原始的 VLA 模型（$\pi_{0.5}$, SmolVLA, MolmoAct2）与集成 SkipVLA 后的版本。
*   **评估指标**：任务成功率（SR）、任务完成时间（Wall-clock time）、VLA 查询次数、能耗（物理实验）。

### 4. 资源与算力
*   **算力说明**：文中提到在 NVIDIA Jetson Thor 边缘计算平台上测量了能耗。
*   **训练细节**：论文未详细列出训练所需的 GPU 型号、数量或具体训练时长，仅提到使用了 650 条人类遥操作演示数据进行训练。

### 5. 实验数量与充分性
*   **实验规模**：在仿真中进行了 13 项任务的评估，每项任务 50 次重复实验；物理实验中每项任务 20 次重复实验。
*   **充分性**：实验涵盖了多种 VLA 主干网络和不同的运动规划器，并进行了消融实验（如高度启发式对比），证明了方法的通用性和鲁棒性。实验设计客观，对比了基准模型与混合策略的直接差异。

### 6. 主要结论与发现
*   **性能提升**：SkipVLA 在保持或提升成功率的前提下，任务完成速度最高提升了 2.5 倍（平均延迟降低约 50%）。
*   **能效比**：在 Jetson Thor 上，能耗平均降低了 52% 以上。
*   **误差抑制**：通过经典规划器在每次交互前“重锚”机器人位姿，有效限制了长程任务中神经网络推理带来的误差累积。

### 7. 优点
*   **即插即用**：无需对昂贵的预训练 VLA 进行重训练或微调，保护了原有的语义理解能力。
*   **模块化**：架构与具体的规划器解耦，可根据需求更换底层规划算法。
*   **物理直觉**：利用经典规划器处理几何问题，利用 VLA 处理语义问题，符合机器人操作的物理本质。

### 8. 不足与局限
*   **启发式依赖**：目前的策略切换依赖于夹爪状态的启发式规则，对于非抓取/放置类的复杂任务（如擦拭、搅拌）可能不适用。
*   **任务范围**：目前主要针对“抓取-放置”类任务，对于需要连续接触或复杂动态交互的任务，其通用性有待进一步验证。
*   **环境假设**：依赖于对场景中物体 3D 边界框的准确检测，若环境极其杂乱或物体遮挡严重，目标预测器的性能可能受限。

（完）
