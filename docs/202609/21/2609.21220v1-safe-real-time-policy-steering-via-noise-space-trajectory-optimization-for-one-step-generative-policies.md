---
title: Safe Real-Time Policy Steering via Noise-Space Trajectory Optimization for One-Step Generative Policies
title_zh: 基于噪声空间轨迹优化的单步生成式策略的安全实时策略引导
authors: "Qingyi Chen, Joseph Ruan, Zachary Kingston"
date: 2026-09-18
pdf: "https://arxiv.org/pdf/2609.21220v1"
tags: ["query:profile-1"]
score: 9.0
evidence: 生成式策略中用于避障的实时轨迹优化
tldr: 针对生成式机器人策略在部署时难以满足避障等约束的问题，本文提出了INSPO方法。该方法通过在策略的输入噪声空间进行轨迹优化，而非直接修改动作，实现了实时、高效的策略引导。实验表明，INSPO在多种任务中显著提升了成功率与约束满足度，且计算开销远低于传统的梯度引导方法。
selection_source: fresh_fetch
motivation: 现有生成式策略的推理时引导方法（如迭代扩散）计算成本过高，难以满足实时控制需求。
method: 提出INSPO方法，通过在策略输入噪声空间进行基于种群的粒子优化，在满足约束的同时保持策略分布的一致性。
result: 在Push-T、LIBERO等任务中，INSPO在任务成功率和约束满足度上均优于最佳N采样和动作投影方法，且运行效率更高。
conclusion: INSPO为单步生成式策略提供了一种高效、实时的安全约束引导方案，有效平衡了任务性能与计算开销。
---

## 摘要
生成式机器人策略能够表现出多样化且多模态的行为，但将预训练策略适配至部署时的约束条件（如避障和姿态保持）仍具挑战性。现有的推理时引导方法通常通过迭代扩散或流过程应用梯度引导，这在实时控制中计算成本较高。我们提出了 INSPO，它将单步生成式策略的推理时引导表述为策略输入噪声空间中的轨迹优化。通过在评估诱导状态轨迹约束的同时优化输入噪声，INSPO 能够在不直接修改生成动作的情况下搜索策略诱导的行为空间。该优化过程包含一个正则化项，旨在鼓励解保持与策略输入分布的一致性，并使用基于种群的粒子优化在线求解。我们在基于状态和图像的任务特定策略以及通用视觉-语言-动作策略上，针对 Push-T、Can pick-and-place 和 LIBERO-Spatial 任务对 INSPO 进行了评估。结果表明，与“最优 N 采样”（best-of-N sampling）和动作投影相比，INSPO 提高了任务成功率和约束满足度，同时在较低的运行时间下表现优于梯度引导生成方法。

## Abstract
Generative robot policies can represent diverse, multimodal behaviors, but adapting pretrained policies to deployment-time constraints such as collision avoidance and orientation maintenance remains challenging. Existing inference-time steering methods typically apply gradient guidance through iterative diffusion or flow processes, which can be computationally expensive for real-time control. We propose INSPO, which formulates inference-time steering of one-step generative policies as trajectory optimization in the policy's input noise space. By optimizing the input noise while evaluating constraints on the induced state trajectory, INSPO searches the policy-induced behavior space without directly modifying generated actions. The optimization includes a regularization term that encourages solutions to remain consistent with the policy's input distribution and is solved online using population-based particle optimization. We evaluate INSPO on state- and image-based task-specific policies and generalist vision-language-action policies across Push-T, Can pick-and-place, and LIBERO-Spatial. INSPO improves task success and constraint satisfaction over best-of-N sampling and action projection, while comparing favorably with gradient-guided generation at lower runtime.

---

## 论文详细总结（自动生成）

你好！我是 Antigravity。很高兴为你分析这篇关于机器人生成式策略引导的论文。以下是针对《Safe Real-Time Policy Steering via Noise-Space Trajectory Optimization for One-Step Generative Policies》(INSPO) 的结构化总结：

---

### 1. 核心问题与研究动机
*   **背景**：生成式机器人策略（如扩散模型、流匹配模型）在处理多模态任务上表现出色，但在实际部署时，往往需要满足特定的安全约束（如避障、保持特定姿态）。
*   **痛点**：现有的推理时引导方法（如迭代扩散引导）通常需要多次反向传播或复杂的梯度计算，计算开销巨大，难以满足机器人实时控制（高频闭环）的需求。
*   **目标**：在不直接修改生成动作的前提下，寻找一种高效、实时的方法，通过引导策略的输入空间来满足复杂的轨迹约束。

### 2. 方法论：INSPO
*   **核心思想**：将策略引导转化为“噪声空间”的轨迹优化问题。单步生成式策略通常接受一个噪声向量作为输入，INSPO 通过优化该噪声向量，间接控制生成的动作序列，从而满足状态空间的约束。
*   **关键技术**：
    *   **噪声空间优化**：不再直接对动作进行投影或梯度更新，而是优化输入噪声 $z$。
    *   **正则化项**：引入正则化项以确保优化后的噪声仍处于策略的先验分布内，防止生成“离谱”的动作。
    *   **基于种群的粒子优化**：采用在线粒子优化算法（如交叉熵方法或类似的粒子滤波变体）来求解该优化问题，避免了对策略模型进行昂贵的梯度反向传播。
    *   **轨迹约束评估**：在优化过程中，通过前向模拟评估生成的动作序列是否满足避障等约束。

### 3. 实验设计
*   **任务场景**：涵盖了多种复杂任务，包括 Push-T（推块）、Can pick-and-place（抓取放置）以及 LIBERO-Spatial（空间约束任务）。
*   **基准对比**：
    *   **Best-of-N Sampling**：通过采样多个动作并选择最优者。
    *   **Action Projection**：直接对生成的动作进行投影以满足约束。
    *   **Gradient-Guided Generation**：传统的基于梯度的引导方法。

### 4. 资源与算力
*   **说明**：论文中未明确列出具体的 GPU 型号、数量或训练时长。
*   **推断**：由于该方法强调“实时性”和“低计算开销”，其设计初衷即为在标准机器人计算平台（如单张消费级或工业级 GPU）上实现毫秒级的推理引导。

### 5. 实验数量与充分性
*   **覆盖范围**：实验涵盖了从状态输入到视觉输入的多种策略类型，并测试了通用视觉-语言-动作（VLA）策略，实验覆盖面较广。
*   **公平性**：对比了多种主流的引导基线，且在成功率和约束满足度两个维度进行了量化评估，实验设计客观且具有说服力。

### 6. 主要结论与发现
*   **性能提升**：INSPO 在任务成功率和约束满足度上均优于 Best-of-N 和动作投影方法。
*   **效率优势**：在计算开销上，INSPO 显著低于梯度引导方法，能够更好地适应实时控制的频率要求。
*   **鲁棒性**：证明了在噪声空间进行优化比直接在动作空间进行约束投影更能保持策略的生成质量。

### 7. 优点
*   **实时性强**：通过规避昂贵的梯度计算，实现了高效的推理时引导。
*   **通用性**：该方法与具体的策略架构解耦，适用于多种单步生成式模型。
*   **分布一致性**：通过正则化项，有效解决了引导过程中可能出现的“分布偏移”问题。

### 8. 不足与局限
*   **优化收敛性**：基于种群的粒子优化在极端约束条件下可能存在收敛速度慢或陷入局部最优的风险。
*   **模拟器依赖**：该方法依赖于对未来状态的预测（轨迹评估），如果环境模型（Dynamics Model）不准确，引导效果会大打折扣。
*   **复杂约束限制**：对于高度非凸或动态变化极快的约束，该方法的实时优化能力仍有待进一步验证。

---

（完）
