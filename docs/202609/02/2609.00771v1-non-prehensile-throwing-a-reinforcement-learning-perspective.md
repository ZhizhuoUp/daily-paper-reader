---
title: "Non-Prehensile Throwing: A Reinforcement Learning Perspective"
title_zh: 非抓取式投掷：强化学习视角
authors: "Abdullah Mustafa, Ryo Hanai, Ixchel G. Ramirez-Alpizar, Floris Erich, Ryoichi Nakajo, Yukiyasu Domae, Tetsuya Ogata"
date: 2026-09-01
pdf: "https://arxiv.org/pdf/2609.00771v1"
tags: ["query:q3"]
score: 8.0
evidence: 优化机器人操作的关节空间轨迹
tldr: 本研究提出一种基于强化学习的非抓取式机器人投掷方法，旨在解决大型、沉重或易变形物体的远程搬运难题。该方法通过直接优化关节空间轨迹，充分利用滑动和滚动接触模式，并结合系统辨识与不确定性感知策略实现高效的虚实迁移。实验表明，该方法在UR5e机器人上实现了极高的投掷精度和泛化性，显著提升了机器人的作业范围和负载处理能力。
selection_source: fresh_fetch
motivation: 传统的抓取式投掷和基于简化物理模型的非抓取方法难以处理大型重物且受限于复杂的接触动力学建模。
method: 利用强化学习直接在关节空间优化轨迹，并结合最小加加速度系统辨识与不确定性感知策略来弥合虚实差距。
result: "在真实世界中实现了97%的投掷成功率，能够将近800克的重物投掷至3.5米远或1.8米高处。"
conclusion: 强化学习能够有效学习复杂的非抓取接触动力学，为机器人扩展作业空间和处理多样化物体提供了强有力的技术支撑。
---

## 摘要
机器人投掷能够实现物体的快速运输，并将机器人的可达工作空间扩展到传统的取放操作之外。虽然抓取式（基于抓取）投掷对于可抓取物品效果良好，但非抓取式（无抓取）投掷更适用于大型、沉重和/或易变形的物体。现有方法依赖于具有简化接触模型（例如动态抓取）和低维轨迹参数化的基于模型的优化，这限制了求解质量和可达工作空间。我们提出了一种强化学习方法，该方法额外利用了滑动和滚动接触模式，并在没有解析接触模型或自定义参数化的情况下直接优化关节空间轨迹。马尔可夫决策过程（MDP）被建模为一个动力系统，该系统根据投掷目标、物体模型和初始配置来演化机器人的关节状态。关节加加速度（Joint-jerk）轨迹在低控制频率下进行离线规划，并上采样为平滑的高频速度指令用于部署。为了实现从仿真到现实（sim-to-real）的迁移，我们通过最小加加速度系统辨识来缩小机器人动力学差距，并训练不确定性感知策略以减轻物体建模误差，特别是对动摩擦的敏感性。在仿真中，该策略在数千种配置下实现了 99% 的成功率，并能泛化到未见过的物体。敏感性分析显示了对质量不确定性的鲁棒性，但对动摩擦具有高度敏感性，这与基于滑动的释放机制相一致。在接近物理极限（末端执行器速度为 5 m/s）运行的 UR5e 机器人上进行零样本（zero-shot）部署，我们的方法可以将包括重物（790 g）和大尺寸物品（20x20x28 cm）在内的多种物体投掷到最远 350 cm 距离或最高 180 cm 高度的目标，实现了 97% 的真实世界成功率。

## Abstract
Robotic throwing enables fast object transport and extends a robot's reachable workspace beyond traditional pick-and-place. While prehensile (grasp-based) throwing works well for graspable items, non-prehensile (grasp-free) throwing is better suited for large, heavy, and/or deformable objects. Existing approaches rely on model-based optimization with simplified contact models (e.g., dynamic grasping) and low-dimensional trajectory parameterizations, which limit solution quality and reachable workspace. We propose a reinforcement learning approach that additionally leverages sliding and rolling contact modes and directly optimizes joint-space trajectories without analytical contact models or custom parameterizations. The Markov Decision Process (MDP) is formulated as a dynamical system that evolves the robot's joint state conditioned on the throwing target, object model, and initial configuration. Joint-jerk trajectories are planned offline at a low control rate and upsampled into smooth, high-rate velocity commands for deployment. For sim-to-real transfer, we minimize the robot-dynamics gap through minimum-jerk system identification and train uncertainty-aware policies to mitigate object-modeling errors, particularly sensitivity to dynamic friction. In simulation, the policy achieves 99% success across thousands of configurations and generalizes to unseen objects. Sensitivity analysis shows robustness to mass uncertainty but high sensitivity to dynamic friction, consistent with the sliding-based release mechanism. Deployed zero-shot on a UR5e operating near its physical limits (5 m/s end-effector velocity), our method throws diverse objects including heavy (790 g) and large (20x20x28 cm) items to targets up to 350 cm distance or 180 cm elevation, achieving a 97% real-world success rate.

---

## 论文详细总结（自动生成）

这篇论文《Non-Prehensile Throwing: A Reinforcement Learning Perspective》探讨了如何利用强化学习（RL）实现机器人的非抓取式（无夹具固定）投掷。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何让机器人能够投掷大型、沉重或易变形的物体，这些物体通常超出了传统夹具的抓取能力。
*   **研究动机**：
    *   **扩展工作空间**：投掷能让机器人超越物理触及范围，实现快速运输。
    *   **克服抓取限制**：传统的“抓取式投掷”受限于夹具尺寸和强度，且对易变形物体释放时机难以控制。
    *   **现有方法局限**：现有的非抓取投掷多依赖简化的物理模型（如动态抓取）和低维轨迹参数化，导致在复杂接触动力学（滑动、滚动）下表现不佳，且优化求解器（如SQP）容易陷入局部最优。

### 2. 方法论
*   **核心思想**：将非抓取投掷建模为一个马尔可夫决策过程（MDP），利用强化学习直接在关节空间优化轨迹，无需显式的解析接触模型。
*   **关键技术细节**：
    *   **MDP建模**：状态空间包含机器人关节状态（位置、速度、加速度）、物体模型参数（质量、尺寸、摩擦力）、目标位置及上一步动作。
    *   **动作空间**：输出关节加加速度（Jerk）指令。这种高阶控制能确保接触模式切换时的平滑性。
    *   **轨迹生成**：在20Hz低频下离线规划加加速度轨迹，随后上采样至高频（仿真120Hz，实机500Hz）的速度指令进行执行。
    *   **虚实迁移（Sim-to-Real）策略**：
        1.  **机器人动力学对齐**：通过最小加加速度系统辨识，调整仿真中执行器的刚度和阻尼参数。
        2.  **物体动力学鲁棒性**：引入“不确定性感知训练”，在训练中对摩擦系数加入随机噪声，以应对真实世界中难以精确建模的动摩擦力。
*   **算法流程**：使用PPO（近端策略优化）算法，配合非对称Actor-Critic架构（Critic在训练时可获取物体状态等特权信息）。

### 3. 实验设计
*   **实验场景**：在 NVIDIA Isaac Sim 环境中训练，并在真实的 UR5e 机器人上验证。
*   **数据集/物体**：
    *   仿真中使用了数千种随机生成的长方体配置。
    *   泛化性测试使用了 YCB 物体集（如木块、薯片罐、洗洁精瓶等）。
*   **对比与基准**：
    *   由于缺乏开源的非抓取投掷基线，作者尝试对比了基于MPC的“Upright”方法，但发现其在投掷任务中求解器频繁失败。
    *   进行了详尽的消融实验，对比了不同历史长度、控制频率、动作空间（速度 vs 加速度 vs 加加速度）的效果。

### 4. 资源与算力
*   **算力设备**：使用了一台搭载 **NVIDIA RTX 4090 GPU 的笔记本电脑**。
*   **训练时长**：在 4096 个并行环境下训练 1000 个 Epoch（约 2.62 亿步），耗时**约 1 小时**。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **仿真评估**：针对 12,288 种随机配置进行了测试，成功率达 99%。
    *   **消融实验**：覆盖了 5 个关键设计维度，每组实验重复 3 个随机种子。
    *   **真实世界测试**：涵盖 5 种不同物理特性的物体，针对 12 个目标位置（组合了 4 种距离和 3 种高度），共进行了 60 组配置测试，每组 10 次重复。
*   **充分性评价**：实验设计非常充分，不仅验证了性能，还通过敏感性分析深入探讨了质量、尺寸和摩擦力对成功率的影响，具有很高的客观性和说服力。

### 6. 主要结论与发现
*   **高成功率**：实机平均成功率达到 **97%**，能将近 800g 的重物投掷至 3.5 米远或 1.8 米高。
*   **摩擦力是关键**：敏感性分析发现，策略对物体质量不敏感（质量在动力学方程中可抵消），但对**动摩擦系数**高度敏感，因为该方法主要依赖滑动释放机制。
*   **虚实迁移可行性**：通过简单的系统辨识和不确定性训练，即可实现零样本（Zero-shot）迁移。

### 7. 优点
*   **突破模型限制**：RL 能够自动发现滑动和滚动的混合接触模式，比手动建模更具表现力。
*   **极高性能**：在 UR5e 机器人物理极限附近运行（末端速度达 5 m/s），展示了极强的动态操作能力。
*   **鲁棒性强**：对物体质量和部分几何误差具有天然的免疫力。

### 8. 不足与局限
*   **维度限制**：目前仅限于**平面（2D）投掷**，尚未扩展到全 3D 空间。
*   **环境建模缺失**：实机测试中，某些极端目标点失败是因为未在仿真中建模机器人底座，导致生成的轨迹产生碰撞。
*   **初始放置依赖**：实验假设物体初始位于托盘中心，对初始放置位置的偏差容忍度未做深入探讨。
*   **安全风险**：高速投掷重物对硬件磨损大，且落地冲击力强，实际工业应用需考虑缓冲和安全防护。

（完）
