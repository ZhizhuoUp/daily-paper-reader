---
title: Behavior--Realization Separation for Constrained Physical Human--Robot Interaction
title_zh: 针对受约束物理人机交互的行为与实现分离
authors: Yongyan Cao
date: 2026-09-01
pdf: "https://arxiv.org/pdf/2609.00669v1"
tags: ["query:q2"]
score: 8.0
evidence: 用于受限机器人指令的收缩时界二次规划
tldr: 本研究针对物理人机交互中行为规范与约束实现耦合的问题，提出了一种分层架构。该架构将行为层与实现层分离，利用退避时界二次规划（QP）将期望加速度转化为满足力矩和空间约束的机器人指令。在Franka FR3机器人上的实验表明，该方法能显著降低边界超调并提高力矩约束精度，实现了行为定义与物理实现的解耦。
selection_source: fresh_fetch
motivation: 旨在解决物理人机交互中期望行为与约束处理相互耦合、导致约束违反和误差难以量化的问题。
method: 提出一种分层控制架构，通过行为层定义加速度目标，实现层利用模型预测控制框架下的二次规划确保约束满足。
result: 实验显示该方法将工作空间边界超调从4.4厘米降低至0.2毫米以内，并在力矩受限情况下表现出更优的可行性。
conclusion: 行为与实现的分离为受限物理人机交互提供了更安全、精确且灵活的控制框架，支持多种控制策略的无缝切换。
---

## 摘要
物理人机交互软件通常将期望行为规范与受约束的实现耦合在一起；我们将这两者视为独立层。一个“行为层”提供期望的接触端口加速度 $a_k^{\mathrm{id}}=f_θ(e_k,\dot e_k,F_{h,k})$。一个“实现层”将其转换为受约束的机器人指令，并报告总体的期望与实际加速度误差，而不是将其隐藏在饱和状态中。具有相同目标的无约束反事实分析将正则化与约束干预分离开来，而受控对象数据则揭示了模型误差。本文实现了一个实现无记忆仿射行为的滚动时界二次规划。改变行为会通过 $(C_θ,G_θ)$ 修改目标系数，而机器人指令变量和可行集保持不变。一项平面研究实例化了阻抗和导纳；在现有的速率限制下，同一个运行层无需重构即可接受阻抗-导纳-阻抗的重新分配。在 MuJoCo 中的力矩控制 7 自由度 Franka FR3 机器人上，运行时系统在每次求解时冻结任务空间动力学，并在其时界内强制执行力矩可行性。在持续 20 N 的推力下，它将松弛约束的工作空间边界保持在约 0.1-0.2 mm 以内，而瞬时裁剪导致的超调量分别为 4.4 cm（阻抗）和 4.7 cm（导纳）。降低执行器预算后激活了力矩约束：全时界强制执行使其冻结模型规划的可行性保持在 $2.1\times10^{-4}$ N·m 以内，而仅针对第一步的消融实验规划出的超出预算高达 11.329 N·m；在执行的非线性受控对象上，两者共享相同的局部模型误差，差距虽有所缩小，但仍倾向于全时界强制执行（0.161 对比 0.380 N·m）。这些结果是对行为与实现分离的有力证明。

## Abstract
Physical human--robot interaction software often couples desired-behavior specification with constrained realization; we treat these as separate layers. A \emph{behavior layer} supplies a desired contact-port acceleration $a_k^{\mathrm{id}}=f_θ(e_k,\dot e_k,F_{h,k})$. A \emph{realization layer} converts it into constrained robot commands and reports total desired-versus-realized acceleration error instead of hiding it in saturation. A same-objective unconstrained counterfactual separates regularization from constraint intervention, while plant data expose model error. This paper implements a receding-horizon quadratic program realizing memoryless affine behaviors. Changing the behavior modifies objective coefficients through $(C_θ,G_θ)$ while the robot-command variable and feasible set remain unchanged. A planar study instantiates impedance and admittance; the same running layer accepts an impedance--admittance--impedance reassignment without reconstruction, under its existing rate limit. On a torque-controlled 7-DOF Franka FR3 in MuJoCo, the runtime freezes task-space dynamics per solve and enforces torque feasibility across its horizon. Under a sustained 20~N push, it holds a slack-relaxed workspace boundary to within approximately 0.1--0.2~mm, versus 4.4~cm (impedance) and 4.7~cm (admittance) overshoot from instantaneous clipping. A derated actuator budget then activates the torque constraint: horizon-wide enforcement keeps its frozen-model plan feasible to $2.1\times10^{-4}$~N$\cdot$m, whereas a first-step-only ablation plans up to 11.329~N$\cdot$m beyond budget; on the executed nonlinear plant, where both share the same local-model error, the gap is smaller but still favors horizon-wide enforcement (0.161 vs.\ 0.380~N$\cdot$m). These results are a focused proof of behavior--realization separation.

---

## 论文详细总结（自动生成）

这篇论文提出了一种针对物理人机交互（pHRI）的新型软件架构，旨在解决期望行为规范与物理约束实现之间的耦合问题。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的 pHRI 控制（如阻抗控制）通常将“期望的交互行为”与“机器人的物理约束（如力矩限制、关节极限）”混合在同一个控制律中。这种耦合导致了“静默饱和”现象（即指令超出极限被裁剪，但控制器并不知道实际执行效果偏离了预期），且使得行为定义难以在不同机器人间复用。
*   **研究动机**：作者主张将**行为（Behavior）**与**实现（Realization）**分离。行为层只负责定义理想的动力学响应（加速度），而实现层负责在满足机器人物理约束的前提下，尽可能地逼近这一加速度。这种分离提高了软件的模块化程度，并增强了交互的安全性。

### 2. 方法论
*   **核心思想**：建立一个以“期望加速度（$a^{id}$）”为契约的显式接口。
*   **行为层（Behavior Layer）**：输出期望的接触端口加速度 $a^{id} = f_\theta(e, \dot{e}, F_h)$。论文重点讨论了无记忆仿射行为（如阻抗和导纳控制）。
*   **实现层（Realization Layer）**：采用**滚动时界二次规划（Receding-Horizon QP）**。
    *   **优化目标**：在预测时界内最小化实际加速度与期望加速度的加权平方差，同时加入控制量及其变化率的正则化项。
    *   **约束条件**：包括机器人动力学模型、力矩极限、工作空间边界（位置极限）和速度极限。
    *   **关键技术**：在每次求解时“冻结”非线性动力学模型（局部线性化），并利用松弛变量（Slacks）处理硬约束冲突。
*   **残差审计（Audit）**：提出将总误差分解为：
    1.  **正则化残差（$r_{reg}$）**：由次要优化目标引起。
    2.  **约束残差（$r_{con}$）**：由物理约束干预引起。
    3.  **模型残差（$r_{mod}$）**：由预测模型与真实物理系统的差异引起。

### 3. 实验设计
*   **实验场景**：
    1.  **平面质点模型**：用于验证架构的基本逻辑和在线行为切换（阻抗 $\leftrightarrow$ 导纳）。
    2.  **Franka FR3 机器人（7自由度）**：在 MuJoCo 仿真环境中进行，考虑复杂的非线性动力学和力矩约束。
*   **Benchmark（对比基准）**：
    *   **反应式裁剪（Reactive Clipping）**：传统的瞬时饱和处理方法，不具备预测性。
    *   **单步约束消融（First-step-only ablation）**：仅在当前时刻强制执行约束，不考虑未来时界的预测。
*   **测试任务**：在持续 20N 的推力下，观察机器人如何处理工作空间边界和力矩预算受限的情况。

### 4. 资源与算力
*   **算力说明**：论文未明确提及具体的 GPU 型号，但指出实验是在 MuJoCo 仿真器中进行的。
*   **实时性表现**：
    *   控制频率：底层力矩环 1kHz，QP 求解器（实现层）运行频率为 50Hz。
    *   求解耗时：使用 OSQP 求解器，在热启动（Warm-start）情况下，平均求解时间约为 **3.45ms - 3.68ms**，99% 的求解在 10ms 内完成，满足 20ms（50Hz）的实时性要求。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了平面模型和 7 自由度复杂机器人的多组对比实验，包括阻抗、导纳两种典型行为，以及针对力矩约束的专项压力测试。
*   **充分性评价**：实验设计较为严谨，通过“反事实分析”验证了残差分解的准确性。消融实验（全时界 vs 单步约束）有力地证明了预测性实现的必要性。虽然是仿真实验，但其针对架构原理的验证是充分且客观的。

### 6. 主要结论与发现
*   **边界保护能力**：在 20N 推力下，该方法将工作空间边界超调控制在 **0.1-0.2 mm** 以内，而传统方法超调高达 **4.4-4.7 cm**。
*   **力矩可行性**：全时界约束强制执行能确保规划出的指令在物理上几乎完全可行（误差 $10^{-4}$ 数量级），而单步约束方法会产生严重的不可行规划（误差达 11.3 N·m）。
*   **灵活性**：证明了可以在不重构控制器状态的情况下，在线无缝切换交互行为（如从阻抗模式切换到导纳模式）。

### 7. 优点
*   **架构清晰**：通过加速度接口实现了行为与物理实现的解耦，具有很强的工程实用价值。
*   **诊断透明**：残差分解机制让开发者能清晰看到是“约束干预”还是“模型误差”导致了行为偏差，而非黑盒化的饱和。
*   **性能卓越**：相比传统方法，极大地提升了受限空间内的交互精度和安全性。

### 8. 不足与局限
*   **实验局限**：目前仅在仿真环境中验证，缺乏真实硬件实验和人类参与者的主观评价。
*   **理论局限**：
    *   仅证明了**无记忆仿射行为**的有效性，对于非线性或具有复杂内部状态的行为层尚未验证。
    *   **稳定性保证**：虽然实现了约束处理，但尚未提供系统性的无源性（Passivity）或耦合稳定性证明。
    *   **局部模型依赖**：依赖于局部冻结的线性模型，在极高动态或模型高度不确定时可能失效。

（完）
