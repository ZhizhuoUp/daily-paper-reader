---
title: Real-Time Model Predictive Control Algorithms for Autonomous Spacecraft Guidance
title_zh: 自主航天器制导的实时模型预测控制算法
authors: Mohammed-Adnane Garab
date: 2026-09-01
pdf: "https://arxiv.org/pdf/2609.00927v1"
tags: ["query:profile-1"]
score: 8.0
evidence: 用于航天器引导和交会的实时模型预测控制
tldr: 本研究针对自主航天器交会对接引导，对比了线性MPC、管道MPC（Tube MPC）、快速MPC和连续凸化（SCvx）四类算法。通过CWH模型分析发现，动力学边缘稳定性会导致梯度求解器在长时域下失效，而基于Cholesky分解的ADMM算法表现更优。研究验证了Tube MPC在多种交会任务中的鲁棒性，推导了最坏情况跟踪误差界限，并实现了无需外部库的底层数值算法，证明了其在星载实时引导中的可行性。
selection_source: fresh_fetch
motivation: 旨在评估并对比不同模型预测控制（MPC）算法在自主航天器交会对接任务中的实时性、鲁棒性及计算可行性。
method: 基于CWH相对运动模型，采用Tube MPC框架结合在线重线性化技术，并从底层重构了Cholesky分解等核心数值计算程序。
result: 实验表明Tube MPC能以统一配置完成多种复杂交会动作，且在长时域压力测试下，精确线性代数求解器比梯度下降法更具稳定性。
conclusion: Tube MPC是一种鲁棒且计算高效的控制器，非常适合作为星载实时航天器交会对接引导的软件方案。
---

## 摘要
本报告研究并比较了用于自主航天器交会对接制导的四类模型预测控制（MPC）算法：线性 MPC、管道 MPC (Tube MPC)、快速/嵌入式 MPC 以及逐次凸化 (SCvx)。利用 Clohessy-Wiltshire-Hill (CWH) 相对运动模型，我们证明了底层动力学的临界稳定性会导致压缩 Hessian 矩阵的条件数随预测时域急剧增长，这解释了为什么基于梯度的求解器（如 Fast MPC）在长时域压力测试下会发散，而精确线性代数求解器（采用 Cholesky 分解的 ADMM）则不受影响。Tube MPC 在五种标准交会机动（平移、R-bar、V-bar、自然运动绕飞和螺旋式机动）中通过单一固定的控制器配置得到了验证，并且推导出了一个双边且可证明正确的界限，直接根据代价权重矩阵来限定最坏情况下的跟踪误差。该框架进一步扩展到通过在线重新计算线性化来跟踪任意非闭式参考轨迹，且安全性保证在整个过程中始终成立。最后，为了评估实际飞行软件的可行性，核心数值例程（Cholesky 分解和 Riccati 方程求解器）基于第一性原理进行了重新实现，不依赖外部库，并对照标准科学计算工具验证达到了机器精度。综上所述，这些结果支持将 Tube MPC 作为一种鲁棒且计算上切实可行的控制器，用于星载实时航天器交会对接制导。

## Abstract
This report studies and compares four families of Model Predictive Control (MPC) algorithms for autonomous spacecraft rendezvous guidance: Linear MPC, Tube MPC, Fast/Embedded MPC, and Successive Convexification (SCvx). Using the Clohessy-Wiltshire-Hill (CWH) relative-motion model, we show that the marginal stability of the underlying dynamics causes the condition number of the condensed Hessian to grow sharply with the prediction horizon, which explains why gradient-based solvers such as Fast MPC diverge under long-horizon stress tests while exact linear-algebra solvers (ADMM with Cholesky factorisation) remain unaffected. Tube MPC is validated across five standard rendezvous manoeuvres (Translation, R-bar, V-bar, Natural Motion Circumnavigation, and Corkscrew) using a single fixed controller configuration, and a two-sided, provably correct bound is derived to bracket the worst-case tracking error directly from the cost weight matrices. The framework is further extended to track an arbitrary, non-closed-form reference trajectory through online-recomputed linearisation, with the safety guarantee holding throughout. Finally, to assess feasibility for real flight software, the core numerical routines (Cholesky factorisation and the Riccati equation solver) are re-implemented from first principles, without external libraries, and validated against standard scientific computing tools to machine precision. Taken together, these results support Tube MPC as a robust and computationally realistic controller for onboard, real-time spacecraft rendezvous guidance.

---

## 论文详细总结（自动生成）

以下是对论文《自主航天器制导的实时模型预测控制算法》（Real-Time Model Predictive Control Algorithms for Autonomous Spacecraft Guidance）的深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文聚焦于**自主航天器近距离交会对接（Rendezvous and Proximity Operations, RPO）**中的制导问题。由于地面通信存在延迟，航天器必须具备高度自主的在线规划能力，在满足推力受限、视线角约束及防碰撞等安全条件下，精确引导“追踪者”与“目标”对接。研究动机在于对比不同模型预测控制（MPC）变体的性能，解决传统开环制导鲁棒性不足的问题，并探索适合星载计算机运行的实时优化算法。

### 2. 论文提出的方法论
论文构建了一个多层次的研究框架，核心技术细节包括：
*   **动力学模型**：采用 **Clohessy-Wiltshire-Hill (CWH)** 线性相对运动方程，描述追踪者相对于目标航天器的状态。
*   **四类 MPC 算法**：
    *   **线性 MPC**：基础的二次规划（QP）形式。
    *   **管道 MPC (Tube MPC)**：核心思想是将状态分解为“名义状态”和“误差状态”，通过预先计算鲁棒正不变集（RPI）来收缩约束，确保在存在扰动时真实轨迹始终保持在名义轨迹周围的“管道”内。
    *   **快速 MPC (Fast MPC)**：利用一阶梯度法（如 Nesterov 加速梯度）降低单步计算量。
    *   **连续凸化 (SCvx)**：通过局部线性化处理非线性动力学或非凸约束。
*   **数值求解器**：对比了 **ADMM（交替方向乘子法）** 和 **Nesterov 加速投影梯度法**。
*   **关键改进**：
    *   提出动力学矩阵的**多项式近似**（3阶截断），大幅降低了矩阵指数运算的开销。
    *   推导了最坏情况跟踪误差的**闭式估计界限**，建立了代价权重（Q, R）与跟踪精度之间的直接数学联系。
    *   实现了**在线重线性化**，使算法能扩展到非闭式的任意参考轨迹。

### 3. 实验设计
*   **实验场景**：平面轨道交会（15km 到 1km）及三维空间交会。
*   **Benchmark（基准机动）**：测试了五种标准航天器交会动作：
    1.  **Translation**（直线平移）
    2.  **R-bar**（径向接近）
    3.  **V-bar**（沿轨道方向接近）
    4.  **NMC**（自然运动绕飞）
    5.  **Corkscrew**（螺旋式接近）
*   **对比维度**：算法在长预测时域（N=100）下的收敛性、计算耗时、最终跟踪误差及鲁棒性。

### 4. 资源与算力
*   **算力说明**：文中未提及使用 GPU，因为航天器制导算法通常运行在资源受限的嵌入式 CPU 上。
*   **实现方式**：实验最初使用 Python (NumPy/SciPy) 进行仿真。为了验证“飞行就绪”可行性，作者从底层重写了所有核心算法（如 Cholesky 分解、Riccati 方程迭代求解），不依赖任何外部科学计算库。
*   **耗时**：在 N=100 的配置下，单步计算耗时约在 2.4ms 至 7.3ms 之间。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对每种标准机动，进行了 **30 组独立的蒙特卡洛（Monte Carlo）仿真**，引入了随机的有界扰动。
    *   在验证误差界限公式时，随机生成了 **1000 组不同的权重矩阵（Q, R）** 进行大样本测试。
*   **充分性评价**：实验设计非常充分且具有极高的压力测试强度。特别是通过将预测时域 N 设为 100，揭示了梯度求解器在处理 CWH 模型（边缘稳定系统）时因 Hessian 矩阵条件数爆炸（达 $10^{11}$ 级）而失效的现象，对比分析客观且深入。

### 6. 主要结论与发现
*   **Tube MPC 胜出**：在鲁棒性、精确度和计算成本之间达到了最佳平衡，能够以单一配置完成所有类型的交会任务。
*   **求解器敏感性**：对于长时域问题，基于精确线性代数（Cholesky 分解）的 ADMM 远优于一阶梯度法（Nesterov），后者在条件数过大时会严重发散。
*   **燃料-精度权衡**：增加位置权重 Q 并不一定能显著提升跟踪精度（受限于扰动补偿机制），但会显著增加燃料消耗。
*   **嵌入式可行性**：通过底层重构算法，证明了该 MPC 框架可以在不依赖外部库的情况下，以极高的精度和可预测的复杂度运行在星载硬件上。

### 7. 优点
*   **工程实用性极强**：不仅讨论理论，还提供了底层算法实现方案，直接对接飞行软件需求。
*   **数学严谨**：给出了误差界限的可证明双边不等式，而非单纯的经验公式。
*   **模型扩展性**：通过在线线性化技术，打破了 CWH 模型只能用于圆轨道的局限。

### 8. 不足与局限
*   **约束简化**：在部分压力测试中为了隔离变量，去除了推力幅值约束，这可能掩盖了某些算法在饱和状态下的表现。
*   **扰动模型**：主要考虑了均匀分布的有界扰动，对于更复杂的非高斯扰动或系统参数不确定性（如质量估计误差）讨论较少。
*   **非线性程度**：虽然讨论了 SCvx，但大部分实验仍基于线性化的 CWH 模型，对于极高偏心率轨道或剧烈非线性环境的覆盖有限。

（完）
