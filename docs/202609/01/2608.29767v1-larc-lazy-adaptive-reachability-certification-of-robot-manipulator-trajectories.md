---
title: "LARC: Lazy Adaptive Reachability Certification of Robot Manipulator Trajectories"
title_zh: LARC：机器人机械臂轨迹的惰性自适应可达性认证
authors: "Yu Feng, Hao Wu, Yuzhe Wang, Jianshu Zhou"
date: 2026-08-30
pdf: "https://arxiv.org/pdf/2608.29767v1"
tags: ["query:profile-1"]
score: 9.0
evidence: 机械臂轨迹的连续时间碰撞认证
tldr: "针对机器人轨迹规划中离散碰撞检测可能漏检的问题，本文提出了一种名为LARC的延迟自适应可达性认证方法。该方法通过仅对分不确定的时间区间，并利用中点胶囊体结合速度最大值来界定连杆占据空间，实现了连续时间的避障认证。实验表明，LARC在保证安全性的同时，计算量仅为基准方法的24.8%，显著提升了轨迹验证的效率与可靠性。"
selection_source: fresh_fetch
motivation: 传统的离散轨迹检测容易忽略采样点间的碰撞，而均匀时间划分的可达性分析在安全间隙较大时存在严重的计算浪费。
method: 提出LARC方法，通过递归对分不确定区间，并利用分段三次埃尔米特轨迹的速度上限对中点胶囊体进行膨胀，从而构建连续时间的安全边界。
result: 在AgileX PIPER机器人上的测试显示，LARC仅需基准方法约四分之一的计算量，实现了10.28倍的中值加速，并成功识别出离散检测漏掉的碰撞。
conclusion: LARC提供了一种高效且可靠的连续时间轨迹认证方案，在大幅减少计算开销的同时，弥补了采样检测无法独立证明连续安全性的缺陷。
---

## 摘要
离散轨迹检查可能会遗漏采样机器人状态之间的碰撞。基于可达性的认证可以界定状态间的运动，但在间隙较大处，均匀的时间划分会浪费计算资源。我们提出了惰性自适应可达性认证（LARC），该方法通过仅对间隙测试结果不确定的区间进行二分来检查规划轨迹。对于分段三次 Hermite 关节轨迹，该方法利用由精确的分量最大速度膨胀的中点胶囊来界定连杆的占据空间。覆盖轨迹的认证区间提供了连续时间的外部障碍物间隙，受几何包含关系、静态障碍物和预设裕度的约束。在来自 80 个起点-目标对的 160 条 AgileX PIPER 轨迹上，LARC 在深度为 9 时与固定精细基准的所有决策一致。它使用了 20328 次区间评估（仅为基准工作量的 24.8%），中值配对加速比达到 10.28 倍。另一项独立的 MoveIt/FCL 审计检查了 158051 个状态，并在 21 个直接插值控制中检测到碰撞，而 LARC 未对其中任何一个进行认证。该方法在共享证书模型下减少了计算量，但在 139 条采样无碰撞轨迹中，仍有 27 条未通过认证。采样审计无法独立证明连续时间的间隙。

## Abstract
Discrete trajectory checks can miss collisions between sampled robot states. Reachability-based certification bounds motion between states, but uniform time partitions waste computation where clearance is large. We present lazy adaptive reachability certification (LARC), which checks a planned trajectory by bisecting only intervals with an inconclusive clearance test. For piecewise-cubic Hermite joint trajectories, the method bounds link occupancy using midpoint capsules inflated by exact componentwise speed maxima. Certified intervals covering the trajectory provide continuous-time external-obstacle clearance, subject to geometric containment, static obstacles, and a prescribed margin. On 160 AgileX PIPER trajectories from 80 start-goal pairs, LARC matched all decisions of the fixed-fine baseline at depth nine. It used 20328 interval evaluations (24.8% of baseline work), with a median paired speedup of 10.28x. A separate MoveIt/FCL audit checked 158051 states and detected collisions in 21 direct-interpolation controls, none of which LARC certified. The method reduced computation under a shared certificate model, but 27 of 139 sampled-clear trajectories remained uncertified. The sampled audit cannot independently prove continuous-time clearance.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **LARC（Lazy Adaptive Reachability Certification，惰性自适应可达性认证）** 的方法，旨在解决机器人机械臂在执行规划轨迹时的连续时间安全性验证问题。

以下是对该论文的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的机器人轨迹碰撞检测通常依赖于离散时间点的采样。然而，在采样点之间可能存在碰撞漏检（即“穿模”现象）。
*   **研究背景**：虽然基于可达性分析（Reachability-based analysis）的方法可以界定运动包络以保证连续安全性，但现有的方法往往采用均匀的时间区间划分。这导致在障碍物较远的区域浪费了大量计算资源，而在接近障碍物时精度可能不足。
*   **整体含义**：LARC 旨在通过“按需细分”的策略，在保证连续时间安全性的前提下，大幅降低计算开销，使实时轨迹认证更加高效。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：采用惰性（Lazy）和自适应（Adaptive）的策略。从整个轨迹区间开始，仅对那些“不确定是否安全”的区间进行二分细化，直到区间被证明安全或达到最大细分深度。
*   **关键技术细节**：
    *   **轨迹表示**：将 MoveIt 规划的轨迹重建为分段三次埃尔米特（Hermite）曲线，这允许精确计算关节速度的最大值。
    *   **占据包络构建**：利用区间中点的机械臂状态构建“中点胶囊体（Midpoint Capsules）”，并根据该区间内关节的最大速度计算一个“运动膨胀半径”。
    *   **可达性边界**：通过将中点胶囊体与运动半径进行闵可夫斯基和（Minkowski sum）运算，得到该时间段内连杆扫过空间的外部包络。
    *   **间隙测试**：定义函数 $g(I)$，计算包络与静态障碍物（AABB 边界框）之间的最小距离减去预设的安全裕度 $\rho$。若 $g(I) > 0$，则该区间认证通过。
*   **算法流程**：从根区间 $[0, T_h]$ 开始深度优先遍历，若当前区间测试不通过且未达最大深度，则对半平分区间并递归检查。

### 3. 实验设计
*   **实验平台**：使用 6 自由度的 AgileX PIPER 机器人模型。
*   **实验场景**：设置了四种不同复杂度的场景：开放空间（Open）、简单窗口（Easy Window）、中等窗口（Medium Window）、简单货架（Easy Shelf）。
*   **数据集**：基于 80 个独立的起点-终点对，生成了 160 条轨迹（包括 OMPL 规划轨迹和直接插值轨迹）。
*   **Benchmark（基准方法）**：
    *   **Fixed-fine**：固定精细分辨率（512 个均匀区间）。
    *   **Fixed-coarse**：固定粗糙分辨率（16 个均匀区间）。
    *   **Sampled Audit**：使用 MoveIt/FCL 在 250Hz 频率下进行的离散采样审计，作为参考基准。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号或 CPU 硬件配置。
*   **软件环境**：运行在 Ubuntu 22.04/WSL2，使用 ROS 2 Humble、MoveIt 2 和 Python 实现。
*   **运行耗时**：LARC 的中值运行时间为 81.8 ms，而精细基准方法（Fixed-fine）为 801.4 ms。

### 5. 实验数量与充分性
*   **实验规模**：共进行了 160 条轨迹的测试，涵盖了从完全空旷到高度受限的多种环境。
*   **审计规模**：通过 FCL 审计了超过 15.8 万个离散状态，用于验证认证结果的准确性。
*   **充分性评价**：实验设计较为客观，通过对比不同难度的场景展示了算法的自适应能力。使用了配对加速比（Paired speedup）来消除轨迹长度对性能评估的影响，具有较好的统计说服力。

### 6. 主要结论与发现
*   **计算效率**：LARC 仅需评估基准方法 24.8% 的区间数量，实现了 **10.28 倍** 的中值配对加速。在空旷场景下，加速比甚至可达 260 倍。
*   **决策一致性**：在最大深度为 9 的情况下，LARC 与固定精细基准方法的认证决策完全一致。
*   **安全性**：LARC 成功识别并拒绝了所有被 FCL 审计检测到碰撞的轨迹（零漏检）。
*   **保守性**：在 139 条采样无碰撞的轨迹中，有 27 条（19.4%）未被 LARC 认证，这反映了基于包络认证的保守性（即存在误报）。

### 7. 优点
*   **自适应性强**：能够根据环境复杂度自动分配计算资源，在安全区域快速通过，在危险区域深入检查。
*   **数学严谨**：基于精确的 Hermite 速度上限，提供了连续时间的外部障碍物避障保证。
*   **即插即用**：作为后处理认证器，不需要修改现有的轨迹规划器。

### 8. 不足与局限
*   **静态环境限制**：目前的模型仅支持静态障碍物，未考虑动态障碍物。
*   **自碰撞缺失**：当前的认证框架尚未包含连续时间的机械臂自碰撞检测。
*   **几何保守性**：使用胶囊体（Capsules）包裹复杂的连杆网格会导致一定的空间浪费，可能导致一些实际安全的轨迹无法通过认证（保守拒绝）。
*   **应用限制**：目前仅在仿真环境中验证，尚未在具有显著跟踪误差的真实物理机器人上进行鲁棒性测试。

（完）
