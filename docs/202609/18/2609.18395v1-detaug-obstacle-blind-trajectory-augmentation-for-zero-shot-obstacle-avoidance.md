---
title: "DetAug: Obstacle-Blind Trajectory Augmentation for Zero-shot Obstacle Avoidance"
title_zh: DetAug：用于零样本避障的障碍物无关轨迹增强
authors: "Reece O'Mahoney, Moritz Zoellner, Ioannis Havoutis"
date: 2026-09-16
pdf: "https://arxiv.org/pdf/2609.18395v1"
tags: ["query:profile-1"]
score: 8.0
evidence: 用于机械臂零样本避障的轨迹增强
tldr: 针对机器人操作策略在含障碍物环境下的泛化难题，本文提出DetAug方法。该方法在训练阶段对自由空间轨迹进行“障碍物盲”增强，并将增强参数作为条件标签记录；推理时通过采样标签并选择碰撞成本最低的轨迹实现避障。DetAug无需预知障碍物几何信息，在SafeLIBERO基准测试及真实硬件实验中均显著优于现有方法，实现了零样本避障。
selection_source: fresh_fetch
motivation: 现有机器人操作策略多基于无障碍数据集训练，难以直接泛化至含障碍物的测试环境。
method: 通过对轨迹的过渡阶段进行障碍物盲增强，并将增强参数作为条件标签，在推理时通过采样标签选择最优避障路径。
result: 在SafeLIBERO基准测试中，DetAug的无碰撞成功率比现有最佳方法高出20个百分点以上。
conclusion: DetAug证明了无需在训练中接触障碍物，仅通过轨迹增强与标签条件化即可实现高效的零样本避障。
---

## 摘要
机器人操作策略通常通过在大规模远程操作数据集上进行训练而生成。这些数据集通常由自由空间轨迹组成，导致其难以迁移到存在障碍物的测试环境中。此前弥合这一差距的方法主要分为两类：数据集增强在训练阶段解决该问题，但需要预先获得障碍物几何信息；而在推理阶段对现有检查点进行引导则无需此要求，但灵活性受限。我们的方法汲取了这两个领域的优势，且规避了各自的缺陷。DetAug 对自由空间数据集的过渡阶段应用了一种障碍物无关的增强方案，保持物体交互部分不变，并将增强参数记录为显式的条件标签。在推理时，它会对一组标签进行采样，并执行碰撞成本最低的轨迹。在 SafeLIBERO 基准测试中，DetAug 的无碰撞成功率比次优方法高出 20 个百分点以上，且通过标签空间进行选择的效果比在同一策略上使用引导的方法高出 26 个百分点。在真实硬件上，推理阶段的引导方法在需要大幅绕道的任务中表现失效，而 DetAug 在训练过程中从未见过障碍物的情况下，其表现仍能匹配或超过基于障碍物条件的基准方法。

## Abstract
Policies for robotic manipulation are produced by training on large teleoperated datasets. These datasets typically consist of free-space trajectories, making them difficult to transfer to test-time environments with obstacles. Previous methods for closing this gap have largely fallen into two groups. Dataset augmentation addresses it at training time but needs obstacle geometry in advance, whereas steering an existing checkpoint at inference time avoids that requirement but is limited in flexibility. Our method draws from both areas without inheriting either drawback. DetAug applies an obstacle-blind augmentation scheme to the transit phases of a free-space dataset, leaving object interactions untouched, and records the augmentation parameters as an explicit conditioning label. At inference it samples a batch of labels and executes the trajectory with the lowest collision cost. On the SafeLIBERO benchmark DetAug achieves a collision-free success rate more than 20pp above the next best method, and selecting over the label space outperforms guidance on the same policy by 26pp. On real hardware, inference-time steering methods collapse on tasks requiring large detours, while DetAug matches or exceeds an obstacle-conditioned baseline without ever seeing obstacles in training.

---

## 论文详细总结（自动生成）

以下是对论文《DetAug: Obstacle-Blind Trajectory Augmentation for Zero-shot Obstacle Avoidance》的结构化总结：

### 1. 核心问题与研究动机
*   **背景**：机器人操作策略（如扩散模型、流匹配模型）通常在“自由空间”数据集上训练，缺乏避障能力。
*   **痛点**：
    *   **训练时增强**：需要预知障碍物几何信息，且泛化能力有限。
    *   **推理时引导（Steering）**：无需训练数据，但灵活性差，当需要大幅绕道时，策略往往会因无法找到路径而停滞或超时。
*   **目标**：提出一种“障碍物盲（Obstacle-Blind）”的增强方法，既无需在训练时接触障碍物，又能实现比推理时引导更灵活的避障效果。

### 2. 方法论：DetAug
*   **核心思想**：将轨迹拆分为“交互阶段”（抓取/释放）和“过渡阶段”（移动）。仅对过渡阶段进行随机弧线增强，并将这些弧线的参数作为条件标签（Conditioning Label）注入模型。
*   **关键技术**：
    *   **轨迹增强**：将末端执行器的直线路径替换为由半角 $\phi$ 和平面旋转 $\theta$ 定义的圆弧。
    *   **条件化**：使用 AdaLN-Zero 将 4 维增强标签注入流匹配（Flow Matching）策略中。
    *   **推理选择**：在推理时，采样 $K$ 个不同的标签，生成 $K$ 条候选轨迹，通过分析碰撞成本（Collision Cost）选择最优的一条执行。
    *   **避障机制**：仅通过碰撞成本对候选轨迹进行排序，而非在生成过程中进行梯度引导。

### 3. 实验设计
*   **基准测试**：使用 SafeLIBERO 模拟基准，包含多种空间、物体、目标和长程任务。
*   **对比方法**：
    *   **DemoGen**（训练时增强，有障碍物先验）。
    *   **CAPE**（推理时引导，基于扩散模型）。
    *   **AEGIS**（推理时约束，基于控制障碍函数 CBF-QP）。
*   **硬件实验**：在真实机械臂上测试了杯子、管子、罐子和架子四种障碍物场景。

### 4. 资源与算力
*   **算力说明**：文中未明确列出具体的 GPU 型号、数量或总训练时长。
*   **模型规模**：使用了 10M 参数的 DiT-style Transformer 架构，强调了其轻量化特性，适合在资源受限的情况下进行策略适配。

### 5. 实验数量与充分性
*   **实验覆盖**：包含模拟基准测试、预训练策略适配（Adapter-only vs. Full fine-tune）、消融实验（标签数量 $K$ 的影响、选择 vs. 引导）以及真实硬件验证。
*   **客观性**：实验设计了多种对比组，并对任务成功率、无碰撞成功率和避障率进行了量化分析，实验设置较为充分且具有说服力。

### 6. 主要结论与发现
*   **性能优势**：在 SafeLIBERO 上，DetAug 的无碰撞成功率比次优方法高出 20 个百分点以上。
*   **选择优于引导**：通过标签空间进行选择比使用碰撞梯度引导的效果高出 26 个百分点。
*   **零样本能力**：在训练时完全不接触障碍物的情况下，DetAug 在真实硬件上的表现优于需要障碍物先验的基准方法。
*   **适配效率**：在冻结主干网络的情况下，DetAug 仅需 3M 参数的适配器即可实现有效的避障，远优于基于点云条件的适配方法。

### 7. 优点
*   **解耦设计**：通过将交互阶段与过渡阶段分离，确保了避障动作不会破坏抓取或释放的精度。
*   **低维搜索空间**：将避障问题转化为低维标签空间的采样选择，比在高维动作空间进行梯度引导更稳定、更高效。
*   **零样本泛化**：无需在训练阶段模拟复杂的障碍物环境，降低了数据生成的复杂度和对环境先验的依赖。

### 8. 不足与局限
*   **任务可行性**：文中提到约 20% 的基准任务因本身无可行路径而被剔除，说明该方法在极端受限空间下的表现仍有待提升。
*   **硬件依赖**：虽然避障效果好，但仍依赖于对障碍物的几何感知（用于计算碰撞成本），在复杂非结构化环境下的感知鲁棒性未深入探讨。
*   **扩展性**：目前主要在较小的策略模型上验证，在大规模 VLA（视觉-语言-动作）模型上的表现及计算开销仍需进一步研究。

（完）
