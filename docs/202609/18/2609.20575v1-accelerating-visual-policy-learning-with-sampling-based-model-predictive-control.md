---
title: Accelerating Visual Policy Learning with Sampling-Based Model Predictive Control
title_zh: 利用基于采样的模型预测控制加速视觉策略学习
authors: "Yilang Liu, Haoxiang You, Qian Wang, Daniel Rakita, Ian Abraham"
date: 2026-09-17
pdf: "https://arxiv.org/pdf/2609.20575v1"
tags: ["query:profile-1"]
score: 8.0
evidence: 用于加速操作任务视觉策略学习的采样MPC
tldr: 针对视觉策略学习中接触动力学复杂及计算成本高的问题，本文提出采样引导策略搜索（SGPS）。该方法结合采样模型预测控制与一阶策略梯度，通过解耦渲染过程实现高效视觉训练。实验证明，SGPS在多种机器人任务中表现优异，且训练出的策略可零样本迁移至真实硬件，实现自主运动与操作。
selection_source: fresh_fetch
motivation: 现有视觉策略学习在处理复杂接触任务时易陷入局部最优，且计算资源消耗巨大。
method: 提出SGPS框架，通过采样模型预测控制引导策略优化，并采用解耦渲染的视觉策略梯度算法。
result: 在单GPU上成功训练了多种机器人运动与操作任务，并实现了真实机器人上的零样本部署。
conclusion: SGPS有效提升了视觉策略学习的效率与鲁棒性，为复杂环境下的机器人自主控制提供了高效解决方案。
---

## 摘要
学习用于运动和操作的视觉策略需要协调与环境的接触，这可能会产生巨大的计算和 GPU 内存开销。一阶策略梯度（FoPG）通过可微仿真降低了训练成本，但局部优化可能会收敛到非预期的接触模式。为了解决这一缺陷，我们提出了采样引导策略搜索（SGPS），它将基于采样的模型预测控制的循环动作目标细化与一阶策略优化相结合。行为克隆利用采样动作初始化策略；随后，训练过程在扰动初始状态和随机动力学下，交替进行基于采样的细化与短视界 FoPG 更新。对于视觉策略训练，我们使用了一种解耦的 FoPG 公式，将渲染过程排除在计算图之外，从而能够在没有状态策略教师的情况下直接从深度观测中进行学习。在单块 GPU 上，SGPS 为模拟环境中的 Unitree Go2 和 G1 机器人学习了运动、障碍物穿越、推箱子和双臂搬运等任务的策略。我们的实验进一步表明，细化过程不仅限于初始化和跟踪，还能显著提升策略学习效果。在硬件部署方面，蒸馏后的策略可零样本迁移至真实的 Go2 机器人，并利用机载深度传感器自主实现小跑、爬行、跨越障碍以及在这些行为之间进行切换。

## Abstract
Learning visual policies for locomotion and manipulation requires coordinating contact with the environment and can incur substantial computation and GPU memory costs. First-order policy gradients (FoPG) reduce training cost through differentiable simulation, but local optimization can converge to unintended contact patterns. To address this shortfall, we propose Sampling-Guided Policy Search (SGPS), which couples recurring action-target refinement by sampling-based model-predictive control with first-order policy optimization. Behavior cloning initializes the policy from sampled actions; training then alternates sampling-based refinement with short-horizon FoPG updates under perturbed initial states and randomized dynamics. For visual policy training, we use a decoupled FoPG formulation that excludes rendering from the computation graph, enabling direct learning from depth observations without a state-policy teacher. On a single GPU, SGPS learns policies for locomotion, obstacle traversal, crate pushing, and bimanual carrying on simulated Unitree Go2 and G1 robots. Our experiments further show that refinement improves policy learning beyond initialization and tracking alone. For hardware deployment, the distilled policy transfers zero-shot to a real Go2 and uses onboard depth to autonomously trot, crawl, clear hurdles, and switch between these behaviors.

---

## 论文详细总结（自动生成）

这是一份关于论文《Accelerating Visual Policy Learning with Sampling-Based Model Predictive Control》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在机器人运动与操作任务中，视觉策略学习面临两大挑战：一是复杂接触动力学导致的局部最优问题（如非预期的步态模式）；二是视觉渲染与策略梯度计算带来的高昂计算与显存开销。
*   **研究动机**：现有方法（如基于教师-学生架构的强化学习）依赖复杂的预训练过程，而一阶策略梯度（FoPG）虽能通过可微仿真加速，但缺乏对接触模式的有效引导。本文旨在通过引入采样模型预测控制（MPC）作为引导，实现更高效、无需教师策略的视觉策略学习。

### 2. 方法论：采样引导策略搜索 (SGPS)
*   **核心思想**：将“基于采样的MPC动作细化”与“解耦FoPG策略优化”相结合。MPC提供高质量的动作目标，FoPG负责在扰动和随机动力学下优化策略。
*   **关键技术细节**：
    *   **参考生成**：利用退火采样（Annealed Sampling）生成可行轨迹，作为训练的初始参考。
    *   **行为克隆（BC）初始化**：通过克隆采样得到的参考动作，快速初始化策略。
    *   **循环细化**：在训练过程中，交替进行MPC细化（生成新的动作目标）和FoPG更新。
    *   **解耦FoPG**：将渲染过程从计算图中剥离（stop-gradient），仅通过物理动力学反向传播梯度，从而直接从深度图像学习，无需状态策略教师。
    *   **目标蒸馏**：通过DAgger算法将多个特定任务的专家策略蒸馏为一个统一的视觉策略，实现自主行为切换。

### 3. 实验设计
*   **场景与任务**：涵盖了 Unitree Go2（四足机器人）的平地小跑、爬行、跨越障碍，以及 Unitree G1（人形机器人）的推箱子和双臂搬运任务。
*   **对比方法**：包括行为克隆（BC only）、从零开始的FoPG、PPO（近端策略优化）以及基于状态的DAgger专家/学生模型。
*   **Benchmark**：主要评估指标为训练效率（环境交互步数、墙上时间）和任务成功率（如步态相关性、任务完成质量）。

### 4. 资源与算力
*   **硬件配置**：所有实验均在单块 NVIDIA RTX 4080 GPU 上完成。
*   **训练效率**：相比 PPO，SGPS 在四足机器人小跑任务中减少了约 196 倍的环境交互步数。训练过程通常在 1-2 小时内（墙上时间）即可达到较高性能。

### 5. 实验数量与充分性
*   **实验覆盖**：包含多种机器人形态（四足、人形）和多种复杂任务（运动、操作、障碍穿越），覆盖面较广。
*   **消融实验**：专门设计了“有无细化（Refinement）”的对比实验，证明了采样细化对提升策略性能的必要性。
*   **客观性**：实验对比了多种基准方法，并使用了多个随机种子进行评估，实验设计较为严谨、公平。

### 6. 主要结论与发现
*   **效率提升**：SGPS 显著降低了视觉策略学习的样本复杂度和计算时间。
*   **零样本迁移**：训练出的策略无需硬件微调，即可在真实 Go2 机器人上实现零样本部署，并能根据机载深度信息自主切换行为。
*   **引导作用**：采样MPC提供的动作目标能有效纠正FoPG在复杂接触任务中产生的错误步态。

### 7. 优点
*   **无需教师策略**：直接从视觉观测学习，简化了训练流程。
*   **计算高效**：解耦渲染的设计极大地降低了显存占用，使得单GPU训练成为可能。
*   **鲁棒性强**：通过在扰动状态和随机动力学下训练，策略具备良好的硬件迁移能力。

### 8. 不足与局限
*   **依赖可微仿真**：方法高度依赖于可微物理引擎（如 MuJoCo MJX），对环境建模的准确性有一定要求。
*   **任务特定性**：虽然框架通用，但每个任务仍需定义特定的采样目标和奖励函数。
*   **复杂操作限制**：目前对人形机器人的操作任务（如推箱子、搬运）仍处于简化模型阶段，对于更复杂的长程操作和精细交互尚待进一步探索。

（完）
