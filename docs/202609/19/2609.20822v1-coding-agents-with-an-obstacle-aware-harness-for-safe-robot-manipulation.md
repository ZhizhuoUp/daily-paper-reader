---
title: Coding Agents with an Obstacle-Aware Harness for Safe Robot Manipulation
title_zh: 面向安全机器人操作的障碍物感知框架编码智能体
authors: "Bingxin Xu, Yuzhang Shang, Zhen Dong, Emilio Ferrara"
date: 2026-09-17
pdf: "https://arxiv.org/pdf/2609.20822v1"
tags: ["query:profile-1"]
score: 8.0
evidence: 安全机器人操作的障碍物感知规划
tldr: 本文针对机器人操作中编码智能体（Coding Agents）缺乏安全意识的问题，提出了SafeHarness框架。该方法通过引入障碍物感知路由规划和接触执行机制，使智能体在执行任务时能优先考虑避障约束。实验表明，SafeHarness显著提升了任务成功率与避障能力，有效解决了现有编码智能体在复杂环境中因忽视安全约束而导致碰撞的缺陷。
selection_source: fresh_fetch
motivation: 现有编码智能体在机器人操作中往往只关注任务目标，而忽视了避障等安全约束。
method: 提出了SafeHarness框架，通过障碍物感知路由规划（预规划与重规划）和障碍物感知接触执行来强化安全约束。
result: "SafeHarness实现了71.9%的任务成功率和87.5%的避障率，性能远超现有最优方法。"
conclusion: 通过引入障碍物感知机制，可以有效弥补编码智能体在规划阶段对安全约束处理的不足，实现更安全的机器人操作。
---

## 摘要
编码智能体已成为机器人操作领域一种极具前景的范式：语言模型将机器人控制器编写为程序，以此构建的智能体无需针对特定机器人进行训练即可操控机器人。然而，这一范式是否安全尚未得到探讨。我们在安全约束条件下对编码智能体进行了评估，其中每项任务都将操作目标与机器人不得触碰的障碍物配对。智能体在追求目标的过程中，大多数情况下会与障碍物发生碰撞，仅将任务完成视为唯一目标而忽视了安全性。智能体在其推理轨迹中能够识别障碍物，且提示词中已明确禁止触碰，因此感知和指令均无误；问题的根源在于规划，即既定的约束条件从未被置于优先地位。通过将操作分解为路径规划阶段和接触密集时刻，我们定位了失败的根源。在路径规划阶段，模型无法优先考虑安全约束，缺乏对避障路径的概念，且在所选路径不可行时无法重新规划。在接触阶段，模型未意识到接触执行同样受到该约束的限制。为弥补这一差距，我们提出了 SafeHarness，它为模型配备了两个障碍物感知框架，使其能够优先考虑安全约束。障碍物感知路径规划将物体建模为边界框，并将其作为航点序列绘制候选路径。智能体随后预先规划路径、进行验证，并在必要时重新规划，最后才执行。障碍物感知接触执行则通过选择接触位置，确保接触过程本身避开障碍物。SafeHarness 实现了 71.9% 的任务成功率和 87.5% 的避障率，分别比现有最优水平（SOTA）提高了 6.5% 和 27.0%。这些结果分别是同一智能体在未使用该框架时的 2.3 倍和 1.5 倍。

## Abstract
Coding agents have emerged as a promising paradigm for robot manipulation: a language model writes the robot controller as a program, and agents built in this way now operate robots without robot-specific training.Whether this paradigm is also safe, however, has not been asked. We evaluate coding agent under a safety constraint, where each task pairs a manipulation goal with an obstacle the robot must not touch. The agent pursues the goal but collides with the obstacle in most cases, treating task completion as its sole objective while neglecting safety. The agent reasons about the obstacle in its traces, and the prompt already forbids touching it, so neither perception nor instruction is at fault; the fault lies in the planning, where the stated constraint never becomes a priority. By decomposing manipulation into a route phase and a contact-rich moment, we locate the source of the failure. Along the route, the model cannot prioritize the safety constraint, having no notion of a clearing route and none of replanning once a chosen route becomes infeasible. At the contact, it is unaware that contact execution is bounded by the same constraint. To close this gap, we present SafeHarness, which equips the model with two obstacle-aware harnesses that enable it to prioritize the safety constraint. Obstacle-aware route planning grounds the objects as bounding boxes and draws candidate routes over them as sequences of waypoints. The agent then plans a route in advance, verifies it, replans when necessary, and only then executes it. Obstacle-aware contact execution instead selects the contact position so that the contact itself avoids the obstacle. SafeHarness attains 71.9% task success and 87.5% collision avoidance, surpassing the previous SOTA by 6.5% and 27.0%, respectively. These results are $2.3\times$ and $1.5\times$ those of the same agent without harnesses.

---

## 论文详细总结（自动生成）

这是一份关于论文《Coding Agents with an Obstacle-Aware Harness for Safe Robot Manipulation》的结构化总结：

### 1. 核心问题与研究动机
*   **背景**：编码智能体（Coding Agents）通过语言模型编写程序来控制机器人，无需特定训练即可实现零样本操作，是当前机器人领域的前沿范式。
*   **问题**：现有研究主要关注“任务成功率”，忽视了“安全性”。在存在障碍物的复杂环境中，智能体往往只追求目标达成，而频繁与障碍物发生碰撞。
*   **根源**：研究发现，智能体并非无法识别障碍物或理解指令，而是缺乏**规划层面的安全优先级**。在长程任务中，安全约束往往在上下文记忆中被遗忘，导致模型在执行阶段忽视避障。

### 2. 方法论：SafeHarness 框架
*   **核心思想**：将操作任务分解为“路径规划（Route）”和“接触执行（Contact）”两个阶段，并为每个阶段引入外部的“障碍物感知框架（Harness）”，将安全约束从提示词（Prompt）转化为硬性验证机制。
*   **关键技术**：
    *   **障碍物感知路径规划**：利用分割模型（SAM-3）将场景物体建模为边界框（Bounding Boxes）。智能体在执行前必须通过“规划-验证-重规划”循环，确保路径不触碰障碍物。若执行中途受阻，则触发重新规划。
    *   **障碍物感知接触执行**：在接触目标（如抓取或放置）时，进行邻接测试。若障碍物靠近，则自动选择远离障碍物的接触方向或调整手腕旋转角度，确保接触动作本身不产生碰撞。
    *   **解耦设计**：这些机制作为外部工具调用，不依赖于语言模型内部的推理，从而规避了长上下文导致的指令漂移问题。

### 3. 实验设计
*   **数据集与 Benchmark**：使用 **SafeLIBERO** 基准测试，包含 32 个任务，涵盖了空间、目标、物体和长程任务四类场景，每个任务均设置了干扰障碍物。
*   **对比方法**：
    *   基础模型：OpenVLA-OFT、π0.5。
    *   现有 SOTA：AEGIS（基于控制障碍函数的安全层方法）。
    *   基线：未加 SafeHarness 的 GPT-5.5 和 GPT-6 编码智能体。

### 4. 资源与算力
*   **说明**：论文未明确列出具体的 GPU 型号、数量或训练时长。该方法主要侧重于“测试时计算（Test-time compute）”和外部工具调用，而非大规模模型训练。

### 5. 实验数量与充分性
*   **实验规模**：每个任务使用 10 个随机种子进行测试，共计 320 次实验。
*   **充分性**：进行了模型规模（GPT-5.5 vs GPT-6）和组件消融实验（Model-only vs Model+Skills vs SafeHarness），对比了不同层级（Level I/II）的避障表现，实验设计客观且具有较强的说服力。

### 6. 主要结论与发现
*   **性能提升**：SafeHarness 在 GPT-6 下达到了 71.9% 的任务成功率和 87.5% 的避障率，分别比 SOTA 方法提升了 6.5% 和 27.0%。
*   **安全本质**：安全性是“框架”的属性，而非“模型”的属性。通过将安全约束固化在工具接口中，可以有效解决长程任务中安全指令失效的问题。
*   **失败模式**：即使在引入框架后，仍存在约 1/8 的碰撞率，且在复杂任务中，安全性的提升可能会以牺牲部分任务完成效率为代价。

### 7. 优点
*   **创新性**：首次将编码智能体的安全问题置于与任务成功率同等重要的地位。
*   **可解释性**：通过显式的边界框验证和接触策略选择，使机器人的避障行为具有高度的可追溯性。
*   **通用性**：该框架作为“插件”设计，不依赖于特定模型架构，可轻松适配不同的编码智能体。

### 8. 不足与局限
*   **场景限制**：目前仅支持轴对齐的边界框建模，对于复杂形状或多个密集障碍物的处理能力有限。
*   **计算开销**：由于引入了多次规划与验证循环，每个任务的执行时间较长（分钟级）。
*   **仿真依赖**：目前主要在仿真环境（SafeLIBERO）中验证，真实物理环境下的感知噪声（如分割失败）可能导致验证机制失效。

（完）
