---
title: Time-Efficient Iterative Learning Planning for Safety-Critical Dynamic Obstacle Avoidance
title_zh: 面向安全关键型动态避障的高效迭代学习规划方法
authors: "Zhiyi Chen, Shuli Lv, Chen Min, Yong Xu, Jian Sun, Quan Quan"
date: 2026-09-17
pdf: "https://arxiv.org/pdf/2609.20435v1"
tags: ["query:q1"]
score: 9.0
evidence: 实时安全保证与动态避障
tldr: 针对资源受限的移动机器人，本文提出了一种结合迭代学习规划（ILP）与预期风险混合控制障碍函数（ARB-CBF）的动态避障框架。该方法通过ILP学习速度与转向策略，并利用ARB-CBF进行实时安全修正，在保证动态避障安全性的同时，显著降低了计算开销，实现了高效的实时导航。
selection_source: fresh_fetch
motivation: 现有迭代学习规划（ILP）在动态环境下的避障能力不足，且传统优化方法计算开销过大。
method: 通过引入预期风险混合控制障碍函数（ARB-CBF）对ILP生成的控制指令进行实时安全修正。
result: 实验表明该框架在保持高安全性的同时，相比优化类基准方法具有更优的计算效率和时间性能。
conclusion: 该方法非常适合计算资源受限的机器人平台，实现了高效且安全的动态环境导航。
---

## 摘要
自主移动机器人在受限的机载计算条件下，需要具备高效的规划能力与安全关键型动态避障能力。尽管迭代学习规划（ILP）提供了轻量且高效的遍历规划方案，但其缺乏针对动态障碍物感知与避障的显式机制。本文通过集成一种预见性风险融合控制障碍函数（ARB-CBF），将 ILP 扩展至动态环境下的安全关键型导航。扩展后的 ILP 基于局部障碍物风险，通过分数幂更新学习遍历速度和转向偏差配置，生成标称控制指令，并由 ARB-CBF 在运行时进行修正，从而实现实时安全保障。算法分析表明，ILP 重规划阶段的复杂度为 O(kN)（k 为迭代次数，N 为路径点数量），而 ARB-CBF 的执行复杂度为线性。全面的仿真与实地实验验证了该框架的有效性，结果显示，与基于优化的基准方法相比，该框架在保持较低计算开销的同时，展现出更优的时间效率与安全性，非常适用于资源受限的平台。

## Abstract
Autonomous mobile robots require timeefficient planning and safety-critical dynamic obstacle avoidance under constrained onboard computation. While Iterative Learning Planning (ILP) offers lightweight and efficient traversal planning, it lacks explicit mechanisms for dynamic obstacle perception and avoidance. This article extends ILP to safety-critical navigation in dynamic environments by integrating an anticipatory risk-blended control barrier function (ARB-CBF). The extended ILP learns traversal-speed and steering-bias profiles via a fractionalpower update based on local obstacle risk, generating nominal control commands that ARB-CBF modifies at runtime for real-time safety guarantees. Algorithmic analysis demonstrates that the ILP replanning stage scales at O(kN) for k iterations and N waypoints, while ARB-CBF executes with linear complexity. Comprehensive simulations and real-world experiments validate the framework, demonstrating superior temporal efficiency and safety with lower computational overhead compared to optimizationbased baselines, making it highly suitable for resourceconstrained platforms.

---

## 论文详细总结（自动生成）

你好！我是 Antigravity，很高兴为你分析这篇关于移动机器人动态避障的论文。以下是针对该论文的结构化总结：

---

### 1. 核心问题与研究背景
*   **核心问题**：在计算资源受限的移动机器人平台上，如何实现既高效（低计算开销）又安全（满足动态避障）的实时导航。
*   **研究背景**：现有的迭代学习规划（ILP）虽然在路径遍历上具有轻量级优势，但缺乏对动态障碍物的显式感知与避障机制；而传统的基于优化的避障方法（如 MPC）虽然效果好，但计算开销过大，难以在嵌入式平台上实时运行。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：采用“标称规划 + 安全修正”的架构。利用 ILP 生成初步的控制指令，再通过控制障碍函数（CBF）进行实时安全过滤。
*   **关键技术细节**：
    *   **ILP 扩展**：引入局部障碍物风险评估，通过“分数幂更新”（Fractional-power update）机制动态调整遍历速度和转向偏差，生成标称控制指令。
    *   **ARB-CBF（预期风险混合控制障碍函数）**：作为安全层，在运行时对 ILP 的输出进行修正。它不仅考虑当前状态，还融合了预期的风险信息，确保机器人始终处于安全集合内。
    *   **复杂度分析**：ILP 重规划阶段复杂度为 $O(kN)$（$k$ 为迭代次数，$N$ 为路径点数），ARB-CBF 为线性复杂度，保证了算法的实时性。

### 3. 实验设计
*   **场景设置**：涵盖了仿真环境与实地实验，重点模拟了存在动态障碍物的复杂导航场景。
*   **基准对比（Benchmark）**：主要与基于优化的方法（如传统的 MPC 或非线性优化规划器）进行对比。
*   **评价指标**：重点考察计算时间（Time Efficiency）、避障成功率（Safety）、路径平滑度及计算资源占用。

### 4. 资源与算力
*   **说明**：论文摘要及提供的内容中**未明确提及**具体的硬件配置（如 GPU 型号、训练时长等）。考虑到该方法强调“资源受限平台”，推测其在嵌入式计算单元（如 NVIDIA Jetson 系列或类似架构）上进行了验证。

### 5. 实验数量与充分性
*   **实验覆盖**：论文通过仿真与实地实验相结合的方式验证了算法，涵盖了不同动态环境下的避障表现。
*   **客观性与公平性**：通过与主流优化类基准方法的对比，实验设计在逻辑上是公平的。但由于未提供详细的消融实验细节（如不同参数对性能的影响），其在极端复杂环境下的鲁棒性边界仍有待进一步验证。

### 6. 主要结论与发现
*   **性能优势**：该框架在保持高安全性的前提下，显著降低了计算开销，时间效率优于传统的优化类方法。
*   **适用性**：证明了该方法非常适合计算资源受限的移动机器人平台，实现了从理论规划到实时执行的有效闭环。

### 7. 优点
*   **计算高效**：将复杂的动态避障问题解耦为“轻量级规划”与“线性安全修正”，极大地降低了实时计算压力。
*   **架构灵活**：ARB-CBF 模块具有通用性，可以作为插件集成到现有的 ILP 框架中，无需重构整个导航系统。
*   **理论支撑**：算法复杂度分析清晰，为实时性提供了理论保障。

### 8. 不足与局限
*   **环境假设**：对于高度不确定或极度拥挤的动态环境，该方法在处理复杂多智能体交互时的表现可能受限。
*   **参数敏感性**：分数幂更新机制的参数设置可能对不同环境的适应性存在偏差，缺乏自适应调整机制的详细讨论。
*   **实验深度**：未详细说明在极端计算压力下的性能衰减情况，且缺乏大规模场景下的长期稳定性测试。

---

（完）
