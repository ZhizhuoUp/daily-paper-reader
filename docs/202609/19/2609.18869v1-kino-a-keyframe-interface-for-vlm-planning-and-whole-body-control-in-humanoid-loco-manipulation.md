---
title: "KINO: A Keyframe Interface for VLM Planning and Whole-Body Control in Humanoid Loco-Manipulation"
title_zh: KINO：用于人形机器人移动操作中 VLM 规划与全身控制的关键帧接口
authors: "Sitong Chen, Fatemeh Zargarbashi, Jin Cheng, Tianxu An, Stelian Coros"
date: 2026-09-16
pdf: "https://arxiv.org/pdf/2609.18869v1"
tags: ["query:q6"]
score: 8.0
evidence: 类人机器人移动操作与全身控制
tldr: KINO是一个用于人形机器人移动操作的分层框架，通过引入“运动关键帧”作为VLM规划与RL控制的中间桥梁。该系统利用VLM根据指令和场景选择关键帧，并结合基于显著性的采样策略训练全身体策略，实现了从指令到复杂操作的高效执行。在Unitree G1人形机器人上的实验表明，该方法显著提升了物体抓取、搬运和放置任务的成功率，并具备良好的泛化能力。
selection_source: fresh_fetch
motivation: 人形机器人执行移动操作任务时，难以有效协同VLM的高层语义规划与底层的全身运动控制。
method: 提出一种分层框架，利用运动关键帧作为中间表示，并引入基于显著性的关键帧采样策略来优化底层策略训练。
result: "在模拟环境及Unitree G1实机上，任务成功率从44%大幅提升至92%，并能泛化至未见过的放置位置。"
conclusion: 通过关键帧解耦规划与控制，KINO有效解决了人形机器人复杂操作任务中的协同难题，提升了系统的鲁棒性与泛化性。
---

## 摘要
人形机器人的移动操作需要机器人在执行协调的全身运动的同时，解读任务指令和场景语义。我们提出了一个分层框架，利用运动关键帧作为视觉语言模型（VLM）规划与强化学习（RL）控制之间的中间表示。每个关键帧指定了机器人的目标全身姿态，并在适用时指定物体姿态。给定语言指令、场景观测和执行反馈，VLM 从预定义的库中选择与任务相关的连续关键帧。所选关键帧会根据当前场景进行重定向，以适应物体的姿态和尺寸。随后，关键帧条件下的全身策略生成关节级动作以达成这些目标。我们引入了一种基于显著性的关键帧采样策略用于底层策略训练，在使用稀疏 VLM 关键帧时，将端到端任务成功率从 44% 提高到了 92%。我们在仿真环境和 Unitree G1 人形机器人上对该框架进行了评估，涵盖了物体拾取、搬运和放置任务。该系统成功实现了单手和双手操作，并能泛化至训练参考数据之外的放置位置。

## Abstract
Humanoid loco-manipulation requires robots to interpret task instructions and scene semantics while executing coordinated whole-body motions. We propose a hierarchical framework that uses motion keyframes as an intermediate representation between Vision-Language Model (VLM) planning and Reinforcement Learning (RL) control. Each keyframe specifies a target whole-body robot pose and, when applicable, an object pose. Given a language instruction, scene observations, and execution feedback, the VLM selects successive task-relevant keyframes from a predefined library. The selected keyframes are retargeted to the current scene to account for object poses and dimensions. A keyframe-conditioned whole-body policy then generates joint-level actions to reach these goals. We introduce a saliency-based keyframe sampling strategy for low-level policy training that improves end-to-end task success rate from 44% to 92% when using sparse VLM keyframes. We evaluate our framework on object pickup, transport, and placement tasks in simulation and on a Unitree G1 humanoid. The system successfully performs both one- and two-handed manipulation and generalises to placement locations beyond the training reference data.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《KINO: A Keyframe Interface for VLM Planning and Whole-Body Control in Humanoid Loco-Manipulation》** 的结构化总结：

### 1. 核心问题与研究背景
*   **研究动机**：人形机器人执行“移动操作”（Loco-manipulation，即结合移动与操作）任务时，面临高层语义规划（VLM）与底层全身运动控制（RL）难以协同的挑战。
*   **背景**：现有的端到端 VLA（视觉-语言-动作）模型需要海量机器人特定数据，且难以处理高维全身动作；而传统的 RL 策略虽擅长运动控制，但缺乏高层任务规划能力。KINO 旨在通过“关键帧”这一中间表示，解耦高层语义推理与底层运动执行。

### 2. 方法论
*   **核心思想**：引入“运动关键帧”作为接口。VLM 负责从库中选择任务相关的关键帧，底层 RL 策略负责执行这些关键帧。
*   **关键技术细节**：
    *   **关键帧库**：预定义包含多种语义动作（如接近、蹲下、抓取）的姿态库。
    *   **场景重定向（Retargeting）**：通过逆运动学（IK）将库中的通用关键帧实时调整，以适应当前场景中物体的具体位置和尺寸。
    *   **基于显著性的采样策略**：在训练底层策略时，通过计算机器人身体加速度和物体运动的局部极大值，筛选出最具语义代表性的关键帧，而非均匀采样。
    *   **分层执行循环**：VLM 在闭环中根据机器人状态和视觉反馈，按需选择下一个关键帧，并具备故障检测与重试机制。

### 3. 实验设计
*   **场景与任务**：在仿真环境（MuJoCo）和 Unitree G1 实机上进行测试，任务包括单手（桶）和双手（箱子）的拾取、搬运与放置。
*   **对比方法**：对比了“均匀采样策略”与“基于显著性的采样策略”。
*   **评估指标**：任务成功率（物体放置在目标 0.3m 范围内）、目标达成误差（Root、关节、物体位置误差）。

### 4. 资源与算力
*   **算力配置**：VLM 使用 Qwen3.6-27B 模型，运行在单张 NVIDIA RTX 4090 GPU 上。
*   **效率**：VLM 推理时间小于 0.2 秒，重定向计算时间约为 5 毫秒，满足实时性要求。

### 5. 实验数量与充分性
*   **实验规模**：进行了 100 次对比实验以验证采样策略的有效性，并测试了不同放置位置的泛化能力。
*   **客观性**：通过消融实验对比了不同采样方法，并展示了实机部署的定性结果，实验设计逻辑清晰，覆盖了从仿真到实机的完整流程，具有较好的客观性。

### 6. 主要结论与发现
*   **性能提升**：基于显著性的采样策略将端到端任务成功率从 44% 提升至 92%。
*   **泛化能力**：系统能够成功执行训练数据之外的放置任务，证明了关键帧接口的通用性。
*   **鲁棒性**：分层架构允许 VLM 在检测到失败时触发重试，显著增强了复杂任务的成功率。

### 7. 优点
*   **解耦设计**：通过关键帧接口，避免了直接训练端到端 VLA 模型所需的大规模机器人动作数据。
*   **语义与控制平衡**：既利用了 VLM 的推理能力，又保留了 RL 在全身控制上的动态稳定性。
*   **高效性**：重定向模块计算开销极小，适合实时控制。

### 8. 不足与局限
*   **库构建依赖**：关键帧库目前需手动构建和标注，扩展性受限。
*   **感知局限**：目前依赖外部动作捕捉系统（Mocap）获取物体相对位姿，尚未完全实现仅依靠机载传感器的“野外”部署。
*   **放置稳定性**：在某些特定放置任务中，由于参考动作的限制，可能出现放置位置过近导致的不稳定，需依赖 RL 策略进行修正。

（完）
