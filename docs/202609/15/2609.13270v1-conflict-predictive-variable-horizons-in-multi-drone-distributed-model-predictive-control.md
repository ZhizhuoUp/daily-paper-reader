---
title: Conflict-Predictive Variable Horizons in Multi-Drone Distributed Model Predictive Control
title_zh: 多无人机分布式模型预测控制中的冲突预测变时域方法
authors: "Linda Mümken, Michael Schwung, Stefan Lier, Andreas Schwung"
date: 2026-09-07
pdf: "https://arxiv.org/pdf/2609.13270v1"
tags: ["query:profile-1"]
score: 8.0
evidence: 用于避障的分布式模型预测控制
tldr: 针对多无人机分布式模型预测控制（DMPC）中固定预测时域在计算成本与避障性能间的权衡难题，本文提出了一种冲突预测变时域策略。该方法通过外推邻居轨迹并计算冲突时间，动态调整每个无人机的预测时域。在空旷区域缩短时域以降低计算开销，在冲突临近时自动延长以确保安全。理论上证明了该方法在满足特定可行性边界下的递归可行性与稳定性，仿真实验表明其在保持高避障成功率的同时显著降低了计算成本。
selection_source: fresh_fetch
motivation: 旨在解决多无人机分布式控制中固定预测时域难以兼顾计算效率与避障安全性的矛盾。
method: 通过分析邻居轨迹外推预测冲突时间，并根据冲突距离动态调整每个无人机的局部预测时域。
result: 在密集避障场景中，该方法在保持与长时域相当的安全性前提下，大幅降低了单步求解成本与总计算量。
conclusion: 该变时域策略在保证系统递归可行性与稳定性的同时，实现了计算效率与避障性能的最优平衡。
---

## 摘要
在用于多无人机避障的分布式模型预测控制中，固定的预测时域往往需要权衡：短时域计算成本低但对邻近无人机的反应滞后，而长时域虽能预见冲突，但其单步计算成本随长度呈超线性增长。我们提出了一种冲突预测变时域方法，每架无人机可局部设定该时域，而无需改变分布式模型预测控制本身。通过对观测位置的历史数据进行分析，无人机可外推邻近无人机的飞行轨迹，利用随预测范围收缩的置信漏斗对每条轨迹进行冲突检测，并以闭式解形式获得各冲突发生的时间。该时域被设定为覆盖最远预测冲突点的最小容许值。在空旷空域中，该时域缩减至最小值，仅在前方存在冲突时才会增加。只要该最小值满足一个可计算的可行性界限，我们证明了该策略所选取的每个时域均能保持递归可行性和渐近稳定性。这些保证适用于线性模型；通过级联内环控制，四旋翼的平移动力学可简化为受扰动的双积分器模型，因此上述结论可推广至线性化四旋翼模型，并作为实用稳定性推广至完整的非线性模型。在密集对向交换基准测试的仿真中，变时域方法显著降低了单步求解器成本和总计算量，其性能远优于长固定时域，且在所有运行中均保持了安全距离，而计算成本相当的短固定时域则无法做到这一点。

## Abstract
In distributed model predictive control for multi-drone collision avoidance, a fixed prediction horizon forces a compromise: a short horizon is inexpensive but reacts late to approaching neighbors, whereas a long one anticipates conflicts at a per-step cost that grows superlinearly with its length. We propose a conflict-predictive variable horizon that each drone sets locally, leaving the distributed model predictive control itself unchanged. From a short history of observed positions, a drone extrapolates the flight lines of its neighbors, tests each against its own using confidence funnels that narrow with prediction range, and obtains each time to conflict in closed form. The horizon is then the smallest admissible value whose planning window covers the farthest predicted conflict. It collapses to its minimum in clear airspace and grows only when a conflict lies ahead. Provided this minimum meets a single computable feasibility bound, we prove that recursive feasibility and asymptotic stability are preserved for every horizon the policy can select. These guarantees hold for a linear model, and a cascaded inner loop reduces each quadrotor's translational dynamics to a perturbed double integrator, so they carry over to the linearized quadrotor model and, as practical stability, to the full nonlinear one. In simulation on dense antipodal-swap benchmarks, the variable horizon reduces both per-step solver cost and total computation well below those of a long fixed horizon, and it maintains separation in every run, which a short fixed horizon of comparable per-step cost does not.

---

## 论文详细总结（自动生成）

这是一份关于论文《多无人机分布式模型预测控制中的冲突预测变时域方法》的结构化总结：

### 1. 核心问题与研究背景
在多无人机分布式模型预测控制（DMPC）中，**预测时域（Prediction Horizon）**是决定系统性能的关键参数。固定时域存在难以调和的矛盾：
*   **短时域**：计算成本低，但对突发冲突反应滞后，易导致避障失败。
*   **长时域**：能提前预见冲突，但计算成本随长度呈超线性增长，导致资源浪费。
**研究动机**：设计一种能够根据冲突风险动态调整预测时域的机制，在空旷区域保持低计算开销，在冲突临近时自动延长时域以确保安全，同时保证系统的递归可行性与稳定性。

### 2. 方法论：冲突预测变时域（CPVH）
*   **核心思想**：将预测时域视为一个动态资源，仅在预测到冲突时才增加。
*   **关键技术**：
    *   **线性轨迹外推**：利用邻居无人机短期的位置历史，通过最小二乘法拟合飞行直线。
    *   **置信漏斗（Confidence Funnels）**：构建一个随预测时间推移而收缩的“漏斗”区域，用于处理预测不确定性。近处容差大，远处容差小。
    *   **闭式冲突检测**：通过计算两无人机轨迹的最近点距离，并与置信漏斗半径对比，快速判断是否存在冲突。
    *   **动态时域策略**：时域长度设定为覆盖最远预测冲突点的最小值，并限制在 $[H_{min}, H_{max}]$ 的预设区间内。
*   **理论保证**：证明了只要 $H_{min}$ 满足特定的可行性边界，无论时域如何动态切换，系统均能保持递归可行性和渐近稳定性。

### 3. 实验设计
*   **场景**：密集对向交换（Antipodal-swap）基准测试，即多架无人机同时向对侧目标点飞行，在中心区域产生高密度冲突。
*   **变量**：无人机数量（2, 4, 8架）与空域大小（紧凑型 53m³ 与宽敞型 203m³）。
*   **对比方法**：
    *   短固定时域（$H_{min}$）
    *   长固定时域（$H_{max}$）
    *   本文提出的变时域方法（CPVH）

### 4. 资源与算力
*   **算力说明**：论文未明确提及具体的 GPU 型号或训练时长。实验主要通过仿真环境进行，强调的是算法在不同时域策略下的**相对计算成本（Wall-clock time）**，而非绝对算力需求。

### 5. 实验数量与充分性
*   **实验规模**：共计 6 种场景组合，每种场景运行 20 次随机种子实验，总计 360 次运行。
*   **评价**：实验设计充分且客观。通过对比不同密度和空间约束下的表现，验证了算法在极端情况（如 8 架无人机）下的鲁棒性，并对比了计算成本与避障成功率，具有较好的公平性。

### 6. 主要结论与发现
*   **计算效率**：变时域方法在大多数场景下显著降低了总计算量（比长固定时域快 2.0-2.7 倍）。
*   **安全性**：在 8 架无人机的高密度场景中，短固定时域出现了避障失败（碰撞），而变时域方法在所有运行中均保持了安全距离。
*   **预测鲁棒性**：理论证明了安全性与预测准确性解耦，即使预测模型存在偏差，也不会破坏系统的稳定性，只会影响计算效率。

### 7. 优点
*   **理论严谨**：不仅给出了启发式算法，还通过扰动分析证明了其在非线性四旋翼模型下的实用稳定性。
*   **解耦设计**：变时域策略作为“插件”集成，无需修改底层的 DMPC 求解器逻辑。
*   **自适应性**：实现了计算资源与冲突风险的精准匹配，解决了固定时域的“一刀切”问题。

### 8. 不足与局限
*   **预测模型简单**：线性外推在处理复杂机动（如急转弯）的邻居时准确度有限，虽然置信漏斗缓解了该问题，但仍有提升空间。
*   **硬件验证缺失**：目前仅在仿真环境下验证，未在真实无人机集群上进行实地飞行测试。
*   **应用限制**：对于极度激进的特技飞行，该方法基于线性化假设的稳定性保证可能失效。

（完）
