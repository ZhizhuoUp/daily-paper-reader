---
title: Primitive-Informed Sampling-Based MPC for Multi-Fingered Dexterous Manipulation
title_zh: 面向多指灵巧操作的基元引导采样模型预测控制
authors: "Emek Barış Küçüktabak, Karankumar Patel, Jinda Cui, Zhaodong Yang, Kazuhiro Sasabuchi, Jun Takamatsu"
date: 2026-09-14
pdf: "https://arxiv.org/pdf/2609.14868v1"
tags: ["query:profile-1"]
score: 8.0
evidence: 基于采样的MPC用于具有轨迹约束的灵巧操作
tldr: 本文提出了一种基于基元引导的采样模型预测控制（MPC）框架，用于解决多指灵巧手操作中的高维空间探索难题。该方法通过低维操作基元引导采样，并结合关节级残差优化，有效提升了复杂接触动力学下的操作效率。实验表明，该框架在Allegro手及MuJoCo仿真中实现了稳定的物体旋转、抓取及协同操作，且具备良好的跨物体泛化能力和鲁棒性。
selection_source: fresh_fetch
motivation: 采样MPC在处理高维多指操作时面临探索效率低且依赖采样分布的问题。
method: 利用低维操作基元引导采样分布，并结合关节级残差优化与任务约束来筛选可行轨迹。
result: 该方法在Allegro手实验中实现了可靠的连续旋转，且在不同物体尺寸及模型偏差下表现出优异的泛化性。
conclusion: 基元引导与残差优化的结合是实现高效、鲁棒多指灵巧操作的关键。
---

## 摘要
我们提出了一种面向多指灵巧操作的基元引导采样模型预测控制（MPC）框架。基于采样的MPC避免了对复杂接触动力学求导的需求，但直接在高维关节空间中进行探索效率低下，且性能高度依赖于采样分布。我们的框架利用编码协调手指运动的低维操作基元来引导采样，同时优化关节级残差，以使这些运动适应当前的手-物配置。任务相关的展开约束在正向仿真过程中剔除了不可行的轨迹，从而提高了采样预算的有效利用率。我们在同步的MuJoCo数字孪生环境下，利用物理Allegro手对该方法进行了评估。消融实验表明，基元和残差对于实现可靠的连续手内旋转缺一不可；仅增加采样预算无法恢复这种协调性；且展开约束显著提高了成功率。从单个物体中提取的基元在不同物体尺寸和模型失配下依然有效。此外，该框架利用从仿真训练策略和人类手部运动数据中提取的基元，进一步支持了抓取、物体重新定向以及协调的手臂-手部到达-抓取-运输任务。

## Abstract
We present a primitive-informed sampling-based model predictive control (MPC) framework for multi-fingered dexterous manipulation. Sampling-based MPC avoids the need for gradients through complex contact dynamics, but direct exploration of the high-dimensional joint space is inefficient and makes performance strongly dependent on the sampling distribution. Our framework biases sampling using low-dimensional manipulation primitives that encode coordinated finger motions, while simultaneously optimizing joint-level residuals to adapt these motions to the current hand-object configuration. Task-related rollout constraints reject infeasible trajectories during forward simulation, improving the effective use of the sampling budget. We evaluate the approach on a physical Allegro hand using a synchronized MuJoCo digital twin. Ablations show that both the primitive and residual are necessary for reliable continuous in-hand rotation, that increasing the sampling budget alone does not recover this coordination, and that rollout constraints substantially improve success rate. A primitive extracted from a single object remains effective across object sizes and under model mismatch, and the framework further supports grasping, object reorientation, and coordinated arm-hand reach-grasp-transport, using primitives extracted from both a simulation-trained policy and human hand-motion data.

---

## 论文详细总结（自动生成）

这是一份关于论文《面向多指灵巧操作的基元引导采样模型预测控制》（Primitive-Informed Sampling-Based MPC for Multi-Fingered Dexterous Manipulation）的结构化总结：

### 1. 核心问题与研究动机
*   **背景**：多指灵巧手操作涉及高维关节空间和复杂的接触动力学（如滑动、接触切换），传统的基于梯度的 MPC 方法难以处理接触带来的非光滑性。
*   **动机**：现有的基于采样的 MPC 方法虽然避开了梯度计算，但直接在高维空间采样效率极低，且极度依赖采样分布。若采样不当，难以发现维持抓取或实现手指步态（finger gaiting）所需的协调运动。
*   **核心目标**：通过引入低维“操作基元”来引导采样，平衡全局协调性与局部关节适应性，从而提升在线控制的效率与鲁棒性。

### 2. 方法论
*   **核心思想**：将动作空间分解为“低维基元系数”与“高维关节残差”。基元提供协调运动的先验结构，残差负责局部微调。
*   **关键技术**：
    *   **基元提取**：利用 PCA 从仿真策略或人类手部运动数据中提取低维协调基元（$\Phi$）。
    *   **参数化采样**：在 MPC 的交叉熵方法（CEM）中，同时优化基元混合系数（$z$）和关节级残差（$\delta q$）。
    *   **任务约束（Rollout Constraints）**：在正向仿真（Rollout）过程中实时检查约束（如物体是否掉落），一旦违规立即终止仿真，避免浪费计算资源在无效轨迹上。
    *   **数字孪生**：利用 MuJoCo 实时同步物理手状态，实现闭环反馈控制。

### 3. 实验设计
*   **场景**：连续手内旋转、物体重新定向、抓取、以及手臂-手部协同的“到达-抓取-运输”任务。
*   **Benchmark**：对比了“无基元（直接关节采样）”、“无残差”、“无约束”等变体。
*   **评估指标**：成功率、旋转速度、物体掉落次数、以及在不同物体尺寸和模型参数偏差下的鲁棒性。

### 4. 资源与算力
*   **硬件**：实验在配备 AMD Ryzen Threadripper PRO 7995WX（192 线程）的服务器上进行。
*   **效率**：MPC 更新频率约为 38ms（N=350），能够满足实时控制需求。文中未提及具体的 GPU 型号，主要依赖 CPU 并行化进行物理仿真。

### 5. 实验数量与充分性
*   **实验规模**：进行了多组消融实验（如不同采样预算、不同采样规模的对比）以及鲁棒性测试（±50% 的质量和摩擦力偏差）。
*   **充分性**：实验设计非常严谨，通过统计学检验（Wilcoxon 秩和检验、Fisher 精确检验）验证了结果的显著性。通过对比不同数据来源（仿真 vs 人类数据）的基元，证明了方法的通用性。

### 6. 主要结论与发现
*   **基元与残差缺一不可**：基元提供了协调性，残差提供了适应性，两者结合才能实现复杂操作。
*   **采样预算的局限性**：单纯增加采样数量无法弥补缺乏协调先验带来的探索效率低下。
*   **约束的价值**：任务约束显著提升了成功率，且不会降低操作的灵活性。
*   **泛化能力**：基元具有良好的跨物体尺寸和模型失配的鲁棒性。

### 7. 优点
*   **结构化探索**：将高维搜索空间降维，显著提升了采样效率。
*   **计算高效**：通过“提前终止”机制，将算力集中在可行轨迹上。
*   **数据来源灵活**：支持从仿真策略或人类演示中提取基元，降低了对特定训练数据的依赖。
*   **闭环鲁棒性**：MPC 的重规划特性使其在模型存在较大偏差时仍能保持稳定。

### 8. 不足与局限
*   **基元依赖性**：虽然基元具有泛化性，但对于完全未知的、与提取基元时运动模式差异巨大的任务，可能需要重新提取基元。
*   **仿真模型依赖**：尽管具备一定的鲁棒性，但该方法仍高度依赖于 MuJoCo 物理引擎的准确性，若物理引擎与现实差距过大，仍可能影响性能。
*   **任务定义**：目前任务目标函数仍需人工设计，自动化构建复杂任务的代价尚不明确。

（完）
