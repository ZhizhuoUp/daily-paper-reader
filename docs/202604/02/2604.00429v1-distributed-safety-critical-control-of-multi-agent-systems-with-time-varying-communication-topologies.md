---
title: Distributed Safety-Critical Control of Multi-Agent Systems with Time-Varying Communication Topologies
title_zh: 具有时变通信拓扑的多智能体系统分布式安全关键控制
authors: "Shiyu Cheng, Luyao Niu, Bhaskar Ramasubramanian, Andrew Clark, Radha Poovendran"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00429v1"
tags: ["query:q2"]
score: 8.0
evidence: 基于分布式优化的避障控制
tldr: 针对多智能体系统在有限通信范围下拓扑结构随时间变化的挑战，本文提出一种分布式安全关键控制框架。通过引入截断函数将时变拓扑平滑化，并利用双时间尺度辅助变量解耦全局约束，实现了在动态拓扑下的避障、连通性保持及目标到达。该方法解决了传统方法难以处理的拓扑切换不连续性问题，并通过奇异摄动理论证明了系统的安全性与收敛性。
selection_source: fresh_fetch
motivation: 传统多智能体控制方法多假设通信拓扑固定，难以处理实际应用中因通信范围受限导致的拓扑动态切换及约束不连续问题。
method: 采用截断函数实现拓扑平滑化，并结合双时间尺度辅助变量与奇异摄动分析，将全局耦合约束解耦为可局部求解的优化问题。
result: 理论证明并仿真验证了该框架在时变拓扑下能同时保证多智能体系统的避障安全性、通信连通性以及对目标区域的收敛性。
conclusion: 该研究为具有动态通信约束的多智能体系统提供了一种稳健的分布式安全控制方案，有效解决了拓扑切换带来的控制不连续性挑战。
---

## 摘要
协调多个自主智能体在避障和保持通信连通性的同时到达目标区域是多智能体系统中的核心问题。在实际应用中，智能体的通信范围有限。因此，随着智能体的移动，网络链路会产生和消失，使得拓扑结构呈现状态依赖性和时变性。现有的多智能体到达-规避问题的分布式解决方案通常假设通信拓扑固定，因此在遇到时变拓扑引起的间断性时并不适用。本文提出了一种基于分布式优化的控制框架，通过两种互补机制解决这些挑战。首先，我们引入了一个截断函数，将时变通信图转换为平滑的状态依赖图，确保在创建或移除通信链路时约束保持连续。其次，我们采用具有双时间尺度动力学的辅助失配变量来解耦全局耦合的状态依赖约束，从而产生一个奇异摄动系统，每个智能体仅利用局部信息和邻居通信即可求解。通过奇异摄动分析，我们证明了该分布式控制器能够保证碰撞规避、连通性保持以及向目标区域的收敛。我们通过涉及障碍物和时变通信拓扑的多智能体导航数值模拟验证了所提框架。

## Abstract
Coordinating multiple autonomous agents to reach a target region while avoiding collisions and maintaining communication connectivity is a core problem in multi-agent systems. In practice, agents have a limited communication range. Thus, network links appear and disappear as agents move, making the topology state-dependent and time-varying. Existing distributed solutions to multi-agent reach-avoid problems typically assume a fixed communication topology, and thus are not applicable when encountering discontinuities raised by time-varying topologies. This paper presents a distributed optimization-based control framework that addresses these challenges through two complementary mechanisms. First, we introduce a truncation function that converts the time-varying communication graph into a smoothly state-dependent one, ensuring that constraints remain continuous as communication links are created or removed. Second, we employ auxiliary mismatch variables with two-time-scale dynamics to decouple globally coupled state-dependent constraints, yielding a singular perturbation system that each agent can solve using only local information and neighbor communication. Through singular perturbation analysis, we prove that the distributed controller guarantees collision avoidance, connectivity preservation, and convergence to the target region. We validate the proposed framework through numerical simulations involving multi-agent navigation with obstacles and time-varying communication topologies.

---

## 论文详细总结（自动生成）

以下是对论文《具有时变通信拓扑的多智能体系统分布式安全关键控制》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：在多智能体系统（MAS）中，实现“到达-规避”（Reach-Avoid）任务（即在避障的同时到达目标区）并保持通信连通性是核心挑战。
*   **核心问题**：现实中智能体的通信范围有限，导致网络拓扑随位置变化而动态切换（状态依赖且时变）。现有的分布式安全控制方案（如基于控制屏障函数 CBF 的方法）大多假设拓扑固定，难以处理拓扑切换带来的约束不连续性和维度变化问题。
*   **整体含义**：本文旨在开发一种分布式优化控制框架，使智能体能够在通信链路不断产生和消失的动态环境下，依然保证安全性、连通性和任务收敛性。

### 2. 论文提出的方法论
核心思想是结合**平滑化处理**与**双时间尺度动力学**来解耦全局约束。
*   **截断函数（Truncation Function）**：引入一个平滑的截断函数 $h_{i,j}$，当智能体间距接近通信极限时，该函数能平滑地将相关约束的影响降至零。这确保了当通信链路创建或移除时，优化问题的约束条件保持连续，避免了控制输入的突变。
*   **辅助失配变量（Mismatch Variables）**：为了将全局耦合约束（如代数连通度 $\lambda_2$ 和全局收敛目标）分解为局部可计算的形式，为每个智能体引入辅助变量 $z$。
*   **双时间尺度动力学（Two-Time-Scale Dynamics）**：
    *   **快尺度**：辅助变量和对偶变量（拉格朗日乘子）快速演化，通过邻居间的局部通信寻找优化问题的鞍点（即最优解）。
    *   **慢尺度**：智能体的物理状态根据快尺度计算出的控制输入进行更新。
*   **奇异摄动分析（Singular Perturbation Analysis）**：利用该理论证明了当快尺度演化足够快时，分布式控制器的轨迹能够收敛到集中式最优解的邻域，并严格保证安全性和连通性。

### 3. 实验设计
*   **实验场景**：5个智能体在二维平面内的导航任务。
*   **任务目标**：避开7个圆形障碍物，同时所有智能体需进入各自的椭圆形目标区域。
*   **约束条件**：包括智能体间的防撞距离 $d_0$、障碍物安全距离、以及基于拉普拉斯矩阵第二最小特征值 $\lambda_2$ 的全局连通性要求。
*   **对比与基准**：实验主要验证了分布式算法在**时变拓扑**下的有效性。通过观察 $\lambda_2$ 的演化曲线作为连通性的 Benchmark，证明其在整个过程中始终大于零。

### 4. 资源与算力
*   **硬件环境**：配备 Apple M3 处理器和 36 GB RAM 的笔记本电脑。
*   **算力细节**：文中未提及使用 GPU，因为该研究属于控制理论仿真而非深度学习训练。计算压力主要来自于每个时间步求解局部二次规划（QP）问题。
*   **训练时长**：不涉及模型训练，仅为实时控制仿真。

### 5. 实验数量与充分性
*   **实验规模**：论文展示了一组具有代表性的多智能体导航仿真，详细记录了 $t=0, 50, 75, 250$ 等关键时刻的拓扑变化。
*   **充分性评价**：
    *   **理论验证充分**：实验成功展示了通信链路断开又重连的过程，验证了截断函数处理不连续性的能力。
    *   **统计性略显不足**：实验仅针对5个智能体的单一场景，缺乏更大规模智能体集群（如50+智能体）的扩展性测试，也未进行不同参数（如 $\tau$ 的取值）的消融实验对比。

### 6. 论文的主要结论与发现
*   **分布式可行性**：证明了通过引入失配变量，智能体仅需局部信息和邻居通信即可处理全局连通性约束。
*   **拓扑鲁棒性**：所提框架能有效应对通信链路的动态增删，保证了控制指令的平滑性。
*   **性能保证**：理论证明并仿真验证了系统在满足硬安全约束（避障、防撞）的前提下，能够以 $\eta$ 的精度收敛到目标区域。

### 7. 优点
*   **理论严谨**：利用奇异摄动理论为分布式安全控制提供了坚实的数学证明。
*   **解决痛点**：针对性地解决了分布式 CBF 方法在处理动态拓扑时常见的“约束跳变”问题。
*   **平滑切换**：截断函数的设计巧妙地平衡了通信范围限制与控制连续性之间的矛盾。

### 8. 不足与局限
*   **可行性假设过强**：算法依赖于“优化问题始终可行”的假设（Assumption 1），但在极端拥挤或障碍物密集的复杂环境下，安全约束与连通性约束可能发生冲突导致无解。
*   **计算延迟风险**：双时间尺度架构要求快尺度变量的更新频率远高于物理系统，在实际嵌入式硬件上可能面临通信带宽和计算频率的限制。
*   **全局信息依赖**：虽然是分布式实现，但计算连通性特征值 $\lambda_2$ 的梯度仍隐含地需要某种形式的分布式估计（如文中提到的分布式共识算法），这增加了系统的复杂性。

（完）
