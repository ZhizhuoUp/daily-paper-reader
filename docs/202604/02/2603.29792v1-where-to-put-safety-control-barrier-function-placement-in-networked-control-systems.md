---
title: Where to Put Safety? Control Barrier Function Placement in Networked Control Systems
title_zh: 安全性应置于何处？网络化控制系统中的控制屏障函数部署
authors: "Severin Beger, Yuling Chen, Sandra Hirche"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29792v1"
tags: ["query:q2"]
score: 8.0
evidence: 用于安全性的MPC和控制屏障函数
tldr: 本研究探讨了在具有网络延迟的自主网络控制系统中，控制屏障函数（CBF）的安全放置问题。通过对比本地CBF过滤器与远程模型预测控制（MPC）中的预测性CBF约束，揭示了安全性与性能之间的权衡：本地CBF利用实时状态提供更高的扰动容忍度，而MPC-CBF通过前瞻性行为提升性能。基于此，提出了一种结合两者的混合架构，并在机器人避障任务中验证了其有效性。
selection_source: fresh_fetch
motivation: 针对网络控制系统中的通信延迟问题，探究在何处部署安全机制（本地或远程）能更有效地保障系统安全。
method: 对比分析了本地近视CBF过滤器与远程MPC嵌入式预测CBF约束，并推导了状态相关的扰动容忍度界限。
result: 研究发现本地CBF具有更强的抗扰动能力，而MPC-CBF则具备更好的性能表现，两者存在基本的权衡关系。
conclusion: 提出并验证了一种集成预测性与本地安全机制的混合架构，能够兼顾系统的鲁棒安全性与高性能。
---

## 摘要
确保安全行为对于现代自主信息物理系统至关重要。控制屏障函数 (CBF) 被广泛用于强制执行自主系统的安全性，但其在网络化控制架构中的部署位置在很大程度上仍未得到探索。在这项工作中，我们研究了在远程模型预测控制器 (MPC) 通过延迟网络与受控对象通信的网络化控制系统中，应在何处强制执行安全性。我们比较了两种安全策略：i) 应用于受控对象的局部近视 CBF 滤波器，以及 ii) 嵌入在远程 MPC 中的预测性 CBF 约束。对于这两种架构，我们推导了依赖于状态的扰动容限边界，并表明安全部署位置引发了一个根本性的权衡：局部 CBF 由于能够获取最新的状态测量值而提供了更高的扰动容限，而 MPC-CBF 通过前瞻性行为实现了性能提升，但其允许的扰动水平更为严格。基于这一见解，我们提出了一种集成预测性和局部安全机制的组合架构。理论研究结果在执行避障任务的平面三自由度机器人仿真中得到了验证。

## Abstract
Ensuring safe behavior is critical for modern autonomous cyber-physical systems. Control barrier functions (CBFs) are widely used to enforce safety in autonomous systems, yet their placement within networked control architectures remains largely unexplored. In this work, we investigate where to enforce safety in a networked control system in which a remote model predictive controller (MPC) communicates with the plant over a delayed network. We compare two safety strategies: i) a local myopic CBF filter applied at the plant and ii) predictive CBF constraints embedded in the remote MPC. For both architectures, we derive state-dependent disturbance tolerance bounds and show that safety placement induces a fundamental trade-off: local CBFs provide higher disturbance tolerance due to access to fresh state measurements, whereas MPC-CBF enables improved performance through anticipatory behavior, but yields stricter admissible disturbance levels. Motivated by this insight, we propose a combined architecture that integrates predictive and local safety mechanisms. The theoretical findings are illustrated in simulations on a planar three-degree-of-freedom robot performing a collision-avoidance task.

---

## 论文详细总结（自动生成）

这篇论文针对网络化控制系统（NCS）中的安全性部署问题进行了深入探讨，以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在存在网络延迟和扰动的自主系统中，**控制屏障函数（CBF）应该部署在何处？** 是部署在受控对象本地（Local），还是部署在远程的模型预测控制器（MPC）中？
*   **研究动机**：现代自主系统（如云机器人、远程手术机器人）通常采用远程计算、本地执行的架构。远程 MPC 具有前瞻性但受延迟影响；本地 CBF 响应快但缺乏远见（近视）。目前缺乏对这两种部署方式在扰动容忍度和性能权衡方面的系统性分析。

### 2. 论文提出的方法论
论文对比分析了两种架构，并提出了一种融合方案：
*   **本地 CBF 架构**：在受控对象端应用一个“近视”的 CBF 过滤器。它利用最新的状态测量值 $x_k$ 最小化对 MPC 指令的修改，以满足单步安全约束。
*   **远程 MPC-CBF 架构**：将 CBF 约束直接嵌入远程 MPC 的优化问题中。它基于预测状态序列执行多步安全约束，能够预见并避开潜在危险。
*   **理论推导**：论文推导了两种架构的状态相关扰动容忍度边界（$\bar{w}_l$ 和 $\bar{w}_r$）。证明了由于网络延迟导致的预测误差累积以及多步收缩特性，**远程架构的扰动容忍度严格小于本地架构**（即 $\bar{w}_r < \bar{w}_l$）。
*   **混合架构（Combined）**：为了结合两者的优点，论文提出了一种鲁棒化的混合方案：
    *   远程 MPC 使用松弛变量（Slack Variables）处理 CBF 约束，以保证在较大扰动下的可行性。
    *   本地 CBF 进行鲁棒化加紧，作为最后一道安全防线，补偿远程指令因延迟和扰动产生的偏差。

### 3. 实验设计
*   **实验场景**：一个平面三自由度（3-DoF）机械臂的避障任务。机器人需要依次到达两个航路点，同时避开两个圆形障碍物。
*   **实验设置**：
    *   **网络延迟**：设置恒定的往返延迟 $\tau = 7$ 个采样周期（35ms）。
    *   **扰动水平**：分为低扰动（$<\bar{w}_r$）和高扰动（介于 $\bar{w}_r$ 和 $\bar{w}_l$ 之间）两种情况。
*   **对比方法**：
    1.  仅本地 CBF（Local CBF）。
    2.  仅远程 MPC-CBF（Remote MPC-CBF）。
    3.  论文提出的混合架构（Combined）。
*   **评价指标**：安全率（Safe Rate）、任务完成率（Reach Rate）、平均到达时间、控制平滑度（Peak Jerk）。

### 4. 资源与算力
*   论文**未明确说明**具体的硬件算力（如 GPU/CPU 型号）或训练时长。
*   由于实验基于离散时间非线性动力学仿真，且优化问题规模较小（MPC 预测步长 $N=5$），预计在普通工作站或笔记本电脑上即可运行。

### 5. 实验数量与充分性
*   **实验数量**：针对每种扰动情况，每种架构各进行了 **50 次** 独立运行。
*   **充分性与客观性**：
    *   实验涵盖了理论推导出的两个关键扰动区间，验证了边界的有效性。
    *   通过统计成功率、安全率和物理指标（加加速度），多维度地评估了性能与安全性的权衡。
    *   实验设计较为客观，直接对比了相同参数下的不同部署位置效果。

### 6. 主要结论与发现
*   **扰动容忍度权衡**：本地 CBF 具有更高的鲁棒性，能处理更大的扰动，因为它使用的是“新鲜”的状态数据；远程 MPC-CBF 对扰动更敏感，容易因延迟导致的预测偏差而违反安全约束。
*   **性能权衡**：远程 MPC-CBF 表现出更好的前瞻性，路径更短，控制更平滑（Jerk 更小）；本地 CBF 往往反应迟钝，可能导致避障动作剧烈或陷入死锁。
*   **混合架构的优越性**：提出的混合架构在保持 100% 安全率（等同于本地 CBF）的同时，获得了接近远程 MPC 的任务效率和平滑度。

### 7. 优点
*   **理论严谨**：首次从解析角度证明了网络化系统中安全机制部署位置导致的扰动容限差异。
*   **架构实用**：提出的混合架构解决了“鲁棒性”与“前瞻性”的矛盾，对实际工程（如远程驾驶、云机器人）具有指导意义。
*   **分析深入**：不仅关注是否安全，还关注了控制品质（平滑度）和任务效率。

### 8. 不足与局限
*   **延迟模型简化**：实验假设网络延迟是恒定的，而实际网络（如 5G 或互联网）往往存在抖动（Jitter）和丢包。
*   **系统复杂度**：实验仅在 3-DoF 机械臂上验证，对于更高维度或具有更复杂非线性动力学的系统，其计算开销和可行性边界可能更加复杂。
*   **参数依赖**：混合架构中松弛变量和鲁棒加紧项的参数调节可能需要较多的先验知识。

（完）
