---
title: Real-Time Algorithms for Model Predictive Control of Hybrid Dynamical Systems
title_zh: 混合动力系统模型预测控制的实时算法
authors: "Armin Nurkanović, Anton Pozharskiy, Moritz Diehl"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18432v1"
tags: ["query:profile-1"]
score: 9.0
evidence: 非线性混合系统的实时MPC算法
tldr: 针对混合动力系统模型预测控制（MPC）中优化问题非光滑及反馈律不连续的挑战，本文提出了一套基于动力学互补系统建模的实时算法。通过将优化问题转化为带互补约束的数学规划（MPCC），并采用求解带互补约束二次规划（QPCC）的方案，实现了对不连续反馈律的局部仿射近似。该方法在机器人操纵任务中成功实现了在线接触序列发现，并证明了误差的一致有界性。
selection_source: fresh_fetch
motivation: 传统的非线性规划方法在混合系统发生切换时容易失效，且混合MPC面临非光滑优化和不连续反馈的难题。
method: 将混合系统建模为动力学互补系统，并提出三种通过求解带互补约束二次规划（QPCC）来近似MPC反馈律的实时算法。
result: 建立了参数化MPCC的连续性与可微性理论，并证明了算法在解不连续情况下仍能保持近似误差的一致有界。
conclusion: 该算法能够有效处理混合系统的实时控制，并在机器人操纵等复杂任务中展现出在线发现接触序列的能力。
---

## 摘要
混合动力系统的模型预测控制（MPC）具有挑战性，因为相关的优化问题是非平滑的，且生成的反馈律是不连续的。本文针对建模为动力学互补系统的非线性混合系统，开发了实时 MPC 算法。由此产生的最优控制问题被表述为带互补约束的数学规划（MPCCs）。我们证明了参数化 MPCC 的解映射是不连续的，并且当混合系统发生切换时，基于标准非线性规划的方法可能会变得不可行。为解决这一问题，我们提出了三种实时混合 MPC 方案，其反馈阶段在每个采样周期求解一个带互补约束的二次规划，从而产生 MPC 反馈律的局部不连续分段仿射近似。此外，我们推导了参数化 MPCC 的连续性和可微性结果，并建立了在解存在不连续性的情况下，新混合 MPC 算法的近似误差保持一致有界的条件。这些算法在一个机器人操作示例中得到了验证，其中接触序列是在线发现的。

## Abstract
Model predictive control (MPC) of hybrid dynamical systems is challenging because the associated optimization problem is nonsmooth and the resulting feedback law is discontinuous. This paper develops real-time MPC algorithms for nonlinear hybrid systems modeled as dynamical complementarity systems. The resulting optimal control problems are formulated as mathematical programs with complementarity constraints (MPCCs). We show that the solution map of parametric MPCCs is discontinuous, and that standard nonlinear-programming-based approaches may become infeasible when the hybrid system switches. To address this, we introduce three real-time hybrid MPC schemes whose feedback phase solves a quadratic program with complementarity constraints per sample, yielding local discontinuous piecewise affine approximations of the MPC feedback law. Moreover, we derive continuity and differentiability results for parametric MPCCs, and establish conditions under which the approximation error of our new hybrid MPC algorithms remains uniformly bounded despite solution discontinuities. The algorithms are demonstrated on a robotic manipulation example, where contact sequences are discovered online.

---

## 论文详细总结（自动生成）

这是一份关于论文《Real-Time Algorithms for Model Predictive Control of Hybrid Dynamical Systems》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
混合动力系统（如带有接触和摩擦的机器人、电子开关电路等）的控制极具挑战性，因为其动力学具有**非光滑性**，且最优控制产生的反馈律往往是**不连续**的。
*   **研究动机**：传统的模型预测控制（MPC）通常依赖非线性规划（NLP）求解器，这些求解器假设系统是光滑的。当系统发生状态切换（如机器人足端着地）时，标准算法往往会因为灵敏度信息失效而无法收敛，或者无法处理反馈律的跳变。
*   **核心目标**：开发一种能够实时运行、且能处理混合系统不连续特性的 MPC 算法，使其在系统发生结构性变化时仍能保持可行性和稳定性。

### 2. 核心方法论
论文提出了一套基于**动力学互补系统（DCS）**建模的实时 MPC 框架：
*   **建模方式**：将混合动力学建模为带互补约束的微分方程，从而将最优控制问题转化为**带互补约束的数学规划（MPCC）**。
*   **实时迭代（RTI）方案**：借鉴了标准非线性 MPC 中的 RTI 思想，但将其扩展到非光滑领域。核心是将复杂的 MPCC 在每个采样时刻进行线性化。
*   **关键技术：QPCC 求解**：
    *   不同于传统 RTI 求解二次规划（QP），本文在反馈阶段求解**带互补约束的二次规划（QPCC）**。
    *   **局部仿射近似**：QPCC 能够生成 MPC 反馈律的局部“分段仿射”近似，这允许反馈律在切换点发生跳变，而传统的 QP 只能提供连续近似。
*   **三种算法变体**：论文提出了三种不同复杂度的 RTI 方案，分别针对计算速度和可行性保障进行了优化。

### 3. 实验设计
*   **实验场景**：**机器人操纵任务（Robotic Manipulation）**。具体为一个机械臂推动滑块的示例，涉及复杂的接触力学。
*   **核心挑战**：算法需要在线发现接触序列（即何时接触、何时脱离、何时发生滑动），而不是预先给定步态或接触顺序。
*   **对比基准（Benchmark）**：
    *   标准的基于 NLP 的实时迭代算法（Standard RTI）。
    *   全非线性 MPCC 离线解（作为最优性参考）。
*   **评估指标**：跟踪误差、计算耗时、在切换点附近的可行性、接触序列识别的准确性。

### 4. 资源与算力
*   **算力说明**：论文中未详细列出具体的 GPU/CPU 型号或具体的训练时长。
*   **实现细节**：算法通常在 CPU 上运行，利用了专门的 MPCC 求解器（如基于内点法或序列二次规划的变体）。由于是实时控制研究，重点在于单次迭代的毫秒级响应速度。

### 5. 实验数量与充分性
*   **实验规模**：论文通过机器人操纵的仿真案例进行了验证。
*   **理论证明**：除了数值实验，论文提供了大量的数学证明，包括参数化 MPCC 的解映射连续性分析，以及在存在不连续性时近似误差的一致有界性证明。
*   **充分性评价**：实验设计较为充分，涵盖了混合系统最核心的“接触序列发现”问题。理论与实验相互印证，证明了算法在处理结构切换时的鲁棒性。

### 6. 主要结论与发现
*   **不连续反馈的必要性**：证明了在混合系统中，基于 QP 的连续近似在切换点会产生较大误差，而 QPCC 能准确捕捉反馈律的跳变。
*   **实时性可行**：提出的 QPCC-RTI 算法能够在实时约束下运行，且计算量与标准 RTI 相当，但处理混合特性的能力显著增强。
*   **误差有界性**：即使在解不连续的情况下，通过合理的算法设计，MPC 的近似误差可以保持在一致有界的范围内，确保了闭环系统的稳定性。

### 7. 优点
*   **理论严谨**：建立了 MPCC 灵敏度分析的新理论，填补了非光滑实时控制的部分理论空白。
*   **无需预设序列**：能够在线自动发现接触序列，极大地提高了机器人在复杂环境下的自主性。
*   **算法创新**：将 QPCC 引入 RTI 框架，解决了传统方法在切换点失效的痛点。

### 8. 不足与局限
*   **计算复杂度**：虽然比全非线性 MPCC 快，但求解 QPCC 仍比标准 QP 复杂，对底层求解器的稳定性要求较高。
*   **局部最优风险**：作为一种基于线性化的实时算法，它可能陷入局部最优，尤其是在动力学极度非线性的情况下。
*   **实验维度**：目前的验证主要集中在仿真环境，尚未在具有高频噪声和硬件延迟的真实物理机器人上进行大规模测试。

（完）
