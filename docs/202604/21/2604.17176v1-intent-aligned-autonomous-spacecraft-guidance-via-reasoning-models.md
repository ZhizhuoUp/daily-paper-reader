---
title: Intent-aligned Autonomous Spacecraft Guidance via Reasoning Models
title_zh: 基于推理模型的意图对齐自主航天器制导
authors: "Yuji Takubo, Simone D'Amico"
date: 2026-04-19
pdf: "https://arxiv.org/pdf/2604.17176v1"
tags: ["query:profile-1"]
score: 8.0
evidence: 通过推理模型进行安全的航天器轨迹优化
tldr: "针对未来航天器自主运行中难以理解高层任务意图且过度依赖专家建模的问题，本文提出一种意图对齐的自主引导框架。该框架通过行为序列和航路点约束作为中间抽象，将大模型的高层推理与安全轨迹优化相结合。实验证明，该方法在近距离操作场景中实现了超过90%的收敛率，且意图满足率比启发式方法高出1.5倍，为安全关键的航天器自主性提供了实用接口。"
selection_source: fresh_fetch
motivation: 现有的航天器轨迹优化高度依赖专家手工建模，缺乏对高层任务意图的理解和灵活的条件决策能力。
method: 提出一种分解式框架，利用基础模型预测行为计划，并将其转化为航路点约束，最后通过优化算法计算安全轨迹。
result: "实验显示该方法在近距离操作中实现了超过90%的收敛率，且在意图优先的性能指标上比启发式决策高出1.5倍。"
conclusion: 引入中间行为抽象层是连接基础模型推理能力与安全关键型航天器自主引导系统的有效且实用的途径。
---

## 摘要
未来的航天器操作需要具备在保障安全的同时解释高层任务意图的自主能力。然而，现有的轨迹优化仍严重依赖专家设计的公式，且不支持受意图约束的决策。本文提出了一种意图对齐的航天器制导框架，通过基于行为序列和航路点约束的显式中间抽象，将高层推理与安全轨迹优化相连接。该框架首先由基础模型预测意图对齐的行为计划，随后由航路点生成模型将其转换为航路点约束，最后通过优化计算出安全轨迹。这种分解方法在不牺牲安全性的前提下实现了可扩展的监督。在近距离操作场景下的数值实验表明，所提流程实现了超过 90% 的 SCP 收敛率，且与启发式决策相比，生成满足首要意图优先级性能标准的轨迹的成功率提高了 1.5 倍。这些结果支持将中间行为抽象作为基础模型推理与安全关键型航天器星载自主系统之间的实用接口。

## Abstract
Future spacecraft operations require autonomy that can interpret high-level mission intent while preserving safety. However, existing trajectory optimization still relies heavily on expert-crafted formulations and does not support intent-conditioned decision-making. This paper proposes an intent-aligned spacecraft guidance framework that links high-level reasoning and safe trajectory optimization through explicit intermediate abstractions, based on behavior sequences and waypoint constraints. A foundation model first predicts an intent-aligned behavior plan, a waypoint generation model then converts it into waypoint constraints, and the safe trajectory is computed via optimization. This decomposition enables scalable supervision without sacrificing safety. Numerical experiments in close-proximity operation scenarios demonstrate that the proposed pipeline achieves over 90\% SCP convergence and yields a $1.5\times$ higher rate of generating trajectories that satisfy the top intent-prioritized performance criteria than heuristic decision-making. These results support the use of intermediate behavior abstraction as a practical interface between foundation-model reasoning and safety-critical onboard spacecraft autonomy.

---

## 论文详细总结（自动生成）

这是一份关于论文《Intent-aligned Autonomous Spacecraft Guidance via Reasoning Models》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何让未来的自主航天器既能理解人类的高层任务意图（如“优先省油”或“优先观测”），又能确保在复杂的近距离操作中满足严格的动力学约束和碰撞规避安全性。
*   **研究背景**：现有的轨迹优化方法（如连续凸规划 SCP）虽然高效，但高度依赖专家手工设计的数学公式，缺乏对语义意图的理解。而基础模型（FM）虽具备推理能力，但直接生成的控制指令往往无法保证物理可行性和安全性。

### 2. 方法论
论文提出了一种**三层分级决策架构**，通过中间抽象层将语义推理与数值优化解耦：
*   **推理模型（Reasoning Model）**：基于大语言模型（Qwen2.5-7B-Instruct），接收任务背景、初始状态和意图优先级（如：燃料 > 时间 > 观测），输出包含推理逻辑的行为序列（Behavior Sequence）和总时长。
*   **航路点生成模型（Waypoint Generator）**：一个多层感知机（MLP），将抽象的行为序列转换为具体的几何航路点约束（位置和时间间隔）。
*   **轨迹优化模块（SCP Solver）**：接收航路点约束，通过连续凸规划（SCP）求解非凸优化问题，生成满足动力学、燃料最小化和被动安全性（Passive Safety）的最终轨迹。
*   **关键技术细节**：
    *   **分阶段引导（Staged Bootstrapping）**：先利用 SCP 结果训练航路点生成器；再利用冻结的生成器和 SCP 评估结果，通过 LLM 自动标注数据，对推理模型进行微调（LoRA）。
    *   **中间抽象**：引入“行为原语”（如漂移、进场、退场等）作为 LLM 与物理控制器之间的桥梁，增加了系统的可解释性和人工干预的可能性。

### 3. 实验设计
*   **实验场景**：航天器近距离操作（Proximity Operations），包括三种典型任务：环绕（Circumnavigation）、飞越（Flyby）和潜行（Ducking）。
*   **数据集**：生成了 50,000 组样本用于航路点模型训练，并抽取 500 组独立测试案例进行端到端评估。
*   **基准方法（Benchmark）**：
    *   **启发式决策**：基于规则随机采样行为序列和航路点。
    *   **消融对比**：对比了“启发式推理+启发式航路点”、“神经推理+启发式航路点”、“启发式推理+神经航路点”以及“全神经模型”四种组合。
*   **评价指标**：SCP 收敛成功率、奖励函数得分（燃料与观测质量）、意图对齐一致性（语义匹配度）。

### 4. 资源与算力
*   **模型规格**：使用了 **Qwen2.5-7B-Instruct** 作为基础推理模型。
*   **训练技术**：采用了 **4-bit 量化** 加载基础模型，并使用 **LoRA（低秩适配）** 进行参数高效微调。
*   **算力细节**：文中**未明确说明**具体的 GPU 型号、数量及训练总时长，但提到使用了参数高效的微调手段，暗示其对算力的需求在商用级 GPU 可接受范围内。

### 5. 实验数量与充分性
*   **实验规模**：进行了 500 组端到端测试，涵盖了多种意图组合和任务场景。
*   **充分性评价**：实验设计较为充分。通过对比不同模块（推理 vs. 航路点生成）的神经化与启发式组合，清晰地展示了各层级对最终性能的贡献。
*   **客观性**：使用了统计学指标（如双指标胜率、收敛率）进行量化分析，并利用 GPT-4o-mini 作为第三方评估器来检测语义一致性，减少了主观偏见。

### 6. 主要结论与发现
*   **高收敛性**：神经航路点生成模型将 SCP 的收敛成功率从启发式方法的 72% 提升至 **90% 以上**。
*   **意图对齐**：训练后的推理模型在生成符合高层意图的轨迹方面，比启发式决策表现出 **1.5 倍的性能提升**（双指标胜率从 12.4% 提升至 20.4%）。
*   **语义一致性**：推理模型生成的行为序列 100% 可行，且推理过程与任务意图高度匹配。

### 7. 优点与亮点
*   **架构解耦**：巧妙地将“慢速、模糊的语义推理”与“快速、精确的数值优化”结合，既保留了 LLM 的灵活性，又通过 SCP 强制保证了物理安全性。
*   **可解释性**：中间的行为序列和航路点为地面操作员提供了检查点，允许人工在最终轨迹生成前进行干预。
*   **自动化闭环**：通过自动标注流程实现了从底层优化结果到高层语义推理的反馈闭环。

### 8. 不足与局限
*   **场景局限性**：实验主要集中在近距离相对运动场景，尚未验证其在深空探测或更复杂轨道转移中的泛化能力。
*   **实时性挑战**：虽然 SCP 很快，但 LLM 推理（尤其是 7B 参数模型）在星载计算资源受限的环境下可能存在延迟问题。
*   **幻觉风险**：尽管有中间层约束，但 LLM 仍可能在极端罕见场景下产生错误的推理逻辑，需进一步研究其鲁棒性边界。

（完）
