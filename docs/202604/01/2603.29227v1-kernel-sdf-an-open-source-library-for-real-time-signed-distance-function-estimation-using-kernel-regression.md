---
title: "Kernel-SDF: An Open-Source Library for Real-Time Signed Distance Function Estimation using Kernel Regression"
authors: "Zhirui Dai, Tianxing Fan, Mani Amani, Jaemin Seo, Ki Myung Brian Lee, Hyondong Oh, Nikolay Atanasov"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29227v1"
tags: ["query:profile-1"]
score: 8.0
evidence: 用于机械臂碰撞检查和轨迹优化的实时有向距离函数(SDF)估计
tldr: Kernel-SDF 为机器人碰撞检测和轨迹优化提供高效的实时环境表示库。
selection_source: fresh_fetch
motivation: 用于机械臂碰撞检查和轨迹优化的实时有向距离函数(SDF)估计。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## Abstract
Accurate and efficient environment representation is crucial for robotic applications such as motion planning, manipulation, and navigation. Signed distance functions (SDFs) have emerged as a powerful representation for encoding distance to obstacle boundaries, enabling efficient collision-checking and trajectory optimization techniques. However, existing SDF reconstruction methods have limitations when it comes to large-scale uncertainty-aware SDF estimation from streaming sensor data. Voxel-based approaches are limited by fixed resolution and lack uncertainty quantification, neural network methods require significant training time, while Gaussian process (GP) methods struggle with scalability, sign estimation, and uncertainty calibration. In this letter, we develop an open-source library, Kernel-SDF, which uses kernel regression to learn SDF with calibrated uncertainty quantification in real-time. Our approach consists of a front-end that learns a continuous occupancy field via kernel regression, and a back-end that estimates accurate SDF via GP regression using samples from the front-end surface boundaries. Kernel-SDF provides accurate SDF, SDF gradient, SDF uncertainty, and mesh construction in real-time. Evaluation results show that Kernel-SDF achieves superior accuracy compared to existing methods, while maintaining real-time performance, making it suitable for various robotics applications requiring reliable uncertainty-aware geometric information.

---

## 论文详细总结（自动生成）

以下是对论文《Kernel-SDF: An Open-Source Library for Real-Time Signed Distance Function Estimation using Kernel Regression》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：在机器人导航、运动规划和操作任务中，准确且高效的环境表示至关重要。有向距离函数（SDF）因能提供到障碍物边界的距离和梯度信息而备受青睐。
*   **现有挑战**：
    *   **体素法（Voxel-based）**：受限于固定分辨率，且缺乏不确定性量化。
    *   **神经网络法（Neural-based）**：训练时间长，难以满足在线实时更新需求。
    *   **高斯过程法（GP-based）**：在大规模场景下扩展性差，符号估计（Sign estimation）不鲁棒，且预测方差与实际误差不一致。
*   **核心目标**：开发一个开源库 **Kernel-SDF**，利用核回归技术实现大规模环境下、具备校准不确定性感知能力的实时 SDF 估计。

### 2. 方法论
Kernel-SDF 采用前端-后端耦合的架构，结合了八叉树（Octree）空间划分：
*   **前端：表面估计（BHM）**：
    *   使用**贝叶斯希尔伯特地图（BHM）**学习连续占据场。
    *   通过线性回归估计权重，并利用 RBF 核函数将查询点映射到希尔伯特空间。
    *   **优化**：引入权重同步机制保证局部地图间的一致性；使用移动平均在线更新占据阈值 $\tau$ 以适应不同传感器特性。
    *   **输出**：通过 Marching Cubes 提取表面点，并基于一阶泰勒展开计算表面点的不确定性。
*   **后端：SDF 预测（GP）**：
    *   在每个八叉树节点内维护**高斯过程（GP）回归**。
    *   **Log-GP 扩展**：将 SDF 分解为符号（来自前端）和无向距离（UDF）。利用核函数的非线性变换（如 RBF 或 Matérn 3/2）近似热方程解，从而估计 UDF。
    *   **不确定性量化**：提出一种基于 **Softmin** 的融合策略，将前端表面点的不确定性直接传播到 SDF 预测值和梯度中，解决了传统 Log-GP 远场不确定性爆炸的问题。
*   **实时性优化**：
    *   **优先级队列更新**：根据查询频率和数据更新时间戳，动态调度 BHM 更新、表面提取和 GP 训练任务。

### 3. 实验设计
*   **数据集**：
    *   **Replica**：合成室内场景，用于评估低噪声下的重建质量。
    *   **Cow and Lady**：真实 RGB-D 场景，评估对传感器噪声的鲁棒性。
    *   **Newer College**：大规模室外 LiDAR 场景，评估系统的扩展性。
*   **对比方法（Baselines）**：
    *   体素类：**Voxblox**、**FIESTA**。
    *   神经网络类：**iSDF**。
    *   高斯过程类：**VDB-GPDF**。
*   **评估指标**：
    *   **网格质量**：F1 分数、召回率、精确度、Chamfer-L1 距离。
    *   **SDF 准确性**：平均绝对误差（MAE）、梯度 MAE。
    *   **效率**：每帧处理时间（FPT）、1k 点查询延迟。

### 4. 资源与算力
*   **硬件平台**：实验在一台配备 **Intel i9-14900K CPU** 和 **NVIDIA RTX 3090 GPU** 的系统上运行。
*   **实现细节**：该库使用 **C++** 编写，提供 Python 和 ROS1/ROS2 接口。虽然配备了 GPU，但核心算法设计注重 CPU 效率（如八叉树和稀疏核计算）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从单个房间到大型建筑（Newer College）的多种尺度。
*   **消融实验**：专门针对**传感器噪声**进行了消融研究，验证了 BHM 前端在噪声增加时的鲁棒性。
*   **应用案例**：展示了一个实际的机器人导航案例，利用估计的 SDF 和不确定性构建“安全气泡（Safe Bubble）”进行路径规划。
*   **评价**：实验设计全面，对比了不同技术路线的代表性方法，涵盖了精度、速度、不确定性一致性等多个维度，结果具有较高的客观性和说服力。

### 6. 主要结论
*   **精度领先**：在所有测试数据集上，Kernel-SDF 的网格重建精度和 SDF 距离/梯度误差均优于或等同于现有最先进方法。
*   **实时性能**：平均每帧更新时间约 **150ms**，查询 1000 个点仅需约 **3ms**，完全满足在线机器人应用。
*   **不确定性可靠**：估计的方差与实际 SDF 预测误差高度相关，能够为风险感知决策提供有效参考。

### 7. 优点
*   **开源与易用性**：提供完整的 C++/Python/ROS 支持，方便集成。
*   **理论创新**：通过 Softmin 融合解决了 GP 在 SDF 估计中方差校准失效的长久难题。
*   **鲁棒性**：BHM 前端能有效过滤传感器噪声，在真实复杂环境下表现稳定。
*   **灵活性**：支持多种核函数（RBF, Matérn），并能通过八叉树结构处理大规模环境。

### 8. 不足与局限
*   **计算开销**：虽然达到了实时性，但相比于极度优化的体素方法（如 FIESTA），其计算资源消耗（尤其是 CPU）仍然较高。
*   **超参数敏感性**：核函数的尺度参数（Length-scale）和八叉树分辨率对不同环境的适应性可能需要手动调优。
*   **动态环境限制**：虽然提到能适应动态环境，但实验主要集中在静态场景重建，对于高速运动物体的实时追踪能力尚待进一步验证。

（完）
