---
title: "Distributed Predictive Control Barrier Functions: Towards Scalable Safety Certification in Modular Multi-Agent Systems"
title_zh: 分布式预测控制屏障函数：迈向模块化多智能体系统中的可扩展安全认证
authors: "Jonas Ohnemus, Alexandre Didier, Ahmed Aboudonia, Andrea Carron, Melanie N. Zeilinger"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29560v1"
tags: ["query:q2"]
score: 8.0
evidence: 基于预测控制屏障函数的优化安全层
tldr: 针对分布式多智能体系统在动态拓扑下的安全性挑战，本文提出了一种结构化控制屏障函数（s-CBF）框架，并结合分布式预测控制屏障函数（D-PCBF）构建安全层。该方法利用模型预测确保系统在面对扰动或网络变化时具备可恢复的安全性，支持智能体即插即用的动态加入与退出。通过赛车编队实验验证了其在保证安全性的同时兼顾了系统的可扩展性与灵活性。
selection_source: fresh_fetch
motivation: 现有的分布式多智能体控制在面对不可预见扰动和网络拓扑动态变化时，缺乏正式的安全保障和鲁棒性。
method: 提出结构化控制屏障函数（s-CBF）并集成到分布式预测控制框架中，利用模型预测确保系统在任何时刻都具备可恢复的安全性。
result: 成功开发出一种支持智能体即插即用的安全协议，并通过小型赛车编队的仿真和实时实验验证了其有效性。
conclusion: 该方法为模块化多智能体系统提供了一种兼具灵活性、可扩展性与正式安全认证的分布式预测控制方案。
---

## 摘要
我们考虑具有分布式控制架构且网络拓扑可能发生变化的安全关键型多智能体系统。虽然基于学习的分布式控制能够实现可扩展性和高性能，但在面对不可预见的扰动和不安全的网络拓扑变化时，缺乏形式化的安全保证可能会导致系统故障。为了应对这一挑战，我们引入了结构化控制屏障函数（s-CBFs）作为一种多智能体安全框架。s-CBFs 被增强为分布式预测控制屏障函数（D-PCBF），这是一种基于优化的预测性安全层，利用模型预测来确保在任何时候都具有可恢复的安全性。所提出的方法实现了一种宽松且形式化的即插即用协议，允许智能体加入或离开网络，同时确保在网络拓扑变化需要暂时性的不安全行为时能够恢复安全。我们通过小型赛车车队的仿真和实时实验验证了该方案。

## Abstract
We consider safety-critical multi-agent systems with distributed control architectures and potentially varying network topologies. While learning-based distributed control enables scalability and high performance, a lack of formal safety guarantees in the face of unforeseen disturbances and unsafe network topology changes may lead to system failure. To address this challenge, we introduce structured control barrier functions (s-CBFs) as a multi-agent safety framework. The s-CBFs are augmented to a distributed predictive control barrier function (D-PCBF), a predictive, optimization-based safety layer that uses model predictions to guarantee recoverable safety at all times. The proposed approach enables a permissive yet formal plug-and-play protocol, allowing agents to join or leave the network while ensuring safety recovery if a change in network topology requires temporarily unsafe behavior. We validate the formulation through simulations and real-time experiments of a miniature race-car platoon.

---

## 论文详细总结（自动生成）

以下是对论文《Distributed Predictive Control Barrier Functions: Towards Scalable Safety Certification in Modular Multi-Agent Systems》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在具有动态拓扑（智能体随时加入或离开）的大规模多智能体系统（MAS）中，既保证高性能（如基于学习的控制），又提供形式化的安全保障？
*   **研究动机**：
    *   **集中式控制的局限性**：随着智能体数量增加，计算量呈指数级增长，难以扩展。
    *   **现有安全滤镜的缺陷**：传统的预测安全滤镜（PSF）仅保证安全集的正不变性，一旦受到强扰动导致状态超出安全集，优化问题就会变得不可行（Infeasible）。
    *   **拓扑动态性**：现有的分布式安全控制在智能体增减时，往往需要系统停下来重新配置，缺乏“即插即用”的灵活性。
    *   **可恢复安全性（Recoverable Safety）的需求**：需要一种机制，即使系统暂时被迫进入不安全状态，也能保证其最终收敛回安全区域。

### 2. 核心方法论
论文提出了一套名为 **D-PCBF** 的分布式预测控制框架，核心组成部分包括：
*   **结构化控制屏障函数（s-CBFs）**：
    *   这是对传统 CBF 的改进。它允许单个智能体的局部安全函数值暂时增加（即局部不安全），只要整个网络的全局安全函数总和保持下降。
    *   通过引入“松弛函数” $\beta_\ell$ 来协调邻居间的安全约束，实现了比传统分布式约束更低的保守性。
*   **分布式预测控制屏障函数（D-PCBF）优化**：
    *   将 s-CBF 作为终端约束嵌入到一个分布式优化问题中。
    *   利用预测模型在时域内规划路径，通过引入松弛变量 $\xi$ 来处理约束违反，确保优化问题始终可行。
    *   证明了该优化问题的最优值函数本身就是一个全局 CBF，从而保证了系统的正不变性和收敛性。
*   **即插即用（PnP）协议**：
    *   设计了基于“违反证书”和“恢复证书”的准入机制。
    *   当新智能体请求加入时，系统通过一次分布式计算检查：1) 临时违反是否在容许范围内；2) 系统是否能收敛回安全集。若通过则允许加入。

### 3. 实验设计
*   **实验场景**：车辆编队控制（Vehicle Platooning），关注纵向动力学。
*   **实验阶段**：
    1.  **数值仿真**：验证算法的可扩展性。智能体数量从 5 个扩展到 40 个。
    2.  **硬件实验**：使用 Chronos 迷你赛车平台进行实时验证。
*   **对比基准（Benchmark）**：
    *   对比了**集中式求解器**与**分布式求解器**的计算耗时。
    *   对比了在极端输入（如恶意加速指令）下，D-PCBF 与普通控制器（如 PID）的表现。
    *   展示了在传统 PSF 会导致不可行解的高速切入场景下，D-PCBF 的恢复能力。

### 4. 资源与算力
*   **软件工具**：使用 MOSEK 作为集中式求解器，自定义基于 ADMM（交替方向乘子法）的分布式求解器。硬件实验中使用 HPIPM 求解 QCQP 问题。
*   **算力说明**：
    *   文中未明确提及具体的 GPU 型号，但指出分布式算法是在顺序 CPU 上模拟并行运行时间。
    *   硬件实验中，控制回路运行频率为 **100 Hz**，显示了极高的实时性。
    *   分布式求解在平均 15 次 ADMM 迭代内收敛。

### 5. 实验数量与充分性
*   **实验规模**：
    *   仿真部分涵盖了不同规模（5-40个智能体）的编队，证明了计算时间随节点增加保持准常数（理想并行状态下），具有良好的可扩展性。
    *   硬件实验展示了多次“加入-离开-再加入”的动态过程，包括高速接近导致的暂时性安全违反。
*   **充分性评价**：实验设计较为充分。不仅有理论证明，还通过仿真验证了扩展性，并通过物理实验验证了鲁棒性和实时性。实验涵盖了从稳态运行到极端拓扑变化的多种工况，结果客观且具有说服力。

### 6. 主要结论与发现
*   **可恢复性验证**：D-PCBF 成功处理了状态超出安全集的情况，引导系统重新回到安全间距。
*   **可扩展性**：分布式架构的计算延迟不随智能体数量显著增加，适合大规模集群。
*   **即插即用可行性**：证明了在不中断系统运行的情况下，可以通过局部信息交换实现智能体的动态增减，且依然维持全局安全边界。
*   **安全性与性能平衡**：该框架能有效过滤掉学习型控制器产生的危险指令，同时在必要时允许最小限度的约束违反以维持可行性。

### 7. 优点
*   **理论完备**：提供了从局部 s-CBF 到全局安全性的严谨数学证明。
*   **低保守性**：s-CBF 的结构允许智能体之间“互助”，比强制每个点都时刻安全的算法更灵活。
*   **实用性强**：即插即用协议解决了模块化机器人系统中的实际痛点。
*   **硬件验证**：在真实的迷你赛车上实现了 100Hz 的闭环控制，证明了算法的工程落地能力。

### 8. 不足与局限
*   **线性系统依赖**：虽然理论适用于非线性，但文中 s-CBF 的具体合成方法（基于 LMI）主要针对线性时不变系统，非线性系统的自动化合成仍具挑战。
*   **模型依赖**：预测控制依赖于动力学模型的准确性，虽然文中提到具有一定的鲁棒性，但在极端模型失配下的表现未深入探讨。
*   **约束类型限制**：目前主要处理解耦的状态约束，对于智能体之间复杂的耦合状态约束（如复杂的避障几何）扩展性有待加强。
*   **通信假设**：假设通信图是连通且双向的，未讨论通信延迟或丢包对安全证书的影响。

（完）
