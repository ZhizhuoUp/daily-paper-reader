---
title: "CMP: Robust Whole-Body Tracking for Loco-Manipulation via Competence Manifold Projection"
title_zh: CMP：通过能力流形投影实现移动操作的鲁棒全身追踪
authors: "Ziyang Cheng, Haoyu Wei, Hang Yin, Xiuwei Xu, Bingyao Yu, Jie Zhou, Jiwen Lu"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.07457v1"
tags: ["query:q6"]
score: 8.0
evidence: 具有安全约束的移动操作全身跟踪
tldr: 针对腿式移动操作机器人全身控制在面对分布外（OOD）输入（如传感器噪声或不可行指令）时鲁棒性不足的问题，本文提出了能力流形投影（CMP）框架。该方法通过逐帧安全方案将长时安全约束转化为单步流形包含问题，利用下界安全估计器识别未知意图，并引入同构潜空间（ILS）实现高效的OOD防御。实验表明，CMP在极端OOD场景下将生存率提升了10倍，同时保持了高性能的跟踪能力，展现出“尽力而为”的泛化行为。
selection_source: fresh_fetch
motivation: 现有的全身控制策略在面对传感器噪声或不可行用户指令等分布外输入时极易崩溃，缺乏鲁棒性。
method: 提出CMP框架，结合逐帧安全方案、下界安全估计器及同构潜空间，将复杂安全约束简化为高效的流形投影。
result: "在典型OOD场景下，CMP将机器人生存率提升了10倍，且跟踪性能下降不到10%，并能以“尽力而为”的方式完成任务。"
conclusion: CMP通过能力流形投影有效增强了腿式移动操作机器人的鲁棒性，为处理不可预见的指令提供了可靠的防御机制。
---

## 摘要
虽然足式移动操作机器人的解耦控制方案已表现出鲁棒性，但用于追踪全局末端执行器位姿的整体全身控制策略在面对由传感器噪声或不可行用户指令引起的分布外（OOD）输入时仍然十分脆弱。为了在不牺牲任务性能和连续性的情况下提高针对这些扰动的鲁棒性，我们提出了能力流形投影（CMP）。具体而言，我们利用一种逐帧安全方案，将无限时界的安全约束转换为计算高效的单步流形包含问题。为了实例化该能力流形，我们采用了一个下界安全估计器，用以区分未掌握的意图与训练分布。随后，我们引入了一个同构潜空间（ILS），将流形几何与安全概率对齐，从而实现对任意 OOD 意图的高效 O(1) 无缝防御。实验表明，在基准方法遭受灾难性失败的典型 OOD 场景中，CMP 实现了高达 10 倍的生存率提升，而追踪性能下降不足 10%。值得注意的是，该系统表现出涌现的“尽力而为”泛化行为，通过遵循能力边界逐步完成 OOD 目标。结果视频见：https://shepherd1226.github.io/CMP。

## Abstract
While decoupled control schemes for legged mobile manipulators have shown robustness, learning holistic whole-body control policies for tracking global end-effector poses remains fragile against Out-of-Distribution (OOD) inputs induced by sensor noise or infeasible user commands. To improve robustness against these perturbations without sacrificing task performance and continuity, we propose Competence Manifold Projection (CMP). Specifically, we utilize a Frame-Wise Safety Scheme that transforms the infinite-horizon safety constraint into a computationally efficient single-step manifold inclusion. To instantiate this competence manifold, we employ a Lower-Bounded Safety Estimator that distinguishes unmastered intentions from the training distribution. We then introduce an Isomorphic Latent Space (ILS) that aligns manifold geometry with safety probability, enabling efficient O(1) seamless defense against arbitrary OOD intents. Experiments demonstrate that CMP achieves up to a 10-fold survival rate improvement in typical OOD scenarios where baselines suffer catastrophic failure, incurring under 10% tracking degradation. Notably, the system exhibits emergent ``best-effort'' generalization behaviors to progressively accomplish OOD goals by adhering to the competence boundaries. Result videos are available at: https://shepherd1226.github.io/CMP.

---

## 论文详细总结（自动生成）

### 论文总结：CMP - 通过能力流形投影实现移动操作的鲁棒全身追踪

#### 1. 核心问题与整体含义（研究动机和背景）
足式移动操作机器人（如带机械臂的四足机器人）在执行复杂任务时，通常采用全身控制（WBC）策略来协调底盘移动与机械臂操作。然而，现有的端到端学习策略在面对**分布外（OOD）输入**时表现得极其脆弱。这些 OOD 输入可能源于视觉惯性里程计（VIO）的漂移、传感器噪声或用户给出的物理不可行指令（如让机器人向后方极远处抓取）。当输入超出策略的“能力范围”时，机器人往往会产生不可预测的危险动作甚至崩溃。论文的研究动机在于：如何在不牺牲全身协调性能的前提下，为机器人提供一种高效、实时的安全防御机制，使其在面对非法指令时能“尽力而为”而非直接崩溃。

#### 2. 方法论：核心思想与技术细节
论文提出了 **CMP (Competence Manifold Projection)** 框架，其核心思想是将复杂的安全约束简化为潜空间中的几何投影。
*   **逐帧安全方案 (Frame-Wise Safety Scheme)：** 利用马尔可夫决策过程（MDP）属性，将难以计算的“无限时界安全约束”简化为单步的“流形包含条件”。即：只要当前的潜意图 $z$ 落在“能力流形” $C$ 内，就存在一条能保证未来持续安全的路径。
*   **下界安全估计器 (Lower-Bounded Safety Estimator)：** 训练一个估计器 $\omega$ 来预测给定状态和意图下的生存概率。通过时序差分学习（TD($\lambda$)）构建安全目标的下界，从而保守地识别出机器人真正掌握的意图边界。
*   **同构潜空间 (Isomorphic Latent Space, ILS)：** 这是 CMP 的关键创新。通过动态 KL 散度正则化，将安全概率与潜空间的几何半径进行映射。在 ILS 中，安全概率越高，意图越靠近原点；安全概率越低，意图半径越大。
*   **O(1) 投影流程：** 在推理时，系统只需检查潜向量 $z$ 的模长。若超出预设的安全半径 $R_{safe}$，则直接将其截断（投影）到边界上。这种操作是常数复杂度的，能够无缝集成到高频控制循环中。

#### 3. 实验设计
*   **实验场景：** 
    *   **仿真：** 使用 Isaac Gym，模拟 Unitree Go2 机器人搭载 Hexfellow Saber 机械臂。
    *   **实物：** 真实的 Go2 机器人与机械臂组合，测试 VIO 漂移和极端指令下的表现。
*   **数据集：** 
    *   **ID (In-Distribution)：** 正常的前方操作任务。
    *   **OOD-Geometry：** 目标位于机器人后方（训练集未覆盖）。
    *   **OOD-Sensor：** 注入高强度传感器噪声模拟 VIO 失效。
*   **Benchmark 与对比方法：** 
    *   **基准：** UMI-on-Legs（目前的 SOTA 全身控制框架）。
    *   **对比安全方法：** Latent Shielding（潜空间屏蔽）、Neural CBF（神经控制屏障函数）。

#### 4. 资源与算力
*   **硬件：** 使用了单块 **NVIDIA RTX A6000 GPU**。
*   **训练时长：** 整个训练过程（包括策略学习和安全估计器训练）大约耗时 **2.5 小时**（进行 2000 次迭代）。
*   **推理延迟：** CMP 的投影操作极快，实测延迟约为 **2.99 ms**，与未加保护的基准方法几乎持平，远优于 Neural CBF（5.36 ms）。

#### 5. 实验数量与充分性
*   **实验规模：** 仿真中使用了 **7000 条** 轨迹进行大规模评估；实物实验涵盖了 **15 个** 典型任务（包括推、投掷、跳跃等），每个任务进行多次重复。
*   **消融实验：** 详细对比了 CVAE、SCVAE 与 CMP 的性能，验证了 ILS 几何对齐的必要性。
*   **客观性：** 实验不仅关注生存率（SR），还量化了位置误差（$e_p$）和角度误差（$e_r$），并讨论了安全半径 $R_{safe}$ 在“保守”与“激进”之间的权衡，评估较为全面、客观。

#### 6. 主要结论与发现
*   **生存率大幅提升：** 在极端 OOD 场景下，CMP 将机器人的生存率提升了 **10 倍**（从基准的 4.7% 提升至 46.9% 以上）。
*   **性能损失极小：** 在处理安全防御时，对正常分布内任务的跟踪精度下降控制在 **10% 以内**。
*   **“尽力而为”行为：** 当给定不可行指令（如向后推）时，CMP 不会死机，而是让机器人执行最接近目标的动作（如原地转身后再推），展现了极强的泛化能力。
*   **防御传感器失效：** CMP 能有效切断“传感器误差 -> 激进动作 -> 更大传感器误差”的恶性正反馈循环。

#### 7. 优点
*   **高效性：** 实现了 O(1) 的安全投影，非常适合对实时性要求极高的足式机器人控制（>50Hz）。
*   **平滑性：** 相比于传统的“紧急停止”或“硬屏蔽”，CMP 提供了一种连续的性能降级方案，保证了任务的连续性。
*   **通用性：** 这种将安全概率映射到潜空间几何的思想，可以推广到其他高维动作空间的机器人任务中。

#### 8. 不足与局限
*   **估计器误差：** 安全估计器 $\omega$ 的准确性受限于神经网络的拟合能力，若估计器失效，系统仍可能崩溃。
*   **统计性边界：** ILS 的球形边界是基于统计分布的，对于某些极特殊的边缘案例，简单的模长截断可能无法完全消除风险。
*   **动力学偏移：** 目前 CMP 主要处理指令分布的偏移（Command OOD），对于环境动力学改变（如机器人突然负重或地面极滑）的鲁棒性尚未在本文中深入探讨。

（完）
