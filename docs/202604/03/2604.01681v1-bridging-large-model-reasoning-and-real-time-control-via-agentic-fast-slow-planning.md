---
title: Bridging Large-Model Reasoning and Real-Time Control via Agentic Fast-Slow Planning
title_zh: 通过智能体快慢规划连接大模型推理与实时控制
authors: "Jiayi Chen, Shuai Wang, Guangxu Zhu, Chengzhong Xu"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01681v1"
tags: ["query:q5"]
score: 8.0
evidence: 通过分层规划连接推理与实时控制
tldr: "本研究针对大模型推理与实时控制结合中的延迟和可靠性挑战，提出了Agentic Fast-Slow Planning框架。该框架通过感知-决策和决策-轨迹两个桥梁，将云端大模型的语义推理与车载端的实时控制解耦。利用VLM提取拓扑结构并由LLM生成指令，再通过语义引导的A*算法和智能细化模块转化为可执行轨迹，最后由MPC跟踪。实验证明该方法显著提升了自动驾驶的鲁棒性和效率。"
selection_source: fresh_fetch
motivation: 现有大模型控制方法存在轨迹生成脆弱、验证困难及实时性差等问题，难以在复杂动态环境中实现可靠控制。
method: "提出一种分层框架，通过VLM/LLM生成语义指令，并结合语义引导的A*搜索与智能参数优化模块，将高层决策转化为底层MPC可执行的轨迹。"
result: "在CARLA仿真中，该方法相比纯MPC和基准算法，横向偏差降低了45%，任务完成时间缩短了12%以上。"
conclusion: 该框架成功实现了大模型语义推理与实时控制的有效解耦与融合，为复杂场景下的自动驾驶提供了更具鲁棒性的解决方案。
---

## 摘要
大型基础模型为自主系统提供了强大的推理能力，但将语义意图映射到可靠的实时控制仍具挑战。现有方法要么 (i) 让大语言模型 (LLM) 直接生成轨迹——这种方式脆弱、难以验证且易产生延迟；要么 (ii) 在线调整模型预测控制 (MPC) 的目标——这混合了慢速深思熟虑与快速控制，模糊了接口界限。我们提出了智能体快慢规划（Agentic Fast-Slow Planning），这是一个在自然时间尺度上解耦感知、推理、规划和控制的分层框架。该框架包含两个桥梁：Perception2Decision 使用车载视觉语言模型 (VLM) 检测器将场景压缩为以自我为中心的拓扑结构，然后通过云端 LLM 决策器将其映射为符号化驾驶指令，在保持可解释性的同时减少了带宽和延迟；Decision2Trajectory 将指令转换为可执行路径，其中语义引导的 A* 算法将源自语言的软成本嵌入经典搜索中，使解偏向可行轨迹，而智能体细化模块则利用反馈和记忆来调整规划器的超参数。最后，MPC 实时跟踪轨迹，并针对困难情况提供可选的云端引导参考。在 CARLA 中的实验表明，与纯 MPC 和 A* 引导的 MPC 基准相比，智能体快慢规划提高了扰动下的鲁棒性，横向偏差减少了高达 45%，完成时间缩短了 12% 以上。代码可在 https://github.com/cjychenjiayi/icra2026_AFSP 获取。

## Abstract
Large foundation models enable powerful reasoning for autonomous systems, but mapping semantic intent to reliable real-time control remains challenging. Existing approaches either (i) let Large Language Models (LLMs) generate trajectories directly - brittle, hard to verify, and latency-prone - or (ii) adjust Model Predictive Control (MPC) objectives online - mixing slow deliberation with fast control and blurring interfaces. We propose Agentic Fast-Slow Planning, a hierarchical framework that decouples perception, reasoning, planning, and control across natural timescales. The framework contains two bridges. Perception2Decision compresses scenes into ego-centric topologies using an on-vehicle Vision-Language Model (VLM) detector, then maps them to symbolic driving directives in the cloud with an LLM decision maker - reducing bandwidth and delay while preserving interpretability. Decision2Trajectory converts directives into executable paths: Semantic-Guided A* embeds language-derived soft costs into classical search to bias solutions toward feasible trajectories, while an Agentic Refinement Module adapts planner hyperparameters using feedback and memory. Finally, MPC tracks the trajectories in real time, with optional cloud-guided references for difficult cases. Experiments in CARLA show that Agentic Fast-Slow Planning improves robustness under perturbations, reducing lateral deviation by up to 45% and completion time by over 12% compared to pure MPC and an A*-guided MPC baseline. Code is available at https://github.com/cjychenjiayi/icra2026_AFSP.

---

## 论文详细总结（自动生成）

这是一份关于论文《Bridging Large-Model Reasoning and Real-Time Control via Agentic Fast-Slow Planning》的深度结构化总结：

### 1. 核心问题与研究背景
*   **核心问题**：如何将大语言模型（LLM）强大的语义推理能力转化为可靠、实时的自动驾驶控制指令。
*   **研究动机**：
    *   **现有方法局限**：直接让 LLM 生成轨迹过于脆弱且难以验证；而在线调整 MPC 参数的方法则模糊了“慢速推理”与“快速控制”的界限，导致接口混乱。
    *   **设计原则**：受人类认知“双系统理论”启发，提出**尺度感知解耦（Scale-aware Decoupling）**，将感知、推理、规划和控制按不同的时间尺度分离，确保系统既有高层智慧又能实时响应。

### 2. 方法论：Agentic Fast-Slow Planning (AFSP)
该框架通过两个关键“桥梁”连接云端推理与车载控制：
*   **Perception2Decision（感知到决策）**：
    *   **车载端（快）**：使用轻量化 VLM（InternVL-2B）作为拓扑检测器，将原始图像压缩为以自我为中心的**拓扑图（Topology Graph）**，仅保留语义和空间几何信息，极大降低了上传带宽。
    *   **云端（慢）**：LLM 决策器根据拓扑图，通过思维链（CoT）生成符号化的驾驶指令（如 LEFT, KEEP）和驾驶风格。
*   **Decision2Trajectory（决策到轨迹）**：
    *   **语义引导 A* 算法**：将 LLM 的符号指令转化为“软成本（Soft Costs）”嵌入经典 A* 搜索。相比硬约束，这种方式允许在指令与几何可行性冲突时进行恢复，保证了路径的鲁棒性。
    *   **智能体细化模块（Agentic Refinement）**：利用 LLM 模拟人类调参过程，根据规划反馈和记忆库自动优化 A* 的超参数（如各项成本权重），无需人工干预。
*   **实时控制**：使用 **云端引导 MPC** 跟踪生成的参考轨迹，在常规情况下使用本地参考，在复杂场景下切换至云端引导路径。

### 3. 实验设计
*   **实验环境**：使用 CARLA 模拟器（Town2–7 和 Town10HD）。
*   **数据集**：构建了一个包含 49 类物体、约 20 万张图像的训练集，以及 1000 张测试图像。
*   **对比基准（Benchmarks）**：
    1.  **纯 MPC**：仅依赖局部避障。
    2.  **A*-MPC**：结合经典 A* 规划与 MPC 控制。
*   **测试场景**：设置了包含 7 个障碍物（车辆与静态物体）的复杂场景，并引入了地图偏移（1.5m-2m）和半径膨胀等扰动来测试鲁棒性。

### 4. 资源与算力
*   **硬件**：使用了 **4 台 NVIDIA A100 GPU**。
*   **训练细节**：VLM 微调进行了 12 个 Epoch，学习率为 $8 \times 10^{-5}$。
*   **模型**：感知端基于 InternVL-2B，决策端使用了 DeepSeek-V3 和 Qwen-VL-Chat 的 API。

### 5. 实验数量与充分性
*   **实验维度**：
    *   **感知消融实验**：对比了三种 VLM 微调策略（仅视觉、全参数、两阶段微调）。
    *   **决策一致性实验**：对比了 VLM 与 LLM 在 48 个决策场景下的匹配得分与延迟。
    *   **鲁棒性实验**：在三种不同程度的地图扰动（Shift 1/2/3）下测试路径生成。
    *   **闭环仿真**：每个 CARLA 场景重复 10 次实验以减少随机性。
*   **评价**：实验设计较为全面，涵盖了从底层感知精度到高层决策逻辑，再到最终闭环控制性能的完整链路，客观地展示了系统在扰动下的优势。

### 6. 主要结论与发现
*   **性能提升**：在 CARLA 仿真中，AFSP 相比纯 MPC，**横向偏差降低了高达 45%**，任务**完成时间缩短了 12% 以上**。
*   **鲁棒性**：语义引导的 A* 能够纠正地图偏移带来的误差，保持意图一致性。
*   **效率**：通过拓扑图压缩，LLM 决策的端到端延迟显著低于直接处理图像的 VLM（4.13s vs 10.24s），证明了云端推理的可行性。

### 7. 优点与亮点
*   **解耦设计**：成功实现了“慢速语义推理”与“快速几何规划”的解耦，解决了 LLM 直接控制的实时性难题。
*   **软成本机制**：将语言指令转化为 A* 的软成本，既保留了经典算法的可解释性与可行性，又引入了 LLM 的灵活性。
*   **自动化调参**：Agentic Refinement 模块通过闭环反馈自动优化规划器参数，体现了“智能体”在系统自适应方面的潜力。

### 8. 不足与局限
*   **云端依赖**：系统高度依赖云端 LLM 的响应，若发生网络中断或高延迟，虽然有本地 MPC 兜底，但高层智能将受限。
*   **仿真与现实差距**：实验完全在 CARLA 模拟器中进行，尚未在真实道路或复杂天气/光照条件下验证感知模型的泛化能力。
*   **计算开销**：虽然压缩了带宽，但云端调用 LLM API 仍存在成本和排队延迟问题，在大规模部署时可能面临挑战。

（完）
