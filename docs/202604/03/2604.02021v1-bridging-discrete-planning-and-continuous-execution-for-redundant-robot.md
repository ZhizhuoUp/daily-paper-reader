---
title: Bridging Discrete Planning and Continuous Execution for Redundant Robot
title_zh: 衔接冗余机器人的离散规划与连续执行
authors: "Teng Yan, Yue Yu, Yihan Liu, Bingzhuo Zhong"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.02021v1"
tags: ["query:profile-1"]
score: 9.0
evidence: 冗余机械臂的路径规划与连续执行
tldr: 针对冗余机械臂在体素网格强化学习规划中存在的执行抖动和奇异点不稳定问题，本文提出了一种连接离散规划与连续执行的桥接框架。该框架在规划端引入步长归一化的26邻域动作和几何平局打破机制，在执行端采用任务优先级阻尼最小二乘逆运动学，有效平滑了路径并抑制了关节突变。实验表明，该方法显著提升了复杂环境下的规划成功率，并大幅降低了关节加速度。
selection_source: fresh_fetch
motivation: 解决冗余机械臂在执行基于体素网格的离散规划路径时，容易出现的步长抖动、关节突变及奇异位姿不稳定等问题。
method: 提出一种桥接框架，在规划层优化动作空间与搜索机制，在执行层利用任务优先级阻尼最小二乘法处理多目标逆运动学。
result: 在稠密障碍物环境下将规划成功率从0.58提升至1.00，缩短了路径长度，并将峰值关节加速度降低了一个数量级以上。
conclusion: 该框架在不改变原始离散规划器的前提下，显著增强了冗余机械臂在复杂环境中的执行平稳性和规划效率。
---

## 摘要
由于其简单性和可重复性，体素网格强化学习被广泛应用于冗余机械臂的路径规划。然而，在七自由度机械臂上通过逐点数值逆运动学直接执行，往往会导致步长抖动、关节突变以及在奇异构型附近的不稳定性。本研究提出了一种衔接离散规划与连续执行的框架，且无需修改离散规划器本身。在规划端，引入了步长归一化的26邻域笛卡尔动作和几何平局决胜机制，以抑制不必要的转向并消除步长振荡。在执行端，实现了一个任务优先级阻尼最小二乘（TP-DLS）逆运动学层。该层将末端执行器位置视为主要任务，而将姿态和关节中心化作为投影到零空间的次要任务处理，并结合了置信域裁剪和关节速度约束。在随机稀疏、中等和密集环境下的七自由度机械臂实验中，该衔接框架将密集场景下的规划成功率从约0.58提高到1.00，将代表性路径长度从约1.53米缩短至1.10米；在保持末端执行器误差低于1毫米的同时，将峰值关节加速度降低了一个数量级以上，显著提升了冗余机械臂上基于体素强化学习路径的连续执行质量。

## Abstract
Voxel-grid reinforcement learning is widely adopted for path planning in redundant manipulators due to its simplicity and reproducibility. However, direct execution through point-wise numerical inverse kinematics on 7-DoF arms often yields step-size jitter, abrupt joint transitions, and instability near singular configurations. This work proposes a bridging framework between discrete planning and continuous execution without modifying the discrete planner itself. On the planning side, step-normalized 26-neighbor Cartesian actions and a geometric tie-breaking mechanism are introduced to suppress unnecessary turns and eliminate step-size oscillations. On the execution side, a task-priority damped least-squares (TP-DLS) inverse kinematics layer is implemented. This layer treats end-effector position as a primary task, while posture and joint centering are handled as subordinate tasks projected into the null space, combined with trust-region clipping and joint velocity constraints. On a 7-DoF manipulator in random sparse, medium, and dense environments, this bridge raises planning success in dense scenes from about 0.58 to 1.00, shortens representative path length from roughly 1.53 m to 1.10 m, and while keeping end-effector error below 1 mm, reduces peak joint accelerations by over an order of magnitude, substantially improving the continuous execution quality of voxel-based RL paths on redundant manipulators.

---

## 论文详细总结（自动生成）

这是一份关于论文《Bridging Discrete Planning and Continuous Execution for Redundant Robot Manipulators》的结构化技术总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：在冗余机械臂（如7-DoF臂）的路径规划中，基于体素网格（Voxel-grid）的强化学习（RL）因其简单和可复用性被广泛采用。然而，离散规划输出的笛卡尔路径与连续执行层之间存在严重失配。
*   **核心问题**：直接使用数值逆运动学（IK）执行离散路径会导致：
    1.  **步长抖动**：离散网格动作长度不一导致末端执行器速度不稳。
    2.  **关节突变**：在奇异点或关节极限附近，关节构型可能发生剧烈跳变。
    3.  **路径质量差**：RL生成的路径常包含“锯齿状”振荡和不必要的转向。
*   **整体含义**：本文旨在不修改原始RL规划器算法的前提下，通过一个“桥接框架”优化规划几何和执行物理，使离散路径转化为平滑、动态可行的连续运动。

### 2. 论文提出的方法论
该框架分为两个紧密耦合的层：

*   **几何层（规划端优化）**：
    *   **步长归一化 26 邻域**：将标准的 26 邻域动作（轴向、面角、体角移动）统一归一化为固定的物理步长 $\delta$，消除因对角线移动较长导致的步长不均。
    *   **几何平局打破机制（Geometric Tie-breaking）**：当多个动作的 Q 值相近时，引入几何评分函数 $J_{geo}$，优先选择转向角小且局部避障间隙（Clearance）大的动作，从而抑制路径振荡。
    *   **样条平滑与微步控制**：使用三次样条拟合离散点，并进行等弧长重采样，确保输入执行层的参考路径连续可微。

*   **物理层（执行端优化）**：
    *   **任务优先级阻尼最小二乘（TP-DLS）**：
        *   **主任务**：末端执行器位置跟踪（采用 DLS 抑制奇异点附近的不稳定性）。
        *   **次要任务**：将“关节中心化”（保持关节远离极限）投影到主任务的零空间（Null Space）中执行。
    *   **置信域裁剪**：对关节增量进行全局范数裁剪和单关节增量限制，严格控制关节速度和加速度。

### 3. 实验设计
*   **实验场景**：在 MATLAB 环境下模拟 7-DoF 冗余机械臂，设置了三种障碍物密度的随机环境：**稀疏（0-30个）**、**中等（31-99个）**、**稠密（100+个）**。
*   **Benchmark**：构建了包含 3000 个随机任务（每种密度 1000 个）的测试集。
*   **对比方法**：
    *   **规划器对比**：Original（6邻域）、NoNorm（未归一化26邻域）、Improved（本文方法）。
    *   **执行器对比**：Num-IK（标准数值 IK 工具箱）、TP-DLS（本文方法）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件配置（如 GPU 型号或 CPU 核心数）。
*   **软件环境**：实验完全在 MATLAB 仿真平台上进行。
*   **耗时提到**：TP-DLS 的计算时间（Time）和回溯次数（Backtracks）显著高于标准 IK，反映了其通过增加计算量换取运动质量的权衡。

### 5. 实验数量与充分性
*   **实验规模**：总计 3000 组随机任务，样本量较大，足以覆盖各种极端情况。
*   **充分性**：实验涵盖了从规划成功率、路径长度到动态指标（关节加速度、速度、增量 95 分位数）的全方位评估。
*   **客观性**：通过固定相同的 RL 训练参数和相同的参考路径来对比执行器，确保了对比的公平性（控制变量法）。

### 6. 论文的主要结论与发现
*   **规划性能提升**：在稠密环境下，改进后的规划器将成功率从 **0.58 提升至 1.00**，路径长度显著缩短（约 28%）。
*   **执行质量飞跃**：
    *   **平滑度**：TP-DLS 将峰值关节加速度降低了**一个数量级以上**。
    *   **稳定性**：关节增量的 95 分位数（DQ 95）从 $10^0$ 数量级压缩至 $10^{-2}$ 数量级，消除了关节跳变。
    *   **精度**：在大幅提升平滑性的同时，末端执行器位置误差始终控制在 **1mm 以内**。

### 7. 优点（亮点）
*   **即插即用**：作为“桥接层”，它不需要重新训练 RL 模型或修改网络结构，具有很强的通用性。
*   **解耦设计**：成功将离散路径搜索的逻辑与连续空间下的冗余消解问题解耦。
*   **鲁棒性**：通过零空间投影和 DLS 有效解决了冗余机械臂在复杂环境下易陷入奇异位姿或关节极限的问题。

### 8. 不足与局限
*   **计算开销**：TP-DLS 涉及多次迭代和零空间投影，计算耗时远高于标准 IK，可能限制其在极高频实时控制中的应用。
*   **姿态忽略**：实验中为了简化，主要关注位置跟踪，**忽略了末端执行器的姿态约束**（Orientation），这在某些需要特定抓取角度的任务中可能不适用。
*   **实机验证缺失**：目前所有结果均基于 MATLAB 仿真，尚未在物理机器人平台上验证摩擦力、通信延迟等现实因素的影响。
*   **静态环境限制**：目前仅针对静态障碍物环境，未讨论动态障碍物下的表现。

（完）
