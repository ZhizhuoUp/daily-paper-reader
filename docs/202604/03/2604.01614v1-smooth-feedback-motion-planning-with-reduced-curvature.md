---
title: Smooth Feedback Motion Planning with Reduced Curvature
authors: "Aref Amiri, Steven M. LaValle"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01614v1"
tags: ["query:q4"]
score: 8.0
evidence: 减少曲率的无碰撞机器人运动规划
tldr: 针对基于单元分解的反馈运动规划中路径弯曲度高、控制开销大的问题，本文提出一种降低曲率的平滑反馈规划方法。该方法通过启发式对齐局部向量场，并结合几何算法构建围绕目标的极大星形单纯形链（漏斗区），从而在安全范围内应用直接指向目标的控制律。实验表明，该方法在保持鲁棒性的同时，显著减少了路径弯曲度和控制能耗，特别适用于低维配置空间。
selection_source: fresh_fetch
motivation: 现有的反馈运动规划算法生成的路径往往存在不必要的弯曲，导致机器人运动缓慢且控制能耗较高。
method: 提出一种启发式向量场对齐策略，并结合构建极大星形单纯形链的几何算法，在目标周围形成可应用直接控制律的“漏斗”区域。
result: "仿真结果显示，该方法平均减少了91.40%的总弯曲度和45.47%的LQR控制开销，且在计算效率上优于传统采样和优化算法。"
conclusion: 该方法在保证碰撞避免和鲁棒性的前提下，有效提升了低维空间下反馈运动规划的路径质量和执行效率。
---

## Abstract
Feedback motion planning over cell decompositions provides a robust method for generating collision-free robot motion with formal guarantees. However, existing algorithms often produce paths with unnecessary bending, leading to slower motion and higher control effort. This paper presents a computationally efficient method to mitigate this issue for a given simplicial decomposition. A heuristic is introduced that systematically aligns and assigns local vector fields to produce more direct trajectories, complemented by a novel geometric algorithm that constructs a maximal star-shaped chain of simplexes around the goal. This creates a large ``funnel'' in which an optimal, direct-to-goal control law can be safely applied. Simulations demonstrate that our method generates measurably more direct paths, reducing total bending by an average of 91.40\% and LQR control effort by an average of 45.47\%. Furthermore, comparative analysis against sampling-based and optimization-based planners confirms the time efficacy and robustness of our approach. While the proposed algorithms work over any finite-dimensional simplicial complex embedded in the collision-free subset of the configuration space, the practical application focuses on low-dimensional ($d\le3$) configuration spaces, where simplicial decomposition is computationally tractable.

---

## 论文详细总结（自动生成）

### 论文总结：具有降低曲率特性的平滑反馈运动规划

#### 1. 论文的核心问题与整体含义
本文针对基于**单纯形分解（Simplicial Decomposition）**的反馈运动规划问题展开研究。虽然现有的反馈规划方法（如 Lindemann 等人的工作）能提供避障和全局收敛的正式保证，但其生成的路径往往存在不必要的弯曲（Bending），导致机器人运动缓慢且控制能耗高。论文的核心动机是提出一种计算高效的方法，在保持鲁棒性和平滑性的前提下，通过优化局部向量场分配来显著降低路径曲率，提高运动效率。

#### 2. 论文提出的方法论
论文提出了一套结合启发式对齐与几何构建的框架：
*   **启发式向量场对齐（Heuristic Alignment）：** 
    *   从目标点开始，按跳数（Hop-distance）反向传播期望的运动方向。
    *   对于每个单纯形（Simplex），计算一个候选向量。通过求解一个简单的线性规划（LP）可行性问题，确保该向量落在允许的“锥形区域”内（保证能穿过出口面）。
    *   若候选向量不可行，则将其投影到最近的边界向量上。
*   **面向量平均化（Face-vector Averaging）：** 在单纯形的出口面上，取当前单元和后继单元向量的平均值，以平滑跨单元的过渡。
*   **极大星形单纯形链（Maximal Star-shaped Chain）：** 
    *   开发了一种几何算法，从目标单纯形开始向外扩展，寻找所有相对于目标点可见的单纯形集合。
    *   在这个“漏斗状”的星形区域内，直接应用指向目标的控制律，从而在安全范围内实现最优的直线收敛。
*   **平滑混合：** 使用 $C^\infty$ 连续的隆起函数（Bump Functions）将单元向量场与面向量场进行加权融合。

#### 3. 实验设计
*   **实验场景：** 迷宫（Maze）、虫子陷阱（Bug Trap）、稀疏环境（基于波士顿城市地图的 Sparse 场景）。
*   **Benchmark 与对比方法：**
    *   **基准对比：** Lindemann 等人的原始反馈规划算法（Baseline）。
    *   **综合对比：** RRT*（采样类）、A*（网格类）、FPP（基于优化的 Fast Path Planning）、APF（人工势场法）。
*   **评估指标：** 路径长度、最大曲率、总弯曲度（Total Bending）、总转角（Total Turning）、LQR 控制开销、旅行时间。

#### 4. 资源与算力
*   **硬件环境：** 使用了配备 AMD Ryzen 9 8945HS CPU 和 32 GB RAM 的笔记本电脑。
*   **算力细节：** 论文未提及 GPU 使用情况，因为该算法主要依赖 CPU 进行几何计算和线性规划求解。
*   **耗时：** 离线预计算（含三角剖分、对齐、漏斗构建）在标准环境下仅需 6-9 ms；在线执行（向量场评估）平均仅需 0.07 ms（约 15 kHz 更新率）。

#### 5. 实验数量与充分性
*   **实验规模：** 针对每个环境，将每个单元的质心都作为目标点进行测试，总共评估了超过 **20,000 条** 积分曲线。
*   **消融实验：** 专门进行了消融研究，量化了“星形区域”对降低弯曲度的具体贡献（在稀疏环境下贡献达 74.35%）。
*   **充分性评价：** 实验设计非常充分且客观。通过大规模的随机起点/终点测试，并引入了动态仿真（LQR 控制器）来验证几何指标与物理开销的关联，具有很强的说服力。

#### 6. 论文的主要结论与发现
*   **性能提升：** 与基准方法相比，该方法平均减少了 **91.40%** 的总弯曲度和 **45.47%** 的 LQR 控制开销。
*   **鲁棒性：** 在所有测试用例中均实现了 100% 的成功率，且路径质量接近优化类算法（如 FPP），但计算速度快得多。
*   **实时性：** 算法极高的评估频率使其能够轻松应对动态环境下的实时反馈控制。

#### 7. 优点
*   **计算高效：** 避免了昂贵的非线性优化或复杂的偏微分方程求解，仅通过局部几何检查实现。
*   **理论完备：** 继承了单纯形分解反馈规划的 $C^\infty$ 平滑性、碰撞避免和全局收敛保证。
*   **实用性强：** 引入的“漏斗区”概念非常符合机器人实际运行逻辑，显著改善了目标点附近的运动表现。

#### 8. 不足与局限
*   **维度限制：** 虽然理论上适用于高维，但单纯形分解（如 CDT）在 3 维以上空间计算开销巨大，目前主要局限于 2D 或 3D 空间。
*   **网格依赖：** 算法性能受三角剖分质量影响。例如，为了提高网格质量引入的 Steiner 点可能会限制星形区域的扩展。
*   **保守性：** 构建的星形区域是基于单纯形链的，可能比实际的物理可见区域（Visibility Polytope）要小。

（完）
