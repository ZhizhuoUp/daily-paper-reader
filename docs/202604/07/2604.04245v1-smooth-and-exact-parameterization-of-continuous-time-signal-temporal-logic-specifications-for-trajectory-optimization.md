---
title: Smooth and Exact Parameterization of Continuous-time Signal Temporal Logic Specifications for Trajectory Optimization
title_zh: 用于轨迹优化的连续时间信号时序逻辑规范的平滑且精确参数化
authors: "Samet Uzun, Behçet Açıkmeşe"
date: 2026-04-05
pdf: "https://arxiv.org/pdf/2604.04245v1"
tags: ["query:q3"]
score: 9.0
evidence: 使用信号时序逻辑进行避障的轨迹优化
tldr: 本研究提出了一种针对非凸轨迹优化的连续时间信号时序逻辑（CT-STL）平滑参数化方法。该方法通过在密集时间内评估规范，确保了“始终”谓词的连续满足并消除了“最终”谓词的离散保守性。通过解决梯度掩蔽问题并允许更粗略的时间网格，该方法在降低计算维度的同时保证了安全性和逻辑保真度，并在无人机复杂任务中得到了验证。
selection_source: fresh_fetch
motivation: 现有的轨迹优化方法通常仅在离散采样点强制执行STL规范，难以保证采样点间的连续安全性且存在梯度消失问题。
method: 提出一种在密集时间内评估CT-STL的平滑参数化方案，支持复杂的“直到”逻辑，并优化了适用于梯度求解器的目标函数景观。
result: 该方法在保证连续时间逻辑满足的前提下，允许使用更粗的时间网格，显著降低了非凸规划问题的维度和计算成本。
conclusion: 该框架为复杂时序逻辑约束下的轨迹优化提供了一种精确、高效且具有良好梯度特性的解决方案。
---

## 摘要
本文提出了一种用于非凸轨迹优化的连续时间信号时序逻辑 (CT-STL) 规范的平滑参数化方法，该方法在底层数值积分方案的精度范围内是可靠且完备的。CT-STL 为编码丰富的时序和逻辑任务需求提供了一个自然的框架，但现有的轨迹优化公式通常仅在离散采样节点处强制执行此类规范。相比之下，所提出的方法在密集时间内评估规范，从而保证了“总是 (always)”谓词的连续时间满足，这对于避障等路径约束至关重要；同时，通过允许在规定间隔内的任何时间满足，消除了“最终 (eventually)”谓词由节点引起的保守性。这两种密集时间构造也作为处理更通用 CT-STL 公式（包括复杂的“直到 (until)”规范）的主要构建模块。此外，所提出的参数化解决了标准定量语义中固有的局部性和梯度掩蔽问题，为基于梯度的求解器提供了更有利的优化景观。虽然密集时间评估在离散化过程中引入了额外的函数评估，但它也允许使用大幅粗糙的时间网格，而不会牺牲安全性或逻辑保真度。这反过来又降低了所得非凸规划的维度，而维度通常是轨迹生成成本的主导因素。通过一个受复杂连续时间“直到”规范约束的敏捷四旋翼飞行问题，证明了所提出框架的数值有效性和语义精确性。代码实现可在 https://github.com/UW-ACL/TrajOpt_CT-STL 获取。

## Abstract
This paper presents a smooth parameterization of continuous-time Signal Temporal Logic (CT-STL) specifications for nonconvex trajectory optimization that is sound and complete up to the accuracy of the underlying numerical integration scheme. CT-STL provides a natural framework for encoding rich temporal and logical task requirements, but existing trajectory-optimization formulations typically enforce such specifications only at discrete sampling nodes. In contrast, the proposed method evaluates specifications in dense time, thereby guaranteeing continuous-time satisfaction of always predicates, which is critical for path constraints such as obstacle avoidance, while eliminating the node-induced conservatism of eventually predicates by allowing satisfaction at any time within the prescribed interval. These two dense-time constructions also serve as the main building blocks for handling more general CT-STL formulas, including complex until specifications. Furthermore, the proposed parameterization resolves the locality and gradient-masking issues inherent in standard quantitative semantics, yielding a more favorable landscape for gradient-based solvers. Although dense-time evaluation introduces additional function evaluations during discretization, it also permits substantially coarser temporal grids without sacrificing safety or logical fidelity. This, in turn, reduces the dimension of the resulting nonconvex program, which is often the dominant factor in trajectory-generation cost. The numerical effectiveness and semantic exactness of the proposed framework are demonstrated on an agile quadrotor flight problem subject to a complex continuous-time until specification. The implementation is available at https:// github.com/UW-ACL/TrajOpt_CT-STL

---

## 论文详细总结（自动生成）

### 论文结构化总结：用于轨迹优化的连续时间信号时序逻辑（CT-STL）的平滑且精确参数化

#### 1. 核心问题与研究动机
*   **核心问题**：如何在非凸轨迹优化中，既能保证逻辑规范（如信号时序逻辑 STL）在**连续时间**上的严格满足，又能提供利于梯度求解器的**平滑优化景观**。
*   **研究动机**：
    *   **离散化风险**：传统的离散时间 STL（DT-STL）仅在采样点检查约束，忽略了采样点间的行为，可能导致避障失败或逻辑违规。
    *   **优化困境**：标准的 STL 鲁棒性语义（基于 min/max）是非光滑的，而常见的平滑近似（如 LSE 或 Softmax）存在“梯度掩蔽”和“局部性”问题，导致求解器难以收敛。
    *   **计算效率**：为了安全，DT-STL 往往需要极细的时间网格，这显著增加了优化问题的维度和计算负担。

#### 2. 方法论
*   **核心思想**：将基于广义均值的平滑鲁棒性（GMSR）扩展到**密集时间（Dense-time）**评估，利用数值积分的子节点来近似连续时间语义。
*   **关键技术细节**：
    *   **GMSR 参数化**：采用广义均值函数 $h_c^\wedge$ 和 $h_c^\vee$ 替代传统的 min/max。这种构造在保持符号精确性（即鲁棒性 $>0$ 等价于逻辑满足）的同时，实现了 $C^1$ 连续平滑。
    *   **密集时间评估**：在多重射击法（Multiple Shooting）的每个时间步内，利用数值积分（如 RK4）产生的中间子节点（Sub-nodes）来评估 STL 规范。
    *   **时序算子实现**：
        *   **Always ($G$)**：要求所有密集采样点的鲁棒性均大于零，通过平滑合取算子聚合。
        *   **Eventually ($F$)**：允许在密集采样点中的任意时刻满足，消除了离散节点带来的保守性。
        *   **Until ($U$)**：通过嵌套的平滑算子，确保在目标达成前持续满足特定条件。
    *   **梯度分布机制**：GMSR 的数学特性使得梯度信息能分布到所有相关的子公式和时间点上，有效解决了梯度消失和掩蔽问题。

#### 3. 实验设计
*   **实验场景**：四旋翼无人机敏捷飞行任务。
*   **任务要求**：
    *   **Until 规范**：无人机必须保持低速模式（$\mu_s$），直到进入充电站区域（$\mu_c$）。
    *   **连续时间约束**：包括倾斜角限制、推力幅值限制和全局速度上限。
*   **算法流程**：使用 **Prox-convex 方法**（一种处理复合非凸优化的近端算法）求解。
*   **对比与基准**：
    *   实验主要展示了该框架处理复杂 $U$ 算子的能力。
    *   对比对象隐含为传统的离散时间采样方法（通过展示密集时间下的鲁棒性曲线来证明其优越性）。

#### 4. 资源与算力
*   **硬件平台**：2023 款 MacBook Pro，搭载 **Apple M2 处理器**。
*   **软件实现**：使用 Python 编写，利用 **JAX** 进行自动微分，**CVXPY** 进行建模，**QOCO** 作为底层圆锥凸优化求解器。
*   **计算耗时**：
    *   凸子问题求解时间：约 **15.35 ms**。
    *   动力学离散化与 CT-STL 评估时间：约 **0.97 ms**。
    *   总迭代次数：10 次迭代即可收敛。

#### 5. 实验数量与充分性
*   **实验规模**：论文重点展示了一个具有代表性的复杂四旋翼飞行案例。
*   **充分性评价**：
    *   **逻辑验证充分**：成功演示了 $G$（始终）、$F$（最终）和 $U$（直到）算子的综合应用。
    *   **数值验证客观**：提供了详细的鲁棒性值、速度曲线和充电站边际曲线，证明了连续时间下的满足情况。
    *   **局限性**：缺乏大规模的蒙特卡洛统计实验或与多种不同平滑算子（如 LSE）的横向性能对比。

#### 6. 主要结论与发现
*   **语义精确性**：该方法在数值积分精度范围内实现了 CT-STL 的精确参数化，消除了离散化带来的安全隐患。
*   **优化友好性**：GMSR 提供的平滑景观使得非凸优化问题更容易被基于梯度的求解器处理，且收敛速度快。
*   **网格效率**：由于可以在子节点评估约束，主优化网格可以设置得相对较粗（如仅 5 个节点），从而大幅降低决策变量的维度。

#### 7. 优点
*   **理论完备**：解决了 STL 优化中长期存在的“平滑性 vs 精确性”以及“离散采样 vs 连续安全”的矛盾。
*   **梯度特性优异**：通过广义均值分配梯度，避免了求解过程中的停滞。
*   **实用性强**：计算开销极低，具备嵌入式实时应用的潜力。

#### 8. 不足与局限
*   **实验覆盖面**：目前仅验证了单机任务，尚未探讨多机协同或动态障碍物环境下的扩展性。
*   **时间参数限制**：实验采用固定终点时间（Fixed-final-time），对于终点时间可变的优化问题，文中提到这属于未来研究方向。
*   **实现偏差**：虽然 Python 实现已很快，但要达到工业级实时性，仍需向 C/C++ 迁移并进行更深层的代码优化。

（完）
