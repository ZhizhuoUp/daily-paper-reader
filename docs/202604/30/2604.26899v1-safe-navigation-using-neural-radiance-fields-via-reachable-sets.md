---
title: Safe Navigation using Neural Radiance Fields via Reachable Sets
title_zh: 基于可达集并利用神经辐射场实现安全导航
authors: "Omanshu Thapliyal, Malarvizhi Sankaranarayanasamy, Ravigopal Vennelakanti"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.26899v1"
tags: ["query:q1"]
score: 8.0
evidence: 针对拥挤环境路径规划的约束最优控制
tldr: 本研究针对复杂环境下的自主导航安全问题，提出了一种结合神经辐射场（NeRF）与可达集（Reachable Sets）的新方法。利用 NeRF 对障碍物和自身进行体积建模，并结合可达集捕捉机器人的实时状态空间能力。通过将路径规划转化为包含线性矩阵不等式（LMI）约束的最优控制问题，实现了在复杂几何约束下的安全避障。仿真结果验证了该方法在多障碍物场景中引导机器人安全到达目标的能力。
selection_source: fresh_fetch
motivation: 在充满障碍物的复杂环境中，实现具有多样几何形状的机器人安全导航是一项重大挑战。
method: 利用 NeRF 进行环境和机器人的体积表示，并结合可达集理论与线性矩阵不等式约束的最优控制进行路径规划。
result: 在两种不同场景下的仿真实验表明，该方法能够有效处理大量障碍物并生成安全的导航路径。
conclusion: 将 NeRF 的体积建模能力与可达集的安全约束相结合，为自主系统在复杂环境下的安全导航提供了有效的解决方案。
---

## 摘要
在杂乱环境中的安全导航是自主系统面临的一个重要挑战。在充满障碍的场景中导航的机器人，需要在存在各种几何形状的障碍物、目标和自身对象的情况下实现安全导航。在这项工作中，利用机器人在状态空间中实时能力的可达集表示来捕捉安全导航需求。同时，利用神经辐射场（NeRFs）根据需要计算、存储和处理障碍物或自身车辆的体积表示。采用约束最优控制来表示由此产生的路径规划问题，其中涉及线性矩阵不等式约束。我们展示了在两种不同场景下存在大量障碍物时的路径规划仿真结果。通过在相应的约束最优控制问题中使用可达集，证明了安全导航的有效性。

## Abstract
Safe navigation in cluttered environments is an important challenge for autonomous systems. Robots navigating through obstacle ridden scenarios need to be able to navigate safely in the presence of obstacles, goals, and ego objects of varying geometries. In this work, reachable set representations of the robot's real-time capabilities in the state space can be utilized to capture safe navigation requirements. While neural radiance fields (NeRFs) are utilized to compute, store, and manipulate the volumetric representations of the obstacles, or ego vehicle, as needed. Constrained optimal control is employed to represent the resulting path planning problem, involving linear matrix inequality constraints. We present simulation results for path planning in the presence of numerous obstacles in two different scenarios. Safe navigation is demonstrated through using reachable sets in the corresponding constrained optimal control problems.

---

## 论文详细总结（自动生成）

这篇论文提出了一种结合神经辐射场（NeRF）与可达集（Reachable Sets）理论的新型安全导航框架，旨在解决自主系统在复杂、拥挤环境中的路径规划问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在包含复杂几何形状障碍物的拥挤环境中，实现既能充分利用高保真环境建模、又能保证动力学安全约束的机器人实时导航。
*   **研究动机**：
    *   传统的路径规划方法往往简化了机器人和障碍物的几何形状（如简化为点或球体），难以处理复杂边界。
    *   **NeRF** 在 3D 场景重建和体积表示方面表现卓越，但直接用于控制决策的研究尚在起步阶段。
    *   **可达集（Reachable Sets）** 是控制理论中表征系统安全性的严谨工具，能预测系统在给定控制输入下可能到达的状态空间。
    *   作者试图将 NeRF 的**几何表达能力**与可达集的**安全验证能力**结合，转化为可高效求解的优化问题。

### 2. 方法论
该方法将导航问题建模为受约束的最优控制问题，核心步骤如下：
*   **NeRF 体积建模**：利用多视角图像训练 NeRF 模型，生成机器人、障碍物或目标的 3D 点云。
*   **多面体简化**：为了计算效率，将 NeRF 生成的点云转化为**凸包多面体（Convex Hull Polytopes）**，表示为线性不等式组 $Fx \le b$。
*   **可达集计算**：针对机器人的线性动力学系统，利用最优控制方法（基于 Varaiya 的理论）实时计算 $T$ 时间步内的多面体可达集。这能捕捉机器人在物理约束下的运动潜力。
*   **配置空间（C-space）构建**：利用 **Minkowski 和（Minkowski Sum）** 运算，将机器人几何形状与障碍物几何形状合并，将避障问题转化为状态空间中的点与膨胀障碍物的关系。
*   **约束最优控制（MPC）**：将路径规划表述为一个模型预测控制问题。目标函数是最小化可达集与目标集之间的距离，约束条件包括线性动力学方程和基于线性矩阵不等式（LMI）的避障约束（即要求可达集与障碍物多面体交集为空）。

### 3. 实验设计
*   **实验场景**：
    1.  **场景一**：机器人自身由 NeRF 模型表示，在包含 8 个随机大小长方体障碍物的 3D 空间中导航。
    2.  **场景二**：目标点由 NeRF 模型表示，机器人在包含 10 个随机障碍物的环境中寻找路径。
*   **动力学模型**：采用三维双积分器（Double Integrator）模型。
*   **Benchmark/对比方法**：论文**未明确列出**与其他经典算法（如 A*、RRT* 或纯视觉导航方法）的定量对比基准，主要侧重于展示所提框架的可行性。

### 4. 资源与算力
*   **硬件配置**：Intel Core i9-12900HX 处理器（2300 Mhz，16 核）。
*   **软件工具**：使用 `nerfstudio` 框架训练 NeRF 模型。
*   **训练时长**：NeRF 模型的平均训练时间约为 **18 分钟**。
*   **算力说明**：文中未提及具体使用的 GPU 型号，但考虑到 NeRF 训练通常依赖显卡，推测使用了主流 NVIDIA GPU。

### 5. 实验数量与充分性
*   **实验数量**：论文展示了 2 组主要的仿真实验（6 障碍物和 10 障碍物场景）。
*   **充分性评价**：
    *   **不足**：实验规模较小，仅限于仿真环境，缺乏消融实验（例如对比不使用可达集的效果）和不同动力学模型的验证。
    *   **局限性**：未进行大规模统计学意义上的重复实验，也未探讨 NeRF 训练时间对实时导航系统的影响（18 分钟的训练时间对于即时动态环境来说过长）。

### 6. 主要结论与发现
*   **可行性验证**：证明了将 NeRF 几何表示转化为凸包多面体后，可以有效地集成到基于可达集的控制框架中。
*   **计算效率**：通过将复杂的 NeRF 模型简化为线性不等式约束，使得原本非凸的避障问题在局部变得可解，能够通过 MPC 实时生成安全轨迹。
*   **安全性**：利用可达集作为“前瞻”约束，比单纯的点避障更能保证机器人在未来一段时间内的物理安全性。

### 7. 优点
*   **跨学科融合**：创新性地结合了计算机视觉的前沿技术（NeRF）与控制理论的经典工具（可达集）。
*   **几何保真度**：相比传统的球体包围盒，NeRF 凸包能更精确地描述不规则形状的机器人和障碍物。
*   **数学严谨性**：将避障约束转化为线性矩阵不等式，保证了优化问题的数学结构清晰。

### 8. 不足与局限
*   **实时性瓶颈**：NeRF 的训练时间（18 分钟）远超实时导航的要求，目前更适用于已知环境的离线建模。
*   **凸性假设**：将 NeRF 点云简化为凸包会损失大量几何细节（如凹陷部分），在极度狭窄的空间内可能导致无法找到可行解。
*   **动力学限制**：目前仅验证了线性系统，对于具有高度非线性动力学的机器人（如无人机大角度飞行）的适用性有待证明。
*   **实验深度**：缺乏与现有主流避障算法的定量性能对比（如成功率、计算耗时、路径长度等）。

（完）
