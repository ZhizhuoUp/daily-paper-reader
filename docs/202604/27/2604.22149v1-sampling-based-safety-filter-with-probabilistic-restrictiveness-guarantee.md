---
title: Sampling-Based Safety Filter with Probabilistic Restrictiveness Guarantee
title_zh: 具有概率限制性保证的基于采样的安全滤波器
authors: "Junyoung Park, Hyeontae Sung, Heejin Ahn"
date: 2026-04-24
pdf: "https://arxiv.org/pdf/2604.22149v1"
tags: ["query:q2"]
score: 8.0
evidence: 用于安全约束控制序列的模型预测控制
tldr: 针对自主系统在复杂非凸环境下的安全保障挑战，本文提出一种基于采样的模块化安全过滤器。该方法利用斯坦变分模型预测控制（SV-MPC）生成控制序列样本，近似安全约束下的后验分布，有效捕捉多峰安全区域。通过场景法提供限制性的概率保证，在名义控制器失效时介入，确保了单车及多车避障任务中的系统安全性。
selection_source: fresh_fetch
motivation: 旨在为任意名义控制器在复杂非凸环境中提供具有形式化保证的安全过滤机制。
method: 采用基于SV-MPC的采样方法近似安全后验分布，并结合场景法提供限制性的概率保证。
result: 在单车和多车避障任务中成功验证了该过滤器在拥挤环境下的有效性，能够有效修正失效的名义控制输入。
conclusion: 该采样安全过滤器为非凸环境下的自主系统提供了一种兼具灵活性与概率安全保证的模块化解决方案。
---

## 摘要
确保安全性是自主系统的关键要求，但为名义控制器提供形式化保证仍是一个重大挑战。在本文中，我们提出了一种模块化的基于采样的安全滤波器，以确保任意名义控制输入的安全性。在每个时间步，该滤波器利用通过斯坦变分模型预测控制（SV-MPC）生成的控制序列样本来评估名义输入的安全性。该方法近似了控制序列上的安全条件后验分布，使滤波器能够有效地捕获复杂非凸环境中的多峰安全区域。当所有采样的候选控制序列都被视为不安全时，该滤波器通过覆盖名义输入来保证安全性。通过利用场景法，所提出的方法为其限制性提供了概率保证。我们通过单车和多车设置下的避障任务验证了该滤波器，证明了其在名义控制器可能失效的拥挤环境中导航的有效性。

## Abstract
Ensuring safety is a critical requirement for autonomous systems, yet providing formal guarantees for nominal controllers remains a significant challenge. In this paper, we propose a modular sampling-based safety filter to ensure the safety of arbitrary nominal control inputs. At each timestep, the filter evaluates the safety of the nominal input by leveraging control sequence samples generated via Stein Variational Model Predictive Control (SV-MPC). This approach approximates a safety-conditioned posterior distribution over control sequences, enabling the filter to effectively capture multimodal safe regions in complex, non-convex environments. The filter guarantees safety by overriding the nominal input when all sampled control sequence candidates are deemed unsafe. By leveraging the scenario approach, the proposed method provides a probabilistic guarantee on its restrictiveness. We validate the filter through collision avoidance tasks in both single- and multi-vehicle settings, demonstrating its efficacy in navigating cluttered environments where nominal controllers may fail.

---

## 论文详细总结（自动生成）

这篇论文提出了一种基于采样的模块化安全滤波器，旨在为任意名义控制器（如强化学习策略或启发式算法）提供形式化的安全保障。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在复杂、非凸且不可微的环境中，为性能导向但缺乏安全保证的名义控制器提供实时的安全监控与干预机制。
*   **研究动机**：
    *   传统的安全证明方法（如HJ可达性分析）面临“维度灾难”；控制屏障函数（CBF）的合成在复杂环境下非常困难。
    *   现有的基于采样的滤波器通常假设单峰高斯分布，难以处理非凸环境中的多峰安全区域（即存在多个互不连通的安全路径）。
    *   缺乏对采样滤波器“限制性”（即滤波器是否过度干预）的理论量化保证。

### 2. 方法论
*   **核心思想**：将安全过滤视为一个贝叶斯推理问题。通过采样评估名义输入的安全性，若采样序列中存在安全路径，则允许名义输入执行；否则，切换至备份安全策略。
*   **关键技术细节**：
    *   **SV-MPC（斯坦变分模型预测控制）**：利用 Stein 变分梯度下降（SVGD）来近似安全条件下的后验分布。通过粒子更新机制（包含吸引力和排斥力），能够同时捕获多个潜在的安全区域（多峰性），避免陷入局部最优。
    *   **场景法（Scenario Approach）**：利用随机约束优化中的场景法，首次为采样滤波器的限制性提供了概率保证。论文证明了当采样数 $N$ 满足特定公式时，滤波器干预名义输入的概率（即限制性）被限制在 $\epsilon$ 之内。
    *   **安全保证机制**：通过定义“安全控制不变集”（如刹车至停止状态），确保即使在干预发生时，系统也能通过备份序列维持在安全状态。
*   **算法流程**：
    1. 预测名义输入下的下一状态。
    2. 使用 SV-MPC 并行生成 $N$ 个候选控制序列。
    3. 评估这些序列的代价（是否违反安全约束）。
    4. 若存在安全序列，执行名义输入；若不存在，执行备份序列并更新安全备份。

### 3. 实验设计
*   **实验场景**：
    1.  **单机器人避障**：在包含多个灰色障碍物的非凸环境中导航。
    2.  **多车无信号交叉口**：3-5 辆车在交叉口进行纵向运动控制，避免碰撞。
*   **Benchmark（对比方法）**：
    *   **CEM-based Filter**：基于交叉熵方法（CEM）的采样滤波器（通常为单峰分布）。
    *   **名义控制器**：在多车场景中使用了基于 GPT 的决策变压器（Decision Transformer, DT）。
*   **评估指标**：碰撞率、干预率、最大安全采样率（用于验证限制性理论）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、CPU 核心数或具体的训练/推理时长。
*   **实现细节**：提到采样过程是并行执行的（in parallel），这暗示了该方法适合在具备并行计算能力的硬件（如 GPU 或多核 CPU）上运行以满足实时性要求。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在多车场景中，随机选择了 **100 个测试案例**（不在训练集中）进行评估。
    *   在单机器人场景中，通过对整个区域进行 **0.01 × 0.01 分辨率的网格化离散采样**来测试干预频率。
*   **充分性评价**：实验设计较为客观。通过对比 SV-MPC 和 CEM，清晰地展示了多峰分布在处理死锁（Deadlock）问题上的优势。同时，通过不同采样数 $N$ 的消融实验验证了理论上的限制性边界，实验结果与数学证明一致。

### 6. 主要结论与发现
*   **安全性**：该滤波器能有效消除名义控制器的所有碰撞情况（在多车实验中碰撞率降为 0）。
*   **限制性**：相比于 CEM，基于 SV-MPC 的滤波器干预次数更少，因为它能更有效地探索安全空间。
*   **理论验证**：实验数据证实，滤波器的实际错误干预率始终低于预设的概率阈值 $\epsilon$，验证了基于场景法的理论正确性。

### 7. 优点
*   **多峰捕获能力**：通过 SV-MPC 解决了非凸环境下传统采样方法容易陷入局部不安全区域的问题。
*   **理论完备性**：不仅保证了安全性，还通过场景法为“不必要的干预”提供了概率上的量化保证，这在采样滤波器领域具有开创性。
*   **模块化**：可以作为“插件”应用于任何预训练的控制器，无需修改原控制器参数。

### 8. 不足与局限
*   **计算开销**：虽然支持并行，但 SV-MPC 的多次迭代和大量采样（如 $N=757$）对实时计算资源有一定要求。
*   **备份策略依赖**：安全保证高度依赖于已知的安全不变集（如必须能随时刹停），对于某些无法轻易停止的动态系统（如固定翼无人机）应用受限。
*   **概率性而非绝对性**：限制性保证是概率性的，这意味着在极端低概率情况下，滤波器仍可能做出不必要的干预。

（完）
