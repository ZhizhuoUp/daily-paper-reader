---
title: "ATRS: Adaptive Trajectory Re-splitting via a Shared Neural Policy for Parallel Optimization"
title_zh: ATRS：通过共享神经策略实现并行优化的自适应轨迹重切分
authors: "Jiajun Yu, Guodong Liu, Li Wang, Pengxiang Zhou, Wentao Liu, Yin He, Chao Xu, Fei Gao, Yanjun Cao"
date: 2026-04-24
pdf: "https://arxiv.org/pdf/2604.22715v1"
tags: ["query:profile-1"]
score: 9.0
evidence: 运动规划的并行轨迹优化
tldr: 针对并行轨迹优化中固定分段结构导致的收敛停滞问题，本文提出ATRS框架。该框架将共享深度强化学习策略嵌入ADMM循环，通过多智能体共享策略MDP实现轨迹段的自适应重切分。该方法具有尺寸不变性和零样本泛化能力，显著提升了复杂约束下的收敛速度和计算效率，适用于大规模离线规划和实时在线重规划。
selection_source: fresh_fetch
motivation: 传统的并行轨迹优化采用固定分段结构，在高度受限区域易出现优化停滞，导致全局收敛缓慢。
method: 提出一种基于共享神经策略的自适应重切分框架，利用强化学习根据求解器内部状态动态调整轨迹分段，并结合置信度选举机制确保稳定性。
result: "仿真实验表明，该方法可减少高达26.0%的迭代次数和19.1%的计算时间，且在真实环境中实现了35ms内的实时重规划。"
conclusion: ATRS通过动态调整优化结构，有效解决了并行轨迹优化的效率瓶颈，展现了极强的泛化能力和实机应用潜力。
---

## 摘要
通过交替方向乘子法（ADMM）进行的并行轨迹优化已成为长时界运动规划的一种可扩展方法。然而，现有框架通常基于预定义的固定结构将问题分解为并行子问题。这种结构僵化往往会导致在高度受限区域出现优化停滞，其中少数滞后的子问题会延迟全局收敛。一种自然的补救措施是在线自适应地对这些停滞段进行重切分。然而，决定何时、何地以及如何切分超出了基于规则的启发式方法的能力。为此，我们提出了 ATRS，这是一个将共享深度强化学习策略嵌入并行 ADMM 循环的新型框架。我们将这种自适应调整建模为多智能体共享策略马尔可夫决策过程，其中所有轨迹段均作为同质智能体并共享统一的神经策略网络。这种参数共享架构赋予了系统尺寸不变性，使其能够处理重切分过程中动态变化的段数，并泛化到任意轨迹长度。此外，由于我们的网络仅依赖于数值求解器的内部状态而非环境的几何特征，因此我们的方案本质上支持对未知环境的零样本泛化。为确保求解器的稳定性，一种基于置信度的选举机制在每一步仅选择最停滞的段进行重切分。大量仿真表明，ATRS 加速了收敛，迭代次数减少了高达 26.0%，计算时间减少了高达 19.1%。真实世界实验进一步证实了其在每周期 35 毫秒内的大规模离线全局规划和实时机载重规划中的适用性，且没有仿真到现实的性能退化。

## Abstract
Parallel trajectory optimization via the Alternating Direction Method of Multipliers (ADMM) has emerged as a scalable approach to long-horizon motion planning. However, existing frameworks typically decompose the problem into parallel subproblems based on a predefined fixed structure. Such structural rigidity often causes optimization stagnation in highly constrained regions, where a few lagging subproblems delay global convergence. A natural remedy is to adaptively re-split these stagnating segments online. Yet, deciding when, where, and how to split exceeds the capability of rule-based heuristics. To this end, we propose ATRS, a novel framework that embeds a shared Deep Reinforcement Learning policy into the parallel ADMM loop. We formulate this adaptive adjustment as a Multi-Agent Shared-Policy Markov Decision Process, where all trajectory segments act as homogeneous agents and share a unified neural policy network. This parameter-sharing architecture endows the system with size invariance, enabling it to handle dynamically changing segment counts during re-splitting and generalize to arbitrary trajectory lengths. Furthermore, our formulation inherently supports zero-shot generalization to unseen environments, as our network relies solely on the internal states of the numerical solver rather than on the geometric features of the environment. To ensure solver stability, a Confidence-Based Election mechanism selects only the most stagnating segment for re-splitting at each step. Extensive simulations demonstrate that ATRS accelerates convergence, reducing the number of iterations by up to 26.0% and the computation time by up to 19.1%. Real-world experiments further confirm its applicability to both large-scale offline global planning and real-time onboard replanning within 35 ms per cycle, with no sim-to-real degradation.

---

## 论文详细总结（自动生成）

### ATRS：通过共享神经策略实现并行优化的自适应轨迹重切分

#### 1. 核心问题与研究动机
在无人机等自主机器人的长时界（Long-horizon）运动规划中，并行轨迹优化（如基于 ADMM 的方法）能显著提升计算效率。然而，现有方法通常采用**固定的轨迹分段结构**。在复杂受限环境中（如狭窄通道），某些轨迹段的优化难度远高于其他段，导致这些“滞后”的子问题拖慢全局收敛速度，甚至导致优化失败。现有的启发式规则难以灵活决定何时、何地以及如何重新切分轨迹。因此，研究动机在于如何通过数据驱动的方法，动态调整优化问题的结构以平衡计算负载。

#### 2. 方法论
论文提出了 **ATRS** 框架，其核心思想是将轨迹结构的自适应调整嵌入到并行 ADMM 优化循环中：
*   **多智能体共享策略 MDP (MASP-MDP)**：将每个轨迹段视为一个同质智能体，所有智能体共享同一个神经策略网络。这种参数共享架构实现了**尺寸不变性**，使模型能处理动态变化的段数并泛化至任意长度的轨迹。
*   **状态空间设计**：观测向量包含局部状态（如残差、对偶变量范数、能量密度、残差趋势等）和全局状态（最大残差、平均残差、当前段数比例）。关键点在于状态仅依赖于**求解器内部数值状态**而非环境几何特征，从而实现零样本泛化。
*   **动作空间设计**：策略输出包含切分倾向（用于竞选）、空间切分比例、时间偏差以及持续时间膨胀因子（用于增加局部自由度）。
*   **置信度选举机制**：为了维持求解器稳定性，每一步仅允许“切分倾向”最高且超过阈值的单个轨迹段执行重切分，避免剧烈的结构变动导致不收敛。
*   **算法流程**：采用 TD3（双延迟深度确定性策略梯度）算法进行离线训练，在线部署时通过轻量级 C++ 推断引导 ADMM 求解器。

#### 3. 实验设计
*   **场景设置**：
    *   **Map A (Perlin Noise)**：用于训练和基础测试，分为稀疏、中等、密集三种障碍物密度。
    *   **Map B (Dense Forest)**：包含 2500 个随机立方体障碍物的极度拥挤环境。
    *   **Map C (3D Maze)**：具有狭窄通道的结构化迷宫。
*   **Benchmark 与对比方法**：
    *   **TOP (Fixed-Structure)**：最先进的固定结构并行轨迹优化器。
    *   **Heuristic (Rule-Based)**：基于规则的重切分方法（选择残差最大的段进行等分）。
*   **评估指标**：迭代次数、总计算时间（含推断耗时）、能量消耗（Jerk 平滑度）、成功率。

#### 4. 资源与算力
*   **硬件**：工作站配备 Intel i5-12600KF CPU 和 NVIDIA RTX 4070 Ti GPU。
*   **训练时长**：策略在 10,000 个回合内收敛，总训练时间约为 **10 分钟**。
*   **部署性能**：在机载设备 NVIDIA Orin NX 上，每周期重规划耗时控制在 **35 毫秒**以内。

#### 5. 实验数量与充分性
*   **实验规模**：在不同密度和规模（短、中、长距离）的 9 种组合设置下进行了测试，每种设置重复 100 次实验。
*   **泛化性验证**：在未见过的森林和迷宫地图中进行了零样本泛化测试。
*   **消融实验**：专门验证了“重切分”动作与“时间膨胀”动作的协同效应。
*   **充分性评价**：实验设计涵盖了从仿真到实机的全流程，对比基准清晰，统计样本量充足，客观地展示了方法在收敛速度和鲁棒性上的提升。

#### 6. 主要结论与发现
*   **效率提升**：相比固定结构的 TOP 方法，ATRS 减少了高达 **26.0%** 的迭代次数和 **19.1%** 的总计算时间。
*   **泛化能力**：由于不依赖几何特征，策略在完全陌生的环境中表现稳健，显著减少了优化停滞的“长尾”案例。
*   **实机可行性**：在真实无人机平台上实现了大规模离线规划（迭代减少 56.7%）和高速在线重规划（3.2 m/s 飞行速度下稳定运行）。

#### 7. 优点
*   **结构自适应**：突破了传统优化器固定维度的限制，通过动态注入局部自由度解决瓶颈问题。
*   **尺寸不变性与零样本泛化**：参数共享和几何无关的状态设计使其具有极强的通用性，无需针对新环境重新训练。
*   **高效稳定**：置信度选举机制平衡了“结构调整带来的收益”与“数值稳定性”，且 C++ 实现保证了机载实时性。

#### 8. 不足与局限
*   **线程开销限制**：在超大规模问题中，当轨迹段数超过硬件并行线程数时，重切分带来的迭代次数收益会被线程调度开销抵消，导致墙钟时间提升收窄。
*   **软预算限制**：虽然设置了最大段数比例作为软约束，但在极端复杂环境下，如何最优地平衡“段数增加带来的计算负担”与“收敛加速收益”仍有优化空间。
*   **应用范围**：目前主要针对微分平坦系统（如多旋翼），对于非平坦或具有更复杂动力学约束的系统，其泛化效果尚待验证。

（完）
