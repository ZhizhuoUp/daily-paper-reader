---
title: Safe Human-to-Humanoid Motion Imitation Using Control Barrier Functions
title_zh: 基于控制屏障函数的人到类人机器人安全运动模仿
authors: "Wenqi Cai, John Abanes, Nikolaos Evangeliou, Anthony Tzes"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11447v1"
tags: ["query:profile-1"]
score: 9.0
evidence: 使用QP和CBF的实时避障运动模仿
tldr: 本研究针对人形机器人模仿人类动作时的安全性问题，提出了一种基于视觉的运动模仿框架。该框架利用单摄像头捕获人体骨骼关键点并转换为机器人关节角度，核心贡献在于引入了控制屏障函数（CBF）层，通过二次规划（QP）实时过滤指令，有效防止机器人自碰撞及人机碰撞，实现了安全且实时的动作模仿。
selection_source: fresh_fetch
motivation: 旨在解决人形机器人在模仿人类动作过程中可能发生的自碰撞及与人类碰撞的安全风险。
method: 采用单目视觉捕捉人体关键点进行运动重定向，并结合控制屏障函数（CBF）构建二次规划层来过滤不安全指令。
result: 仿真实验验证了该框架在实时运动模仿中能够有效规避碰撞并保持动作跟随。
conclusion: 该框架为人形机器人实现安全、实时的动作模仿提供了一种有效的视觉与控制集成方案。
---

## 摘要
确保操作安全性对于人到类人机器人的运动模仿至关重要。本文提出了一种基于视觉的框架，使类人机器人在模仿人类动作的同时能够避免碰撞。该框架通过单目相机捕捉人体骨骼关键点，并将其转换为关节角度以进行运动重定向。安全性通过一个构建为二次规划（QP）的控制屏障函数（CBF）层来保障，该层通过过滤模仿指令来防止自碰撞和人机碰撞。仿真结果验证了所提框架在实时碰撞感知运动模仿中的有效性。

## Abstract
Ensuring operational safety is critical for human-to-humanoid motion imitation. This paper presents a vision-based framework that enables a humanoid robot to imitate human movements while avoiding collisions. Human skeletal keypoints are captured by a single camera and converted into joint angles for motion retargeting. Safety is enforced through a Control Barrier Function (CBF) layer formulated as a Quadratic Program (QP), which filters imitation commands to prevent both self-collisions and human-robot collisions. Simulation results validate the effectiveness of the proposed framework for real-time collision-aware motion imitation.

---

## 论文详细总结（自动生成）

这是一份关于论文《Safe Human-to-Humanoid Motion Imitation Using Control Barrier Functions》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在利用单目视觉进行“人到类人机器人”动作模仿时，实时且有效地确保机器人的安全性（包括**自碰撞**和**人机碰撞**）。
*   **研究背景**：传统的动作模仿依赖昂贵的穿戴设备。虽然视觉方案更便捷，但在实时追踪过程中，机器人手臂极易与躯干或演示者发生碰撞。现有的视觉重定向研究往往缺乏在线的几何安全过滤机制。

### 2. 核心方法论
该框架包含三个主要阶段：
*   **姿态估计与重定向**：
    *   通过单相机捕获人体骨骼关键点，利用指数移动平均（EMA）和跳变拒绝进行滤波。
    *   定义了一个 8 自由度（DoF）的简化人体姿态（躯干、肩、肘），通过仿射映射和关节限制裁剪，将人类动作映射到机器人的关节空间，生成“名义指令”（Nominal Command）。
*   **胶囊体碰撞模型（Capsule-Based Model）**：
    *   将机器人和人类均简化为由 7 个胶囊体（躯干、大臂、小臂、大腿）组成的几何模型。
    *   机器人模型随关节状态实时更新，人类模型则直接从视觉骨骼点提取并转换至机器人坐标系。
*   **CBF-QP 安全过滤层**：
    *   **核心思想**：将安全问题转化为二次规划（QP）问题。
    *   **关键技术**：利用控制屏障函数（CBF）定义安全边界 $h(q)$。当机器人接近碰撞边界时，CBF 约束会强制修正关节速度，确保距离始终大于安全阈值。
    *   **优化目标**：在满足所有安全约束的前提下，使修正后的速度指令与原始模仿指令的偏差最小化。

### 3. 实验设计
*   **实验场景**：
    1.  **交叉臂触碰（Cross-arm reach）**：测试自碰撞规避。
    2.  **并排举手（Side-by-side arm raise）**：测试人机碰撞规避。
*   **基准测试（Benchmark）**：
    *   对比了不同的碰撞几何体模型：**球体（Spheres）**、**包围盒（Boxes）**和**胶囊体（Capsules）**。
    *   对比了不同的计算后端：**CPU（基于 Pinocchio 库）** vs **GPU（基于 JAX 库）**。
*   **对比指标**：计算频率（Hz）、约束数量、初始编译时间、求解耗时。

### 4. 资源与算力
*   **硬件设备**：使用了一台配备 **Intel Core Ultra 9 275HX CPU** 和 **NVIDIA RTX 5080 笔记本 GPU** 的电脑。
*   **软件框架**：CPU 端使用 Pinocchio 进行微分运动学计算，GPU 端利用 JAX 实现并行加速。
*   **性能表现**：在 GPU 加速下，胶囊体模型的计算频率可达 **33.084 Hz**，满足实时性要求。

### 5. 实验数量与充分性
*   **实验规模**：论文展示了两组典型的碰撞规避仿真案例，并提供了一组详尽的计算性能对比表格。
*   **充分性评价**：
    *   **计算性能分析充分**：对比了多种几何体和硬件后端，有力支撑了选择“胶囊体+GPU”的合理性。
    *   **功能验证较为初步**：虽然验证了避障的有效性，但实验主要集中在仿真环境，且动作类型相对有限（仅上肢 8 自由度）。

### 6. 主要结论与发现
*   **安全性**：CBF-QP 层能有效过滤危险指令，在不中断模仿的前提下保持安全裕度。
*   **几何体选择**：胶囊体模型在描述人体/机器人肢体时，比球体更节省约束数量，比包围盒计算更高效。
*   **算力优势**：利用 GPU 并行处理大量 CBF 约束，能显著提升求解速度（从 CPU 的约 4Hz 提升至 33Hz）。

### 7. 优点
*   **统一框架**：将自碰撞和人机碰撞规避集成在一个统一的 CBF-QP 层中。
*   **实时性强**：通过 JAX 和 GPU 加速，解决了复杂人形机器人多约束求解的延迟问题。
*   **实用性**：采用单目视觉而非昂贵设备，降低了部署门槛。

### 8. 不足与局限
*   **缺乏实机验证**：论文结果完全基于仿真，尚未在真实物理机器人上进行硬件实验，需考虑视觉噪声和通信延迟的影响。
*   **自由度受限**：目前仅针对上肢模仿，下肢假设由内置平衡控制器维持，未实现全身协同模仿。
*   **人体模型简化**：将人类简化为静态胶囊体（在单个控制步内），未考虑人类运动的预测，在高速互动中可能存在风险。

（完）
