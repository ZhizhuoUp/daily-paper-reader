---
title: Accelerating trajectory optimization with Sobolev-trained diffusion policies
title_zh: 利用基于 Sobolev 训练的扩散策略加速轨迹优化
authors: "Théotime Le Hellard, Franki Nguimatsia Tiofack, Quentin Le Lidec, Justin Carpentier"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19011v1"
tags: ["query:q3"]
score: 9.0
evidence: 利用学习到的扩散策略加速轨迹优化求解器
tldr: 针对轨迹优化（TO）求解速度慢且依赖初始值的问题，本文提出了一种利用Sobolev训练的扩散策略进行预热启动的方法。通过结合TO求解器提供的反馈增益，在扩散模型训练中引入一阶导数信息，有效解决了模仿学习中的复合误差问题。实验表明，该策略仅需少量数据即可显著提升TO收敛速度（2-20倍），并降低推理延迟。
selection_source: fresh_fetch
motivation: 传统的轨迹优化求解器对初始值敏感且计算耗时，而现有的模仿学习策略在预热启动时容易因复合误差导致性能下降。
method: 提出一种基于Sobolev学习的扩散策略，利用梯度下降TO求解器提供的轨迹和反馈增益来训练模型的一阶导数信息。
result: 该方法能以极少的数据量训练出高质量策略，将轨迹优化的求解时间缩短了2至20倍，并显著降低了扩散推理步数。
conclusion: 结合一阶信息的Sobolev扩散策略能有效提升轨迹优化的效率和鲁棒性，是加速复杂机器人任务求解的有效手段。
---

## 摘要
轨迹优化 (TO) 求解器利用已知的系统动力学，通过迭代改进来计算局部最优轨迹。其缺点是每个新的问题实例都是独立求解的；因此，收敛速度和所获解的质量取决于所提出的初始轨迹。为了提高效率，一种自然的方法是使用学习到的策略生成的初始猜测来热启动 TO，该策略是在求解器先前生成的轨迹上训练的。基于扩散的策略最近作为具有表现力的模仿学习模型出现，使其成为这一角色的有力候选者。然而，一个违反直觉的挑战来自于 TO 演示的局部最优性：当策略展开时，微小的非最优偏差可能会将其推入训练数据中未呈现的情境，从而在长时域内引发复合误差。在这项工作中，我们专注于针对同时提供反馈增益的基于梯度的 TO 求解器的基于学习的热启动。利用这一特性，我们推导出了一个一阶损失函数，用于结合轨迹和反馈增益对基于扩散的策略进行 Sobolev 学习。通过全面的实验，我们证明了所得策略避免了复合误差，因此可以从极少数轨迹中学习，以提供初始猜测，从而将求解时间缩短 2 到 20 倍。结合一阶信息使得能够以更少的扩散步骤进行预测，从而降低了推理延迟。

## Abstract
Trajectory Optimization (TO) solvers exploit known system dynamics to compute locally optimal trajectories through iterative improvements. A downside is that each new problem instance is solved independently; therefore, convergence speed and quality of the solution found depend on the initial trajectory proposed. To improve efficiency, a natural approach is to warm-start TO with initial guesses produced by a learned policy trained on trajectories previously generated by the solver. Diffusion-based policies have recently emerged as expressive imitation learning models, making them promising candidates for this role. Yet, a counterintuitive challenge comes from the local optimality of TO demonstrations: when a policy is rolled out, small non-optimal deviations may push it into situations not represented in the training data, triggering compounding errors over long horizons. In this work, we focus on learning-based warm-starting for gradient-based TO solvers that also provide feedback gains. Exploiting this specificity, we derive a first-order loss for Sobolev learning of diffusion-based policies using both trajectories and feedback gains. Through comprehensive experiments, we demonstrate that the resulting policy avoids compounding errors, and so can learn from very few trajectories to provide initial guesses reducing solving time by $2\times$ to $20 \times$. Incorporating first-order information enables predictions with fewer diffusion steps, reducing inference latency.

---

## 论文详细总结（自动生成）

这是一份关于论文《Accelerating trajectory optimization with Sobolev-trained diffusion policies》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：轨迹优化（Trajectory Optimization, TO）求解器虽然能利用动力学模型生成局部最优解，但其计算速度慢且高度依赖初始猜测（Initial Guess）。
*   **研究背景**：
    *   传统的模仿学习（Imitation Learning）常被用于为 TO 提供“热启动”初始值。
    *   **复合误差挑战**：TO 生成的演示数据是局部最优的，缺乏“如何从错误中恢复”的样本。当策略在执行中出现微小偏差时，会进入训练分布之外的状态，导致误差累积并最终失败。
    *   **扩散模型局限**：虽然扩散策略（Diffusion Policies）表达能力强，但通常需要海量数据（数万条轨迹）来覆盖状态空间以缓解复合误差。
*   **整体含义**：本文旨在通过引入一阶导数信息（Sobolev 学习），利用极少量的数据训练出鲁棒的扩散策略，从而显著加速 TO 求解器的收敛。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：利用基于梯度的 TO 求解器（如 iLQR, ProxDDP）在求解过程中产生的**反馈增益（Feedback Gains）**。这些增益本质上是控制量对状态的导数，包含了“如何修正偏差”的一阶信息。
*   **关键技术细节**：
    *   **Sobolev 扩散损失函数**：在标准扩散模型损失（重建轨迹 $\tau_0$）的基础上，增加了一个一阶项。该项要求模型预测的轨迹对初始状态的梯度，必须与 TO 求解器提供的反馈增益（通过链式法则传播）相匹配。
    *   **随机 Sobolev 训练**：为了避免计算高阶 Hessian 矩阵带来的巨大开销，采用随机投影法，将梯度投影到随机向量上进行匹配。
    *   **交替迭代算法（Interplay Loop）**：类似于 DAgger 算法，交替进行“数据采集”和“策略训练”。在采集阶段，使用当前策略为 TO 提供初始猜测，TO 优化后的结果再反馈给训练集。
    *   **推理加速**：由于引入了一阶信息，策略在推理时可以使用更少的扩散步数（K=5）和更长的预测时域，显著降低了延迟。

### 3. 实验设计
*   **实验场景**：
    1.  **倒立摆（单/双）**：测试力矩控制下的非线性摆动任务。
    2.  **UR5 机械臂**：设置了 4 种变体（固定/随机初始位姿，固定/随机目标点）。
    3.  **四旋翼无人机**：包括无障碍物导航和带有 9 个柱状障碍物的复杂避障任务。
*   **Benchmark 与对比方法**：
    *   **TO alone**：仅使用插值初始化的传统 TO 求解器。
    *   **MLP / Sob+MLP**：直接使用多层感知机及其 Sobolev 版本（PDDP+S）。
    *   **Diff**：标准的扩散策略（不含 Sobolev 项，类似于 DiffuSolve 的消融版）。
    *   **Sob+Diff**：本文提出的方法。

### 4. 资源与算力
*   **硬件环境**：所有实验均在一台笔记本电脑上完成。
    *   **CPU**：Intel Core i9-13950HX。
    *   **GPU**：Nvidia RTX 2000 ADA。
*   **训练时长**：文中未给出总训练小时数，但提到 Sobolev 训练单次 Epoch 的耗时约为标准扩散训练的 2 倍。由于所需轨迹极少（几十条），整体训练开销在个人工作站可控范围内。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 类机器人任务，共 8 个变体。
*   **充分性分析**：
    *   **样本效率实验**：测试了从 3 条到 100 条轨迹不同数据量下的表现，证明了 Sobolev 项的显著增益。
    *   **消融与灵敏度**：对训练 Epoch 数、预测时域（$T_h$）、动作长度（$T_a$）进行了详细的参数化研究。
    *   **统计客观性**：所有结果均基于 5 个随机种子取平均值，并给出了置信区间，实验设计较为客观、公平。

### 6. 主要结论与发现
*   **极高的样本效率**：Sob+Diff 仅需 5-10 条轨迹即可达到传统扩散模型需要数百条轨迹才能达到的性能。
*   **显著的加速效果**：相比于无热启动的 TO，该方法将求解时间缩短了 **2 倍至 20 倍**。
*   **鲁棒性增强**：Sobolev 训练使策略能够有效应对复合误差，即使在长时域预测（如 $T_a=63$）下也能保持稳定。
*   **避障能力**：在四旋翼避障任务中，Sob+Diff 能生成更平滑、更精准的轨迹，显著降低了碰撞风险。

### 7. 优点
*   **理论与实际结合**：巧妙地将控制理论中的反馈增益转化为深度学习中的 Sobolev 约束。
*   **解决痛点**：直接针对模仿学习在 TO 领域中“数据太完美导致不鲁棒”的悖论。
*   **实用性强**：推理步数极少（K=5），适合实时机器人控制系统的预热启动。

### 8. 不足与局限
*   **求解器依赖**：该方法依赖于能提供反馈增益的基于梯度的 TO 求解器（如 ProxDDP），对于非梯度类或黑盒优化器不适用。
*   **约束处理**：目前的 Sobolev 损失项中没有显式处理不等式约束（如避障），虽然实验显示其表现良好，但在严苛约束下的理论保证尚不明确。
*   **接触动力学**：实验主要集中在连续动力学系统，尚未在涉及频繁接触切换（如足式机器人行走）的复杂任务中验证。

（完）
