---
title: Multi-UAV Path Following using Vector-Field Guidance
title_zh: 基于向量场引导的多无人机路径跟踪
authors: "Gautam Kumar, Amit Shivam, Ashwini Ratnoo"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.17995v2"
tags: ["query:q1"]
score: 8.0
evidence: 多无人机路径跟随的分布式无碰撞框架
tldr: 本研究提出了一种去中心化的多无人机路径跟踪框架，旨在实现无碰撞的路径跟随并保持均匀间距。通过结合向量场引导律、基于相对距离和方位的旋转排斥机制以及速度控制律，该方法确保了无人机在向参考路径收敛过程中的安全性，并实现了沿路径的等间距分布。理论分析和数值仿真验证了该方案在避障和间距收敛方面的有效性。
selection_source: fresh_fetch
motivation: 旨在解决多无人机协同任务中，如何在确保避障安全的同时实现精确的路径跟随与均匀间距控制。
method: 利用向量场引导律进行路径驱动，引入旋转排斥机制实现避障，并基于间距误差调整速度以维持均匀分布。
result: 建立了避障和间距误差收敛至零的解析保证，并通过数值仿真展示了该方法的实际效能。
conclusion: 该去中心化框架为多无人机系统的协同路径跟随提供了一种具有理论保障且高效的控制方案。
---

## 摘要
本文提出了一种用于多无人机（UAV）路径跟踪引导的分布式、无碰撞框架，同时在参考路径上保持均匀间距。采用基于向量场的引导律驱动每架无人机趋向参考路径。提出了一种利用无人机间相对距离和方位的旋转排斥机制，以避免在趋向路径过程中的碰撞；并提出了一种基于无人机间距误差的速度控制律，以实现沿路径的均匀间隔。为避障和无人机间距误差收敛至零建立了理论保证，确保了沿路径的均匀分离。数值仿真证明了所提方法的有效性。

## Abstract
This paper presents a decentralized, collision-free framework for path following guidance of multiple uncrewed aerial vehicles (UAVs), while maintaining uniform spacing along a reference path. A vector field-based guidance law is employed to drive each UAV toward the reference path. A rotational repulsion mechanism, utilizing relative distance and bearing between UAVs, is proposed to avoid collisions during convergence to the path, and an inter-UAV spacing error-based velocity control law is presented to achieve uniform separation along the path. Analytical guarantees are established for collision avoidance and convergence of the inter-UAV spacing errors to zero, ensuring uniform separation along the path. Numerical simulations demonstrate the efficacy of the proposed method.

---

## 论文详细总结（自动生成）

### 论文总结：基于向量场引导的多无人机路径跟踪

#### 1. 论文的核心问题与整体含义
随着无人机（UAV）在物流、巡检等领域的广泛应用，如何在结构化空域中引导多架无人机沿预定义路径（如直线或正弦曲线等开曲线）飞行，同时确保**避障安全**和**均匀间距**，成为了一个关键挑战。
*   **研究动机**：现有的相位角协调法多适用于闭合路径（如圆周），而模型预测控制（MPC）等方法计算量巨大，难以扩展至大规模集群。
*   **核心问题**：设计一种去中心化、计算轻量且具有理论收敛保证的框架，使多架无人机能安全地收敛到同一条开路径上并保持等间距分布。

#### 2. 论文提出的方法论
该框架由三个核心模块组成，均采用去中心化设计：
*   **基于反正弦函数的向量场引导（Path Following）**：
    *   利用横向跟踪误差（Cross-track error）构建一个反正弦形状的偏移函数。
    *   通过结合局部路径切线方向和偏移函数，计算出期望航向角 $\psi_{des}$，引导无人机渐近收敛至目标路径。
*   **旋转排斥机制（Collision Avoidance）**：
    *   在角速度控制中引入排斥项 $\omega_{rep}$。该项基于邻近无人机间的相对距离和方位角。
    *   当两机距离小于安全阈值时，产生一个旋转力矩使无人机转向远离对方。论文通过解析证明，只要排斥增益 $k_r$ 足够大，即可保证无人机间距始终大于安全距离。
*   **基于间距误差的速度控制（Equispacing Control）**：
    *   采用“领航者-跟随者”链式拓扑，根据与前机的间距误差 $\Delta_i$ 调整纵向速度 $v_i$。
    *   使用 `tanh` 函数限制速度波动范围，确保速度始终为正且最终收敛至名义速度 $v_{nom}$，从而实现全队的等间距飞行。

#### 3. 实验设计
论文通过 **MATLAB 数值仿真** 验证了算法的有效性：
*   **实验场景**：
    1.  **直线路径跟踪**：参考路径为 $x=0$。
    2.  **正弦曲线路径跟踪**：参考路径为 $x = A \sin(ky)$。
*   **实验规模**：使用了 15 架无人机进行集群仿真。
*   **Benchmark/对比方法**：文中在引言部分对比了 MPC、Serret-Frenet 公式法和势场法等现有技术的优缺点，但在实验部分主要侧重于验证自身算法的收敛性和安全性，未直接与其他特定算法进行量化对比实验。

#### 4. 资源与算力
*   论文**未明确说明**具体的硬件算力（如 GPU/CPU 型号）或训练时长。
*   由于该方法基于解析控制律而非深度学习或复杂优化算法，其计算开销极低，通常在普通的个人电脑（PC）上即可实现实时仿真。

#### 5. 实验数量与充分性
*   **实验数量**：展示了两组代表性的仿真实验（直线和正弦曲线），每组实验均包含了 15 架无人机的轨迹、角速度、线速度、间距误差及最小间距随时间变化的曲线。
*   **充分性与客观性**：
    *   实验展示了从随机初始位置到最终路径对齐及等间距化的全过程，证明了算法在处理大规模集群时的鲁棒性。
    *   **局限性**：实验仅限于二维平面仿真，且未考虑传感器噪声、通信延迟或动态障碍物等复杂环境因素。

#### 6. 论文的主要结论与发现
*   **收敛性保证**：通过 Lyapunov 分析和 LaSalle 不变性原理，证明了横向跟踪误差和间距误差均能渐近收敛至零。
*   **安全性保证**：推导出了避障增益的临界值条件，确保在满足增益要求时不会发生碰撞。
*   **高效性**：该去中心化框架无需全局信息或复杂的在线优化，非常适合资源受限的小型无人机集群。

#### 7. 优点
*   **理论严谨**：不仅提出了算法，还提供了关于避障和间距收敛的解析证明。
*   **计算轻量**：相比 MPC 等方法，向量场和解析控制律对算力要求极低，易于嵌入式部署。
*   **扩展性强**：去中心化架构使得无人机数量的增减不会显著增加单个智能体的计算负担。

#### 8. 不足与局限
*   **环境假设过于理想**：假设环境无障碍物且通信完美（需实时获取邻机位置和速度），在实际复杂电磁环境下可能受限。
*   **路径类型限制**：虽然支持正弦曲线，但对于具有尖锐转角或非连续曲率的复杂路径，该向量场引导律的性能尚未得到验证。
*   **缺乏实物实验**：所有结论均基于数值仿真，缺乏在真实无人机硬件平台上的飞行验证。

（完）
