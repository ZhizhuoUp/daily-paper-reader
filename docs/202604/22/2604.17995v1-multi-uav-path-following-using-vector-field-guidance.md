---
title: Multi-UAV Path Following using Vector-Field Guidance
title_zh: 基于向量场引导的多无人机路径跟踪
authors: "Gautam Kumar, Amit Shivam, Ashwini Ratnoo"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.17995v1"
tags: ["query:q1"]
score: 8.0
evidence: 分布式无碰撞路径跟踪
tldr: 本研究针对多无人机（UAV）路径跟随任务，提出了一种去中心化且无碰撞的引导框架。该框架利用向量场引导律驱动无人机向参考路径收敛，并引入旋转排斥机制以避免收敛过程中的碰撞。同时，通过基于间距误差的速度控制律实现无人机在路径上的均匀分布。该方法提供了碰撞避免和间距收敛的理论保证，并通过仿真验证了其在多机协同任务中的有效性。
selection_source: fresh_fetch
motivation: 旨在解决多无人机在执行路径跟随任务时，如何实现去中心化的碰撞规避及在路径上的均匀间距保持。
method: 结合向量场引导律、基于相对距离和方位的旋转排斥机制以及间距误差反馈的速度控制律。
result: 理论分析证明了系统具备碰撞避免能力且间距误差可收敛至零，数值仿真进一步验证了方案的有效性。
conclusion: 该框架为多无人机协同路径跟随提供了一种可靠的去中心化解决方案，确保了任务执行的安全性和协同性。
---

## 摘要
本文提出了一种用于多无人机（UAV）路径跟踪引导的分散式、无碰撞框架，同时在参考路径上保持均匀间距。采用基于向量场的引导律驱动每架无人机向参考路径收敛。提出了一种利用无人机间相对距离和方位的旋转排斥机制，以避免在向路径收敛过程中的碰撞；并提出了一种基于无人机间距误差的速度控制律，以实现沿路径的均匀间隔。为避障和无人机间距误差收敛至零提供了理论保证，确保了沿路径的均匀分离。数值仿真证明了所提方法的有效性。

## Abstract
This paper presents a decentralized, collision-free framework for path following guidance of multiple uncrewed aerial vehicles (UAVs), while maintaining uniform spacing along a reference path. A vector field-based guidance law is employed to drive each UAV toward the reference path. A rotational repulsion mechanism, utilizing relative distance and bearing between UAVs, is proposed to avoid collisions during convergence to the path, and an inter-UAV spacing error-based velocity control law is presented to achieve uniform separation along the path. Analytical guarantees are established for collision avoidance and convergence of the inter-UAV spacing errors to zero, ensuring uniform separation along the path. Numerical simulations demonstrate the efficacy of the proposed method.

---

## 论文详细总结（自动生成）

这是一份关于论文《Multi-UAV Path Following using Vector-Field Guidance》的结构化技术总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：随着无人机（UAV）在物流、巡检等领域的广泛应用，如何在结构化空域（如预定义航路）中实现多机协同至关重要。
*   **核心问题**：现有的多机协同方法多针对闭合路径（如圆周运动），而对于**开放路径**（如直线、正弦曲线）的去中心化、无碰撞且保持均匀间距的路径跟踪研究相对较少。
*   **研究动机**：旨在开发一种计算量低、可扩展性强且具有理论安全保证的控制框架，使多架无人机能够收敛到同一条开放路径，并沿路径保持相等的间距，同时避免相互碰撞。

### 2. 论文提出的方法论
该框架由三个核心模块组成，均采用去中心化设计：
*   **基于反正弦函数的向量场引导（路径收敛）**：
    *   利用横向跟踪误差（Cross-track error）构建一个反正弦形状的偏移函数。
    *   该函数生成的期望航向角在远离路径时垂直于路径，在接近路径时平滑切入，最终与路径切线对齐。
*   **旋转排斥机制（碰撞规避）**：
    *   在角速度控制中引入排斥项。当两机距离小于安全阈值时，根据相对距离和方位角产生一个旋转力矩。
    *   **关键公式逻辑**：排斥角速度与距离的倒数相关（模拟势场梯度），并通过正弦函数决定转向方向（顺时针或逆时针），从而引导无人机相互绕开。
*   **基于间距误差的速度控制（均匀分布）**：
    *   每架无人机根据与前序机的弧长间距误差调整自身线速度。
    *   采用 `tanh` 饱和函数控制速度波动，确保速度始终在物理可行的范围内，并利用 LaSalle 不变性原理证明了间距误差会渐近收敛至零。

### 3. 实验设计
*   **实验场景**：在 MATLAB 环境下进行了数值仿真。
*   **测试路径**：
    1.  **直线路径** ($x=0$)。
    2.  **正弦曲线路径** ($x = A \sin(ky)$)。
*   **实验设置**：使用了 15 架无人机，初始位置随机分布在 $40m \times 40m$ 的区域内。
*   **对比与基准**：论文主要通过理论证明（Lyapunov 稳定性分析）作为支撑，仿真部分侧重于验证所提算法在多机环境下的有效性，未直接与其他 SOTA（如分布式 MPC）进行量化对比。

### 4. 资源与算力
*   **算力说明**：文中**未明确提到**具体的硬件型号（如 GPU/CPU）或训练时长。
*   **算法特性**：由于该方法基于解析式的向量场和简单的反馈控制律，不涉及高维优化问题的求解，因此其计算负担极低，理论上可在低功耗嵌入式飞控平台上实时运行。

### 5. 实验数量与充分性
*   **实验规模**：主要展示了两组代表性的仿真实验（直线和正弦曲线），每组实验包含 15 架无人机。
*   **充分性评价**：
    *   **理论充分性**：论文提供了详尽的数学证明，包括碰撞规避的充分条件和间距收敛的稳定性分析，这在控制类论文中具有较高的说服力。
    *   **仿真充分性**：仿真验证了从随机初始状态到最终形成等间距队列的全过程。但实验场景较为理想化，未考虑传感器噪声、通信延迟或动态障碍物等复杂干扰。

### 6. 论文的主要结论与发现
*   **收敛性**：所提向量场引导律能确保无人机从任何初始位置收敛至目标开放路径。
*   **安全性**：通过调整排斥增益，可以严格保证无人机之间的最小距离大于安全阈值，即使在向路径汇聚的动态过程中也不会发生碰撞。
*   **协同性**：速度控制律能有效消除间距误差，使无人机群在路径上形成稳定的“流水线”式交通流。

### 7. 优点
*   **去中心化与可扩展性**：每架无人机仅需邻居的相对位置信息，无需中央协调器，适合大规模集群。
*   **计算高效**：相比于模型预测控制（MPC），该方法无需迭代优化，响应速度快。
*   **数学严谨**：不仅给出了算法，还给出了碰撞规避和收敛性的解析保证。

### 8. 不足与局限
*   **维度限制**：目前的研究仅限于 2D 平面，未扩展到 3D 空间。
*   **环境假设**：假设环境为无障碍空间，未考虑静态障碍物规避。
*   **通信依赖**：算法假设无人机之间可以实时交换位置和速度信息，对通信链路的可靠性有一定依赖。
*   **路径类型**：虽然涵盖了直线和正弦线，但对于具有尖锐拐角或不连续曲率的复杂路径，向量场的平滑性可能受到影响。

（完）
