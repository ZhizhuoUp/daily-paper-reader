---
title: Tempered Sequential Monte Carlo for Trajectory and Policy Optimization with Differentiable Dynamics
title_zh: 基于可微动力学的轨迹与策略优化的退火顺序蒙特卡洛方法
authors: Heng Yang
date: 2026-04-23
pdf: "https://arxiv.org/pdf/2604.21456v1"
tags: ["query:q3"]
score: 8.0
evidence: 基于采样的轨迹与策略优化框架
tldr: 本研究针对可微动力学环境下的轨迹与策略优化问题，提出了一种基于采样的推理框架。通过最小化KL正则化的期望轨迹代价，将控制器设计转化为对玻尔兹曼分布的采样。核心方法是回火顺序蒙特卡洛（TSMC），结合了退火策略、自适应重采样以及利用梯度信息的哈密顿蒙特卡洛（HMC）更新。该方法在处理多峰分布和高维优化任务中表现出色，为强化学习和控制提供了高效的采样方案。
selection_source: fresh_fetch
motivation: 旨在解决在具有可微动力学的复杂环境中，如何高效地进行多峰分布下的轨迹和策略优化。
method: 提出回火顺序蒙特卡洛（TSMC）框架，利用退火机制和哈密顿蒙特卡洛（HMC）在粒子滤波过程中引入梯度信息。
result: 在多个轨迹和策略优化基准测试中，TSMC 展现了广泛的适用性，且性能优于现有的最先进基准方法。
conclusion: TSMC 成功地将基于梯度的优化与基于采样的推理相结合，为复杂动力学系统下的鲁棒控制提供了强有力的工具。
---

## 摘要
我们通过将控制器设计建模为推理问题，提出了一种在可微动力学下进行有限时界轨迹与策略优化的采样框架。具体而言，我们最小化 KL 正则化的期望轨迹代价，从而得到一个关于控制器参数的最优“玻尔兹曼倾斜”（Boltzmann-tilted）分布；随着温度降低，该分布会集中在低代价解上。为了从这种尖锐且可能存在多峰的目标分布中高效采样，我们引入了退火顺序蒙特卡洛（TSMC）：这是一种退火方案，它沿着从先验分布到目标分布的退火路径自适应地对粒子进行重加权和重采样，同时利用哈密顿蒙特卡洛（Hamiltonian Monte Carlo）刷新来保持多样性，并利用通过对轨迹展开进行微分获得的精确梯度。对于策略优化，我们通过以下方式扩展了 TSMC：(i) 初始状态分布的确定性经验近似，以及 (ii) 将展开随机性视为辅助变量的扩展空间构建。在轨迹和策略优化基准测试上的实验表明，TSMC 具有广泛的适用性，且其表现优于目前最先进的基准方法。

## Abstract
We propose a sampling-based framework for finite-horizon trajectory and policy optimization under differentiable dynamics by casting controller design as inference. Specifically, we minimize a KL-regularized expected trajectory cost, which yields an optimal "Boltzmann-tilted" distribution over controller parameters that concentrates on low-cost solutions as temperature decreases. To sample efficiently from this sharp, potentially multimodal target, we introduce tempered sequential Monte Carlo (TSMC): an annealing scheme that adaptively reweights and resamples particles along a tempering path from a prior to the target distribution, while using Hamiltonian Monte Carlo rejuvenation to maintain diversity and exploit exact gradients obtained by differentiating through trajectory rollouts. For policy optimization, we extend TSMC via (i) a deterministic empirical approximation of the initial-state distribution and (ii) an extended-space construction that treats rollout randomness as auxiliary variables. Experiments across trajectory- and policy-optimization benchmarks show that TSMC is broadly applicable and compares favorably to state-of-the-art baselines.

---

## 论文详细总结（自动生成）

以下是对论文《Tempered Sequential Monte Carlo for Trajectory and Policy Optimization with Differentiable Dynamics》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文探讨了在**可微动力学（Differentiable Dynamics）**环境下的有限时界轨迹优化（TO）与策略优化（PO）问题。
*   **研究动机**：传统的基于梯度的优化方法（如单次射击法）容易陷入局部最优，且在非光滑动力学中梯度信息可能失效；而基于采样的算法（如 MPPI）虽然探索性强，但采样效率低下。
*   **核心目标**：设计一种统一且原则性的算法，既能利用可微物理引擎提供的精确梯度进行局部精细搜索，又能通过采样机制进行全局探索，从而解决复杂、多峰（Multimodal）的控制问题。

### 2. 方法论：核心思想与关键技术
论文将控制器设计重新建模为一个**概率推理问题**。
*   **核心思想**：通过最小化 KL 正则化的期望轨迹代价，推导出最优控制器参数遵循**玻尔兹曼倾斜分布（Boltzmann-tilted distribution）**。优化问题由此转化为从该分布中采样的任务。
*   **关键技术：回火顺序蒙特卡洛（TSMC）**
    1.  **退火路径（Tempering Path）**：引入一系列中间分布，从简单的先验分布逐渐过渡到尖锐的目标分布，避免直接采样高难度目标。
    2.  **自适应重加权与重采样**：利用有效样本量（ESS）动态调整退火步长，通过重采样集中算力于低代价区域。
    3.  **HMC 刷新（Rejuvenation）**：使用**哈密顿蒙特卡洛（HMC）**在每一层退火中移动粒子。HMC 利用通过伴随方法（Adjoint Method）获得的轨迹梯度，实现高效的参数空间探索。
    4.  **策略优化扩展**：针对初始状态的随机性，提出了“确定性经验近似”和“扩展空间采样”两种变体，将随机性转化为辅助变量处理。

### 3. 实验设计
论文在轨迹优化（TO）和策略优化（PO）两个维度进行了广泛测试：
*   **轨迹优化场景**：倒立摆（Inverted Pendulum）、双节摆（Acrobot）、具有非光滑接触动力学的推块任务（Pusher-Slider）。
    *   **对比方法**：Parallel-NUTS、Parallel-MALA、Parallel-MPPI、Parallel-IPOPT。
*   **策略优化场景**：稀疏奖励倒立摆、双节摆、小车双极摆（Double Pendulum on a Cart）。
    *   **对比方法**：强化学习主流算法 PPO 和 SAC。
*   **基准参考**：在倒立摆任务中使用了半正定规划（SDP）产生的认证下界作为全局最优参考。

### 4. 资源与算力
*   **硬件环境**：Lambda 工作站，配备 AMD Ryzen Threadripper PRO 5975WX（32核）和 **2块 NVIDIA Ada6000 GPU**。
*   **软件框架**：基于 **JAX** 实现，利用其自动微分和 GPU 加速能力；采样部分使用了 BlackJAX 库。
*   **运行时间**：文中提供了详细的时间表。TSMC 在复杂任务（如 Pusher-Slider）中耗时约 2000 秒，虽然比 MPPI 慢，但远快于在复杂非光滑任务中失效的 IPOPT。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从简单经典控制到复杂接触动力学的 6 个主要基准任务。
*   **充分性**：实验设计较为充分。不仅对比了采样类方法，还对比了工业级非线性规划求解器（IPOPT）和主流强化学习算法（PPO/SAC）。
*   **客观性**：通过设置相同的初始粒子分布和计算预算，确保了对比的公平性。特别是在 Acrobot 任务中，将 PPO/SAC 的训练步数延长至 $10^9$ 步以验证其是否能追赶上 TSMC 的表现。

### 6. 主要结论与发现
*   **全局寻优能力**：TSMC 在多峰分布任务中表现卓越，能够同时发现多个全局最优解，而基准方法往往陷入局部极小值。
*   **梯度利用效率**：结合 HMC 后，TSMC 比纯采样方法（MPPI）更具样本效率，比纯梯度方法（IPOPT）更具鲁棒性。
*   **策略优化优势**：在 Acrobot 和双极摆等硬核控制任务中，TSMC 发现的策略优于经过长时间训练的 PPO 和 SAC。

### 7. 优点与亮点
*   **理论完备**：将控制问题转化为推理问题，并提供了 TSMC 的渐近一致性证明。
*   **处理非光滑性**：在 Pusher-Slider 任务中，通过 Gumbel-Softmax 技巧处理离散接触模式，展示了在可微物理环境下的强大适应力。
*   **统一框架**：一个算法同时适用于开环轨迹优化和闭环神经网络策略优化。

### 8. 不足与局限
*   **计算开销**：TSMC 需要维护大量粒子并进行多次轨迹微分，内存和计算需求较高，在大规模神经网络或极长轨迹任务中可能面临瓶颈。
*   **对可微性的依赖**：虽然能处理一定程度的非光滑，但仍高度依赖物理引擎提供高质量梯度的能力（如文中提到某些引擎梯度爆炸导致无法使用）。
*   **超参数敏感性**：退火路径的设置、HMC 的步长等参数仍需针对不同任务进行一定程度的调优。

（完）
