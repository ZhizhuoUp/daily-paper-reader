---
title: Open-Ended Instruction Realization with LLM-Enabled Multi-Planner Scheduling in Autonomous Vehicles
title_zh: 自动驾驶汽车中基于大语言模型赋能多规划器调度的开放式指令实现
authors: "Jiawei Liu, Xun Gong, Fen Fang, Muli Yang, Bohao Qu, Yunfeng Hu, Hong Chen, Xulei Yang, Qing Guo"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08031v1"
tags: ["query:q2"]
score: 8.0
evidence: 基于MPC的运动规划器和实时反馈控制
tldr: 本研究针对自动驾驶中乘客开放式指令难以转化为精确控制信号的问题，提出了一种基于大语言模型（LLM）的多规划器调度框架。该框架利用LLM解析指令并生成可执行脚本，通过调度多个基于模型预测控制（MPC）的运动规划器来实现车辆控制，实现了语义推理与底层控制的解耦。研究还引入了闭环评估基准，实验证明该方法在提升任务完成率的同时，确保了决策链的透明性、追溯性及安全性。
selection_source: fresh_fetch
motivation: 现有的自动驾驶交互研究缺乏将乘客开放式自然语言指令转化为可解释、可追溯且安全的车辆控制信号的有效机制。
method: 提出一种利用LLM生成脚本来调度多个MPC运动规划器的框架，通过解耦高层语义推理与底层实时控制实现指令执行。
result: 实验显示该框架显著提高了任务完成率并降低了LLM调用成本，同时在安全合规性及对推理延迟的容忍度上表现优异。
conclusion: 该研究通过多规划器调度设计，成功构建了一个透明且高效的自动驾驶指令实现系统，为复杂人机交互场景提供了新思路。
---

## 摘要
大多数人机交互（HMI）研究忽视了自动驾驶（AD）中乘客的操控需求。自然语言提供了一种直观的界面，但在不牺牲可解释性和可追溯性的前提下，将乘客的开放式指令转化为控制信号仍是一项挑战。本研究提出了一种指令实现框架，该框架利用大语言模型（LLM）解析指令，生成可执行脚本以根据实时反馈调度多个基于模型预测控制（MPC）的运动规划器，并将规划轨迹转化为控制信号。这种以调度为中心的设计在不同时间尺度上实现了语义推理与车辆控制的解耦，建立了从高层指令到底层动作的透明且可追溯的决策链。针对高保真评估工具缺失的问题，本研究引入了一个闭环环境下的开放式指令实现基准。综合实验表明，该框架相比指令实现基准显著提高了任务完成率，降低了 LLM 查询成本，在安全性和合规性方面达到了专业自动驾驶方法的水平，并对 LLM 推理延迟表现出相当的容忍度。欲了解更多定性演示和更清晰的理解。

## Abstract
Most Human-Machine Interaction (HMI) research overlooks the maneuvering needs of passengers in autonomous driving (AD). Natural language offers an intuitive interface, yet translating passenger open-ended instructions into control signals, without sacrificing interpretability and traceability, remains a challenge. This study proposes an instruction-realization framework that leverages a large language model (LLM) to interpret instructions, generates executable scripts that schedule multiple model predictive control (MPC)-based motion planners based on real-time feedback, and converts planned trajectories into control signals. This scheduling-centric design decouples semantic reasoning from vehicle control at different timescales, establishing a transparent, traceable decision-making chain from high-level instructions to low-level actions. Due to the absence of high-fidelity evaluation tools, this study introduces a benchmark for open-ended instruction realization in a closed-loop setting. Comprehensive experiments reveal that the framework significantly improves task-completion rates over instruction-realization baselines, reduces LLM query costs, achieves safety and compliance on par with specialized AD approaches, and exhibits considerable tolerance to LLM inference latency. For more qualitative illustrations and a clearer understanding.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为“以调度为中心”的框架，旨在解决自动驾驶汽车（AV）如何理解并执行乘客提出的开放式、非标准化自然语言指令的问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究背景
*   **研究动机**：随着自动驾驶向 L4/L5 级（如 Robotaxi）演进，HMI（人机交互）的重心从驾驶员转向了乘客。现有的交互系统多局限于信息娱乐或简单的导航指令，难以处理如“我觉得不安全”或“在前面商店停一下”这类**开放式（Open-Ended）**且涉及**具体操控（Maneuver-level）**的指令。
*   **核心挑战**：
    *   如何准确解析多样化、含糊的自然语言。
    *   如何在动态交通流中协调多个驾驶行为（如减速、换道、停车）。
    *   如何在引入大语言模型（LLM）的同时，确保决策链的**透明性、可追溯性**以及符合安全标准（如 ISO 26262）。

### 2. 方法论
该论文提出了一种**以调度为中心（Scheduling-Centric）的框架**，核心思想是将高层语义推理与底层实时控制解耦，分为三个阶段：
*   **阶段 1：指令理解（语义层）**：利用 LLM 作为解释器，结合当前结构化的交通场景描述（如车道信息、周围车辆状态），将开放式指令映射为一组有序的**原子驾驶行为序列**（如：[减速, 右换道, 停车]）。
*   **阶段 2：调度脚本生成（逻辑层）**：LLM 生成一个可执行的 Python 风格脚本。该脚本不仅包含行为序列，还包含**异步触发条件（Triggers）**。这些触发器利用协程机制监控实时环境反馈（如“当右侧间隙大于 20 米时触发换道”），从而实现动态适配。
*   **阶段 3：闭环规划执行（控制层）**：
    *   脚本调度预定义的**基于 MPC（模型预测控制）的运动规划器**。
    *   每个原子行为（换道、跟车等）都有专门的 MPC 规划器生成轨迹，再由 LQR 控制器转化为底层的油门、刹车和转向信号。
*   **关键技术细节**：论文对比了三种 LLM 驱动模式，最终采用 **Mode-III（混合调度）**，即单次 LLM 调用生成全局调度逻辑，由底层快速反馈循环（Schedule-Plan-Control Loop）处理高频安全响应。

### 3. 实验设计
*   **基准测试（POINT Benchmark）**：由于缺乏现成工具，作者基于 **nuPlan** 模拟器构建了 POINT 基准，包含 **1,050 个指令-场景对**，涵盖了 70% 以上的高风险侧向操控（如换道、超车）。
*   **评估指标**：
    *   **任务维度**：意图识别率、指令实现率（REA）。
    *   **安全维度**：碰撞率、最小碰撞时间（TTC）。
    *   **合规维度**：可行驶区域比例、限速遵守率、方向一致性。
*   **对比方法**：
    *   **专业 AD 方法**：LogReplay（专家回放）、IDM（规则跟车）、PDM（SOTA 规划器）。
    *   **指令实现方法**：Diffusion-ES（基于扩散模型）、DiLu++（基于 LLM 的动态决策）。

### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **Intel Xeon Gold 5220 CPU** 和 **NVIDIA A40 GPU** 的工作站上运行。
*   **模型使用**：测试了多种开源 LLM 家族，包括 **Qwen-2.5 (7B/72B)**、**DeepSeek-V3** 和 **DeepSeek-R1**。
*   **训练时长**：文中未提及具体的模型训练时长，因为该框架主要利用预训练 LLM 的推理能力和上下文学习（In-context Learning），而非从头训练。

### 5. 实验数量与充分性
*   **实验规模**：使用了 1,050 组场景进行闭环仿真，规模较大。
*   **消融实验**：针对“交通上下文（Context）”和“规划器调度（Scheduling）”进行了消融，验证了各组件对提升识别准确率和任务完成率的贡献。
*   **延迟敏感性分析**：专门测试了 LLM 推理延迟（0-4 秒）对系统性能的影响，证明了该解耦架构对延迟具有较强的容忍度。
*   **客观性**：通过使用第三方商业模型生成指令、开源模型进行评估的方式，减少了模型偏见。

### 6. 主要结论与发现
*   **LLM 规模至关重要**：只有 70B 以上参数规模的模型（如 DeepSeek-V3）在意图识别上能超过 85%，说明理解开放式指令具有挑战性。
*   **性能领先**：该框架在指令实现率（0.84）上比基准方法提升了 **64% 至 200%**。
*   **安全性卓越**：尽管执行的是乘客给出的高风险指令，但其安全性指标（碰撞率等）与不听从指令的专业自动驾驶规划器（如 PDM）持平。
*   **鲁棒性**：由于底层 MPC 循环独立运行，即使 LLM 推理缓慢，车辆仍能保持基本的安全避障。

### 7. 优点与亮点
*   **解耦设计**：巧妙地解决了 LLM “幻觉”和推理延迟与自动驾驶“实时性”要求之间的矛盾。
*   **可追溯性**：生成的调度脚本是人类可读的，方便开发者调试和安全审计。
*   **POINT 基准**：填补了自动驾驶领域缺乏“开放式指令-闭环仿真”测试集的空白。
*   **低成本**：单次 LLM 查询即可完成复杂任务调度，相比频繁调用的动态决策模式更具商业可行性。

### 8. 不足与局限
*   **文本瓶颈**：将复杂的 3D 交通场景转化为纯文本描述会丢失细节（如精确的道路几何形状），可能影响 LLM 的判断。
*   **原子行为限制**：系统的灵活性受限于预定义的原子规划器库，虽然可以通过组合实现复杂行为，但无法处理库之外的极端特殊动作。
*   **视觉感知缺失**：目前主要依赖结构化数据，未直接集成视觉语言模型（VLM）来处理更丰富的视觉语义（如路边行人的手势）。
*   **模拟器局限**：nuPlan 缺乏第一视角图像渲染，限制了端到端视觉-语言-动作（VLA）模型的深度对比。

（完）
