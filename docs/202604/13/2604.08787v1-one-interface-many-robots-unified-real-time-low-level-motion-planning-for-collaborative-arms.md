---
title: "One Interface, Many Robots: Unified Real-Time Low-Level Motion Planning for Collaborative Arms"
title_zh: 一个接口，多种机器人：协作机械臂的统一实时底层运动规划
authors: "Yue Feng, Weicheng Huang, I-Ming Chen"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08787v1"
tags: ["query:profile-1"]
score: 9.0
evidence: 基于二次规划的协作机械臂实时底层运动规划
tldr: 该研究针对异构协作机器人平台，提出了一种统一的实时底层运动规划接口。基于WinGs操作系统（WOS），通过结合n阶多项式插值器与二次规划（QP）求解器，实现了末端执行器平滑且连续可微的轨迹控制。该方法在离线绘图、动态抓取和远程操作等多种场景下验证了其通用性、可靠性及易用性，显著提升了机器人软件的可移植性。
selection_source: fresh_fetch
motivation: 旨在解决异构机器人硬件平台之间底层运动规划接口不统一、开发可移植性差的问题。
method: 采用n阶多项式插值器结合二次规划求解器，生成具有精确位置、速度和加速度剖面的平滑轨迹。
result: 在几何绘图、动态物体抓取及远程协作操作三个实验场景中，验证了该接口的精确性与实时性。
conclusion: 该统一接口为协作机器人提供了灵活且高效的实时控制方案，显著增强了跨平台应用的开发效率。
---

## 摘要
本文提出了一种用于协作机械臂实时底层运动规划的通用接口，旨在增强其在异构硬件平台上的适用性并提高可移植性。在之前的工作中，我们介绍了 WinGs Operating Studio (WOS)，这是一种将各种机器人组件抽象为统一软件资源并提供一系列语言无关 API 的中间件解决方案。本文重点讨论其用于实时末端执行器轨迹控制的极简且灵活的接口。通过结合使用 n 次多项式插值器与二次规划求解器，该方法能够生成具有精确位置、速度和加速度剖面的平滑且连续可微的轨迹。我们在三个不同的场景中验证了该方法。首先，在离线演示中，协作机械臂在纸上精确绘制了各种几何形状。其次，在可中断的低频重规划设置中，机械臂抓取了放置在移动机器人上的动态物体。最后，我们进行了一项遥操作实验，由一个机械臂控制另一个机械臂执行一系列灵巧操作，验证了所提方法的可靠性、通用性和易用性。

## Abstract
This paper proposes a common interface for real-time low-level motion planning of collaborative robotic arms, aimed at enabling broader applicability and improved portability across heterogeneous hardware platforms. In previous work, we introduced WinGs Operating Studio (WOS), a middleware solution that abstracts diverse robotic components into uniform software resources and provides a broad suite of language-agnostic APIs. This paper specifically focuses on its minimal yet flexible interface for real-time end-effector trajectory control. By employing an n-degree polynomial interpolator in conjunction with a quadratic programming solver, the proposed method generates smooth, continuously differentiable trajectories with precise position, velocity, and acceleration profiles. We validate our approach in three distinct scenarios. First, in an offline demonstration, a collaborative arm accurately draws various geometric shapes on paper. Second, in an interruptible, low-frequency re-planning setting, a robotic manipulator grasps a dynamic object placed on a moving mobile robot. Finally, we conducted a teleoperation experiment in which one robotic arm controlled another to perform a series of dexterous manipulations, confirming the proposed method's reliability, versatility, and ease of use.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种用于协作机械臂的统一实时底层运动规划接口。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：协作机械臂在工业和服务领域应用广泛，但面临**硬件异构性高**、**软件接口不统一**以及**实时响应性差**等挑战。现有的框架（如 ROS MoveIt）多侧重于离线路径优化，在处理高频实时控制、动态环境下的中断与重规划时存在局限。
*   **研究动机**：旨在开发一种轻量级、跨平台且语言无关的统一接口，使开发者无需针对特定硬件编写底层代码，即可实现平滑、连续且实时的末端执行器轨迹控制。

### 2. 论文提出的方法论
*   **核心思想**：在 WinGs Operating Studio (WOS) 中间件基础上，构建一个从笛卡尔空间路标点到关节空间平滑轨迹的自动化流水线。
*   **关键技术细节**：
    *   **逆运动学（IK）转换**：将用户输入的笛卡尔路标点（位置+时间戳）通过通用 IK 求解器（如 TRAC-IK）转换为关节空间目标。
    *   **n 阶多项式插值**：在关节空间采用 $n$ 阶多项式（实验中使用 5 阶）对相邻路标点进行拟合，确保轨迹在位置、速度和加速度上连续可微。
    *   **二次规划（QP）优化**：建立优化模型，以**最小化加加速度（Jerk）**为目标函数，同时将初始状态、目标状态、段间连续性以及硬件的物理极限（速度、加速度限制）作为约束条件。
    *   **实时调度**：采用双线程架构，一个线程负责接收指令并求解 QP，另一个线程以固定频率（如 100Hz）向硬件发送插值后的参考点。

### 3. 实验设计
论文设计了三个代表性场景来验证接口的通用性和性能：
*   **离线运动规划（几何绘图）**：使用 Franka FR3 机械臂在纸上绘制直线和圆形，验证多项式插值在笛卡尔空间下的几何保真度。
*   **可中断低频重规划（动态抓取）**：机械臂根据双相机视觉反馈，实时追踪并抓取移动机器人上的橙子。该实验验证了接口在目标动态变化时中断旧路径并平滑切换新路径的能力。
*   **高频实时控制（双臂遥操作）**：操作员通过 Lite6 机械臂控制远端的 FR3 机械臂执行开抽屉、取印章、盖章等复杂任务，验证了接口在缓冲流模式下的实时性和鲁棒性。
*   **基准对比**：在计算性能测试中，对比了不同算力平台（Jetson Nano, Raspberry Pi 5, Desktop）处理 QP 问题的耗时。

### 4. 资源与算力
*   **硬件平台**：
    *   **嵌入式端**：NVIDIA Jetson Nano (1.4 GHz CPU), Raspberry Pi 5 (2.4 GHz CPU)。
    *   **桌面端**：12核 4.5 GHz CPU。
*   **软件工具**：使用 OSQP 求解器处理二次规划问题。
*   **算力需求**：论文未涉及深度学习训练，因此无 GPU 训练时长。实验重点在于**推理/求解实时性**：桌面端求解 6 自由度 QP 约需 0.5ms，树莓派 5 约需 0.7ms，Jetson Nano 约需 2.1ms，均满足百赫兹级的实时控制需求。

### 5. 实验数量与充分性
*   **实验规模**：共进行了 1 组计算性能评估和 3 组功能性应用实验。
*   **充分性评价**：实验覆盖了从静态到动态、从自主到遥操作的多种典型机器人任务，具有较好的代表性。
*   **客观性**：通过编码器反馈的实际轨迹与规划轨迹的对比图（如 Fig. 5, 6, 8, 9）直观展示了误差和连续性，实验结果较为客观。但未与 MoveIt Servo 等其他实时控制框架进行直接的精度或延迟对比。

### 6. 论文的主要结论与发现
*   **统一性**：提出的 `rt-move-cartesian` 接口能有效屏蔽不同机械臂的硬件差异。
*   **平滑性**：基于 QP 的 Jerk 最小化方法能生成极其平滑的轨迹，减少了机械磨损。
*   **实时性**：即使在低功耗嵌入式设备上，该算法也能在毫秒级内完成复杂轨迹的重规划。
*   **易用性**：通过简单的路标点列表即可实现复杂的动态交互任务。

### 7. 优点
*   **高度抽象**：将复杂的底层控制逻辑封装为简单的 API，降低了开发门槛。
*   **数学严谨性**：结合多项式插值与 QP 优化，在保证物理约束的同时实现了运动学最优。
*   **跨平台兼容**：WOS 的架构使其能够运行在从 Android 到 PC 的多种设备上。

### 8. 不足与局限
*   **精度限制**：由于是在关节空间进行多项式插值，在笛卡尔空间可能会产生微小的几何偏差，不适用于微米级精度的加工任务。
*   **遥操作延迟**：在遥操作模式下，为了保证平滑性引入了约 200ms 的固定缓冲区延迟，这在极高动态的交互中可能成为瓶颈。
*   **避障缺失**：该底层规划接口目前主要关注轨迹平滑性，未直接集成实时避障功能（需高层规划器配合）。

（完）
