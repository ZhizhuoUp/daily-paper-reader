---
title: Scale-Invariant Sampling in Multi-Arm Bandit Motion Planning for Object Extraction
title_zh: 物体提取中基于多臂老虎机运动规划的尺度不变采样
authors: "Servet B. Bayraktar, Andreas Orthey, Marc Toussaint"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.14026v1"
tags: ["query:q4"]
score: 8.0
evidence: 针对狭窄空间物体提取的采样运动规划
tldr: 针对拆卸任务中狭窄空间的物体提取难题，本文提出一种尺度不变采样策略。该方法通过增减搜索寻找高熵采样尺度，并利用主成分分析（PCA）确定提取方向，将其集成至多臂老虎机快速探索随机树（MAB-RRT）中。实验表明，该方法在多种复杂3D提取场景下，成功率较传统及现代采样策略提升了一个数量级，显著增强了运动规划在精密拆卸任务中的性能。
selection_source: fresh_fetch
motivation: 传统采样规划器在处理毫米级间隙的物体提取任务时，因碰撞率极高而面临严重的采样瓶颈。
method: 提出一种尺度不变采样策略，结合增减搜索确定有效尺度并利用PCA寻找提取方向，最后将其嵌入MAB-RRT框架。
result: 在8个挑战性3D提取场景中，该方法在其中7个场景下的成功率比现有主流算法提高了一个数量级。
conclusion: 尺度不变采样是解决通用物体提取和拆卸任务中运动规划问题的关键技术。
---

## 摘要
物体提取任务常出现在拆卸问题中，例如必须从狭窄紧凑的空间中移除螺栓、螺钉或销钉。在这类问题中，与环境的距离通常在毫米量级。基于采样的规划器可以解决此类问题并提供完备性保证。然而，采样成为了瓶颈，因为几乎所有的运动都会导致与环境发生碰撞。为解决这一问题，我们提出了一种新颖的尺度不变采样策略，该策略利用增长-收缩搜索（grow-shrink search）探索配置空间，以寻找有用的、高熵的采样尺度。一旦找到有用的采样尺度，我们的框架就会利用主成分分析（PCA）来寻找物体提取的有效方向。我们将该采样器嵌入到多臂老虎机快速探索随机树（MAB-RRT）规划器中，并在涉及螺栓、齿轮、连杆、销钉和插座的八个具有挑战性的 3D 物体提取场景中进行了测试。为了评估我们的框架，我们将其与均匀采样、基于障碍物的采样和窄通道采样等经典采样策略，以及配合向量（mate vectors）、基于物理的规划和拆卸广度优先搜索等现代策略进行了比较。实验结果表明，在 8 个场景中的 7 个场景里，尺度不变采样将成功率提高了一个数量级。这表明尺度不变采样是拆卸任务中通用物体提取的一个重要概念。

## Abstract
Object extraction tasks often occur in disassembly problems, where bolts, screws, or pins have to be removed from tight, narrow spaces. In such problems, the distance to the environment is often on the millimeter scale. Sampling-based planners can solve such problems and provide completeness guarantees. However, sampling becomes a bottleneck, since almost all motions will result in collisions with the environment. To overcome this problem, we propose a novel scale-invariant sampling strategy which explores the configuration space using a grow-shrink search to find useful, high-entropy sampling scales. Once a useful sampling scale has been found, our framework exploits this scale by using a principal components analysis (PCA) to find useful directions for object extraction. We embed this sampler into a multi-arm bandit rapidly-exploring random tree (MAB-RRT) planner and test it on eight challenging 3D object extraction scenarios, involving bolts, gears, rods, pins, and sockets. To evaluate our framework, we compare it with classical sampling strategies like uniform sampling, obstacle-based sampling, and narrow-passage sampling, and with modern strategies like mate vectors, physics-based planning, and disassembly breadth first search. Our experiments show that scale-invariant sampling improves success rate by one order of magnitude on 7 out of 8 scenarios. This demonstrates that scale-invariant sampling is an important concept for general purpose object extraction in disassembly tasks.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种针对机器人拆卸任务中“物体提取”难题的创新运动规划方法。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
在自动化回收、维修和再制造领域，机器人需要从紧凑、狭窄的空间中提取螺栓、销钉或齿轮等零件。这类任务的挑战在于零件与环境之间的间隙通常在毫米量级。
*   **传统瓶颈**：基于采样的规划器（如 RRT）在处理此类问题时效率极低，因为在极小间隙内，随机采样点几乎都会发生碰撞，导致规划器无法找到有效路径。
*   **核心动机**：作者观察到“尺度（Scale）”是关键。采样半径过大会导致全碰撞，过小则进度缓慢。论文旨在寻找一个具有“高信息熵”的采样尺度（即约一半样本有效、一半无效的平衡点），并利用该尺度下的几何特征引导物体脱离。

### 2. 方法论：核心思想与关键技术
论文提出了一种**尺度不变采样策略**，并将其集成到**多臂老虎机 RRT (MAB-RRT)** 框架中：
*   **尺度采样器（探索阶段）**：采用“增长-收缩（Grow-Shrink）”搜索算法。通过动态调整采样半径 $r$，观察样本的有效率 $\alpha$。若有效率太低则缩小半径，太高则增大，直到找到使有效率处于目标区间（如 0.1 到 0.5）的半径 $r^*$。
*   **PCA 采样器（利用阶段）**：在找到的高熵尺度下，对有效样本进行主成分分析（PCA），提取出零件最容易移动的“逃逸方向”（第一主成分）。随后，沿着该轴线构建超圆柱体进行定向采样。
*   **MAB-RRT 集成**：使用多臂老虎机（UCB 策略）在三种采样器之间动态切换：1) 均匀采样；2) PCA 正向采样；3) PCA 负向采样。
*   **动态更新**：引入滑动窗口机制和在线重校准，使规划器能随着物体位置的变化实时调整逃逸方向和采样半径。

### 3. 实验设计
*   **实验场景**：设计了 8 个具有挑战性的 3D 物体提取场景，包括插座（Socket）、吊环螺栓（Eye Bolt）、U型螺栓、电机法兰、齿轮减速器、T型螺栓、环形螺栓和跨销连接器。
*   **对比基准（Benchmarks）**：
    *   **经典策略**：RRT 结合桥接采样（Bridge）、高斯采样（Gaussian）和基于障碍物的采样（OBP）。
    *   **现代/专业策略**：配合向量法（MateVec-TRRT）、基于物理的规划（BK-RRT）和拆卸广度优先搜索（BFS）。
*   **评估指标**：随时间变化的成功率（对数时间尺度）。

### 4. 资源与算力
*   **硬件环境**：笔记本电脑，Ubuntu 20.04，Intel Core i7-5820K CPU (3.30GHz, 12核)，32GB RAM。
*   **算力说明**：该方法属于几何/采样规划，不涉及深度学习训练，因此无需 GPU。实验主要关注在 100 秒超时限制内的计算效率。

### 5. 实验数量与充分性
*   **实验规模**：针对 8 个场景，每种算法独立运行 10 次。
*   **充分性评价**：实验覆盖了多种机械结构的拆卸逻辑，对比了从经典到最前沿的多种专业拆卸算法。通过对数坐标展示成功率，清晰地体现了算法在运行速度上的量级优势。实验设计客观、公平，且提供了开源代码和 2D 演示环境。

### 6. 主要结论与发现
*   **性能卓越**：MAB-RRT 在所有 8 个场景中均达到了 **100% 的成功率**。
*   **速度优势**：在 7 个场景中，该方法的运行速度比次优方法快了**一个数量级（10倍以上）**。
*   **鲁棒性**：增长-收缩算法对初始半径设置不敏感，能够可靠地收敛到有效尺度。
*   **自适应性**：多臂老虎机能有效识别何时该在狭窄通道内“钻营”（PCA 采样），何时该在开放空间“扩散”（均匀采样）。

### 7. 优点
*   **尺度自适应**：无需人工调参即可自动找到最适合当前间隙的采样密度。
*   **几何启发式**：利用 PCA 提取逃逸方向，比昂贵的物理模拟（如力导向法）更高效。
*   **理论完备**：保留了采样规划器的概率完备性。
*   **实用性强**：作为 OMPL 库的扩展实现，易于集成到现有的机器人规划系统中。

### 8. 不足与局限
*   **运动复杂度限制**：目前主要验证了平移为主的提取，对于需要复杂旋转（如螺纹旋出）或非线性极强的路径（如迷宫类）的验证尚不充分。
*   **收敛性证明**：虽然实验表现稳定，但缺乏关于“高熵尺度搜索”在任意几何形状下收敛性的严格数学证明。
*   **物理特性缺失**：未考虑物体的质量、摩擦力或可变形性（如电缆提取）。

（完）
