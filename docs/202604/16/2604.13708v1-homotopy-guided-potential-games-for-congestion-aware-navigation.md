---
title: Homotopy-Guided Potential Games for Congestion-Aware Navigation
title_zh: 用于拥塞感知导航的同伦引导势博弈
authors: "Mohammed Irshadh Ismaaeel Sathyamangalam Imran, Lasse Peters, Michael Khayyat, Stefano Arrigoni, Francesco Braghin, Laura Ferranti"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.13708v1"
tags: ["query:q1"]
score: 8.0
evidence: 具有碰撞和拥塞感知能力的多智能体运动规划
tldr: 针对多智能体导航中的拥堵和交互难题，本文提出一种同伦引导的势博弈框架。该方法将同伦类作为结构化策略集，结合收缩时域规划，利用同伦规划器生成拓扑各异的路径，并通过启发式过滤和势博弈求解广义纳什均衡。相比传统方法，该框架能有效探索不同拓扑路径，减少拥堵并提升安全性，在仿真和硬件实验中展现出更优的效率和对非理性行为的鲁棒性。
selection_source: fresh_fetch
motivation: 传统博弈规划器易陷入保守的拥堵均衡，而同伦规划器虽能探索多样路径却缺乏对智能体间未来动作依赖性的建模。
method: 提出一种将同伦类作为策略集的势博弈框架，通过启发式过滤候选路径并利用势博弈求解满足同伦约束的广义纳什均衡。
result: 仿真显示该方法在多智能体场景下比基准算法更高效、更安全，硬件实验证明其能灵活应对人类的非理性行为。
conclusion: 结合同伦拓扑与博弈论能显著提升多智能体在复杂拥堵环境下的导航性能和决策鲁棒性。
---

## 摘要
本文解决了交互、碰撞与拥塞并存的多智能体运动规划问题。传统的博弈论规划器虽然能捕捉智能体间的交互，但往往收敛于保守且拥塞的均衡。另一方面，同伦规划器能够探索拓扑上不同的路径，但缺乏考虑智能体未来动作相互依赖性的机制。我们提出了一个统一框架，在滚动时域（receding-horizon）设置中将同伦类作为结构化策略集。在每个规划阶段，确定性同伦规划器根据联合配置为每个智能体生成拓扑上不同的路径。为了避免候选路径的爆炸式增长，我们提出了一个简单的启发式过滤步骤，筛选出前 K 个最合适的无拥塞联合策略子集，以确保计算的可行性。这些策略作为势博弈（potential game）的初始化，该博弈强制执行同伦一致性约束并产生广义开环纳什均衡（OLNE），同时通过惩罚项抑制滚动时域设置中突发的策略转变。涉及三个智能体的仿真表明，与不考虑同伦的局部基准方法和 NH-ORCA 相比，该方法提高了效率（完成任务更快）并增强了安全性（智能体间间隙更大，从而减少了拥塞）。涉及两个机器人和一个人的硬件实验证明了该方法对非理性行为的鲁棒性：当基准博弈失效时，我们的方法能够通过切换到其他可行的均衡来实现自适应。

## Abstract
We address the multi-agent motion planning problem where interactions, collisions, and congestion co-exist. Conventional game-theoretic planners capture interactions among agents but often converge to conservative, congested equilibria. Homotopy planners, on the other hand, can explore topologically distinct paths, but lack mechanisms to account for the interdependence of agents' future actions. We propose a unified framework that leverages homotopy classes as structured strategy sets within a receding-horizon setup. At each planning stage, a deterministic homotopy planner generates topologically distinct paths for each agent, conditioned on the joint configuration. To avoid intractable growth of candidate paths, we propose a simple heuristic filtering step that selects a top-$K$ subset of the most suitable congestion-free joint strategies to ensure computational tractability. These serve as initializations for a potential game that enforces homotopy-consistent constraints and yields a generalized open-loop Nash equilibrium (OLNE), with penalties discouraging abrupt strategy shifts in a receding-horizon setting. Simulations with three agents demonstrate improved efficiency (faster completion) and enhanced safety (greater inter-agent clearance, leading to reduced congestion) compared to a local baseline and NH-ORCA that do not reason about homotopies. Hardware trials with two robots and one human demonstrate robustness to irrational behaviors, where our method adapts by switching to alternative feasible equilibria while the baseline game fails.

---

## 论文详细总结（自动生成）

这篇论文题为《Homotopy-Guided Potential Games for Congestion-Aware Navigation》（用于拥塞感知导航的同伦引导势博弈），由米兰理工大学和代尔夫特理工大学的研究人员合作完成。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：在多智能体（如机器人、自动驾驶汽车）协同导航中，传统的博弈论规划器（如纳什均衡求解）容易陷入局部最优，导致智能体行为过于保守或产生拥塞。而同伦规划（Homotopy Planning）虽然能探索不同拓扑路径（如从障碍物左侧或右侧绕行），但通常忽略了智能体之间未来的动作依赖性。
*   **核心问题**：如何结合同伦路径的多样性和博弈论的交互建模能力，使多智能体系统在复杂拥堵环境中既能安全避障，又能高效、非保守地达成全局目标。

### 2. 方法论
该论文提出了一个三阶段的统一框架：
*   **同伦规划器（Homotopy Planner）**：利用确定性节点采样和“H-签名”（H-signature）描述符，为每个智能体生成一组拓扑上互不相同的候选路径。它将其他智能体视为静态障碍物来构建初始拓扑图。
*   **启发式预过滤（Heuristic Prefiltering）**：由于联合同伦组合随智能体数量呈指数增长，论文设计了一个轻量级过滤器。根据路径长度、平滑度和邻近度（拥塞程度）对组合进行评分，仅保留前 $K$ 个最优组合，以确保计算可行性。
*   **同伦引导的势博弈（Potential Game）**：
    *   将筛选出的同伦路径作为势博弈的初始化。
    *   **核心公式**：构建一个全局势函数 $\Phi_h$，将所有智能体的目标函数聚合。通过求解广义开环纳什均衡（OLNE），确保解落在选定的同伦类内。
    *   **关键技术**：引入“同伦强制约束”（利用分离超平面确保智能体不偏离选定的拓扑路径）和“正则化惩罚项”（防止在滚动时域执行中出现剧烈的策略切换）。

### 3. 实验设计
*   **实验场景**：
    *   **仿真**：3至6个智能体的随机起始/目标点场景。
    *   **硬件验证**：使用两台 Clearpath Jackal 机器人和一名人类行人（模拟非理性行为）。
*   **对比基准（Benchmarks）**：
    1.  **NH-ORCA**：非圆型/非完整约束的优化倒数碰撞避免算法（经典的反应式避障）。
    2.  **基准势博弈（Baseline Potential Game）**：不具备同伦引导，仅基于直线参考路径的博弈规划器。
*   **评估指标**：路径长度、智能体间最小距离（安全性）、任务完成时间、计算耗时。

### 4. 资源与算力
*   **软件环境**：使用 `acados` 库配合 SQP（序列二次规划）求解器。
*   **硬件环境**：硬件实验采用 VICON 动捕系统提供定位，中央计算机通过 WebSockets 发送控制指令。
*   **算力说明**：文中未提及具体的 GPU 型号（此类优化算法通常运行在 CPU 上）。论文给出了详细的计算耗时表：3个智能体总耗时约 22-44ms，5个智能体约 168-232ms，6个智能体则显著增加至 1.2s 以上。

### 5. 实验数量与充分性
*   **实验规模**：针对3、4、5名玩家的场景分别进行了 **100次随机仿真**，并记录了预过滤器的排名分布。
*   **充分性评价**：实验设计较为充分。通过大规模随机仿真证明了统计学上的性能提升（效率与安全）；通过硬件实验验证了算法在真实物理环境和面对“非理性行为”（人类干扰）时的鲁棒性。对比了经典算法（ORCA）和消融了同伦模块的博弈算法，对比公平且客观。

### 6. 主要结论与发现
*   **效率提升**：相比基准博弈方法，该框架使任务完成时间缩短了约 9.41%。
*   **安全性增强**：智能体间的平均间隙增加了 22.8%，有效减少了局部拥塞。
*   **鲁棒性**：在面对非理性智能体（不按博弈均衡行事的人类）时，该方法能通过切换到备选同伦路径来维持可行解，而基准方法往往会因求解失败而崩溃。
*   **预过滤有效性**：实验证明前 $K$ 个候选组合（如 $N=5$ 时取 $K=22$）足以覆盖最优解，大幅降低了搜索空间。

### 7. 优点
*   **拓扑感知**：通过同伦类显式地划分搜索空间，解决了博弈论规划器容易陷入“死锁”或局部极小值的问题。
*   **拥塞预防**：启发式过滤步骤提前排除了潜在的拥堵路径组合。
*   **策略稳定性**：正则化项有效解决了滚动时域规划中常见的策略抖动问题。

### 8. 不足与局限
*   **可扩展性受限**：尽管有预过滤，但当智能体数量 $N > 6$ 时，同伦图的构建和组合搜索仍会出现组合爆炸，计算耗时达到秒级，难以满足高频实时性要求。
*   **简化假设**：同伦规划阶段将其他智能体视为静态，这在极高动态的环境下可能导致初始候选路径质量下降。
*   **观测要求**：依赖全状态观测，未考虑感知不确定性或通信延迟对博弈均衡的影响。

（完）
