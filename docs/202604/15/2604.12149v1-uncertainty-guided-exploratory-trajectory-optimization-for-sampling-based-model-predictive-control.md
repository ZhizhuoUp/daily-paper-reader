---
title: Uncertainty Guided Exploratory Trajectory Optimization for Sampling-Based Model Predictive Control
title_zh: 基于采样的模型预测控制的不确定性引导探索性轨迹优化
authors: "O. Goktug Poyrazoglu, Yukang Cao, Rahul Moorthy, Volkan Isler"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.12149v1"
tags: ["query:q3"]
score: 9.0
evidence: 用于MPC的不确定性引导探索性轨迹优化
tldr: 针对基于采样的模型预测控制（MPC）易陷入局部最优的问题，本文提出UGE-TO算法。该方法将轨迹表示为受不确定性椭球诱导的概率分布，不仅考虑动作空间，还结合系统动力学影响。通过Hellinger距离强制分布分离，增强了轨迹多样性和探索能力。实验表明，UGE-MPC在复杂环境下比基准方法收敛更快、成功率更高，并已在实物中验证。
selection_source: fresh_fetch
motivation: 传统的基于采样的轨迹优化方法受限于动作空间的探索，在复杂环境中容易陷入局部最优且对初始解高度敏感。
method: 提出UGE-TO算法，利用不确定性椭球将轨迹建模为概率分布，并通过Hellinger距离度量分布差异以引导系统化探索。
result: "在避障任务中，该方法比最优基准算法收敛速度提升达66%，成功率提高6.7%，并在真实世界实验中表现出更强的鲁棒性。"
conclusion: 结合系统动力学的不确定性引导探索能显著提升采样效率和避障性能，是解决复杂环境下轨迹优化问题的有效方案。
---

## 摘要
轨迹优化高度依赖于初始化。特别是，基于采样的方法对初始解高度敏感，且有限的探索往往导致它们在复杂环境中收敛至局部最优。我们提出了不确定性引导的探索性轨迹优化（UGE-TO），这是一种通过生成良好分离的样本以实现对配置空间更好覆盖的轨迹优化算法。UGE-TO 将轨迹表示为由不确定性椭球体诱导的概率分布。与仅在动作空间进行探索的采样方法不同，这种表示方法同时捕捉了系统动力学和动作选择的影响。通过将动力学影响（除动作空间外）纳入分布中，我们的方法通过利用 Hellinger 距离强制分布分离，从而增强了轨迹的多样性。它实现了对配置空间的系统性探索，并提高了针对局部最优的鲁棒性。此外，我们提出了 UGE-MPC，将 UGE-TO 集成到基于采样的模型预测控制器方法中。实验表明，在相同的采样预算下，与基准方法相比，UGE-MPC 在轨迹优化中实现了更高的探索率和更快的收敛速度；在无障碍环境中收敛速度提高了 72.1%，在复杂环境中收敛速度提高了 66%，且成功率比表现最好的基准方法高出 6.7%。此外，我们通过一系列仿真场景和真实世界实验验证了该方法。我们的结果表明，UGE-MPC 具有更高的成功率和更快的收敛速度，特别是在需要大幅偏离名义轨迹以避免失败的环境中。项目和代码可在 https://ogpoyrazoglu.github.io/cuniform_sampling/ 获取。

## Abstract
Trajectory optimization depends heavily on initialization. In particular, sampling-based approaches are highly sensitive to initial solutions, and limited exploration frequently leads them to converge to local minima in complex environments. We present Uncertainty Guided Exploratory Trajectory Optimization (UGE-TO), a trajectory optimization algorithm that generates well-separated samples to achieve a better coverage of the configuration space. UGE-TO represents trajectories as probability distributions induced by uncertainty ellipsoids. Unlike sampling-based approaches that explore only in the action space, this representation captures the effects of both system dynamics and action selection. By incorporating the impact of dynamics, in addition to the action space, into our distributions, our method enhances trajectory diversity by enforcing distributional separation via the Hellinger distance between them. It enables a systematic exploration of the configuration space and improves robustness against local minima. Further, we present UGE-MPC, which integrates UGE-TO into sampling-based model predictive controller methods. Experiments demonstrate that UGE-MPC achieves higher exploration and faster convergence in trajectory optimization compared to baselines under the same sampling budget, achieving 72.1% faster convergence in obstacle-free environments and 66% faster convergence with a 6.7% higher success rate in the cluttered environment compared to the best-performing baseline. Additionally, we validate the approach through a range of simulation scenarios and real-world experiments. Our results indicate that UGE-MPC has higher success rates and faster convergence, especially in environments that demand significant deviations from nominal trajectories to avoid failures. The project and code are available at https://ogpoyrazoglu.github.io/cuniform_sampling/.

---

## 论文详细总结（自动生成）

这是一份关于论文《Uncertainty Guided Exploratory Trajectory Optimization for Sampling-Based Model Predictive Control》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：基于采样的模型预测控制（如 MPPI）在复杂、多峰值的成本函数景观中，极易受到初始解的影响，且由于探索能力有限（通常仅在动作空间进行扰动），容易陷入局部最优解。
*   **研究背景**：传统的梯度方法依赖可微性，而采样方法虽能处理非光滑成本，但若名义轨迹（Nominal Trajectory）初始化不佳，采样分布会集中在低效区域，导致避障失败或收敛缓慢。

### 2. 核心方法论
论文提出了 **UGE-TO（不确定性引导的探索性轨迹优化）** 及其集成版本 **UGE-MPC**：
*   **轨迹分布建模**：不仅将轨迹视为状态序列，还利用不确定性椭球体将其表示为概率分布。通过线性化动力学方程，将过程噪声和动作扰动传播到整个时域，得到每个轨迹的均值和协方差矩阵。
*   **分布分离机制**：引入 **Hellinger 距离**（一种对称且有界的散度度量）来衡量不同轨迹分布之间的重叠程度。
*   **算法流程**：
    1.  在名义控制序列周围生成多个候选轨迹。
    2.  通过最小化分布重叠（最大化 Hellinger 距离）来强制轨迹在配置空间中“散开”，实现系统化探索。
    3.  从分离后的多样化轨迹中选择成本最低的作为新的名义轨迹。
    4.  **UGE-MPC 集成**：将 UGE-TO 作为 MPPI 的前端，先进行全局探索优化名义轨迹，再由 MPPI 进行局部精细搜索。

### 3. 实验设计
*   **实验场景**：
    1.  **仿真环境**：8x8m 的无障碍空间和复杂障碍物空间；20x20m 的未知复杂环境（含凹多边形陷阱）。
    2.  **真实世界**：使用 1/5 比例的无人卡车，配备 Slamtec R2 激光雷达，在未知环境中进行点对点导航。
*   **对比基准（Benchmarks）**：
    *   **MPPI**：标准的单峰高斯采样方法。
    *   **MoG-MPPI**：多高斯混合模型采样。
    *   **SV-MPC**：基于 Stein 变分梯度下降的方法，利用粒子间的排斥力增加多样性。
    *   **Log-MPPI**（仅实测对比）：利用 Normal-Log-Normal 分布增强探索。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 或 CPU 型号及训练时长。
*   **实时性表现**：提到控制器在实机实验中以 **10 Hz** 的频率运行，以匹配激光雷达的采样率。作者指出 SV-MPC 和 MoG-MPPI 由于计算复杂度较高，在相同硬件下难以稳定维持此频率，而 UGE-MPC 表现更优。

### 5. 实验数量与充分性
*   **实验规模**：在仿真中，每个起点-终点对进行了 **10 次重复实验**；在未知环境中进行了 5 组不同地图的测试；实机实验每种方法进行 5 次试验。
*   **充分性评价**：实验设计较为充分，涵盖了从简单到复杂的多种场景，并对比了当前主流的多模态采样 MPC 算法。通过固定所有方法的采样预算（2048 个样本）来确保对比的**公平性**。

### 6. 主要结论与发现
*   **收敛速度**：在无障碍环境中，UGE-MPC 的收敛速度比基准方法快 **72.1%**；在复杂环境中快 **66%**。
*   **成功率**：在复杂避障任务中，UGE-MPC 的成功率比表现最好的基准方法高出 **6.7%**。
*   **探索效能**：UGE-MPC 能够通过一次迭代就生成绕过障碍物的候选轨迹（如 180 度掉头），而 MPPI 等方法往往需要多次迭代或根本无法跳出局部最优。

### 7. 优点与亮点
*   **动力学感知探索**：不同于仅在动作空间扰动的方法，UGE-TO 考虑了系统动力学对状态分布的影响，使探索在配置空间中更具物理意义。
*   **分布分离指标**：使用 Hellinger 距离代替 KL 散度，避免了计算中的数值不稳定（无界问题），使轨迹分布的排斥更稳健。
*   **即插即用**：该方法可以作为现有采样 MPC 框架的增强模块，显著提升其在复杂环境下的鲁棒性。

### 8. 不足与局限
*   **线性化依赖**：协方差传播依赖于动力学的线性化（Jacobian 矩阵），对于高度非线性或不连续的动力学系统，预测准确性可能会下降。
*   **计算开销**：虽然比 SV-MPC 高效，但计算 Hellinger 距离和传播协方差矩阵仍比标准 MPPI 增加了一定计算负担。
*   **高维扩展性**：实验主要集中在 2D/3D 导航任务，虽然作者提到未来将尝试人形机器人，但在极高维动作空间（如多自由度机械臂）中的表现尚待验证。

（完）
