---
title: Whole-Body Mobile Manipulation using Offline Reinforcement Learning on Sub-optimal Controllers
title_zh: 基于次优控制器的离线强化学习全身移动操作
authors: "Snehal Jauhri, Vignesh Prasad, Georgia Chalvatzaki"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12509v1"
tags: ["query:profile-1"]
score: 8.0
evidence: 利用分层优化的全身移动操作
tldr: 针对移动操作（MoMa）中全身协调难、数据获取贵的问题，本文提出WHOLE-MoMa框架。该方法利用次优的全身控制器（WBC）作为结构先验生成多样化演示数据，随后通过改进的离线强化学习算法（结合Q-chunking的IQL）对行为进行优化与缝合。实验证明，该方法在模拟和真实机器人任务中均显著优于基准，且无需人类演示或真实数据即可实现高质量的零样本迁移。
selection_source: fresh_fetch
motivation: 传统的全身控制器调参复杂且脆弱，而学习类方法又极度依赖昂贵的遥操作数据或复杂的奖励设计。
method: 提出两阶段流程：先通过随机化轻量级WBC生成多样化演示，再利用带Q-chunking的离线隐式Q学习（IQL）训练扩散策略。
result: "在TIAGo++机器人上实现了零样本迁移，双臂抽屉操作成功率达80%，橱柜开启与放置成功率达68%。"
conclusion: 证明了利用次优控制器生成的合成数据配合离线强化学习，可以有效解决复杂的全身移动操作任务，并具备极强的实机泛化能力。
---

## 摘要
对关节物体（如门、抽屉和橱柜）的移动操作（MoMa）要求机器人的底座和机械臂之间进行同步的全身协调。经典的全身控制器（WBC）可以通过分层优化解决此类问题，但需要大量的手动调优，且鲁棒性较差。另一方面，基于学习的方法表现出强大的泛化能力，但通常依赖昂贵的全身遥操作数据或繁重的奖励工程。我们观察到，即使是次优的 WBC 也是一种强大的结构先验：它可以用于在受限的、与任务相关的状态-动作空间区域内收集数据，并且其行为仍可以通过离线强化学习得到改进。基于此，我们提出了 WHOLE-MoMa，这是一个两阶段流水线：首先通过随机化轻量级 WBC 生成多样化的演示数据，然后应用离线强化学习通过奖励信号识别并缝合改进的行为。为了支持复杂协调任务所需的高表达力动作分块扩散策略（action-chunked diffusion policies），我们扩展了离线隐式 Q 学习（IQL），引入了 Q-chunking 用于块级评论家（critic）评估和优势加权策略提取。在仿真环境中使用 TIAGo++ 移动操作机器人的三个难度递增的任务中，WHOLE-MoMa 的表现显著优于 WBC、行为克隆以及几种离线强化学习基准方法。策略无需微调即可直接迁移到真实机器人上，在双臂抽屉操作中实现了 80% 的成功率，在同步开启橱柜和放置物体中实现了 68% 的成功率，且所有过程均未使用任何遥操作或真实世界训练数据。

## Abstract
Mobile Manipulation (MoMa) of articulated objects, such as opening doors, drawers, and cupboards, demands simultaneous, whole-body coordination between a robot's base and arms. Classical whole-body controllers (WBCs) can solve such problems via hierarchical optimization, but require extensive hand-tuned optimization and remain brittle. Learning-based methods, on the other hand, show strong generalization capabilities but typically rely on expensive whole-body teleoperation data or heavy reward engineering. We observe that even a sub-optimal WBC is a powerful structural prior: it can be used to collect data in a constrained, task-relevant region of the state-action space, and its behavior can still be improved upon using offline reinforcement learning. Building on this, we propose WHOLE-MoMa, a two-stage pipeline that first generates diverse demonstrations by randomizing a lightweight WBC, and then applies offline RL to identify and stitch together improved behaviors via a reward signal. To support the expressive action-chunked diffusion policies needed for complex coordination tasks, we extend offline implicit Q-learning with Q-chunking for chunk-level critic evaluation and advantage-weighted policy extraction. On three tasks of increasing difficulty using a TIAGo++ mobile manipulator in simulation, WHOLE-MoMa significantly outperforms WBC, behavior cloning, and several offline RL baselines. Policies transfer directly to the real robot without finetuning, achieving 80% success in bimanual drawer manipulation and 68% in simultaneous cupboard opening and object placement, all without any teleoperated or real-world training data.

---

## 论文详细总结（自动生成）

这是一份关于论文《Whole-Body Mobile Manipulation using Offline Reinforcement Learning on Sub-optimal Controllers》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
移动操作（Mobile Manipulation, MoMa）要求机器人的底座与机械臂实现高度协调，尤其是在处理门、抽屉、橱柜等关节物体时。
*   **研究动机**：传统的全身控制器（WBC）依赖复杂的手动调优且在长程任务中表现脆弱；而现有的学习方法要么依赖昂贵的全身遥操作数据，要么在处理高维全身状态空间时面临“维度灾难”。
*   **核心思想**：论文提出 **WHOLE-MoMa** 框架，认为即使是**次优的 WBC** 也可以作为强大的结构先验。通过随机化 WBC 参数生成多样化的仿真演示数据，再利用离线强化学习（Offline RL）从这些次优数据中“缝合”并提取出更优的全身协调策略，从而摆脱对人类演示数据的依赖。

### 2. 方法论：核心思想与关键技术
该方法分为两个阶段：
*   **阶段一：基于 WBC 的数据生成**
    *   使用分层二次规划（HQP）构建轻量级 WBC。
    *   通过随机化 WBC 的任务权重、阈值和步长（如表 I 所示），在仿真中生成 3000 条包含成功与失败尝试的多样化轨迹。
*   **阶段二：带 Q-chunking 的离线强化学习**
    *   **Q-chunking 扩展**：为了适配扩散策略（Diffusion Policy）的动作分块（Action Chunking）特性，作者扩展了隐式 Q 学习（IQL）。评论家（Critic）不再评估单步动作，而是评估整个动作块 $a_{t:t+H-1}$，并使用块级 TD 误差进行训练。
    *   **策略提取**：采用优势加权回归（AWR）目标函数。利用训练好的 Q 函数计算动作块的优势值（Advantage），对扩散策略的训练损失进行加权，使策略倾向于学习高回报的行为。
    *   **架构**：使用 Transformer 结构的扩散策略和 Transformer 结构的评论家，以处理长序列的状态历史。

### 3. 实验设计
*   **实验平台**：TIAGo++ 移动操作机器人（双臂 + 全向底座）。
*   **仿真场景**：基于 Isaac Sim 和 GAPartNet 数据集设计了三个难度等级的任务：
    1.  **Door**（推门并穿过）：基础全身协调。
    2.  **Drawer**（双臂抽屉操作）：要求底座移动以配合一拉一推。
    3.  **Cupboard**（开启橱柜并放置物体）：最难任务，需持续的底座-双臂三者协调。
*   **对比基准（Benchmarks）**：
    *   **WBC-policy**：原始的手动调优控制器。
    *   **BC (Diffusion Policy)**：纯行为克隆。
    *   **RL (TD3)**：从零开始的在线强化学习。
    *   **离线 RL 变体**：IQL+DDPG BC、IDQL、RISE。

### 4. 资源与算力
*   **算力支持**：论文提到使用了埃尔朗根国家高性能计算中心（NHR）的资源。
*   **具体细节**：文中**未明确列出**具体的 GPU 型号、数量或总训练时长。但提到控制频率为 40Hz，且为了在实机上实时运行扩散策略，采用了异步推理方案（推理耗时约 80-100ms）。

### 5. 实验数量与充分性
*   **实验规模**：仿真中每个任务评估 50 个回合，实机测试每个任务评估 25 个回合。
*   **消融实验**：非常充分。包括了对策略架构（Transformer vs U-Net）、评论家架构（Transformer vs MLP）、Q-chunking 的必要性以及实机状态估计方法（位姿跟踪 vs 动捕标记）的对比。
*   **客观性**：对比了多种最先进的离线 RL 算法，并提供了 95% 置信区间，实验设计较为公正。

### 6. 主要结论与发现
*   **性能优越**：WHOLE-MoMa 在所有任务中均优于基准方法。在最难的橱柜任务中，仿真成功率达 78%，而在线 RL（TD3）完全无法学会（0%）。
*   **零样本迁移**：无需任何真实世界数据或微调，策略可直接迁移至实机。实机双臂抽屉任务成功率 80%，橱柜任务成功率 68%。
*   **行为缝合有效**：离线 RL 成功纠正了 WBC 在衔接阶段（如抓取到操作的过渡）的脆弱性，实现了比原始数据源更优的表现。

### 7. 优点与亮点
*   **无需遥操作**：完全通过仿真中的次优控制器生成数据，极大地降低了数据获取成本。
*   **Q-chunking 创新**：将动作分块与离线 IQL 结合，解决了扩散策略在强化学习框架下的评估对齐问题。
*   **全身协调能力**：展示了极高难度的双臂+底座同步操作能力，尤其是在动态开启橱柜的同时进行物体放置。

### 8. 不足与局限
*   **实机精度瓶颈**：实机成功率受限于视觉状态估计的精度。如果 6D 位姿跟踪出现微小漂移，策略容易在操作关节物体时停滞。
*   **动力学间隙**：实机中关节物体的物理特性（如摩擦力、柔性）与仿真存在差异，导致实机表现略低于仿真。
*   **依赖状态估计**：目前仍依赖显式的物体位姿跟踪，未来若能实现端到端的视觉输入将更具泛化性。

（完）
