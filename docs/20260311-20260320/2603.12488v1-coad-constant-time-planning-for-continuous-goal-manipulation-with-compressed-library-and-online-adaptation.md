---
title: "COAD: Constant-Time Planning for Continuous Goal Manipulation with Compressed Library and Online Adaptation"
title_zh: COAD：基于压缩库和在线自适应的连续目标操作常数时间规划
authors: "Adil Shiyas, Zhuoyun Zhong, Constantinos Chamzas"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.12488v1"
tags: ["query:profile-1"]
score: 9.0
evidence: 针对带障碍物的机器人操作进行恒定时间规划
tldr: COAD是一个针对连续目标参数化任务的常数时间运动规划框架。它通过将连续任务空间离散化为有限的任务覆盖区域，并仅存储代表性“根问题”的压缩库，解决了传统方法存储开销大且难以覆盖连续空间的问题。在线查询时，系统能以亚毫秒级速度检索并适配根路径，在保持高成功率的同时显著提升了规划效率和路径质量。
selection_source: fresh_fetch
motivation: 现有的基于经验的规划方法在处理连续任务空间时，面临存储开销巨大且难以保证全空间覆盖的挑战。
method: 该框架将任务空间划分为覆盖区域，仅离线存储少量根问题的解，并在查询时通过线性插值或动态运动基元等轻量级模块进行在线适配。
result: 实验表明，COAD在多种机械臂和环境下实现了极高的库压缩率和亚毫秒级的查询速度，且路径质量优于基准方法。
conclusion: COAD证明了结合离线压缩库与在线快速适配可以在保证规划成功率的前提下，实现对连续任务空间的极速运动规划。
---

## 摘要
在许多机器人操作任务中，机器人需要重复解决运动规划问题，这些问题的主要区别在于目标物体及其相关障碍物的位置，而周围的工作空间保持不变。先前的研究表明，利用经验和离线计算可以加速重复的规划查询，但它们缺乏覆盖连续任务空间的保证，并且需要存储庞大的解库。在这项工作中，我们提出了 COAD，这是一个在连续目标参数化任务空间上提供常数时间规划的框架。COAD 将连续任务空间离散化为有限个任务覆盖区域。它并不离线规划并存储每个区域的解，而是通过仅解决具有代表性的根问题来构建压缩库。其他问题则通过对这些根解进行快速自适应来处理。在查询时，系统以常数时间检索根运动，并使用轻量级自适应模块（如线性插值、动态运动基元或简单的轨迹优化）将其调整至目标位置。我们在仿真和真实世界的各种机械臂及环境中评估了该框架，结果表明 COAD 在实现运动库大幅压缩的同时，保持了高成功率和亚毫秒级的查询速度，在效率和路径质量上均优于基准方法。源代码可在 https://github.com/elpis-lab/CoAd 获取。

## Abstract
In many robotic manipulation tasks, the robot repeatedly solves motion-planning problems that differ mainly in the location of the goal object and its associated obstacle, while the surrounding workspace remains fixed. Prior works have shown that leveraging experience and offline computation can accelerate repeated planning queries, but they lack guarantees of covering the continuous task space and require storing large libraries of solutions. In this work, we present COAD, a framework that provides constant-time planning over a continuous goal-parameterized task space. COAD discretizes the continuous task space into finitely many Task Coverage Regions. Instead of planning and storing solutions for every region offline, it constructs a compressed library by only solving representative root problems. Other problems are handled through fast adaptation from these root solutions. At query time, the system retrieves a root motion in constant time and adapts it to the desired goal using lightweight adaptation modules such as linear interpolation, Dynamic Movement Primitives, or simple trajectory optimization. We evaluate the framework on various manipulators and environments in simulation and the real world, showing that COAD achieves substantial compression of the motion library while maintaining high success rates and sub-millisecond-level queries, outperforming baseline methods in both efficiency and path quality. The source code is available at https://github.com/elpis-lab/CoAd.

---

## 论文详细总结（自动生成）

### 论文总结：COAD —— 基于压缩库与在线自适应的连续目标操作常数时间规划

#### 1. 核心问题与整体含义（研究动机和背景）
在工业生产（如分拣、包装）中，机器人常需重复执行任务，环境中的固定障碍物保持不变，但目标物体的位姿在连续空间内波动。传统的运动规划算法（如 RRT*）在每次查询时重新计算，耗时且缺乏实时性保证；而现有的“基于经验”的方法虽能加速规划，但往往无法保证覆盖整个连续的任务空间，且需要存储海量的路径解，导致内存开销巨大。
**COAD 的核心目标**是：在保证 100% 覆盖连续目标空间的前提下，实现**常数时间（Constant-Time）**的在线规划，并大幅压缩离线存储的路径库。

#### 2. 方法论：核心思想与技术细节
COAD 框架分为离线库构建和在线查询两个阶段：
*   **任务覆盖区域（TCR）理论**：
    *   引入 **TSR（Task Space Regions）** 描述目标物体的容差。
    *   提出 **TCR（Task Coverage Region）** 概念：即一个固定的末端执行器位姿能够成功抓取目标物体的所有可能位姿集合。
    *   证明了 TCR 内部包含轴对齐方块，从而将连续任务空间离散化为有限的网格索引，确保了覆盖的完备性。
*   **离线压缩库构建（Algorithm 1）**：
    *   系统随机选择一个未覆盖的区域作为“根区域”，规划一条“根运动”路径。
    *   **核心创新点**：尝试将该根路径通过自适应算法推广到邻近区域。如果适配后的路径在邻近区域也是碰撞自由且有效的，则该区域仅存储指向根路径的索引，而不存储完整路径，从而实现高达 97% 的压缩率。
*   **在线自适应模块（Algorithm 2）**：
    *   **LI（线性插值）**：最快，直接连接根路径末端与新目标。
    *   **DMP（动态运动基元）**：通过动力学系统调整路径形状，保持运动特征。
    *   **STO（简单轨迹优化）**：利用二次规划（QP）在保持原路径特征的同时优化新路径。
*   **常数时间保证**：在线查询仅涉及 $O(1)$ 的哈希表查找和计算复杂度固定的自适应算法，不依赖于环境复杂度。

#### 3. 实验设计
*   **实验场景**：使用了 **Table（桌面）**、**Cage（笼子/复杂障碍）**、**Shelf（货架/狭窄通道）** 三种典型环境。
*   **实验平台**：
    *   仿真：Panda（7-DOF）和 Fetch（8-DOF）机械臂。
    *   实物：UR10 机械臂桌面抓取实验。
*   **Benchmark 与对比方法**：
    *   **RRT-Connect**：经典的采样规划器。
    *   **Library Baseline**：传统的基于经验的路径库方法（不具备空间覆盖保证）。
    *   **COAD 变体**：对比了 LI、DMP、STO 三种不同的自适应后端。

#### 4. 资源与算力
*   **硬件环境**：Intel i7-14700 CPU, 32GB RAM。
*   **算力说明**：论文未提及使用 GPU，这表明该方法主要依赖 CPU 进行几何计算和优化。
*   **训练/预处理时长**：文中未明确给出离线构建库的总耗时，但强调了在线查询达到了**亚毫秒级（最低 0.03ms）**。

#### 5. 实验数量与充分性
*   **实验规模**：每个设置下进行了 **1000 次随机查询**。
*   **覆盖范围**：涵盖了不同自由度（7-DOF, 8-DOF, 6-DOF）的机器人，以及从开放空间到极度受限空间的多种环境。
*   **充分性评价**：实验设计较为充分，不仅对比了成功率和时间，还量化了路径质量（路径长度）和内存压缩率。通过实物 UR10 的验证，证明了算法从仿真到现实的迁移能力。

#### 6. 主要结论与发现
*   **极速查询**：COAD 的查询速度比 RRT-Connect 快 2 到 3 个数量级，通常在 0.1ms 以内。
*   **高效压缩**：在保持 100% 成功率的同时，路径库压缩率最高可达 **97.9%**。
*   **高质量路径**：相比于 RRT-Connect 产生的随机路径，COAD 适配出的路径在平滑度和长度上表现更好，尤其是使用 DMP 后端时。
*   **完备性**：通过 TCR 离散化，COAD 解决了传统经验规划器无法保证覆盖整个连续目标分布的问题。

#### 7. 优点与亮点
*   **理论严谨**：通过数学证明确保了连续空间到有限网格映射的可行性。
*   **实用性强**：常数时间规划对于需要高速反应的工业场景（如传送带抓取）具有极高价值。
*   **灵活性**：框架支持多种自适应后端，用户可以根据需求在“极速（LI）”和“高质量（STO/DMP）”之间权衡。

#### 8. 不足与局限
*   **半静态环境假设**：COAD 假设除了目标物体外，其他障碍物是静止的。如果工作空间内的其他大尺寸障碍物频繁移动，离线库将失效。
*   **离线计算开销**：虽然在线很快，但离线构建 TCR 覆盖和验证适配路径可能需要较长时间（文中未详述此部分代价）。
*   **根节点选择**：目前采用随机选择根区域，可能不是最优的压缩策略；若能优化根节点的分布，压缩率可能进一步提升。
*   **应用限制**：主要针对目标位姿变化的任务，对于起始位姿也大幅变化或任务逻辑改变的场景支持有限。

（完）
