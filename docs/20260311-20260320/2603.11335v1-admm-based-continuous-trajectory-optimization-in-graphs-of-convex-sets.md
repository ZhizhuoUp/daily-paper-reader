---
title: ADMM-based Continuous Trajectory Optimization in Graphs of Convex Sets
title_zh: 基于 ADMM 的凸集图连续轨迹优化
authors: "Lukas Pries, Jon Arrizabalaga, Zachary Manchester, Markus Ryll"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.11335v1"
tags: ["query:q3"]
score: 9.0
evidence: 非凸环境下基于ADMM的连续轨迹优化
tldr: 本文提出一种基于ADMM的数值求解器，用于解决非凸环境下的连续轨迹优化问题。该方法将轨迹参数化为多项式，使原始更新具备闭式解，并引入时空分配图将松弛更新转化为最短路径搜索。通过联合优化离散空间与连续时间领域，该方法比传统解耦方法拥有更大的搜索空间，且具备结构鲁棒性，无需复杂的热启动即可在非凸环境中可靠收敛，显著提升了轨迹质量。
selection_source: fresh_fetch
motivation: 针对非凸环境下轨迹优化中空间路径选择与时间分配难以高效联合优化且依赖复杂热启动的挑战。
method: 采用ADMM框架，结合多项式轨迹参数化与基于混合整数规划的时空分配图，将优化过程转化为闭式解计算与最短路径搜索。
result: 实验表明该方法能有效探索更大的搜索空间，在无需精细初始化的情况下实现高质量轨迹的可靠收敛。
conclusion: 该求解器通过时空联合优化和结构化改进，为非凸环境下的高效、鲁棒轨迹规划提供了新方案。
---

## 摘要
本文提出了一种用于在非凸环境中计算连续轨迹的数值求解器。我们的方法依赖于基于两个关键组件构建的交替方向乘子法 (ADMM) 的定制实现：首先，我们将轨迹参数化为多项式，使得原始更新可以作为最小控制代价问题以闭式解形式计算。其次，我们引入了基于混合整数公式的时空分配图概念，并将松弛更新建模为最短路径搜索。这些要素的结合使得该求解器与现有技术相比具有若干显著优势。通过对离散空间域和连续时间域进行联合优化，我们的方法比现有的解耦方法能够访问更大的搜索空间，从而发现更优的轨迹。此外，该求解器的结构鲁棒性确保了从朴素初始化开始的可靠收敛，消除了非凸环境中复杂热启动的瓶颈。

## Abstract
This paper presents a numerical solver for computing continuous trajectories in non-convex environments. Our approach relies on a customized implementation of the Alternating Direction Method of Multipliers (ADMM) built upon two key components: First, we parameterize trajectories as polynomials, allowing the primal update to be computed in closed form as a minimum-control-effort problem. Second, we introduce the concept of a spatio-temporal allocation graph based on a mixed-integer formulation and pose the slack update as a shortest-path search. The combination of these ingredients results in a solver with several distinct advantages over the state of the art. By jointly optimizing over both discrete spatial and continuous temporal domains, our method accesses a larger search space than existing decoupled approaches, enabling the discovery of superior trajectories. Additionally, the solver's structural robustness ensures reliable convergence from naive initializations, removing the bottleneck of complex warm starting in non-convex environments.

---

## 论文详细总结（自动生成）

### 论文总结：基于 ADMM 的凸集图连续轨迹优化 (ACTOR)

#### 1. 核心问题与整体含义
本文针对**非凸环境下的连续轨迹优化**问题，旨在解决传统方法中“离散规划”与“连续控制”相互脱节导致的全局次优性。在复杂环境中（如充满障碍物的空间），机器人需要同时决定经过哪些区域（离散选择）以及如何平滑运动（连续优化）。传统方法通常先搜索路径再优化轨迹，而本文提出的 **ACTOR** 求解器通过联合优化离散空间和连续时间域，能够发现更优的轨迹，并克服了非凸优化对初始值高度敏感（易陷入局部极小值）的难题。

#### 2. 方法论
核心思想是将轨迹优化问题嵌入到 **ADMM（交替方向乘子法）** 框架中，利用数学结构的特殊性实现高效求解：
*   **轨迹参数化**：使用分段**贝塞尔多项式（Bézier polynomials）**表示轨迹。利用其凸包性质，将复杂的安全约束转化为控制点的线性约束。
*   **时空分配图 (Spatio-temporal Allocation Graph)**：基于混合整数规划（MIP）公式构建一个有向无环图（DAG）。图中每个节点代表轨迹段与凸安全集的分配关系，路径代表一种可行的空间拓扑选择。
*   **ADMM 三步迭代**：
    1.  **原始更新 (Primal Update)**：求解一个最小控制代价（Minimum-control-effort）的二次规划问题。由于结构固定，可预先分解矩阵，实现闭式解计算。
    2.  **松弛更新 (Slack Update)**：这是本文的核心创新。将非凸的安全约束投影转化为**加权图上的最短路径搜索**。由于图是 DAG，通过动态规划（DP）即可在线性时间内完成。
    3.  **对偶更新 (Dual Update)**：执行梯度上升，强制原始变量与松弛变量达成一致（共识）。

#### 3. 实验设计
论文通过多个数值仿真场景验证了 ACTOR 的性能：
*   **测试场景**：
    *   **U型障碍物**：测试算法跳出局部极小值的能力。
    *   **多路径墙壁**：测试在不同同伦类（Homotopy classes）中选择最优路径的能力。
    *   **点云环境**：模拟现实中基于原始传感器数据的导航任务。
    *   **复杂迷宫**：针对四旋翼无人机的高阶（Minimum-snap）轨迹规划。
*   **对比基准 (Benchmarks)**：
    *   **OBCA**：基于非线性编程（NLP/IPOPT）的直接碰撞避免方法。
    *   **SafeC**：预选安全走廊后的轨迹优化（基于 SNOPT）。
    *   **GCS (Graph of Convex Sets)**：基于凸松弛的路径规划方法。
    *   **MIQP (Gurobi)**：混合整数二次规划的全局最优求解器。

#### 4. 资源与算力
*   **实现细节**：ACTOR 使用 **Python/NumPy** 编写，而对比基准（如 SNOPT, Gurobi）多为经过高度优化的 C++/商业求解器。
*   **算力说明**：文中未明确提及具体的 GPU 或 CPU 型号及训练时长。但强调了该算法是**无矩阵分解（Factorization-free）**的，且每一步迭代的计算复杂度随障碍物数量和轨迹段数**线性增长**，暗示其对算力要求较低，适合嵌入式应用。

#### 5. 实验数量与充分性
*   **实验规模**：论文展示了约 4-5 组具有代表性的定性实验和 1 组关于可扩展性的定量实验。
*   **充分性评价**：实验设计较为全面，涵盖了从基础的局部极小值逃逸到复杂的高阶动力学约束。通过与商业求解器 Gurobi 的对比，证明了算法在处理大规模问题时的效率优势。然而，缺乏在大规模随机生成环境下的统计学性能分析（如成功率、平均耗时的分布）。

#### 6. 主要结论与发现
*   **全局探索能力**：ACTOR 能够从简单的直线初始化中自动收敛到复杂的避障轨迹，无需复杂的热启动。
*   **优越的收敛性**：相比于 OBCA 等 NLP 方法，ACTOR 不易卡在不可行的平稳点。
*   **高效扩展性**：随着环境复杂度增加，ACTOR 的运行时间增长远慢于 MIQP 和传统的 NLP 求解器。
*   **联合优化优势**：通过同时考虑空间分配和时间动态，ACTOR 找到的轨迹比“先路径搜索再轨迹优化”的解耦方法更平滑、代价更低。

#### 7. 优点
*   **结构鲁棒性**：将非凸投影转化为 DAG 最短路径问题，保证了离散决策的有效性。
*   **计算高效**：利用多项式性质获得闭式解，避免了昂贵的非线性约束求导。
*   **易于实现**：超参数较少（主要是 ADMM 的惩罚参数 $\rho$），且对初始值不敏感。

#### 8. 不足与局限
*   **局部最优风险**：尽管能跳出部分局部极小值，但作为基于梯度（ADMM）的方法，本质上仍是局部优化，不能像 MIQP 那样保证全局最优性。
*   **完备性限制**：仅在无界情况下能保证完备性；在存在严格动力学边界约束时，可能无法保证一定找到可行解。
*   **环境依赖**：算法依赖于预先对自由空间进行凸分解（Union of Convex Sets），若环境分解质量差，会影响最终轨迹质量。

（完）
