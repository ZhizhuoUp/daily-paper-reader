---
title: Parallel-in-Time Nonlinear Optimal Control via GPU-native Sequential Convex Programming
title_zh: 基于 GPU 原生序列凸规划的时间并行非线性最优控制
authors: "Yilin Zou, Zhong Zhang, Maxime Robic, Fanghua Jiang"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10711v2"
tags: ["query:q8"]
score: 9.0
evidence: 基于GPU的原生实时轨迹优化
tldr: "针对非线性约束自主系统的实时轨迹优化，本文提出了一种完全基于GPU原生的框架。该框架结合了序列凸规划（SCP）与基于共识的交替方向乘子法（ADMM），通过时间分割策略将优化问题解耦为可大规模并行执行的子问题。实验证明，该方法在无人机敏捷飞行和火星着陆任务中，相比高性能CPU基准实现了4倍吞吐量提升和51%的能耗降低，支持超过100Hz的规划频率。"
selection_source: fresh_fetch
motivation: 传统的CPU串行求解器受限于全局稀疏线性代数或动态规划的串行特性，难以充分利用GPU的大规模并行计算能力。
method: 提出一种结合SCP与共识ADMM的GPU原生框架，通过时间分割将优化时域解耦为独立的并行子问题，并消除内存传输开销。
result: "在嵌入式平台上实现了超过100Hz的规划速率，相比12核CPU基准，吞吐量提升4倍，能耗降低51%，且GPU利用率超过96%。"
conclusion: 该框架通过充分利用硬件并行性，为非线性最优控制和鲁棒模型预测控制提供了一种高效、节能且可扩展的解决方案。
---

## 摘要
非线性约束自主系统的实时轨迹优化至关重要，通常由基于 CPU 的序列求解器执行。具体而言，对全局稀疏线性代数的依赖或动态规划算法的串行特性限制了对 GPU 等大规模并行计算架构的利用。为弥补这一差距，我们引入了一个完全 GPU 原生的轨迹优化框架，该框架将序列凸规划与基于共识的交替方向乘子法（ADMM）相结合。通过应用时间拆分策略，我们的算法将优化时域解耦为独立的、按节点划分的子问题，从而实现大规模并行执行。整个过程完全在 GPU 上运行，消除了昂贵的内存传输和大规模稀疏分解。该架构可自然扩展到多轨迹优化。我们在边缘计算平台上，通过四旋翼飞行器敏捷飞行任务和火星动力下降问题验证了该求解器。基准测试显示，与经过深度优化的 12 核 CPU 基准相比，吞吐量持续提升了 4 倍，能耗降低了 51%。至关重要的是，该框架使硬件达到饱和状态，保持 96% 以上的 GPU 活跃利用率，从而实现超过 100 Hz 的规划频率。此外，我们通过联合优化随机扰动下的动态耦合场景，展示了该求解器在鲁棒模型预测控制（MPC）方面的可扩展性，从而实现了可扩展且安全的自主性。

## Abstract
Real-time trajectory optimization for nonlinear constrained autonomous systems is critical and typically performed by CPU-based sequential solvers. Specifically, reliance on global sparse linear algebra or the serial nature of dynamic programming algorithms restricts the utilization of massively parallel computing architectures like GPUs. To bridge this gap, we introduce a fully GPU-native trajectory optimization framework that combines sequential convex programming with a consensus-based alternating direction method of multipliers. By applying a temporal splitting strategy, our algorithm decouples the optimization horizon into independent, per-node subproblems that execute massively in parallel. The entire process runs fully on the GPU, eliminating costly memory transfers and large-scale sparse factorizations. This architecture naturally scales to multi-trajectory optimization. We validate the solver on a quadrotor agile flight task and a Mars powered descent problem using an on-board edge computing platform. Benchmarks reveal a sustained 4x throughput speedup and a 51% reduction in energy consumption over a heavily optimized 12-core CPU baseline. Crucially, the framework saturates the hardware, maintaining over 96% active GPU utilization to achieve planning rates exceeding 100 Hz. Furthermore, we demonstrate the solver's extensibility to robust Model Predictive Control by jointly optimizing dynamically coupled scenarios under stochastic disturbances, enabling scalable and safe autonomy.

---

## 论文详细总结（自动生成）

这是一份关于论文《Parallel-in-Time Nonlinear Optimal Control via GPU-native Sequential Convex Programming》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的非线性约束系统轨迹优化主要依赖 CPU 串行求解器。这些求解器要么依赖全局稀疏线性代数，要么具有动态规划的串行本质（如 iLQR），难以充分利用 GPU 的大规模并行架构（SIMT）。
*   **研究动机**：随着机器人任务（如敏捷无人机飞行、火箭回收着陆）日益复杂，实时求解大规模、非凸最优控制问题（OCP）的需求激增。现有的 GPU 加速方案往往在非线性前向模拟或硬约束处理上存在瓶颈。
*   **整体含义**：本文旨在开发一种完全“GPU 原生”的求解框架，通过时间维度的解耦，将复杂的全局优化问题转化为可在 GPU 上并行执行的局部子问题，从而实现极高的计算吞吐量和能效比。

### 2. 方法论：核心思想与技术细节
该框架名为 **ucenter**，采用两层分层架构：
*   **外层循环（SCP 接口）**：使用序列凸规划（SCP）处理非线性。通过对动力学进行一阶泰勒展开，对代价函数进行二阶展开，将非线性问题转化为一系列凸二次规划（QP）子问题。
*   **内层循环（并行共识 ADMM）**：这是本文的核心创新。
    *   **变量拆分策略**：引入物理变量（状态/控制）、动态辅助变量（用于解耦时间步）和几何镜像变量（用于处理硬约束）。
    *   **时间分割（Temporal Splitting）**：将轨迹中的每个时间节点视为独立实体。通过 ADMM 算法，各节点并行优化自身目标，同时通过“共识”机制满足动力学一致性。
    *   **计算优化**：
        *   **物理层**：求解独立的稠密线性系统，避免了全局稀疏矩阵分解。
        *   **动态层**：通过闭式加权平均更新。
        *   **几何层**：通过简单的元素级裁剪（Clamping）实现硬约束投影。
*   **GPU 原生实现**：整个算法循环完全在 GPU 上运行，消除了 CPU-GPU 之间昂贵的内存传输开销。

### 3. 实验设计
*   **实验场景**：
    1.  **6-DOF 四旋翼敏捷飞行**：在充满障碍物的环境中进行高速避障飞行。
    2.  **6-DOF 火星动力下降着陆**：考虑质量消耗、推力矢量限制、滑移坡度约束等严苛条件的燃料最优着陆。
    3.  **鲁棒 MPC 场景**：在随机横风扰动下，通过联合优化 15 个耦合场景生成安全预测管（Safety Tube）。
*   **Benchmark（基准方法）**：
    *   **CPU 基准**：经过高度优化的 12 核 CPU 并行化 iLQR 算法（使用 Numba JIT 编译）。
*   **对比指标**：计算吞吐量（Hz）、计算耗时（ms）、能耗（Joule）、GPU 利用率、成功率。

### 4. 资源与算力
*   **硬件平台**：**Nvidia Jetson AGX Orin 64GB**（边缘计算平台，适用于嵌入式机器人）。
*   **软件后端**：基于 **Python** 和 **JAX** 框架，利用 `vmap`（矢量化映射）和 `jit`（即时编译）实现高效的张量运算。
*   **算力表现**：在处理 5000 个并行轨迹时，GPU 活跃利用率稳定在 **96% 以上**。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了消融实验，测试了惩罚参数 $\rho$ 和内层 ADMM 迭代次数对收敛性的影响。
    *   进行了大规模随机化测试，生成了 **1000 到 5000 组** 随机障碍物/初始条件的轨迹批处理实验。
*   **充分性评价**：实验设计较为充分。不仅对比了速度，还深入分析了能效比和硬件利用率。通过火星着陆和无人机两个截然不同的动力学模型验证了算法的通用性。此外，鲁棒 MPC 的案例展示了该求解器在处理“场景束”优化方面的独特优势。

### 6. 主要结论与发现
*   **性能提升**：相比于 12 核 CPU 基准，该 GPU 求解器实现了 **4.1 倍** 的持续吞吐量提升。
*   **高频规划**：在嵌入式平台上实现了超过 **100 Hz** 的重规划速率，完全满足实时控制需求。
*   **能效优势**：在处理相同规模的任务时，能耗降低了 **51%**，这对电池驱动的移动机器人至关重要。
*   **鲁棒性**：在火星着陆任务中实现了 **99.8%** 的求解成功率，并能实时生成应对不确定性的鲁棒轨迹管。

### 7. 优点
*   **架构创新**：通过时间分割巧妙避开了 GPU 不擅长的稀疏矩阵分解问题，将其转化为擅长的稠密并行运算。
*   **端到端 GPU 执行**：最小化了数据同步延迟，使硬件性能几乎达到饱和利用（>96%）。
*   **多任务扩展性**：天然支持“系综求解”（Ensemble Solving），能够同时优化数百个扰动场景，为鲁棒 MPC 和大规模数据生成（用于强化学习）提供了可能。

### 8. 不足与局限
*   **收敛性理论**：论文提到 SCP 外层与未完全收敛的 ADMM 内层之间的相互作用仍需更深入的理论收敛性证明。
*   **约束类型限制**：虽然支持硬约束，但主要针对“易于投影”（Prox-friendly）的凸约束（如方框、范数球），对于极其复杂的非凸几何约束可能仍需进一步转化。
*   **硬件依赖**：性能高度依赖于 JAX 的编译优化和特定 GPU 架构，在不支持高性能张量运算的低端硬件上优势可能不明显。
*   **实机验证**：目前主要在边缘计算板卡上进行仿真验证，尚未在真实的物理飞行平台上进行闭环飞行实验。

（完）
