---
title: "CSSDF-Net: Safe Motion Planning Based on Neural Implicit Representations of Configuration Space Distance Field"
title_zh: CSSDF-Net：基于构型空间距离场神经隐式表示的安全运动规划
authors: "Haohua Chen, Yixuan Zhou, Yifan Zhou, Hesheng Wang"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18669v1"
tags: ["query:profile-1"]
score: 10.0
evidence: 高维机械臂的安全运动规划
tldr: 针对高维机械臂在非结构化环境中的避障难题，本文提出CSSDF-Net，一种直接在配置空间学习连续有符号距离场（SDF）的神经隐式表示方法。该方法通过空间哈希数据生成流程实现了对未知环境的零次学习泛化，统一处理自碰撞与环境碰撞。实验证明，该方法能提供稳定的梯度，支持离线轨迹优化与在线反应式避障，在静态和动态场景中均表现出色，具有较高的实用价值。
selection_source: fresh_fetch
motivation: 现有的碰撞检测方法通常不可微，且基于工作空间的隐式模型在映射到配置空间时面临严重的非线性问题和收敛困难。
method: 提出CSSDF-Net，通过空间哈希编码机器人几何先验，直接在配置空间学习连续SDF，并将其集成到轨迹优化和模型预测控制中。
result: 在平面臂和7自由度机械臂上的实验验证了该方法在静态和动态环境下的避障有效性，且具备支持实时部署的推理延迟。
conclusion: CSSDF-Net为复杂环境下的机械臂运动规划提供了一种高效、可微且具备强泛化能力的配置空间距离查询新方案。
---

## 摘要
在非结构化环境中，高维机械臂的操作需要一种可微且与场景无关的距离查询机制，以引导安全运动的生成。现有的几何碰撞检测器通常是不可微的，而基于工作空间的隐式距离模型则受到高度非线性的工作空间-构型映射的阻碍，且往往面临收敛性差的问题；此外，自碰撞和环境碰撞通常被作为独立的约束来处理。我们提出了构型空间有向距离场网络（CSSDF-Net），它直接在构型空间中学习连续的有向距离场，从而在统一的几何安全概念下提供关节空间的距离和梯度查询。为了实现无需针对特定环境重新训练的零样本泛化，我们引入了一种基于空间哈希的数据生成流水线，该流水线编码了以机器人为中心的几何先验，并支持针对任意障碍物点集的高效风险构型检索。学习到的距离场被集成到安全约束轨迹优化和收缩时界模型预测控制（MPC）中，从而实现了离线规划和在线反应式避障。在平面臂和 7 自由度机械臂上的实验表明，该方法具有稳定的梯度、在静态和动态场景中有效的避障能力，以及针对大规模点云查询的实用推理延迟，支持在先前未见过的环境中进行部署。

## Abstract
High-dimensional manipulator operation in unstructured environments requires a differentiable, scene-agnostic distance query mechanism to guide safe motion generation. Existing geometric collision checkers are typically non-differentiable, while workspace-based implicit distance models are hindered by the highly nonlinear workspace--configuration mapping and often suffer from poor convergence; moreover, self-collision and environment collision are commonly handled as separate constraints. We propose Configuration-Space Signed Distance Field-Net (CSSDF-Net), which learns a continuous signed distance field directly in configuration space to provide joint-space distance and gradient queries under a unified geometric notion of safety. To enable zero-shot generalization without environment-specific retraining, we introduce a spatial-hashing-based data generation pipeline that encodes robot-centric geometric priors and supports efficient retrieval of risk configurations for arbitrary obstacle point sets. The learned distance field is integrated into safety-constrained trajectory optimization and receding-horizon MPC, enabling both offline planning and online reactive avoidance. Experiments on a planar arm and a 7-DoF manipulator demonstrate stable gradients, effective collision avoidance in static and dynamic scenes, and practical inference latency for large-scale point-cloud queries, supporting deployment in previously unseen environments.

---

## 论文详细总结（自动生成）

以下是对论文《CSSDF-Net: Safe Motion Planning Based on Neural Implicit Representations of Configuration Space Distance Field》的结构化总结：

### 1. 核心问题与整体含义
该论文针对高维机械臂在复杂、非结构化环境中的**安全运动规划**问题。
*   **研究动机**：传统的碰撞检测方法（如 GJK）通常不可微，难以直接引导基于梯度的优化；而现有的隐式距离模型多基于工作空间（Workspace），在映射到构型空间（C-space）时存在高度非线性，容易陷入局部最优。
*   **核心目标**：建立一个直接在构型空间运行的、连续且可微的神经隐式有符号距离场（SDF），统一处理**自碰撞**与**环境碰撞**，并实现无需针对新场景重新训练的**零样本泛化**。

### 2. 方法论
*   **核心思想**：提出 **CSSDF-Net**，通过多层感知机（MLP）学习一个函数 $d(q, p)$，其中 $q$ 是机器人构型，$p$ 是障碍物点。该网络能同时输出距离值和构型空间梯度。
*   **关键技术细节**：
    *   **网络架构**：采用 5 层 MLP，包含残差连接、ReLU 激活、批归一化和位置编码。
    *   **复合损失函数**：由三部分组成：
        1.  **回归损失 ($L_{dist}$)**：最小化预测距离与真实值的误差。
        2.  **Eikonal 正则化 ($L_{eikonal}$)**：强制梯度模长为 1，确保 SDF 的几何特性。
        3.  **方向一致性损失 ($L_{dir}$)**：通过余弦相似度对齐预测梯度与真实梯度方向。
    *   **空间哈希数据生成**：这是实现泛化的关键。通过将机器人几何体近似为球体簇，并利用空间哈希建立“体素-构型”映射，离线编码机器人的几何先验。在线推理时，任意障碍物点云可直接输入，无需重新训练。
*   **规划集成**：
    *   **离线规划**：将 CSSDF 集成到安全约束轨迹优化中。
    *   **在线避障**：集成到收缩时界模型预测控制（MPC）中，通过二次规划（QP）求解。

### 3. 实验设计
*   **实验场景**：
    1.  **2D 平面臂 (2-DoF)**：用于直观验证梯度引导的有效性。
    2.  **7 自由度机械臂 (7-DoF)**：在静态和动态（含人体干扰）环境中进行测试。
*   **Benchmark 与对比方法**：
    *   **采样算法**：RRT, RRT-Connect, RRT*, BiEST, KPIECE 等。
    *   **优化算法**：CHOMP, STOMP, TrajOpt。
    *   **评估指标**：碰撞风险 (CR)、轨迹长度 (TL)、平均耗时 (AT)、误报率 (FPR)、推理延迟。

### 4. 资源与算力
*   **硬件环境**：使用了 **NVIDIA RTX 3060 GPU** 进行训练和推理。
*   **训练细节**：使用了 $1 \times 10^4$ 个样本，按照 8:2 的比例划分训练集和验证集。
*   **推理性能**：在 GPU 上，处理 $10^4$ 个点云点的距离+梯度查询仅需 **4.302 ms**，满足实时性要求。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 2 自由度到 7 自由度的仿真与实机实验，并进行了详细的消融实验。
*   **消融实验**：分别验证了“边界挖掘策略”和“复合损失函数各项”对模型精度（MAE）和梯度准确性的影响。
*   **充分性评价**：实验设计较为全面，对比了十余种主流规划算法，涵盖了静态与动态场景，客观地展示了该方法在轨迹长度和安全性之间的平衡。

### 6. 主要结论与发现
*   **梯度稳定性**：CSSDF-Net 提供的梯度能够引导机械臂在单步迭代内沿最短路径逃离碰撞区域。
*   **泛化能力**：通过空间哈希机制，模型在未见过的障碍物布局中表现出极强的零样本泛化能力。
*   **性能优势**：相比于纯采样算法，该方法生成的轨迹更短且更平滑；相比于 CHOMP/STOMP 等优化算法，其计算效率显著提高（耗时缩短数倍）。
*   **实时性**：在 7-DoF 机械臂上实现了高频（100Hz）的动态避障响应。

### 7. 优点
*   **统一性**：将自碰撞和环境碰撞整合进同一个几何安全框架，简化了约束处理。
*   **可微性**：直接提供构型空间梯度，避免了复杂的逆运动学投影。
*   **场景无关**：空间哈希流水线解决了神经隐式表示通常需要针对新环境重训练的痛点。
*   **高效推理**：支持大规模点云的并行查询，适合视觉引导的机器人任务。

### 8. 不足与局限
*   **时空建模缺失**：目前模型主要处理空间几何，未显式考虑时间维度（4D），在预测极高速运动障碍物的轨迹方面可能存在局限。
*   **感知噪声敏感性**：实验主要在理想或受控点云下进行，实际部署中传感器噪声和遮挡可能影响距离场的准确性。
*   **多机协作限制**：目前侧重于单臂避障，尚未扩展到多机器人协同下的复杂安全约束。

（完）
