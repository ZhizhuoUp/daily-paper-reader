---
title: Graph-of-Constraints Model Predictive Control for Reactive Multi-agent Task and Motion Planning
title_zh: 用于反应式多智能体任务与运动规划的约束图模型预测控制
authors: "Anastasios Manganaris, Jeremy Lu, Ahmed H. Qureshi, Suresh Jagannathan"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18400v1"
tags: ["query:profile-1"]
score: 9.0
evidence: 基于MPC的多智能体机械臂任务与运动规划
tldr: 针对多智能体任务与运动规划（TAMP）中处理偏序任务和动态分配的难题，本文提出了一种名为GoC-MPC的新框架。该方法将约束图模型与模型预测控制相结合，通过跟踪3D关键点定义几何约束，实现了无需训练数据、仅依赖视觉观测的实时反应式规划。它能有效应对环境扰动，在多智能体协作操纵任务中表现出极高的鲁棒性、计算效率和路径优化能力。
selection_source: fresh_fetch
motivation: 现有的多智能体TAMP方法在处理偏序任务和动态代理分配时缺乏灵活性，难以应对实时扰动。
method: 提出GoC-MPC框架，将广义约束序列与MPC集成，利用3D关键点定义约束并实现基于视觉的在线闭环控制。
result: 实验表明，该方法在多智能体操纵任务中比现有基准具有更高的成功率、更快的计算速度和更短的路径长度。
conclusion: GoC-MPC为现实世界扰动下的多智能体协作提供了一种高效、鲁棒且无需环境模型的反应式规划解决方案。
---

## 摘要
相互依赖的几何约束序列是许多多智能体任务与运动规划（TAMP）问题的核心。然而，现有的处理此类约束序列的方法在处理偏序任务和动态智能体分配方面存在困难。它们通常假设静态分配，且当扰动改变任务分配时无法进行调整。为了克服这些局限性，我们引入了约束图模型预测控制（GoC-MPC），这是一种与 MPC 集成的通用约束序列框架。GoC-MPC 自然地支持偏序任务、动态智能体协同以及扰动恢复。通过在跟踪的 3D 关键点上定义约束，我们的方法能够稳健地解决多种多智能体操作任务——仅根据视觉观测结果即可协调智能体并进行在线调整，而不依赖于训练数据或环境模型。实验表明，与最近的基准方法相比，GoC-MPC 实现了更高的成功率、显著更快的 TAMP 计算速度以及更短的总路径，证明了它是应对现实世界扰动下多智能体操作的一种高效且稳健的解决方案。我们的补充视频和代码可在 https://sites.google.com/view/goc-mpc/home 找到。

## Abstract
Sequences of interdependent geometric constraints are central to many multi-agent Task and Motion Planning (TAMP) problems. However, existing methods for handling such constraint sequences struggle with partially ordered tasks and dynamic agent assignments. They typically assume static assignments and cannot adapt when disturbances alter task allocations. To overcome these limitations, we introduce Graph-of-Constraints Model Predictive Control (GoC-MPC), a generalized sequence-of-constraints framework integrated with MPC. GoC-MPC naturally supports partially ordered tasks, dynamic agent coordination, and disturbance recovery. By defining constraints over tracked 3D keypoints, our method robustly solves diverse multi-agent manipulation tasks-coordinating agents and adapting online from visual observations alone, without relying on training data or environment models. Experiments demonstrate that GoC-MPC achieves higher success rates, significantly faster TAMP computation, and shorter overall paths compared to recent baselines, establishing it as an efficient and robust solution for multi-agent manipulation under real-world disturbances. Our supplementary video and code can be found at https://sites.google.com/view/goc-mpc/home .

---

## 论文详细总结（自动生成）

以下是对论文《Graph-of-Constraints Model Predictive Control for Reactive Multi-agent Task and Motion Planning》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在存在实时扰动的情况下，高效地为多机器人系统进行任务与运动规划（TAMP）。
*   **研究背景**：
    *   现有的基于优化或约束序列的方法（如 ReKep）通常假设任务是**全序**的（即必须按固定顺序执行）且智能体分配是**静态**的。
    *   这种局限性导致多机器人系统无法充分利用并行性，且在遇到扰动（如某个机器人抓取失败）时，整个团队往往必须停下等待，无法灵活重新分配任务或独立恢复。
*   **研究动机**：提出一种能够支持**偏序任务**、**动态智能体分配**以及**局部扰动恢复**的反应式规划框架。

### 2. 方法论
核心思想是将任务表示为**约束图（Graph-of-Constraints, GoC）**，并将其集成到模型预测控制（MPC）循环中。

*   **约束图 (GoC) 定义**：
    *   使用有向无环图（DAG）结构。节点（Nodes）代表路标点约束，边（Edges）代表路径约束。
    *   引入**动态分配矩阵 $A$**：通过二值决策变量将 $M$ 个智能体动态分配给 $K$ 个子任务。
*   **三阶段优化分解**：
    1.  **路标点与分配子问题**：求解混合整数非线性规划（MINLP），确定最优路标点位置和智能体任务分配。
    2.  **智能体样条曲线子问题**：基于路标点，求解二次规划（QP）以确定各智能体的时间分配（Timing）和速度，确保多机器人间的同步或先后顺序。
    3.  **短时域收敛问题**：在 MPC 循环中执行短时域路径跟踪，处理避障和精细动力学成本。
*   **反应式机制**：
    *   **相位推进（Phase Progression）**：当机器人接近路标点且约束满足时，自动推进任务进度。
    *   **相位回溯（Phase Backtracking）**：若检测到边缘约束被违反（如物体掉落），受影响的智能体可独立回溯到之前的任务阶段，而无需重置整个团队的状态。

### 3. 实验设计
*   **实验场景**：
    *   **积木堆叠 (Block-Stacking)**：涉及并行抓取和顺序放置。
    *   **倒水任务 (Pick-and-Pour)**：涉及两个机器人之间的精细空间协调。
    *   **桌布折叠 (Cloth-Folding)**：涉及双臂的高度同步操作。
*   **对比基准 (Benchmark)**：
    *   **ReKep**：目前最先进的基于关键点约束的反应式 TAMP 方法（主要针对单臂或全序任务）。
*   **实验环境**：
    *   仿真：Isaac-Sim (OmniGibson) 和 Drake。
    *   现实世界：两台 UR5e 协作机器人，使用 RealSense D455 相机进行视觉观测。

### 4. 资源与算力
*   **硬件配置**：实验在一台配备 **12th Gen Intel i7-12700F CPU** 和 **32 GB RAM** 的本地工作站上运行。
*   **算力说明**：
    *   论文未明确提及使用了多少块 GPU，但提到视觉感知部分使用了 **SAM2**（Segment Anything Model 2）进行关键点跟踪，这通常需要主流 NVIDIA GPU 支持。
    *   **计算效率**：GoC-MPC 的计算速度极快，初始解耗时约 0.373s，后续 MPC 循环平均仅需 **0.065s**。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对静态场景和扰动场景分别进行了 10 次重复实验。
    *   **可扩展性实验**：测试了不同数量的物体（5, 8, 11个）和智能体（2, 3, 4个）组合，共计 45 组试验。
    *   **现实验证**：在三种不同任务上各进行了 5 次物理实验。
*   **充分性评价**：实验设计较为全面，涵盖了从仿真到现实、从简单到复杂、从静态到动态扰动的多种维度。通过对比成功率、计算时间和路径长度，客观地证明了该方法在多智能体协作中的优势。

### 6. 主要结论与发现
*   **性能飞跃**：在计算速度上比 ReKep 快约 **40 到 80 倍**，且路径长度更短。
*   **高成功率**：在静态和扰动环境下均保持了近乎 100% 的成功率，而基准方法在复杂协作任务中容易失败。
*   **鲁棒性**：GoC 结构允许智能体独立回溯，有效处理了外部干扰导致的局部任务失败。
*   **无需训练**：该方法完全基于几何约束优化，不需要针对特定任务的训练数据或精确的环境模型。

### 7. 优点
*   **灵活性**：通过 DAG 结构自然地处理了任务的并行性，避免了不必要的等待。
*   **实时性**：优化的分解策略使得复杂的 TAMP 问题能够在机器人控制频率下实时求解。
*   **通用性**：基于关键点（Keypoints）的约束定义使其能够轻松迁移到不同的操纵任务中。
*   **动态分配**：能够根据当前环境状态在线决定哪个机器人执行哪个任务。

### 8. 不足与局限
*   **感知依赖**：高度依赖外部视觉跟踪模块（如 SAM2）。如果发生严重遮挡或跟踪丢失，系统会失效。
*   **初始骨架依赖**：仍需要预先定义任务的“逻辑骨架”（即约束图的结构），无法完全自主地从高层指令生成图结构。
*   **局部最优风险**：作为一种基于优化的方法，在极度复杂的环境中可能会陷入局部最优解，导致路标点规划失败。
*   **应用限制**：目前主要验证了机械臂操纵，对于涉及复杂接触动力学（如摩擦力极大的物体）的建模可能不够精确。

（完）
