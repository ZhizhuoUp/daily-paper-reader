---
title: "Coupling Tensor Trains with Graph of Convex Sets: Effective Compression, Exploration, and Planning in the C-Space"
title_zh: 将张量列与凸集图耦合：在配置空间中进行有效的压缩、探索与规划
authors: "Gerhard Reinerth, Riddhiman Laha, Marcello Romano"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11658v1"
tags: ["query:profile-1"]
score: 9.0
evidence: 用于平滑最优轨迹的基于优化的规划器
tldr: 本研究提出 TANGO 框架，旨在解决高维配置空间中运动规划的效率与建模难题。针对传统凸集图（GCS）方法依赖预定义凸分解的局限，TANGO 创新性地结合了张量列（Tensor Train）分解技术，对可行配置空间进行高效压缩与近似。通过将压缩后的任务相关区域嵌入图优化结构，实现了兼顾几何约束与环境复杂性的轨迹生成。该方法在保持规划质量的同时显著提升了扩展性，为复杂机器人系统的实时运动规划提供了有效方案。
selection_source: fresh_fetch
motivation: 传统的凸集图（GCS）规划方法高度依赖于难以预先获取的高维配置空间精确凸分解。
method: 提出 TANGO 框架，利用张量列分解压缩近似可行配置空间，并将其集成到结构化图优化中进行轨迹生成。
result: 在平面和真实机器人上的仿真实验表明，该方法实现了有效的空间压缩并生成了更高质量的运动轨迹。
conclusion: TANGO 通过耦合张量压缩与图推理，为高维配置空间的高效表示和几何感知运动规划奠定了基础。
---

## 摘要
我们提出了 TANGO（张量与图优化），这是一种新型运动规划框架，它将基于张量的压缩与结构化图优化相结合，以实现高效且可扩展的轨迹生成。虽然诸如凸集图 (GCS) 等基于优化的规划器为生成平滑、最优轨迹提供了强大的工具，但它们通常依赖于高维配置空间的预定义凸表征——这一要求对于通用机器人任务通常是难以处理的。TANGO 通过使用张量列 (Tensor Train) 分解以压缩形式近似可行配置空间，从而进一步实现了对任务相关区域的快速发现和估计。随后，这些区域被嵌入到类 GCS 结构中，从而实现既符合系统约束又考虑环境复杂性的几何感知运动规划。通过将基于张量的压缩与结构化图推理相结合，TANGO 实现了高效的几何感知运动规划，并为未来机器人系统中更具表现力和可扩展性的配置空间表示奠定了基础。在平面和真实机器人上进行的严格仿真研究证实了我们在有效压缩和更高质量轨迹方面的论点。

## Abstract
We present TANGO (Tensor ANd Graph Optimization), a novel motion planning framework that integrates tensor-based compression with structured graph optimization to enable efficient and scalable trajectory generation. While optimization-based planners such as the Graph of Convex Sets (GCS) offer powerful tools for generating smooth, optimal trajectories, they typically rely on a predefined convex characterization of the high-dimensional configuration space-a requirement that is often intractable for general robotic tasks. TANGO builds further by using Tensor Train decomposition to approximate the feasible configuration space in a compressed form, enabling rapid discovery and estimation of task-relevant regions. These regions are then embedded into a GCS-like structure, allowing for geometry-aware motion planning that respects both system constraints and environmental complexity. By coupling tensor-based compression with structured graph reasoning, TANGO enables efficient, geometry-aware motion planning and lays the groundwork for more expressive and scalable representations of configuration space in future robotic systems. Rigorous simulation studies on planar and real robots reinforce our claims of effective compression and higher quality trajectories.

---

## 论文详细总结（自动生成）

### 论文结构化深度总结：TANGO 框架

#### 1. 核心问题与研究动机
在高维配置空间（C-Space）中进行机器人运动规划时，如何高效地表征可行区域并生成平滑、最优的轨迹是一个长期挑战。
*   **研究背景**：基于优化的规划器（如凸集图 GCS）虽然强大，但高度依赖于对配置空间的预定义凸分解，而这种分解在复杂任务中往往难以计算。
*   **核心问题**：传统的采样方法（如 RRT）在高维空间中效率低下，且难以处理复杂的任务约束（如避开奇异点）。
*   **研究动机**：利用张量压缩技术来近似高维配置空间，从而引导高效的区域探索和凸集构建，解决 GCS 规划器的“区域生成”瓶颈。

#### 2. 方法论：TANGO 框架
TANGO（Tensor ANd Graph Optimization）将张量列（Tensor Train, TT）分解与凸集图优化相结合，分为两个主要阶段：

*   **核心思想**：将配置空间的可行性建模为一个高维张量，通过压缩表示来规避“维度灾难”。
*   **关键技术细节**：
    1.  **概率密度函数 (PDF) 构建**：使用任务特定指标（如 Yoshikawa 可操作度或黎曼几何度量）定义配置 $q$ 的成本，并转化为 PDF。
    2.  **TT-Cross 近似**：利用 TT-Cross 算法以贪婪采样方式近似高维 PDF，将存储需求从指数级降为线性级。
    3.  **逆向采样与障碍物识别**：通过 TT 格式的算术运算生成逆 PDF，采样识别出障碍物配置和高质量可行配置。
    4.  **IRIS 区域膨胀**：使用 IRIS 算法在采样点周围膨胀出最大的空闲凸多胞形（Convex Polytopes）。
    5.  **GCS 规划**：将发现的凸集作为图的节点，通过检测集合间的交集建立边，最后在凸集图上求解最短路径问题（SPP）。

#### 3. 实验设计
*   **实验场景**：
    1.  **3-DoF 平面机械臂**：用于可视化分析，对比不同度量指标（Yoshikawa vs. 黎曼度量）对采样分布的影响。
    2.  **4-DoF Franka Emika Panda 机器人**：验证高维扩展性和轨迹质量。
*   **Benchmark（基准方法）**：传统的 RRT 规划器。
*   **对比指标**：执行时间、PDF 分数（可操作度质量）、路径点数量、内存占用、张量参数量。

#### 4. 资源与算力
*   **硬件说明**：论文中**未明确提及**具体的 GPU 型号或 CPU 核心数。
*   **软件实现**：提到使用了多进程（Multiprocessing）来加速 IRIS 区域膨胀和凸集剪枝过程。
*   **离线处理**：张量预处理和凸集生成属于静态初始化步骤，一次计算后可多次用于规划。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   针对 Panda 机器人进行了 **100 组**随机起点-终点对的规划测试。
    *   针对内存和时间足迹进行了 **15 次**重复试验以获取均值和标准差。
*   **充分性评价**：实验涵盖了从简单可视化到复杂机械臂的过渡，对比了 RRT 这一经典基准，并详细分析了张量重塑（Reshaping）对性能的影响。实验设计较为客观，但目前仅限于 4-DoF，尚未完全展示 7-DoF 全空间下的表现。

#### 6. 主要结论与发现
*   **压缩效率**：通过将高维张量重塑（如将 $128^4$ 重塑为 $4^{14}$），内存占用减少了约 84%，执行时间缩短了约 80%。
*   **轨迹质量**：相比 RRT，TANGO 生成的轨迹更平滑（路径点更少，通常仅需 3-4 个），且平均可操作度（PDF Score）更高，能有效避开奇异位姿。
*   **几何感知**：黎曼度量比传统的 Yoshikawa 指标更能准确捕捉奇异点，引导生成更高质量的采样分布。

#### 7. 优点与亮点
*   **创新性耦合**：首次将张量列分解（数学压缩工具）与 GCS（现代优化规划工具）结合，解决了 C-Space 凸分解的难题。
*   **高效表征**：利用 TT 格式的线性扩展性，使得在高维空间中进行复杂的 PDF 采样和区域发现变得可行。
*   **鲁棒性**：通过 IRIS 迭代确保了生成的凸区域在配置空间内是绝对安全的。

#### 8. 不足与局限
*   **维度限制**：虽然理论上可扩展，但文中 Panda 机器人的实验被简化到了 4-DoF，全 7-DoF 的计算复杂度和几何重叠检测仍是挑战。
*   **环境动态性**：目前主要针对静态配置空间预处理，对于动态障碍物的实时响应能力尚未讨论。
*   **任务空间障碍物**：论文未显式处理任务空间（笛卡尔空间）中的复杂障碍物，虽提出了通过 SDF（符号距离函数）结合 TT 的思路，但未在实验中验证。

（完）
