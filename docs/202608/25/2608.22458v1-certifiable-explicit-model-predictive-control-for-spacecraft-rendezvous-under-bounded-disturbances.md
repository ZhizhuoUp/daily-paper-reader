---
title: Certifiable Explicit Model Predictive Control for Spacecraft Rendezvous under Bounded Disturbances
title_zh: 有界扰动下航天器交会的可验证显式模型预测控制
authors: "Diogo Silva, Daniel Silvestre"
date: 2026-08-23
pdf: "https://arxiv.org/pdf/2608.22458v1"
tags: ["query:q2"]
score: 8.0
evidence: 航天器的显式模型预测控制
tldr: 针对航天器交会任务中模型预测控制（MPC）计算耗时波动且缺乏形式化保证的问题，本文提出了一种可验证的显式MPC方案。通过利用参数化二次规划求解器和高效数据结构，结合基于混合带状体的可达性分析，实现了具有离线稳定性保证的控制律。该方法在51步预测时界下仅占用10.5MB内存，计算时间低于300ns，证明了显式MPC在资源受限的立方星任务中的可行性。
selection_source: fresh_fetch
motivation: 传统MPC在航天任务中面临在线计算时间不确定及缺乏闭环稳定性形式化验证的挑战。
method: 采用参数化二次规划求解技术构建显式控制律，并利用混合带状体进行非凸可达性分析以提供离线稳定性证明。
result: 在51步预测时界下，控制律仅需10.5MB内存且评估时间小于300纳秒，适配ARM9级立方星星载计算机。
conclusion: 本文证明了通过优化数据结构和验证流程，显式MPC可以克服预测时界限制，为航天器交会提供高效且可验证的控制方案。
---

## 摘要
基于优化的控制器（如模型预测控制，MPC）的两个特性限制了它们在航天飞行中的应用：在线计算时间不确定且可能超过采样周期，以及闭环行为缺乏形式化保证。显式 MPC 通过计算优化问题的参数化解，将星载优化替换为具有固定执行时间的分段仿射函数查找表。然而，显式 MPC 通常被认为仅适用于短预测时域，因为区域数量限制了查找表占用的内存，且该数量随预测时域呈指数级增长。本文基于二次规划参数化解和非凸可达性分析的最新进展，为 Clohessy-Wiltshire 动力学下的航天器交会控制器提供了一套完整的流程。针对该应用，可以从计算时间和内存占用两个角度构建高效的数据结构来编码控制律。此外，通过利用混合分带算子（hybrid zonotopes）进行可达性分析，可以离线验证控制器并获得有保证的闭环稳定性证明。在 51 步预测时域下，交会控制律仅占用 10.5 MB 内存，且评估时间不足 300 纳秒。所占内存可适配如 CubeSat 飞行计算机 ARM9 级 AT91RM9200，这表明当使用本文提出的参数化求解器和数据结构构建分区时，预测时域的限制将不再存在。

## Abstract
Two properties of optimization-based controllers such as model predictive control (MPC) limit their use in space flight. The online computing time varies and can exceed the sampling period, and the closed-loop behavior carries no formal guarantee. Explicit MPC, where a parametric solution of the optimization problem is computed, replaces the on-board optimization with a lookup table of piecewise-affine functions with a fixed execution time. However, it has been regarded as only applicable to small horizons, since the number of regions bounds the memory the table occupies and admits only an exponential bound in the horizon. This paper builds on recent developments in parametric solutions of quadratic programs and nonconvex reachability analysis to provide an entire pipeline for spacecraft rendezvous controllers under the Clohessy-Wiltshire dynamics. For this application, efficient data structures can be constructed to encode the control law, both from a computing time and a memory standpoint. In addition, the controller can be verified offline with a guaranteed closed-loop stability certificate by resorting to reachability analysis with hybrid zonotopes. At a 51-step horizon the rendezvous law occupies 10.5 MB and is evaluated in under 300 ns. The occupied memory would fit for instance the CubeSat flight computer ARM9-class AT91RM9200 meaning that the horizon limitation does not hold when the partition is built with the parametric solvers and data structures shown in this paper.

---

## 论文详细总结（自动生成）

这篇论文针对航天器交会任务，提出了一种既能满足嵌入式计算资源限制，又能提供形式化稳定性保证的显式模型预测控制（Explicit MPC）方案。以下是对该论文的深度总结：

### 1. 核心问题与整体含义
*   **研究动机**：传统的在线优化 MPC 在航天器上应用面临两大挑战：一是计算时间不确定，可能超过采样周期；二是闭环行为难以提供严格的形式化保证（通常依赖蒙特卡洛模拟，无法覆盖所有极端情况）。
*   **核心矛盾**：显式 MPC 虽然计算时间固定且极快，但其内存占用随预测时域（Horizon）呈指数级增长。业界普遍认为显式 MPC 不适用于长时域任务。
*   **研究目标**：证明通过合理的参数化和数据结构优化，长时域显式 MPC 不仅能装入立方星（CubeSat）的内存，还能通过离线计算获得闭环稳定性的“证书”。

### 2. 方法论
*   **核心思想**：结合“输入参数化”缩减显式解的复杂度，并利用“混合带状体（Hybrid Zonotopes）”进行精确的可达性分析。
*   **关键技术细节**：
    *   **输入参数化（Input Parametrization）**：不直接优化整个预测序列，而是将控制输入限制在低维子空间。对比了标准基、控制时域（Control Horizon）、Laguerre 基和动作阻塞（Move-blocking）四种方式。
    *   **恒定增益区域（Constant-Gain Region, $C_T$）**：推导出了一个闭式解区域。在此区域内，受约束的 MPC 律等价于一个简单的线性反馈增益 $K_T$。
    *   **鲁棒正不变集（RPI）**：在 $C_T$ 内部构建最大鲁棒正不变子集 $O_{W\infty}$，作为验证的停止准则。
    *   **形式化验证流程**：利用混合带状体表示显式 MPC 的分段仿射特性，通过精确的可达性传播，证明从初始色散集出发的所有轨迹都会在有限步 $k^*$ 内进入 $O_{W\infty}$。
*   **算法流程**：离线求解多参数二次规划（mp-QP） $\rightarrow$ 构建二叉搜索树（BST）存储控制律 $\rightarrow$ 进行基于混合带状体的可达性分析 $\rightarrow$ 获得稳定性证书。

### 3. 实验设计
*   **场景设置**：低地球轨道（LEO）航天器交会，使用 Clohessy-Wiltshire (CW) 动力学模型。初始距离约 1.4km 至 2km，目标是进入 $\pm 20$m 的对接框。
*   **Benchmark 与对比**：
    *   对比了不同预测时域（$N=6, 11, 16, 51$）。
    *   对比了多种参数化方法在内存占用、计算时间、离线构建时间和控制性能（燃料消耗）上的表现。
    *   在非线性植物模型（考虑 J2 摄动、大气阻力等）中验证线性控制器的鲁棒性。

### 4. 资源与算力
*   **算力设备**：离线计算使用 AMD Ryzen 7 5700X3D CPU (8核, 3.0-4.1 GHz)，32GB RAM。
*   **计算耗时**：
    *   **离线**：51 步时域的显式解构建约需 707 秒。
    *   **在线**：单次控制律评估时间平均为 86 纳秒，最差情况低于 300 纳秒。
*   **存储空间**：51 步时域下的控制律占用 10.5 MB 内存。

### 5. 实验数量与充分性
*   **实验规模**：针对每种参数化配置进行了 100 次蒙特卡洛模拟以评估平均性能。
*   **充分性**：实验不仅涵盖了名义模型，还通过可达性分析覆盖了整个初始状态色散空间和有界扰动空间。
*   **客观性**：通过调整权重使所有对比方法的燃料消耗（$\Delta v$）匹配，从而在相同的“燃料预算”下公平比较控制精度和内存效率。

### 6. 主要结论与发现
*   **打破认知**：显式 MPC 完全可以支持长达 51 步的时域，且内存占用（10.5 MB）完全适配 ARM9 级的星载计算机。
*   **最优参数化**：在航天器交会中，“控制时域（Control Horizon）”法表现最优，其闭环成本比标准设计低 22%，且构建速度快 3 倍。
*   **验证效率**：对于 LEO 场景，证明了系统在 4 到 6 步内即可进入线性稳定区域，从而简化了长时域任务的安全性证明。

### 7. 优点
*   **极高性能**：评估时间在纳秒级，比采样周期（秒级）小 6 个数量级，消除了计算延迟风险。
*   **形式化保证**：不同于概率性的蒙特卡洛模拟，该方法提供了确定性的约束满足和稳定性证明。
*   **闭式解创新**：提出了任意线性输入参数化下 $C_T$ 区域的闭式表达式，极大降低了离线验证的计算负担。

### 8. 不足与局限
*   **模型依赖**：主要基于线性 CW 模型。虽然通过扰动项考虑了非线性，但对于高偏心率轨道或大范围机动，线性化误差可能过大导致 $O_{W\infty}$ 为空。
*   **扰动边界**：验证的有效性高度依赖于对扰动边界 $W$ 的准确估计，若实际扰动超出预设边界，稳定性证书将失效。
*   **维度瓶颈**：虽然解决了时域增长问题，但如果状态维度（如加入姿态、挠性动力学）大幅增加，显式分区的数量仍可能面临爆炸风险。

（完）
