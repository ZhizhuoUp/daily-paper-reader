---
title: "PathCover: A Fast Convex Decomposition along a Path via Randomized Iterative Space Partitioning (RISP) on Point Clouds"
title_zh: PathCover：一种基于点云随机迭代空间划分 (RISP) 的沿路径快速凸分解方法
authors: "Kunal S. Narkhede, Abhijeet M. Kulkarni, Guoquan Huang, Ioannis Poulakakis"
date: 2026-08-06
pdf: "https://arxiv.org/pdf/2608.05586v1"
tags: ["query:profile-1"]
score: 9.0
evidence: 用于实时轨迹规划和MPC约束的快速凸分解
tldr: PathCover是一个用于机器人导航的高效凸分解框架，旨在解决走廊生成算法无法满足实时传感器频率计算需求的瓶颈。该框架引入了RISP随机算法，能直接从原始点云中以期望线性时间构建无障碍凸多胞形序列。该方法在数学上保证了沿路径的持续进展和有限步终止，在保持走廊体积的同时，比现有最先进方法快一个数量级，并在无人机和四足机器人上完成了实机验证。
selection_source: fresh_fetch
motivation: 现有的走廊生成算法计算效率较低，难以满足自主机器人导航中实时、传感器频率级别的轨迹规划需求。
method: 提出RISP随机迭代空间划分算法，通过随机化策略从原始点云中快速构建重叠的无障碍凸多胞形。
result: 在合成和真实LiDAR数据集上的测试表明，该方法比现有技术快一个数量级，且在四足机器人和无人机上实现了成功部署。
conclusion: PathCover为受限环境下的机器人实时避障规划提供了一种具有数学完备性且极具计算效率的解决方案。
---

## 摘要
自主机器人导航需要为轨迹规划快速生成无障碍区域。然而，现有的走廊生成器难以满足实时、传感器频率的计算约束。为了解决这一瓶颈，我们引入了 PathCover，这是一个由 RISP 驱动的框架；RISP 是一种新颖的随机算法，在温和的概率消除条件下，能以期望线性时间直接从原始点云数据构建凸多胞体。PathCover 生成一系列重叠的无障碍多胞体，为下游的 MPC 和轨迹优化提供安全约束。我们在数学上保证了该算法在有限步内终止，同时确保沿任何无障碍参考路径的持续进展。在合成和真实世界 LiDAR 数据集上的广泛基准测试表明，与现有最先进方法相比，该方法在保持相当的走廊体积的同时，实现了数量级的加速。完整的流程通过高保真四旋翼无人机仿真以及在受限环境中使用实时 LiDAR 感知的四足机器人物理部署得到了验证。

## Abstract
Autonomous robot navigation requires the rapid generation of obstacle-free regions for trajectory planning. However, existing corridor generators struggle to meet real-time, sensor-rate computational constraints. To resolve this bottleneck, we introduce PathCover, a framework driven by RISP; a novel randomized algorithm that constructs convex polytopes directly from raw point cloud data in expected linear time under a mild probabilistic elimination condition. PathCover generates sequences of overlapping, obstacle-free polytopes that safely constrain downstream MPC and trajectory optimization. We mathematically guarantee that the algorithm terminates in finite steps while ensuring continuous progress along any obstacle-free reference path. Extensive benchmarks on synthetic and real-world LiDAR datasets demonstrate an order-of-magnitude speedup over state-of-the-art methods while maintaining comparable corridor volumes. The complete pipeline is validated via high-fidelity quadrotor simulations and physical deployment on a quadrupedal robot navigating constrained environments using live LiDAR perception.

---

## 论文详细总结（自动生成）

以下是对论文《PathCover: A Fast Convex Decomposition along a Path via Randomized Iterative Space Partitioning (RISP) on Point Clouds》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
在自主机器人导航中，将原始传感器数据（如点云）实时转化为“安全走廊”（Safe Corridors，一系列重叠的凸多胞形）是轨迹规划的关键步骤。现有的方法（如 IRIS, FIRI, Decomp）面临计算瓶颈：
*   **优化驱动型方法**（如 IRIS）：虽然生成的空间体积大，但涉及复杂的半正定规划（SDP），计算耗时过长，难以匹配传感器的更新频率。
*   **非优化方法**：虽然速度较快，但往往与路径分辨率耦合，导致生成的走廊过于碎片化，或在处理高密度点云时扩展性差。
**研究动机**：开发一种能够直接处理原始点云、具有线性时间复杂度、且能保证实时性与路径覆盖完备性的快速凸分解算法。

### 2. 方法论：核心思想与算法流程
论文提出了 **PathCover** 框架，其核心是 **RISP（随机迭代空间划分）** 算法：

*   **RISP 算法核心思想**：
    1.  **随机采样**：从障碍物点云中随机选择一个点 $p$。
    2.  **构建超平面**：在种子点（Seed）与采样点 $p$ 之间构建一个正交超平面，并引入安全裕度参数 $\alpha$。
    3.  **快速剪枝**：立即剔除位于超平面“障碍物侧”的所有点。
    4.  **迭代**：重复上述过程，直到点云为空。由于每次随机采样通常能消除大量点，算法在期望意义下具有 **$O(n)$ 的线性时间复杂度**。
    5.  **冗余消除**：利用对偶空间变换和 Quickhull 算法移除冗余的超平面约束。

*   **PathCover 框架流程**：
    1.  输入参考路径（如 JPS 生成的路径）和原始点云。
    2.  从起点开始调用 RISP 生成第一个凸多胞形。
    3.  寻找路径与当前多胞形边界的交点，将其作为下一个种子点再次调用 RISP。
    4.  重复直至覆盖整个路径，生成一系列重叠的凸多胞形。

### 3. 实验设计
论文通过多维度基准测试验证了性能：
*   **数据集/场景**：
    *   **合成环境**：50x50x5m 的空间，分为稀疏（约6万点）、中等（约18万点）、稠密（约55万点）三种障碍物密度。
    *   **真实世界**：使用 LiDAR 采集的办公环境和室内走廊数据。
*   **对比方法（Benchmark）**：
    *   **Decomp**（基于膨胀的基准方法）
    *   **FIRI / CIRI**（最先进的迭代区域膨胀方法）
    *   **IRIS**（基于 SDP 的体积最大化方法，作为质量上限）
    *   **Galaxy**（基于凸包的 2D 方法）
*   **评估指标**：计算耗时（ms）、多胞形数量、多胞形平均体积。

### 4. 资源与算力
*   **硬件环境**：所有测试均在配备 **Intel i7-9750H CPU (2.6 GHz)** 和 **16GB RAM** 的笔记本电脑上运行。
*   **算力需求**：该算法主要依赖 CPU 计算，**未提及使用 GPU 加速**。这证明了该算法对嵌入式计算平台的友好性。
*   **软件实现**：使用 C++/ROS 实现，优化器使用了 OSQP 和 FATROP。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在合成测试中，每种密度下进行了 100 次随机起点-终点测试（共 300 组路径覆盖实验）。
    *   在单多胞形压力测试中，跨越 3 个数量级的点云规模进行了 600 次独立试验。
    *   **实机验证**：在 Gazebo 仿真中的四旋翼无人机和物理实体四足机器人（Ghost Robotics Vision60）上进行了闭环导航实验。
*   **充分性评价**：实验设计非常充分，涵盖了从理论复杂度验证到端到端系统部署的完整链条。通过对比不同密度的点云，客观展示了算法在极端情况下的鲁棒性。

### 6. 主要结论与发现
*   **速度优势**：PathCover 比最快的优化方法（CIRI）快 **17 倍**，比 FIRI 快 **44 倍**。在处理 55 万个点时，耗时仅约 106ms。
*   **复杂度验证**：实验数据证实了 RISP 算法的 **期望线性时间复杂度**，且迭代次数随点云规模呈对数增长（$O(\log n)$）。
*   **走廊质量**：虽然生成的单个多胞形体积比 IRIS 小（约为其 40%-50%），但生成的走廊更加紧凑，且多胞形数量更少（减少了 50%-75%），这显著降低了下游轨迹优化器的约束负担。
*   **完备性**：数学证明了算法能在有限步内终止并完全覆盖无障碍路径。

### 7. 优点
*   **极高的计算效率**：将凸分解从“分钟/秒”级降低到了“毫秒”级，真正实现了传感器频率（20Hz+）的实时重规划。
*   **算法简洁性**：基于简单的几何操作而非复杂的非线性优化，易于实现、调试和部署。
*   **预测性强**：计算时间随点云规模线性增长，避免了优化算法可能出现的收敛失败或耗时波动。
*   **硬件通用性**：在无人机和四足机器人上均表现良好，证明了其作为通用导航模块的潜力。

### 8. 不足与局限
*   **保守性**：由于采用随机划分而非体积最大化优化，生成的安全区域相对保守（体积较小），在极度狭窄的空间内可能导致规划器难以找到可行解。
*   **随机性波动**：虽然期望时间是线性的，但单次运行的几何形状受随机采样影响，可能存在一定的方差（尽管实验表明这种方差在可接受范围内）。
*   **静态假设**：虽然在动态仿真中进行了测试，但算法本身主要处理静态点云快照，对于高速动态障碍物的预测性建模尚需结合其他机制。

（完）
