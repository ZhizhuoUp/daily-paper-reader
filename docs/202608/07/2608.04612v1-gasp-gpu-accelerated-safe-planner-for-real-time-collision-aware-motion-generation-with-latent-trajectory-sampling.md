---
title: "GASP: GPU-Accelerated Safe Planner for Real-Time Collision-Aware Motion Generation with Latent Trajectory Sampling"
title_zh: GASP：基于潜空间轨迹采样的实时碰撞感知运动生成 GPU 加速安全规划器
authors: "Colin Merk, Stefanos Charalambous, Peter Dürr, Farshad Khadivar"
date: 2026-08-05
pdf: "https://arxiv.org/pdf/2608.04612v1"
tags: ["query:profile-1"]
score: 10.0
evidence: GPU加速的关节空间实时避障运动生成
tldr: 本研究提出GASP，一种GPU加速的安全规划器，旨在解决已知环境下的实时避障关节空间运动生成问题。该方法结合了B样条轨迹参数化与卷积残差神经网络，利用条件变分自编码器（CVAE）在潜空间采样轨迹候选，并在GPU上并行验证。GASP实现了近毫秒级的推理速度，在保持高成功率的同时显著降低了计算开销，并成功应用于机器人乒乓球强化学习任务中，有效减少了训练碰撞。
selection_source: fresh_fetch
motivation: 旨在解决已知环境下实时、避障的关节空间运动生成中计算开销大、推理速度慢的问题。
method: 结合B样条参数化与卷积残差网络，利用CVAE采样轨迹候选并在GPU上进行大规模并行解码与验证。
result: 实现了近毫秒级的推理速度和极高的避障成功率，显著优于现有的GPU轨迹优化方法。
conclusion: GASP作为一种高效的在线运动生成模块，能有效提升机器人复杂任务的训练效率与安全性。
---

## 摘要
我们提出了 GASP，这是一种用于已知环境中实时、碰撞感知关节空间运动生成的 GPU 加速安全规划器。GASP 将夹紧式 B 样条轨迹参数化与卷积残差神经网络相结合，该网络预测自由内部控制点，同时通过解析插入的边界控制点强制执行初始和最终导数约束，以实现在非平稳条件下的碰撞感知规划。条件变分自编码器采样多个候选轨迹，这些轨迹在 GPU 上并行解码和验证，从而产生了一个用于碰撞感知耦合关节空间运动的批量规划器，其推理时间接近毫秒级。我们将 GASP 作为在线运动生成模块进行了验证，它实现了解析级的成功率和极高的碰撞感知可行性，并且相对于基于 GPU 的轨迹优化，显著缩短了推理时间。我们进一步将 GASP 部署为竞技机器人乒乓球中的强化学习重置规划器，在匹配基准回球率的同时，将训练时的碰撞次数减少了约一半。

## Abstract
We present GASP, a GPU-Accelerated Safe Planner for real-time, collision-aware joint-space motion generation in known environments. GASP combines a clamped B-spline trajectory parameterization with a convolutional residual neural network that predicts the free interior control points, while analytically inserted boundary control points enforce initial and final derivative constraints for collision-aware planning under non-stationary conditions. A conditional variational autoencoder samples multiple trajectory candidates, which are decoded and validated in parallel on the GPU, yielding a batched planner for collision-aware coupled joint-space motion with near-millisecond inference. We validate GASP as an online motion-generation module, where it achieves analytical-level success rates with high collision-aware feasibility and substantially reduces inference time relative to GPU-based trajectory optimization. We further deploy GASP as a reinforcement-learning reset planner in competitive robotic table tennis, matching the baseline return rate while roughly halving training-time collisions.

---

## 论文详细总结（自动生成）

以下是对论文《GASP: GPU-Accelerated Safe Planner for Real-Time Collision-Aware Motion Generation with Latent Trajectory Sampling》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在动态变化的机器人操作任务中（如乒乓球机器人），如何在保证**实时性（毫秒级推理）**的同时，生成既满足**动力学约束**又具备**避障能力**的关节空间轨迹。
*   **研究背景**：
    *   **解析式规划器**（如 Ruckig）：速度极快且确定，但通常忽略环境碰撞。
    *   **优化式规划器**（如 cuRobo）：避障能力强，但计算开销大且推理时间波动大，难以集成到高频强化学习（RL）循环中。
    *   **研究动机**：开发一种结合解析法速度与学习法避障能力的 GPU 加速规划器，通过并行采样多个候选轨迹来提高复杂环境下的成功率。

### 2. 方法论：核心思想与技术细节
GASP 结合了 B 样条参数化、卷积神经网络（CNN）和条件变分自编码器（CVAE）：
*   **轨迹表示**：使用 7 阶夹紧式 B 样条（Clamped B-spline）。
    *   **边界约束解析化**：通过解析法计算前 3 个和后 2 个控制点，以严格保证起始点（位置、速度、加速度）和终点（位置、速度）的导数约束。
    *   **内部点学习化**：神经网络仅预测中间自由控制点的残差偏移量，降低了学习难度并保证了轨迹平滑性。
*   **核心架构**：
    *   **CVAE 潜空间采样**：利用 CVAE 在潜空间采样多个随机向量 $z$，允许针对同一查询生成多种避障路径候选。
    *   **卷积残差网络**：采用 1D 卷积处理时间序列特征，通过残差块优化内部控制点。
*   **GPU 并行验证流水线**：
    *   在 GPU 上批量生成候选轨迹，并行进行密集 B 样条解码、关节限位检查、自碰撞及环境碰撞检测（使用球体和长方体近似几何体）。
    *   **选择器**：从所有可行候选轨迹中根据启发式规则（如速度优先）选择最优路径。
*   **训练目标**：使用可微惩罚项，包括动力学限位损失、碰撞损失、加加速度（Jerk）平滑损失和时间效率损失，无需专家轨迹监督。

### 3. 实验设计
*   **实验场景**：针对一个 8 自由度（DOF）的自定义机械臂，在已知静态环境中进行运动规划。
*   **Benchmark 与对比方法**：
    *   **Ruckig**：解析式时间最优轨迹生成器（不避障）。
    *   **cuRobo**：基于 GPU 的轨迹优化规划器（避障）。
    *   **MLP Baseline**：基于多层感知器的神经规划器。
*   **评估任务**：
    *   **离线数据集验证**：在包含非平稳初始状态（带初速度和加速度）的大规模数据集上测试成功率。
    *   **强化学习重置规划**：在机器人乒乓球训练任务中作为重置规划器（Reset Planner）部署。

### 4. 资源与算力
*   **算力说明**：论文强调了“近毫秒级”的推理性能，并指出所有计算（采样、解码、碰撞检测）均在 GPU 上并行完成。
*   **具体型号**：文中**未明确提及**训练和推理所使用的具体 GPU 型号（如 A100 或 RTX 4090），但提到推理时间在 16 批次下约为 1.08ms。
*   **训练细节**：使用 PyTorch Lightning 和 AdamW 优化器，训练 600 个 Epoch，包含视野课程学习（Horizon Curriculum）。

### 5. 实验数量与充分性
*   **实验规模**：进行了多组对比实验，每组实验均包含 5 次独立运行以获取均值和标准差。
*   **消融实验**：非常充分，涵盖了 CVAE 模块、视野课程学习、卷积核大小、分组卷积数量等关键组件的消融分析。
*   **公平性**：通过使用相同的评估集，并根据不同规划器的特性（如是否过滤动力学不可行样本）进行分组对比，体现了实验的客观性。

### 6. 主要结论与发现
*   **性能优越**：GASP 的推理速度（约 1ms）远快于 cuRobo（约 56ms），且在非平稳初始条件下（高初速/加速度）的成功率显著高于 cuRobo。
*   **避障有效性**：相比于不避障的 Ruckig，GASP 在保持相近速度的同时，大幅降低了碰撞率。
*   **RL 增益**：在乒乓球机器人训练中，GASP 将轨迹碰撞段减少了约 50%，同时保持了与基准相当的回球率，证明了其作为在线规划器的稳定性。
*   **批量采样优势**：随着部署批次增加（从 1 到 16），成功率明显提升，而推理延迟增加极小。

### 7. 优点
*   **混合驱动**：巧妙结合了解析约束（保证边界安全）与深度学习（处理复杂避障）。
*   **极高效率**：充分利用 GPU 并行性，实现了极低的推理延迟和确定性时间开销。
*   **无需专家数据**：通过可微物理损失进行自监督训练，降低了数据获取成本。
*   **鲁棒性**：对非平稳边界条件（Moving targets/Tracking errors）具有很强的适应性。

### 8. 不足与局限
*   **机器人特定性**：模型训练与特定机器人的运动学限位和几何结构绑定，缺乏跨机器人的泛化能力。
*   **静态环境限制**：目前的碰撞检测基于静态原始几何体，对于复杂动态障碍物的处理能力尚待验证。
*   **调参依赖**：损失函数中的多个权重（如碰撞 vs 平滑）需要手动调节，可能存在任务敏感性。
*   **泛化风险**：虽然在已知环境中表现优异，但在未见过的极端障碍物分布下的表现未被充分讨论。

（完）
