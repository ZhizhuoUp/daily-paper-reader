---
title: "Grasp Execution Without a Planner: Configuration-Space Grasp Distance Fields with Certified Safety & Guaranteed Quality"
title_zh: 无需规划器的抓取执行：具有认证安全性和保证质量的位形空间抓取距离场
authors: "Clinton Enwerem, John S. Baras, Calin Belta"
date: 2026-08-01
pdf: "https://arxiv.org/pdf/2608.00600v2"
tags: ["query:profile-1"]
score: 9.0
evidence: 无碰撞轨迹、CBF-CLF二次规划以及臂手配置空间
tldr: 针对多指抓取中传统规划-跟踪架构在面对不确定性时需频繁重规划的问题，本文提出抓取距离场（GDF）。该方法在机械臂-手配置空间内构建平滑的软最小值距离场，通过梯度下降实现抓取，无需预设路径。结合CBF-CLF二次规划确保安全性，并利用滞后切换机制处理接触。实验证明其在复杂动态场景下具有高成功率和实时性。
selection_source: fresh_fetch
motivation: 传统的抓取执行依赖预先规划的轨迹，在物体位置不确定或受扰动时容易失效且重规划成本高。
method: 提出在配置空间构建抓取距离场（GDF），通过负梯度控制并结合CBF-CLF二次规划来保证避障安全与抓取质量。
result: 在仿真中成功抓取并提升了50个测试物体中的46个，QP求解时间仅为0.09毫秒，且保持了极高的抓取质量。
conclusion: 该方法消除了对显式路径规划器的需求，为复杂动态环境下的安全、高质量多指抓取提供了一种高效的实时控制方案。
---

## 摘要
标准的多指抓取执行架构通常为选定的抓取位姿规划一条无碰撞轨迹，并利用反馈控制律进行跟踪。执行时的物体位姿不确定性或扰动可能会使规划的轨迹失效，从而被迫进行代价高昂的重新规划。我们提出了抓取距离场（GDFs），这是一种在臂-手位形空间中定义的、关于抓取位形的平滑 softmin 距离场。我们的控制器通过静态反馈控制律沿 GDF 负梯度方向执行抓取，消除了对规划器、存储轨迹或抓取选择的需求。为了安全性，我们通过控制屏障函数-控制李雅普诺夫函数（CBF-CLF）二次规划（QP）对生成的指令进行滤波，约束自碰撞、工作空间、物体和障碍物的间隙，并将受阻的进度报告为显式松弛变量。我们证明了对于 $N$ 个候选位形和平衡参数 $ρ$，softmin 能够在 $\log N/ρ$ 误差内跟踪真实的集合距离，并且滤波后的闭环系统使安全集具有前向不变性。由于平滑场无法捕捉手与物体之间的接触切换，我们在预抓取位形处使用滞回机制在手部闭合和抓取保持模式之间切换，以牺牲物体避障为代价换取接触准入。随后，一个力旋量质量 CBF 将实现的抓取力封闭裕度限制在抓取开始时数值的规定容差范围内。我们在固定底座机械臂和宇树 G1 人形机器人上进行了运动学仿真评估，两者均配备了相同的欠驱动手。我们的控制器在杂乱和动态场景中导航，安全地抓取并举起了 50 个测试物体中的 46 个，涵盖了基本几何体、家用物品和对抗性类别。在这 46 次举起任务中，执行的抓取保留了合成质量裕度的中位数 94%，且在 20 ms 的控制步长中，QP 求解时间仅为 0.09 ms。项目页面：www.clintonenwerem.com/gdf。

## Abstract
Standard multifingered grasp execution architectures plan a collision-free trajectory to a selected grasp pose and track it with a feedback law. Execution-time object pose uncertainty or perturbations may invalidate the planned trajectory, forcing a costly replanning step. We present Grasp Distance Fields (GDFs), smooth softmin distance fields over grasp configurations in the arm-hand configuration space. Our controller executes a grasp by following the negative GDF gradient with a stationary feedback law, eliminating the need for a planner, stored trajectory, or grasp selection. For safety, we filter the resulting command through a CBF-CLF quadratic program (QP), constraining self-collision, workspace, object, and obstacle clearance, reporting impeded progress as explicit slack. We prove that the softmin tracks the true set distance within $\log N/ρ$ for $N$ candidates and smoothing parameter $ρ$, and that the filtered closed loop renders the safe set forward invariant. Since no smooth field captures hand-object contact switching, we switch between hand closure and grasp retention modes using hysteresis at a pregrasp configuration, trading object collision avoidance for contact admission. A wrench-quality CBF then bounds the realized grasp's force-closure margin within a prescribed tolerance of its value at hold onset. We evaluate in kinematic simulation on a fixed-base arm and a Unitree G1 humanoid, both fitted with the same underactuated hand. Our controller navigates cluttered and dynamic scenes to safely reach for, grasp, and lift 46 of 50 test objects spanning primitive, household, and adversarial classes. Across the 46 lifts, the executed grasps retain a median 94% of their synthesized quality margin, at 0.09 ms QP solve time per 20 ms control step. Project page: www.clintonenwerem.com/gdf.

---

## 论文详细总结（自动生成）

### 论文总结：无需规划器的抓取执行——具有认证安全性和保证质量的位形空间抓取距离场

#### 1. 核心问题与研究动机
传统的多指抓取执行通常采用“先规划、后跟踪”的架构：首先生成抓取位姿，然后规划一条无碰撞轨迹，最后由控制器跟踪。然而，在实际执行中，物体位姿的不确定性或环境的动态变化（如障碍物移动）往往会导致预设轨迹失效，迫使系统进行计算昂贵的重新规划（Replanning）。
**研究动机**：开发一种**反应式（Reactive）**抓取控制方法，消除显式的路径规划步骤，使机器人能够实时应对环境扰动，同时在数学上保证避障安全性和抓取质量。

#### 2. 方法论核心思想
论文提出了一种基于**抓取距离场（GDF）**的控制框架，核心技术包括：
*   **位形空间抓取距离场（GDF）**：在机械臂和手的联合位形空间（C-space）中定义。利用 `softmin` 函数将多个候选抓取位形融合成一个平滑的距离场。其负梯度方向自然指向当前状态下“最近”的抓取目标，从而实现了自动的抓取选择，无需离散的选择步骤。
*   **CBF-CLF 二次规划（QP）滤波器**：
    *   **CLF（控制李雅普诺夫函数）**：驱动系统沿 GDF 梯度下降，向目标抓取位形收敛。
    *   **CBF（控制屏障函数）**：作为硬约束，确保系统不发生自碰撞，并与环境障碍物、工作空间边界及目标物体保持安全间隙。
    *   **松弛变量（Slack Variables）**：当安全约束与进展目标冲突时，通过松弛变量记录受阻进度，保证 QP 始终可解。
*   **力旋量质量约束（Wrench-Quality Constraint）**：引入一种特殊的 CBF，将抓取过程中的力封闭（Force-closure）裕度限制在初始值的容差范围内，确保抓取后的稳定性。
*   **混合模式切换机制**：由于平滑场无法处理“接触”这一非连续物理事件，设计了一个包含 REACH（接近）、CLOSE（闭合）、HOLD（保持）、LIFT（提升）四个模式的有限状态机，利用滞后（Hysteresis）机制处理接触准入。

#### 3. 实验设计
*   **实验场景**：包括杂乱的桌面环境、带有动态障碍物的路径、以及接近路径被部分遮挡的复杂场景。
*   **测试对象**：使用了包含 50 个物体的测试集，涵盖了基本几何体（Primitives）、YCB 家用物品数据集以及 EGAD 对抗性几何形状数据集。
*   **对比基准（Benchmark）**：
    *   **标称指令（Nominal Command）**：不带安全滤波器的直接梯度下降。
    *   **质量中性 QP（Quality-Neutral QP）**：带避障约束但不带力旋量质量约束的控制器。
*   **机器人平台**：一个 7-DOF 机械臂配 11 关节多指手，以及一个 Unitree G1 人形机器人。

#### 4. 资源与算力
*   **计算效率**：论文强调了算法的实时性。在 20ms 的控制周期内，QP 求解时间仅需 **0.09ms**，包含碰撞检测在内的完整控制步骤耗时约 **2ms**。
*   **算力说明**：文中未明确提及具体的 GPU/CPU 型号，但从计算耗时来看，该算法对算力要求较低，适合在嵌入式控制器上运行。实验主要在运动学仿真环境（基于 Pinocchio 和 Drake）中完成。

#### 5. 实验数量与充分性
*   **实验规模**：对 50 个不同类别的物体进行了抓取测试，并针对动态障碍物和人形机器人平台进行了专项验证。
*   **充分性评价**：实验设计较为充分。不仅验证了成功率（46/50），还通过消融实验展示了“质量约束”在防止物体滑落方面的必要性。通过在不同机器人形态（固定底座 vs 人形）上的测试，证明了算法的通用性。实验过程客观，对失败案例（如陷入局部最优）也进行了诚实的分析。

#### 6. 主要结论与发现
*   **无需规划器**：通过 GDF 梯度下降配合安全滤波，可以实现复杂环境下的安全抓取，无需显式路径规划。
*   **安全性与质量保证**：数学上证明了安全集的前向不变性（Forward Invariance），且执行过程能保留 94%（中位数）的合成抓取质量。
*   **实时反应能力**：系统能实时避开动态障碍物，并在路径完全封死时安全停止，而非发生碰撞。
*   **跨平台通用**：同一套控制器逻辑无需修改即可直接应用于人形机器人。

#### 7. 优点与亮点
*   **理论完备**：结合了现代控制理论（CBF/CLF）与抓取分析，提供了严格的安全性和质量证明。
*   **反应式控制**：相比传统的“规划-跟踪”架构，对环境扰动和感知噪声的容忍度更高。
*   **高效性**：极低的计算开销使其非常适合实际工业或服务机器人应用。
*   **无缝集成**：将抓取选择、避障和执行整合进一个统一的优化框架中。

#### 8. 不足与局限
*   **局部极小值（Local Minima）**：作为一种势场类方法，在极度复杂的非凸空间中可能会陷入局部最优（如在障碍物前停止而无法绕过），论文也承认了这一点。
*   **仿真与现实的差距**：实验主要在运动学仿真中进行，虽然考虑了接触模型，但在真实硬件上的动力学效应（如惯性、摩擦力突变、感知延迟）仍需进一步验证。
*   **感知依赖**：算法高度依赖于目标物体和障碍物的实时位姿/网格信息，若感知系统失效，安全性将无法保障。

（完）
