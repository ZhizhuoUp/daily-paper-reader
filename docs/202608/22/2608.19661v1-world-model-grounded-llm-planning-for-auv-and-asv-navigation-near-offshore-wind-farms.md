---
title: World-Model-Grounded LLM Planning for AUV and ASV Navigation Near Offshore Wind Farms
title_zh: 基于世界模型的 AUV 与 ASV 海上风电场附近导航 LLM 规划
authors: "Markus Buchholz, Ignacio Carlucho, Yvan R. Petillot"
date: 2026-08-20
pdf: "https://arxiv.org/pdf/2608.19661v1"
tags: ["query:q3"]
score: 8.0
evidence: 基于梯度的轨迹优化器与MPC式重规划器
tldr: 针对大语言模型（LLM）在机器人规划中缺乏物理感知的问题，本文提出一种基于世界模型增强的LLM规划框架，用于海上风电场附近的AUV和ASV导航。该方法结合了物理神经世界模型、轨迹优化器和闭环重规划器，使LLM负责高层决策而世界模型负责底层物理执行。实验表明，该方法在复杂海洋环境下显著降低了导航误差并实现了零碰撞，同时支持利用视觉语言模型进行高精度的语义地图构建。
selection_source: fresh_fetch
motivation: LLM在将自然语言指令转化为机器人动作时缺乏物理常识，难以准确判断动作执行时长或预测环境中的物理碰撞。
method: 提出一种包含物理神经世界模型、三阶段梯度轨迹优化器和带信任区域保护的MPC闭环重规划器的集成框架。
result: "在AUV和ASV的仿真测试中，该方法消除了预测碰撞，并将目标距离误差降低了70%至93%，且VLM语义映射准确率达96%。"
conclusion: 通过将LLM的决策能力与世界模型的物理接地能力相结合，显著提升了自主水下和水面机器人在复杂海洋环境中的规划精度和安全性。
---

## 摘要
大语言模型可以将自然语言任务转化为一系列机器人动作，但它们缺乏物理感：无法判断指令应运行多长时间，或者是否会导致机器人漂移并撞上障碍物。我们提出使用世界模型来扩展基于大语言模型的规划器的能力。我们的方法包含三个组件：一个基于物理的神经世界模型、一个三阶段基于梯度的轨迹优化器，以及一个带有置信域保护的模型预测控制（MPC）式闭环重规划器。语言模型决定做什么，而世界模型决定做多久，无论这意味着通过 6 自由度驱动八个推进器，还是通过 3 自由度驱动两个差分推进器。我们评估了在海上风电基础设施附近运行的两类海洋航行器：一个 6 自由度自主水下航行器（AUV）和一个 3 自由度差分驱动自主水面航行器（ASV）。在每个平台的五个基准任务中，两类航行器均以零预测碰撞到达了所有目标，并在洋流、波浪和推进器动力学环境下成功迁移至 GazeboSim 仿真。在经过残差微调（分别将代理展开均方根误差 RMSE 降低了 60% (AUV) 和 69% (ASV)）后，它们保持了无碰撞状态，且与未结合物理模型的基准相比，GazeboSim 目标距离误差降低了 70-82% (ASV) 和约 93% (AUV)。对于 ASV，我们进一步展示了一个视觉语言模型（VLM）辅助的语义映射流水线，该流水线从卫星图像、海图和预报 API（而非车载传感器）中提取障碍物和环境上下文，作为手动指定障碍物几何形状的直接替代方案，达到了 96% 的可航行性准确度。

## Abstract
Large language models can turn a natural-language mission into a sequence of robot actions, but they do not have a sense of physics: they cannot judge how long a command should run, or whether it will make the robot drift into an obstacle. We proposed the use of a world model to expand the capabilities of Large Language model-based planners. Our method has three components: a physics-grounded neural world model, a three-phase gradient-based trajectory optimizer, and a Model Predictive Controller (MPC)-style closed-loop replanner with a trust-region guard. The language model decides what to do, and the world model decides how long, whether that means driving eight thrusters through 6 DOF or two differential thrusters through 3 DOF. We evaluate two marine vehicle classes operating near offshore wind infrastructure: a 6-DOF Autonomous Underwater Vehicle (AUV) and a 3-DOF differential-drive Autonomous Surface Vehicle (ASV). In five benchmark missions per platform, both vehicles reach every goal with zero predicted collisions, and both transfer to GazeboSim under ocean current, waves, and thruster dynamics, remaining collision-free and cutting GazeboSim goal-distance error versus the ungrounded baseline by 70-82% (ASV) and roughly 93% (AUV), after a residual fine-tuning pass that separately reduces surrogate rollout Root Mean Square Error (RMSE) by 60% (AUV) and 69% (ASV). For the ASV we further demonstrate a Vision language model (VLM)-assisted semantic-mapping pipeline that extracts obstacles and environmental context from satellite imagery, nautical charts, and forecast Application Programming Interface (API) instead of onboard sensors, reaching 96% navigability accuracy as a drop-in replacement for hand-specified obstacle geometry.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种结合大语言模型（LLM）与物理世界模型的混合规划框架，旨在解决海洋机器人在复杂环境下导航时的“物理缺失”问题。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：LLM 虽然擅长将自然语言指令分解为逻辑步骤，但缺乏对物理规律的感知（如推进器延迟、流体动力学耦合、洋流导致的侧滑等）。在海上风电场等复杂环境中，这种“物理盲区”会导致机器人规划出的路径在几何上合理但在物理上不可行，从而引发碰撞或任务失败。
*   **研究动机**：通过引入一个可微分的“世界模型”作为物理接地（Grounding）层，让 LLM 负责“做什么”（动作序列），而让世界模型负责“做多久”（动作执行时长），从而实现安全、精确的自主导航。

### 2. 方法论
该框架由三个核心组件组成：
*   **物理接地神经世界模型（Surrogate World Model）**：
    *   采用混合架构：结合了经典的 **Fossen 动力学解析模型**（处理质量、阻力、浮力等已知物理量）和一个**残差多层感知机（MLP）**（学习解析模型无法捕捉的复杂非线性扰动）。
    *   该模型是可微分的，允许通过梯度下降优化动作时长。
*   **三阶段梯度轨迹优化器**：
    *   **阶段 1（均匀缩放）**：在多个时间尺度上评估 LLM 生成的动作序列，选择最佳初始值。
    *   **阶段 2（自适应坐标下降）**：逐个微调每个动作的持续时间。
    *   **阶段 3（Adam 梯度优化）**：利用世界模型的可微性，通过 Adam 优化器对所有时长进行联合微调，以最小化包含目标距离和碰撞惩罚的代价函数。
*   **带信任区域保护的 MPC 式重规划器**：
    *   在执行过程中引入闭环反馈，根据实时状态（DVL/GPS）重新优化剩余动作。
    *   **信任区域保护（Trust-region Guard）**：如果重规划导致执行时间剧烈收缩（暗示模型预测失效），则拒绝更新，增强了系统在分布外（OOD）状态下的鲁棒性。
*   **VLM 辅助语义映射（针对 ASV）**：利用视觉语言模型（Gemma）处理卫星图像和海图，自动提取障碍物并结合天气 API 数据生成语义地图，替代昂贵的车载传感器。

### 3. 实验设计
*   **实验平台**：
    *   **AUV（自主水下航行器）**：6 自由度，8 推进器（BlueROV2 Heavy）。
    *   **ASV（自主水面航行器）**：3 自由度，差分驱动（Blueboat）。
*   **实验环境**：
    *   轻量级 GPU 加速解析模拟器（用于训练和初步测试）。
    *   **GazeboSim**：高保真物理仿真，包含洋流、波浪和推进器动态。
*   **基准任务**：每个平台 5 个任务（直线、斜线、避障、绕桩、复杂障碍区）。
*   **对比方法**：
    1.  **Gradient+MPC（本文方法）**。
    2.  **Gradient（本文方法的开环版本）**。
    3.  **WM+Uniform Scaling（消融实验：仅进行全局时间缩放）**。
    4.  **No WM（基准：原始 LLM 规划，无物理接地）**。

### 4. 资源与算力
*   **硬件**：Intel i9-14th gen CPU, **NVIDIA RTX 4080 GPU**, 128 GB RAM。
*   **训练数据**：AUV 使用 50 万次转换数据，ASV 使用 20 万次。
*   **训练时长**：在 Gazebo 环境下的残差微调仅需不到 3 分钟。
*   **模型**：本地部署的 **Gemma-2b**（通过 Ollama 运行）。

### 5. 实验数量与充分性
*   **实验规模**：每个平台进行了 200 次规划试验。在 GazeboSim 中，每个“任务-规划器”组合进行了 10 次重复实验。
*   **充分性**：
    *   涵盖了从简单到复杂的多种海洋任务。
    *   进行了消融实验（验证了梯度优化和信任区域保护的重要性）。
    *   验证了从解析模拟器到高保真 GazeboSim 的迁移能力。
    *   对 VLM 语义映射进行了独立验证（180 个手动标记单元格，准确率 96%）。
*   **评价**：实验设计较为全面，通过对比不同层级的物理接地方法，客观证明了该框架的有效性。

### 6. 主要结论
*   **安全性**：在所有基准任务中，基于世界模型的规划器实现了 **0 碰撞**，而无接地的 LLM 基准在所有任务中均发生碰撞。
*   **精度**：在 GazeboSim 中，该方法将目标距离误差降低了 **70-82% (ASV)** 和约 **93% (AUV)**。
*   **鲁棒性**：信任区域保护在复杂绕桩任务中表现关键，将 AUV 的任务成功率从 10% 提升至 100%。
*   **感知替代**：VLM 驱动的语义映射可以作为手动指定障碍物的有效替代方案，支持低成本航行器的复杂导航。

### 7. 优点
*   **物理与逻辑解耦**：LLM 处理高层语义，世界模型处理底层物理，这种分工符合机器人控制的层级逻辑。
*   **混合建模**：结合解析模型和神经网络，既保证了物理约束（如能量守恒、角度循环），又具备学习复杂扰动的灵活性。
*   **跨平台通用性**：同一套优化算法和重规划逻辑可直接应用于动力学特性完全不同的 AUV 和 ASV。
*   **传感器廉价化**：通过 VLM 利用现有地理空间数据，降低了对昂贵感知硬件的依赖。

### 8. 不足与局限
*   **拓扑修复能力有限**：优化器只能调整动作的“时长”，如果 LLM 生成的动作序列（拓扑结构）本身是错误的（例如漏掉了必要的转向），优化器无法修复。
*   **LLM 角色受限**：LLM 目前仅生成简单的宏动作序列，尚未充分发挥其在处理复杂语义权衡（如时间 vs 风险）方面的潜力。
*   **实机验证缺失**：论文主要基于高保真仿真（GazeboSim），尚未在真实的海洋环境中进行实物测试。

（完）
