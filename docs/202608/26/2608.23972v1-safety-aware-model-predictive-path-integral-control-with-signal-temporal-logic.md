---
title: Safety-aware Model Predictive Path Integral Control with Signal Temporal Logic
title_zh: 基于信号时序逻辑的安全感知模型预测路径积分控制
authors: "Yiqi Zhao, Taekyung Kim, Hideki Okamoto, Bardh Hoxha, Jyotirmoy V. Deshmukh, Lars Lindemann, Georgios Fainekos"
date: 2026-08-25
pdf: "https://arxiv.org/pdf/2608.23972v1"
tags: ["query:profile-1"]
score: 8.0
evidence: 带安全约束的采样后退时域规划
tldr: 针对具有复杂时序约束和高安全性要求的机器人运动规划问题，本文提出了一种结合信号时序逻辑（STL）与模型预测路径积分（MPPI）的规划框架。该方法将STL公式编码为随时间变化的控制屏障函数（CBF），并集成到MPPI控制器中。通过并行化采样，该框架在保证低计算成本的同时，显著提升了复杂环境下的任务成功率和安全性，并在火星车和无人机仿真中验证了其优越性。
selection_source: fresh_fetch
motivation: 现有的机器人运动规划在处理具有复杂时序要求和严格安全约束的任务时，面临计算效率与约束满足之间的平衡挑战。
method: 提出一种将离散时间STL公式转化为时变控制屏障函数（CBF），并将其嵌入到可并行化的MPPI采样控制框架中的新方法。
result: 在火星车避障和无人机规划实验中，该方法在保持高计算效率的同时，展现出比传统MPPI基准更高的安全性和任务达成率。
conclusion: 该研究成功结合了STL的逻辑表达能力与MPPI的实时规划优势，为复杂动态环境下的安全机器人导航提供了高效的解决方案。
---

## 摘要
安全感知运动规划在机器人领域仍是一项挑战，特别是在任务具有时间紧迫性且处于复杂规范约束下的情况下。在本文中，我们提出了 safety-aware-stl-mppi，这是一种计算高效的基于采样的收缩时界规划框架，旨在提高对信号时序逻辑（STL）所表达约束的满足度。我们的方法将离散时间 STL 公式编码为候选的时变控制屏障函数（CBF），并将其集成到模型预测路径积分（MPPI）控制器中。该方法继承了高效可并行化采样规划器低计算成本的优点，并利用 CBF 处理以 STL 表达的约束。我们在四种具有多样化环境和代价设置的人造火星车规划案例研究中，将该方法与几种 MPPI 基准进行了对比，结果表明我们的方法始终能实现较高的安全性和效率。最后，我们展示了一个使用 NVIDIA Isaac Lab 进行的四旋翼飞行器规划实验。

## Abstract
Safety-aware motion planning remains a challenge in robotics, especially when missions are time-critical and are under complex specifications. In this paper, we propose safety-aware-stl-mppi, a computationally efficient sampling-based receding-horizon planning framework designed to promote satisfaction of constraints expressed in Signal Temporal Logic (STL). Our approach encodes discrete-time STL formulas into candidate time-varying control barrier functions (CBF), which are integrated into a model predictive path integral (MPPI) controller. Our method inherits the benefits of low computational cost from an efficiently parallelizable sampling based planner and utilizes CBF for constraints expressed in STL. We compare against several MPPI baselines using four artificial Mars Rover planning case studies with a diverse environment and cost setups, where we show our method consistently achieving high safety and efficiency. We show a quadcopter planning experiment with NVIDIA Isaac Lab.

---

## 论文详细总结（自动生成）

以下是对论文《Safety-aware Model Predictive Path Integral Control with Signal Temporal Logic》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：在机器人领域，如何在满足复杂时空规范（如“在10秒内到达A点，且全程避开B区”）的同时进行高效的实时运动规划是一个难题。
*   **核心挑战**：
    *   **信号时序逻辑（STL）** 虽然能表达复杂的任务规范，但传统的求解方法（如混合整数规划）计算成本极高，难以在线运行。
    *   **模型预测路径积分（MPPI）** 是一种基于采样的规划器，具有极高的并行计算效率，但它通常难以处理“硬”安全约束，容易在复杂逻辑约束下失效。
*   **整体含义**：本文旨在结合 STL 的形式化表达能力与 MPPI 的计算效率，提出一种能够保证安全且满足复杂时序任务的实时规划框架。

### 2. 方法论
核心思想是将 STL 公式转化为**时变控制屏障函数（CBF）**，并将其嵌入 MPPI 的采样与优化过程中。
*   **STL 到 CBF 的编码**：利用递归规则将 STL 公式（如 $G$ 始终, $F$ 最终, $U$ 直到）转化为离散时间下的时变函数 $h(x, t)$。当 $h(x, t) \ge 0$ 时，系统状态满足 STL 规范。
*   **安全感知 MPPI 流程**：
    1.  **并行采样**：在当前状态下采样数千条控制序列。
    2.  **代价增强**：在样本代价函数中加入 CBF 违规惩罚项（$C_{CBF}$），引导样本向安全区域靠拢。
    3.  **加权聚合**：根据样本表现计算加权平均控制序列。
    4.  **轻量级投影（关键步骤）**：由于加权平均后的控制序列仍可能违反约束，算法通过求解一个二次规划（QP）问题，将控制量投影到由 CBF 定义的安全半空间内。对于无约束执行器，该投影有闭式解，计算极快。
*   **鲁棒化处理**：针对泰勒展开带来的线性化误差，论文还提出了可选的鲁棒化方案，通过补偿 Lipschitz 常数项来增强安全性。

### 3. 实验设计
*   **实验场景**：
    *   **火星车路径规划**：设置了 A、B、C、D 四个案例，涵盖非线性动力学、单/双积分器模型、复杂障碍物环境以及具有挑战性的时序目标。
    *   **无人机仿真**：在 NVIDIA Isaac Lab 物理仿真环境中，模拟四旋翼飞行器在工厂环境（含叉车、工作台等动态/静态障碍物）中的巡检任务。
*   **基准方法（Benchmarks）**：
    *   **Vanilla MPPI**：标准 MPPI，仅使用简单的障碍物惩罚。
    *   **Reach-avoid MPPI**：专门处理到达-规避任务的 MPPI 变体。
    *   **Robustness-based MPPI**：将 STL 鲁棒性语义直接作为代价函数的优化方法。
    *   **Penalty-based MPPI**：基于惩罚项的 STL 规划方法。

### 4. 资源与算力
*   **硬件环境**：Dell G16 7630 笔记本电脑，搭载第13代 Intel Core i9 处理器。
*   **软件框架**：使用 PyTorch 实现，利用其张量运算能力实现采样过程的并行化。
*   **算力说明**：论文未提及使用了多 GPU 集群，主要依靠单机 CPU/GPU 协同。由于 MPPI 的特性，该算法在普通消费级硬件上即可实现实时（如 200Hz）规划。

### 5. 实验数量与充分性
*   **实验规模**：火星车每个案例进行 20 次随机试验，无人机仿真进行 50 次试验。
*   **充分性**：
    *   **多样性**：涵盖了从简单线性系统到复杂非线性系统的多种动力学模型。
    *   **对比维度**：对比了成功率（SR）、计算耗时（Timing）和执行器限制满足率（ALSR）。
    *   **客观性**：在相同参数（如采样数、权重方案）下对比基准方法，实验设计较为公平。

### 6. 主要结论与发现
*   **安全性极高**：在所有火星车案例中，该方法均实现了 **100% 的成功率**，而基准方法在复杂约束（案例 C 和 D）下成功率大幅下降（甚至降至 0%）。
*   **计算效率优越**：相比于基于鲁棒性语义的方法（耗时动辄数百秒），本文方法在保持高安全性的同时，计算耗时仅为毫秒级或秒级，快了 10-100 倍。
*   **鲁棒性**：即使目标点与安全约束冲突（如案例 C），该算法也能优先保证安全约束的满足。

### 7. 优点
*   **理论与工程结合**：将严谨的 STL-CBF 理论引入到高效率的采样规划器中，解决了 MPPI 难以处理硬约束的痛点。
*   **实时性强**：通过闭式解投影和并行采样，能够满足高频闭环控制的需求。
*   **通用性**：支持非凸代价函数和复杂的非线性动力学模型。

### 8. 不足与局限
*   **近似误差**：投影过程依赖于 CBF 的一阶泰勒展开，虽然有鲁棒化方案，但在极度非线性或高速动态下仍可能存在微小违规风险。
*   **CBF 构造依赖**：算法假设存在有效的候选 CBF，且要求 CBF 是一阶的（对控制量显含），对于高阶约束需要额外的降阶处理。
*   **单智能体限制**：目前实验主要集中在单机器人，尚未深入探讨多智能体协同下的扩展性及通信延迟影响。

（完）
