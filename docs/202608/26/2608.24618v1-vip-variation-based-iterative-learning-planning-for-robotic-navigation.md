---
title: "VIP: Variation-based Iterative-learning Planning for Robotic Navigation"
title_zh: VIP：用于机器人导航的基于变分的迭代学习规划
authors: "Shuli Lv, Pengda Mao, Chen Min, Li Hong, Runxiao Liu, Shuai Wang, Quan Quan"
date: 2026-08-25
pdf: "https://arxiv.org/pdf/2608.24618v1"
tags: ["query:q3"]
score: 8.0
evidence: 避免有限维轨迹参数化的高效运动规划
tldr: 针对复杂环境下机器人导航计算开销大的问题，本文提出VIP框架。该框架通过在无限维函数空间中直接更新连续规划指令，避免了传统方法中高维离散化和长预测时域带来的计算负担。VIP支持单机及集群的离线与在线迭代优化，具有线性计算复杂度，在仿真和实测中展现了高效性与可扩展性。
selection_source: fresh_fetch
motivation: 传统规划方法依赖高维轨迹参数化或长预测时域，导致在复杂环境和多机器人场景下计算成本激增。
method: 提出一种基于变分的迭代学习规划框架，将规划指令视为无限维空间中的连续函数进行直接更新。
result: 实验证明VIP具有O(n)的线性计算复杂度，能有效生成并迭代改进单机及集群机器人的运动规划。
conclusion: VIP作为一种通用的规划方法，在处理不同目标和平台时表现出极高的计算效率、有效性和扩展性。
---

## 摘要
在过去的十年中，自主机器人系统已越来越多地应用于测绘、搜索与救援以及最后一公里配送等领域。这些应用要求机器人在大型、复杂且障碍物密集的场景中生成安全高效的运动规划，且通常受限于有限的车载计算资源。然而，传统的规划方法通常依赖于有限维轨迹参数化或不断增加的预测时域，导致计算成本迅速增长，特别是在多机器人场景中。本文提出了一种新颖的基于变分的迭代学习规划（VIP）框架，用于单机器人和机器人集群的高效运动规划。VIP 并非优化大量的离散轨迹变量，而是直接在无限维函数空间中将规划指令更新为连续函数。同样的基于变分的更新可以以模型在环（model-in-the-loop）的方式用于离线规划，或者以机器人在环（robot-in-the-loop）的方式在在线物理执行之间实施。通过避免与时域扩展和高维轨迹离散化相关的计算负担，VIP 保持了 O(n) 的单次迭代计算复杂度，其中 n 表示空间离散点的数量。广泛的仿真和真实世界实验表明，所提出的框架能够针对不同的规划目标、机器人平台和集群配置，高效地生成并迭代改进运动规划，突显了其作为一种通用规划方法的有效性、计算效率和可扩展性。

## Abstract
Over the past decade, autonomous robotic systems have been increasingly deployed in applications such as surveying, search and rescue, and last-mile delivery. These applications require robots to generate safe and efficient motion plans in large, complex, and obstacle-dense environments, often under limited onboard computing resources. However, conventional planning methods commonly rely on finite-dimensional trajectory parameterization or increasingly long prediction horizons, leading to rapidly growing computational costs, particularly in multi-robot scenarios. This paper presents a novel variation-based iterative-learning planning (VIP) framework for efficient motion planning of both single robots and robotic swarms. Instead of optimizing a large number of discrete trajectory variables, VIP directly updates the planning command as a continuous function in an infinite-dimensional function space. The same variation-based update can be implemented in a model-in-the-loop manner for offline planning or in a robot-in-the-loop manner between online physical executions. By avoiding the computational burden associated with horizon expansion and high-dimensional trajectory discretization, VIP maintains a per-iteration computational complexity of $\mathcal{O}(n)$, where $n$ denotes the number of spatial discretization points. Extensive simulations and real-world experiments demonstrate that the proposed framework can efficiently generate and iteratively improve motion plans for different planning objectives, robotic platforms, and swarm configurations, highlighting its effectiveness, computational efficiency, and scalability as a general planning methodology.

---

## 论文详细总结（自动生成）

### 1. 核心问题与整体含义
在复杂且障碍物密集的未知环境中，自主机器人（单机或集群）的运动规划通常受限于车载计算资源。传统的规划方法（如模型预测控制 MPC 或参数化轨迹优化）随着预测时域的延长、轨迹分辨率的提高或机器人数量的增加，计算开销呈指数或超线性增长。

本文的核心研究动机在于**打破传统规划方法中“计算复杂度与轨迹维度/时域高度耦合”的瓶颈**。为此，论文提出了一种基于变分的迭代学习规划（VIP）框架，将规划问题从传统的欧氏空间离散点优化，提升到无限维函数空间中直接对连续控制指令进行更新，从而在保证安全性的同时，将单次迭代的计算复杂度控制在空间离散点数的线性水平 $\mathcal{O}(n)$。

---

### 2. 方法论
VIP 框架的核心思想是利用变分法在函数空间中直接优化遍历速度指令，并通过多次运行（Rollout）的反馈数据进行迭代改进。其关键技术细节包括：

*   **时空转换**：将时间域坐标转换为空间路径坐标（弧长 $l$），使得不同执行时间的轨迹可以在相同的空间位置进行对齐和记录。
*   **统一能量系统建模**：
    *   对于单机器人，将偏离几何路径的距离定义为跟踪能量 $V_s$。
    *   对于机器人集群，利用核密度估计（KDE）重建宏观分布密度，将当前密度与期望密度之间的偏差定义为分布能量 $V_m$。
    *   将两者的误差动力学统一简化为一个标量能量耗散系统。
*   **控制指令分解与饱和**：控制指令被分解为用于消除能量偏差的法向调节分量和用于向前推进的切向遍历分量，并通过径向饱和函数限制最大执行速度。
*   **变分更新律**：
    *   **有模型模式**：通过构建哈密顿函数和伴随方程，推导泛函梯度方向，指导速度指令的更新。
    *   **无模型模式（实际应用）**：无需精确的动力学模型或伴随变量计算。直接利用上一次运行记录的空间能量分布，通过一个非线性映射函数 $g(\cdot)$ 来调整速度。当局部能量低于安全阈值时加速，高于阈值时减速，从而在安全与效率之间取得平衡。

---

### 3. 实验设计
论文通过仿真和多种物理平台对 VIP 框架进行了验证：

*   **实验场景**：
    *   **单机仿真**：在包含 60、80、100 个随机障碍物的大型未知环境（230m × 200m × 30m）中进行自主导航。
    *   **集群仿真**：在 2D 虚拟管道（30 个机器人）和 3D 复杂障碍物环境（20 个机器人）中进行协同通过。
    *   **实物实验**：
        1. 搭载 LiDAR 的四轴无人机在未知户外环境（约 150m 航程）中进行实时建图与快速避障飞行。
        2. 具有复杂空气动力学特性的升力翼四轴无人机进行大范围轨迹跟踪。
        3. 三架四轴无人机组成的集群在户外虚拟安全管道中进行协同飞行。
*   **对比基准（Benchmark）**：
    *   **MPCC**（模型预测轮廓控制）
    *   **Polynomial**（基于时间分配的多项式最小加加速度 Minimum Snap 轨迹优化）

---

### 4. 资源与算力
*   **计算平台**：
    *   仿真在配备 Intel Core i7-12700F 或 i9-10900 CPU、16 GB RAM 的工作站上运行，算法使用 C++ 和 MATLAB 实现。
    *   实物实验中，无人机车载计算平台为 **NVIDIA Jetson Orin**，飞控采用 Pixhawk/PX4。
*   **算力说明**：由于 VIP 属于基于控制理论的迭代学习控制（ILC）范畴，其无模型更新律仅涉及简单的代数运算，**不需要传统深度学习的 GPU 训练过程**，因此文中未提及 GPU 训练时长或显存占用等指标。

---

### 5. 实验数量与充分性
*   **实验规模**：仿真部分针对不同障碍物密度（60/80/100 个障碍物）进行了多轮对比；实物部分进行了 3 次重复的户外未知环境飞行，升力翼无人机进行了“6次仿真 + 2次实物”的跨域迭代，集群进行了 6 次实物迭代。
*   **充分性评估**：实验设计较为充分且公平。对比实验均在相同的初始路径（由 Tube-RRT* 生成）和相同的物理限制下进行。实验不仅给出了最终的轨迹对比，还详细记录了重规划耗时、遍历时间随迭代次数的收敛曲线，客观地展示了算法在计算效率和路径效率上的双重优势。

---

### 6. 主要结论与发现
*   **极高的计算效率**：在单机导航中，VIP 的平均重规划时间仅为 2.37ms - 2.62ms，相比 MPCC 减少了 **47.4% - 55.5%**，相比多项式方法减少了 **86.6% - 87.7%**。
*   **优异的路径效率**：在通过时间上，VIP 相比多项式方法缩短了高达 6.8%，与计算开销巨大的 MPCC 相当（偏差在 1.5% 以内）。
*   **跨域适应性**：无模型 VIP 能够通过实际物理运行数据，有效补偿升力翼无人机复杂的未建模气动干扰，在 2 次实物迭代后即可使通过时间进一步缩短。

---

### 7. 优点
*   **泛函空间优化的引入**：避开了传统方法中对高维离散轨迹点进行非线性约束求解的难题，将复杂度降为 $\mathcal{O}(n)$。
*   **无模型自适应能力**：通过“机器人在环”的实际执行历史来更新指令，天然具备对传感器噪声、定位漂移和未建模动力学的鲁棒性。
*   **宏观与微观的解耦**：在集群控制中，VIP 仅在宏观层面优化集群的共同推进速度，而将避障等微观约束留给低级分布式控制器，极大地提高了集群规划的扩展性。

---

### 8. 不足与局限
*   **对重复性任务的依赖**：作为迭代学习方法，VIP 依赖于在相同空间路径（或相同虚拟管道）上的多次尝试。对于完全随机、无法重复的即时动态避障场景，其迭代改进的效果会打折扣。
*   **对初始可行路径的依赖**：VIP 属于轨迹精细化（Refinement）层面的算法，其性能上限和安全性在很大程度上取决于前级全局规划器（如 Tube-RRT*）生成的初始可行路径的拓扑结构。
*   **参数敏感性**：无模型更新律中的能量容忍区间（$\zeta_{min}, \zeta_{max}$）和学习步长需要根据具体平台手动调参，缺乏自适应确定机制。

（完）
