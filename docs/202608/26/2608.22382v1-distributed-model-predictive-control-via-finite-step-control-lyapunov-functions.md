---
title: Distributed model predictive control via finite-step control Lyapunov functions
title_zh: 基于有限步控制 Lyapunov 函数的分布式模型预测控制
authors: "Navid Noroozi, Maryam Sharifi"
date: 2026-08-23
pdf: "https://arxiv.org/pdf/2608.22382v1"
tags: ["query:q2"]
score: 8.0
evidence: 约束系统的分布式模型预测控制
tldr: 本文针对受限互联非线性离散时间系统，提出了一种基于有限步控制李雅普诺夫函数（CLF）的分布式模型预测控制（DMPC）框架。利用有限步逆李雅普诺夫结果的构造性特点，该方法通过局部优化、邻居预测和有限步小增益条件，解决了分布式控制中预测不匹配导致的稳定性难题。研究开发了移位兼容的约束裕量和终端可行性测试，确保了系统的递归可行性与约束满足。实验结果在直流微网功率变换器上验证了该框架在电流共享和电压安全方面的有效性。
selection_source: fresh_fetch
motivation: 传统的逆李雅普诺夫定理在分布式控制中往往过于保守，且难以直接构造适用于互联非线性系统的稳定性判据。
method: 提出一种基于有限步控制李雅普诺夫函数的DMPC框架，结合邻居预测、约束紧缩和有限步小增益终端不等式进行局部优化。
result: 理论证明了系统的递归可行性和约束满足性，并在直流微网实验平台上成功实现了电流共享与电压安全控制。
conclusion: 该框架为受限互联非线性系统提供了一种非保守且具有构造性的分布式控制方案，有效平衡了计算复杂性与控制性能。
---

## 摘要
与经典的逆 Lyapunov 定理不同，有限步逆结果是构造性的，并提供了一个不同的起点：在显式意义上，对于足够大的有限前向步数（设为 M），任何缩放范数都可以作为逆有限步 Lyapunov 函数。对于互联离散时间系统，类似的论证思路可以推导出“非保守”的小增益条件。受此观点启发，本文为受约束的互联非线性离散时间系统开发了一种分布式模型预测控制框架。每个子系统在每个系统时间步瞬时通过将局部阶段函数设置为局部控制有限步类 Lyapunov 函数的形式，利用时间对齐的邻居预测、优化的状态约束收紧半径以及有限步小增益终端不等式，来求解一个局部优化问题。由于接收到的邻居预测不一定等于未来滚动优化生成的轨迹，名义逆证明本身并不能确保递归可行性或稳定性。因此，我们开发了移位兼容的约束裕度、针对控制仿射网络的局部一步终端可行性测试，以及预测与重新优化失配的解析界限。所得分析给出了递归可行性、约束满足和实用的 M 步 Lyapunov 估计，且当预测与重新优化失配界限趋于零时具有渐近收敛性。对于受约束的线性网络，这些条件简化为有限维矩阵、QP 和 SOCP 测试。该框架专门应用于双 DGU 直流微网中 DC/DC 功率变换器运行约束下的电流共享和终端母线电压安全，并在小型实验室规模原型上进行了评估。

## Abstract
As opposed to classical converse Lyapunov theorems, finite-step converse results are constructive and offer a different starting point: for sufficiently large finite step ahead, say $M$, in an explicit sense, any scaled norm can serve as a converse finite-step Lyapunov function. As for interconnected discrete-time systems, similar lines of argument lead to ``non-conservative'' small-gain conditions. Motivated by this viewpoint, this paper develops a distributed model predictive control framework for constrained interconnected nonlinear discrete-time systems. Each subsystem solves one local optimization problem at each system time step instant by setting the local stage function in form of a local control finite-step like Lyapunov function, using time-aligned neighbor predictions, optimized state-constraint tightening radii, and a finite-step small-gain terminal inequality. Since received neighbor predictions need not equal the trajectories generated by future receding-horizon optimizations, the nominal converse certificates do not alone ensure recursive feasibility or stability. We therefore develop shift-compatible constraint margins, a local one-step terminal feasibility test for networks that are affine in control, and an analytical bound for the prediction and reoptimization mismatch. The resulting analysis gives recursive feasibility, constraint satisfaction, and a practical $M$-step Lyapunov estimate, with asymptotic convergence when the prediction and reoptimization mismatch bound tends to zero. For constrained linear networks, the conditions reduce to finite-dimensional matrix, QP, and SOCP tests. The framework is specialized to current sharing and terminal bus voltage safety under DC/DC power converters' operational constraints in a two-DGU DC microgrid evaluated on a small laboratory-scale prototype.

---

## 论文详细总结（自动生成）

### 论文总结：基于有限步控制 Lyapunov 函数的分布式模型预测控制

#### 1. 核心问题与整体含义（研究动机和背景）
本文探讨了受限互联非线性离散时间系统的分布式模型预测控制（DMPC）问题。
*   **研究动机**：传统的逆 Lyapunov 定理多为“存在性”证明，难以直接用于控制器构造。而“有限步（finite-step）”理论具有构造性：只要步数 $M$ 足够大，简单的缩放范数即可作为 Lyapunov 函数。
*   **核心挑战**：在分布式架构中，子系统仅能获取邻居的“预测轨迹”而非真实未来轨迹。这种“预测失配”会导致系统失去递归可行性（Recursive Feasibility）或稳定性。
*   **整体含义**：本文旨在利用有限步小增益（Small-gain）条件的非保守性，构建一个无需预设终端不变集、且能显式处理预测失配的 DMPC 框架。

#### 2. 方法论：核心思想与关键技术
论文提出了一种非迭代、非协作的并行 DMPC 架构：
*   **局部优化问题 (OCP)**：每个子系统求解一个局部优化，其阶段代价函数采用局部有限步类 Lyapunov 函数（通常为范数形式）。
*   **状态约束紧缩**：引入可优化的紧缩半径 $r_i(l|t)$。通过 Pontryagin 集合差运算，将原始状态约束收紧，以抵消邻居预测误差和重新优化带来的扰动。
*   **有限步小增益终端条件**：在预测时界的末端（第 $M$ 步）施加一个小增益不等式约束。该约束利用邻居传来的标量信息，确保全网在有限步后能量衰减。
*   **递归可行性保障**：
    *   **移位兼容裕量**：定义了考虑预测漂移的约束裕量 $\delta_i$。
    *   **一步终端可行性测试**：针对控制仿射系统，将终端可行性转化为二阶锥规划（SOCP）或二次规划（QP）的在线测试，确保即使在重新优化时也能找到可行解。
*   **线性系统特化**：对于线性网络，所有条件简化为线性矩阵不等式（LMI）或简单的标量区间测试。

#### 3. 实验设计
*   **实验场景**：双分布式电源单元（DGU）组成的直流微网（DC Microgrid）。
*   **控制目标**：源电流共享（Current Sharing）、母线电压安全（11V-13V 限制）以及变换器输出电压约束。
*   **Benchmark 与对比**：
    *   **仿真 vs. 硬件**：对比了基于精确模型的数值仿真与实验室规模的硬件原型表现。
    *   **扰动测试**：在 DGU 1 处接入 235 $\Omega$ 的电阻负载脉冲，观察系统在负载突变下的电流重新分配能力和约束满足情况。
*   **硬件架构**：采用 Raspberry Pi 4 作为局部控制器，ESP32 负责底层 I/O，通过 ZeroMQ 协议进行分布式通信。

#### 4. 资源与算力
*   **计算平台**：
    *   **控制器**：Raspberry Pi 4 (RPi4)。
    *   **底层执行**：ESP32 微控制器。
    *   **协调/日志**：Ubuntu 虚拟机。
*   **软件环境**：Python 原生开发，使用 `CVXPY` 求解优化问题。
*   **算力开销**：硬件实验中，95% 的优化求解时间约为 20ms，完整控制循环约 25ms。在 100ms 的采样周期内，算力冗余充足，证明了算法的实时可行性。

#### 5. 实验数量与充分性
*   **实验规模**：进行了 1200 个样本点的连续运行实验（约 120 秒数据）。
*   **充分性评价**：
    *   **多维度验证**：涵盖了理论证明、数值仿真和硬件在环实验，形成了闭环验证。
    *   **客观性**：实验不仅展示了成功的电流共享，也客观记录了硬件实验中由于传感器噪声和模型参数失配导致的稳态偏差（约 3.4mA），证明了实验结果的真实性。
    *   **公平性**：仿真与硬件采用相同的控制参数和物理参数，对比具有参考价值。

#### 6. 主要结论与发现
*   **递归可行性**：理论分析与实验均证实，通过动态紧缩半径和终端可行性测试，DMPC 在运行中不会出现无解情况。
*   **稳定性**：系统在有限步内满足小增益条件，闭环轨迹最终收敛至目标集合（电流共享状态）。
*   **实用性**：该框架成功处理了直流微网中的硬约束（电压安全），且在分布式通信延迟和预测失配的情况下表现稳健。
*   **发现**：分布式电流共享的精度（误差约 0.57mA）远高于绝对电流参考跟踪的精度，体现了分布式协同的优势。

#### 7. 优点
*   **构造性强**：不同于需要复杂离线计算终端集的传统 MPC，该方法利用有限步理论，使得 Lyapunov 函数的选择非常直观（如简单的二次型）。
*   **非保守性**：有限步小增益条件允许子系统在短期内能量上升，只要 $M$ 步后整体下降即可，增加了控制器的自由度。
*   **无需迭代**：采用并行非迭代架构，降低了通信频率和计算延迟。
*   **硬件友好**：在低成本嵌入式设备（RPi4）上即可实现复杂非线性约束控制。

#### 8. 不足与局限
*   **Lipschitz 依赖**：算法性能高度依赖于系统 Lipschitz 常数的估计，若估计过大（保守）会导致约束紧缩过度，缩小可行域；若估计过小则可能失去稳定性保障。
*   **通信需求**：虽然仅需交换标量，但仍要求邻居间具有时间同步的预测轨迹交换，对通信网络的可靠性有一定要求。
*   **线性化偏差**：在处理高度非线性系统时，若线性化误差较大，预测失配界限 $\rho(t)$ 可能难以收敛至零，导致系统仅能达到“实际稳定性（Practical Stability）”而非渐近稳定。

（完）
