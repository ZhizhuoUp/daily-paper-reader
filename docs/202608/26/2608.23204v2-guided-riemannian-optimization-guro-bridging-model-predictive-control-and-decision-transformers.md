---
title: "Guided Riemannian Optimization (GuRO): Bridging Model Predictive Control and Decision Transformers"
title_zh: 引导式黎曼优化 (GuRO)：桥接模型预测控制与决策 Transformer
authors: "Hossein Abdi, Satya Prakash Dash, Mingfei Sun"
date: 2026-08-24
pdf: "https://arxiv.org/pdf/2608.23204v2"
tags: ["query:q2"]
score: 8.0
evidence: 桥接模型预测控制
tldr: 本研究提出GuRO框架，旨在解决高维非线性机器人控制中MPC模型依赖与RL优化不稳定的难题。该方法将MPC与决策Transformer结合，利用MPC生成的局部最优轨迹引导模型训练，无需大规模离线预训练。同时，引入黎曼流形优化技术应对非凸损失景观，提升了训练的收敛速度与鲁棒性。实验证明，GuRO在四足机器人控制任务中表现优异，显著超越了SAC和在线决策Transformer等主流算法。
selection_source: fresh_fetch
motivation: 针对高维非线性系统中MPC对模型精度要求高以及强化学习采样效率低、优化不稳定的挑战，寻求一种结合两者优势的决策框架。
method: 提出一种集成MPC与决策Transformer的序列决策框架，并利用曲率感知的黎曼优化方法来高效处理非凸损失景观。
result: 在四足机器人控制任务的评估中，该框架在回报值和收敛速度上均一致优于TRPO、SAC和在线决策Transformer等强基准方法。
conclusion: 通过MPC引导与黎曼优化的结合，GuRO成功弥合了模型预测控制与序列建模之间的鸿沟，为复杂机器人任务提供了更稳健的优化方案。
---

## 摘要
高维非线性系统中的决策仍然是机器人领域的一个核心挑战。虽然像模型预测控制 (MPC) 这样的基于模型的方法具有样本效率和可解释性，但当动力学模型不准确或需要长时界预测时，其性能会下降。相反，无模型强化学习 (RL) 直接从交互中学习策略，但面临样本复杂度高和优化不稳定的问题。序列建模的最新进展启发了基于 Transformer 的决策框架，这些框架可以统一 MPC 和 RL，但由于高度非凸的损失景观，它们的训练通常面临重大的优化挑战。在这项工作中，我们提出了一个新颖的框架，该框架在序列决策框架中集成了 MPC 与 RL，并利用曲率感知优化来高效处理非凸损失景观。MPC 提供局部最优轨迹的预测来引导决策 Transformer，从而消除了对大规模离线预训练的需求。为了解决传统优化器收敛缓慢且不稳定的问题，我们在黎曼参数空间中使用高效的黎曼（曲率感知）方法训练策略，从而实现更快、更稳健的优化。我们在高维四足机器人控制任务上评估了我们的框架，并证明了其相对于 TRPO、SAC 和在线决策 Transformer 等强基准模型的持续改进，实现了更高的回报和更快的收敛。

## Abstract
Decision-making in high-dimensional, nonlinear systems remains a central challenge in robotics. While model-based methods like Model Predictive Control (MPC) offer sample efficiency and interpretability, their performance degrades when the dynamics model is inaccurate or long-horizon predictions are required. Conversely, model-free reinforcement learning (RL) learns policies directly from interaction but suffers from high sample complexity and unstable optimization. Recent advances in sequence modeling have inspired transformer-based decision-making frameworks that can unify MPC and RL, but their training typically faces significant optimization challenges due to highly non-convex loss landscapes. In this work, we propose a novel framework that integrates MPC with RL in a sequence decision-making framework and leverages a curvature-aware optimization to efficiently tackle non-convex loss landscapes. MPC provides predictions of locally optimal trajectories that guide the decision transformer, removing the need for extensive offline pretraining. To address the slow and unstable convergence of traditional optimizers, we train the policy in a Riemannian parameter space using an efficient Riemannian (curvature-aware) method, leading to faster and more robust optimization. We evaluate our framework on high-dimensional quadruped control tasks and demonstrate consistent improvements over strong baselines, including TRPO, SAC, and Online Decision Transformer, achieving higher returns and faster convergence.

---

## 论文详细总结（自动生成）

这篇论文提出了 **GuRO (Guided Riemannian Optimization)** 框架，旨在通过结合模型预测控制（MPC）与决策 Transformer（Decision Transformer, DT），并引入黎曼优化技术，解决高维非线性机器人控制中的效率与稳定性问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究背景
*   **研究动机**：高维非线性系统（如四足机器人）的决策面临两难境地：
    *   **MPC（基于模型）**：样本效率高、可解释性强，但高度依赖动力学模型的准确性，且在长时界（Long-horizon）预测和终端成本估计不准时性能大幅下降。
    *   **RL（无模型）**：能处理复杂动力学，但采样复杂度极高且优化过程不稳定。
    *   **Transformer 决策模型**：虽然能统一 MPC 和 RL，但通常依赖大规模离线预训练，且其高度非凸的损失景观（Loss Landscape）导致标准优化器（如 Adam）收敛缓慢且易陷入局部最优。
*   **核心目标**：构建一个无需离线预训练、能在线高效学习、且优化过程对曲率敏感的机器人决策框架。

### 2. 方法论
*   **核心思想**：利用 MPC 作为“引导者”生成局部近优轨迹，驱动决策 Transformer 的在线学习，并利用黎曼流形上的曲率信息加速参数更新。
*   **关键技术细节**：
    1.  **MPC 引导（Guiding）**：在每个时间步运行有限时界的 MPC，生成局部最优的动作序列和状态轨迹。这些轨迹存入重放池，作为 DT 训练的“专家数据”，解决了在线 DT 训练中的分布偏移问题。
    2.  **序列建模**：采用 GPT 风格的 Transformer 架构，将状态、动作、余数回报（Return-to-go）作为序列输入，通过最大似然估计（MLE）优化策略。
    3.  **约束优化**：在损失函数中引入**最大熵原理**（鼓励探索）和 **KL 散度信任区域约束**（保证策略更新的平滑性）。
    4.  **黎曼优化（Riemannian Optimization）**：
        *   将参数空间视为黎曼流形，利用**费舍尔信息矩阵（Fisher Information Matrix）**的对角近似作为黎曼度量。
        *   通过计算黎曼梯度（即度量矩阵的伪逆与欧几里得梯度的乘积）来调整更新方向，使其适应损失景观的局部几何结构（曲率感知）。
        *   使用回溯行搜索（Backtracking Line Search）确保满足信任区域约束。

### 3. 实验设计
*   **实验场景**：使用 **Unitree AlienGo** 四足机器人，在三个挑战性任务中进行评估：
    1.  **不平整地面行走**（Uneven Terrains）。
    2.  **爬楼梯**（Stair Climbing）。
    3.  **低摩擦斜坡攀爬**（Low-friction Inclined Surface）。
*   **Benchmark 与对比方法**：
    *   **经典 RL**：SAC（Soft Actor-Critic）、TRPO（Trust Region Policy Optimization）。
    *   **序列模型**：Online-DT（在线决策 Transformer）。
    *   **消融实验**：将 GuRO 中的黎曼优化器替换为 **SGD** 和 **Adam**，以验证曲率感知优化的有效性。

### 4. 资源与算力
*   **硬件**：使用了单张 **NVIDIA GeForce RTX 4090** GPU。
*   **软件框架**：基于 **JAX** 和 **MuJoCo XLA (MJX)** 物理引擎，实现了高度并行的仿真环境。
*   **训练细节**：论文提到利用了 GPU 的并行仿真能力（图中显示了多个机器人同时运行），但未明确给出总训练时长（以总步数/迭代次数展示）。

### 5. 实验数量与充分性
*   **实验规模**：每个任务均进行了 **5 次独立实验**（不同随机种子），并报告了平均回报和标准差。
*   **充分性评价**：
    *   实验覆盖了多种地形，具有较好的代表性。
    *   引入了 **Welch’s t-test** 和 **Cohen’s d 效应量分析**，从统计学角度证明了 GuRO 相对于基准方法的显著性（尤其是在不平整地面任务中，效应量极高）。
    *   消融实验清晰地展示了黎曼优化在收敛速度和最终性能上优于 Adam/SGD。

### 6. 主要结论与发现
*   **性能领先**：GuRO 在所有任务中的平均总回报均高于 SAC、TRPO 和 Online-DT。
*   **收敛极快**：黎曼优化器显著加快了训练初期的收敛速度，且损失函数下降更平滑，证明了曲率感知在处理 Transformer 非凸优化时的优势。
*   **样本效率**：通过 MPC 引导，模型无需离线数据即可在较少的环境交互步数内达到高性能。

### 7. 优点
*   **理论与工程结合**：将深奥的黎曼几何优化成功应用于复杂的 Transformer 机器人策略训练。
*   **无需预训练**：克服了传统 DT 依赖高质量离线数据集的局限性。
*   **鲁棒性**：在低摩擦和不规则地形下表现出比纯 RL 方法更强的稳定性。

### 8. 不足与局限
*   **MPC 依赖性**：虽然减少了对精确动力学模型的依赖，但底层仍需一个名义模型（Nominal Model）来运行 MPC。如果名义模型极度偏差，引导效果可能会受限。
*   **计算开销**：虽然黎曼优化减少了迭代次数，但每一步需要计算度量矩阵的近似和伪逆，单步计算成本高于简单梯度下降（尽管论文通过对角近似降低了开销）。
*   **任务单一性**：实验集中在四足机器人行走任务，尚未在机械臂操纵等其他高维机器人任务中验证通用性。

（完）
