Title: Computing the Maximal Controlled Invariant Set for Neural Network Control Systems

URL Source: https://arxiv.org/pdf/2608.07908v1

Published Time: Tue, 11 Aug 2026 00:24:56 GMT

Number of Pages: 9

Markdown Content:
# Computing the Maximal Controlled Invariant Set for Neural Network Control Systems 

Tianxiao Ye, Hang Zhang, and Xiangru Xu 

Abstract — This article studies the computation of the maxi-mal controlled invariant set (MCIS) for neural network control systems (NNCSs) with a nominal plant model and a neural network residual model. An interval-based method is developed to construct a control-affine inclusion function for the NNCS dynamics and to exploit the induced hyperplane arrangement in the input space, which reduces the verification of controlled invariance from an infinite control search to a finite set of representative evaluations. Based on this finite characterization, verification-guided algorithms iteratively construct inner and outer approximations of the the MCIS, which are naturally parallelizable. Numerical examples demonstrate the effective-ness of the proposed approach. 

I. I NTRODUCTION 

With the extensive adoption of neural networks (NNs), an increasing number of dynamical systems incorporate NN components to universally approximate complex nonlinear behaviors that are difficult or expensive to derive from first principles [1], [2], [3], [4]. This trend leads to the rapid development of neural network control systems (NNCSs). Despite their empirical success, providing formal guarantees for NNCSs remains a significant challenge due to the highly nonlinear and compositional structure of NNs. Controlled invariant sets (CISs) play a fundamental role in formally analyzing control systems because they char-acterize the states from which state and input constraints can be satisfied for all future time under admissible control inputs. Hence, CISs serve as a key tool for designing safety filters and guaranteeing recursive feasibility in model predictive control [5], [6], [7], [8]. For linear systems, efficient methods to compute CISs are proposed based on the Riccati-type recursive matrix equation [9], polyhedral backward reachability iterations [10], and Lagrangian meth-ods [11]. For nonlinear systems, the problem becomes more challenging due to the loss of convexity and a tractable analytical structure. Existing approaches typically rely on convex approximations, interval analysis, or reachable-set over/under-approximations, often at the cost of conservatism or additional assumptions [12], [13], [14]. However, these methods are not directly applicable to NNCSs, and synthe-sizing CISs of NNCSs is still an open problem. A recent work [15] addresses this for rectified linear unit (ReLU)-activated multilayer perceptrons by combining state-space quantization with mixed-integer linear programming (MILP). Nevertheless, the proposed approach is limited to stand-alone NNs representing system dynamics; moreover, its reliance 

> T. Ye, H. Zhang, and X. Xu are with the Department of Mechanical Engineering, University of Wisconsin-Madison, Madison, WI, USA. Email:

{tye46,hang.zhang,xiangru.xu }@wisc.edu .

on MILP hinders scalability to larger networks and restricts applicability to piecewise-linear activation functions. This paper addresses the inner and outer approximations of the maximal controlled invariant set (MCIS) for NNCSs whose dynamics consist of a nominal plant model and an NN residual model. Our main contributions are twofold: (i) We develop an efficient, optimization-free method for the verification of a relaxed controlled invariance condition, based on the function inclusion and hyperplane arrangement techniques. Specifically, we construct a control-affine inclu-sion function of the NNCS dynamics and show that the induced hyperplane arrangement for this inclusion function partitions the admissible control space into finite subsets, within each of which the verification result is invariant. This property converts the infinite existential search over admissi-ble controls to the evaluation of a single representative point per subset. (ii) We develop verification-guided synthesis algorithms to compute the inner and outer approximation of the MCIS represented as the union of intervals. The proposed algorithms are inherently parallelizable, making them well-suited for high-performance computing environments, such as GPU-based implementations. The remainder of this article is organized as follows. Sec-tion II introduces preliminaries and the problem statement; Section III introduces a relaxed CIS condition via function inclusion; Section IV develops a control-affine enclosure of the NNCS dynamics; Section V shows how the relaxed CIS test can be reduced to a finite search over representative con-trols via hyperplane arrangements; Section VI presents the verification-guided algorithms for constructing the inner and outer approximations of the MCIS; Section VII demonstrates the effectiveness of the proposed approach through numerical examples; finally, Section VIII concludes the article. II. P RELIMINARIES & P ROBLEM STATEMENT 

A. Preliminaries 

The set of real numbers and natural numbers are denoted by R and N, respectively. For a given set A ⊆ Rn, int( A),

∂A , A, and 2A denote its interior, boundary, closure, and power set, respectively. For a given set A that is finite, |A|

denotes its cardinality. The symbol B(x, r ) denotes the open 

ℓ∞-ball of radius r centered at x in Rn, i.e., B(x, r ) = 

{z ∈ Rn | ∥ z − x∥∞ < r }, where ∥·∥ ∞ is the infinity norm. 

1) Interval Analysis: We introduce notations and def-initions from interval analysis that will be used in the sequel [16], [17]. The set of all intervals in R is denoted by 

IR . A closed interval in IR is denoted by [x] = [ x, x ] and an open interval in IR is denoted by (x) = ( x, x ), where x and arXiv:2608.07908v1 [math.OC] 8 Aug 2026 x are the lower and upper bounds, respectively, satisfying 

x ≤ x elementwise. Given n intervals [x1], . . . , [xn] ∈ IR ,their Cartesian product is denoted by ×ni=1 [xi] ≜ [x1] ×· · · × [xn]. A box [x] ∈ IR n is defined as such a Cartesian product, i.e., [x] = ×ni=1 [xi]. The width and midpoint of a box are defined as w([ x]) ≜ ∥x − x∥∞, mid([ x]) ≜ 

> 12

(x + x), respectively. For y ∈ Rn, we denote by {y}

the degenerate box [y, y ] ∈ IR n and a box is called non-degenerate if it has nonempty interior. Given a function f : Rn → Rm, an interval function 

[f ] : IR n → IR m is called an inclusion function for f if 

f ([ x]) ⊆ [f ]([ x]) for any [x] ∈ IR n [17, Section 2.4]. A subpaving of a box [x] ∈ IR n is a finite set of boxes 

{[b1], . . . , [bN ]} ⊆ IR n such that [bi] ⊆ [x] for all i and 

int([ bi]) ∩ int([ bj ]) = ∅ for any i̸ = j. Besides viewing a subpaving as the set of boxes {[b1], . . . , [bN ]}, it can also be viewed as the union [b1] ∪ · · · ∪ [bN ], depending on the context. Hence, a subpaving can be interpreted either as a discrete subset of IR n or as a compact subset of Rn [18, Section 1.5]. A paving of a box [x] ∈ IR n is a collection of subpavings K1, . . . , KN such that [x] = SNi=1 

S 

> [b]∈Ki

[b].

2) Controlled Invariant Set: Consider a discrete-time con-trol system x+ = f (x, u ) where the state x ∈ X ⊆ Rn and input u ∈ U ⊆ Rm. A set S ⊆ X is called a controlled invariant set (CIS) if, for every x ∈ S , there exists u ∈ U 

such that f (x, u ) ∈ S [10], [19]. The maximal controlled invariant set (MCIS) is the largest CIS, defined as 

S∗ = [{S ⊆ X : S is a CIS }.

The regulation map associated with a CIS S ⊆ X is the set-valued map RS : X → 2U defined by RS (x) := {u ∈U : f (x, u ) ∈ S} .

The one-step predecessor of a set S ⊆ X is defined as 

Pre( S) := {x ∈ X : ∃u ∈ U , f (x, u ) ∈ S} .

Clearly, a set S ⊆ X is a CIS if and only if 

S ⊆ Pre( S).

3) Hyperplane Arrangements: We now introduce several definitions from the theory of hyperplane arrangements, following [20], [21], that will be used in Section V. An 

affine hyperplane in Rn is defined as {v ∈ Rn | α⊤v = β}

where α ∈ Rn is a nonzero vector and β ∈ R. A (finite) 

hyperplane arrangement in Rn is a finite set of affine hyperplanes in Rn, denoted as A ≜ {H 1, H2, · · · , HN },where Hi = {v ∈ Rn | α⊤ 

> i

v = βi}, i = 1 , 2, . . . , N .The sign pattern of a vector x ∈ Rn with respect to A is defined as s(x) ≜  sgn( α⊤ 

> 1

x − β1), . . . , sgn( α⊤ 

> N

x − βN ) ∈{− 1, 0, +1 }N . The (open) face of A is a maximal connected set of points sharing the same sign pattern. The collection of all faces forms a partition of Rn, i.e., every point in Rn

belongs to exactly one face. Faces may have any dimension from 0 to n. The full-dimensional faces are open convex polyhedra and are called the regions of the arrangement. 

B. Problem Statement 

In this work, we consider the following discrete-time control system: 

xk+1 = f0(xk, u k) + fNN (xk, u k)

| {z }   

> f(xk,u k)

(1) where xk ∈ X ⊆ Rn is the state, uk ∈ U ⊆ Rm is the control input, f0 : Rn ×Rm → Rn represents the nominal dynamics, and fNN : Rn × Rm → Rn is a function parameterized as a feed-forward neural network (FFNN) that captures the residual dynamics. For clarity of presentation, we assume that both U and X are boxes; however, the proposed method extends naturally to the case where X and U are finite unions of boxes. Assume that f0 is twice continuously differentiable on an open set containing X ×U , with bounded second-order partial derivatives. Assume that fNN has ℓ − 1 hidden layers; let W (i) ∈ Rni×ni−1 and b(i) ∈ Rni denote the weight matrix and bias vector of the i-th layer, respectively, where 

ni is the number of neurons in the i-th layer, with n0 = n+m

and nℓ = n; denote by z(i) the output of the neurons in the 

i-th layer. Then, we have 

z(0) =

xk

uk



∈ Rn+m,z(i) = σ W (i)z(i−1) + b(i) ∈ Rni , i = 1 , . . . , ℓ − 1,fNN (xk, u k) = W (ℓ)z(ℓ−1) + b(ℓ) ∈ Rn,

where σ(·) denotes the activation function (such as ReLU and tanh ) applied element-wise. We will address the problem stated as follows. 

Problem 1: Given the NNCS (1), with xk ∈ X ⊆ Rn and 

uk ∈ U ⊆ Rm, where both U and X are boxes, compute two subpavings S∗ and S∗ as the inner and outer approximations of the MCIS, respectively, such that 

S∗ ⊆ S ∗ ⊆ S ∗

with S∗ being a CIS, while making the approximations as tight as possible. To solve this problem, we construct a paving of X of the form X = C ∪ O ∪ U such that C ⊆ S ∗ and O ⊆ X \ S ∗.Here, C is a CIS, O contains states that lie outside the MCIS, and U consists of states that remain undetermined. Therefore, Problem 1 can be addressed by setting S∗ = C and S∗ =

C ∪ U while making U as small as possible. The detailed algorithms for constructing the subpavings C, O, and U are presented in Section VI, following the introduction of a CIS sufficient condition via function inclusion in Section III, the control-affine enclosure of the NNCS dynamics in Section IV and the finite search for a feasible control via hyperplane arrangements in Section V. III. R ELAXED CIS C ONDITION VIA FUNCTION 

INCLUSION 

Recall that a set S is a CIS if and only if S ⊆ Pre( S).For a subpaving S, we relax this condition by using an inclusion function [f ] for f and verifying each box [b] ∈ S

individually: for each [b], find u ∈ U such that 

[f ]([ b], u ) ⊆ S

where [f ]([ b], u ) ⊇ { f (x, u ) : x ∈ [b]} is the inclusion func-tion evaluated on the box [b] with control u. The following proposition establishes the soundness and completeness of this relaxed condition. 

Proposition 1: Let S be a subpaving of X and [f ] an inclusion function for f . Then the following two statements hold. (i) (Soundness.) If there exists u∗ ∈ U such that 

[f ]([ b], u ∗) ⊆ S, then [b] ⊆ Pre( S).(ii) (Completeness.) Suppose that [f ] is convergent, i.e., for any sequence of boxes [x]( k), lim k→∞ w([ x]( k)) = 0 =⇒ lim k→∞ w([ f ]([ x]( k))) = 0 . If x ∈ Pre( S) and there exists u∗ ∈ U with f (x, u ∗) ∈ int( S), then there exists δ > 0 such that, for all [b] containing x with 

w([ b]) < δ , [f ]([ b], u ∗) ⊆ S.

Proof: (i). Since [f ] is an inclusion function, it holds that f ([ b], u ∗) ⊆ [f ]([ b], u ∗), and therefore, f ([ b], u ∗) ⊆ S.In particular, every x ∈ [b] satisfies f (x, u ∗) ∈ S, which means that u∗ witnesses the containment [b] ⊆ Pre( S).(ii). Since f (x, u ∗) ∈ int( S), there exists r > 0 such that B(f (x, u ∗), r ) ⊆ S. Because [f ] is convergent and 

{u∗} = [ u∗, u ∗] is a degenerate box, it holds that w([ b] ×{u∗}) = w([ b]) . Since [f ] is convergent, there exists δ > 0

such that w([ b]) < δ =⇒ w [f ]([ b], u ∗) < r. Now for any x ∈ [b] with w([ b]) < δ, by the inclusion prop-erty, f (x, u ∗) ∈ [f ]([ b], u ∗). For every y ∈ [f ]([ b], u ∗),since both y and f (x, u ∗) lie in the box [f ]([ b], u ∗), we have ∥y − f (x, u ∗)∥∞ ≤ w [f ]([ b], u ∗) < r . Therefore, 

[f ]([ b], u ∗) ⊆ B(f (x, u ∗), r ) ⊆ S.Proposition 1 shows that, under mild conditions on f and 

S, the gap between the relaxed and the original inclusion conditions for CIS can be made arbitrarily small by refining the box widths. However, the relaxed condition still requires searching over an infinite input set U to find a suitable control input, which is computationally prohibitive, especially for the NNCS (1). To address this issue, the next two sections develop methods that reduce this infinite search to a finite search over representative control inputs, by using a control-affine enclosure of NNCS dynamics and hyperplane arrange-ments in the input space. IV. C ONTROL -AFFINE ENCLOSURE OF NNCS D YNAMICS 

In this section, we derive affine functions of u that bound 

f (x, u ) from above and below over arbitrarily given boxes in X × U . Specifically, given any [x] ⊆ X and [u] ⊆ U , we aim to determine ai ∈ Rm, ai ∈ Rm, ci ∈ R, and ci ∈ R,where i = 1 , 2, . . . , n , such that 

f (x, u ) ∈ [f ][x](u), ∀x ∈ [x], ∀u ∈ [u], (2) where 

[f ][x](u) ≜ n

# ×i=1 

a⊤ 

> i

u + ci, a⊤ 

> i

u + ci

. (3) We refer to [f ][x](u) in (3) the control-affine enclosure of f

over [x]. Since f = f0 + fNN , we construct separate control-affine enclosures for f0 and fNN , and then combine them through summation. 

A. Control-affine Enclosure of f0

Suppose that the nominal dynamics f0 is written as f0 =(f0,1, f 0,2, . . . , f 0,n )⊤. We can apply the standard second-order Taylor expansion with an interval-bounded remainder to construct a control-affine enclosure for f0,i and, conse-quently, for f0 [16]. Let xc = mid([ x]) and uc = mid([ u]) denote the respec-tive midpoints of [x] and [u]. For any i ∈ { 1, 2, . . . , n },we can expand f0,i around (xc, u c) as f0,i (x, u ) =

f0,i (xc, u c)+ ∇uf0,i (xc, u c)⊤(u−uc)+ ∇xf0,i (xc, u c)⊤(x−

xc) + ri. Here, ri is the Lagrange remainder term sat-isfying |ri| ≤ ∆i ≜ 12 ρ⊤Hiρ where ρ = 12 (x1 −

x1, . . . , x n − xn, u 1 − u1, . . . , u m − um)⊤ ∈ Rn+m 

> ≥0

is the vector of componentwise half-widths of [x] × [u], and 

Hi ∈ R(n+m)×(n+m) 

> ≥0

is an elementwise upper bound on the absolute value of the Hessian of f0,i over [x] × [u]. This bound is computed by evaluating each entry of Hi over 

[x] × [u] via natural interval extension [16, Section 5.3]. Furthermore, the linear function ∇xf0,i (xc, u c)⊤x can be lower and upper bounded as θi ≤ ∇xf0,i (xc, u c)⊤x ≤

θi, where θi = min x∈[x] ∇xf0,i (xc, u c)⊤x and θi =max x∈[x] ∇xf0,i (xc, u c)⊤x.Setting δi = ∇uf0,i (xc, u c), ηi = f0,i (xc, u c) −∇uf0,i (xc, u c)⊤uc − ∇ xf0,i (xc, u c)⊤xc + θi − ∆i, and ηi =

f0,i (xc, u c)−∇ uf0,i (xc, u c)⊤uc −∇ xf0,i (xc, u c)⊤xc +θi +∆i, it follows that 

δ⊤ 

> i

u + ηi ≤ f0,i (x, u ) ≤ δ⊤ 

> i

u + ηi (4) holds for any x ∈ [x], u ∈ [u].

B. Control-affine Enclosure of fNN 

Suppose that the FFNN residual dynamics fNN is written as fNN = ( fNN ,1, f NN ,2, . . . , f NN ,n )⊤. We derive a control-affine enclosure for each fNN ,i by leveraging the affine bounds from linear relaxation–based perturbation analysis (LiRPA) method [22]. Specifically, by [22, Theorem 3.2], for any i ∈ { 1, 2, . . . , n }, there exist γu,i ∈ Rm, γu,i ∈ Rm,

γx,i ∈ Rn, γx,i ∈ Rn, βi ∈ R, and βi ∈ R, such that 

γ⊤

> u,i

u + γ⊤

> x,i

x + βi ≤ fNN ,i (x, u ) ≤ γ⊤

> u,i

u + γ⊤

> x,i

x + βi (5) holds for any x ∈ [x], u ∈ [u]. By letting ζi =max x∈[x] γ⊤

> x,i

x + βi and ζi = min x∈[x] γ⊤

> x,i

x + βi, equation (5) yields 

γ⊤

> u,i

u + ζi ≤ fNN ,i (x, u ) ≤ γ⊤

> u,i

u + ζi. (6) Combining the control-affine enclosure for f0,i in (4) with that for fNN ,i in (6), we obtain a control-affine enclosure 

[f ][x](u) for f as expressed in (2)-(3), where ai = δi + γu,i ,

ai = δi + γu,i , ci = ηi + ζi, and ci = ηi + ζi.Fig. 1: Illustration of Example 1 with n = 2 and X = [0 , 4] ×

[0 , 4] . Left: the subpaving S (light green) with the grid lines (including dashed lines) induced by G1 = {0, 2, 3, 4} and 

G2 = {0, 3, 4}. Middle: the box [b1] ∈ S (dark green) and the control-affine enclosure box [f ][b1](u) for a particular u

(yellow). Right: the subpaving S = {[b1], [b2], [b3]}.V. F INITE INPUT SEARCH REDUCTION VIA HYPERPLANE 

ARRANGEMENT 

In this section, we introduce hyperplane arrangements in the control space based on the control-affine enclosure developed in Section IV, and show how they enable the reduction of the infinite control search required for verifying the CIS condition to a finite set of evaluations. Suppose that X and U are boxes given by X =

# ×nj=1 [xj , x j ] and U = ×mk=1 [uk, u k], respectively, and S

is a subpaving of X given as S = {[b1], . . . , [bN ]}. To verify the relaxed CIS condition introduced in Section III, we aim to determine, for each [b] ∈ S, whether there exists a control input u ∈ U such that [f ][b](u) ⊆ S, where [f ][b](u) denotes the control-affine enclosure (3) constructed over [b]. As u

varies continuously in the control space U, the enclosure box [f ][b](u) also varies continuously in the state space X .Therefore, the set containment relation above can change only when a lower or upper bound of the box [f ][b](u)

crosses the coordinate of the boundary of boxes in S. Thus, the containment relation changes in a discrete manner. Mathematically, for each coordinate j ∈ { 1, . . . , n }, let 

Gj := {g(q) 

> j

}Nj 

> q=1

denote the sorted set of all j-th coordinates of vertices of boxes in S, i.e., g(1)  

> j

< g (2)  

> j

< · · · < g (Nj ) 

> j

.Letting p = ( p1, . . . , p n) as the indexes of the boxes induced by Gj , where pj ∈ { 1, . . . , N j − 1}, we define the grid cell 

as C◦ 

> p

:= ×nj=1 

 g(pj ) 

> j

, g (pj +1) 

> j

.

Example 1: Consider n = 2 and X = [0 , 4] × [0 , 4] , as illustrated in Figure 1. Suppose S = {[b1], [b2], [b3]} is a sub-paving of X whose boxes have boundaries with coordinates 

G1 = {0, 2, 3, 4} and G2 = {0, 3, 4}, so that N1 = 4 and 

N2 = 3 . The grid cells are the (N1−1) ×(N2−1) = 3 ×2 = 6 

open rectangles C◦ 

> p

= ( g(p1)1 , g (p1+1) 1 ) × (g(p2)2 , g (p2+1) 2 ); for instance, C◦ 

> (1 ,1)

= (0 , 2) × (0 , 3) and C◦ 

> (2 ,2)

= (2 , 3) × (3 , 4) .In Figure 1 (left), the grid cells contained in S are those covered by light green, while the remaining grid cell C◦

> (3 ,2)

(white) is disjoint from S. Now choose the box [b1] ∈ S

to be tested (the dark green box in Figure 1, middle). For a particular control u, the enclosure [f ][b1](u) is the yellow rectangle. In this configuration, [f ][b1](u) intersects two grid cells C◦ 

> (1 ,1)

and C◦

> (2 ,1)

, both of which lie inside S, so the containment [f ][b1](u) ⊆ S holds. If u were to change so that 

Fig. 2: Illustration of Example 2 with m = 2 . Left: the combined view. Right: four components shown separately. 

U: the control box. A: the hyperplane arrangement A([ b]) ,consisting of the hyperplanes Hj,q and Hj,q from (7) together with the boundary hyperplanes H(k) 

> u

and H(k) 

> u

of U. F:the faces of A([ b]) within U, including vertices, open line segments, and open polygons. R: the regions, i.e., the full-dimensional faces. 

[f ][b1](u) shifts to the right and begins to intersect the white grid cell, or moves to extend beyond X , the containment would fail. Now we introduce hyperplane arrangements in the control space U. From the preceding discussion, it is clear that the containment [f ][b](u) ⊆ S can change only when a lower or upper bound of [f ][b](u) crosses a grid coordinate 

g(q) 

> j

∈ Gj . Since each bound depends affinely on u, each such crossing event corresponds to an affine hyperplane in the control space U. More precisely, for each j ∈ { 1, . . . , n }

and each q ∈ { 1, . . . , N j }, the crossing of the lower and upper bounds of [f ][b](u) with g(q) 

> j

defines the two types of affine hyperplanes 

Hj,q := u ∈ Rm : a⊤ 

> j

u + cj = g(q) 

> j

,

Hj,q := u ∈ Rm : a⊤ 

> j

u + cj = g(q) 

> j

, (7) where aj , a j , c j , c j are the enclosure coefficients determined by [b] and U via (3). To account for the boundary of U, we include the 2m additional boundary hyperplanes H(k) 

> u

:= 

{u ∈ Rm : uk = uk} and H(k) 

> u

:= {u ∈ Rm : uk = uk}

for k = 1 , . . . , m . Collecting all these hyperplanes yields a hyperplane arrangement in Rm, denoted by A([ b]) . Recall from Section II that each face of A([ b]) is a maximal connected set of points sharing the same sign pattern, and that the full-dimensional faces are called regions. Clearly, the arrangement A([ b]) partitions U into finitely many faces. We denote F as the set of all faces of A([ b]) contained in U,and R = {R1, . . . , R N } ⊂ F the set of regions. 

Example 2: Consider a two-dimensional control space with U = [ u1, u 1] × [u2, u 2] ⊂ R2 as illustrated in Figure 2. For a fixed box [b] ∈ S, suppose that, as u varies in U,the lower bound of [f ][b](u) in coordinate j attains the grid coordinate g(q) 

> j

, i.e., a⊤ 

> j

u + cj = g(q) 

> j

. Since m = 2 , this equation defines a line Hj,q in R2. Similarly, the upper bound meeting g(q) 

> j

gives another line Hj,q . Repeating this for all coordinates j = 1 , . . . , n and all grid indices q = 1 , . . . , N j

produces the full collection of hyperplanes. However, for illustration, we only focus on the two hyperplanes Hj,q and 

Hj,q . Together with the four boundary lines H(k) 

> u

, H(k) 

> u

for 

k = 1 , 2, these lines form the arrangement A([ b]) . The faces 

F of A([ b]) within U, bottom left) consist of 9 vertices, 12 open line segments, and 4 open convex polygonal regions 

R = {R1, R 2, R 3, R 4}.

Proposition 2: Let [b] ∈ S and assume that [f ][b](u) is a non-degenerate box for every u ∈ U . Define the covering indicator φ(u) := I([ f ][b](u) ⊆ S) ∈ { 0, 1}. The following two statements hold: (i) For each face F ∈ F , φ(u) is invariant for any u ∈ F .(ii) If φ(u) = 1 on a region Ri ∈ R , then φ(u) = 1 on every face contained in Ri.

Proof: (i) By definition, all points on a face F share the same sign pattern. For the proof, we augment Gj by setting g(0)  

> j

:= −∞ and g(Nj +1)  

> j

:= ∞, and extend the definition of a grid cell to C◦ 

> p

= ×nj=1 (g(pj ) 

> j

, g (pj +1)  

> j

) with 

pj ∈ { 0, . . . , N j }. For any u ∈ U , let C(u) denote the set of grid cells intersected by int([ f ][b](u)) . Since Gj contains all 

j-th coordinates of the boundaries of boxes in S, each grid cell is either contained in S or disjoint from S.The non-degeneracy assumption ensures that [f ][b](u) has nonempty interior and satisfies [f ][b](u) = int([ f ][b](u)) .Since S is closed, [f ][b](u) ⊆ S if and only if int([ f ][b](u)) ⊆

S. If int([ f ][b](u)) ̸ ⊆ S, then the open set int([ f ][b](u)) ∩

(Rn \ S) intersects at lease one grid cell disjoint from S.Therefore, φ(u) = 1 if and only if every cell in C(u) is contained in S. Equivalently, φ(u) = 0 if and only if some cell in C(u) is disjoint from S.Let u, u ′ ∈ F . Since u and u′ share the same sign pattern, it remains to show that C(u) is completely determined by 

s(u). The open box int([ f ][b](u)) = ×nj=1 (a⊤ 

> j

u+cj , a ⊤ 

> j

u+

cj ) intersects a grid cell C◦ 

> p

= ×nj=1 (g(pj ) 

> j

, g (pj +1)  

> j

) if and only if, for every j, a⊤ 

> j

u + cj < g (pj +1)  

> j

and a⊤ 

> j

u +

cj > g (pj ) 

> j

. For 1 ≤ pj ≤ Nj − 1, the truth values of these two inequalities are determined by the sign-pattern entries associated with Hj,p j +1 and Hj,p j , respectively, as defined in (7). When pj = 0 , the second inequality holds trivially because g(0)  

> j

= −∞ , while the first inequality is determined by the sign-pattern entry associated with Hj, 1.Similarly, when pj = Nj , the first inequality holds trivially because g(Nj +1)  

> j

= ∞, while the second inequality is determined by the sign-pattern entry associated with Hj,N j .Therefore, whether int([ f ][b](u)) intersects any given grid cell is completely determined by s(u), and hence so is C(u).Since s(u) = s(u′), we have C(u) = C(u′), which implies 

φ(u) = φ(u′). Thus, φ is constant on F .(ii) We first prove that the set U ([ b]) = {u ∈ U :[f ][b](u) ⊆ S} = {u ∈ U : φ(u) = 1 } is closed in U.The complement of U ([ b]) consists of those u ∈ U for which 

int([ f ][b](u)) intersects at least one grid cell C◦ 

> p

disjoint from 

S. For any fixed such cell, this intersection is equivalent to the strict inequalities a⊤ 

> j

u + cj < g (pj +1)  

> j

and a⊤ 

> j

u + cj > g (pj )

> j

for every j, where an inequality involving g(0)  

> j

= −∞ or 

g(Nj +1)  

> j

= ∞ is automatically satisfied. Hence, the set of controls for which a fixed grid cell is intersected is open in 

U. Since there are finitely many grid cells, the complement of U ([ b]) is a finite union of open sets, so U ([ b]) is closed in U.If φ = 1 on a region Ri, then Ri ⊆ U ([ b]) . Since U ([ b]) 

is closed in U, we have Ri ⊆ U ([ b]) , and hence φ = 1 for all points of Ri. Equivalently, φ = 1 on every face contained in Ri.Proposition 2 shows that the covering indicator φ(u) is invariant on each face, so it suffices to evaluate φ at one representative point per face. Since the number of faces is finite, the search for an admissible control input in the CIS condition reduces to a finite set of candidates. Furthermore, from the proof of Proposition 2 it follows that U ([ b]) is a finite union of convex polytopes, since each face closure is a convex polytope and A([ b]) has finitely many faces. Hence, U ([ b]) provides a uniform inner approximation of the regulation map over the box [b].VI. A LGORITHMS FOR MCIS A PPROXIMATIONS 

In this section, we present Algorithms 1-3 for constructing inner and outer approximations of the MCIS, along with their theoretical guarantees. In practice, the tightness of the control-affine enclosure depends on the width of [u], as the Taylor remainder bound of f0 contains quadratic terms in 

w([ u]) while the LiRPA relaxation gap of fNN grows with 

w([ u]) . To mitigate the potential enclosure conservatism, we partition U into control slices 

U =

> Nu

[

> i=1

[u(i) 

> s

]

and compute a separate control-affine enclosure on each slice. In the following algorithms, we instantiate the generic subpaving S in Sections III–V as Ck, the candidate CIS subpaving at the k-th iteration. Algorithm 1 takes as input the state constraint set X , the control constraint set U, a resolution parameter ε > 0, and the number of control slices Nu, and returns a subpaving 

C that is certified to be a CIS, or ∅ if no CIS is found at the given resolution. The algorithm performs a fixed-point iteration: starting from C0 = {X } (line 1), at each iteration 

k it calls Algorithm 2 to compute Ck+1 ⊆ Ck (line 3) by retaining only those boxes that pass the relaxed CIS test. The iteration terminates when either Ck+1 = ∅ (lines 4–5) or Ck+1 = Ck (lines 6–7). Algorithm 2 processes each box [b] ∈ Ck from a queue 

Q (lines 1, 3–4). For each control slice [u(i) 

> s

] (line 2, 6), the algorithm computes the control-affine enclosure coeffi-cients via (3) (line 7), constructs the hyperplane arrangement 

A([ b]) (line 8), and enumerates its regions (line 9). For the enumeration of polyhedral regions induced by a hyperplane arrangement, standard region-enumeration methods can be employed, such as the Incremental Enumeration method [23]. For each region R, one representative u∗ ∈ R is selected and tested for [f ][b](u∗) ⊆ Ck (lines 10–11). For compu-tational efficiency, Algorithm 2 only enumerates and tests the full-dimensional regions of the hyperplane arrangement. By Proposition 2(ii), once a region is certified feasible, all lower-dimensional faces contained in its closure are also feasible and therefore need not be tested separately. If a feasible u∗ is found on any slice, [b] is added to Ck+1 

(line 13-14); otherwise, if w([ b]) > ε , [b] is bisected into two boxes [b](1) , [b](2) satisfying [b](1) ∪[b](2) = [ b] with disjoint interiors, which are then returned to the queue (lines 15–16). Boxes with w([ b]) ≤ ε that fail the test are discarded. Algorithm 3 computes a subpaving O of X satisfying O ⊆X \ S ∗, which certifies that every box in O lies outside the MCIS. Together with C from Algorithm 1, the remaining states that belong neither to C nor to O are collected in a subpaving U. This yields a paving of X satisfying X =

C ∪ O ∪ U where C ⊆ S ∗ and O ⊆ X \ S ∗. Since the procedure is adopted from [13], we briefly outline the idea below. For a given state box [b] and control slice [u(i) 

> s

], the inclusion function [f ] evaluated on the joint box [b] × [u(i) 

> s

]

satisfies 

[f ]([ b], [u(i) 

> s

]) ⊇ { f (x, u ) : x ∈ [b], u ∈ [u(i) 

> s

]}. (8) This can be computed using standard interval enclosure techniques [16], [18]. A box [b] is then classified as outside the MCIS if, for every control slice i ∈ { 1, . . . , N u}, the over-approximated image [f ]([ b], [u(i) 

> s

]) either lies outside 

X or is contained in the already-classified outside region Ok

at iteration k. The algorithm terminates when the outside subpaving stabilizes, i.e., Ok+1 = Ok.The following theorems show the correctness and termi-nation guarantees of the proposed algorithms. 

Theorem 1: For any ε > 0 and Nu ≥ 1, the subpaving C

returned by Algorithm 1 is a CIS for system (1). 

Proof: The control-affine enclosure is a valid inclusion function for the full dynamics f = f0 + fNN , hence for every box [b] ∈ C, there exists u ∈ U such that f ([ b], u ) ⊆

[f ][b](u) ⊆ C. Note that u may vary for different choices of 

[b]. Now let x be any point in C. Then x belongs to some box [b] ∈ C, and therefore f (x, u ) ∈ f ([ b], u ) ⊆ C. Since this holds for every x ∈ C, we conclude that C is a CIS. 

Theorem 2: For any ε > 0 and Nu ≥ 1, the subpaving 

O returned by Algorithm 3 satisfies O ⊆ X \ S ∗.

Proof: We show by induction on k that Ok ∩ S ∗ = ∅.

Base case. O0 = ∅, so the claim holds trivially. 

Algorithm 1: Fixed-point iteration for CIS 

Input: X , U, ε, N u

Output: C as a CIS, or ∅ 

> 1

C0 ← {X } ; k ← 0; converged ← false ; 

> 2

while ¬ converged do  

> 3

Ck+1 ← INPRE (Ck, ε, U, N u); // Alg. 2  

> 4

if Ck+1 = ∅ then  

> 5

return ∅; // No CIS at resolution ε 

> 6

if Ck+1 = Ck then  

> 7

converged ← true ; 

> 8

k ← k + 1 ; 

> 9

return Ck

Algorithm 2: INPRE : Inner approximation of Pre( Ck)

Input: Ck, ε, U, N u

Output: Ck+1 with Ck+1 ⊆ Ck 

> 1

Q ← Ck; Ck+1 ← ∅ ; 

> 2

Partition U into Nu slices [u(1)  

> s

], . . . , [u(Nu) 

> s

]; 

> 3

while Q̸ = ∅ do  

> 4

[b] ← Pop( Q); 

> 5

feasible ← false ; 

> 6

for i = 1 , . . . , N u do 

> 7

aj , a j , c j , c j 

> nj=1

← [f ][b] over [u(i) 

> s

]; // (3)  

> 8

A([ b]) ← Snj=1 

SNj

> q=1

Hj,q , Hj,q ∪ Smk=1 

{u :

uk = u(i)

> s,k

}, {u : uk = u(i)

> s,k

} ; 

> 9

foreach region R of A([ b]) within [u(i) 

> s

] do  

> 10

Choose one u∗ ∈ R; 

> 11

if φ(u∗) = 1 then  

> 12

feasible ← true ; 

> 13

if feasible then  

> 14

Ck+1 ← Ck+1 ∪ { [b]}; 

> 15

else if w([ b]) > ε then  

> 16

bisect [b] → [b](1) , [b](2) ;

Q ← Q ∪ { [b](1) , [b](2) }; 

> 17

return Ck+1 

Inductive step. Assume Ok ∩ S ∗ = ∅. Let [b] be a box added to Ok+1 , so that [f ]([ b], [u(i) 

> s

]) ∩ X ⊆ Ok for all 

i ∈ { 1, . . . , N u}. Since U = SNu

> i=1

[u(i) 

> s

], for any x ∈ [b] and any u ∈ U , there exists i such that (x, u ) ∈ [b] × [u(i) 

> s

], so by (8), f (x, u ) ∈ [f ]([ b], [u(i) 

> s

]) . Since [f ]([ b], [u(i) 

> s

]) ∩ X ⊆ 

Ok, either f (x, u ) /∈ X or f (x, u ) ∈ Ok. In the first case, 

f (x, u ) /∈ S ∗ since S∗ ⊆ X ; in the second case, f (x, u ) /∈S∗ by the inductive hypothesis Ok ∩S ∗ = ∅. Since this holds for all u ∈ U , x / ∈ Pre( S∗). By S∗ ⊆ Pre( S∗), it follows that x / ∈ S ∗. Thus, [b] ∩ S ∗ = ∅ and Ok+1 ∩ S ∗ = ∅.

Theorem 3: For any ε > 0 and Nu ≥ 1, Algorithms 1, 2, and 3 terminate in a finite number of steps. 

Proof: In Algorithm 2, each box popped from the queue is either added to Ck+1 , bisected, or discarded. Bisection reduces the width of the box by half. After at most ⌈log 2(w(X )/ε )⌉ bisections along any single dimension, the box width falls below ε and the box is discarded, so Algorithm 2 terminates. For Algorithms 1 and 3, the bisection tree of X at resolution ε has finitely many leaves, so the collection of all subpavings at this resolution is finite. Since Ck+1 ⊆ Ck and 

Ok ⊆ Ok+1 at every iteration, both sequences are monotone in a finite collection and hence must terminate. Since the computations for different boxes and different control representatives are independent, they are naturally parallelizable. In our implementation, state box and control slice pairs are collected into batches, and the matrix mul-tiplications in the LiRPA bound propagation are executed concurrently on the GPU. Algorithm 3: Determining States Outside MCIS 

Input: X , U, ε, N u

Output: O with O ⊆ X \ S ∗ 

> 1

O0 ← ∅ ; k ← 0; converged ← false ; 

> 2

Partition U into Nu slices [u(1)  

> s

], . . . , [u(Nu) 

> s

]; 

> 3

while ¬ converged do  

> 4

Q ← X ; Ok+1 ← Ok; 

> 5

while Q̸ = ∅ do  

> 6

[b] ← Pop( Q); 

> 7

outside ← true ; 

> 8

for i = 1 , . . . , N u do  

> 9

Compute [f ]([ b], [u(i) 

> s

]) ; // (8)  

> 10

if [f ]([ b], [u(i) 

> s

]) ∩ X̸ ⊆ Ok then  

> 11

outside ← false ; 

> 12

if outside then  

> 13

Ok+1 ← Ok+1 ∪ { [b]}; 

> 14

else if w([ b]) > ε then  

> 15

bisect [b] → [b](1) , [b](2) ;

Q ← Q ∪ { [b](1) , [b](2) }; 

> 16

if Ok+1 = Ok then  

> 17

converged ← true ; 

> 18

k ← k + 1 ; 

> 19

return Ok

VII. S IMULATION EXAMPLES 

We present two simulation examples to demonstrate the effectiveness of the proposed method. All experiments are conducted on a desktop equipped with an AMD Ryzen 7 7700X 8-Core CPU, 32 GB RAM, and an NVIDIA GeForce RTX 5070 Ti GPU with 16 GB VRAM. 

A. Lane Keeping 

Consider the discrete-time bicycle model in [15]: 

yk+1 = yk + v dt sin θk,θk+1 = θk + v dt tan uk 

> l1

, (9) where yk ∈ R is the lateral displacement of the vehicle, 

θk ∈ R is the yaw angle, uk ∈ R is the steering angle, and the parameters are chosen as l1 = 5 m (vehicle length), l2 = 2 m(vehicle width), v = 6 m/s (vehicle speed), and w = 3 .5 m(lane width). The state is defined as xk = ( yk, y k +l1θk)⊤ ∈

R2, with scalar control uk ∈ R. We consider the NNCS (1) where f0 ≡ 0 and fNN is a 3-layer, ReLU-activated FFNN with two hidden layers of sizes n1 = 8 , n 2 = 4 , trained to imitate the bicycle model in (9). Specifically, the network is of the form xk+1 = fNN (xk, u k) and is trained on 2 × 10 5

training samples and validated on 5 × 10 4 samples for 200 

epochs, achieving a final validation MSE of 5.06 × 10 −7.We consider the state constraint set X = [ −0.75 , 0.75] ×

[−0.75 , 0.75] and two different control constraint sets: U =[−5◦, 5◦] or [−10 ◦, 10 ◦].We fix Nu = 3 and vary the resolution ε = w−l2 

> K

with 

K ∈ { 32 , 64 , 128 , 256 , 512 , 1024 } in both Algorithm 1 and 3, yielding increasingly fine pavings. Since the control is a scalar, each hyperplane (7) reduces to a point in R, and the regions of the hyperplane arrangement are open intervals whose midpoints could serve as the representatives in Propo-sition 2. Figure 3 shows the pavings C, O, U as results of Algorithm 1 and 3, for varying K and two different U. It can be observed that the size of U, which is the set of states with undetermined classification, decreases as K increases, i.e, as the paving becomes finer with higher resolution. Table I reports the wall-clock runtimes of Algorithm 1 for computing the CIS C.

B. Flow Navigation in Frozen Synthetic Turbulence 

Consider a point agent moving in a two-dimensional frozen incompressible velocity field constructed via the ran-dom Fourier mode synthesis of [24], with stream function 

ψ(x, y ) = PNj=1 Aj sin( kx,j x + ky,j y + φj ) and veloc-ity V = (∂y ψ, −∂xψ)⊤. We use N = 40 modes with wavevectors (kx,j , k y,j ) sampled from {± 1, . . . , ±8}2, ran-dom phases φj ∼ Uniform(0 , 2π), and amplitudes Aj =8/∥(kx,j , k y,j )∥2.5. We train a 4-layer, tanh -activated FFNN, 

ˆV = ( ˆVx, ˆVy )⊤ : R2 → R2, with three hidden layers of sizes 

n1 = n2 = n3 = 256 to approximate the velocity field V .The network is trained on 8 × 10 5 samples and validated on 

8 × 10 4 samples for 500 epochs, achieving a final validation MSE of 3.48 × 10 −4. Figure 4 shows the learned speed map and streamlines. Now consider the discrete-time flow navigation model: 

xk+1 = xk + dt   ˆVx(xk, y k) + ux,k 

,yk+1 = yk + dt   ˆVy (xk, y k) + uy,k 

, (10) where xk and yk denote the agent position coordinates, ux,k 

and uy,k are the control inputs, and dt = 0.02 s is the sampling period. The state is defined as (xk, y k)⊤ ∈ R2 with control uk = ( ux,k , u y,k )⊤ ∈ R2. We consider the NNCS (1) where f0(x, u ) = x+dt u and fNN (xk) = dt ˆV (xk, y k). The state and control constraint sets are chosen as X = [ −π, π ]×

[−π, π ] and U = [ −0.5, 0.5] × [−0.5, 0.5] . We fix Nu = 1 ,i.e., no control slicing, and vary the resolution ε = 2πK with 

K ∈ { 1024 , 2048 , 4096 } in both Algorithms 1 and 3. Since the control is two-dimensional, each hyperplane (7) is a line in R2, and the regions of the arrangement are open convex polygons. Figure 5 shows the pavings C, O, U as results of Algorithms 1 and 3 for varying K. As in the lane keeping example, the size of U decreases as K increases, i.e., as the paving becomes finer with higher resolution. The wall-clock runtimes of Algorithm 1 for computing the CIS C are 

79 .5 s, 245 .1 s, and 2056 .3 s for K = 1024 , 2048 , and 4096 ,respectively. TABLE I: Runtime (in sec) for computing the CIS C in the lane keeping example.                     

> U\ K32 64 128 256 512 1024
> [−5◦,5◦]0.35 0.56 0.91 1.14 1.49 3.81
> [−10 ◦,10 ◦]0.22 0.34 0.31 0.36 0.51 0.86

(a) U = [ −5◦, 5◦].

(b) U = [ −10 ◦, 10 ◦].

Fig. 3: Simulation results for the lane keeping example in Section VII-A with varying K and two different control sets U.The subpaving C (green boxes) represents a CIS as an inner approximation of the MCIS; O (gray boxes) contains states outside the MCIS; and U (yellow boxes) contains states with undetermined classification. It can be observed that the size of U decreases as K increases, i.e, as the paving becomes finer with higher resolution. 

Fig. 4: Learned velocity field ˆV produced by the FFNN in the flow navigation example. Left: speed map ∥ ˆV (x)∥.Right: streamlines of ˆV .VIII. C ONCLUSION 

In this paper, we proposed an optimization-free approach for computing inner and outer approximations of the max-imal controlled invariant set for NNCSs. By leveraging the control-affine inclusion function of NNCS dynamics and hyperplane arrangements, the infinite control search for verifying CISs is reduced to a finite set of evaluations. Verification-guided algorithms are developed with inherent parallelizability. Simulation results on lane keeping and frozen-turbulence navigation demonstrated the effectiveness of the proposed method in the computation of three-class pavings and certified CIS. Future work includes the extension to robust controlled invariance under uncertainty and the quantification of approximation errors. 

Fig. 5: Simulation results for the flow navigation example in Section VII-B with varying K. The subpaving C (green boxes) represents a CIS as an inner approximation of the MCIS; O (gray boxes) contains states outside the MCIS; and U (yellow boxes) contains states with undetermined classification. REFERENCES [1] T. Salzmann, E. Kaufmann, J. Arrizabalaga, M. Pavone, D. Scara-muzza, and M. Ryll, “Real-Time Neural MPC: Deep learning model predictive control for quadrotors and agile robotic platforms,” IEEE Robotics and Automation Letters , vol. 8, no. 4, pp. 2397–2404, 2023. [2] R. Schwan, C. N. Jones, and D. Kuhn, “Stability verification of neural network controllers using mixed-integer programming,” IEEE Transactions on Automatic Control , vol. 68, no. 12, pp. 7514–7529, 2023. [3] F. Fabiani and P. J. Goulart, “Reliably-stabilizing piecewise-affine neural network controllers,” IEEE Transactions on Automatic Control ,vol. 68, no. 9, pp. 5201 – 5215, 2022. [4] M. Fazlyab, M. Morari, and G. J. Pappas, “Safety verification and robustness analysis of neural networks via quadratic constraints and semidefinite programming,” IEEE Transactions on Automatic Control ,vol. 67, no. 1, pp. 1–15, 2020. [5] A. D. Ames, X. Xu, J. W. Grizzle, and P. Tabuada, “Control barrier function based quadratic programs for safety critical systems,” IEEE Transactions on Automatic Control , vol. 62, no. 8, pp. 3861–3876, 2016. [6] X. Xu, P. Tabuada, J. W. Grizzle, and A. D. Ames, “Robustness of con-trol barrier functions for safety critical control,” IFAC-PapersOnLine ,vol. 48, no. 27, pp. 54–61, 2015. [7] J. Rawlings, D. Mayne, and M. Diehl, Model Predictive Control: Theory, Computation, and Design . Nob Hill Publishing, LLC, 2020. [8] D. Q. Mayne, J. B. Rawlings, C. V. Rao, and P. O. Scokaert, “Constrained model predictive control: Stability and optimality,” Au-tomatica , vol. 36, no. 6, pp. 789–814, 2000. [9] D. P. Bertsekas, “Infinite-time reachability of state-space regions by using feedback control,” IEEE Transactions on Automatic Control ,vol. 17, no. 5, pp. 604–613, 1972. [10] F. Blanchini, “Set invariance in control,” Automatica , vol. 35, no. 11, pp. 1747–1767, 1999. [11] J. N. Maidens, S. Kaynama, I. M. Mitchell, M. M. Oishi, and G. A. Dumont, “Lagrangian methods for approximating the viability kernel in high-dimensional systems,” Automatica , vol. 49, no. 7, pp. 2017– 2029, 2013. [12] M. Fiacchini, T. Alamo, and E. F. Camacho, “On the computation of convex robust control invariant sets for nonlinear systems,” Automat-ica , vol. 46, no. 8, pp. 1334–1338, 2010. [13] D. Monnet, J. Ninin, and L. Jaulin, “Computing an inner and an outer approximation of the viability kernel,” Reliable Computing , vol. 22, 2016. [14] S. Brown, M. Khajenejad, S. Z. Yong, and S. Mart´ ınez, “Computing controlled invariant sets of nonlinear control-affine systems,” in IEEE Conference on Decision and Control , 2023, pp. 7830–7836. [15] X. Li, T. Wei, C. Liu, A. Girard, and I. Kolmanovsky, “Control invari-ant sets for neural network dynamical systems and recursive feasibility in model predictive control,” arXiv preprint arXiv:2505.11546 , 2025. [16] R. E. Moore, R. B. Kearfott, and M. J. Cloud, Introduction to Interval Analysis . SIAM, 2009. [17] L. Jaulin, M. Kieffer, O. Didrit, and E. Walter, Applied Interval Analysis . Springer, 2001. [18] G. Chabert and L. Jaulin, “Contractor programming,” Artificial Intel-ligence , vol. 173, no. 11-12, pp. 1079–1100, 2009. [19] J.-P. Aubin, A. M. Bayen, and P. Saint-Pierre, Viability Theory: New Directions . Springer Science & Business Media, 2011. [20] R. P. Stanley et al. , “An introduction to hyperplane arrangements,” 

Geometric Combinatorics , vol. 13, pp. 389–496, 2007. [21] P. Orlik and H. Terao, Arrangements of Hyperplanes . Springer Science & Business Media, 2013, vol. 300. [22] H. Zhang, T.-W. Weng, P.-Y. Chen, C.-J. Hsieh, and L. Daniel, “Effi-cient neural network robustness certification with general activation functions,” in Advances in Neural Information Processing Systems (NeurIPS) , vol. 31, 2018, pp. 4939–4948. [23] M. Rada and M. Cerny, “A new algorithm for enumeration of cells of hyperplane arrangements and a comparison with avis and fukuda’s reverse search,” SIAM Journal on Discrete Mathematics , vol. 32, no. 1, pp. 455–473, 2018. [24] R. H. Kraichnan, “Diffusion by a random velocity field,” The Physics of Fluids , vol. 13, no. 1, pp. 22–31, 1970.