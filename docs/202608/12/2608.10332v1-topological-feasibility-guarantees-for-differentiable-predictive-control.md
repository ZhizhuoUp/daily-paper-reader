---
title: Topological Feasibility Guarantees for Differentiable Predictive Control
title_zh: 可微预测控制的拓扑可行性保证
authors: "Guangyu Wu, Ján Drgoňa"
date: 2026-08-11
pdf: "https://arxiv.org/pdf/2608.10332v1"
tags: ["query:q2"]
score: 8.0
evidence: 微分预测控制的可行性
tldr: 本研究针对可微分预测控制（DPC）在离线策略优化中缺乏确定性可行性保证的难题，提出了一种基于拓扑分析的新方法。通过将系统动力学嵌入计算图，利用诱导可达安全集的几何特性，并结合控制屏障函数（CBF）设计自监督学习策略。该方法无需在线安全过滤器，即可从有限训练样本中推导出严格的确定性可行性保证，为学习控制的安全性提供了理论支撑。
selection_source: fresh_fetch
motivation: 针对可微分预测控制在离线策略优化中缺乏严谨确定性可行性保证的挑战。
method: 通过对诱导可达安全集进行拓扑分析，并结合控制屏障函数（CBF）设计了一种新型自监督离线学习策略。
result: 实验表明约束违规随训练样本增加而单调减少至零，成功从有限样本中获得了严格的可行性保证。
conclusion: 该工作证明了DPC能提供传统黑盒学习方法难以实现的正式安全证书，为学习控制的安全性保障开辟了新路径。
---

## 摘要
可微预测控制（DPC）是一种用于逼近显式模型预测控制（MPC）策略的自监督学习方法，与基于在线优化的 MPC 相比，具有显著的计算优势。然而，作为安全控制的核心要求，可行性保证目前要么是以概率方式提供的，要么是通过在线安全过滤器实现的。离线策略优化缺乏严格的可行性保证仍然是一个开放性问题。本文通过对诱导可达安全集进行新颖的拓扑分析，为 DPC 建立了确定性的可行性保证，且无需在线安全过滤器。通过利用 DPC 固有的基于模型的特性（即可微系统动力学直接嵌入到计算图中），我们从拓扑和几何角度分析了所学控制策略及相应系统状态的性质。受理论分析的启发，我们提出了一种新颖的自监督离线策略学习策略，该策略利用了带有控制屏障函数（CBF）的代理损失。至关重要的是，这些性质不仅显著改善了策略训练，还能够从有限数量的训练样本中推导出严格的、确定性的可行性保证。广泛的闭环仿真验证了我们的理论发现，表明随着训练样本量的增加，经验约束违反量单调下降至零。最终，这项工作表明，DPC 策略优化产生了传统黑盒方法（如强化学习 (RL) 或基于监督学习的近似 MPC）在结构上无法实现的形式化安全证书，从而为学习控制中的可行性保证提供了新的视角。

## Abstract
Differentiable predictive control (DPC), a self-supervised learning approach for approximating explicit model predictive control (MPC) policies, offers significant computational advantages over online optimization-based MPC. However, feasibility guarantees, a core requirement for safe control, are currently provided either probabilistically or via online safety filters. The lack of rigorous feasibility guarantees for offline policy optimization remains an open problem. This paper establishes deterministic feasibility guarantees for DPC using a novel topological analysis of the induced reachable safe set, without requiring online safety filters. By exploiting the inherent model-based nature of DPC, in which differentiable system dynamics are embedded directly into the computational graph, we analyze the properties of the learned control policies and the corresponding system states from topological and geometric perspectives. Inspired by our theoretical analysis, we propose a novel self-supervised offline policy learning strategy that utilizes a proxy loss with Control Barrier Functions (CBFs). Crucially, these properties not only significantly improve policy training but also enable the derivation of strict, deterministic feasibility guarantees from a finite number of training samples. Extensive closed-loop simulations validate our theoretical findings, demonstrating that the empirical constraint violations monotonically decrease to zero as the training sample size increases. Ultimately, this work illustrates that DPC policy optimization yields formal safety certificates that are structurally unattainable with conventional black-box methods, e.g., reinforcement learning (RL) or supervised learning-based approximate MPC, thereby providing a new perspective on feasibility guarantees in learning-based control.

---

## 论文详细总结（自动生成）

这篇论文《Topological Feasibility Guarantees for Differentiable Predictive Control》探讨了可微预测控制（DPC）在离线训练中实现确定性可行性保证的理论与方法。以下是对该论文的深度结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在不依赖在线安全过滤器（如QP投影）或在线优化的情况下，为基于神经网络的显式控制策略（特别是DPC）提供**确定性的可行性（Feasibility）保证**。
*   **研究背景**：
    *   **传统MPC**：具备严谨的递归可行性和稳定性，但在线计算开销大。
    *   **近似MPC/强化学习**：推理速度快，但安全性通常只能以概率形式保证，或者需要昂贵的在线安全层进行修正。
    *   **DPC**：通过将动力学模型嵌入计算图进行自监督学习，虽具有扩展性，但此前缺乏离线训练后的形式化安全证明。
*   **动机**：填补学习控制中“离线训练”与“确定性安全证书”之间的理论空白。

### 2. 论文提出的方法论
论文的核心思想是利用**拓扑分析**证明：只要训练样本足够且满足特定条件，所学的策略就能覆盖整个可达安全集。
*   **拓扑可行性分析**：
    *   利用**Heine-Borel定理**：证明可达安全集是紧集（Compact Set），可以被有限个基于训练轨迹的开球（Open Cover）所覆盖。
    *   **Lipschitz连续性假设**：假设系统动力学、控制策略和约束函数均满足局部Lipschitz条件，从而推导出每个训练样本周围存在一个“可行性半径”。
*   **CBF-proxy DPC 算法**：
    *   **核心公式**：引入离线控制屏障函数（CBF）残差 $r_{k+1}$，要求满足 $h(x_{k+1}) \ge (1-\eta)h(x_k)$。
    *   **两阶段训练方案**：
        1.  **预热阶段（Warm-start）**：使用软惩罚（如ReLU/GeLU）进行初步优化，使策略接近可行区域。
        2.  **微调阶段（Fine-tuning）**：引入基于对数屏障（Log-barrier）的CBF代理损失函数，并配合松弛变量 $\delta$。当 $\delta=0$ 时，该样本点获得确定性安全证书。
*   **确定性保证**：证明随着训练样本数 $m$ 的增加，约束违反概率单调递减，并在有限样本量下收敛至零。

### 3. 实验设计
论文在三个具有代表性的非线性/复杂控制场景中进行了验证：
*   **场景1：非完整移动机器人导航**（2D网格，静态障碍物避障）。
*   **场景2：不稳定线性系统稳定化**（具有控制输入饱和与状态约束的开环不稳定系统）。
*   **场景3：受限四旋翼无人机系统**（12维高维非线性系统，严重欠驱动）。
*   **Benchmark（基准）**：主要对比了**标准DPC（Vanilla DPC）**（仅使用软惩罚）。
*   **对比指标**：点对点违反率（Pointwise violation ratio）和轨迹违反率（Trajectory violation ratio）。

### 4. 资源与算力
*   **软件框架**：使用 PyTorch 进行自动微分和反向传播（BPTT）。
*   **优化器**：Adam 和 AdamW。
*   **算力说明**：文中**未明确提及**具体的 GPU 型号、数量或具体的训练时长。但从模型结构（多层感知机 MLP，隐藏层神经元在 64 到 512 之间）来看，该方法对算力的要求处于常规深度学习工作站可接受的范围内。

### 5. 实验数量与充分性
*   **实验规模**：
    *   机器人实验：测试了 1000 个未见过的随机环境。
    *   线性系统：进行了 1000 次独立随机初始条件的蒙特卡洛仿真。
    *   四旋翼：测试了 2000 个随机初始样本。
*   **充分性**：实验涵盖了从低维到高维、从线性到非线性的多种动力学系统。通过改变训练样本数量（如从 50 到 1000）观察性能变化，验证了理论中的单调收敛性。
*   **客观性**：实验设计了消融对比（有无 CBF-proxy 损失），结果清晰地展示了所提方法在消除约束违反方面的优越性。

### 6. 论文的主要结论与发现
*   **有限样本覆盖**：证明了通过有限数量的离线训练样本，可以实现对连续状态空间内可达安全集的完全覆盖。
*   **单调收敛性**：实验证实，随着训练样本增加，DPC 策略的经验约束违反率单调下降至零。
*   **无需在线修正**：所提方法训练出的策略在推理阶段无需任何在线优化或安全过滤，即可保持确定性安全。
*   **结构化优势**：DPC 的“白盒”特性（可微动力学）使其能够提供黑盒强化学习无法实现的形式化安全证书。

### 7. 优点
*   **理论严谨**：首次从拓扑学角度为 DPC 提供了确定性可行性证明，而非传统的概率保证。
*   **计算高效**：保持了神经网络策略的高速推理特性，消除了在线安全层的计算负担。
*   **训练稳定**：提出的两阶段策略（软惩罚预热 + 硬约束微调）有效解决了屏障函数在训练初期的数值不稳定性问题。

### 8. 不足与局限
*   **Lipschitz常数依赖**：理论保证依赖于对系统 Lipschitz 常数的估计，而在复杂非线性系统中，准确估计这些常数具有挑战性。
*   **样本分布敏感性**：虽然证明了有限样本可行，但前提是样本分布需要“足够密集”以覆盖安全集，如何高效进行主动采样（Active Sampling）以最小化所需样本量仍是未来课题。
*   **环境动态性**：目前的实验主要针对静态障碍物和确定性动力学，对于高度动态或具有强随机扰动的环境，其拓扑覆盖理论可能需要进一步扩展。

（完）
