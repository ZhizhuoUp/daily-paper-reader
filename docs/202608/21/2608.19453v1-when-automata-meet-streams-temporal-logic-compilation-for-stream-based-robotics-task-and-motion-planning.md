---
title: "When Automata Meet Streams: Temporal Logic Compilation for Stream-Based Robotics Task and Motion Planning"
title_zh: 当自动机遇到流：基于流的机器人任务与运动规划的时序逻辑编译
authors: "Sayem Nazmuz Zaman, Cyrus Neary"
date: 2026-08-19
pdf: "https://arxiv.org/pdf/2608.19453v1"
tags: ["query:q4"]
score: 8.0
evidence: 基于流的机器人任务与运动规划
tldr: 针对基于流的机器人任务与运动规划（TAMP）中难以处理时序逻辑约束的问题，本文提出了SAM-TD编译方法。该方法将LTLf规范转换为自动机，并将其嵌入动作模式中，从而支持在规划过程中动态生成的几何对象。SAM-TD通过同步更新自动机状态和共享验证令牌来剪枝违规分支，实现了在流式TAMP中对复杂时序约束的支持，且性能优于现有方法。
selection_source: fresh_fetch
motivation: 现有的流式TAMP规划器仅关注目标可达性，无法处理规划过程中动态生成对象时的复杂时序逻辑约束（如安全性、顺序等）。
method: 提出SAM-TD方法，将LTLf约束编译为自动机并嵌入动作模式，利用回归自动机守卫和验证令牌在搜索过程中同步监控状态并剪枝。
result: 在三个PDDLStream机器人环境和标准PDDL基准测试中，SAM-TD首次实现了带LTLf约束的流式TAMP，且性能与最先进的离散编译方法相当。
conclusion: SAM-TD成功解决了流式TAMP中动态对象与时序逻辑编译的兼容性问题，为复杂长程机器人任务提供了有效的规划手段。
---

## 摘要
基于流的机器人任务与运动规划（TAMP）将离散符号规划与动态生成的连续几何参数（如位姿、抓取和轨迹）相结合。然而，基于流的规划器通常仅推理目标的可达性，而长时程任务还要求遵守时序规范，例如安全关键的排序、不变性和活性约束。目前尚无方法能为基于流的求解器强制执行此类时序约束，因为流在规划过程中通过迭代流细化循环生成不断扩展的几何对象集，使得现有的时序逻辑编译技术无法兼容。因此，我们提出了带令牌销毁的同步动作监控（SAM-TD），这是一种在基于流的 TAMP 中强制执行任意有限轨迹线性时序逻辑（$\textrm{LTL}_f$）规范的编译方法。SAM-TD 将任意 $\textrm{LTL}_f$ 约束转换为自动机，并将回归的自动机守卫嵌入到规划开始前预先指定的动作模式中。通过这种方式，SAM-TD 可以处理规划期间由流生成的对象，从而避免了枚举固定对象集或修改底层规划器的需要。在搜索过程中，SAM-TD 同步更新自动机状态，并使用所有自动机共享的有效性令牌来剪枝违反约束的分支。我们证明了 SAM-TD 支持规划搜索过程中来自迭代流细化的动态生成流对象。实验结果首次展示了在三个机器人 PDDLStream 环境中受 $\textrm{LTL}_f$ 约束的基于流的 TAMP。此外，在标准的离散 PDDL 基准测试中，SAM-TD 与最先进的时序约束编译方法相比具有竞争力。

## Abstract
Stream-based robotics Task and Motion Planning (TAMP) integrates discrete symbolic planning with dynamically generated continuous geometric parameters, such as poses, grasps, and trajectories. However, stream-based planners typically reason only about goal reachability, whereas long-horizon tasks also demand adherence to temporal specifications, such as safety-critical ordering, invariance, and liveness constraints. No methods currently exist to enforce such temporal constraints for stream-based solvers because streams generate an expanding geometric object set via iterative stream refinement loops during planning, rendering existing temporal-logic compilation techniques incompatible. We therefore present Synchronous Action Monitoring with Token Destruction (SAM-TD), a compilation method that enforces arbitrary Linear Temporal Logic over finite traces ($\textrm{LTL}_f$) specifications in stream-based TAMP. SAM-TD translates arbitrary $\textrm{LTL}_f$ constraints into automata and embeds regressed automaton guards into action schemas, which are pre-specified before planning begins. By doing so, SAM-TD can handle objects generated by streams during planning, thus circumventing the need to enumerate a fixed object set or modify the underlying planner. During search, SAM-TD synchronously updates automaton states and uses a validity token shared across all automata to prune constraint-violating branches. We show that SAM-TD supports dynamically generated stream objects from iterative stream refinements during plan search. Experimental results provide the first ever demonstration of stream-based TAMP under $\textrm{LTL}_f$ constraints in three robotics PDDLStream environments. Furthermore, on standard discrete PDDL benchmarks, SAM-TD is competitive with state-of-the-art temporal-constraint compilation methods.

---

## 论文详细总结（自动生成）

这是一份关于论文《When Automata Meet Streams: Temporal Logic Compilation for Stream-Based Robotics Task and Motion Planning》的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：在机器人领域，任务与运动规划（TAMP）需要同时处理高层离散逻辑和底层连续几何（如抓取、轨迹）。基于流（Stream-based）的规划器（如 PDDLStream）通过在规划过程中动态生成几何对象来解决这一问题。
*   **核心问题**：现有的时序逻辑（如 $LTL_f$）编译技术假设对象集合是固定且预先已知的。然而，基于流的规划器在搜索过程中会不断产生新对象，导致传统编译方法失效。目前尚无方法能在这种动态对象环境下强制执行复杂的时序约束（如“在移动红块前必须先放置白块”）。
*   **整体含义**：本文旨在填补这一空白，提出一种能够处理动态生成对象的编译框架，使机器人能在复杂的长程任务中遵守安全和顺序规范。

### 2. 方法论：SAM-TD
核心思想是**带令牌销毁的同步动作监控（SAM-TD）**，其关键技术细节包括：
*   **自动机转换**：将每个 $LTL_f$ 约束转换为确定性有限自动机（DFA）。
*   **提升式动作回归（Lifted Action Regression）**：这是处理动态对象的关键。SAM-TD 不在具体的动作实例上操作，而是在“动作模式”（Action Schemas）层面进行回归。它将自动机的状态转移守卫（Guards）反向推导为动作执行前的等效条件，从而支持规划时才确定的几何参数。
*   **同步更新**：通过在动作模式中添加“条件效应”（Conditional Effects），使自动机状态与机器人动作同步更新，无需插入额外的同步动作，保证了规划效率。
*   **令牌销毁机制（Token Destruction）**：引入一个全局共享的 `Valid` 令牌。如果某个动作导致任何自动机进入“死状态”（Sink State），该动作的效应将永久删除该令牌。由于目标状态要求必须持有该令牌，规划器（利用 $h_{FF}$ 等启发式算法）能立即识别并剪枝掉违规的分支，避免在无效路径上浪费昂贵的几何采样算力。

### 3. 实验设计
*   **实验场景**：
    1.  **Kitchen**：厨房环境，涉及餐具操作和复杂的顺序约束。
    2.  **Tabletop Manipulation**：桌面操作，要求避开危险区域并遵守放置顺序。
    3.  **ZoneSort**：区域分类，涉及多个块的排序和路径安全。
*   **Benchmark 与对比方法**：
    *   **离散基准**：在标准 PDDL3 任务（Openstacks, Rovers, Storage）上对比了 BM06、TB15、TCORE、LiftedTCORE、LCC 和 Plan4Past 等 6 种最先进的编译方法。
    *   **消融实验**：对比了 SAM-TD 与 **SAM-PB**（前提条件阻塞法），验证令牌销毁机制在处理复杂约束时的优越性。

### 4. 资源与算力
*   **算力说明**：论文未详细列出具体的 GPU/CPU 型号。
*   **实验限制**：实验设置了 **180 秒**的编译加搜索总时长限制，以及 **3 GiB** 的内存限制。
*   **软件环境**：使用了 Fast Downward 规划器和 PDDLStream 的 Adaptive 算法。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对 3 个机器人环境进行了约束数量的扩展性实验（从 1 个到 16 个约束）。
    *   设计了“对抗性约束”实验，强制规划器在添加每个新约束时都必须重新规划。
    *   在 6 个经典 PDDL 任务上进行了 30 组不同约束水平的对比测试。
*   **充分性评价**：实验设计较为充分且客观。它不仅验证了在新型流式 TAMP 中的有效性，还通过经典基准测试证明了其在传统领域的竞争力。消融实验清晰地展示了为何“令牌销毁”比“前提条件阻塞”更适合现代规划器的翻译层。

### 6 主要结论与发现
*   **首次实现**：SAM-TD 是首个支持在基于流的 TAMP 中强制执行任意 $LTL_f$ 约束的方法。
*   **线性扩展性**：编译时间随约束数量呈线性增长，编译层本身不会成为性能瓶颈。
*   **剪枝效率**：令牌销毁机制能有效引导启发式搜索，在进行昂贵的几何采样之前就剔除违规路径。
*   **兼容性**：在传统离散任务中，SAM-TD 的性能与当前最先进的方法相当，具有良好的通用性。

### 7. 优点
*   **架构优雅**：通过在模式层面进行回归，完美解决了动态对象生成的兼容性问题。
*   **无侵入性**：不需要修改底层的 PDDLStream 规划器或采样器，仅通过修改 PDDL 领域文件即可实现。
*   **搜索友好**：利用令牌销毁机制巧妙地将时序违规转化为启发式算法可识别的“死胡同”，大幅提升搜索效率。

### 8. 不足与局限
*   **瓶颈转移**：虽然编译本身很快，但复杂的约束会显著增加 PDDL 翻译器（如 Fast Downward 的转换层）的负担，导致接地（Grounding）成本上升。
*   **约束范围**：目前的约束主要定义在离散的任务层（Task-level），尚未直接处理运动层（Motion-level）的连续轨迹约束（尽管可以通过离散化间接处理）。
*   **对象预知**：虽然支持动态生成的对象，但 $LTL_f$ 公式中提到的特定命名对象（如 `blue_block`）仍需在编译前已知。

（完）
