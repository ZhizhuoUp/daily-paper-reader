---
title: Multimodal Trajectory Planning for Surface Vehicles using Turning Circle-based Control Barrier Functions
title_zh: 基于回转圆控制屏障函数的水面无人艇多模态轨迹规划
authors: Changyu Lee
date: 2026-08-20
pdf: "https://arxiv.org/pdf/2608.19537v1"
tags: ["query:q1"]
score: 9.0
evidence: 使用控制屏障函数的避障轨迹规划
tldr: 针对自主水面船舶在动态环境中的避障问题，本文提出一种基于回转圆控制屏障函数（TC-CBF）的多模态轨迹规划框架。该方法将模型预测控制与考虑船舶非圆约束及有限转向能力的TC-CBF相结合，通过识别左右转向避障模式，使优化器能在无全局引导路径的情况下探索不同拓扑的轨迹，有效解决了传统方法的局部最优和死锁问题，提升了复杂环境下的规划鲁棒性。
selection_source: fresh_fetch
motivation: 传统的避障方法主要依赖欧氏距离，忽略了船舶的非圆运动约束和有限转向能力，容易导致局部最优或死锁。
method: 提出一种基于回转圆的控制屏障函数（TC-CBF）并集成至MPC中，利用几何特性生成左转和右转两种避障模式进行多模态规划。
result: 在多船动态环境的仿真中，该方法在不同交通密度下均表现出更高的任务成功率和更低的安全违规率。
conclusion: 该框架在无需全局引导路径的情况下，通过嵌入避障方向约束，显著增强了自主船舶在动态环境中的避障性能和计算效率。
---

## 摘要
本文提出了一种用于动态环境下自主水面无人艇（ASV）的无引导路径多模态轨迹规划框架。该方法将模型预测控制（MPC）与基于回转圆的控制屏障函数（TC-CBF）相结合。与仅基于邻近度评估安全性的传统欧几里得距离控制屏障函数（ED-CBF）不同，TC-CBF 考虑了水面无人艇的非完整运动和有限的回转能力。其几何公式根据无人艇的回转圆识别可行的避障区域，并生成独特的左转和右转避障模式。这些模式允许优化求解器在不依赖全局规划引导路径的情况下，探索并选择拓扑结构不同的轨迹，而许多传统的多模态规划方法通常需要引导路径。通过将避障方向直接嵌入安全约束，该框架在保持计算效率的同时，缓解了单模态 MPC 的局部最小值和死锁问题。涉及多艘移动船只的大量仿真表明，在所有测试的交通密度下，该方法比单模态基准方法具有更高的成功率、更少的安全违规和更小的残余违规。

## Abstract
This paper presents a guide path-free multimodal trajectory planning framework for autonomous surface vehicles operating in dynamic environments. The proposed method integrates model predictive control (MPC) with a turning circle-based control barrier function (TC-CBF). Unlike conventional Euclidean distance-based CBFs (ED-CBFs), which evaluate safety solely based on proximity, the TC-CBF accounts for the nonholonomic motion and finite turning capability of a surface vehicle. Its geometric formulation identifies feasible avoidance regions according to the vehicle's turning circles and generates distinct left- and right-turning avoidance modes. These modes allow the optimization solver to explore and select topologically different trajectories without relying on globally planned guide paths, as required by many conventional multimodal planning approaches. By embedding the avoidance direction directly into the safety constraint, the proposed framework alleviates the local-minimum and deadlock problems of single-mode MPC while maintaining computational efficiency. Extensive simulations involving multiple moving vessels demonstrate that the proposed method achieves higher success rates, fewer safety violations, and smaller residual violations than single-mode baselines across all tested traffic densities.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种针对自主水面无人艇（ASV）在动态环境中进行避障的多模态轨迹规划框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在复杂的动态交通流中，为具有有限转向能力（非完整约束）的水面船舶生成既符合物理特性又具备拓扑多样性的避障轨迹。
*   **研究背景**：
    *   传统的避障方法（如基于欧氏距离的控制屏障函数 ED-CBF）仅考虑距离，忽略了船舶巨大的转弯半径和迟滞的转向响应。
    *   单模态优化器（如标准 MPC）容易陷入局部最优或死锁，无法在“左舷绕行”和“右舷绕行”等不同拓扑路径间灵活切换。
    *   现有的多模态规划通常依赖高层的采样或图搜索来生成引导路径，增加了计算负担且难以保证物理可行性。

### 2. 方法论
*   **核心思想**：提出一种**基于回转圆的控制屏障函数（TC-CBF）**，并将其直接嵌入 MPC 框架中。通过枚举障碍物的绕行侧（左或右），在无需外部引导路径的情况下生成多个候选轨迹。
*   **关键技术细节**：
    *   **TC-CBF 构造**：安全指标不再是船体间的距离，而是障碍物与船舶**瞬时回转圆中心**之间的距离。这直接将船舶的最小转弯半径 $R_{min}$ 引入约束。
    *   **多模态生成**：针对最近的 $M$ 艘船只，枚举所有可能的绕行组合（$K = 2^M$ 个模式）。每个模式对应一个结构相同但参数（避障方向 $\sigma$）不同的优化问题（OCP）。
    *   **分层障碍物处理**：将障碍物分为“分支集”（全枚举）和“警戒集”（基于方位固定绕行侧），在控制计算量的同时保证感知覆盖。
    *   **并行求解与选择**：利用 `acados` 求解器和 OpenMP 并行计算框架，同时求解所有模式。通过带有滞后机制的成本函数选择最优模式。
    *   **船舶模型**：采用一阶 Nomoto 模型捕捉船舶的航向滞后和速率限制。

### 3. 实验设计
*   **实验场景**：模拟了开阔水域的多船相遇场景，包括对头、交叉、追越以及存在静态障碍物的复杂工况。
*   **交通密度**：设置了三个等级：D1（8 动态 + 3 静态）、D2（12+4）、D3（16+5）。
*   **对比基准（Benchmark）**：
    1.  **ED-CBF (K=1)**：传统的基于欧氏距离的单模态 MPC。
    2.  **TC-CBF single (K=1)**：基于回转圆但使用启发式规则固定绕行侧的单模态方法。
    3.  **Proposed (K=16)**：本文提出的多模态框架（$M=4$）。
*   **评估指标**：任务成功率、安全区违规率、平均侵入深度、计算耗时。

### 4. 资源与算力
*   **硬件平台**：台式机，配备 **Intel Core Ultra 7 270K Plus CPU**（24 核，8 性能核 + 16 能效核），32 GB RAM。
*   **软件环境**：Ubuntu 24.04 (WSL2)，使用 `acados` 和 `HPIPM` 求解器栈，基于 OpenMP 实现并行化。
*   **算力表现**：求解 32 个模式的并行耗时仅为 **2.26 ms**，远低于 1 秒的重规划周期，证明了极高的实时性。

### 5. 实验数量与充分性
*   **实验规模**：针对每种交通密度进行了 **100 组配对蒙特卡洛模拟**（共 300 组对比实验），并提供了详细的统计分布（如侵入深度的箱线图）。
*   **充分性评价**：实验设计较为充分。通过密度梯度测试验证了算法的鲁棒性；通过配对实验排除了随机环境干扰；此外还增加了一个符合国际海上避碰规则（COLREGs）的扩展实验，验证了框架的可扩展性。

### 6. 主要结论与发现
*   **安全性显著提升**：在最高密度（D3）下，提议方法的成功率为 **86%**，远高于单模态基准（20% 和 65%）。
*   **违规程度减轻**：即使发生违规，提议方法的安全区侵入深度也远小于基准方法（中位数仅 19m，而基准方法高达 600m 左右）。
*   **计算效率极高**：得益于 OCP 结构的同构性，并行加速比达到了 7.1 倍，增加模态数量对总耗时的影响极小。
*   **决策优势**：多模态机制允许船舶在复杂集群中提前预判并选择全局最优的绕行拓扑，避免了单模态方法常见的死锁。

### 7. 优点
*   **物理一致性**：TC-CBF 巧妙地将船舶的机动性限制（转弯半径）转化为几何约束，生成的轨迹更具可执行性。
*   **无需引导路径**：摆脱了对高层路径规划器的依赖，简化了导航架构，减少了因高层路径不可行导致的规划失败。
*   **实时性强**：充分利用现代多核 CPU 的并行能力，适合嵌入式部署。

### 8. 不足与局限
*   **环境假设限制**：假设为开阔水域（Assumption A2），未考虑岸线、浅滩等复杂地理边界约束。
*   **预测模型单一**：对他船采用恒定速度预测（Assumption A1），未考虑他船可能的避障反应（非博弈型预测）。
*   **指数级增长风险**：虽然并行化缓解了压力，但模式数量随分支障碍物 $M$ 呈指数增长，在极端拥挤环境下的扩展性仍有上限。

（完）
