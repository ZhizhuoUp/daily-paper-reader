---
title: Model Predictive Supervisory Control for Hierarchical and Distributed UAS Traffic Management
title_zh: 用于分层与分布式无人机系统交通管理的模型预测监督控制
authors: "Matheus P. Loures, Guilherme V. Raffo, Patrícia N. Pena"
date: 2026-08-18
pdf: "https://arxiv.org/pdf/2608.18353v1"
tags: ["query:q2"]
score: 8.0
evidence: 用于安全和优化的模型预测监督控制
tldr: 本研究针对多智能体系统中的资源共享问题，提出了一种分层模型预测监督控制（MPSC）框架。该框架将模型预测控制的最优性与监督控制理论（SCT）的安全性、非阻塞性和资源排他性相结合。通过分层和可扩展的监督器模板，实现了无需整体合成的分布式执行，显著提升了系统的可扩展性。该方法成功应用于城市无人机交通管理（UTM），在动态需求下高效完成了取送货任务。
selection_source: fresh_fetch
motivation: 旨在解决城市无人机交通管理中多智能体协同及资源竞争带来的安全保障与系统扩展性难题。
method: 提出一种将模型预测控制与分层监督控制理论相结合的分布式控制框架，利用模板化设计实现高效计算。
result: 在模拟的城市无人机物流场景中，该框架成功实现了动态需求下的安全、非阻塞且高效的资源调度。
conclusion: 该分层MPSC框架为大规模多智能体系统的实时安全控制与交通管理提供了一种极具扩展性的解决方案。
---

## 摘要
本研究针对具有共享资源的多智能体系统，提出了一种分层模型预测监督控制（MPSC）框架。MPSC 将后退地平线成本最优控制与基于监督控制理论（SCT）的监督相结合，以强制执行安全性、非阻塞性和资源排他性。可扩展性源于分层且可扩展的监督器和自动机模板，从而实现了无需整体合成的分布式执行。利用该框架，本研究开发了一个城市无人机系统交通管理（UTM）模型。该模型能够高效地支持时变需求下的取送货任务。

## Abstract
This work proposes a hierarchical Model Predictive Supervisory Control (MPSC) framework for multi-agent systems with shared resources. MPSC integrates receding-horizon cost-optimal control with Supervisory control theory (SCT) based supervision that enforces safety, nonblockingness, and resource exclusivity. Scalability arises from hierarchical and scalable supervisor and automaton templates, enabling distributed execution without monolithic synthesis. Using this framework, this work develops an urban Unmanned aircraft system Traffic Management (UTM) model. The model supports pickup-and-delivery missions under time-varying demand efficiently.

---

## 论文详细总结（自动生成）

这篇论文提出了一种用于无人机系统交通管理（UTM）的分层模型预测监督控制（MPSC）框架，旨在解决城市低空空域中多无人机协同的安全性与计算扩展性难题。以下是对该论文的深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：随着无人机（UAV）在物流、巡检等领域的普及，城市低空空域变得拥挤。传统的监督控制理论（SCT）虽然能保证安全性（无碰撞、非阻塞），但在多智能体环境下会面临“状态爆炸”问题，导致计算不可行。
*   **研究动机**：现有的 UTM 系统需要同时满足形式化安全验证和实时路径/任务优化。作者试图寻找一种既能利用 SCT 提供安全保障，又能通过模型预测控制（MPC）实现成本最优，且具备大规模扩展能力的控制方案。

### 2. 方法论
该论文的核心是 **分层模型预测监督控制（MPSC）** 框架，主要包含以下关键技术：
*   **分层架构**：
    *   **UTM 层（全局监督）**：负责维护全局空域状态，强制执行互斥约束（如顶点占用、航道方向）和动态地理围栏。它通过“禁止事件集（$P$）”向无人机下达指令。
    *   **UAV 层（局部决策）**：每个无人机在满足 UTM 层限制的前提下，利用 MPSC 解决本地的后退地平线优化问题。
*   **可扩展性设计**：采用“模板化”思想，为同类无人机合成一个通用监督器模板，通过事件重标记（Relabeling）实例化到各个体，避免了整体合成时的组合爆炸。
*   **代数化建模与 MILP 求解**：
    *   将确定性有限自动机（DFA）编码为邻接矩阵 $A$、事件可达矩阵 $B$ 和事件使能矩阵 $C$。
    *   状态转移方程表示为：$x_{t+1} = (Ax_t) \odot (Bu_t)$。
    *   构建混合整数线性规划（MILP）模型，在预测时域 $H$ 内最小化运行成本（能量、时间等），同时最大化任务激励。

### 3. 实验设计
*   **实验场景**：
    *   **R1 场景**：小型规模，包含 9 个节点、15 条航道、2 架无人机。
    *   **R2 场景**：中型规模，包含 19 个节点、34 条航道、4 架无人机，并引入了双层空域结构。
*   **Benchmark（基准对比）**：
    *   对比了**分层设计**与**集中式精确合成（Centralized exact）**在监督器状态数、转换数及合成时间上的差异。
*   **实验平台**：基于 ROS 2 和 Gazebo 物理仿真环境，使用 Python.NET 调用 ULTRA DES .NET 库进行控制设计，并使用 Gurobi 求解器处理 MILP 优化。

### 4. 资源与算力
*   **软件工具**：ROS 2 (Humble/Foxy), Gazebo, Gurobi Optimizer, .NET 环境。
*   **算力说明**：文中**未明确提及**具体的硬件配置（如 CPU/GPU 型号及数量）。但从实验描述看，该算法主要依赖 CPU 进行逻辑推理和 MILP 求解，未涉及深度学习的大规模 GPU 训练。

### 5. 实验数量与充分性
*   **实验数量**：主要展示了两组代表性实验（R1 和 R2）。
*   **充分性评价**：
    *   **客观性**：通过量化指标（状态数减少约 780 倍，转换数减少约 2300 倍）有力证明了分层架构在缓解状态爆炸方面的优势。
    *   **局限性**：实验规模（最高 4 架无人机）虽然验证了逻辑可行性，但对于“大规模”无人机群（如数十甚至上百架）的实时性表现尚缺乏更深层次的压力测试。

### 6. 主要结论与发现
*   **计算效率显著提升**：分层 MPSC 极大地降低了监督器的复杂性，使实时在线优化成为可能。
*   **安全性与最优性兼顾**：SCT 确保了系统永远不会进入冲突或死锁状态，而 MPC 则在安全边界内实现了任务效率的最优。
*   **吞吐量增加**：在更复杂的 R2 场景中，通过多层空域管理，系统吞吐量从 1.29 提升至 2.46 任务/分钟。

### 7. 优点
*   **形式化保证**：相比纯启发式或强化学习方法，该方法能提供严格的非阻塞和安全性证明。
*   **解耦设计**：UTM 层与 UAV 层的解耦使得系统易于维护和扩展，单个无人机的加入不需要重新合成全局监督器。
*   **矩阵化创新**：将自动机理论与 Hadamard 积结合引入 MILP，为离散事件系统的预测控制提供了简洁的数学表达。

### 8. 不足与局限
*   **静态成本分配**：状态成本向量（能量、时间等）目前是离线预设的，缺乏根据实时环境动态调整成本的能力。
*   **通信依赖**：系统高度依赖 UTM 与 UAV 之间的实时通信（广播禁止事件集），若发生通信延迟或丢包，安全性保障机制可能面临挑战。
*   **电池策略简单**：虽然考虑了电池监控，但对于极端低电量下的紧急避障和任务抢占逻辑还需进一步细化。
*   **场景复杂度**：目前的仿真环境仍属于结构化空域，对于非结构化、存在随机障碍物的复杂环境适应性有待验证。

（完）
