---
title: A Theoretical Framework for Parallel Lifelong MAPF Using Group Decentralized Planning
title_zh: 一种基于分组去中心化规划的并行终身多智能体路径规划理论框架
authors: "Alex DeWeese, Jiaoyang Li, Guannan Qu"
date: 2026-08-18
pdf: "https://arxiv.org/pdf/2608.17928v1"
tags: ["query:q1"]
score: 8.0
evidence: 路径规划中的避障与多智能体碰撞解决
tldr: 针对终身多智能体路径规划（L-MAPF）中RHCR框架计算成本高的问题，本文基于局部相互依赖MDP理论证明了RHCR的近似最优性，并提出GD-RHCR框架。该框架通过传递通信机制对智能体进行空间分区并并行规划，在保持与RHCR相当的理论最优性保证的同时，显著降低了计算开销并提升了大规模场景下的吞吐量。
selection_source: fresh_fetch
motivation: 现有的高性能RHCR框架在处理大规模智能体群体时面临巨大的计算压力，限制了其实际应用。
method: 提出GD-RHCR框架，利用传递通信方案将智能体划分为不同组别，并对各组进行并行的去中心化路径规划。
result: 理论证明了GD-RHCR与RHCR具有相似的指数级接近最优的保证，且实验表明其在降低规划成本的同时实现了高吞吐量。
conclusion: 该研究建立了时间限制与空间分区之间的理论对等性，为大规模L-MAPF问题提供了一种高效且具有理论保障的并行化解决方案。
---

## 摘要
在终身多智能体路径规划（L-MAPF）问题中，智能体必须在避开障碍物和智能体间碰撞的同时，重复地从一个目的地移动到另一个目的地。滚动时域碰撞解决（RHCR）框架被广泛认为是该问题性能最高的解决方案之一。然而，与其高质量解相对应的是，它带来了巨大的计算开销，限制了其在即使是中等规模智能体数量下的适用性。在本文中，利用局部相互依赖多智能体 MDP 文献中的理论方法，我们首先在 L-MAPF 问题的折扣 MDP 公式中从理论上证明了 RHCR 的近优性。然后，我们利用这些结果自然地引出了一种名为分组去中心化 RHCR（GD-RHCR）的扩展框架，该框架结合了分组去中心化结构，根据传递通信方案对智能体进行划分，并为每个智能体分区进行并行规划。我们证明了 RHCR 和 GD-RHCR 都能实现类似的指数级接近最优的保证，在原始 RHCR 执行的基于时间的限制与 GD-RHCR 执行的额外基于空间的划分之间建立了理论上的对偶性。最后，我们证明了在不同的地图上，GD-RHCR 能够获得高吞吐量，并可扩展到更多的智能体数量，同时保持显著较低的单次规划成本。

## Abstract
In the Lifelong Multi-Agent Path Finding (L-MAPF) problem, agents must repeatedly move from one destination to another while avoiding obstacles and inter-agent collisions. Widely regarded as one of the highest-performing solutions to this problem is the Rolling-Horizon Collision Resolution (RHCR) framework. However, commensurate with its quality solutions, it incurs a computational cost that limits its applicability to even modest agent counts. In this paper, leveraging theoretical methods from the Locally Interdependent Multi-Agent MDP literature, we first theoretically prove the near-optimality of RHCR in a discounted MDP formulation of the L-MAPF problem. Then, we leverage these results to naturally motivate an extended framework called Group Decentralized RHCR (GD-RHCR) which incorporates a group decentralized structure that partitions agents based on a transitive communication scheme and plans for each partition of agents in parallel. We show that both RHCR and GD-RHCR achieve similar exponentially close to optimal guarantees, establishing a theoretical duality between the time based restrictions performed by vanilla RHCR and the additional space based partitioning performed by GD-RHCR. Lastly, we show that across varying maps, GD-RHCR is able to attain high throughput that scales into higher agent counts while maintaining a significantly lower per plan cost.

---

## 论文详细总结（自动生成）

以下是对论文《A Theoretical Framework for Parallel Lifelong MAPF Using Group Decentralized Planning》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：终身多智能体路径规划（L-MAPF）要求大量智能体在动态任务分配下持续移动且不发生碰撞。目前高性能的**滚动时域碰撞解决（RHCR）**框架虽然吞吐量高，但计算成本随智能体数量呈指数级增长，难以实时应用于大规模场景。
*   **研究背景**：现有的快速方法（如 PIBT）虽然扩展性强但过于贪婪且易陷入死锁；而 RHCR 依赖的单次 MAPF 求解器（如 PBS、CBS）在智能体密集时计算压力巨大。
*   **整体含义**：本文旨在建立一个理论框架，证明 RHCR 的近优性，并提出一种**分组去中心化 RHCR（GD-RHCR）**，通过空间分区和并行规划，在保持高吞吐量的同时大幅降低计算开销。

### 2. 方法论：核心思想与关键技术
*   **理论建模**：将 L-MAPF 建模为折扣奖励的**局部相互依赖多智能体 MDP（LI-MDP）**。证明了 RHCR 的性能随规划时域 $H$ 的增加呈指数级趋近于最优解。
*   **核心思想（GD-RHCR）**：利用空间距离和可见性半径 $V$ 将智能体划分为若干独立的“连通分量”（组）。组内进行协同规划，组间并行执行。
*   **关键技术细节**：
    *   **分组去中心化**：基于传递通信方案，距离在 $V$ 以内的智能体归为一组，每组独立并行求解。
    *   **延迟评估（Lazy Evaluation）**：仅在组内成员发生变化（新智能体加入）或原计划耗尽时才重新触发规划，动态调整重规划窗口 $\alpha$。
    *   **软约束（Soft Constraints）**：在规划当前组时，将其他组已有的计划视为“软约束”，通过惩罚项 $c_{soft}$ 减少跨组碰撞风险。
    *   **二级求解器（Secondary Solver）**：当某组规模超过阈值 $K_{th}$ 时，自动降级使用极速的 PIBT 求解器，防止局部拥堵导致整体计算超时。

### 3. 实验设计
*   **实验场景**：
    *   **MAPD（拾取与交付）**：包括 warehouse（仓库）和 sortation（分拣）地图。
    *   **随机导航 L-MAPF**：包括 random、room（房间）和 empty（空旷）地图。
*   **基准方法（Benchmarks）**：
    *   **PIBT**：极速贪婪基准。
    *   **RHCR**：高性能但高耗时基准（带 PIBT 备选方案）。
*   **对比指标**：吞吐量（单位时间内完成的任务数）、平均规划耗时、总墙钟时间（Wall Time）。

### 4. 资源与算力
*   **硬件环境**：实验在配备 **13th Gen Intel Core i9-13900HX** 处理器的设备上运行。
*   **并行设置**：使用了 30 个随机种子，其中每 3 个实验并行运行。
*   **算力说明**：论文未提及 GPU 使用情况（MAPF 算法通常主要消耗 CPU 算力进行搜索），重点强调了通过并行化实现的计算加速。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 10 种不同拓扑结构的地图，智能体数量从低密度扩展至高密度（最高达 1000 个智能体）。
*   **消融实验**：针对可见性半径 $V$、软约束成本 $c_{soft}$ 和回退阈值 $K_{th}$ 进行了专门的消融分析。
*   **充分性评价**：实验设计较为充分，不仅对比了吞吐量，还深入分析了地图拓扑（如连通性）对分组效率的影响，结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **理论对偶性**：证明了 RHCR 的“时间限制”与 GD-RHCR 的“空间分区”在理论保证上具有对偶性，两者均能达到指数级近优。
*   **性能提升**：在保持与 RHCR 相当的吞吐量时，GD-RHCR 的平均规划时间缩短了约 **24.9倍**。
*   **大规模扩展性**：在 RHCR 因计算超时而崩溃的高密度场景下，GD-RHCR 仍能运行，且吞吐量比 PIBT 提升了高达 **57.7%**。
*   **拓扑敏感性**：地图的障碍物密度和拓扑结构显著影响分组效果，在障碍物密集的仓库地图中优势最明显。

### 7. 优点
*   **理论与实践结合**：不仅提出了新算法，还填补了 RHCR 在 MDP 理论框架下的近优性证明空白。
*   **优雅的降级机制**：通过二级求解器处理超大分组，使算法在极端拥堵下表现得比原始 RHCR 更稳健。
*   **高度模块化**：框架允许为不同的小组分配不同的求解器，具有极强的灵活性。

### 8. 不足与局限
*   **死锁风险**：在非双连通（non-biconnected）地图中，若过度依赖二级求解器（PIBT），仍可能面临死锁问题。
*   **通信假设**：依赖于智能体间的传递通信来形成分组，在通信受限的极端现实环境下可能受阻。
*   **参数敏感性**：软约束 $c_{soft}$ 和可见性 $V$ 的选择对性能有较大影响，需针对不同地图进行调优。

（完）
