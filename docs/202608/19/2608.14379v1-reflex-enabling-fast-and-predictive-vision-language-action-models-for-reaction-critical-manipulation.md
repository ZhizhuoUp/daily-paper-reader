---
title: "Reflex: Enabling Fast and Predictive Vision-Language-Action Models for Reaction-Critical Manipulation"
title_zh: Reflex：为反应关键型操作实现快速且具预测性的视觉-语言-动作模型
authors: "Yuxuan Chen, Wanruo Zhang, Xiao Li"
date: 2026-08-14
pdf: "https://arxiv.org/pdf/2608.14379v1"
tags: ["query:profile-1"]
score: 8.0
evidence: 反应关键型操作的实时控制
tldr: 针对现有VLA模型在动态交互任务中的不足，本文提出了ReflexBench基准测试，专门评估反应关键型操作。同时开发了ReflexVLA模型，通过潜在未来预测和多帧时间融合增强时间推理能力，并利用CUDA Graph等技术降低推理延迟。实验证明该模型在动态任务上表现优异，且在静态任务中保持竞争力，有效提升了机器人在复杂动态环境下的反应能力。
selection_source: fresh_fetch
motivation: 现有的VLA模型主要关注静态任务的泛化，忽视了对反应速度和时间推理要求极高的动态交互场景。
method: 提出了ReflexBench基准和ReflexVLA模型，通过潜在未来预测、多帧时间融合以及CUDA Graph加速技术优化动态操作。
result: ReflexVLA在动态操作任务上显著提升了性能，同时在标准静态基准测试和真实世界部署中均表现出极强的竞争力。
conclusion: 该研究通过结合时间建模与推理加速，成功解决了VLA模型在反应关键型机器人操作中的性能瓶颈。
---

## 摘要
视觉-语言-动作（VLA）模型近期在机器人操作领域取得了令人鼓舞的性能。然而，现有的基准测试主要评估静态操作任务的泛化能力，在很大程度上忽视了动态交互场景。为了填补这一空白，我们提出了 ReflexBench，这是一个针对反应关键型操作的基准测试。ReflexBench 包含六个动态任务，并引入了一个评估框架，该框架将模拟器步进与机器人控制解耦，同时支持同步和异步推理下的可配置延迟。基于 ReflexBench，我们提出了 ReflexVLA，这是一种高效的 VLA 模型，专为反应关键型操作设计，无需大规模机器人数据预训练。ReflexVLA 通过视觉骨干网络中的潜在未来预测和多帧时间融合增强了时间推理能力，同时通过批量视觉编码和 CUDA Graph 重放降低了部署延迟。实验表明，ReflexVLA 在保持标准静态操作基准测试中具有竞争力的准确性的同时，一致地提升了动态操作性能，真实世界实验进一步证明了其在实际部署条件下的有效性。项目网站：https://reflexvla.github.io

## Abstract
Vision-Language-Action (VLA) models have recently achieved promising performance in robotic manipulation. However, existing benchmarks mainly evaluate generalization on static manipulation tasks and largely overlook dynamic interaction scenarios. To address this gap, we present ReflexBench, a benchmark for reaction-critical manipulation. ReflexBench contains six dynamic tasks and introduces an evaluation framework that decouples simulator stepping from robot control while supporting configurable latency under synchronous and asynchronous inference. Building upon ReflexBench, we propose ReflexVLA, an efficient VLA model designed for reaction-critical manipulation without large-scale robot-data pretraining. ReflexVLA enhances temporal reasoning through latent future prediction and multi-frame temporal fusion within the vision backbone, while reducing deployment latency through batched visual encoding and CUDA Graph replay. Experiments show that ReflexVLA consistently improves dynamic manipulation performance while maintaining competitive accuracy on standard static manipulation benchmarks, and real-world experiments further demonstrate its effectiveness under practical deployment conditions. Project website: https://reflexvla.github.io

---

## 论文详细总结（自动生成）

以下是对论文《Reflex: Enabling Fast and Predictive Vision-Language-Action Models for Reaction-Critical Manipulation》的结构化总结：

### 1. 研究动机与核心问题
*   **核心问题**：现有的视觉-语言-动作（VLA）模型虽然在静态操作任务中表现出色，但在需要**快速反应**和**预测未来状态**的动态交互场景（如拦截移动物体）中面临巨大挑战。
*   **研究动机**：
    *   **延迟瓶颈**：现有模型推理延迟高，导致感知与执行之间存在时间差。
    *   **基准缺失**：大多数仿真基准在推理时会暂停环境，无法真实反映现实世界中的执行延迟。
    *   **缺乏预见性**：现有模型多基于当前观察，缺乏对环境动态演变的推理能力。

### 2. 方法论
论文提出了两个核心贡献：**ReflexBench**（基准测试）和 **ReflexVLA**（模型框架）。

*   **ReflexBench（延迟感知基准）**：
    *   包含6个动态任务：传送带抓取、接球、打地鼠、拦截滚球、投篮、旋转插轴。
    *   **解耦仿真与控制**：模拟器步进不因推理而停止，支持同步和异步推理模式，并能根据真实硬件延迟调整仿真中的“阻塞窗口”。
*   **ReflexVLA（模型架构）**：
    *   **潜在未来预测（Latent Future Prediction）**：在训练中引入辅助任务，使用学习到的未来标记（Future Tokens）去预测未来帧在语义空间（Frozen DINOv3）中的特征，增强模型对动态演变的预见性。
    *   **多帧时间融合（Multi-frame Temporal Fusion）**：在视觉骨干网络的中间层进行时间融合，利用因果注意力机制处理历史帧，既保留了运动线索，又避免了在语言模型端增加过多的Token负担。
    *   **推理延迟优化**：
        *   **批量视觉编码**：将多视角、多帧图像打包成一个Batch一次性通过视觉编码器。
        *   **CUDA Graph 重放**：将整个推理计算图捕获为CUDA Graph，消除内核调度和运行时分发的开销。

### 3. 实验设计
*   **数据集/场景**：
    *   **仿真环境**：ReflexBench（6个动态任务）和 LIBERO（标准静态操作基准）。
    *   **真实世界**：使用 AgileX Piper 机械臂，执行传送带抓取、按按钮、接球三个任务。
*   **对比方法（Baselines）**：
    *   大型模型：OpenVLA-OFT (7B), $\pi_{0.5}$ (4B)。
    *   动态优化模型：PUMA (4B), DynamicVLA (0.5B)。
    *   轻量级模型：SmolVLA (0.5B), VLA-Adapter (1B, 本文基准模型)。

### 4. 资源与算力
*   **硬件环境**：评估在单张 **NVIDIA RTX 5880 Ada GPU** 上进行。
*   **训练细节**：每个任务使用200个演示片段进行训练。ReflexVLA 采用 1B 参数规模。
*   **算力说明**：文中未明确给出总训练时长，但强调了其模型规模较小（1B），且通过系统级优化（CUDA Graph）显著降低了推理延迟（从约125ms降至65ms）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 ReflexBench 的 6 个任务上进行了详尽对比，每个任务评估 150 个回合，并报告了三次运行的均值和标准差。
    *   在 LIBERO 静态基准上进行了泛化能力测试。
    *   进行了**消融实验**，分别验证了未来预测、时间融合策略（中间层 vs 最终层）以及延迟优化的贡献。
*   **充分性评价**：实验设计较为全面，涵盖了从仿真到现实、从动态到静态、从算法组件到系统优化的多维度验证，对比对象包含了当前主流的开源 VLA 模型，具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **动态任务优势**：ReflexVLA 在 ReflexBench 上的平均成功率达到 50.4%，显著优于基准模型 VLA-Adapter (30.3%)，甚至超过了参数量更大的 PUMA (50.2%) 和 OpenVLA (36.0%)。
*   **静态任务保持**：在 LIBERO 静态任务中，ReflexVLA 保持了 97.2% 的高成功率，证明增强动态能力并未损害其基础操作能力。
*   **延迟的重要性**：实验发现，高频异步推理配合较小的执行步长（Action Horizon）在动态任务中表现最佳；推理延迟的降低直接提升了任务成功率。

### 7. 优点
*   **系统级视角**：不仅关注模型架构，还通过 CUDA Graph 等底层优化解决机器人领域核心的延迟问题。
*   **高效的时间建模**：在视觉中间层融合特征，既捕捉了运动信息，又维持了推理效率。
*   **基准创新**：ReflexBench 填补了 VLA 领域缺乏“延迟感知”动态评估工具的空白。

### 8. 不足与局限
*   **预训练缺失**：ReflexVLA 的改进主要在微调阶段引入，尚未在大规模机器人数据预训练中验证其潜力。
*   **推理模式有限**：目前仅探讨了标准的同步和异步推理，未涉及更先进的实时控制机制（如 RTC）。
*   **任务复杂度**：虽然涉及动态任务，但环境复杂度（如背景干扰、多物体交互）仍有提升空间。

（完）
