---
title: Arm-Aware Guided Dexterous Grasp Generation with Arm-Agnostic Grasp Models
title_zh: 基于机械臂无关抓取模型的机械臂感知引导式灵巧抓取生成
authors: "Yongyi Jia, Yongpeng Jiang, Kangchen Lv, Yi Ren, Mingrui Yu, Xiang Li"
date: 2026-08-17
pdf: "https://arxiv.org/pdf/2608.16351v1"
tags: ["query:q1"]
score: 9.0
evidence: 抓取生成中的机械臂环境避障
tldr: 针对灵巧手抓取中忽略机械臂约束的问题，本文提出一种机械臂感知的引导式生成框架。该方法利用预训练的通用抓取模型，在推理阶段通过联合优化手部姿态和机械臂构型，并结合闭式梯度引导扩散模型采样，实现了在复杂约束环境下（如避障、工作空间限制）的高效抓取生成，显著提升了真实场景下的抓取可行性。
selection_source: fresh_fetch
motivation: 现有的灵巧手抓取模型多关注浮动手部姿态，忽略了机械臂碰撞和工作空间限制，且现有机械臂感知方法采样效率低或泛化性差。
method: 将机械臂感知的抓取生成建模为手部姿态与机械臂构型的联合优化问题，并利用闭式梯度引导扩散模型进行推理阶段的约束求解。
result: "在包含10,000个物体和6种复杂场景的评估中，该框架在高度受限的环境下生成可行抓取的概率显著提高。"
conclusion: 该研究证明了通过在推理阶段引入机械臂约束引导，可以使通用的灵巧手抓取模型在复杂现实任务中具备更强的实用性和鲁棒性。
---

## 摘要
考虑机械臂相关约束的灵巧抓取生成在涉及机械臂与环境避障、工作空间边界抓取以及连续抓取的现实场景中至关重要。现有的以手部为中心的抓取模型主要关注浮动手部的姿态，不足以应对这些情况。传统的机械臂感知方法要么依赖拒绝采样来剔除不可行样本，要么需要在特定机械臂数据上进行重新训练，这导致在恶劣条件下的采样效率低下，或在不同机器人和环境中的泛化能力有限。为了克服这些局限性，本研究提出了一种机械臂感知的灵巧抓取生成框架，该框架利用预训练的机械臂无关抓取模型，且仅在推理阶段整合机械臂和环境信息。具体而言，我们将机械臂感知的受限抓取生成建模为手部姿态和机械臂构型的联合优化，并推导了机械臂相关约束的闭式梯度。假设手部姿态分布由扩散模型表示，我们证明了基于梯度的优化等同于引导式扩散采样，从而将接近可行的样本引导至可行区域。通过对 6 个场景中 1 万个物体的全面评估，我们证明了所提框架在高度受限的设置中能以显著更高的概率生成可行抓取，突显了其在实际应用中的优势。补充材料和附录可在 https://arm-aware-dexgrasp.github.io/ 获取。

## Abstract
Dexterous grasp generation that considers arm-related constraints is crucial in real-world scenarios involving arm environment collision avoidance, workspace boundary grasps, and consecutive grasping. Existing hand-centric grasp models, which primarily focus on the floating hand's pose, are insufficient for such cases. Conventional arm-aware methods either rely on rejection sampling to discard infeasible samples or require retraining on arm-specific data, leading to low sample efficiency under adverse conditions or limited generalization across different robots and environments. To overcome these limitations, this letter presents an arm-aware dexterous grasp generation framework that leverages pretrained arm-agnostic grasp models while integrating arm and environmental information only at inference time. Specifically, we formulate arm-aware constrained grasp generation as a joint optimization of hand pose and arm configuration, and derive closed-form gradients for arm-related constraints. Assuming the hand pose distribution is represented by a diffusion model, we prove that gradient-based optimization is equivalent to guided diffusion sampling, steering near-feasible samples toward the feasible region. Through comprehensive evaluation involving 10k objects across 6 scenarios, we demonstrate that the proposed framework generates feasible grasps in highly constrained settings with significantly higher probability, highlighting its advantages in real-world applications. Supplementary materials and appendix are available at https://arm-aware-dexgrasp.github.io/.

---

## 论文详细总结（自动生成）

以下是对论文《Arm-Aware Guided Dexterous Grasp Generation with Arm-Agnostic Grasp Models》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的灵巧抓取生成模型大多是“以手为中心”（Hand-centric）的，即只考虑灵巧手相对于物体的姿态，而忽略了机械臂的运动学约束（如工作空间限制）和环境障碍物（碰撞风险）。
*   **研究动机**：在现实场景中，机械臂必须避开障碍物，且抓取点必须在机械臂的可达范围内。现有的解决方法要么效率极低（如“拒绝采样”，即生成大量手部姿态后剔除不可行的），要么泛化性差（如针对特定机械臂和环境重新训练模型）。
*   **整体含义**：本文旨在提出一种框架，使预训练的、与机械臂无关的通用抓取模型能够在推理阶段（Inference-time）通过“引导”机制，自动适应特定的机械臂约束和复杂环境，而无需任何重新训练。

### 2. 方法论
*   **核心思想**：将机械臂感知的抓取生成建模为**手部姿态（Wrist Pose）与机械臂构型（Arm Configuration）的联合优化问题**。利用扩散模型（Diffusion Model）的数学特性，在去噪采样过程中引入约束梯度。
*   **关键技术细节**：
    *   **双层优化架构**：外层优化调整扩散模型的采样分布，使其靠近可行区域；内层优化在给定手部姿态下寻找最优的机械臂关节角。
    *   **闭式梯度引导**：推导了三种关键约束的解析梯度：
        1.  **手部可达性（Reachability）**：通过逆运动学（IK）残差引导手部进入机械臂工作空间。
        2.  **环境避障（Collision Avoidance）**：利用环境的符号距离场（SDF）和机械臂的球体碰撞模型计算梯度，推开碰撞部位。
        3.  **关节邻近性（Joint Proximity）**：惩罚与当前关节位置的偏差，实现平滑的连续抓取。
    *   **算法流程**：从高斯噪声开始，在每一步 DDIM 去噪迭代中，计算上述约束的综合梯度，并利用该梯度修正当前采样值，将“接近可行”的样本拉向“完全可行”区域。

### 3. 实验设计
*   **数据集与场景**：
    *   使用 **DexGraspNet (DGN)** 数据集，包含 10,892 个物体。
    *   设计了 6 种挑战性场景：狭窄走廊（S1）、房间角落（S2）、货架顶部（S3）、隔板（S4）、工作空间边界以及连续抓取场景。
*   **Benchmark 与对比方法**：
    *   **Baseline (RS)**：拒绝采样（Rejection Sampling），即直接从预训练模型采样并过滤。
    *   **PD (Projected Denoising)**：将去噪方向投影到线性化可行域的方法。
    *   **PSR (Post-Sampling Refinement)**：采样后再进行局部优化的方法。
*   **评估指标**：可行率（FR）、抓取成功率（GSR）、总成功率（SR）和物体覆盖成功率（OSR）。

### 4. 资源与算力
*   **硬件环境**：实验在一台单显卡 **NVIDIA RTX 3090 Ti** 上完成。
*   **效率表现**：
    *   推理时间：每个成功抓取的平均推理时间约为 **0.5 - 2 毫秒**（取决于 Batch Size）。
    *   训练时长：文中未详细说明预训练时长，但强调了其方法在推理阶段是**零重训（Zero-retraining）**的。

### 5. 实验数量与充分性
*   **实验规模**：针对 10,892 个物体，每个物体采样 10 次，总计进行了约 **10 万次** 抓取实验。
*   **充分性与客观性**：
    *   **跨平台验证**：在 UR5 和 Franka 两种不同运动学特性的机械臂上进行了验证。
    *   **消融实验**：详细分析了约束权重 ($\lambda_c$)、学习率 ($\eta_\lambda$)、去噪步数 ($T_N$) 以及不同约束项对结果的影响。
    *   **真实世界测试**：在 UR5 机械臂 + LEAP Hand 灵巧手上进行了实物验证，涵盖了 8 种日常物体和多种复杂任务（如深箱抓取、双臂协作）。实验设计全面且具有说服力。

### 6. 主要结论与发现
*   **显著提升可行性**：在高度受限的环境中，该方法生成可行抓取的概率（FR）远高于拒绝采样（例如在 S1 场景中从 15% 提升至 88%）。
*   **保持抓取质量**：虽然引入引导会轻微扰动原始分布（导致 GSR 略微下降），但由于可行样本基数大幅增加，最终的总成功率（SR）和物体覆盖率（OSR）显著优于所有基准方法。
*   **高效性**：相比于 PD 等方法，该方法通过解析梯度和 IK 选择策略，在更短的时间内实现了更高的成功率。

### 7. 优点
*   **即插即用**：无需针对特定机器人或环境重新训练，具有极强的通用性。
*   **数学严谨**：推导了闭式的机械臂约束梯度，解决了关节空间约束与笛卡尔空间去噪之间的映射难题。
*   **实用性强**：推理速度极快，能够处理实时性要求较高的动态环境或复杂避障任务。

### 8. 不足与局限
*   **局部优化风险**：引导机制本质上是局部优化，如果初始采样点距离可行域过远，可能无法成功引导至可行解。
*   **质量权衡**：强力的约束引导可能会迫使手部姿态偏离最优抓取点，导致抓取稳定性下降（虽然文中提出了零空间投影作为缓解方案）。
*   **环境依赖**：依赖于准确的环境 SDF 模型，对于视觉感知误差较大的场景，避障效果可能受限。
*   **开环执行**：实物实验中提到，由于是开环执行，忽略了物体质量分布不均或摩擦力不足导致的滑动失败。

（完）
