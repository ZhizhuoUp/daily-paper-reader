---
title: Fast Coordinated Bimanual Motion Planning With Hard Constraints
title_zh: 具有硬约束的高效协同双臂运动规划
authors: "Borna Paro, Luka Petrović, Ivan Marković"
date: 2026-08-21
pdf: "https://arxiv.org/pdf/2608.20946v1"
tags: ["query:profile-1"]
score: 9.0
evidence: 带非线性约束的快速双臂运动规划
tldr: 针对双臂协同操作中处理刚性物体时面临的高自由度和非线性硬约束挑战，本文提出了一种快速运动规划框架。该方法采用领航者-跟随者参数化策略，将领航者位姿设为自由变量，通过逆运动学实时计算跟随者位姿，确保在整个路径上连续满足变换约束。实验表明，该方法比现有技术快19.4倍，并在真实双臂机器人平台上成功验证了其有效性。
selection_source: fresh_fetch
motivation: 双臂搬运刚性物体时，两端执行器间的固定变换约束使得可行配置空间极小，传统规划算法难以高效处理。
method: 提出一种领航者-跟随者参数化方法，通过逆运动学求解跟随者位姿，从而在整个运动路径中连续强制执行硬性变换约束。
result: 在多种仿真环境和真实机器人实验中，该方法实现了比前人工作快19.4倍的规划速度，并保证了约束的连续满足。
conclusion: 该研究为双臂协同操作提供了一种高效且能保证硬约束的规划方案，具有极强的实用性和实时性。
---

## 摘要
双臂操作能够实现复杂的任务，但由于涉及的高自由度而增加了复杂性。在处理刚性物体时，两个末端执行器之间的相对变换在整个运动过程中必须保持固定，这表现为一个非线性等式约束，将可行配置空间限制在测度为零的流形上，并对传统的运动规划器提出了挑战。我们提出了一种高效的双臂运动规划流水线，该流水线利用主从参数化方法在整个路径上连续强制执行此硬变换约束：主臂的配置被视为自由变量，而从臂的配置则通过逆运动学确定以满足约束。我们在多种环境、约束和双臂平台上对该方法进行了广泛的仿真评估，在保证连续约束满足的同时，规划速度比之前的工作快了 19.4 倍。在 Kinova Gen3 双臂装置上进行的真实世界实验（包括托盘运输和长条形物体操作）验证了规划轨迹向物理硬件的直接迁移。

## Abstract
Bimanual manipulation enables complex tasks but introduces added complexity from the high number of degrees of freedom involved. When handling rigid objects, the relative transformation between the two end effectors must remain fixed throughout the motion, manifesting as a nonlinear equality constraint that confines the feasible configuration space to a measure-zero manifold and challenges conventional motion planners. We propose a fast bimanual motion planning pipeline that enforces this hard transformation constraint continuously along the entire path, using a leader-follower parameterization: the leader's configuration is treated as a free variable, while the follower's is determined via inverse kinematics to satisfy the constraint. We extensively evaluate the method in simulation across diverse environments, constraints and bimanual platforms, achieving 19.4x faster planning than prior work while guaranteeing continuous constraint satisfaction. Real-world experiments on a bimanual Kinova Gen3 setup, involving tray transport and elongated-object manipulation, validate direct transfer of planned trajectories to physical hardware.

---

## 论文详细总结（自动生成）

这篇论文《Fast Coordinated Bimanual Motion Planning With Hard Constraints》提出了一种针对双臂机器人协同操作的高效运动规划框架。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：双臂机器人在共同搬运刚性物体时，两个末端执行器之间必须保持固定的相对位姿。这在数学上表现为**非线性等式约束**，使得有效的配置空间（C-space）缩减为一个测度为零的流形。
*   **研究动机**：传统的采样规划器很难在如此狭窄的流形中随机采样到有效状态；而现有的投影法或优化法往往计算量大，或者依赖于特定机器人的解析逆运动学（IK）解，且通常需要耗时的离线预计算，难以适应动态环境。

### 2. 论文提出的方法论
*   **核心思想**：采用**领航者-跟随者（Leader-Follower）参数化策略**。将双臂系统解耦，主臂（Leader）在配置空间内自由采样，从臂（Follower）的配置则通过逆运动学（IK）根据主臂位姿和预设的变换约束实时计算得出。
*   **关键技术细节**：
    *   **流形参数化**：通过主臂配置 $q_L$ 直接映射到约束流形 $M$ 上，避免了盲目采样后的投影过程。
    *   **约束感知状态插值**：在两个有效状态之间进行路径搜索时，不使用简单的线性插值，而是采用“预测-校正”方案。在每一步插值中，先线性移动主臂，再通过 IK 修正从臂，确保整条路径上的每一个点都严格满足硬约束。
    *   **树的稠密化**：在进行运动合法性检查时，保留并重用中间计算出的 IK 解，将其作为新的节点插入规划树中，从而加速搜索。
    *   **不连续性修正**：针对 IK 可能产生的关节空间跳变（支路切换），通过交替主从角色或增加插值步长进行后处理修正。

### 3. 实验设计
*   **实验场景**：
    1.  **仿真环境**：设置了无障碍、稀疏障碍和高密度障碍三种环境。
    2.  **约束类型**：测试了三种典型约束（T1：平行搬运；T2：相对搬运；T3：长条物体搬运）。
*   **对比基准（Benchmark）**：
    *   **SOTA 对比**：在 KUKA iiwa 平台上对比了 Cohn 等人提出的 IK-BiRRT、IK-PRM 和 IK-GCS。
    *   **消融/基准对比**：在 Kinova Gen3 和 UR5 平台上对比了一个“朴素基准”（先规划主臂路径，后处理计算从臂 IK）。
*   **测试平台**：KUKA iiwa (7-DoF)、Kinova Gen3 (7-DoF)、UR5 (6-DoF)。

### 4. 资源与算力
*   **硬件环境**：所有实验均在 **Intel Core i7-11800H CPU** 上运行。
*   **算力特点**：该方法**不需要 GPU 加速**，也不需要任何离线预计算（相比之下，对比方法 IK-GCS 需要约 5.1 小时的预处理）。
*   **运行时间**：规划时间通常在几百毫秒到几秒之间，比同类方法快一个数量级。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 KUKA 平台上进行了 10 次重复实验以统计平均值。
    *   在 Kinova 和 UR5 平台上，无障碍环境进行 100 次随机查询，有障碍环境各进行 30 次查询。
*   **充分性评价**：实验覆盖了不同自由度（6-DoF 和 7-DoF）的机器人、多种障碍物密度以及多种约束类型，并包含了真实世界的物理实验（托盘运水、长条物体搬运）。实验设计较为全面，对比了成功率（PSR）、规划时间和路径长度，结果具有客观性。

### 6. 论文的主要结论与发现
*   **性能飞跃**：在 KUKA 平台上，该方法的规划速度比之前的 SOTA 方法（IK-BiRRT）快 **19.4 倍**。
*   **高精度**：由于直接在参数化流形上规划，约束违反量保持在数值精度级别（$10^{-17}$ cm），远优于现有方法。
*   **鲁棒性**：在复杂障碍物环境下，该方法的成功率显著高于朴素基准法，证明了在规划过程中实时考虑约束的必要性。
*   **易用性**：该流水线是“机器人无关”的，可以快速部署到不同的机械臂上，无需解析 IK 公式。

### 7. 优点
*   **高效性**：通过重用中间 IK 解和高效的参数化方法，极大地缩短了规划时间。
*   **连续性保证**：不仅在采样点满足约束，通过约束感知插值保证了整条轨迹的连续约束满足。
*   **无需预计算**：能够直接应用于未知的、动态变化的环境。
*   **开源贡献**：作者承诺开源代码，有助于社区复现和应用。

### 8. 不足与局限
*   **IK 依赖性**：主要的计算瓶颈在于数值 IK 求解器。如果机器人处于奇异位姿，IK 求解可能变慢或失败。
*   **C 空间跳变**：虽然有后处理修正，但在某些极端情况下，IK 支路的切换仍可能导致关节空间的剧烈运动，难以完全消除。
*   **主从角色选择**：在非对称任务中，主臂的选择（谁作为 Leader）会影响规划效率，目前主要靠人工指定。

（完）
