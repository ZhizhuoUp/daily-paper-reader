---
title: Homotopy-Aware Corridor Generation without Predefined Reference Paths
title_zh: 无需预定义参考路径的同伦感知走廊生成
authors: "Haoze Dong, Minghan Li, Meng Guo, Zhongkui Li"
date: 2026-07-31
pdf: "https://arxiv.org/pdf/2607.29513v1"
tags: ["query:q1"]
score: 8.0
evidence: 用于无碰撞机器人运动规划的走廊生成
tldr: 本研究针对机器人运动规划中安全走廊生成依赖预定义参考路径的问题，提出了一种基于凸集图（GCS）的无参考路径走廊生成框架。该框架直接将走廊构建为凸集序列，通过扩展基于可见性的变形理论实现同伦感知的走廊融合与探索。此外，引入自适应多尺度GCS结构，结合采样细尺度图与可见性粗尺度图，支持环境不确定下的增量更新。实验证明该方法在轨迹质量和计算效率上优于现有基准，并在多种机器人硬件上验证了其鲁棒性。
selection_source: fresh_fetch
motivation: 传统安全走廊生成方法过度依赖预定义参考路径，限制了对不同同伦类路径的探索并导致几何偏差。
method: 提出一种基于凸集图的无参考路径框架，利用多尺度图结构和扩展的可见性变形理论进行同伦感知的走廊生成与动态更新。
result: 数值实验和硬件测试表明，该方法能高效生成更短的同伦感知轨迹，并支持在未知环境下的实时局部更新。
conclusion: 该框架摆脱了对参考路径的依赖，通过多尺度拓扑一致性显著提升了机器人在复杂动态环境中的规划灵活性和效率。
---

## 摘要
生成安全走廊对于无碰撞机器人运动规划至关重要，然而大多数现有方法依赖于预定义的参考路径，这会使走廊几何形状产生偏差，并隐式限制了可探索的同伦类。我们提出了一种基于凸集图（GCS）的无需参考路径的走廊生成框架，该框架直接将走廊构建为凸集序列，使走廊结构能够从自由空间表示中产生，而非源自引导路径。为了推断走廊之间的相似性，我们将基于可见性的变形从路径扩展到凸集序列，从而能够融合拓扑冗余的走廊，同时保留不同的备选方案。为了克服现有基于静态全局分解的 GCS 方法适应性有限的问题，我们进一步开发了一种自适应多尺度 GCS，其中基于采样的细尺度图支持局部更新，而基于可见性的粗尺度图实现紧凑的全局探索。这两个层级保持拓扑一致性，允许在环境不确定性下进行增量更新，而无需重新构建整个图。数值实验表征了 GCS 构建、走廊生成、同伦感知探索和局部更新，结果显示其具有高效的图构建能力、稳定的轨迹级性能，并且比现有基准方法生成的同伦感知轨迹耗时更短。在地面和空中机器人上进行的硬件实验（包括使用机载定位的部署）进一步验证了该框架在平移障碍物和先前未知障碍物环境下的有效性。

## Abstract
Generating safe corridors is essential for collision-free robotic motion planning, yet most existing methods rely on predefined reference paths, which bias corridor geometry and implicitly limit the homotopy classes that can be explored. We propose a reference-path-free corridor generation framework on graphs of convex sets (GCS) that constructs corridors directly as sequences of convex sets, allowing corridor structure to emerge from the free-space representation rather than from a guiding path. To reason about similarity among corridors, we extend visibility-based deformation from paths to convex-set sequences, enabling the fusion of topologically redundant corridors while preserving distinct alternatives. To overcome the limited adaptability of existing GCS methods based on static global decompositions, we further develop an adaptive multi-scale GCS, in which a sampling-based fine-scale graph supports localized updates and a visibility-based coarse-scale graph enables compact global exploration. The two levels maintain topological consistency, allowing incremental updates without full graph reconstruction under environmental uncertainty. Numerical experiments characterize GCS construction, corridor generation, homotopy-aware exploration, and local updates, showing efficient graph construction, stable trajectory-level performance, and shorter-duration homotopy-aware trajectories than existing baselines. Hardware experiments on ground and aerial robots, including deployment with onboard localization, further validate the framework under translated and previously unknown obstacles.

---

## 论文详细总结（自动生成）

这篇论文提出了一种无需预定义参考路径的同伦感知走廊生成框架，旨在解决传统运动规划中走廊生成过度依赖离散路径引导的问题。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的安全走廊（Safe Corridors）生成通常依赖于预先计算的离散参考路径（如 A* 或 RRT*），然后通过膨胀路径点形成凸集序列。
*   **研究动机**：
    *   **几何偏差**：走廊的几何形状受限于参考路径，可能导致下游轨迹优化空间不足。
    *   **拓扑局限**：依赖单一路径会限制对不同同伦类（Homotopy Classes）的探索，难以在复杂环境中找到全局最优或更多样化的避障方案。
    *   **适应性差**：在环境发生局部变化时，现有的凸集图（GCS）方法往往需要重新构建全局图，计算开销大。

### 2. 方法论
该论文提出了一种基于**凸集图（GCS）**的层次化框架，核心思想是直接在自由空间表示中搜索走廊。
*   **自适应多尺度 GCS (MSGCS)**：
    *   **细尺度图 ($G_f$)**：基于采样生成高分辨率的凸集，捕捉环境的局部几何细节，用于局部更新。
    *   **粗尺度图 ($G_c$)**：通过可见性聚合细尺度凸集，形成紧凑的全局抽象，用于快速探索不同的拓扑路径。
*   **同伦感知走廊推理**：
    *   将**可见性变形（VD）**和**均匀可见性变形（UVD）**的概念从“路径级”扩展到“凸集序列（SCS）级”。
    *   提出了一种基于 UVD 的走廊融合准则，能够识别并合并拓扑冗余的走廊，仅保留具有代表性的不同拓扑分支。
*   **局部更新机制**：当感知到环境变化（如新增障碍物）时，仅对受影响的局部区域进行细尺度重采样和粗尺度重构，避免了全局重算。

### 3. 实验设计
*   **实验场景**：
    *   **数值仿真**：包括 2D/3D 随机障碍物环境（稀疏、中等、密集）和 2D 迷宫环境（宽、中、窄通道）。
    *   **硬件实验**：使用 OptiTrack 动捕系统的地面机器人和四旋翼无人机，以及基于机载 LiDAR 和 AMCL 定位的地面机器人。
*   **对比基准 (Benchmarks)**：
    *   **图构建对比**：VCC (Clique covers)、R-IRIS (Random-seed IRIS)。
    *   **走廊生成对比**：SFC (Safe Flight Corridor)、IMPC (Iterative Margin-based Planning)。
    *   **轨迹优化**：所有方法均统一使用 MINCO 轨迹优化器以确保公平性。

### 4. 资源与算力
*   **硬件环境**：Intel Core i7-13700K (3.4 GHz) CPU，32 GB RAM。
*   **软件环境**：Ubuntu 20.04/24.04，C++17，Python 3，使用 Drake 库进行几何优化和碰撞检测。
*   **算力说明**：论文未涉及深度学习训练，因此未提及 GPU 算力需求。实验侧重于实时规划算法的 CPU 耗时分析。

### 5. 实验数量与充分性
*   **实验规模**：
    *   图构建实验在 9 种不同设置下各进行了 10 次随机地图测试。
    *   同伦感知实验在 2D 和 3D 多拓扑环境中各进行了 10 组对比。
    *   硬件实验涵盖了 15 次物理运行，包括平移障碍物和完全未知的障碍物场景。
*   **充分性评价**：实验设计较为充分，涵盖了从底层图构建效率到高层轨迹质量（时间、速度、平滑度）的全方位评估，且通过消融实验验证了局部更新的加速效果，对比实验具有客观性和公平性。

### 6. 主要结论与发现
*   **效率提升**：MSGCS 的构建速度比 VCC 快 3.8–13.4 倍，且生成的图结构更稀疏（节点度数降低 24%–91%）。
*   **性能优化**：由于摆脱了参考路径的偏置，该方法生成的轨迹在多拓扑环境中的持续时间（Duration）比基准方法缩短了约 12%–20%。
*   **实时性**：局部更新机制在环境变化时比完全重构图快 1.7–27 倍，能够支持机器人在未知环境中的动态避障。

### 7. 优点
*   **理论创新**：首次将同伦变形理论从路径层面严格扩展到凸集序列层面，为走廊级拓扑推理提供了数学基础。
*   **架构灵活**：多尺度设计平衡了“全局搜索的紧凑性”与“局部更新的精细度”。
*   **无需调参**：避免了传统方法中 A* 路径膨胀时对额外安全裕度（Inflation Margin）的敏感性。

### 8. 不足与局限
*   **环境假设**：虽然支持局部更新，但核心算法仍基于静态环境假设，对于高速动态障碍物的处理能力尚待验证。
*   **计算复杂度**：在极度复杂的 3D 环境中，凸包（Convex Hull）的频繁计算可能仍会带来一定的计算压力。
*   **应用限制**：目前主要针对单机规划，尚未扩展到多机协同或存在动态交互约束的场景。

（完）
