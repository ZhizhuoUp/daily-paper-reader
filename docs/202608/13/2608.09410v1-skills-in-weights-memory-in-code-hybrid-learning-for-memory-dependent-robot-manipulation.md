---
title: "Skills in Weights, Memory in Code: Hybrid Learning for Memory-Dependent Robot Manipulation"
title_zh: 权重中的技能，代码中的记忆：面向记忆依赖型机器人操纵的混合学习
authors: "Yunhao Zhao, Zhenyang Ni, Haoyang Chen, Ruohan Zhang, Qi Zhu"
date: 2026-08-10
pdf: "https://arxiv.org/pdf/2608.09410v1"
tags: ["query:q6"]
score: 8.0
evidence: 记忆依赖型机器人操作的混合学习
tldr: 针对机器人操作中长时程记忆缺失导致的非马尔可夫决策难题，本文提出HyMeS混合学习框架。该框架将基于梯度的模仿学习（用于底层运动技能）与编码智能体（用于高层记忆管理）相结合，通过启发式学习迭代更新执行逻辑。HyMeS利用多模态阶段完成验证闭环，显著提升了数据效率和组合泛化能力，在RoboMemArena基准测试中表现优于现有VLA模型。
selection_source: fresh_fetch
motivation: 现有的视觉-语言-动作（VLA）策略通常仅依赖短期历史，难以处理需要长期记忆和推理的复杂非马尔可夫机器人操作任务。
method: 提出HyMeS框架，通过模仿学习训练底层技能，并利用编码智能体通过启发式学习管理高层记忆与任务逻辑，结合多模态验证实现闭环控制。
result: "在RoboMemArena测试中，HyMeS将平均任务成功率从41.3%提升至60.1%，在累积成功率和任务成功率上均显著超越了pi0.5和PrediMem。"
conclusion: 这种将底层运动技能与高层代码逻辑相结合的混合学习方法，为解决具有记忆依赖性的机器人操作任务提供了一种高效且泛化性强的新方案。
---

## 摘要
现代视觉-语言-动作（VLA）策略已具备广泛的操纵技能，但通常仅根据当前观测或短的固定长度历史生成每个动作块。然而，现实世界的操纵通常是非马尔可夫的，要求机器人保留并推理长时程交互历史中与任务相关的信息，以确定下一步动作。为了应对这一挑战，我们提出了 HyMeS，这是一种混合学习框架，它利用编程智能体的推理和记忆管理能力来引导马尔可夫 VLA 进行记忆依赖型操纵。具体而言，HyMeS 通过基于梯度的模仿学习来学习底层运动技能，而编程智能体则通过启发式学习，从回放反馈中迭代更新可执行的启发式系统，从而获取高层记忆管理策略。此外，我们通过多模态阶段完成验证闭合了引导与执行之间的环路，该验证利用本体感受信号和多帧 VLM 判断来更新记忆。与端到端的记忆增强型 VLA 相比，HyMeS 仅需要可重用运动技能的演示，而不需要针对每个依赖历史的任务配置进行演示，从而实现了高效的数据组合泛化。在 RoboMemArena 上，HyMeS 将平均累积成功率从 52.5% 提高到 66.2%，平均任务成功率从 41.3% 提高到 60.1%（相较于 pi0.5），同时在累积成功率和任务成功率上分别超过 PrediMem 4.5 个百分点和 14.5 个百分点。

## Abstract
Modern vision-language-action (VLA) policies have acquired broad manipulation skills, but typically generate each action chunk from the current observation or a short fixed-length history. However, real-world manipulation is often non-Markovian, requiring robots to retain and reason over task-relevant information from long-horizon interaction histories to determine the next action. To address this challenge, we propose HyMeS, a hybrid learning framework that leverages the reasoning and memory-management capabilities of coding agents to steer a Markovian VLA for memory-dependent manipulation. Specifically, HyMeS learns low-level motor skills through gradient-based imitation learning, while a coding agent acquires high-level memory-management strategies through heuristic learning by iteratively updating an executable heuristic system from rollout feedback. Furthermore, we close the loop between steering and execution through multimodal stage-completion verification, which updates memory using proprioceptive signals and multi-frame VLM judgments. Compared with end-to-end memory-augmented VLAs, HyMeS requires demonstrations only for reusable motor skills rather than for every history-dependent task configuration, enabling data-efficient compositional generalization. On RoboMemArena, HyMeS improves mean cumulative success from 52.5% to 66.2% and mean task success from 41.3% to 60.1% over pi0.5, while outperforming PrediMem by 4.5 points in cumulative success and 14.5 points in task success.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **HyMeS** 的混合学习框架，旨在解决机器人操纵中由于长时程记忆缺失导致的非马尔可夫（Non-Markovian）决策难题。以下是对该论文的深度总结：

### 1. 核心问题与整体含义
*   **研究动机**：当前的视觉-语言-动作（VLA）模型（如 $\pi_{0.5}$）虽然具备强大的底层运动技能，但本质上是**马尔可夫**的，即动作生成仅依赖当前观测或极短的历史。
*   **核心挑战**：现实任务常需长期记忆（例如：“按按钮的次数等于卡片上的数字”）。现有的记忆增强方法通常采用端到端训练，这需要海量的演示数据来覆盖所有可能的记忆组合（如不同的计数、不同的物体关联），导致数据效率极低且难以泛化。
*   **整体含义**：论文提出将“技能”留在神经网络权重中，将“记忆”交给可执行代码。通过这种解耦，机器人可以利用预训练模型的运动能力，同时通过编程智能体（Coding Agent）灵活管理复杂的任务逻辑。

### 2. 方法论
HyMeS 的核心思想是**权重空间（Weight Space）与代码空间（Code Space）的混合学习**：
*   **底层运动技能（权重空间）**：使用基于流匹配（Flow-matching）的 VLA 模型，通过模仿学习从专家演示中获取基础动作能力。一旦训练完成，权重保持固定。
*   **高层记忆策略（代码空间）**：利用编程智能体（如 Claude 或 Codex）编写可执行的 Python 启发式程序。该程序包含：
    *   **约束选择规则 (C)**：根据当前记忆状态选择目标。
    *   **事件验证规则 (V)**：判断阶段是否完成。
    *   **记忆更新规则 (U)**：更新计数器或任务进度。
*   **记忆条件动作引导（Steering）**：将代码生成的奖励函数梯度注入到 VLA 的去噪过程中，引导模型生成符合记忆逻辑的动作，而无需修改模型权重。
*   **PACE 验证机制**：引入“本体感受与完成驱动的阶段切换”（PACE），结合本体感受信号（如关节力矩）和多帧视觉语言模型（VLM，如 Qwen3-VL）的判断，实现闭环的阶段转换和记忆更新。

### 3. 实验设计
*   **数据集/场景**：
    *   **仿真环境**：使用 **RoboMemArena** 基准测试，涵盖 12 个任务，分为四大类：转移（Transferring）、计数（Counting）、顺序执行（Sequential）和遮挡（Occlusion）。
    *   **真实世界**：在 **SO-101 机器人**上进行了三个任务：观察并抓取、数字引导按压、放回方块。
*   **Benchmark 与对比方法**：
    *   **$\pi_{0.5}$**：原始的反应式（Reactive）马尔可夫策略。
    *   **PrediMem**：现有的记忆增强型 VLA 基准模型。
*   **评价指标**：任务成功率（TSR）和累积成功率（CSR，衡量阶段性进展）。

### 4. 资源与算力
*   **模型使用**：使用了 $\pi_{0.5}$ 作为基础策略，Qwen3-VL-8B 用于视觉验证，Claude 用于代码生成与迭代。
*   **算力细节**：论文**未明确说明**具体的 GPU 数量、型号或训练总时长。但提到仿真使用了 OpenPI 框架，真机 fine-tuning 使用了 LeRobot 框架。由于核心逻辑在代码空间，其对大规模 GPU 重新训练的依赖程度低于端到端方法。

### 5. 实验数量与充分性
*   **实验规模**：仿真实验包含 160 个评估回合；真机实验包含 35 次试验。
*   **消融实验**：针对“启发式学习（迭代更新代码）”和“PACE 多模态证据”进行了专门的消融研究。
*   **充分性评价**：实验覆盖了多种典型的记忆依赖场景，对比了最先进的基准模型，并进行了真机验证。实验设计较为客观，通过固定底层权重来公平比较记忆管理能力的提升。

### 6. 主要结论与发现
*   **性能提升**：在 RoboMemArena 上，HyMeS 将任务成功率（TSR）从 41.3% 提升至 60.1%，显著优于 $\pi_{0.5}$ 和 PrediMem。
*   **数据效率**：HyMeS 证明了不需要为每种记忆配置（如按 1 次 vs 按 5 次）收集演示，只需学习基础技能即可通过代码逻辑实现组合泛化。
*   **验证的重要性**：消融实验显示，结合视觉和本体感受的 PACE 机制比单一信号验证更可靠，能有效防止记忆状态的错误跳转。

### 7. 优点（亮点）
*   **组合泛化性**：通过代码管理逻辑，极大地降低了对复杂任务演示数据的需求。
*   **可解释性与可干预性**：记忆状态以符号化代码形式存在，人类可以直接查看和调试机器人为何失败（是动作没做好，还是记错了数）。
*   **即插即用**：可以引导冻结权重的预训练模型，无需昂贵的微调即可赋予现有模型长时程处理能力。

### 8. 不足与局限
*   **推理延迟**：调用大型 VLM 进行视觉验证和 Coding Agent 进行逻辑推理会增加每一步的计算开销和延迟。
*   **底层依赖**：如果底层 VLA 模型的运动技能本身不达标（如抓取精度不足），高层的记忆逻辑再正确也无法完成任务。
*   **符号化限制**：目前主要处理可符号化表达的记忆任务，对于某些难以用代码描述的模糊感官记忆可能效果有限。

（完）
