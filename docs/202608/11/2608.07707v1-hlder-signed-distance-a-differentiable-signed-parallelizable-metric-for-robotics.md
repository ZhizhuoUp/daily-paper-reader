---
title: "Hölder Signed Distance: A Differentiable, Signed, Parallelizable Metric for Robotics"
title_zh: Hölder 有符号距离：一种用于机器人学的可微、有符号且可并行的度量
authors: "Felipe Bartelt, Ali Umut Kaypak, Anthony Tzes, Farshad Khorrami, Luciano C. A. Pimenta, Vinicius M. Gonçalves"
date: 2026-08-07
pdf: "https://arxiv.org/pdf/2608.07707v1"
tags: ["query:profile-1"]
score: 9.0
evidence: 用于实时优化运动规划的可微距离度量
tldr: 针对机器人运动规划中欧氏符号距离函数（SDF）不可微且计算效率受限的问题，本文提出了一种新型的“Hölder符号距离”。该方法通过引入可微的Hölder最小和最大算子，实现了凸多面体间距离的闭式计算。该度量不仅保持了符号信息，还具有良好的可微性和GPU并行化能力，显著提升了实时优化与控制的性能。
selection_source: fresh_fetch
motivation: 传统的欧氏符号距离函数在某些点不可微，且现有替代方案难以同时兼顾可微性、符号信息和计算效率。
method: 通过定义可微的Hölder最小与最大算子来替换经典SDF中的极值操作，构建出一种无需迭代的闭式符号距离公式。
result: 实验验证了该方法在计算速度上优于现有技术，并成功演示了其在机器人机械臂实时控制任务中的有效性。
conclusion: Hölder符号距离为机器人领域提供了一个高效、可微且易于并行化的距离度量工具，解决了复杂环境下的优化难题。
---

## 摘要
计算集合间的距离在机器人运动规划与控制中至关重要，其中可微梯度能够实现实时优化。然而，欧几里得有符号距离函数 (SDF) 并非处处可微，而现有的替代方案往往会牺牲可微性、符号信息或计算效率。在本文中，我们介绍了一种凸多面体之间的新型可微有符号距离。为此，我们首先提出了最小和最大算子的可微版本，称为 Hölder 最小值和 Hölder 最大值。然后，我们替换了经典 SDF 公式中的原始 min-max 算子，从而得到了 Hölder 有符号距离。与以往依赖迭代算法的可微距离公式不同，我们的方法以闭式解形式计算，消除了收敛问题，同时自然地适用于 GPU 并行化。我们通过与现有方法的运行时间对比，验证了所提距离的实际优势和计算性能。我们还展示了一个机器人机械臂实验，证明了其在控制应用中的适用性。

## Abstract
Computing distances between sets is essential in robotic motion planning and control, where differentiable gradients enable real-time optimization. The Euclidean Signed Distance Function (SDF), however, is not differentiable everywhere, and existing alternatives often sacrifice differentiability, sign information, or computational efficiency. In this letter, we introduce a novel differentiable signed distance between convex polyhedra. To this end, we first propose differentiable versions of the minimum and maximum operators, termed the Hölder minimum and Hölder maximum. We then replace the original min-max operators in the classical SDF formulation, yielding the Hölder signed distance. Unlike prior differentiable distance formulations that rely on iterative algorithms, our approach is computed in closed form, eliminating convergence issues while remaining naturally amenable to GPU parallelization. We validate the practical advantages and computational performance of the proposed distance through runtime comparisons with existing approaches. We also present a robotic manipulator experiment, demonstrating its suitability for applications in control.

---

## 论文详细总结（自动生成）

以下是对论文《Hölder Signed Distance: A Differentiable, Signed, Parallelizable Metric for Robotics》的结构化总结：

### 1. 核心问题与整体含义
在机器人运动规划和控制（如控制屏障函数 CBF）中，计算物体间的距离及其梯度至关重要。然而，传统的**欧氏有符号距离函数 (SDF)** 在见证点（witness points）不唯一时不可微，会导致控制信号出现剧烈抖动（chattering）。现有的平滑距离度量要么不支持有符号信息（无法表示穿透深度），要么依赖迭代算法（存在收敛问题且难以并行化）。
**本文的核心贡献**是提出了一种名为 **HD-SDF（Hölder 有符号距离）** 的新型度量，它在保持有符号特性的同时，实现了全空间可微、闭式计算（非迭代）且天然支持 GPU 并行化。

### 2. 方法论核心思想
该方法通过重构经典 SDF 的 min-max 公式，引入了可微的替代算子：
*   **Hölder 最小/最大算子 ($\text{HMin}_\gamma, \text{HMax}_\gamma$)**：基于 Hölder 范数（$p$-norm）的思想，定义了几乎处处可微的极值算子。通过参数 $\gamma$ 控制可微阶数，当 $\gamma \to \infty$ 时退化为标准 min/max。
*   **整形函数 (Shaping Function)**：为了消除算子在零点处的不可微性，引入了非线性整形函数 $\Phi_{\gamma, \epsilon}$。它在远离零点时接近恒等映射，在零点附近通过高阶平滑处理确保全局 $\gamma$ 阶可微。
*   **基于 SAT 的闭式公式**：利用分离轴定理 (SAT) 的候选法向量集，将连续的最优化问题转化为对有限顶点和法向量集的离散运算。
*   **计算流程**：
    1.  提取两个凸多面体的顶点集和候选法向量集（面法线及边叉积）。
    2.  使用 $\text{HMin}_\gamma$ 计算每个法向量方向上的最小投影距离。
    3.  使用 $\text{HMax}_\gamma$ 综合所有方向的距离，得到最终的有符号距离值。

### 3. 实验设计
*   **实验场景与基准**：
    *   **运行时间对比**：使用 5 种柏拉图多面体（四面体到二十面体）的 15 种组合，生成随机位姿进行测试。对比基准包括 **FCL 库**（标准欧氏 SDF）和 **GDF**（基于广义交替投影的可微距离）。
    *   **保守性评估**：通过 500,000 组随机配置，分析 HD-SDF 判定为碰撞而实际未碰撞的“误报率”。
    *   **机器人控制实验**：Franka Emika Panda 机械臂在狭窄走廊中移动，对比基于欧氏 SDF 和 HD-SDF 的 CBF 控制器表现。
*   **对比维度**：计算效率（CPU/GPU）、控制信号平滑度、距离准确性。

### 4. 资源与算力
*   **硬件环境**：
    *   **CPU**：Intel Core i7-14700 (5.40 GHz)。
    *   **GPU**：NVIDIA GeForce RTX 4060。
    *   **内存**：32 GB RAM。
*   **软件实现**：C++ 实现 CPU 版本，PyTorch 实现 GPU 加速版本。
*   **算力表现**：在 GPU 上处理大规模查询时，相比 CPU 版本实现了最高 **13 倍** 的加速。

### 5. 实验数量与充分性
*   **实验规模**：
    *   时间测试涵盖了从简单到复杂的多种凸几何体组合，每种组合进行 100 个位姿 $\times$ 50 次查询。
    *   保守性测试采用了 **50 万组** 样本，数据量非常充分，足以证明其在实际应用中的可靠性。
*   **客观性**：实验对比了工业界常用的 FCL 库和学术界最新的可微距离方法，涵盖了从底层算法效率到高层机器人控制效果的完整链路，评价较为客观。

### 6. 主要结论与发现
*   **可微性优势**：HD-SDF 成功消除了欧氏距离在临界位姿下的不可微“拐点”，使机器人控制指令非常平滑，彻底解决了控制抖动问题。
*   **计算性能**：虽然 HD-SDF 的理论复杂度为 $O(V^3)$（$V$ 为顶点数），在 CPU 上慢于 FCL，但其闭式结构使其在 GPU 上表现极佳，能够胜任大规模并行距离查询。
*   **保守性**：HD-SDF 是一种保守估计（即 D > 0 保证不碰撞，但 D ≤ 0 不一定碰撞）。实验表明，在 10cm 的安全裕度下，误报率低于 0.2%，在实际工程中完全可接受。

### 7. 优点
*   **数学优雅性**：通过 Hölder 算子将不可微问题转化为参数化可微问题，理论完备。
*   **工程实用性**：无需迭代，避免了传统优化方法可能出现的收敛失败或死循环。
*   **灵活性**：用户可以通过调整 $\gamma$ 和 $\epsilon$ 在计算精度、平滑度和保守性之间进行权衡。

### 8. 不足与局限
*   **形状限制**：目前仅适用于**凸多面体**，对于非凸形状需要进行凸分解，这会增加预处理成本。
*   **计算复杂度**：对于顶点数极多的复杂模型，其 $O(V^3)$ 的复杂度在单核 CPU 上可能会成为瓶颈。
*   **保守性偏差**：虽然误报率低，但在极度狭窄的空间内，这种保守性可能会导致机器人无法找到可行路径（过于“胆小”）。

（完）
