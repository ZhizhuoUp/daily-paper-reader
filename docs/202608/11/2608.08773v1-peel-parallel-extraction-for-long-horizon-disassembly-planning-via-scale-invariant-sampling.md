---
title: "PEEL: Parallel Extraction for Long-Horizon Disassembly Planning via Scale-Invariant Sampling"
title_zh: PEEL：通过尺度不变采样实现长程拆卸规划的并行提取
authors: "Servet B. Bayraktar, Andreas Orthey, Zachary Kingston, Marc Toussaint"
date: 2026-08-09
pdf: "https://arxiv.org/pdf/2608.08773v1"
tags: ["query:profile-1"]
score: 8.0
evidence: 使用采样规划为机器人机械臂计算无碰撞移除动作
tldr: 本研究针对长程多部件物体拆卸任务中狭窄通道和复杂碰撞约束的挑战，提出了PEEL算法。该算法结合了尺度不变采样方案与多臂老虎机快速探索随机树（MAB-RRT）规划器，通过并行运行多个规划器来高效生成无碰撞的拆卸序列。PEEL能够自动估计物体尺度并动态调整采样策略，在Fetch机器人上成功实现了包含10至17个部件的复杂拆卸任务，显著提升了长程拆卸规划的效率和成功率。
selection_source: fresh_fetch
motivation: 旨在解决长程多部件物体拆卸中，由于狭窄逃逸通道导致的无碰撞移除路径计算效率低下的问题。
method: 提出PEEL算法，利用尺度不变采样和MAB-RRT规划器并行计算部件移除序列，并构建有序拆卸图。
result: "在76个单部件拆卸任务中达到100%成功率，并成功在Fetch机器人上完成了涉及10至17个部件的复杂长程拆卸。"
conclusion: PEEL算法通过结合自适应采样与并行规划，为复杂环境下的长程机器人拆卸任务提供了一种高效且鲁棒的解决方案。
---

## 摘要
长程多部件物体拆卸要求机器人计算出可行的无碰撞移除动作序列，即使在存在狭窄逃逸通道的情况下也是如此。为了高效解决此类拆卸问题，我们提出了长程拆卸并行提取（PEEL）算法，该算法能够高效计算物体组件的拆卸动作，并将其提供给机器人机械臂执行。PEEL 使用基于采样的运动规划，通过尺度不变采样方案计算单个物体的运动，其中物体尺度在预热阶段进行估计，随后的定向采样器利用该尺度。该采样方案被集成到多臂老虎机快速探索随机树（MAB-RRT）规划器中，该规划器根据接收到的奖励信号在不同的采样器之间切换。利用 MAB-RRT，PEEL 算法并行运行一批规划器，以获得一个指定物体部件移除顺序的有向图。我们证明了 MAB-RRT 在 76 个组件上能以 100% 的成功率高效解决单部件拆卸问题，并且对其参数具有鲁棒性。通过将 MAB-RRT 集成到 PEEL 中，我们使用 Fetch 机械臂机器人解决了涉及 10 到 17 个独立物体部件的四个长程拆卸问题。

## Abstract
Long-horizon multi-part object disassembly requires robots to compute feasible sequences of collision-free removal motions, even in the presence of tight, narrow escape corridors. To efficiently solve such disassembly problems, we propose Parallel Extraction for Long-Horizon Disassembly (PEEL), an algorithm which efficiently computes disassembly motions for object assemblies and feeds them to a robot manipulator for execution. PEEL uses sampling-based motion planning to compute single-object motions through the use of a scale-invariant sampling scheme, where the object scale is estimated in a burn-in phase and a subsequent directional sampler exploits the scale. This sampling scheme is integrated into a multi-arm bandit rapidly-exploring random tree (MAB-RRT) planner, which switches between different samplers depending on the reward signal received. Using MAB-RRT, the PEEL algorithm runs a batch of planners in parallel to obtain an ordered graph specifying the sequence in which object parts have to be removed. We show that MAB-RRT can efficiently solve single-part disassemblies with 100 percent success rate on 76 assemblies, and that it is robust to its parameters. By integrating MAB-RRT into PEEL, we solve four long-horizon disassembly problems using the Fetch manipulator robot involving 10 to 17 individual object parts.

---

## 论文详细总结（自动生成）

### 论文总结：PEEL——基于尺度不变采样的长程并行拆卸规划

#### 1. 核心问题与整体含义
论文针对**长程多部件物体拆卸（Long-Horizon Multi-part Disassembly）**中的两大核心挑战：
*   **几何约束极强**：部件往往处于狭窄的“逃逸通道”中（如深孔中的螺栓），传统均匀采样难以发现有效路径。
*   **序列组合爆炸**：随着部件数量增加，确定拆卸顺序的搜索空间呈指数级增长，传统的图搜索方法难以扩展。
**整体含义**：提出了一种名为 **PEEL** 的框架，通过并行化的“规划竞赛”机制自动发现拆卸序列，并结合自适应的采样策略（MAB-RRT）高效解决狭窄通道的路径规划问题，最终实现从几何规划到机器人执行的闭环。

#### 2. 方法论核心思想与技术细节
*   **PEEL 并行提取框架**：
    *   **核心思想**：不再显式搜索拆卸顺序图，而是采用“贪婪竞赛”机制。在每一轮中，为所有当前可能拆卸的部件并行启动独立的规划器。
    *   **竞赛机制**：第一个成功找到无碰撞路径的规划器成为“赢家”，该部件被移除，其余并行进程终止，系统更新几何环境并进入下一轮。这种方式将顺序发现与几何可行性检查合二为一。
*   **MAB-RRT 规划器与尺度不变采样**：
    *   **多臂老虎机（MAB）**：在规划过程中，通过 UCB 算法在均匀采样、尺度采样和 PCA（主成分分析）采样器之间动态切换。
    *   **尺度估计**：在“预热阶段”通过增长/缩小半径来估计局部自由空间的尺度 $r^*$。
    *   **定向采样**：利用 PCA 分析有效样本，识别出狭窄通道的逃逸方向，并沿该方向进行偏置采样。
*   **机器人落地流水线（5阶段）**：
    *   包括：抓取规划、拆卸运动执行（含自由状态检测）、目标逆运动学（IK）计算、换手（Regrasp）动作、以及最终的搬运。

#### 3. 实验设计
*   **数据集与场景**：
    *   **单部件基准**：从 Automate 数据集中筛选出 76 个无碰撞机械组件，每个组件进行 10 次随机旋转测试（共 760 次试验）。
    *   **长程拆卸任务**：选取 4 个复杂组件（显微镜、盘式制动器、联轴器块、钳子），包含 10 到 17 个部件。
*   **对比方法**：
    *   对比了经典采样策略（Bridge, Gaussian, Obstacle-based RRT）。
    *   对比了现代拆卸规划基准（AssembleThemAll 中的 BFS, BK-RRT, MateVec-TRRT, TRRT 等）。
*   **评估指标**：成功率、规划耗时、路径长度、参数敏感性。

#### 4. 资源与算力
*   **硬件环境**：Intel Core i7-5820K CPU (3.30 GHz, 12 核心), 32 GB RAM。
*   **并行设置**：PEEL 在多部件任务中使用了 **10 个并行进程**（每个部件占用一个 CPU 核心）。
*   **算力说明**：论文未提及使用 GPU 进行规划计算，主要依赖 CPU 的多核并行能力。碰撞检测使用了 DART-Sim 引擎。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   单部件测试涵盖 76 种不同几何结构，总计 760 次实验，样本量充足。
    *   多部件任务对 4 种复杂模型各进行了 10 次随机初始顺序的重复实验，验证了算法对初始状态的鲁棒性。
    *   进行了详尽的**超参数消融实验**（窗口大小、有效性边界、初始半径、样本数量等）。
*   **客观性**：对比了多种 SOTA（先进）基准方法，并使用了公开的 Automate 数据集，实验设计较为客观公平。

#### 6. 主要结论与发现
*   **性能卓越**：MAB-RRT 在单部件拆卸中达到了 **100% 的成功率**，远超 BFS（53.9%）和传统 RRT（11.4%）。
*   **效率提升**：在多部件任务中，PEEL 比基准方法快 1 到 2 个数量级，且方差极小，表现出极高的稳定性。
*   **鲁棒性**：尺度不变采样能够自动适应不同的几何尺度，对超参数设置不敏感。
*   **可行性**：生成的路径在物理仿真环境（Fetch 机器人）中得到了成功验证，证明了从几何规划到机器人控制的有效衔接。

#### 7. 优点与亮点
*   **并行机制创新**：通过“规划竞赛”巧妙避开了复杂的拆卸顺序搜索问题，极大地提高了长程规划的效率。
*   **自适应采样**：尺度不变采样解决了采样规划器在处理极度狭窄空间时的痛点，无需人工调优即可适应不同尺寸的零件。
*   **闭环验证**：不仅停留在几何算法层面，还提供了完整的机器人执行流水线，包括处理运动学限制的换手策略。

#### 8. 不足与局限
*   **非顺序拆卸限制**：目前仅支持“顺序可分解”的物体。对于需要多个部件同时移动才能拆除的**运动学互锁结构**（如鲁班锁/Burr Puzzles），该算法尚无法直接处理。
*   **纯几何假设**：规划阶段未考虑重力、摩擦力等物理动力学因素，虽然在执行阶段有补偿，但可能导致生成的几何路径在某些物理环境下难以实现。
*   **算力依赖**：虽然避免了指数级搜索，但并行竞赛机制在部件极多时对 CPU 核心数量有一定依赖。

（完）
