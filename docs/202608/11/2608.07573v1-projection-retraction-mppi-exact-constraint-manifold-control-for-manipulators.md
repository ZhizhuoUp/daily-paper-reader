---
title: "Projection-Retraction MPPI: Exact Constraint-Manifold Control for Manipulators"
title_zh: 投影-回撤 MPPI：机械臂的精确约束流形控制
authors: "Seulchan Lee, Leesai Park, Minhyeong Kang, Sanghyun Kim"
date: 2026-08-04
pdf: "https://arxiv.org/pdf/2608.07573v1"
tags: ["query:profile-1"]
score: 9.0
evidence: 基于优化的机械臂控制，包含避障约束
tldr: 针对机械臂操作中MPPI算法仅能以软惩罚处理约束导致精度不足的问题，本文提出PR-MPPI算法。该方法通过在采样动力学中引入投影与回撤机制，将速度投影至满足等式与不等式约束的空间，并利用回撤消除数值漂移，实现了对闭链运动学等硬约束的精确控制，在双臂协作和避障任务中表现优异。
selection_source: fresh_fetch
motivation: 传统的MPPI算法通过代价函数处理约束，难以在强任务目标下保证闭链运动学或关节极限等硬约束的精确性。
method: 提出PR-MPPI，在每步采样中将速度投影至约束子空间，并通过回撤操作将控制指令精确映射回约束流形。
result: 在14自由度双臂系统仿真和Unitree H1-2机器人实机实验中，该方法实现了数值容差级别的约束满足和实时避障。
conclusion: PR-MPPI为具有复杂几何约束的机械臂控制提供了一种高效且精确的无梯度模型预测控制方案。
---

## 摘要
模型预测路径积分 (MPPI) 控制因其无梯度、可并行处理非凸代价的特性，在操作任务中得到了广泛应用。然而，操作任务通常会施加贯穿整个运动过程的约束：例如双臂抓取时必须精确保持的闭链运动学约束，或者绝不能逾越的关节限位和障碍物间隙。MPPI 仅通过代价函数来处理此类约束，将其视为软惩罚，这只能实现近似满足，且在任务代价较强时会失效。为解决这一问题，我们提出了投影-回撤 MPPI (PR-MPPI)，它在采样动力学内部强制执行约束。在每个展开步骤中，采样的速度被投影以同时满足两类约束：等式约束将其限制在一个子空间内，而每个不等式约束则将其限制在该子空间内的一个半空间中，从而确保不等式处理不会破坏等式约束。然而，这种投影仅在一阶意义上满足约束，有限的步长会导致偏离等式约束的微小漂移。因此，我们将返回的指令回撤到约束流形上，使其达到数值容差精度，且该过程与任务权重无关。我们在 14 自由度双臂系统上验证了 PR-MPPI。在仿真中，通过关节限位压力测试和随机避障实验，返回的指令在数值容差范围内满足了闭链等式约束。在真实硬件上，Unitree H1-2 人形机器人的双臂能够反应式地避开移动障碍物。代码和实验视频可在 https://rcilab.github.io/prmppi 获取。

## Abstract
Model Predictive Path Integral (MPPI) control is widely used in manipulation for its gradient-free, parallel handling of non-convex costs. Manipulation tasks, however, often impose constraints that hold throughout the motion: a closed kinematic chain that two grasping arms keep exactly, or joint limits and obstacle clearances that are never crossed. MPPI handles such constraints only through the cost, as soft penalties that hold approximately and fail under a strong task cost. To address this, we propose Projection-Retraction MPPI (PR-MPPI), which enforces the constraints inside the sampled dynamics. At every rollout step, the sampled velocity is projected to satisfy both constraint types: the equality restricts it to a subspace, and each inequality to a half-space within that subspace, so inequality handling never breaks the equality. This projection, however, satisfies the constraints only to first order, and a finite step leaves a small drift off the equality. Therefore, we retract the returned command back onto the constraint to numerical tolerance and independent of task weighting. We validate PR-MPPI on 14-DoF dual-arm systems. In simulation, the returned commands satisfy the closed-chain equality to numerical tolerance through a joint-limit stress test and randomized obstacle avoidance. On real hardware, the arms of a Unitree H1-2 humanoid reactively avoid a moving obstacle. Code and experiment videos are available at https://rcilab.github.io/prmppi.

---

## 论文详细总结（自动生成）

这是一份关于论文《Projection-Retraction MPPI: Exact Constraint-Manifold Control for Manipulators》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是**机械臂操作中的精确约束控制**问题。
*   **研究动机**：模型预测路径积分（MPPI）控制因其无梯度和并行化特性被广泛用于处理非凸代价函数。然而，传统的 MPPI 将约束（如双臂协作的闭链运动学、关节极限、避障）视为“软惩罚”加入代价函数。在任务目标较强时，这种软惩罚往往无法保证约束的精确满足，导致闭链断裂或碰撞。
*   **核心问题**：如何在保持 MPPI 采样优势的同时，强制执行硬性的等式约束（流形约束）和不等式约束（安全边界）。

### 2. 方法论
论文提出了 **PR-MPPI（投影-回撤 MPPI）**，其核心思想是将约束处理嵌入到采样动力学的内部，而非仅仅依靠代价函数。
*   **核心思想**：在每一个采样展开（Rollout）步骤中，对采样速度进行双重过滤。
*   **关键技术细节**：
    1.  **等式投影（Equality Projection）**：将采样速度投影到约束流形的切空间（Tangent Space）中，确保速度方向不破坏运动学等式（如 $J_c u = 0$）。
    2.  **不等式修正（Inequality Correction）**：在切空间内部，利用控制屏障函数（CBF）将速度限制在安全半空间内。这种“嵌套”结构确保了避障等操作不会破坏闭链等式。
    3.  **回撤机制（Retraction）**：由于一阶投影在有限步长下会产生数值漂移，算法在最终输出指令前，利用高斯-牛顿法将状态“回撤”到约束流形上，使等式残差达到数值容差级别（$10^{-10}$）。
*   **算法流程**：采样 -> 约束感知动力学展开（投影） -> 指令加权平均 -> 执行层过滤 -> 回撤至流形 -> 输出指令。

### 3. 实验设计
*   **实验场景**：
    1.  **关节极限压力测试**：使用 14 自由度 Franka 双臂系统，设定目标点在关节极限之外，测试算法是否能强制停在边界。
    2.  **随机避障任务**：在仿真中进行 30 次随机障碍物位置的托盘运输实验。
    3.  **硬件演示**：在 Unitree H1-2 人形机器人双臂上进行实时避障实验。
*   **对比基准（Benchmark）**：
    *   **DQ-MPPI**：基于对偶四元数的两阶段采样 MPC。
    *   **MC-MPPI**：基于学习流形的 MPPI。
    *   **消融实验**：对比了“无回撤”和“无不等式投影”的版本。

### 4. 资源与算力
*   **硬件配置**：Intel i5-13400F CPU, 16GB RAM, **NVIDIA RTX 4060 Ti GPU (8GB VRAM)**。
*   **计算性能**：
    *   PR-MPPI 在 GPU 上并行处理 1000 个样本，预测步长 30 步。
    *   **更新频率**：PR-MPPI 达到了 **30Hz** 的实时重规划速度（计算耗时约 19ms）。
    *   **对比**：DQ-MPPI 在相同硬件下的计算耗时约为 238ms（约 4.2Hz）。

### 5. 实验数量与充分性
*   **实验规模**：进行了 30 组随机避障对比实验，并提供了详细的统计数据（成功率、路径长度、约束残差）。
*   **充分性**：实验涵盖了从仿真压力测试到真实硬件部署的完整流程。通过消融实验清晰地展示了投影保证了安全性，而回撤保证了运动学的精确性。实验设计客观，对比了当前最先进的同类方法。

### 6. 主要结论与发现
*   **精确性**：PR-MPPI 能将闭链等式约束维持在 $10^{-10}$ 级别的数值精度，远优于基于惩罚项的方法。
*   **安全性**：即使在强任务驱动下，不等式投影也能确保关节不越限、不发生碰撞。
*   **搜索效率**：在采样内部嵌入投影，使得 MPPI 能够生成更多“近乎可行”的样本，从而在复杂避障任务中比仅在执行层过滤的方法更易成功。

### 7. 优点
*   **硬约束保证**：首次在 MPPI 框架下实现了数值精度的硬等式约束处理。
*   **计算高效**：利用 GPU 并行化和高效的活动集投影算法，实现了高频实时控制。
*   **通用性**：不依赖于特定的学习模型，适用于任何解析表达的运动学约束。

### 8. 不足与局限
*   **结构性冲突**：当障碍物避障方向与闭链约束方向完全冲突时（即切空间内无逃逸解），算法会失效，这是运动学层面的固有局限。
*   **动力学缺失**：目前主要在运动学（速度/位置）层面操作，尚未直接处理力矩层面的动力学约束（如力矩限制、接触力）。
*   **参数依赖**：仍需手动调节 CBF 的安全裕度参数以抵消有限步长带来的误差。

（完）
