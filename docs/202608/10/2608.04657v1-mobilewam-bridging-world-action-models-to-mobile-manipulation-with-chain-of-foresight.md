---
title: "MobileWAM: Bridging World Action Models to Mobile Manipulation with Chain-of-Foresight"
title_zh: MobileWAM：通过预见链将世界动作模型引入移动操作
authors: "Zehua Fan, Junjie He, Wenxuan Song, Xi Wang, Wenqi Lyu, Linge Zhao, Fuhao Li, Zihan You, Yifei Yang, Kaiming Xu, Qi Jiang, Yue Jiang, Haoang Li, Cheng Chi, Bailin Li, Yan Wang"
date: 2026-08-05
pdf: "https://arxiv.org/pdf/2608.04657v1"
tags: ["query:q6"]
score: 8.0
evidence: 移动操作的全身控制
tldr: MobileWAM将世界动作模型（WAM）扩展至移动操作领域，通过混合Transformer架构融合预训练视频扩散模型与轻量级动作专家。该研究引入了“预见链”（Chain-of-Foresight）机制，利用未来状态预测来增强监督信号，并采用针对移动与操作任务设计的混合专家系统。实验表明，该模型在模拟和真实机器人任务中均显著优于现有方法，且推理效率高。
selection_source: fresh_fetch
motivation: 旨在解决现有移动操作策略缺乏视觉动力学建模以及难以协调全身运动与场景动态的问题。
method: 采用层级联合注意力的混合专家架构融合视频先验，并结合预见链机制进行序列化未来状态预测。
result: 在ManiSkill-HAB基准测试中取得领先，并成功在真实移动机器人上实现多种任务的强泛化部署。
conclusion: MobileWAM通过将大规模运动先验与任务特定专家相结合，为复杂环境下的全身移动操作提供了高效且强大的解决方案。
---

## 摘要
基于视频生成骨干网络构建的世界动作模型（WAMs）是机器人学习领域一种新兴的方法，但目前仍局限于桌面操作。移动操作要求在场景级动态环境中同时进行移动和全身操作，但目前仍由缺乏动态感知的视觉编码器和手工设计的协调机制所主导。我们通过 MobileWAM 弥补了这一差距。这是一种混合 Transformer（mixture-of-transformers）架构，通过逐层联合注意力机制将预训练的视频扩散 Transformer 与轻量级动作专家模型融合，从而将互联网规模的运动先验转化为全身控制。为了协调移动和操作的异构动力学，动作专家的每个前馈层都由共享、移动和操作三个专家组成的混合专家模型构成，并根据动作 token 中的运动意图进行软路由。为了增加监督信号的密度，我们进一步提出了预见链（Chain-of-Foresight, CoF）：中间表示按顺序预测一系列未来的潜变量块（latent chunks），每一步都以其前序步骤为条件。CoF 与我们的解耦视频-动作去噪方案自然结合。在部署时，WAM 充当纯粹的当前帧编码器；预见仅通过梯度起作用，因此在推理时会丢弃预见链和视频生成部分，仅保留策略级的计算开销。MobileWAM 在 ManiSkill-HAB 上超越了最先进的移动操作策略，并在真实的 ARX Lift2 移动机器人上针对多种任务进行了微调，展现出强大的泛化能力。代码将很快发布。

## Abstract
World action models (WAMs) built on video generation backbones are a rising recipe for robot learning, yet remain confined to tabletop manipulation. Mobile manipulation demands simultaneous locomotion and whole-body manipulation amid scene-scale dynamics, yet is still dominated by dynamics-blind visual encoders with hand-crafted coordination. We bridge this gap with MobileWAM, a mixture-of-transformers architecture that fuses a pretrained video diffusion transformer with a lightweight action expert through layerwise joint attention, translating internet-scale motion priors into whole-body control. To reconcile the heterogeneous dynamics of moving and manipulating, each feed-forward layer of the action expert becomes a three-expert mixture of shared, locomotion, and manipulation experts, softly routed by the motion intent in the action tokens. To densify supervision, we further propose Chain-of-Foresight (CoF): intermediate representations sequentially predict a chain of future latent chunks, each step conditioned on its predecessor. CoF pairs naturally with our decoupled video--action denoising scheme. At deployment, the WAM serves as a pure current-frame encoder; foresight acts only through gradients, so at inference the foresight chain and video generation are discarded, leaving only policy-level cost. MobileWAM surpasses state-of-the-art mobile manipulation policies on ManiSkill-HAB and fine-tunes to a real ARX Lift2 mobile manipulator across diverse tasks with strong generalization. Code will be released soon.

---

## 论文详细总结（自动生成）

以下是对论文《MobileWAM: Bridging World Action Models to Mobile Manipulation with Chain-of-Foresight》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的机器人学习模型（如世界动作模型 WAMs）大多局限于桌面操作（固定视角、短因果链）。而**移动操作（Mobile Manipulation）**面临三大挑战：视角随底座移动而剧烈变化、底座与机械臂路径耦合导致的动作多模态性、以及长时程任务中的因果深度（早期的导航错误会导致后期的抓取失败）。
*   **研究动机**：目前的移动操作方案多依赖于“动力学盲”的视觉编码器或手工设计的协调机制。作者认为移动操作并非简单的“导航+操作”，而需要一种能够理解世界演变规律的全身控制模型。
*   **整体含义**：MobileWAM 旨在将预训练视频扩散模型的物理先验引入移动操作，通过创新的架构设计解决移动与操作之间的动力学冲突，并提高长时程任务的预测一致性。

### 2. 方法论
*   **核心思想**：采用混合 Transformer（Mixture-of-Transformers）架构，将预训练的视频扩散 Transformer（作为世界专家）与轻量级动作专家融合，并引入“预见链”增强时空一致性。
*   **关键技术细节**：
    *   **混合 Transformer 架构**：通过层级联合注意力机制（Layerwise Joint Attention），让动作 token 在每一层都能读取视频骨干网络的运动先验。采用非对称掩码，确保视频流不读取动作信息，从而实现推理时的解耦。
    *   **移动混合专家系统（Mobile MoE）**：在动作专家的前馈层（FFN）中，设计了三个专家：共享专家、移动专家和操作专家。通过软路由（Soft Router）根据动作 token 的运动意图动态分配权重，解决底座移动与精细操作之间的动力学干扰。
    *   **预见链（Chain-of-Foresight, CoF）**：这是一种训练阶段的辅助监督机制。它采用类 RNN 的结构，序列化地预测未来多个潜变量块（latent chunks）。每一步预测都依赖于前一步的“信念状态”（belief state），迫使模型编码长程物理演变。
    *   **解耦去噪方案**：在部署时，丢弃视频生成分支和 CoF 模块，将 WAM 作为当前帧编码器，仅对动作进行去噪，大幅提升推理速度。

### 3. 实验设计
*   **数据集与场景**：
    *   **仿真环境**：使用 **ManiSkill-HAB** 基准测试，包含 SetTable 套件中的 7 个子任务（如从冰箱拿苹果、放碗到桌子、开关抽屉等）。
    *   **真实世界**：使用 **ARX Lift2** 移动机器人，设计了 5 个难度递增的任务（从单步开抽屉到复杂的“开抽屉-取物-放物-关抽屉”组合任务）。
*   **对比方法**：
    *   **基础策略**：ACT, Diffusion Policy (DP), DP3, RDT。
    *   **移动操作 SOTA**：AC-DiT（需点云）、AnchorVLA、$\pi_{0.5}$。
*   **评估指标**：闭环测试的成功率（Success Rate, S.R.）。

### 4. 资源与算力
*   **算力设备**：
    *   **推理/部署**：使用 **NVIDIA A800** GPU（80GB）。
    *   **训练**：文中提到使用 AdamW 优化器，全局 Batch Size 为 256。虽然未明确给出训练总时长和总 GPU 数量，但提到在 A800 上推理延迟为 938ms，比传统的“先想象再执行”模型快 5-8 倍。
*   **模型规模**：骨干网络约为 6.5B 参数。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **仿真实验**：在 7 个子任务上进行了对比，每个任务进行 3 次独立评估取均值。
    *   **消融实验**：非常详尽，涵盖了 CoF 的有效性、MoE 的必要性、预见链的长度（K 值）、层级选择策略以及 MoE 与硬架构分割的对比。
    *   **真实机器人**：在 5 个不同复杂度的任务上对比了 $\pi_{0.5}$。
*   **充分性评价**：实验设计较为全面，不仅验证了性能提升，还通过定性分析（生成视频的质量对比）解释了模型为何有效。对比基准包含了最新的 SOTA 模型，具有较高的说服力。

### 6. 主要结论与发现
*   **性能领先**：MobileWAM 在 ManiSkill-HAB 上达到 73.0% 的平均成功率，显著超过所有基准模型（如比 AC-DiT 高出 17.4%）。
*   **长时程优势**：在真实机器人实验中，随着任务因果深度增加，MobileWAM 相比 $\pi_{0.5}$ 的领先优势扩大，证明了 CoF 在处理长时程依赖上的作用。
*   **高效推理**：通过解耦设计，推理速度达到 938ms，满足移动机器人的实时控制需求。
*   **MoE 的价值**：实验证明，软路由的 MoE 比强行将底座和手臂动作分开处理效果更好，因为它保留了全身协调的共享知识。

### 7. 优点
*   **零成本预见**：CoF 机制仅在训练时存在，增强了表示能力却不增加推理负担。
*   **架构优雅**：通过非对称注意力掩码实现了视频先验的深度融合与推理时的解耦。
*   **纯视觉方案**：仅使用 RGB 输入，无需昂贵的深度相机或点云处理，降低了硬件门槛。
*   **泛化性强**：在真实世界中表现出对视角变化和复杂因果链的强鲁棒性。

### 8. 不足与局限
*   **极端位置失效**：在机器人工作空间边缘（Workspace Fringe）的动作成功率较低，主要受限于逆运动学精度和碰撞感知。
*   **缺乏错误恢复**：模型在初始尝试失败后的自我修正能力（Recovery）仍有提升空间（约 20% 的失败源于此）。
*   **算力门槛**：尽管推理优化较好，但 6.5B 参数的模型仍需高性能服务器支持，难以在嵌入式端侧直接运行。

（完）
