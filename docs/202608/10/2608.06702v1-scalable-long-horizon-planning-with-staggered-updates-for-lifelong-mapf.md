---
title: Scalable Long-Horizon Planning with Staggered Updates for Lifelong MAPF
title_zh: 针对终身多智能体路径规划的具有交错更新的可扩展长时界规划
authors: "Vaibhav Sanjay, Jiaoyang Li"
date: 2026-08-07
pdf: "https://arxiv.org/pdf/2608.06702v1"
tags: ["query:q1"]
score: 8.0
evidence: 实时约束下的无碰撞路径
tldr: 针对终身多智能体路径规划（LMAPF）中大规模智能体与长时域推理的权衡问题，本文提出PUSH算法。该方法结合了PIBT的扩展性、RHCR的长时域规划和TP的交错更新机制，通过在通用地图上采用交错规划窗口和优先级继承策略，实现了在秒级内协调万级智能体，显著提升了复杂环境下的系统吞吐量。
selection_source: fresh_fetch
motivation: 现有的LMAPF算法在处理大规模智能体时，要么因视野受限导致效率低下，要么因计算开销过大而难以扩展到通用地图。
method: 提出PUSH算法，通过交错规划窗口减少每步计算量，并集成优先级继承与回溯机制以在通用地图上实现高效的长时域路径规划。
result: 实验表明PUSH能支持多达1万个智能体，且在需要长时域推理的现实场景中，其系统吞吐量显著优于EPIBT和RHCR等基准方法。
conclusion: PUSH成功解决了大规模LMAPF中实时性与长时域规划的矛盾，为复杂环境下的超大规模多智能体调度提供了高效的解决方案。
---

## 摘要
终身多智能体路径规划 (LMAPF) 要求在严格的实时约束下为大规模智能体集群生成无碰撞路径。诸如 PIBT 和增强型 PIBT (EPIBT) 等反应式框架通过基于规则的逐步协调，可以轻松扩展到数千个智能体，但存在严重的时间近视问题，导致它们在需要长时界推理的场景中效果不佳。RHCR 在多步时界内规划窗口化路径，但会产生巨大的规划开销，从而阻碍了可扩展性。TP 通过在每个时间步仅规划智能体子集来应对这两个挑战，但其适用性仅限于高度结构化的地图。为了在通用地图上实现大规模长时界规划，我们提出了交错时界路径更新 (PUSH)，这是一种 LMAPF 规划器，能够在不到一秒的时间内协调数千个智能体，同时进行多步时界规划。PUSH 结合了 PIBT、RHCR 和 TP 的主要优点。与 TP 类似，PUSH 通过使用交错规划窗口在每个时间步仅规划一部分智能体来降低计算复杂度。然而，与 TP 不同的是，PUSH 在通用地图中规划 RHCR 风格的窗口化路径，而不依赖于限制性的地图假设。为了在拥挤环境中保持高吞吐量，PUSH 进一步将受 EPIBT 启发的优先级继承、回溯和随时改进集成到其窗口化规划中。在两个需要长时界推理的真实 MAPF 场景中的实证评估表明，PUSH 可以扩展到与 EPIBT 相同的大规模智能体负载（例如 10,000 个智能体），同时实现比所有基准方法显著更高的系统吞吐量。

## Abstract
Lifelong Multi-Agent Path Finding (LMAPF) requires generating collision-free paths for large agent fleets under strict real-time constraints. Reactive frameworks such as PIBT and Enhanced PIBT (EPIBT) scale effortlessly to thousands of agents through rule-based, step-by-step coordination but suffer from severe temporal myopia, making them ineffective in scenarios where long-horizon reasoning is essential. RHCR plans windowed paths over multi-step horizons but incurs substantial planning overheads that hinder scalability. TP tackles both challenges by planning only subsets of agents at each timestep, yet its applicability is restricted to highly structured maps. To achieve long-horizon planning at scale across general maps, we propose Path Updates over Staggered Horizons (PUSH), a LMAPF planner capable of coordinating thousands of agents in under a second while planning over multi-step horizons. PUSH combines the key advantages of PIBT, RHCR, and TP. Like TP, PUSH reduces computational complexity by planning only a subset of agents at each timestep using staggered planning windows. Unlike TP, however, PUSH plans RHCR-style windowed paths in general maps without relying on restrictive map assumptions. To maintain high throughput in congested environments, PUSH further integrates EPIBT-inspired priority inheritance, backtracking, and anytime improvements into its windowed planning. Empirical evaluations across two realistic MAPF scenarios requiring long-horizon reasoning show that PUSH scales to the same massive agent loads as EPIBT (e.g., 10k agents) while achieving significantly higher system throughput than all baselines.

---

## 论文详细总结（自动生成）

以下是对论文《Scalable Long-Horizon Planning with Staggered Updates for Lifelong MAPF》的结构化总结：

### 1. 核心问题与整体含义
*   **核心问题**：如何在保证**实时性**（秒级响应）的前提下，为**大规模**（万级智能体）的终身多智能体路径规划（LMAPF）提供具有**远见**（长时界推理）的协调方案。
*   **研究背景**：
    *   **反应式方法（如PIBT/EPIBT）**：扩展性极强，但因“近视”无法处理长走廊或任务停留（TCT）导致的拥堵。
    *   **窗口化方法（如RHCR）**：具有远见，但因每个窗口需重新规划所有智能体，计算开销巨大，难以扩展。
    *   **子集规划（如TP）**：通过只规划部分智能体来提速，但仅限于特定结构的地图。
*   **整体含义**：本文提出 **PUSH** 算法，旨在打破“可扩展性”与“长时界推理”之间的权衡，实现通用地图上的大规模高效调度。

### 2. 方法论
PUSH 结合了子集规划的效率、窗口规划的远见以及反应式算法的冲突解决机制：
*   **交错更新（Staggered Updates）**：借鉴 TP 思想，不要求所有智能体同步规划。通过 `time2Replan` 计数器，让智能体在不同时间步交错触发规划请求，从而平滑计算负载。
*   **窗口化路径规划**：在 $W$ 步的时空窗口内进行规划，而非仅看下一步。智能体维护一套全局安全的“回退路径”（Fallback Paths），确保任何时候都有可行解。
*   **优先级继承推送（Priority Inheritance Pushing）**：
    *   集成 EPIBT 的递归机制：高优先级智能体在规划 $W$ 步路径时，若与低优先级智能体冲突，可“推送”对方重新规划。
    *   **改进的时空 A***：允许路径与最多一个低优先级智能体碰撞以触发推送，并使用约束集（Constraints）处理回溯，避免穷举搜索。
*   **随时优化（Anytime LNS）**：在完成基础规划后的剩余时间内，利用大邻域搜索（LNS）随机选择智能体进行路径改进，提升系统整体吞吐量。

### 3. 实验设计
*   **测试场景**：
    1.  **TCT 场景**：在随机地图、分拣地图、大型/小型仓库地图中设置任务完成时间（TCT=20），模拟装卸货导致的路径占用。
    2.  **复杂拓扑场景**：使用包含长走廊和死胡同的 Symbotic 和 Delivery 真实工业地图。
*   **基准方法（Benchmarks）**：
    *   **PIBT / EPIBT-LNS**：代表最先进的反应式/增强反应式方法。
    *   **RHCR-PBS**：代表最先进的窗口化规划方法。
    *   **PUSH-lite**：消融版本，去除了优先级推送机制。
*   **评估指标**：系统吞吐量（每步完成的任务总数）、平均决策时间。

### 4. 资源与算力
*   **硬件环境**：AMD Threadripper 3990X (64核处理器), 192 GB RAM。
*   **算力分配**：
    *   所有算法在每个时间步的规划预算为 **1 秒**。
    *   RHCR 由于每 $K$ 步规划一次，给予其 $K$ 秒的累计预算以示公平。
*   **实现**：基于 C++ 开发。文中未提及使用 GPU，MAPF 规划通常依赖 CPU 的逻辑运算。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从数百到 **10,000** 个智能体的极端规模测试。
*   **实验类型**：
    *   不同地图类型的对比实验（6种地图）。
    *   **消融实验**：验证了优先级推送机制的必要性（PUSH vs PUSH-lite）。
    *   **参数敏感性分析**：测试了不同 TCT 时间（0, 10, 20）和不同窗口大小 $W$ 对性能的影响。
    *   **运动模型扩展**：在旋转运动模型（Rotation Motion Model）下验证了算法的鲁棒性。
*   **充分性**：实验设计客观且全面，覆盖了从理论边界到实际工业布局的多种情况，对比基准涵盖了当前主流的各类范式。

### 6. 主要结论与发现
*   **性能飞跃**：在需要长时界推理的场景中，PUSH 的吞吐量显著优于所有基准。在某些随机地图中，吞吐量比 EPIBT-LNS 高出 **300%**。
*   **极高扩展性**：PUSH 是首个能在通用地图上协调 **10,000** 个智能体并进行长时界规划的算法。
*   **长时界的重要性**：实验证明，在有 TCT 或长走廊的环境下，仅靠反应式规划（PIBT）会导致严重的排队和死锁，而 PUSH 的 $W$ 步预见性有效缓解了这一问题。
*   **理论保证**：证明了 PUSH 在有限时间内终止，且在特定条件下（双连通图、TCT=0）具有无死锁保证。

### 7. 优点
*   **架构融合**：巧妙地将子集规划的低开销与窗口规划的高质量结合，解决了大规模 LMAPF 的实时性难题。
*   **通用性强**：不像 TP 算法那样依赖“良构地图”（Well-formed maps），PUSH 可直接应用于任何通用图结构。
*   **鲁棒性**：通过优先级继承和回溯机制，在极度拥挤的环境下仍能保持高成功率。

### 8. 不足与局限
*   **推送限制**：目前算法限制一个智能体只能推送“一个”低优先级智能体，在极度密集的瓶颈区域，这可能限制了更优解的搜索空间。
*   **死锁风险**：虽然理论上在 TCT=0 时无死锁，但在现实的 TCT > 0 场景中，仍存在因目标点被永久占用而导致死锁的潜在风险（这是 LMAPF 领域的共同挑战）。
*   **超参数依赖**：窗口大小 $W$ 和执行步数 $K$ 需要根据地图手动调整，缺乏自动化的参数自适应机制。

（完）
