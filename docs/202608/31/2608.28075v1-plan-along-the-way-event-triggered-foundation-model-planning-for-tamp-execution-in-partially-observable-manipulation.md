---
title: "Plan Along the Way: Event-Triggered Foundation-Model Planning for TAMP Execution in Partially Observable Manipulation"
title_zh: 边走边规划：部分可观测操作中用于 TAMP 执行的事件触发基础模型规划
authors: "Puru Ojha, Narendhiran Vijayakumar, Nav Singhal, Girish Varma, Antony Thomas"
date: 2026-08-28
pdf: "https://arxiv.org/pdf/2608.28075v1"
tags: ["query:profile-1"]
score: 8.0
evidence: 针对操作任务的反应式任务与运动规划
tldr: 针对部分可观测环境下的机器人操作，本文提出 ROBUST TAMP 框架，利用 LLM/VLM 进行反应式任务与运动规划。该框架仅依赖当前可见场景状态，并将新物体的发现视为触发重规划的关键事件，而非仅在失败时重规划。在 RLBench 仿真环境的复杂厨房任务中，该方法展示了在处理隐藏物体、非目标物体及长程操作任务中的卓越鲁棒性和执行效率。
selection_source: fresh_fetch
motivation: 现有 TAMP 系统多假设场景信息完全已知，难以应对执行过程中因物体发现或环境变化导致的信息不完整问题。
method: 开发了一个模块化框架，通过可见场景状态约束规划器，并引入事件触发机制，将物体发现与执行稳定期作为重规划的触发点。
result: 在六种涉及隐藏物体和复杂交互的仿真任务中，该框架在任务成功率、非目标物体处理及推理成本平衡方面表现优异。
conclusion: 将基础模型的推理能力与事件驱动的重规划机制相结合，能显著提升机器人在部分可观测环境下的自主操作能力。
---

## 摘要
在部分可观测环境中的操作需要在场景信息不完整的情况下进行规划。在这种设置下，最初有效的计划可能执行成功，但仍不足以完成任务。现有的基础模型引导的任务与运动规划（TAMP）系统可以生成有用的长时程任务分解、子目标或约束，但它们通常假设可以访问完全指定的场景状态，或者在子目标、细化或执行尝试失败后调用模型级重规划。我们提出了 ROBUST TAMP，这是一个模块化的 LLM/VLM 引导的规划框架，用于反应式 TAMP，其中未见的任务相关对象和非目标对象可能在执行过程中变得可见。该框架将基础模型规划器限制在当前可见的关系场景状态，根据严格的可执行接口验证生成的任务级动作，并将接受的动作路由到特定场景的执行适配器。物体发现被视为一个独特的重规划事件，在稳定的执行时程之后，系统会重建可见场景状态，并利用已完成的动作历史和结构化的重规划事件上下文进行重规划。评估在六个 RLBench/CoppeliaSim 厨房和烤架变体上进行，涉及隐藏物体、非目标物体发现、关节式容器交互和时序操作程序。我们在相同的验证、执行、监控和重规划流水线下比较了不同规模的纯文本 LLM 和 VLM 规划器，报告了任务成功率、部分目标完成情况、发现和失败触发的重规划行为、隐式非目标物体处理以及规划器推理成本。

## Abstract
Manipulation in partially observable environments requires planning under incomplete scene information. In such settings, an initially valid plan may execute successfully yet remain insufficient for task completion. Existing foundation-model-guided task and motion planning (TAMP) systems can generate useful long-horizon task decompositions, subgoals, or constraints, but they often assume having access to a fully specified scene state or invoke model-level replanning after a subgoal, refinement, or execution attempt fails. We present ROBUST TAMP, a modular LLM/VLM-guided planning framework for reactive TAMP where unseen task-relevant and non-target objects may become visible during execution. The framework restricts the foundation-model planner to the currently visible relational scene state, validates generated task-level actions against a strict executable interface, and routes the accepted actions to scene-specific execution adapters. Object discovery is treated as a distinct replanning event and, after a stable execution horizon, the system reconstructs the visible scene state and replans using completed-action history and structured replanning event context. Evaluations are performed on six RLBench/CoppeliaSim kitchen and grill variants involving hidden objects, non-target object discovery, articulated-container interaction, and temporal manipulation procedures. We compare text-only LLM and VLM planners of different sizes under the same validation, execution, monitoring, and replanning pipeline, reporting task success, partial goal completion, discovery- and failure-triggered replanning behavior, implicit non-target-object handling, and planner inference cost.

---

## 论文详细总结（自动生成）

这是一份关于论文《Plan Along the Way: Event-Triggered Foundation-Model Planning for TAMP Execution in Partially Observable Manipulation》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：在现实世界的机器人操作中，环境往往是“部分可观测”的（例如物体被遮挡或放在关闭的容器内）。传统的任务与运动规划（TAMP）系统通常假设场景信息完全已知，或者仅在执行失败时才进行重规划。
*   **核心问题**：当机器人通过交互发现新物体（如打开盖子发现内部有食材）时，原有的“有效计划”虽然执行成功，但已不足以完成最终目标。论文旨在解决如何在执行过程中动态整合新发现的信息，实现鲁棒的长程操作。

### 2. 方法论：ROBUST TAMP 框架
*   **核心思想**：提出一种模块化的、由基础模型（LLM/VLM）引导的反应式规划框架，其核心特征是将“物体发现”视为与“执行失败”同等地位的**第一类重规划触发事件**。
*   **关键技术细节**：
    *   **可见场景状态构建**：规划器仅接收当前视野内物体的关系状态（如 `on(mug, table)`），不假设未见物体的存在，避免模型产生幻觉。
    *   **事件触发机制**：系统在每个动作执行后监控环境。若发现新物体（`O_new`）或遇到无法恢复的执行失败，立即中断当前序列，重建场景状态并请求模型重规划。
    *   **分层验证与失败升级**：
        *   **L1 层（语法/逻辑）**：在调用执行器前，验证模型生成的动作序列是否符合接口规范（如是否在没拿东西时尝试放置）。
        *   **L2 层（场景/运动）**：执行适配器处理具体的运动规划（如 RRT*），若遇到碰撞或逆运动学（IK）无解，先尝试局部恢复（如换个抓取位姿），失败后再升级为模型级重规划。
    *   **算法流程**：感知 -> 构建可见状态上下文 $c_k$ -> 模型生成动作序列 $\alpha_k$ -> 结构验证 -> 执行与闭环监控 -> 触发事件（发现/失败） -> 循环。

### 3. 实验设计
*   **实验场景**：基于 RLBench 和 CoppeliaSim 仿真环境，设计了 6 个复杂的变体场景：
    *   **厨房系列 (K1-K3)**：涉及打开容器、移动遮挡物、处理隐藏在容器内的杂货。
    *   **烤架系列 (G1-G3)**：涉及复杂的时序逻辑（烹饪肉类需先放进去、关盖、开盖、取出）以及处理非目标物体（如烤架上放着一部手机，需先移走）。
*   **Benchmark 与对比方法**：
    *   **模型规模**：对比了 Qwen3 系列的 4B（小）、8B（中）、32B（大）三种规模。
    *   **模态对比**：对比了纯文本 LLM 和多模态 VLM。
    *   **提示策略**：对比了 Zero-shot（零样本）和 ICL（带示例的上下文学习）。
*   **评价指标**：任务成功率 (TSR)、部分目标完成度 (PGC)、隐式非目标处理 (INH) 以及推理延迟。

### 4. 资源与算力
*   **硬件环境**：实验使用了 **2 台 NVIDIA RTX 4090 GPU**。
*   **软件框架**：模型通过 Hugging Face 库部署，作为远程规划服务器运行。
*   **训练/推理时长**：文中未提及模型训练时长（使用的是预训练模型），但详细记录了推理延迟：8B 模型单次调用约 2-5 秒，32B 模型则显著更高。

### 5. 实验数量与充分性
*   **实验规模**：每个模型在每个变体场景下进行了 **10 次独立重复实验**，总计进行了数百次完整的任务尝试。
*   **充分性与公平性**：
    *   实验涵盖了从 4B 到 32B 的跨度，对比了模态和提示词的影响，维度较为全面。
    *   所有模型共用同一套感知、验证和执行后端，确保了对比的客观性。
    *   引入 PGC 指标能够量化即便任务失败时的进度，比单一的成功率更具信息量。

### 6. 主要结论与发现
*   **重规划的必要性**：在隐藏物体场景下，若无事件触发重规划，所有模型的成功率均为 0。ROBUST TAMP 使 8B 以上模型的平均成功率达到 85% 以上。
*   **规模与性能的平衡**：**8B 模型是“甜点级”选择**，其成功率与 32B 模型相当，但推理延迟和计算成本大幅降低。
*   **ICL 的关键作用**：上下文学习显著提升了小模型（4B）的逻辑能力（从 0% 提升至 100% 成功率），并帮助大模型理解如何处理非目标物体（如主动移走手机）。
*   **模态差异**：在提供结构化状态描述的情况下，VLM 相比 LLM 并没有表现出显著的规划优势，反而增加了推理延迟。

### 7. 优点
*   **反应式规划**：通过将物体发现形式化为重规划事件，解决了部分可观测环境下的长程任务连续性问题。
*   **分层架构**：将底层的运动规划失败与高层的语义逻辑错误分离，减少了昂贵的模型调用次数。
*   **鲁棒性**：能够处理非目标物体的干扰，展示了基础模型在处理非预设约束时的常识推理能力。

### 8. 不足与局限
*   **感知假设**：实验中使用了仿真器的分割掩码作为感知输入，简化了真实世界中视觉噪声和识别错误带来的挑战。
*   **非目标物体推理**：虽然 ICL 有所帮助，但模型在 Zero-shot 下往往会忽略非目标物体，这可能在复杂现实场景中导致碰撞或任务停滞。
*   **应用限制**：目前仅在仿真环境中验证，尚未在具有传感器不确定性和动力学误差的真实物理机器人上进行测试。

（完）
