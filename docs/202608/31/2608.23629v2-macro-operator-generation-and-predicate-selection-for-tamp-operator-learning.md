---
title: Macro-Operator Generation and Predicate Selection for TAMP Operator Learning
title_zh: 针对 TAMP 算子学习的宏算子生成与谓词选择
authors: "Can Emir Bora, Emre Ugur"
date: 2026-08-23
pdf: "https://arxiv.org/pdf/2608.23629v2"
tags: ["query:q6"]
score: 8.0
evidence: 针对操作任务的任务与运动规划 (TAMP)
tldr: 针对任务与运动规划（TAMP）中手动编写符号算子困难及长序列任务搜索效率低的问题，本文提出了一种自动生成宏算子和谓词选择的方法。该系统通过从演示数据中识别因果关联的动作序列并将其压缩为复合动作，同时剔除无关谓词，显著缩短了规划步长并减小了状态空间。实验表明，该方法在多个领域实现了显著的规划加速，并能解决基准方法无法处理的长序列任务。
selection_source: fresh_fetch
motivation: 手动设计TAMP算子耗时且现有学习方法在处理长序列任务时因缺乏多步结构捕捉而导致搜索效率低下。
method: 通过从训练数据中发现因果关联的动作对生成宏算子，并剪枝掉所有未被学习算子引用的谓词以优化符号状态评估。
result: 在四个TAMP领域中，该方法相比基准方法实现了最高4.6倍的规划加速，并成功解决了基准方法无法处理的长序列任务。
conclusion: 宏算子发现与谓词选择不仅能显著提升规划速度，在处理复杂长序列任务时更是决定任务是否可解的关键。
---

## 摘要
手动创建符号算子是部署任务与运动规划（TAMP）系统的主要瓶颈之一。最近的研究表明，这些算子可以直接从演示数据中学习。然而，现有方法通常孤立地学习每个动作，无法捕捉操纵任务中反复出现的多步结构，导致在长序列任务上的搜索变得难以处理。符号状态中还存在另一种低效：每个提供的谓词都会在每个搜索节点上进行评估，即使它从未出现在任何已学习的算子中。我们提出了一个同时解决这两个问题的系统。其核心组件是宏算子的自动生成，宏算子是将反复出现的单个动作序列压缩为单个规划步骤的复合动作。我们的系统直接从训练数据中发现具有因果关联的动作对（其中一个动作恰好产生下一个动作所需的条件），并将每一对动作转换为一个新的算子。与此同时，我们的系统会剪枝掉所有未被已学习算子引用的谓词，从而缩小了在每个搜索节点评估的符号状态。这些改进共同缩短了有效的规划时域，且其带来的收益随任务长度的增加而增长。在四个 TAMP 领域中，与基准方法（即 Learning Operators for TAMP）相比，我们的方法实现了高达 4.6 倍的规划加速。更重要的是，它解决了一个基准方法无法解决的长序列任务。因此，宏算子发现不仅加速了规划，而且在某些领域中决定了实际应用中的可解性。

## Abstract
Creating symbolic operators by hand is one of the main bottlenecks in deploying Task and Motion Planning systems (TAMP). Recent works show that these operators can instead be learned directly from demonstration data. Existing methods, however, typically learn each action in isolation and cannot capture the recurring multi-step structure of manipulation tasks, so the search becomes intractable on long sequential tasks. A further inefficiency arises in the symbolic state: every provided predicate is evaluated at every search node, even when it never appears in any learned operator. We present a system that addresses both problems together. Its central component is the automatic generation of macro-operators, composite actions that compress a recurring sequence of individual actions into a single planning step. Our system discovers causally linked action pairs directly from the training data, where one action produces exactly the condition that the next one requires, and turns each pair into a new operator. Alongside this, our system prunes every predicate that no learned operator references, which shrinks the symbolic state evaluated at each search node. Together, these changes shorten the effective planning horizon, and the benefit they bring grows with the length of the task. Across four TAMP domains, our method reaches up to a 4.6x planning speedup compared to the baseline method, namely Learning Operators for TAMP. More importantly, it solves a long sequential task that the baseline cannot solve. Macro-operator discovery thus not only accelerates planning but, in certain domains, determines solvability in practice.

---

## 论文详细总结（自动生成）

### 针对 TAMP 算子学习的宏算子生成与谓词选择论文总结

#### 1. 论文的核心问题与整体含义
*   **研究背景**：任务与运动规划（TAMP）结合了高层符号推理和底层连续几何决策。传统的 TAMP 系统高度依赖人工编写的符号算子（如 PDDL），这不仅费时费力，且难以覆盖复杂环境。
*   **核心问题**：虽然现有方法（如 LOFT）可以从演示数据中学习算子，但存在两个主要瓶颈：
    1.  **孤立学习**：将每个动作视为独立个体，无法捕捉多步操作中的重复结构，导致在长序列任务中搜索空间爆炸。
    2.  **谓词冗余**：在每个搜索节点都会评估所有提供的谓词，即使某些谓词从未出现在学习到的算子中，造成了计算浪费。
*   **研究意义**：通过自动生成宏算子（Macro-Operators）和精简谓词，缩短规划时域并优化搜索效率，使机器人能够处理更长、更复杂的任务。

#### 2. 论文提出的方法论
该系统主要由两个核心组件组成：**宏算子生成**和**迭代谓词选择（IPS）**。

*   **宏算子生成（四阶段流程）**：
    1.  **一致性效果分析**：从演示数据中识别每个动作类型一致添加或删除的谓词。
    2.  **因果对识别**：通过“效果互补分析”，寻找动作对 $(a_i, a_{i+1})$，其中 $a_i$ 产生的谓词恰好被 $a_{i+1}$ 消耗（删除）。
    3.  **验证与冲突解决**：统计动作对在数据中出现的频率，保留高频且无冲突的因果对。
    4.  **构建宏算子**：进行参数统一（Argument Unification），将两个动作的参数合并，并生成合成的负样本以供后续的算子学习模块使用。
*   **顺序采样器（Sequential Sampler）**：为宏算子设计了专门的底层采样机制，先采样第一个动作的参数，模拟后再根据中间状态采样第二个动作的参数，确保几何可行性。
*   **迭代谓词选择（IPS）**：在算子学习完成后，扫描所有已学习算子的前提条件和效果，剔除所有未被引用的谓词。这减小了符号状态的大小，降低了 A* 搜索中每个节点的解析开销。

#### 3. 实验设计
*   **实验场景**：
    1.  **Cover**：简单的放置任务（2-4 步）。
    2.  **Blocks**：经典的积木堆叠任务（约 10 步）。
    3.  **Painting**：涉及物体属性（颜色、干湿）的复杂任务（约 32 步）。
    4.  **Kitchen（新引入）**：极长序列任务，包含 7 种食材，每种需经过抓取、切碎、烹饪、装盘四个阶段（超过 50 步）。
*   **Benchmark 与对比方法**：
    *   **基准方法**：LOFT（Learning Operators for TAMP），即不带宏算子和谓词选择的原始学习框架。
    *   **对比指标**：规划成功率、规划时间、计划长度、算子/谓词数量。

#### 4. 资源与算力
*   **硬件环境**：论文未明确提及具体的 GPU 或 CPU 型号。
*   **软件环境**：使用 PyBullet 作为物理仿真后端。
*   **训练时长**：论文给出了训练时间数据，显示该方法非常高效。在 Blocks 领域训练约 0.196 秒，最复杂的 Painting 领域也仅需约 3.9 秒。这表明该方法的计算开销主要在离线学习阶段，且时间极短。

#### 5. 实验数量与充分性
*   **实验规模**：每个领域使用了 20 个测试问题，并进行了 5 个独立随机种子的重复实验（共 100 次运行/领域）。
*   **消融实验**：专门针对 Blocks 和 Kitchen 领域进行了消融实验，分别验证了宏算子和 IPS 对性能的贡献。
*   **充分性评价**：实验设计较为充分。通过引入 Kitchen 这一长时域领域，清晰地展示了宏算子在处理基准方法无法解决的任务时的必要性。对比实验涵盖了从简单到复杂的梯度，结果具有客观性。

#### 6. 论文的主要结论与发现
*   **规划加速**：在 Blocks 领域实现了约 **4.6 倍** 的规划加速。
*   **解决长序列任务**：在 Kitchen 领域，基准方法在 30 分钟内无法找到解，而本文方法仅需 **0.05 秒** 即可完成规划，成功率达 100%。
*   **谓词精简**：IPS 有效减少了 20%-40% 的冗余谓词评估，降低了搜索节点的固定开销。
*   **权衡关系**：在极短任务（如 Cover）中，宏算子增加的决策分支可能会略微增加规划时间，但随着任务长度增加，宏算子的收益呈指数级增长。

#### 7. 优点
*   **自动化程度高**：无需人工干预即可从原始转换数据中发现有意义的复合动作（如“抓取并堆叠”）。
*   **显著提升长程规划能力**：通过压缩规划时域，将原本不可解的长序列问题转化为可解问题。
*   **通用性强**：提出的宏算子生成逻辑不依赖于特定领域，可扩展至多种 TAMP 场景。

#### 8. 不足与局限
*   **宏算子长度限制**：目前仅考虑了两个连续动作的组合，尚未探索更长链条（如三步或四步动作）的自动折叠。
*   **离线学习限制**：系统采用离线批量学习，无法在机器人运行过程中实时增量式地学习新算子。
*   **仿真与现实差距**：所有实验均在 PyBullet 仿真中完成，未在真实机器人上验证感知噪声和执行误差对学习算子的影响。
*   **缺乏谓词发明**：IPS 只能剔除已有谓词，无法像某些先进方法那样根据传感器数据自动发明（Invention）新的符号谓词。

（完）
