Title: Compiling Spatial Certificates into Temporal Contracts for Latency-Aware Control

URL Source: https://arxiv.org/pdf/2608.25228v1

Published Time: Thu, 27 Aug 2026 00:18:59 GMT

Number of Pages: 6

Markdown Content:
# Compiling Spatial Certificates into Temporal Contracts for Latency-Aware Control 

Avinash Malik 

Abstract — We introduce CIPS, a contract-driven execution abstraction for managing computational latency and sampled-data updates in safety-critical cyber-physical systems (CPS). A fundamental challenge in real-time control is that physical safety certificates are defined spatially, yet predicting their validity under non-zero computation and handoff latency re-quires online numerical integration of plant dynamics. CIPS resolves this operational dichotomy by systematically compiling heterogeneous spatial safety certificates into normalized, unit-rate temporal contracts entirely offline. This transformation abstracts complex plant dynamics, exposing a deterministic, 

O(1) temporal budget to a generic sampled-data scheduler. We formally prove that this architecture guarantees global hybrid safety invariance under bounded computational latency and asynchronous execution. Finally, we validate the framework via an autonomous vehicle braking benchmark, demonstrating a 60 .5× reduction in micro-architectural evaluation overhead compared to latency-aware event-triggered control (LA-ETC) while preserving safety bounds. 

I. I NTRODUCTION 

Modern safety-critical Cyber-Physical Systems (CPS) [1] increasingly rely on formal spatial certificates, such as Con-trol Barrier Functions (CBFs) [2] and Hamilton-Jacobi reach-ability sets [3], to guarantee operational correctness. How-ever, continuously evaluating these certificates and synthe-sizing updated control laws is computationally prohibitive, necessitating discrete, sampled-data execution. This intro-duces a fundamental divergence between cyber execution and physical dynamics: a control policy and its associated certificate become computationally stale during the latency period required to generate a new input, even while the physical plant remains safe .Consider a practical motivating scenario: a controller is certified safe for a vehicle state during Adaptive Cruise Control (ACC), but its certificate is only periodically re-computed. A regeneration request, such as computing a new control law, may take 20 ms to compute and verify, while the current certificate has only 19 ms of certified remaining validity. A standard naive scheduler that knows only that the certificate is currently valid as a Boolean fact cannot determine whether regeneration can complete safely before validity expires, leading to safety violations. Conversely, an advanced scheduler could predict this deadline dynamically by numerically integrating the plant’s future states online (e.g., Latency-Aware ETC [4]). However, this not only introduces a severe runtime computational bottleneck, but it also fundamentally assumes the scheduler has access to               

> Avinash Malik is with Department of Electrical, Computer, and Software Engineering, University of Auckland, New Zealand

avinash.malik@auckland.ac.nz 

a perfectly specified, continuous plant model at runtime, in-formation that is often underspecified or entirely unavailable to the scheduling middleware. CIPS resolves this dichotomy. It exposes this missing quantity as a consumable temporal persistence budget, eliminating the need for online plant simulations by relying on contracts compiled entirely offline. CIPS provides a rigorously defined runtime scheduling ab-straction to manage this information staleness. CIPS does not model temporal persistence of the information representation itself. Rather, it models the persistence of the certificate’s validity under continued execution. CIPS does not replace existing control theories; rather, CIPS acts as an information interface. CIPS does not make the plant model unnecessary for certification; it makes the plant model unnecessary for the runtime scheduling decision once a sound temporal persistence contract has been compiled offline. The central novelty of this work is the architectural pipeline: transforming an offline spatial certificate into a normalized temporal persistence contract, which is then con-sumed by a plant-model-independent runtime scheduler. We explicitly make the following three contributions spanning from the offline certificate to global hybrid invariance: 1) Persistence Compilation: A formal compiler map-ping certificate degradation inequalities into normal-ized unit-rate temporal persistence contracts. 2) Contract-Based Scheduling: A plant-model-independent sampled runtime scheduler that explicitly accounts for regeneration latency, asynchronous completion, rejection, and terminal fallback. 3) Safety Composition Theorem: A global hybrid in-variance theorem showing that the above contracts compose under explicit handoff and offline fallback compatibility conditions.                                  

> TABLE I CIPS I NFORMATION FLOW AND HIERARCHICAL PROOF DEPENDENCIES
> Hierarchy Stage Data Transformation 1. Certificate Compiler In: Spatial hI(x), Decay α(s)
> Out: Temporal Contract ΦI(x)
> 2. CIPS Contract In: Temporal Contract ΦI(x)
> Out: Valid Bound Φ≤τ∗
> 3. Latency Safety In: Bound Φ≤τ∗, Limits ( Lreg ,∆t)
> Out: Safe Handoff or Fallback
> 4. Global Hybrid Safety In: Safe Handoff or Fallback
> Out: Certified System Safety

Table I outlines the hierarchical information flow and mathematical proof dependencies within the CIPS frame-work. It illustrates how continuous physical dynamics and arXiv:2608.25228v1 [eess.SY] 25 Aug 2026 spatial bounds are systematically abstracted into discrete temporal guarantees, culminating in global system safety. The pipeline begins with the Certificate Compiler (Stage 1), detailed in Section III, which maps a physical spatial certificate hI (x) and its continuous decay bound α(s) into a normalized temporal contract ΦI (x). This functional forms the basis of the CIPS Contract (Stage 2), formalized in Section II, establishing that the certified persistence acts as a valid, conservative lower bound to the exact physical horizon (ΦI (x) ≤ τ ∗). By shifting the abstraction from spatial states to temporal budgets, the framework enables Latency Safety (Stage 3). As analyzed in Sections IV and V, the runtime scheduler consumes this temporal bound alongside discrete system limits ( Lreg , ∆t) to evaluate trigger margins and guarantee safe handoffs or terminal fallbacks. Finally, Stage 4 achieves Global Hybrid Safety by composing these latency-aware transitions into an indefinite system-wide safety invariant, which is mathematically proved in Section V and operationally managed by the sampled event-triggered engine in Section IV. II. T HE CIPS C ONTRACT AND INFORMATION OBJECTS 

We first define the abstract interface that any valid CIPS implementation must satisfy. Each closed-loop vector field fI

is assumed to admit a unique forward continuous trajectory. 

Definition 2.1 (Information Object and Induced Trajectory): 

An information object Ik ∈ I encapsulates a specific set of parameters and control policies. While Ik is actively executing, the physical plant evolves according to ˙x = fIk (x). We denote the trajectory obtained by continuing the immutable object I from state x0 at time t0

as xI (t0 + τ ; x0).

Definition 2.2 (Physical Safety Horizon): Each object I

is associated with a ground-truth physical admissible region 

A∗ 

> I

⊆ X . For a state x evaluated at time t0, the exact physical safety horizon is: 

τ ∗(x; I) := inf {τ ≥ 0 | xI (t0 + τ ; x) /∈ A ∗ 

> I

}. (1) For any boundary or external state x / ∈ A ∗ 

> I

, τ ∗(x; I) = 0 .Because computing τ ∗ online is generally unavailable to the runtime scheduler, CIPS relies on a one-sided, con-servative certified remaining-time lower bound , termed the persistence functional. 

Definition 2.3 (Certified Persistence Functional): A valid CIPS system provides a computable persistence functional 

ΦI : X → R with ΦI ∈ C(X ), which intrinsically defines its certified persistence domain as DI := {x ∈ X : Φ I (x) ≥ 0}.This functional satisfies the following two axioms for all active objects I: 

> •

Axiom 1 (Certificate Soundness): ΦI (x) ≥ 0 =⇒

x ∈ A ∗ 

> I

. 

> •

Axiom 2 (Temporal Persistence): For every trajectory starting from x(t0) ∈ D I ,

ΦI (x(t0 + s)) ≥ ΦI (x(t0)) − s (2) for every s ∈ [0 , t e] up to the first exit time te from DI .

Remark 2.1: All temporal inequalities are understood only while the trajectory remains inside the certified per-sistence domain. 

Lemma 2.1 (Certified Horizon): If ΦI satisfies the CIPS Contract, then 0 ≤ ΦI (x) ≤ τ ∗(x; I) whenever ΦI (x) ≥ 0.

Proof: Let ΦI (x(t0)) = r ≥ 0. By Axiom 2, the trajectory satisfies ΦI (x(t0 + s)) ≥ ΦI (x(t0)) − s = r − s.Therefore, for every 0 ≤ s < r , we strictly have ΦI (x(t0 +

s)) > 0. By Axiom 1, this implies x(t0 + s) ∈ D I ⊆ A ∗ 

> I

.Since the state remains strictly inside the physically safe set for at least r seconds, the exact physical horizon must satisfy 

τ ∗(x(t0); I) ≥ r = Φ I (x(t0)) .III. O FFLINE CERTIFICATE COMPILATION 

The CIPS contract acts as an abstract specification. The application-specific proof obligation is to construct a domain certificate and compile it into a persistence functional ΦI (x).

Theorem 3.1 (Certificate-to-Persistence Compilation): 

Let hI : X → R be a continuously differentiable domain certificate with a validity threshold hmin . Assume explicitly that the domain DhI := {x ∈ X : hI (x) ≥ hmin }

satisfies physical soundness: DhI ⊆ A∗ 

> I

. Assume trajectories x(t) are continuous and there exists afunction α ∈ C([ hmin , ∞); R>0) such that the closed-loop system satisfies ˙hI (x) ≥ − α(hI (x)) for all x ∈ D hI ,and R hI (x) 

> hmin
> ds α(s)

< ∞ ∀x ∈ D hI . Define the functional explicitly as: 

ΦI (x) = 

R hI (x) 

> hmin
> ds α(s)

, hI (x) ≥ hmin 

− dist( x, DhI ), hI (x) < h min 

(3) This guarantees that the continuous functional ΦI satisfies the CIPS Contract (Definition 2.3) on every trajectory up to the first exit from DhI .

Proof: By construction, ΦI (x) ≥ 0 ⇐⇒ hI (x) ≥

hmin =⇒ x ∈ D hI ⊆ A ∗ 

> I

, satisfying Axiom 1. For x / ∈ D hI ,the strictly negative distance penalization trivially ensures 

ΦI (x) < 0, yielding exact equivalence of the domains. For Axiom 2, while the trajectory remains inside DhI , applying the chain rule gives ˙ΦI (x(t)) = ˙hI (x(t))   

> α(hI(x(t)))

≥ − 1. Integrat-ing this differential inequality from t0 to t0 + s up to the first exit directly yields ΦI (x(t0 + s)) − ΦI (x(t0)) ≥ − s,satisfying Axiom 2. 

Remark 3.1: The integral transform is mathematically re-lated to finite-time stability analysis by Bhat and Bern-stein [5], but its role and resulting guarantee are different. Their construction transforms a Lyapunov decay inequality into a bound on the time required to reach a target equi-librium. Here, the transformation is applied to a safety-certificate degradation inequality and yields a conservative lower bound on the time remaining before the certificate reaches its validity boundary. ΦI (x) is subsequently exposed as a runtime contract, rather than merely being used as an analysis device for a particular controller. 

Proposition 3.1 (Closure under Finite Intersections): 

Assume the composite object Icomp induces a common vector field fIcomp = f under which multiple certificates Φisatisfy their temporal contracts along the same trajectory. Their composite object has an admissible region defined as A∗ 

> comp

:= T 

> i

A∗ 

> i

, and the single composite certificate defined as Φ( x) = min i Φi(x) is also CIPS-valid for A∗

> comp

.IV. R UNTIME ENGINE AND INTERFACE 

IMPLEMENTATION 

The runtime execution induces a hybrid event system modeled by the state tuple q = ( I, σ, x ). Here, I is the continuous active object, σ ∈ { idle , pending , terminal } is the discrete scheduler mode, and the physical state flows as 

˙x = fI (x).To ensure plant-model-independence, the scheduler utilizes the runtime contract CI =(OI , L reg , ∆t, τ op , ρ reset , Verify , I stop ). The oracle OI

evaluates persistence ΦI (x(t)) statically from current measurements, avoiding online numerical simulation. Additionally, τop (I) enforces a maximum operational age limit, decoupling policy-level deadlines from certificate-level physical safety. We define Lreg operationally as the worst-case regen-eration trigger-to-certified-handoff latency . This bound ex-plicitly encompasses computation, communication, verifica-tion, and handoff time: Lreg ≥ Lgeneration + Lcommunication +

Lverification + Lhandoff .The scheduler manages updates via sampled evaluations 

tk (at intervals ∆t) and asynchronous completions (at times 

tc), utilizing two distinct evaluation margins:  

> •

ρtrigger = Lreg + 2∆ t: The threshold to trigger regenera-tion (transitions from idle to pending ). The 2∆ t factor provides sampling-detection conservatism.  

> •

ρreset > ∆t: The minimum persistence required to accept a new object (resets from pending to idle ). Algorithm 1 operationalizes the state machine. Following initialization (lines 1–3), the engine enters an indefinite event loop, waking upon either an asynchronous completion or a sampled evaluation (line 5). Sampled events (lines 6–27) explicitly take precedence if both events co-occur, rigorously managing all critical safety bounds and regeneration triggers. The engine first checks for physical unsafety (lines 8–12) or a policy deadline violation coupled with insufficient buffer (lines 15–19); if either condition is met, it abandons the active object and breaks into the terminal fallback state. Provided the system remains safe, the scheduler evaluates the crossing and deadline thresholds (lines 22–25). If a threshold is crossed while the system is idle , it initiates the generation of a new object and transitions the state to pending 

(lines 22–25). Conversely, if an asynchronous generation completes while the system is pending (lines 28–35), the oracle evaluates the new candidate. If the candidate passes verification and meets the reset margin ( γhandoff ≥ ρreset ), the object is successfully handed off, and the system resets to the idle state. While Algorithm 1 guarantees strict safety by defaulting to a fallback state upon threshold violations, it does not guaran-tee regeneration liveness. Delayed or rejected candidates will 

Algorithm 1 Sampled Event-Triggered CIPS Runtime En-gine                                                                                                                                                    

> Require: Contract Tuple CI= ( OI, L reg ,∆t, τ op , ρ reset ,Verify , I stop )
> 1: Initialize: Iactive ←I0, status ←idle, tact ←0
> 2: γsamp last ← O I0(t0)
> 3: ρtrigger ←Lreg + 2∆ t
> 4: while status ̸=terminal do
> 5: Wait for Next Sample tkor Async Completion tc
> 6: if Event is Sample at tkthen
> 7: γk← O Iactive (tk)
> 8: if γk≤∆tthen
> 9: Engage Fallback: Switch controller to Istop
> 10: status ←terminal 11: break
> 12: end if
> 13: texpiry ←tact +τop (Iactive )
> 14: deadline cond ←(tk≥texpiry )
> 15: if deadline cond and γk≤Lreg + ∆ tthen
> 16: Engage Fallback: Switch controller to Istop
> 17: status ←terminal 18: break
> 19: end if
> 20: Cross k←(γsamp last > ρ trigger and γk≤ρtrigger )
> 21: DeadlineTrigger k←(deadline cond and γk> L reg + ∆ t)
> 22: if status =idle and (Cross kor DeadlineTrigger k)then
> 23: Inext ←StartRegeneration (Iactive )
> 24: status ←pending 25: end if
> 26: γsamp last ←γk
> 27: end if
> 28: if Event is Async Completion at tcand status =pending then
> 29: γhandoff ← O Inext (tc)
> 30: if Verify (Inext )and γhandoff ≥ρreset then
> 31: Iactive ←Inext ,tact ←tc
> 32: γsamp last ← ∞ ▷Sentinel to await first sample post-handoff 33: end if
> 34: status ←idle 35: end if
> 36: end while

simply cause the active object to safely deplete its persistence budget until fallback is engaged. V. L ATENCY -A WARE RUNTIME SCHEDULING AND 

GLOBAL SAFETY 

Having defined the discrete event-triggered states and hybrid mechanics of the runtime engine in Section IV, we now formally verify its global safety invariants based on the prescribed assumptions. 

Assumption 5.1 (Runtime Scheduling Semantics): The event-triggered architecture explicitly enforces the following conditions: 1) Bounded Latency & Periodicity: Lreg < ∞ and sampled events occur periodically every ∆t > 0.2) Active-Object Immutability: Iactive (t) remains strictly unchanged during a pending regeneration (both while being generated and upon rejection). 3) Handoff State Continuity: At every nominal transi-tion Iactive → Inext , x(t+

> sw

) = x(t−

> sw

).4) Verification Soundness: A candidate Inext is accepted only if its independently verified persistence satisfies 

ΦInext (x(t+

> sw

)) ≥ ρreset .5) Terminal Mode Safety: The terminal mode Istop has a domain Dstop ⊆ A ∗ 

> stop

that is forward invariant: x(t0) ∈Dstop =⇒ x(t) ∈ D stop ∀t ≥ t0.6) Fallback Instantaneousness: Switching to Istop occurs instantly upon violation of the ∆t sampled margin. 7) Event Priority (Tie-breaking): If a sampled event at tk and an asynchronous completion event at tc

occur simultaneously ( tk = tc), the sample event strictly takes precedence to ensure deterministic hybrid semantic resolution. 

Assumption 5.2 (Offline Fallback Compatibility): Define the reachable fallback switching set for an object I as 

Sfb ,I = {x : 0 < ΦI (x) ≤ ∆t}, and let Sfb = S 

> I

Sfb ,I . We require Sfb ⊆ D stop . Fallback compatibility is intentionally an offline verification obligation. It guarantees that whenever CIPS decides the active certificate is approaching expiry, the current state lies inside the terminal controller’s safe domain. 

Theorem 5.1 (Sampled Latency Safety): Under Assump-tion 5.1, if regeneration begins at a sampled instant tk

and ΦIactive (x(tk)) > L reg + ∆ t, then at any asynchronous completion time tc satisfying 0 ≤ tc − tk ≤ Lreg , the active object retains at least ∆t of certified persistence: 

ΦIactive (x(tc)) > ∆t.

Proof: At tc ≤ tk + Lreg , applying Axiom 2 yields 

ΦIactive (x(tc)) ≥ ΦIactive (x(tk)) − (tc − tk) ≥ ΦIactive (x(tk)) −

Lreg > L reg + ∆ t − Lreg = ∆ t.

Theorem 5.2 (Global Hybrid Safety Invariance): Let 

Q = {(I, x ) : x ∈ A ∗ 

> I

} define the hybrid safety invariant. This reflects that CIPS does not assume a universal safe set, but rather a controller-indexed family of safe sets 

{A ∗ 

> I

}I∈I . Suppose every object I ∈ I satisfies the CIPS contract, and assume ρreset > ∆t > 0 with ΦI0 (x0) ≥ ρreset .Under Assumptions 5.1 and 5.2, if the scheduler executes regeneration upon detecting a valid trigger and invokes fallback at sample tk whenever ΦIactive (x(tk)) ≤ ∆t, then 

(I(t), x (t)) ∈ Q for all t ≥ 0.

Proof: We prove (I(t), x (t)) ∈ Q holds across all operating modes by establishing the invariant that x(t) ∈A∗ 

> I(t)

at all times, alongside: At every sampling instant, either 

ΦIactive (x(tk)) > ∆t or fallback is immediately invoked.  

> •

Normal Inter-Sample Execution: For any sample tk

where fallback is not invoked, ΦIactive (x(tk)) > ∆t.Axiom 2 ensures x(t) ∈ DIactive ⊆ A∗ 

> Iactive

for t ∈

[tk, t k+1 ). 

> •

Regeneration Pending / Rejected: Suppose regeneration starts at tk with γk = Φ Iactive (x(tk)) > ∆t. Regime A: If γk > L reg + ∆ t, then Theorem 5.1 guarantees 

ΦIactive (x(tc)) > ∆t at every completion time tc ≤ tk +

Lreg . Regime B: If ∆t < γ k ≤ Lreg + ∆ t, regardless of whether regeneration completes before the next sample, Axiom 2 gives ΦIactive (x(t)) > 0 for t ∈ [tk, t k+1 ]

because tk+1 − tk = ∆ t < γ k. Hence the active object remains physically safe until the next sample. At tk+1 ,either a successful candidate has been handed off, or the scheduler invokes fallback if the sampled persistence is 

≤ ∆t. In both regimes, safety is preserved.  

> •

Successful Handoff: State continuity and 

ΦInext (x(tsw )) ≥ ρreset > ∆t establish the successor object remains safe for at least the next interval.  

> •

Fallback Invocation: If ΦIactive (x(tk)) ≤ ∆t is observed, fallback is immediately invoked. Since 

ΦIactive (x(tk−1)) > ∆t at the preceding sample 

tk−1 = tk − ∆t, Axiom 2 guarantees ΦIactive (x(tk)) ≥

ΦIactive (x(tk−1)) − ∆t > 0. Thus, immediately prior to fallback at tk, the active object satisfies 0 <

ΦIactive (x(tk)) ≤ ∆t. This implies x(tk) ∈ S fb ,I active ⊆Sfb . By Assumption 5.2, Sfb ⊆ Dstop , establishing indefinite safety under Istop .VI. I LLUSTRATIVE AUTONOMOUS VEHICLE 

CERTIFICATE & E XPERIMENTAL VALIDATION 

A. Experimental Setup 

To conceptually illustrate and validate CIPS, we imple-ment a dynamic Adaptive Cruise Control (ACC) benchmark operating under bounded kinematics representing a leader-follower scenario. The physical state of the system is given by x = [ plead , v lead , p follow , v follow ]T .The follower vehicle tracks a leader and must regenerate its control action I = ufollow safely in the presence of computational latencies. We enforce strict kinematic con-straints: a maximum braking deceleration ab = 6 .0 m/s 2,a maximum forward acceleration umax = 2 .0 m/s 2, and a maximum allowable velocity vmax = 20 .0 m/s. Additionally, we mandate a ground-truth physical minimum boundary 

dmin = 3 .0 m representing an absolute collision threshold, and a measurement uncertainty margin η = 0 .5 m. 

B. CIPS Certificate Derivation 

To compile spatial bounds into a temporal persistence contract offline, we define the robust safety threshold drobust :

drobust = dmin + η + dkin = 3 .0 + 0 .5 + 2 .05 = 5 .55 m, where 

dkin is the kinematic buffer for braking and actuator latency: 

dkin = vrel Treact + v2 

> rel
> 2ab

= (4 .0)(0 .18) + 4.02 

> 2(6 .0)

= 2 .05 m. The spatial certificate margin is: h(x) = ( plead − pfollow ) −

drobust . To avoid open-loop over-conservatism, we bound the decay rate α(s) of h(x). The domain depletion rate is the relative closing speed: − ˙h(x) = vfollow − vlead := vrel . The worst-case relative acceleration closing the gap is: amax  

> rel

=

umax + ab = 2 .0 + 6 .0 = 8 .0 m/s 2.We explicitly restrict the certified operating domain to states satisfying the kinematic braking envelope v2 

> rel

≤ v2 

> b

+2amax  

> rel

h(x), which is enforced by the offline certificate and controller construction. Consequently, enforcing a bound-ary closing-speed vb = 4 .0 m/s at h(x) = 0 yields the kinematic degradation bound: − ˙h(x) = vrel ≤ α(h(x)) = pv2 

> b

+ 2 amax  

> rel

h(x)

Applying Theorem 3.1, the CIPS temporal persistence functional maps the spatial state into certified validity sec-onds: Φ( x) = R h(x)0

> ds

√v2   

> b+2 amax
> rel s

=

√v2  

> b+2 amax
> rel h(x)−vb
> amax
> rel

Substituting system parameters yields the closed-form contract: Φ( x) = 

√16 .0+16 .0 h(x)−4.08.0 . This satisfies ˙Φ( x) ≥−1 along all valid trajectories, enabling the runtime sched-uler to evaluate Φ( x) in O(1) deterministic execution time without online ODE propagation. C. Scheduler Implementations 

We evaluated five runtime scheduling policies under la-tency constraints. Table II outlines their trigger conditions and evaluation inputs. 

TABLE II SCHEDULER IMPLEMENTATIONS AND TRIGGER CONDITIONS 

Scheduler Evaluation Input Trigger Condition 

Periodic Sampling period dt Time elapsed ≥ P

Boolean (Reactive) Inter-vehicle gap d(t) d(t) ≤ dmin 

Spatial ETC Inter-vehicle gap d(t) d(t) ≤ drobust 

LA-ETC Online reachability d(t + Lreg + 2∆ t) ≤ drobust 

CIPS (Proposed) Persistence Φ( x) Φ( x) ≤ Lreg + 2∆ t

Crucially, LA-ETC has runtime access to the complete plant model f (x) and numerically integrates the closed-loop dynamics forward over Lpred = Lreg + 2∆ t at every sample 

tk using explicit 4th-order Runge-Kutta (RK4) integration (δt = 1 ms) under worst-case leader braking ( alead = −ab). In contrast, CIPS performs no forward propagation and consumes only the precompiled scalar persistence functional 

ΦI (x).

D. Experimental Results & Evaluation Methodology 

The schedulers were subjected to structured robustness sweeps by varying both the regeneration latency Lreg (Ta-ble III) and the sampled evaluation interval ∆t (Table IV). Algorithms were compiled in C++-20 with -O2 optimiza-tions. To evaluate runtime overhead, evaluation latency ( Eval )was measured on the Pico-W Cortex-M0+ ARM architecture running upto 133 MHz, with 16 KB shared data and program L1 cache and 256 KB SRAM. Reported timing values (Table V) reflect the median execution time per evaluation over 10 , 000 iterations. We evaluate Latency Efficiency ( Elat ) as the percentage of the safe temporal budget utilized at the effective regeneration trigger: Elat =



1 − Φ( xtrigger )Φinitial 



× 100% , where Φinitial is the initial persistence budget upon object activation, and 

Φ( xtrigger ) is the remaining persistence functional evaluated at the trigger instant. If no regeneration is required during an evaluation run, the effective trigger is defined at Φ( xtrigger ) = 0, yielding Elat = 100% .

TABLE III COMPREHENSIVE SCHEDULING METRICS & R OBUSTNESS SWEEP 

(V ARYING LREG )Scheduler L reg(ms) Total Reg Unnec Reg Miss/Late Fallbacks Min H(m) Violations E lat (%) Periodic 10 625 625 0 0 0.74 0 75.0 Boolean 10 0 0 1 1 -12.45 1 100.0 Spatial ETC 10 35 0 1 1 -8.05 1 98.6 LA-ETC 10 0 0 0 0 0.44 0 100.0 CIPS 10 0 0 0 0 0.44 0 100.0 Periodic 50 454 454 0 0 0.74 0 9.2 Boolean 50 0 0 1 1 -12.45 1 100.0 Spatial ETC 50 10 0 1 1 -8.05 1 98.0 LA-ETC 50 0 0 0 0 0.44 0 100.0 CIPS 50 0 0 0 0 0.44 0 100.0 Periodic 100 238 220 0 0 0.74 0 4.8 Boolean 100 0 0 1 1 -12.45 1 100.0 Spatial ETC 100 5 0 1 1 -8.05 1 98.0 LA-ETC 100 0 0 0 0 0.44 0 100.0 CIPS 100 3 0 0 0 0.44 0 98.8 TABLE IV ROBUSTNESS AS SAMPLING INTERVAL (∆t) V ARIES (LREG = 50 MS )Scheduler dt(ms) Total Reg Unnec Reg Miss/Late Fallbacks Min H(m) Violations E lat (%) Periodic 5 454 454 0 0 0.74 0 9.2 Boolean 5 0 0 1 1 -12.45 1 100.0 Spatial ETC 5 10 0 1 1 -8.05 1 98.0 LA-ETC 5 0 0 0 0 0.44 0 100.0 CIPS 5 0 0 0 0 0.44 0 100.0 Periodic 20 1 1 0 0 0.74 0 99.8 Boolean 20 0 0 1 1 -12.45 1 100.0 Spatial ETC 20 1 0 1 1 -8.05 1 99.8 LA-ETC 20 0 0 0 0 0.44 0 100.0 CIPS 20 0 0 0 0 0.44 0 100.0 Periodic 50 250 250 0 0 0.74 0 50.0 Boolean 50 0 0 1 1 -12.45 1 100.0 Spatial ETC 50 6 0 1 1 -8.05 1 98.8 LA-ETC 50 0 0 0 0 0.44 0 100.0 CIPS 50 8 0 0 0 0.44 0 98.4 TABLE V CYCLE -A CCURATE EVALUATION MICRO -BENCHMARK (GEM 5 ARM) 

Scheduler Median Eval Time ( μs) Speedup vs. LA-ETC CIPS (Ours) 2 60.5 ×

Spatial ETC 3 40.3 ×

Periodic 3 40.3 ×

Boolean 3 40.3 ×

LA-ETC 121 1.00 ×

The benchmark results highlight distinct trade-offs across the evaluation ladder: 

• Periodic Scheduling: Preserves safety but induces high numbers of unnecessary regenerations, degrading la-tency budget efficiency ( Elat drops to 4.8% at Lreg =100 ms). 

• Boolean and Spatial ETC: Both suffer systemic safety violations because they lack temporal latency aware-ness, triggering fallback maneuvers too late (Figure 1). 

• LA-ETC (Online Reachability): Maintains safety across delays by explicitly propagating state trajectories over the latency horizon. However, this incurs sub-stantial computational overhead ( 121 μs) due to online numerical integration. 

• CIPS Framework: CIPS exhibits the same safety outcome and minimum-gap bound as LA-ETC across evaluated latency and sampling settings, while requiring substantially ( 60 .5×) lower evaluation overhead. No-tably, the results demonstrate substantial latency bud-get efficiency ( Elat ), remaining near optimal (98.4% to 100.0%). Unlike LA-ETC, whose runtime trigger requires access to and numerical propagation of plant 10 15 20 25 30 35 

> Time [s]
> 0246810
> Inter-Vehicle Gap d(t) [m]
> Static Periodic (30Hz) Boolean Scheduling Spatial ETC LA-ETC (Forward Reachable) CIPS (Proposed) Unsafe Zone Ground-Truth Boundary (3.0m) CIPS Robust Boundary (5.55m)

Fig. 1. Inter-Vehicle State Validity Tracking illustrating CIPS adapting dynamically against Boolean and ETC failures. dynamics, CIPS consumes only the precompiled scalar persistence functional. VII. R ELATED WORK AND INTERFACE COMPARISON 

Ensuring physical safety under discrete computation and non-zero latencies is a core cyber-physical systems (CPS) challenge. CIPS complements existing synthesis and spatial verification techniques by introducing a temporal compila-tion layer that normalizes heterogeneous spatial certificates into consumable runtime contracts. 

Event-Triggered Control and Predictive Safety Filters. 

Event-Triggered Control (ETC) reduces overhead via state-dependent updates [4], [6], and latency-aware variants like lookahead ETC (LA-ETC) or predictive safety filters ex-plicitly incorporate execution delays by evaluating future trajectories online [7], [8]. However, these approaches re-quire continuous online plant vector field evaluations and numerical integration of plant dynamics, incurring high computational overhead ( ≈ 60 .5× more, per Section VI). CIPS shifts this burden offline, eliminating online forward reachability by compiling spatial decay bounds into a scalar temporal functional Φ( x). This provides O(1) latency-aware scheduling and drastically reduces deterministic evaluation overhead. 

Self-Triggered Safety and Sampled-Data CBFs. Self-triggered and sampled-data Control Barrier Functions (CBFs) predict safe sampling instants or address inter-sampling gaps, but they tie temporal intervals directly to specific continuous controllers [9], [10], [11]. CIPS decouples this dependency by compiling any spatial certificate into a platform-agnostic persistence contract that manages execution budgets and fallback transitions independently. 

Reachability-Based Runtime Monitoring. Hamilton-Jacobi (HJ) reachability provides exact, non-conservative safety certificates [3], [12], but evaluating multi-dimensional implicit value functions online is computationally pro-hibitive. CIPS complements offline HJ toolboxes by compil-ing their computed spatial domains into fast, microsecond-scale temporal functionals for runtime execution. 

Runtime Assurance, Simplex, and Information Stal-eness. Runtime Assurance (RTA) safely overrides primary controllers with verified backups [13], [14], [15], while Age of Information (AoI) models data staleness [16], [17]. CIPS bridges these paradigms by formalizing staleness as a consumable temporal asset, yielding an explicit latency-aware trigger ( Φ( x) ≤ Lreg +2∆ t) that guarantees safe mode switches before physical violations occur. VIII. C ONCLUSION AND FUTURE WORK 

We introduced CIPS, a contract-driven runtime abstraction that bridges the gap between continuous physical safety and discrete cyber execution. By formalizing the Certificate-to-Persistence compiler, we demonstrated that continuously differentiable spatial certificates can be systematically trans-formed into consumable, unit-rate temporal contracts. Cru-cially, CIPS shifts the computational burden of plant model evaluation entirely offline. This decoupling of physical cer-tificate derivation from runtime scheduling achieves provable global hybrid safety invariance under bounded latency and sampling, entirely eliminating the need for online numerical integration. Future work includes: (1) Extending the compiler for non-smooth barrier functions and neural network-based bounds. (2) Adapting contracts to handle probabilistic safety bounds under process noise and environmental uncertainty. (3) Using advanced reachability bounds to reduce spatial decay conser-vatism, yielding tighter, more efficient temporal contracts. REFERENCES [1] R. Alur, Principles of cyber-physical systems . MIT press, 2015. [2] A. D. Ames, S. Coogan, M. Egerstedt, G. Notomista, K. Sreenath, and P. Tabuada, “Control barrier functions: Theory and applications,” in 2019 18th European control conference (ECC) . Ieee, 2019, pp. 3420–3431. [3] S. Bansal, M. Chen, S. Herbert, and C. J. Tomlin, “Hamilton-jacobi reachability: A brief overview and recent advances,” in 2017 IEEE 56th annual conference on decision and control (CDC) . IEEE, 2017, pp. 2242–2253. [4] P. Tabuada, “Event-triggered real-time scheduling of stabilizing control tasks,” IEEE Transactions on Automatic control , vol. 52, no. 9, pp. 1680–1685, 2007. [5] S. P. Bhat and D. S. Bernstein, “Finite-time stability of continuous autonomous systems,” SIAM Journal on Control and Optimization ,vol. 38, no. 3, pp. 751–766, 2000. [6] W. Heemels, K. H. Johansson, and P. Tabuada, “An introduction to event-triggered and self-triggered control,” in IEEE Conference on Decision and Control (CDC) , 2012, pp. 3270–3285. [7] T. Gurriet, A. Singletary, J. Reher, R. Babuska, and A. D. Ames, “To-wards a framework for realizable safety critical control through active set invariance,” in 2018 ACM/IEEE 9th International Conference on Cyber-Physical Systems (ICCPS) . IEEE, 2018, pp. 98–106. [8] L. Brunke, M. Greeff, A. W. Hall, Z. Yuan, S. Zhou, J. Panerati, and A. P. Schoellig, “Safe learning in robotics: From learning-based control to safe reinforcement learning,” Annual Review of Control, Robotics, and Autonomous Systems , vol. 5, no. 1, pp. 411–444, 2022. [9] A. Anta and P. Tabuada, “To sample or not to sample: Self-triggered control for nonlinear systems,” IEEE Transactions on Automatic Control , vol. 55, no. 9, pp. 2030–2042, 2010. [10] A. J. Taylor, V. D. Dorobantu, R. K. Cosner, Y. Yue, and A. D. Ames, “Safety of sampled-data systems with control barrier functions via approximate discrete time models,” in 2022 IEEE 61st Conference on Decision and Control (CDC) . IEEE, 2022, pp. 7127–7134. [11] G. Yang, C. Belta, and R. Tron, “Self-triggered control for safety critical systems using control barrier functions,” in 2019 American control conference (ACC) . IEEE, 2019, pp. 4454–4459. [12] I. M. Mitchell, A. M. Bayen, and C. J. Tomlin, “A time-dependent hamilton-jacobi formulation of reachable sets for continuous dynamic games,” IEEE Transactions on Automatic Control , vol. 50, no. 7, pp. 947–957, 2005. [13] D. Seto, B. Krogh, L. Sha, and A. Chutinan, “The simplex architecture for safe online control system upgrades,” in Proceedings of the 1998 American Control Conference. ACC (IEEE Cat. No. 98CH36207) ,vol. 6. IEEE, 1998, pp. 3504–3508. [14] L. Sha, “Using simplicity to control complexity,” IEEE Software ,vol. 18, no. 4, p. 20, 2001. [15] D. Cofer, I. Amundson, R. Sattigeri, A. Passi, C. Boggs, E. Smith, L. Gilham, T. Byun, and S. Rayadurgam, “Run-time assurance for learning-enabled systems,” in NASA Formal Methods Symposium .Springer, 2020, pp. 361–368. [16] S. Kaul, R. Yates, and M. Gruteser, “Real-time status: How often should one update?” in 2012 Proceedings IEEE INFOCOM . IEEE, 2012, pp. 2731–2735. [17] R. D. Yates, Y. Sun, D. R. Brown, S. K. Kaul, E. Modiano, and S. Ulukus, “Age of information: An introduction and survey,” IEEE Journal on Selected Areas in Communications , vol. 39, no. 5, pp. 1183–1210, 2021.