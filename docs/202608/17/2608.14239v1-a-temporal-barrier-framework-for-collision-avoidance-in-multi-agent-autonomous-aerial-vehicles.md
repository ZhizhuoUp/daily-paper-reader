---
title: A Temporal Barrier Framework for Collision Avoidance in Multi-Agent Autonomous Aerial Vehicles
title_zh: 一种用于多智能体自主飞行器避障的时间屏障框架
authors: "Benedikt Barthel Sorensen, Mitchell Black, Erfaun Noorani, Themistoklis Sapsis"
date: 2026-08-14
pdf: "https://arxiv.org/pdf/2608.14239v1"
tags: ["query:q1"]
score: 9.0
evidence: 自主飞行器避障的时间屏障框架
tldr: 针对多无人机在动态及潜在对抗环境下的避障挑战，本文提出了一种基于对抗碰撞时间（aTTC）的时间屏障框架。该方法将aTTC指标嵌入控制屏障函数（CBF），使智能体能够预判周围目标在极端情况下的最快碰撞时间并据此调整航速。通过神经网络代理模型实现实时计算，该框架在保障安全的同时显著提升了多机协作的任务效率。
selection_source: fresh_fetch
motivation: 传统避障方法在紧凑空间或对抗环境下往往过于保守，难以在确保安全的同时维持高效的任务进度。
method: 引入对抗碰撞时间（aTTC）作为风险度量，并将其集成到CBF框架中，利用神经网络加速实时优化求解。
result: 在3D仿真实验中，aTTC-CBF在碰撞率降低一半的情况下，实现了两倍于传统距离基准方法的任务进度。
conclusion: 该研究证明了基于时间的屏障函数在处理多智能体复杂动态避障问题上具有比传统距离度量更高的安全性和灵活性。
---

## 摘要
在动态、不确定且可能存在对抗性的环境中运行自主飞行器团队，需要既可靠又具有选择性的安全协议，使智能体能够在保持近距离飞行的同时朝着任务目标推进。我们引入了对抗性碰撞时间（aTTC），这是一种风险度量指标，用于量化在假设存在对抗意图的情况下，周围任何智能体到达给定智能体的速度。我们将 aTTC 嵌入到控制屏障函数（CBF）框架中，直接在时间维度而非距离或速度维度上定义屏障。由此产生的 aTTC-CBF 具有内在的前瞻性：智能体调节自身速度的依据不是同伴是否处于碰撞路径上，而是在给定动力学约束的情况下，同伴达到碰撞状态的速度。一个可微神经网络代理使得 aTTC 能够在标准 CBF 二次规划中进行实时计算。在 3D 独立追踪和编队飞行场景的长时域模拟中，与基于距离的高阶 CBF 基准相比，aTTC-CBF 在碰撞率减半的情况下，实现了高达两倍的航点推进进度。

## Abstract
Operating teams of autonomous aircraft in dynamic, uncertain, and potentially adversarial environments requires safety protocols that are reliable yet selective, and allow agents to fly in close proximity while making progress toward mission objectives. We introduce adversarial time-to-collision (aTTC), a risk metric that quantifies, for a given agent, how quickly any surrounding agent could reach it assuming adversarial intent. We embed aTTC into the control barrier function (CBF) framework, defining the barrier directly in time rather than distance or velocity. The resulting aTTC-CBF is inherently anticipatory: agents modulate their own velocity based not on whether a peer is on a collision course, but on how quickly one could reach collision given its dynamical constraints. A differentiable neural-network surrogate makes the aTTC computable in real time within a standard CBF quadratic program. Across long time-horizon simulations of 3D independent-pursuit and formation-flight scenarios, the aTTC-CBF achieves up to twice the waypoint progress at half the collision rate of a higher-order distance-based CBF baseline.

---

## 论文详细总结（自动生成）

### 论文总结：一种用于多智能体自主飞行器避障的时间屏障框架

#### 1. 核心问题与整体含义
在动态、不确定且可能存在敌对意图的环境中，多自主飞行器（UAV）的避障是一个极具挑战性的问题。传统方法（如基于距离的控制屏障函数 CBF）在 3D 空间中往往过于保守，或者无法有效区分“空间接近但安全（如背向飞行）”与“空间接近且危险（如对冲）”的情况。本文的研究动机在于：**时间是衡量风险最自然的维度**。论文提出了一种基于“时间”而非“空间距离”的屏障框架，旨在让飞行器在保持任务进度的同时，能够根据潜在威胁到达的时间来预判并执行避障动作。

#### 2. 方法论
*   **核心思想：** 引入**对抗碰撞时间（aTTC）**作为风险度量。aTTC 定义为：假设本机保持当前速度，而对方智能体以最大控制能力尝试制造碰撞时，两者发生碰撞所需的最短时间。
*   **关键技术细节：**
    *   **aTTC 定义：** 与传统的常速度 TTC 不同，aTTC 考虑了对方的动力学约束（速度和加速度限制），确保在 3D 环境中该指标始终有限且可计算。
    *   **时间屏障函数（aTTC-CBF）：** 将屏障函数 $h$ 定义为 $h = \tau - \tau_c$（其中 $\tau$ 是预测的碰撞时间，$\tau_c$ 是安全阈值）。该函数具有一阶相对度，简化了控制律设计。
    *   **神经网络代理模型：** 由于直接求解 aTTC 涉及复杂的轨迹积分，论文训练了一个轻量级、可微的神经网络（采用 FiLM 架构进行参数调节），将状态映射到 aTTC 值，从而在二次规划（QP）中实现毫秒级的实时求解。
    *   **鲁棒安全保证：** 在 CBF 约束中加入了扰动支撑函数，以应对过程噪声和敌对输入。

#### 3. 实验设计
*   **场景设置：** 采用 3D Dubins 飞行器模型，设计了两种主要场景：
    1.  **独立多智能体追踪-逃逸：** 8 个逃逸者在球形空间内随机前往航点，同时应对 0-3 个敌对追踪者。
    2.  **编队飞行：** 8 个逃逸者保持人字形编队飞行，同时躲避追踪者。
*   **基准方法（Benchmark）：** 
    *   **HOCBF：** 基于欧几里得距离的高阶控制屏障函数。
    *   **No-CBF：** 无避障干预的基准。
*   **对比维度：** 碰撞率、航点达成进度、编队一致性（偏差距离）。

#### 4. 资源与算力
*   **训练数据：** 生成了约 1000 万个样本，涵盖了不同的初始速度和敌对能力。
*   **算力说明：** 文中未明确提及训练所用的具体 GPU 型号或训练时长。但在推理端，提到在 CPU 上运行 Matlab 仿真时，单次控制调用的耗时小于 1.0 毫秒，证明了该方法在嵌入式系统上的实时性潜力。

#### 5. 实验数量与充分性
*   **实验规模：** 进行了长达 50,000 秒（模拟时间）的长时域仿真，这在同类研究中较为罕见，能够提供可靠的统计学评价。
*   **充分性：** 实验涵盖了无追踪者、慢速追踪者和快速追踪者三种敌对强度，并对比了独立任务与协作任务。通过 PDF（概率密度函数）分析了智能体在空间中的分布和近距离遭遇时的逃逸概率，实验设计客观且对比公平。

#### 6. 主要结论与发现
*   **安全性与效率的双赢：** 在 3D 独立追踪场景中，aTTC-CBF 的碰撞率仅为 HOCBF 的一半，而任务进度（航点达成数）却是后者的两倍。
*   **对抗环境下的韧性：** 随着追踪者速度增加，HOCBF 的性能大幅下降，而 aTTC-CBF 仍能保持较高的任务效率。
*   **编队保持能力：** 在编队飞行中，aTTC-CBF 能够让受威胁的智能体在避障后迅速回归编队，其编队偏差显著低于基于距离的方法。

#### 7. 优点
*   **前瞻性：** 相比于距离度量，时间度量能更早地识别潜在的拦截风险。
*   **低保守性：** 允许智能体在安全的前提下进行更近距离的交互，减少了不必要的避让动作。
*   **实时性：** 通过神经网络代理解决了复杂动力学下的实时计算难题。

#### 8. 不足与局限
*   **非对抗场景的保守性：** 在完全友好的环境中，该方法相比传统方法的提升相对有限，可能存在过度假设敌意的倾向。
*   **模型依赖：** aTTC 的计算依赖于对对方动力学参数（如最大速度、加速度）的准确估计，若参数失真，安全性保证可能失效。
*   **代理模型误差：** 神经网络的近似误差可能导致在极端边缘情况下的安全边界模糊，需要更完善的混合验证机制。

（完）
