---
title: "DPNet: Efficient Dead-End Prediction and Avoidance for Vision-Based UAV Navigation"
title_zh: DPNet：基于视觉的无人机导航中高效死胡同预测与规避
authors: "Ruibin Zhang, Lun Pan, Zelong Xia, Jialiang Hou, Fei Gao"
date: 2026-08-17
pdf: "https://arxiv.org/pdf/2608.16640v1"
tags: ["query:q1"]
score: 8.0
evidence: 无人机导航中的死胡同预测与规避
tldr: 针对视觉无人机在死胡同场景中因感知范围有限导致的导航失败问题，本文提出DPNet。该方法利用轻量级神经网络从RGB-D输入中预测潜在死胡同的距离和方位，并据此修剪预定义的轨迹库，实现主动避障。系统支持50Hz高频重规划，无需真实数据微调即可迁移至现实场景，显著提升了复杂环境下的导航成功率和效率。
selection_source: fresh_fetch
motivation: 视觉无人机常因感知精度和范围有限，在死胡同场景中面临导航失败的挑战。
method: 提出一种轻量级网络DPNet预测死胡同位置，并结合轨迹库修剪策略实现主动规避。
result: 仿真实验显示其在成功率和飞行效率上表现优异，且在真实世界中实现了50Hz的高频重规划。
conclusion: 该系统通过高效的死胡同预测与规避机制，显著增强了无人机在复杂未知环境中的自主导航能力。
---

## 摘要
基于视觉的无人机（UAV）由于感知精度和范围有限，常在死胡同中遭遇导航失败。为应对这一挑战，本文提出了一种用于高效死胡同预测与规避的系统化解决方案。所提方法引入了一个轻量级神经网络，利用 RGB-D 输入预测当前视野内潜在死胡同的相对距离和方位。这些预测结果用于剪枝预定义的紧凑轨迹库，使规划器能够在保持导航平滑性的同时主动规避死胡同。值得注意的是，我们的方法无需人工标注或在真实数据上进行微调，即可迁移至真实场景。该系统在机载设备上实现了 50 Hz 的高频重规划。广泛的仿真基准测试证明了其在成功率、飞行时间和轨迹长度方面的优越性能，真实世界实验进一步验证了其在复杂场景下的有效性。

## Abstract
Vision-based Unmanned Aerial Vehicles (UAVs) often suffer from navigation failures in dead ends due to limited sensing accuracy and range. To address this challenge, this paper proposes a systematic solution for efficient dead-end prediction and avoidance. The proposed method introduces a lightweight neural network to predict the relative distance and bearing of potential dead ends within the current field of view using RGB-D inputs. These predictions prune a predefined, compact trajectory library, enabling the planner to proactively avoid dead ends while maintaining navigational smoothness. Notably, our approach transfers across real-world scenarios without manual annotation or fine-tuning on real-world data. The system achieves high-frequency replanning at 50 Hz onboard. Extensive simulation benchmarks demonstrate superior performance in success rate, flight time, and trajectory length, and real-world experiments further validate its effectiveness in complex scenarios.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **DPNet** 的系统化解决方案，旨在解决视觉无人机（UAV）在未知环境中因感知范围有限而容易陷入“死胡同”（Dead-end）的问题。以下是对该论文的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：微型无人机通常依赖 RGB-D 相机进行导航，但受限于传感器的视场角（FOV）和感知距离，局部规划器往往只能看到眼前的障碍物。这种“近视”导致无人机容易进入 U 型或 V 型等死胡同结构，等发现无法通行时往往为时已晚，导致碰撞、震荡或任务失败。
*   **核心问题**：如何在有限的局部感知下，提前预测并主动规避尚未完全进入视野的死胡同结构，同时保持飞行的平滑性和实时性。

### 2. 论文提出的方法论
*   **核心思想**：将死胡同视为一种需要提前预判的“结构化危险”，通过轻量级网络预测其位置，并将其转化为“虚拟障碍物”投影到局部地图中，从而在轨迹规划阶段直接剪掉进入死胡同的路径。
*   **关键技术细节**：
    *   **DPNet 架构**：采用双分支视觉编码器（分别处理 RGB 和深度图），利用预训练的 DINOv3 权重提取语义和几何特征。网络输出两个分支：一个是死胡同掩码（Mask），另一个是死胡同深度图（Depth）。
    *   **自动标注算法**：在仿真中通过几何可见性逻辑（判断死胡同的内外顶点是否可见或被遮挡）自动生成训练标签，无需人工标注。
    *   **虚拟屏障生成**：将预测的死胡同像素投影到 3D 空间，并进行点云上采样，形成一道“虚拟墙”阻断死胡同入口。
    *   **死胡同感知规划**：在轨迹库（Motion Primitives）中进行碰撞检测时，同时考虑真实障碍物和虚拟屏障。引入**平滑一致性代价函数**，防止无人机在绕行过程中因死胡同暂时离开视野而再次转头切入。

### 3. 实验设计
*   **实验场景**：
    *   **仿真环境**：包含随机分布的圆柱、圆环以及 U 型、V 型死胡同，并设置了 II 型（可通行）通道作为负样本。
    *   **真实环境**：在室内动捕场地，使用搭载 Jetson Orin NX 的四旋翼无人机进行验证。
*   **Benchmark 与对比方法**：
    *   对比了三种代表性规划器：**EGO-Planner**（基于梯度的局部规划）、**Primitive-Planner**（基于运动原语的轻量级规划）、**Faster**（具有安全备份机制的规划器）。
    *   评估指标包括：成功率（Success Rate）、飞行时间（Flight Time）和轨迹长度（Trajectory Length）。

### 4. 资源与算力
*   **训练算力**：使用单张 **NVIDIA RTX 4090 GPU**，训练 100 个 epoch。训练数据集包含 50,000 对同步的 RGB-D 图像。
*   **机载算力**：部署在 **NVIDIA Jetson Orin NX** 上。
*   **实时性**：网络推理耗时 9.2ms，后处理 2.4ms，规划 5.1ms，整体系统实现了 **50 Hz** 的高频重规划。

### 5. 实验数量与充分性
*   **实验规模**：仿真中针对“简单”和“困难”两种难度各进行了 100 次随机试验。
*   **充分性**：实验涵盖了不同形状的死胡同和干扰项（如可通行通道），验证了模型的判别能力。通过 Sim-to-Real 实验证明了模型在未经真实数据微调的情况下具有良好的泛化性。对比实验选择了当前主流的开源算法，评估维度较为全面。

### 6. 论文的主要结论与发现
*   **性能提升**：在困难场景下，DPNet 的成功率达到 99%，远高于 Primitive-Planner (47%) 和 EGO-Planner (76%)，且比 Faster 规划器缩短了约 25% 的飞行时间。
*   **主动规避优势**：相比于 Faster 等被动触发恢复动作（如后退、旋转）的方法，DPNet 的主动规避机制保证了飞行的连续性和效率。
*   **跨域能力**：冻结 RGB 编码器并仅微调深度分支的策略，有效利用了基础模型的语义理解能力，实现了零样本（Zero-shot）的仿真到现实迁移。

### 7. 优点
*   **高效性**：将复杂的场景预测简化为极低分辨率（14x14）的掩码和深度预测，极大地节省了计算资源。
*   **系统集成度高**：不仅提出了感知网络，还完整设计了如何将预测结果无缝集成到现有的轨迹库规划框架中。
*   **鲁棒性**：通过引入平滑性代价函数，解决了局部感知下目标点诱导无人机反复进入死胡同的“重入”问题。

### 8. 不足与局限
*   **形状限制**：目前主要针对 U 型和 V 型死胡同，对于更复杂的迷宫结构或多层嵌套死胡同的预测能力尚待验证。
*   **感知视角**：仅依赖前视相机，如果死胡同结构非常巨大且超出视野范围，预测精度可能会下降。
*   **依赖深度质量**：虽然使用了 RGB 辅助，但深度图的质量（如反光、空洞）仍可能影响最终的 3D 投影效果。

（完）
