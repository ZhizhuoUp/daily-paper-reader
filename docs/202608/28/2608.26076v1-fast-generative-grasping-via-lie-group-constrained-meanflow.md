---
title: Fast Generative Grasping via Lie Group-Constrained MeanFlow
title_zh: 基于李群约束 MeanFlow 的快速生成式抓取
authors: "S. Talha Bukhari, Yi Wei, Ruiqi Ni, Zachary Kingston, Aniket Bera"
date: 2026-08-26
pdf: "https://arxiv.org/pdf/2608.26076v1"
tags: ["query:profile-1"]
score: 8.0
evidence: 面向实时操作的快速生成式抓取
tldr: 针对机器人抓取合成中生成式模型采样速度慢的问题，本文提出了一种基于李群约束 MeanFlow 的快速生成方法。该方法在 SO(3) x R^3 乘积群上构建，结合了代数半群一致性与黎曼条件流匹配。实验表明，该方法仅需 5 次以内网络评估即可生成高质量抓取，在 ACRONYM 数据集上实现了高达 39 倍的加速，并能直接迁移至真实机器人环境。
selection_source: fresh_fetch
motivation: 现有的生成式抓取模型因多步迭代采样导致推理延迟高，难以满足机器人任务的实时性需求。
method: 提出一种在李群 SO(3) x R^3 上的 MeanFlow 框架，通过耦合半群一致性条件与黎曼条件流匹配来优化采样路径。
result: 在 ACRONYM 数据集上，该方法仅需极少采样步数即可达到 SOTA 性能，推理速度提升高达 39 倍。
conclusion: 该方法在保证抓取质量的同时显著提升了生成效率，且具备极强的现实环境鲁棒性和跨域迁移能力。
---

## 摘要
抓取合成是机器人操作中的核心任务，其解通常形成多模态分布而非点估计。生成式机器人抓取旨在利用扩散模型和基于流的方法等深度生成模型来学习这种分布。这类生成模型的迭代特性使其具有灵活性和泛化性；然而，多步采样阻碍了机器人技术所需的对时间敏感的操作。我们设计了一种基于乘积李群 $\mathcal{G} = \mathrm{SO}(3) \times \mathbb{R}^3$ 上 MeanFlow 的快速生成式抓取方法。训练目标将纯代数半群一致性条件与 $\mathcal{G}$ 上的黎曼条件流匹配（Riemannian Conditional Flow Matching）相结合，从而将平均速度锚定到数据分布。由此产生的李群约束 MeanFlow 公式在不超过 5 次网络评估中即可采样出可靠的抓取，在 ACRONYM 数据集上达到了毫秒级的推理延迟（提速高达 39 倍），且抓取生成性能与最先进的扩散和基于流的模型相当。我们进一步证明，该方法无需额外训练或领域自适应即可直接转化为现实世界的机器人抓取，并在观测噪声下表现出鲁棒的抓取合成能力。

## Abstract
Grasp synthesis is a core task in robotic manipulation, for which the solution typically forms a multimodal distribution rather than a point estimate. Generative robotic grasping aims to learn this distribution with deep generative models such as diffusion and flow-based approaches. The iterative nature of such generative models makes them flexible and generalizable; however, multi-step sampling impedes the time-critical operation required in robotics. We devise an approach to fast generative grasping based on MeanFlow on the product Lie group $\mathcal{G} = \mathrm{SO}(3) \times \mathbb{R}^3$. The training objective couples a purely algebraic semigroup consistency condition with Riemannian Conditional Flow Matching on $\mathcal{G}$ that anchors the average velocity to the data distribution. The resulting Lie Group-constrained MeanFlow formulation samples reliable grasps in $\leq 5$ network evaluations, matching the grasp generation performance of state-of-the-art diffusion and flow-based models on the ACRONYM dataset at millisecond-scale inference latency (up to $39\times$ speed-up). We further demonstrate that the approach directly translates to real-world robotic grasping without additional training or domain adaptation, exhibiting robust grasp synthesis under observation noise.

---

## 论文详细总结（自动生成）

这是一份关于论文《Fast Generative Grasping via Lie Group-Constrained MeanFlow》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：机器人抓取合成是一个多模态分布问题。虽然扩散模型（Diffusion）和流匹配（Flow Matching）等生成模型能很好地建模这种分布，但其**迭代采样过程极其缓慢**（通常需要几十到几百步），无法满足机器人闭环控制和实时反应的需求。
*   **核心目标**：开发一种既能保持生成模型的高保真度和多模态覆盖能力，又能实现**极少数步数（甚至单步）快速采样**的抓取生成框架。

### 2. 方法论
*   **核心思想**：引入 **MeanFlow（平均流）** 理论，并将其扩展到李群 $\mathcal{G} = \mathrm{SO}(3) \times \mathbb{R}^3$ 上。不同于传统模型预测瞬时速度，MeanFlow 预测给定时间间隔内的**平均速度**，从而允许通过“弦式（chord-form）”更新实现大步长跳跃。
*   **关键技术细节**：
    *   **李群约束**：在旋转群 $\mathrm{SO}(3)$ 和平移空间 $\mathbb{R}^3$ 的乘积流形上建模，利用李群的指数映射和对数映射处理几何约束。
    *   **端点参数化**：网络直接预测最终的抓取位姿 $\hat{H}_1$，并据此推导出诱导流图（Induced Flow Map）。
    *   **双重训练目标**：
        1.  **流匹配锚点（CFM Anchor）**：在时间对角线上将模型输出与真实数据对齐，确保模型学习到正确的抓取分布。
        2.  **代数半群一致性（Semigroup Consistency）**：一种纯代数损失函数，要求“一步大跳跃”的结果与“两步小跳跃”的结果一致。该方法避免了复杂的协变导数计算，训练更稳定。
    *   **辅助任务**：同时学习物体的符号距离函数（SDF），将抓取生成与物体几何特征紧密耦合。

### 3. 实验设计
*   **数据集**：使用 **ACRONYM** 大规模抓取数据集（包含约 416 个物体实例，78 万个有效抓取）。
*   **实验场景**：
    *   **仿真评估**：在 Isaac Gym 中进行物理仿真，验证抓取成功率。
    *   **真实世界**：使用 Franka Research 3 机械臂和 Orbbec 深度相机进行实物抓取测试。
*   **Benchmark 与对比方法**：
    *   **SE3Dif**（基于扩散模型）
    *   **EquiGraspFlow (EGF)**（基于等变流匹配）
    *   **BRIDGER**（基于插值细化）
    *   **VSIGD**（基于形状推理的扩散模型）

### 4. 资源与算力
*   **硬件**：使用单块 **NVIDIA RTX 5080 (16GB)** GPU。
*   **训练时长**：约 **48 小时**（约 10,000 个 epoch）。
*   **训练配置**：Adam 优化器，学习率 $1 \times 10^{-4}$，有效 Batch Size 为 400。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **领域内（ID）与跨领域（OOD）**：针对训练集内外的物体类别分别进行了详尽测试。
    *   **采样步数消融**：测试了从 $T=1$ 到 $T=100$ 的性能变化。
    *   **消融实验**：对比了 SDF 辅助、权重退火、旋转投影方式及不同 MeanFlow 损失函数的影响。
    *   **鲁棒性测试**：模拟了单视角部分点云观测下的抓取表现。
*   **充分性评价**：实验设计非常全面，涵盖了从理论验证（SO(3) 玩具实验）到大规模仿真，再到真实机器人迁移的全过程，对比基准均为近年来的 SOTA 方法，结果具有高度说服力。

### 6. 主要结论与发现
*   **极速推理**：GraspMF 在 **5 次以内**网络评估（NFE）下即可达到甚至超过 SOTA 扩散模型的性能，推理延迟仅为 **15.5ms**，提速高达 **39 倍**。
*   **性能稳定**：在单步采样（$T=1$）时仍能保持较高的成功率，而传统扩散模型在步数减少时性能会剧烈下降。
*   **强泛化性**：无需额外微调即可直接应用于真实世界物体，且对观测噪声和遮挡具有良好的鲁棒性。

### 7. 优点
*   **数学严谨性**：将 MeanFlow 成功引入李群框架，解决了 $SE(3)$ 空间中的非欧几里得几何处理问题。
*   **效率极高**：通过代数一致性损失解决了生成模型“慢”的痛点，使生成式抓取具备了闭环控制的潜力。
*   **Pareto 前沿**：在“成功率 vs 推理延迟”的权衡中，该方法明显优于所有现有基准。

### 8. 不足与局限
*   **归一化依赖**：模型依赖于物体质心的归一化，在部分观测（Partial View）导致质心偏移时，性能虽仍领先但较完整观测有所下降。
*   **李群简化**：虽然使用了 $\mathrm{SO}(3) \times \mathbb{R}^3$，但这只是 $SE(3)$ 的一种简化表示（直接乘积而非半直积），虽然在抓取任务中常用，但在某些极端的动力学场景下可能存在局限。
*   **实时重规划验证**：虽然论文提到了毫秒级延迟支持闭环，但实验部分主要还是静态抓取测试，尚未深入展示在动态环境中的实时重规划效果。

（完）
