---
title: Diffusion Policies for Short-Horizon Planning in Robot Crowd Navigation
title_zh: 机器人人群导航中用于短时域规划的扩散策略
authors: "Wendong Li, Jochen Garcke"
date: 2026-08-27
pdf: "https://arxiv.org/pdf/2608.27158v1"
tags: ["query:q1"]
score: 8.0
evidence: 导航中避障的短时域规划
tldr: 针对机器人人群导航中单步反应式动作难以应对复杂多模态交互的问题，本文提出PDPO框架。该框架结合扩散模型生成短时域动作块，通过离线预训练与在线强化学习微调，使机器人具备多策略避障能力。研究还引入了边界约束以解决评估偏差，实验证明PDPO在成功率上显著优于现有基准。
selection_source: fresh_fetch
motivation: 传统的强化学习方法在人群导航中仅输出单步动作，难以捕捉和表达复杂环境下的多样化短期避障策略。
method: 提出PDPO框架，利用扩散策略生成五步动作块，并采用离线演示预训练结合在线PPO微调的训练模式。
result: PDPO在人群导航基准测试中取得了比强基线更高的成功率，且动作块机制在带边界约束的场景下表现尤为突出。
conclusion: 结合扩散策略的短时域规划能有效提升机器人在动态人群中的决策质量和避障性能。
---

## 摘要
机器人人群导航要求在密集、动态且多模态的人机交互环境下进行安全高效的决策。现有的强化学习方法通常在每个时间步输出单个反应式动作，这限制了它们表示多样化短期避障策略的能力。我们提出了规划扩散策略优化（Planning Diffusion Policy Optimization, PDPO），这是一种离线到在线的强化学习框架，利用扩散策略为人群导航生成短时域动作块（action chunks）。PDPO 首先在避障演示数据上进行预训练，然后通过将去噪过程视为内部决策过程，利用 PPO 进行在线微调。在执行过程中，该策略生成一个五步动作块，并以滚动时域（receding-horizon）的方式应用。此外，我们在常用的人群导航基准测试中观察到一种评估伪影：在没有显式边界约束的情况下，学习到的智能体可能会离开有效区域并绕过密集人群。为了解决这个问题，我们引入了一种将违反边界视为碰撞的设置。实验表明，PDPO 相比强基准模型获得了更高的成功率，消融实验证明动作块对于修改后的有界基准测试尤为重要。

## Abstract
Robot crowd navigation requires safe and efficient decision-making under dense, dynamic, and multimodal human--robot interactions. Existing reinforcement-learning methods typically output a single reactive action at each timestep, which limits their ability to represent diverse short-term avoidance strategies. We propose Planning Diffusion Policy Optimization (PDPO), an offline-to-online reinforcement-learning framework that uses a diffusion policy to generate short-horizon action chunks for crowd navigation. PDPO is first pretrained on collision-avoidance demonstrations and then fine-tuned online with PPO by treating the denoising process as an internal decision process. During execution, the policy generates a five-step action chunk and applies it in a receding-horizon manner. Furthermore, we observe an evaluation artifact in common crowd-navigation benchmarks: without explicit boundary constraints, learned agents may leave the valid domain and bypass dense crowds. To address this, we introduce a setting in which boundary violations are treated as collisions. Experiments show that PDPO obtains an improved success rate over strong baselines, and ablations demonstrate that action chunks are especially important for the modified bounded benchmark.

---

## 论文详细总结（自动生成）

这是一份关于论文《Diffusion Policies for Short-Horizon Planning in Robot Crowd Navigation》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
在密集且动态的人群中，机器人导航面临高度的**多模态交互**挑战（例如：面对行人时，向左绕行、向右绕行或减速避让都是合理的策略）。
*   **现有研究局限**：传统的强化学习（RL）方法通常采用单步反应式动作（Single-step reactive action），且策略分布多为简单的单峰高斯分布，难以捕捉这种多模态的决策空间，也缺乏对短期路径的连贯规划。
*   **评估基准缺陷**：作者发现主流的人群导航基准（如 CrowdNav++）缺乏显式的边界约束，导致智能体往往通过“跑出地图边界”来绕过拥挤人群，产生了虚高的性能评估。
*   **研究目标**：提出一种能够表达复杂多模态策略、具备短时域规划能力，且在更严格（有边界）的环境下依然稳健的导航框架。

### 2. 方法论：PDPO 框架
论文提出了 **PDPO (Planning Diffusion Policy Optimization)**，其核心思想是将扩散模型（Diffusion Models）引入人群导航的动作生成中。

*   **核心思想**：
    *   **动作块（Action Chunking）**：策略不再输出单一速度指令，而是输出一个包含未来 5 个时间步（约 1.25 秒）的动作序列（动作块）。
    *   **扩散策略**：利用扩散模型的迭代去噪能力，从高斯噪声中生成动作块。这使得策略能够表示非高斯、多模态的动作分布。
    *   **滚动时域执行**：在每个时间步生成 5 步规划，但仅执行第一步，随后根据新观测重新规划，兼顾了规划的连贯性与对动态环境的响应性。
*   **关键技术流程**：
    1.  **状态编码**：使用自注意力机制（Self-Attention）和池化层处理机器人与周围多名行人的交互特征。
    2.  **离线预训练**：首先使用行为克隆（BC），在 ORCA（一种经典的规则避障算法）生成的演示数据上预训练扩散策略，为模型提供基础避障能力。
    3.  **在线强化学习微调**：将扩散模型的去噪过程建模为一个内部 MDP，使用 PPO 算法进行微调。环境奖励被分配给生成动作块的去噪步骤，从而优化任务性能。

### 3. 实验设计
*   **实验场景**：基于 CrowdNav++ 模拟器，设置 12×12 米的区域，包含 1 个机器人和 20 名随机移动的行人。
*   **基准测试（Benchmarks）**：
    *   **Original Benchmark**：原始无边界约束的环境。
    *   **Bounded CrowdNav（本文引入）**：增加墙壁约束，出界即视为碰撞失败。
*   **对比方法**：
    *   **ORCA**：基于规则的经典避障算法。
    *   **CrowdNav++ (Const. Vel.)**：基于注意力机制和恒定速度预测的强力 RL 基准。
    *   **PDPO w/o action chunk**：消融实验版本，仅使用扩散策略生成单步动作。

### 4. 资源与算力
*   **训练规模**：模型在在线微调阶段训练了约 **1200 万个环境时间步（12M timesteps）**。离线预训练使用了 3000 个演示回合。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或具体的训练耗时。但从参数量来看（PDPO 约 1.05M 参数，少于 CrowdNav++ 的 2.5M），该方法在参数效率上具有优势。

### 5. 实验数量与充分性
*   **实验规模**：最终评估基于 **500 个未见过的测试种子**，并对学习类方法进行了 3 次随机种子训练以计算标准差。
*   **充分性评价**：实验设计较为充分。作者不仅对比了成功率，还分析了导航时间、路径长度、侵入率（社交距离违规）等多个维度。
*   **客观性**：通过引入“有界环境”并揭示原始基准的漏洞（16%-31% 的成功案例实际上是靠出界实现的），体现了极高的学术严谨性和客观性。消融实验清晰地证明了“动作块”和“扩散策略”各自的贡献。

### 6. 主要结论与发现
*   **性能提升**：在更具挑战性的“有界环境”中，PDPO 的成功率达到 **84.7%**，比强基准 CrowdNav++ 高出 **11.7%**。
*   **动作块的重要性**：消融实验显示，如果不使用动作块（仅单步扩散），成功率会下降约 11%，证明了短期规划对复杂避障的关键作用。
*   **多模态表达**：定性分析（可视化）显示，PDPO 能够针对同一观测生成多种可行的避障路径，体现了扩散模型在处理复杂交互时的优越性。

### 7. 优点
*   **创新性结合**：成功将扩散策略与人群导航结合，解决了传统 RL 策略表达力不足的问题。
*   **基准修正**：敏锐察觉并修正了现有评估体系的“出界”漏洞，对该领域后续研究具有重要参考价值。
*   **连贯性与灵活性**：通过动作块实现了局部路径规划，同时通过滚动时域执行保持了对动态行人的实时反馈。

### 8. 不足与局限
*   **计算开销**：扩散模型需要多次迭代去噪（文中为 10-20 步），相比单步策略，其推理延迟更高，可能影响在低功耗嵌入式设备上的实时表现。
*   **模拟与现实差距**：实验完全在模拟器中进行，且行人模型（ORCA）不具备对机器人的反应性，未能在真实世界或具有高度社交智能的人群中验证。
*   **一致性监督**：目前仅执行动作块的第一步，对于动作块中后续步骤的准确性缺乏直接的轨迹级监督。

（完）
