---
title: "SCRAMPPI: Efficient Contingency Planning for Mobile Robot Navigation via Hamilton-Jacobi Reachability"
title_zh: SCRAMPPI：基于 Hamilton-Jacobi 可达性的移动机器人导航高效应急规划
authors: "Raj Harshit Srirangam, Leonard Jung, Rohith Poola, Michael Everett"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26995v1"
tags: ["query:q3"]
score: 8.0
evidence: 带安全约束的轨迹优化
tldr: 针对自主机器人在复杂环境中面临的安全性挑战，本文提出SCRAMPPI框架。该方法将应急规划建模为带有硬安全约束的轨迹优化问题，利用Hamilton-Jacobi (HJ) 可达性分析在线计算安全集的后向可达集值函数。通过将该函数与采样规划器MPPI结合，确保机器人从名义轨迹的任意点都能切换到可行的应急路径。实验证明，该方法在保证硬约束的同时显著提升了采样效率，并在移动机器人对抗逃逸任务中实现了实时规划。
selection_source: fresh_fetch
motivation: 现有的机器人规划方法在处理应急安全约束时，往往在约束严谨性与计算效率之间难以兼得。
method: 结合HJ可达性分析与MPPI采样规划器，通过在线计算安全价值函数来指导重采样，确保应急路径的可行性。
result: 在仿真和硬件实验中，该算法成功实现了移动机器人在对抗环境下的实时名义规划与应急规划。
conclusion: SCRAMPPI通过融合可达性分析与随机优化，为移动机器人提供了一种高效且具有硬安全保证的应急规划方案。
---

## 摘要
自主机器人通常旨在完成名义行为并最小化代价；这使得它们在面对故障或计划外场景时显得脆弱，此时需要一个通往安全集的备份或应急计划，以避免任务彻底失败。这被形式化为一个带有安全约束的名义代价轨迹优化问题：在名义计划的任何一点上，必须存在一条通往指定安全集的可行轨迹。以往的方法要么放宽这一硬约束，要么使用昂贵的基于采样的策略来优化该约束。相反，我们将这一要求形式化为到达-规避（reach-avoid）问题，并利用 Hamilton-Jacobi (HJ) 可达性分析来验证应急可行性。通过在环境显现时在线计算安全集后向可达集的价值函数，并利用基于重采样的展开（rollouts）将其与基于采样的规划器（MPPI）集成，我们在保证满足硬约束的同时，大幅提高了采样效率。最后，我们展示了仿真和硬件实验，证明了我们的算法在对抗性规避任务中，能够在移动机器人上实时生成名义计划和应急计划。

## Abstract
Autonomous robots commonly aim to complete a nominal behavior while minimizing a cost; this leaves them vulnerable to failure or unplanned scenarios, where a backup or contingency plan to a safe set is needed to avoid a total mission failure. This is formalized as a trajectory optimization problem over the nominal cost with a safety constraint: from any point along the nominal plan, a feasible trajectory to a designated safe set must exist. Previous methods either relax this hard constraint, or use an expensive sampling-based strategy to optimize for this constraint. Instead, we formalize this requirement as a reach-avoid problem and leverage Hamilton-Jacobi (HJ) reachability analysis to certify contingency feasibility. By computing the value function of our safe-set's backward reachable set online as the environment is revealed and integrating it with a sampling based planner (MPPI) via resampling based rollouts, we guarantee satisfaction of the hard constraint while greatly increasing sampling efficiency. Finally, we present simulated and hardware experiments demonstrating our algorithm generating nominal and contingency plans in real time on a mobile robot in an adversarial evasion task.

---

## 论文详细总结（自动生成）

以下是对论文《SCRAMPPI: Efficient Contingency Planning for Mobile Robot Navigation via Hamilton-Jacobi Reachability》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
自主机器人在执行任务时，常因传感器故障、电量不足或外部指令需要撤退到“安全集”（如充电站、掩体）。传统的规划方法通常只关注局部避障（Safety-type），而忽视了**存活性应急（Liveness-type contingency）**：即必须保证在名义路径的**任何一点**，都存在一条动力学可行且无碰撞的路径通往预设的安全区域。
*   **现有挑战**：现有的应急规划方法要么放宽了硬约束，要么依赖高昂的采样成本（如 Contingency-MPPI），且无法在有限采样下给出“路径不存在”的确定性证明。
*   **核心动机**：利用 Hamilton-Jacobi (HJ) 可达性分析的严谨性，为机器人提供一种既能保证应急可行性（有形式化证明），又能实时运行的高效规划框架。

### 2. 方法论
论文提出了 **SCRAMPPI** 框架，其核心思想是将应急可行性建模为一个“到达-规避”（Reach-Avoid）问题。
*   **HJ 可达性分析**：通过求解 Hamilton-Jacobi 变分不等式，得到价值函数 $V(x)$。若 $V(x) \le 0$，则证明当前状态 $x$ 存在一条在规定时间 $T_c$ 内到达安全集且避开障碍物的路径。
*   **在线价值函数计算**：利用 GPU 加速的 HJ 求解器（基于 JAX），在机器人感知到新环境（占用网格更新）时，实时重新计算 $V(x)$。
*   **集成 MPPI 与 RBR**：
    *   将 $V(x)$ 作为 **MPPI（模型预测路径积分控制）** 采样过程中的“生存准则”。
    *   引入**基于重采样的展开（Resampling-Based Rollouts, RBR）**：在预测序列中，一旦某个样本违反 $V(x) \le 0$，则将其重采样到安全的样本状态，避免无效计算。
    *   **多模态组内重采样**：为了防止模式崩溃（Mode Collapse），将样本按不同的拓扑路径（由 PRM 引导）分组，仅在组内进行重采样，保留探索多样性。
*   **控制切换**：正常模式下执行 MPPI 优化序列；一旦触发应急信号，立即切换到由 $\nabla V$ 导出的最优反馈控制器，确保进入安全集。

### 3. 实验设计
*   **仿真场景**：在 100 个随机生成的复杂障碍物环境中进行测试，机器人为单轮车（Unicycle）模型，目标是到达终点并始终维持通往稀疏安全点的应急路径。
*   **硬件实验**：使用 AgileX Scout Mini 移动机器人，搭载 LiDAR 进行实时定位与建图，在“对抗逃逸”任务中验证算法。
*   **对比基准（Benchmarks）**：
    1.  **Vanilla MPPI**：无应急意识的标准规划。
    2.  **Contingency-MPPI (C-MPPI)**：使用嵌套采样验证应急路径的方法（分别测试 100 和 1000 个内部采样）。
    3.  **SCRAMPPI (no RBR)**：仅将 $V(x)$ 作为代价惩罚，不使用重采样机制。

### 4. 资源与算力
*   **硬件平台**：实验使用了配备 **NVIDIA RTX 4070 GPU** 的 ROG NUC 电脑。
*   **计算效率**：
    *   **HJ 求解**：在 400×160×60 的网格上，全量重新计算价值函数约需 **43ms**。
    *   **MPPI 采样**：每步规划耗时约 **2ms**。
    *   **整体频率**：硬件部署时运行频率为 **15 Hz**。
*   **显存占用**：SCRAMPPI 仅需 **33.6 MB** VRAM，而 C-MPPI-1000 在同等条件下需要 **1159.7 MB**，显存效率提升约 34 倍。

### 5. 实验数量与充分性
*   **仿真规模**：进行了 100 组随机环境实验，涵盖了成功率、平均步数、有效样本量（ESS）和安全性指标。
*   **消融实验**：对比了有无 RBR 机制的效果，证明了 RBR 能显著提升采样效率（ESS 从 0.321 提升至 0.446）。
*   **充分性评价**：实验设计较为充分，不仅有统计学意义上的定量仿真，还有实物机器人的定性演示，涵盖了从算法性能到资源消耗的多维度对比，评估过程客观公平。

### 6. 主要结论与发现
*   **100% 安全保证**：SCRAMPPI 在所有测试中均实现了 100% 的有效应急率，而基于采样的 C-MPPI 即使增加到 1000 个采样，仍存在无法找到可行路径的情况。
*   **高效性**：由于将复杂的嵌套路径搜索简化为点对点的价值函数查表，SCRAMPPI 的计算速度远快于同类应急规划器。
*   **环境适应性**：通过在线更新 $V(x)$，算法能处理部分未知环境，且随着环境被探测，可行集 $C$ 会单调增长（保守性逐渐降低）。

### 7. 优点
*   **理论严谨**：首次将 HJ 可达性分析引入应急规划，提供了硬性的可行性证明（Certificate）。
*   **采样效率高**：结合 RBR 机制，解决了非凸约束下 MPPI 样本容易失效的问题。
*   **资源友好**：显存占用不随规划时界（Horizon）爆炸，适合嵌入式部署。
*   **多模态保留**：组内重采样策略有效解决了复杂环境下的路径多样性问题。

### 8. 不足与局限
*   **维度灾难**：HJ 求解器随状态维度呈指数级增长，目前仅限于 3-4 维系统（如单轮车），难以直接扩展到高维机械臂。
*   **静态环境假设**：当前模型假设环境是静态的，未考虑动态障碍物（如行人）对可达集的影响。
*   **保守性**：在价值函数重新计算的间隙，算法采用悲观假设（未知区域视为障碍），可能导致规划路径过于保守。

（完）
