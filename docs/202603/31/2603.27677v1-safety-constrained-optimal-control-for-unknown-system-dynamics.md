---
title: Safety-Constrained Optimal Control for Unknown System Dynamics
title_zh: 未知系统动力学的安全约束最优控制
authors: "Panagiotis Kounatidis, Andreas A. Malikopoulos"
date: 2026-03-29
pdf: "https://arxiv.org/pdf/2603.27677v1"
tags: ["query:q2"]
score: 8.0
evidence: 机器人实验平台的安全约束最优控制
tldr: 该研究针对系统动力学模型不精确的问题，提出了一种受安全约束的连续最优控制框架。通过在哈密顿函数中引入偏差惩罚项并应用庞特里亚金极小值原理，推导出一种鲁棒的控制策略。研究证明了该策略在特定条件下与真实最优控制的一致性，并通过机器人巡航控制实验验证了其在处理未知动力学和安全约束方面的有效性。
selection_source: fresh_fetch
motivation: 旨在解决当系统动力学模型不完整或存在偏差时，如何确保最优控制的有效性与安全性。
method: 在基于模型的哈密顿泛函中引入偏差惩罚项，并利用庞特里亚金极小值原理推导控制律及一致性条件。
result: 理论上确定了模型策略等同于真实最优策略的条件，并在实际机器人平台上成功实现了带安全约束的巡航控制。
conclusion: 该框架为动力学不确定环境下的安全最优控制提供了有效的理论支撑和实践指导。
---

## 摘要
本文提出了一种在真实系统动力学通过不完美模型进行近似的情况下，解决连续最优控制问题的框架。我们通过将庞特里亚金极小值原理应用于基于模型的哈密顿泛函来推导控制策略，该泛函包含一个额外的惩罚项，用于捕捉模型与真实系统之间的偏差。随后，我们在温和的凸性假设下，推导了该基于模型的策略与真实系统最优控制策略相一致的条件。最后，我们在具有安全距离约束的巡航控制应用中，通过真实的机器人测试平台验证了该框架。

## Abstract
In this paper, we present a framework for solving continuous optimal control problems when the true system dynamics are approximated through an imperfect model. We derive a control strategy by applying Pontryagin's Minimum Principle to the model-based Hamiltonian functional, which includes an additional penalty term that captures the deviation between the model and the true system. We then derive conditions under which this model-based strategy coincides with the optimal control strategy for the true system under mild convexity assumptions. We demonstrate the framework on a real robotic testbed for the cruise control application with safety distance constraints.

---

## 论文详细总结（自动生成）

这篇论文针对系统动力学模型不精确时的安全约束最优控制问题，提出了一种理论框架与实验验证。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在实际应用（如自动驾驶）中，精确的物理动力学模型往往难以获得或建模成本极高。传统的最优控制理论（如 PMP、动态规划）通常假设模型是完美的。当使用不精确的近似模型（Digital Twin）进行控制综合时，模型偏差会导致性能下降甚至安全隐患。
*   **研究动机**：探索在何种结构化条件下，基于“近似模型”推导出的控制策略能够与“真实系统”的最优策略保持一致，从而在动力学未知的情况下实现安全的最优控制。

### 2. 方法论
*   **核心思想**：通过构建一个带有**惩罚项（Penalty Term）**的代理最优控制问题，将模型与真实系统状态之间的偏差引入代价函数，利用庞特里亚金极小值原理（PMP）分析两者的等价性。
*   **关键技术细节**：
    *   **代理代价函数**：在原始运行代价 $\ell$ 的基础上，增加了一个随时间变化的惩罚项 $\beta(t)\|x(t) - \hat{x}(t)\|^2$，其中 $x$ 是模型状态，$\hat{x}$ 是观测到的真实系统状态。
    *   **哈密顿分析**：分别构建真实系统和近似模型的哈密顿函数（Hamiltonian）。
    *   **等价性条件**：论文证明了在满足温和的凸性假设（Assumptions 1-2）和状态对齐（State Alignment）的情况下，如果两个问题的哈密顿量关于控制输入 $u$ 的梯度在极小值点重合，则两者的最优控制轨迹一致。
    *   **二次控制效能特化**：针对常见的二次项控制代价（如 LQR 形式），推导出了更易于验证的充分条件。
*   **逻辑流程**：定义问题 -> 引入带惩罚项的代理模型 -> 应用 PMP 导出最优性条件 -> 证明模型策略与真实策略的等价性。

### 3. 实验设计
*   **实验场景**：使用 **LIMO ROS 2 机器人** 进行巡航控制（Cruise Control）实验。
*   **任务目标**：控制后车（Ego LIMO）跟随前车，在保持参考速度的同时，必须满足安全距离约束（防止追尾）。
*   **模型偏差设置**：
    *   **真实系统（Plant）**：执行器滞后常数 $\hat{\tau}=0.1$，增益 $\hat{k}=1.4$。
    *   **近似模型（Model）**：假设 $\tau=0.3$，增益 $k=1.2$（存在显著参数偏差）。
*   **对比基准（Benchmark）**：
    1.  **原始最优控制**：假设已知真实参数推导出的理论最优解。
    2.  **基于模型的惩罚控制**：使用错误参数模型结合论文提出的框架推导出的解。

### 4. 资源与算力
*   **硬件平台**：Agilex LIMO 移动机器人。
*   **软件环境**：ROS 2 系统，使用扩展卡尔曼滤波（EKF）进行定位。
*   **算力说明**：文中**未明确提到**具体的 GPU 或大规模算力需求。由于该方法基于解析推导和实时状态反馈，而非深度学习训练，其计算开销主要集中在实时求解哈密顿极小化问题（在本例中为解析解），对算力要求较低，适合嵌入式系统。

### 5. 实验数量与充分性
*   **实验规模**：论文展示了两组核心对比实验（模型策略 vs. 真实最优策略）的运行结果，包括位置轨迹图和控制输入对比图。
*   **充分性评价**：
    *   **理论验证充分**：实验结果清晰地展示了即使在模型参数错误的情况下，控制输入 $u_{mb}$ 与 $u_{opt}$ 几乎完全重合，有力支持了理论推导。
    *   **多样性略显不足**：实验主要集中在单一的巡航控制场景，缺乏不同程度模型偏差或更复杂非线性动力学系统的广泛消融实验。

### 6. 主要结论与发现
*   **结构等价性**：惩罚项虽然改变了状态和协态（costate）的演化，但并不改变哈密顿函数关于控制输入 $u$ 极小化问题的结构。
*   **解耦模型精度与最优性**：只要学习过程或反馈机制能够保持哈密顿极小化的结构属性，即使模型不精确，也能获得真实系统的最优控制策略。
*   **安全约束有效性**：该框架能够成功处理带安全约束的情况，使系统在约束边界（如安全距离）处表现出与真实最优解一致的行为。

### 7. 优点
*   **理论严谨**：提供了严格的数学证明，确定了模型近似不影响最优性的边界条件。
*   **实用性强**：不需要精确的系统辨识，降低了对高保真模型的依赖，非常适合数字孪生和机器人应用。
*   **实时性好**：推导出的控制律结构简单，易于在实时硬件上部署。

### 8. 不足与局限
*   **状态观测依赖**：该框架假设真实系统状态 $\hat{x}$ 是完全可观测的，在存在高噪声或部分观测的环境下性能可能受限。
*   **凸性限制**：理论保证依赖于哈密顿函数对控制输入的凸性假设，对于高度非线性的复杂系统，可能难以满足或求解。
*   **实验复杂度**：目前的物理实验场景相对简单（一维运动控制），尚未在多智能体协同或高维度复杂任务中验证其鲁棒性。

（完）
