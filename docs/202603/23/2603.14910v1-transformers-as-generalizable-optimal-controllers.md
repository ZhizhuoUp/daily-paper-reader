---
title: Transformers As Generalizable Optimal Controllers
title_zh: Transformer 作为可泛化的最优控制器
authors: "Turki Bin Mohaya, Maitham F. AL-Sunni, John M. Dolan, Peter Seiler"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.14910v1"
tags: ["query:q2"]
score: 8.0
evidence: 最优状态反馈律与LQR生成的轨迹
tldr: 本研究探讨了Transformer作为通用控制器在异构线性时不变（LTI）系统中的应用。通过在不同维度的LQR轨迹上进行训练，研究者利用标准化、填充和掩码损失等技术构建了统一的Transformer策略。该策略仅需历史状态即可实现接近最优的控制，无需系统矩阵，且在面对参数扰动和未知系统时表现出良好的鲁棒性与泛化能力，证明了其作为结构化线性系统族近似最优反馈律的潜力。
selection_source: carryover_cache
motivation: 探索单个学习到的控制器是否能捕捉不同维度和特性的异构线性系统的最优状态反馈律。
method: 使用Transformer模型在多种LQR生成的轨迹上进行训练，通过共享表示和维度编码处理不同维度的输入输出。
result: 该策略在广泛的系统集合上实现了极低的次优性，在参数扰动下保持稳定，并能通过微调快速适应新系统。
conclusion: Transformer策略是结构化线性系统族中近似最优反馈律的有效且通用的函数近似器。
---

## 摘要
我们研究了一系列异构多输入多输出（MIMO）线性时不变（LTI）系统的最优状态反馈律是否可以由单个学习得到的控制器来捕捉。我们在具有不同状态和输入维度的系统所产生的 LQR 轨迹上训练了一个 Transformer 策略，并采用了包含标准化、填充、维度编码和掩码损失的共享表示。该策略将最近的状态历史映射到控制动作，在推理阶段无需系统矩阵。在广泛的系统集合中，该策略相对于线性二次型调节器（LQR）实现了经验上的极小次优性，在适度的参数扰动下保持稳定，并能从对未见系统的轻量级微调中获益。这些结果支持将 Transformer 策略作为结构化线性系统族上近优反馈律的实用近似器。

## Abstract
We study whether optimal state-feedback laws for a family of heterogeneous Multiple-Input, Multiple-Output (MIMO) Linear Time-Invariant (LTI) systems can be captured by a single learned controller. We train one transformer policy on LQR-generated trajectories from systems with different state and input dimensions, using a shared representation with standardization, padding, dimension encoding, and masked loss. The policy maps recent state history to control actions without requiring plant matrices at inference time. Across a broad set of systems, it achieves empirically small sub-optimality relative to Linear Quadratic Regulator (LQR), remains stabilizing under moderate parameter perturbations, and benefits from lightweight fine-tuning on unseen systems. These results support transformer policies as practical approximators of near-optimal feedback laws over structured linear-system families.

---

## 论文详细总结（自动生成）

这是一份关于论文《Transformers As Generalizable Optimal Controllers》的结构化总结：

### 1. 核心问题与整体含义
该研究探讨了**Transformer 模型是否能成为跨异构系统的通用最优控制器**。在机器人和信息物理系统（CPS）中，系统动力学常因负载变化、磨损或环境改变而波动。传统方法（如 LQR）需要精确的系统矩阵（A, B），而本研究旨在训练一个单一的 Transformer 策略，使其仅通过观察最近的状态历史，就能在具有不同状态和输入维度的多种线性时不变（LTI）系统上，产生接近最优的控制动作。

### 2. 方法论
核心思想是将最优控制建模为一个**序列到动作（Sequence-to-Action）**的模仿学习问题。
*   **数据采集**：针对 N 个不同的 LQR 问题实例，利用代数黎卡提方程（ARE）生成最优状态反馈增益 $K^*$，并模拟产生大量最优轨迹作为监督信号。
*   **共享表示与标准化**：
    *   **系统级标准化**：对每个系统的状态和输入进行均值和标准差归一化，消除不同物理量纲的影响。
    *   **零填充（Padding）**：将不同维度的状态和输入填充至预设的最大维度 $n_{max}$，以适配统一的 Transformer 输入层。
    *   **维度编码**：将原始系统的状态维度 $n_x$ 和输入维度 $n_u$ 转换为二进制编码，并拼接到输入向量中，显式告知模型当前系统的结构。
*   **Transformer 架构**：采用多块（Multi-block）注意力机制。输入为长度为 $w$ 的滑动窗口状态序列，通过位置编码和自注意力层提取特征。
*   **损失函数**：使用**掩码柯西损失（Masked Cauchy Loss）**。掩码确保模型仅在有效的控制维度上受到惩罚，柯西损失则增强了对异常值的鲁棒性。
*   **推理与微调**：推理时无需系统矩阵；对于未见过的系统，通过极少量的样本进行“少样本微调（Few-shot Fine-tuning）”。

### 3. 实验设计
*   **数据集/场景**：
    *   **已见系统（Seen）**：包含倒立摆、Segway 机器人、双臂机器人、六自由度机械臂等 17 种经典 LTI 系统。
    *   **未见系统（Unseen）**：包含非对称振荡器、流体箱、振动梁等 11 种系统，用于测试泛化性。
*   **Benchmark（基准）**：理论最优的 **LQR 控制器**（基于精确模型求解）。
*   **对比与评估**：
    *   计算**相对次优性（Relative Sub-optimality）**，衡量 Transformer 成本与 LQR 最优成本的差距。
    *   **鲁棒性测试**：对系统矩阵 A 和 B 进行 $\pm 30\%$ 的随机参数扰动。

### 4. 资源与算力
*   论文**未明确说明**具体的硬件型号（如 GPU 类型及数量）。
*   **训练参数**：训练了 500 个 epoch，Batch Size 为 4096，学习率为 $10^{-5}$。
*   **数据规模**：包含 850 个系统变体，每个变体 50 条轨迹，每条轨迹 1250 个时间步。

### 5. 实验数量与充分性
*   **实验规模**：总共涉及 28 种不同类型的动力学系统，并对每种系统生成了多个扰动变体，实验覆盖面较广。
*   **消融实验**：作者进行了针对**历史窗口长度 $w$** 和**训练序列数量**的消融研究，验证了窗口长度对“隐式系统辨识”的重要性（发现 $w=12$ 是性能与计算量的平衡点）。
*   **充分性评价**：实验设计较为充分，涵盖了从简单二阶系统到高维机械臂系统的跨度，且包含了参数扰动下的稳定性分析，客观展示了模型在极端情况（如低阻尼系统）下的局限性。

### 6. 主要结论与发现
*   **近优性**：Transformer 策略在大多数已见和未见系统上均实现了极小的次优性（通常在 1% 左右）。
*   **隐式辨识**：模型能够通过状态历史序列隐式地“辨识”系统动力学，从而在不输入 A、B 矩阵的情况下实现精确控制。
*   **泛化与微调**：通过简单的微调，模型能迅速适应从未见过的系统族，表现出类似“基础模型”的迁移能力。
*   **鲁棒性**：在 $\pm 30\%$ 的参数扰动下，Transformer 控制器依然能保持系统稳定。

### 7. 优点
*   **通用性强**：单一模型即可处理不同维度（MIMO）和不同物理特性的异构系统。
*   **推理高效**：部署时不需要在线求解复杂的黎卡提方程或优化问题，仅需前向传播。
*   **结构创新**：通过维度编码和掩码损失解决了 Transformer 处理变维控制问题的架构难题。

### 8. 不足与局限
*   **缺乏理论保证**：目前的稳定性及泛化性结论主要基于经验观察，缺乏控制理论中的硬性稳定性证明（如 Lyapunov 稳定性）。
*   **特定场景失效**：在处理具有**极低阻尼极点**的系统（如实验中的非对称振荡器）时，次优性显著增加，说明模型对高度敏感的动力学特征捕捉尚不完美。
*   **线性限制**：目前仅限于线性系统（LTI），尚未扩展到更复杂的非线性动力学领域。

（完）
