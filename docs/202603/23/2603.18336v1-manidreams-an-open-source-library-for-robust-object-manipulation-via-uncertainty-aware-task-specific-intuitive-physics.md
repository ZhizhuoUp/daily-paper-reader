---
title: "ManiDreams: An Open-Source Library for Robust Object Manipulation via Uncertainty-aware Task-specific Intuitive Physics"
title_zh: ManiDreams：一个通过不确定性感知的任务特定直觉物理实现鲁棒物体操控的开源库
authors: "Gaotian Wang, Kejia Ren, Andrew S. Morgan, Kaiyu Hang"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.18336v1"
tags: ["query:q6"]
score: 9.0
evidence: 基于物理模型的机器人操作不确定性感知规划
tldr: ManiDreams是一个开源的机器人操作框架，旨在解决现实世界操作中的不确定性问题。它通过将不确定性的表示、传播和约束集成到规划循环中，而非仅仅在训练中抑制误差，实现了鲁棒的操作。该框架支持分布状态表示和后端无关的动力学预测，在ManiSkill任务中表现出优于强化学习基准的鲁棒性，适用于多种物理后端和执行器。
selection_source: fresh_fetch
motivation: 传统的动力学模型往往只关注最小化预测误差，而忽略了现实世界操作中固有的不确定性对鲁棒性的挑战。
method: 提出了一个模块化框架，通过“采样-预测-约束”循环，将感知、参数和结构不确定性显式集成到动作优化过程中。
result: 在ManiSkill任务的实验表明，该框架在各种扰动下均能保持稳健性能，显著优于性能退化的强化学习基准。
conclusion: ManiDreams为实现不确定性感知下的鲁棒机器人操作提供了一个灵活、可组合且开源的解决方案。
---

## 摘要
动力学模型，无论是模拟器还是学习到的世界模型，长期以来一直是机器人操控的核心，但大多数模型侧重于最小化预测误差，而非应对一个更根本的挑战：现实世界的操控本质上具有不确定性。我们认为，不确定性下的鲁棒操控从根本上是一个集成问题：不确定性必须在规划循环中进行表示、传播和约束，而不仅仅是在训练过程中被抑制。我们提出并开源了 ManiDreams，这是一个基于直觉物理模型进行不确定性感知操控规划的模块化框架。它通过可组合的抽象实现了这种集成，包括分布状态表示、后端无关的动力学预测以及用于动作优化的声明式约束规范。该框架明确解决了三种不确定性来源：感知、参数和结构。它通过一个“采样-预测-约束”循环封装任何基础策略，根据分布结果评估候选动作，从而在无需重新训练的情况下增强鲁棒性。在 ManiSkill 任务上的实验表明，在强化学习（RL）基准性能显著下降的各种扰动下，ManiDreams 仍能保持鲁棒的性能。在推、取、接以及现实世界部署中的可运行示例展示了其在不同策略、优化器、物理后端和执行器之间的灵活性。该框架已在 https://github.com/Rice-RobotPI-Lab/ManiDreams 公开。

## Abstract
Dynamics models, whether simulators or learned world models, have long been central to robotic manipulation, but most focus on minimizing prediction error rather than confronting a more fundamental challenge: real-world manipulation is inherently uncertain. We argue that robust manipulation under uncertainty is fundamentally an integration problem: uncertainties must be represented, propagated, and constrained within the planning loop, not merely suppressed during training. We present and open-source ManiDreams, a modular framework for uncertainty-aware manipulation planning over intuitive physics models. It realizes this integration through composable abstractions for distributional state representation, backend-agnostic dynamics prediction, and declarative constraint specification for action optimization. The framework explicitly addresses three sources of uncertainty: perceptual, parametric, and structural. It wraps any base policy with a sample-predict-constrain loop that evaluates candidate actions against distributional outcomes, adding robustness without retraining. Experiments on ManiSkill tasks show that ManiDreams maintains robust performance under various perturbations where the RL baseline degrades significantly. Runnable examples on pushing, picking, catching, and real-world deployment demonstrate flexibility across different policies, optimizers, physics backends, and executors. The framework is publicly available at https://github.com/Rice-RobotPI-Lab/ManiDreams

---

## 论文详细总结（自动生成）

### ManiDreams：基于不确定性感知的任务特定直觉物理的鲁棒物体操控开源库

#### 1. 核心问题与整体含义（研究动机和背景）
论文的核心问题在于：**如何在充满不确定性的现实世界中实现鲁棒的机器人操控？**
传统的动力学模型（如物理模拟器或学习到的世界模型）通常致力于最小化预测误差，但在部署时往往忽略了感知噪声、物理参数不准（如摩擦力、质量）以及模型结构失真带来的不确定性。作者认为，鲁棒操控不应仅仅在训练阶段通过领域随机化来“抑制”误差，而应是一个**集成问题**——即在规划循环中显式地表示、传播并约束这些不确定性。为此，论文提出了 ManiDreams 框架，旨在提供一个模块化的、支持多种物理后端的“采样-预测-约束”规划体系。

#### 2. 方法论：核心思想与关键技术
ManiDreams 采用 **“采样-预测-约束”（Sample-Predict-Constrain）** 范式，通过四个核心抽象层实现：
*   **DRIS（领域随机化实例集）**：一种分布式的状态表示。它不只携带单一的状态估计，而是捆绑了一组具有随机物理参数（如不同质量、摩擦力）的对象实例，用以捕捉感知和参数不确定性。
*   **TSIP（任务特定直觉物理）**：后端无关的动力学预测接口。它可以是基于 GPU 并行的物理模拟器（如 ManiSkill3），也可以是预训练的扩散模型（Diffusion World Model）。它负责将当前的 DRIS 状态向前推演。
*   **笼式约束（Caging Constraints）**：用于界定任务安全边界。不同于传统的单点有效性检查，它评估整个 DRIS 分布的扩散情况，确保所有可能的物理演化结果都处于预设的“笼子”内。
*   **求解器（Solver）**：结合动作采样器（如 PPO 策略、高斯采样）和优化器（如 MPPI），在预测的分布结果中筛选出满足约束的最优动作。

**算法流程**：同步执行器状态 -> 构建 DRIS -> 采样候选动作 -> TSIP 并行预测未来分布 -> 笼式约束评估与验证 -> 筛选并执行最优动作块。

#### 3. 实验设计
*   **任务场景**：
    *   **基础任务**：随机物体推行（使用 YCB 数据集）、薄卡片拾取、动态接球。
    *   **基准测试**：使用 ManiSkill3 的默认任务（PushCube、PickCube、PushT）。
    *   **现实部署**：在 Franka Panda 机械臂上进行杂乱环境下的推挤拾取和角落铲起任务。
*   **对比基准（Benchmark）**：标准的 **PPO（近端策略优化）** 强化学习基准。
*   **扰动变量**：实验通过扫描三种扰动强度来测试鲁棒性：感知噪声（标准差）、观测延迟（步数）、物理参数随机化范围。

#### 4. 资源与算力
*   **硬件**：所有实验（包括 TSIP 传播和求解器优化）均在单台配备 **NVIDIA RTX 5070 Ti 笔记本 GPU** 的设备上运行。
*   **算力表现**：
    *   PPO 基准运行耗时约 20.2ms/步。
    *   ManiDreams 在 $N=8$（样本数）和 $m=4$（实例数）配置下耗时约 56.2ms/步。
    *   基于扩散模型的 TSIP 耗时约 55.2ms/步。
*   **训练时长**：文中未详细说明基础策略（PPO）或扩散模型的具体训练时长，重点在于推理和规划阶段的开销。

#### 5. 实验数量与充分性
*   **实验规模**：针对 3 个 ManiSkill 任务，在 3 种扰动类型下进行了广泛的参数扫描。每个配置重复 10 次实验，每次包含 100 个回合（Episode），数据量较为充足。
*   **消融实验**：对 DRIS 实例数量 $m$、求解器样本数 $N$ 以及分布宽度进行了消融研究，验证了各组件对性能的贡献。
*   **客观性**：实验涵盖了从仿真到现实的零样本（Zero-shot）迁移，对比了不同类型的 TSIP 后端，证明了框架的通用性。对比实验在相同条件下进行，仅改变是否开启“采样-预测-约束”循环，具有较高的公平性。

#### 6. 主要结论与发现
*   **鲁棒性显著提升**：在感知噪声、观测延迟和物理参数波动的情况下，ManiDreams 的成功率显著高于 PPO 基准。
*   **不确定性传播的必要性**：消融实验显示，当 $m=1$（退化为单点估计）时性能大幅下降，证明了分布式表示（DRIS）在应对不确定性时的关键作用。
*   **后端灵活性**：框架成功适配了物理模拟器和学习到的扩散模型，证明了其后端无关的特性。
*   **实时性可行**：尽管增加了计算开销，但在 10-20Hz 的操控任务中依然具有实用性。

#### 7. 优点
*   **模块化设计**：解耦了动力学模型、约束条件和优化算法，易于扩展和复用。
*   **开源贡献**：提供了一个标准化的框架，填补了机器人操控领域缺乏统一不确定性规划工具的空白。
*   **零样本迁移**：通过在规划层处理不确定性，实现了从模拟环境到真实世界（含杂乱背景）的直接部署。

#### 8. 不足与局限
*   **参数调优依赖**：DRIS 的随机化范围和笼式约束的几何参数目前仍需针对具体任务进行手动配置。
*   **计算延迟**：对于极高动态的任务（如高速碰撞），当前的计算开销可能会限制其表现。
*   **模型依赖**：框架的有效性高度依赖于 TSIP 后端的质量，如果物理预测模型与现实差距过大，规划效果会受限。
*   **覆盖范围**：虽然处理了三种不确定性，但对于长程逻辑推理或极其复杂的接触动力学，仍有待进一步验证。

（完）
