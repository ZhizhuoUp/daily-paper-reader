---
title: "FASTER: Rethinking Real-Time Flow VLAs"
title_zh: FASTER：重新思考实时流式 VLA 模型
authors: "Yuxiang Lu, Zhe Liu, Xianzhe Fan, Zhenya Yang, Jinghua Hou, Junyi Li, Kaixin Ding, Hengshuang Zhao"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19199v1"
tags: ["query:q5"]
score: 8.0
evidence: 动作策略中的实时执行与反应时间
tldr: 针对视觉-语言-动作（VLA）模型在物理世界部署中反应延迟高的问题，本文提出了FASTER框架。通过重新审视动作分块策略中的反应机制，研究发现传统流式VLA的恒定采样调度是延迟瓶颈。FASTER引入了视界感知调度，优先处理近期动作采样，将初始反应的去噪步骤压缩至一步，并结合流式客户端-服务器架构，在保持轨迹质量的同时显著提升了机器人在动态任务（如乒乓球）中的实时响应速度。
selection_source: carryover_cache
motivation: 现有的异步推理方法侧重于轨迹平滑度，却忽视了流式VLA模型在应对环境变化时因采样步骤过多导致的反应延迟问题。
method: 提出了FASTER框架，通过视界感知调度自适应地优先采样近期动作，并将即时反应的去噪过程压缩为单步执行。
result: 在消费级GPU上显著降低了有效反应延迟，并在高动态的乒乓球等现实任务中实现了前所未有的实时响应能力。
conclusion: FASTER通过优化采样调度和推理管线，成功解决了通用策略在实时机器人控制中的延迟瓶颈，实现了精准且平滑的动作生成。
---

## 摘要
实时执行对于在物理世界中部署视觉-语言-动作（VLA）模型至关重要。现有的异步推理方法主要优化轨迹平滑度，但忽略了对环境变化做出反应的关键延迟。通过重新思考动作分块（action chunking）策略中的反应概念，本文对决定反应时间的因素进行了系统分析。我们表明，反应时间遵循由首个动作时间（TTFA）和执行时界（execution horizon）共同决定的均匀分布。此外，我们揭示了在基于流（flow-based）的 VLA 中应用恒定调度（constant schedule）的标准做法可能效率低下，并迫使系统在任何动作开始前完成所有采样步骤，从而形成了反应延迟的瓶颈。为了克服这一问题，我们提出了用于即时反应的快速动作采样（FASTER）。通过引入时界感知调度（Horizon-Aware Schedule），FASTER 在流采样过程中自适应地优先处理近期动作，将即时反应的去噪过程（例如在 $π_{0.5}$ 和 X-VLA 中）压缩了十倍至单步完成，同时保持了长时界轨迹的质量。结合流式客户端-服务器流水线，FASTER 显著降低了真实机器人上的有效反应延迟，尤其是在部署于消费级 GPU 时。包括高动态乒乓球任务在内的真实世界实验证明，FASTER 为通用策略解锁了前所未有的实时响应能力，能够快速生成准确且平滑的轨迹。

## Abstract
Real-time execution is crucial for deploying Vision-Language-Action (VLA) models in the physical world. Existing asynchronous inference methods primarily optimize trajectory smoothness, but neglect the critical latency in reacting to environmental changes. By rethinking the notion of reaction in action chunking policies, this paper presents a systematic analysis of the factors governing reaction time. We show that reaction time follows a uniform distribution determined jointly by the Time to First Action (TTFA) and the execution horizon. Moreover, we reveal that the standard practice of applying a constant schedule in flow-based VLAs can be inefficient and forces the system to complete all sampling steps before any movement can start, forming the bottleneck in reaction latency. To overcome this issue, we propose Fast Action Sampling for ImmediaTE Reaction (FASTER). By introducing a Horizon-Aware Schedule, FASTER adaptively prioritizes near-term actions during flow sampling, compressing the denoising of the immediate reaction by tenfold (e.g., in $π_{0.5}$ and X-VLA) into a single step, while preserving the quality of long-horizon trajectory. Coupled with a streaming client-server pipeline, FASTER substantially reduces the effective reaction latency on real robots, especially when deployed on consumer-grade GPUs. Real-world experiments, including a highly dynamic table tennis task, prove that FASTER unlocks unprecedented real-time responsiveness for generalist policies, enabling rapid generation of accurate and smooth trajectories.

---

## 论文详细总结（自动生成）

### FASTER: 重新思考实时流式 VLA 模型论文总结

#### 1. 论文的核心问题与整体含义
*   **研究背景**：视觉-语言-动作（VLA）模型在机器人控制中表现出色，通常采用“动作分块”（Action Chunking）和“流匹配”（Flow Matching）技术。
*   **核心问题**：现有的实时执行方法（如异步推理）侧重于消除动作块之间的停顿以保证轨迹平滑，但忽视了**反应延迟**（Reaction Latency）。在标准的流式 VLA 中，必须完成所有采样步骤（通常为 10 步）后才能输出第一个动作，这在处理乒乓球等高动态任务时会导致严重的反应滞后。
*   **研究动机**：通过缩短“首个动作时间”（TTFA），在不牺牲长程轨迹质量的前提下，提升机器人在动态环境中的即时响应能力。

#### 2. 论文提出的方法论：FASTER
*   **核心思想**：利用动作序列的因果结构，即近期动作比远期动作更容易预测且约束更强。通过为不同索引的动作分配不同的采样进度，实现近期动作的快速生成。
*   **关键技术细节**：
    *   **视界感知调度（Horizon-Aware Schedule, HAS）**：引入索引相关的命中时间（Hit Time）。公式上，通过设置 $u_i$（第 $i$ 个动作完成去噪的全局时间点），使得首个动作在采样第 1 步时即达到 $u_0$ 并完成去噪，而后续动作随采样进行逐步完成。
    *   **混合调度微调（Mixed Schedule Fine-tuning）**：为了弥补预训练（恒定调度）与推理（HAS 调度）之间的分布差异，在微调阶段以一定概率 $p$ 随机切换两种调度模式，增强模型的鲁棒性。
    *   **流式客户端-服务器接口**：改变以往“整块传输”的模式，服务器每完成一个动作的去噪就立即通过流式协议发送给客户端，实现边采样边执行。
    *   **早停策略（Early Stopping）**：一旦执行时界（Execution Horizon）内的动作全部生成，立即停止后续采样步骤，从而节省算力并提高推理频率。

#### 3. 实验设计
*   **实验场景**：
    *   **真实世界**：高动态乒乓球任务（核心测试）、抓取饮料（泛化性）、折叠毛巾（双臂协作）。
    *   **仿真基准**：LIBERO（多任务学习）、CALVIN（长程指令遵循）、Kinetix（动态物理模拟）。
*   **对比方法**：同步推理（Sync）、朴素异步推理（Naive Async）、最先进的训练时动作调节方法（Training-time RTC）。
*   **硬件平台**：对比了高性能 GPU（RTX 4090）和消费级 GPU（RTX 4060）上的表现。

#### 4. 资源与算力
*   **训练算力**：使用 **8 台 NVIDIA A800 GPU** 进行模型微调。
*   **训练时长**：针对不同任务，微调步数在 30k 到 60k 之间（例如 $\pi_{0.5}$ 在仿真上为 30k 步，在真实任务上为 50k 步）。
*   **推理算力**：重点测试了在 **RTX 4090** 和 **RTX 4060** 上的实时表现，证明了该方法在低端硬件上的有效性。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   真实世界评估：乒乓球 15 次试验，抓取 35 次，折叠毛巾 10 次。
    *   仿真评估：LIBERO 每个任务 50 次试验；CALVIN 评估了 1000 条指令链。
    *   消融实验：针对调度因子 $\alpha$、混合概率 $p$ 进行了详细的参数搜索。
*   **充分性评价**：实验设计较为全面，涵盖了从高动态到常规操作的不同维度，且通过对比不同等级的 GPU 证明了算法的普适性。引入了 TTFA 这一新指标，使得对“反应速度”的衡量更加客观。

#### 6. 论文的主要结论与发现
*   **性能提升**：FASTER 将首个动作的采样步骤从 10 步压缩至 1 步，TTFA 缩短了约 **10 倍**。
*   **动态任务优势**：在乒乓球任务中，FASTER 显著优于所有基线方法，尤其是在算力受限的 RTX 4060 上，成功率提升尤为明显。
*   **精度保持**：在 LIBERO 和 CALVIN 仿真中，尽管采用了激进的加速采样，模型性能仅有轻微下降，证明了 HAS 调度的有效性。
*   **反应时间分布**：揭示了反应时间并非恒定值，而是受推理延迟和执行频率共同影响的随机变量。

#### 7. 优点
*   **即插即用**：无需修改 VLA 模型的底层架构（如 VLM 主干或动作专家模块），仅需简单的微调。
*   **端云友好**：通过流式接口和早停策略，极大地降低了对边缘计算设备的性能要求。
*   **理论支撑**：通过对动作采样路径“直度”（Straightness）的定量分析，为“近期动作更易采样”的直觉提供了科学依据。

#### 8. 不足与局限
*   **微调依赖**：虽然不需要架构改动，但仍需要针对特定任务进行微调以适应 HAS 调度，无法直接应用于纯零样本（Zero-shot）推理。
*   **长程精度风险**：在极其复杂的长程任务中，过于激进的采样调度可能会导致轨迹末端的精度略微下降（如仿真实验所示）。
*   **任务覆盖**：虽然测试了乒乓球，但对于更极端的超高频反应任务（如 >100Hz 需求），VLM 主干本身的推理延迟仍是最终瓶颈。

（完）
