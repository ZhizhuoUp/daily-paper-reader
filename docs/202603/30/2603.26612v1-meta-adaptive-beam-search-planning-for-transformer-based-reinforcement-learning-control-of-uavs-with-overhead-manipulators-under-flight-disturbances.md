---
title: Meta-Adaptive Beam Search Planning for Transformer-Based Reinforcement Learning Control of UAVs with Overhead Manipulators under Flight Disturbances
title_zh: 飞行扰动下基于 Transformer 强化学习控制的带顶部机械臂无人机元自适应束搜索规划
authors: "Hazim Alzorgan, Sayed Pedram Haeri Boroujeni, Abolfazl Razi"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26612v1"
tags: ["query:profile-1"]
score: 8.0
evidence: 扰动下带悬挂机械臂的无人机规划
tldr: 针对无人机载机械臂在飞行扰动下末端执行器跟踪精度低的问题，本文提出一种基于Transformer和双重深度Q网络（DDQN）的强化学习框架。核心创新在于引入元自适应束搜索规划器，利用Transformer Critic进行短时前瞻预测，实现软件在环控制。实验表明，该方法在3自由度空中机械臂任务中显著提升了奖励值并降低了跟踪误差，在外部扰动下表现出极高的稳定性。
selection_source: fresh_fetch
motivation: 无人机机体运动与机械臂之间存在强耦合，外部扰动导致的姿态变化会严重影响末端执行器的路径跟踪精度。
method: 结合Transformer架构与DDQN，通过自适应束搜索规划器在候选控制序列上进行短时前瞻模拟，利用学习到的评价网络指导动作选择。
result: "在3自由度空中机械臂测试中，该方法相比DDQN基准提升了10.2%的奖励，并将平均跟踪误差从6%降低至3%。"
conclusion: 该元自适应规划方法有效解决了空中机械臂在动态扰动下的控制难题，显著增强了系统的鲁棒性和轨迹跟踪性能。
---

## 摘要
配备顶部机械臂的无人机在巡检、维护和基于接触的交互方面具有独特的能力。然而，无人机与其机械臂的运动是紧密耦合的，即使是由风力或控制缺陷引起的微小姿态变化，也会使末端执行器偏离预定路径。这种耦合使得可靠的跟踪变得困难，同时也限制了原本为固定基座机器人设计的基于学习的机械臂控制器的直接应用。在我们的测试中，每当无人机机体出现漂移或快速姿态修正时，这些影响都会持续出现。为了解决这一问题，我们开发了一个结合 Transformer 和双深度 Q 网络 (DDQN) 的强化学习 (RL) 框架，其核心思想是使用自适应束搜索规划器，利用学习到的评价网络 (critic) 作为前向估计器，对候选控制序列进行短时域束搜索。这使得控制器能够通过模拟展开预测末端执行器的运动，而不是直接在实际模型上执行这些动作，从而实现了一种软件在环 (SITL) 方法。前瞻过程依赖于处理短状态序列的 Transformer 评价网络给出的价值估计，而 DDQN 骨干网络则提供保持学习过程稳定所需的单步目标。在相同训练条件下，对 3 自由度空中机械臂进行的评估表明，所提出的元自适应规划器表现出最强的综合性能，与 DDQN 基准相比，奖励增加了 10.2%，平均跟踪误差大幅降低（从约 6% 降至 3%），综合奖励-误差指标提升了 29.6%。与固定束搜索和仅使用 Transformer 的变体相比，当无人机基座因外部干扰而出现漂移时，我们的方法在跟踪目标末端轨迹方面表现出更高的稳定性（保持 5 厘米的跟踪误差）。

## Abstract
Drones equipped with overhead manipulators offer unique capabilities for inspection, maintenance, and contact-based interaction. However, the motion of the drone and its manipulator is tightly linked, and even small attitude changes caused by wind or control imperfections shift the end-effector away from its intended path. This coupling makes reliable tracking difficult and also limits the direct use of learning-based arm controllers that were originally designed for fixed-base robots. These effects appear consistently in our tests whenever the UAV body experiences drift or rapid attitude corrections. To address this behavior, we develop a reinforcement-learning (RL) framework with a transformer-based double deep Q learning (DDQN), with the core idea of using an adaptive beam-search planner that applies a short-horizon beam search over candidate control sequences using the learned critic as the forward estimator. This allows the controller to anticipate the end-effector's motion through simulated rollouts rather than executing those actions directly on the actual model, realizing a software-in-the-loop (SITL) approach. The lookahead relies on value estimates from a Transformer critic that processes short sequences of states, while a DDQN backbone provides the one-step targets needed to keep the learning process stable. Evaluated on a 3-DoF aerial manipulator under identical training conditions, the proposed meta-adaptive planner shows the strongest overall performance with a 10.2% reward increase, a substantial reduction in mean tracking error (from about 6% to 3%), and a 29.6% improvement in the combined reward-error metric relative to the DDQN baseline. Our method exhibits elevated stability in tracking target tip trajectory (by maintaining 5 cm tracking error) when the drone base exhibits drifts due to external disturbances, as opposed to the fixed-beam and Transformer-only variants.

---

## 论文详细总结（自动生成）

### 论文结构化深度总结：飞行扰动下基于 Transformer 强化学习控制的带顶部机械臂无人机元自适应束搜索规划

#### 1. 核心问题与研究背景
本文针对**带顶部机械臂的无人机（Aerial Manipulators）**在执行高精度任务（如墙面巡检、接触式维护）时面临的挑战。核心问题在于：
*   **强动态耦合**：机械臂的运动会改变无人机的重心并诱发姿态变化，而无人机的微小漂移或姿态修正又会使机械臂末端执行器偏离目标。
*   **环境扰动**：风力或控制不完善导致的飞行不稳定，使得传统的固定基座机器人控制算法难以直接应用。
*   **长程依赖与探索效率**：传统强化学习（RL）在处理此类高维、强耦合任务时，往往缺乏对未来状态的预见性，且探索过程可能导致系统失稳。

#### 2. 核心方法论
论文提出了一种结合 **Transformer 架构**与**元自适应束搜索（Meta-Adaptive Beam Search）**的强化学习框架：
*   **Transformer-DDQN 骨干网络**：使用 Transformer 编码器处理历史状态序列，利用注意力机制捕捉时间相关性和动态耦合特征。采用双重深度 Q 网络（DDQN）结构来减少价值估计的过充。
*   **束搜索规划（Beam Search Planning）**：在决策阶段，不直接执行单步最优动作，而是利用学习到的 Q 函数作为启发式估计器，在候选动作序列上进行短时域（Short-horizon）前瞻搜索。
*   **元自适应策略（Meta-Policy）**：这是本文的核心创新。系统根据当前的**跟踪误差、Q 值不确定性（评价网络间的分歧）、误差趋势和剩余时域**，动态调整束搜索的宽度（$B_t$）和深度（$D_t$）。
    *   在轨迹曲率大或不确定性高时，增加搜索宽度以探索更多可能性；
    *   在平滑轨迹上，增加搜索深度以利用预测性，或减小宽度以节省算力。
*   **软件在环（SITL）前瞻**：通过模拟展开（Rollouts）预测末端执行器路径，实现了一种预防性的控制机制。

#### 3. 实验设计
*   **实验场景**：模拟一个 3 自由度（3-DoF）空中机械臂（包含 1 个偏航关节和 2 个俯仰关节）在无人机飞行过程中跟踪墙面上的复杂曲线。
*   **基准模型（Benchmarks）**：
    1.  **DDQN**：基于多层感知机（MLP）的标准双重深度 Q 网络。
    2.  **Transformer-DDQN**：仅使用 Transformer 改进评价网络，但不含束搜索规划。
    3.  **固定束搜索（Fixed Beam Search）**：使用固定参数 $B$ 和 $D$ 的规划器。
*   **对比维度**：累积奖励、平均跟踪误差、收敛速度、在外部扰动下的稳定性。

#### 4. 资源与算力
*   **算力说明**：论文提到 Transformer 的前向计算采用了**批处理（Batched）**方式在 GPU 上运行，以保证实时性。
*   **具体细节**：文中**未明确说明**具体的 GPU 型号（如 RTX 4090 或 A100）、显存占用或具体的训练总时长。但从算法复杂度分析看，其开销主要集中在束搜索的多次 Q 函数查询上。

#### 5. 实验数量与充分性
*   **实验规模**：每个模型变体均进行了 **1500 个 Episode** 的训练，结果基于 **50 次独立运行**的平均值，并给出了标准差（Error Bars）。
*   **消融实验**：在 Pendulum-v1 环境中进行了专门的消融研究，验证了束搜索参数（宽度和深度）与轨迹曲率之间的相关性。
*   **评价**：实验设计较为充分，通过多种轨迹（直线、正弦波、非均匀波）验证了泛化性。对比实验涵盖了从基础 RL 到高级架构的递进，逻辑清晰、客观公平。

#### 6. 主要结论与发现
*   **性能提升**：元自适应规划器相比 DDQN 基准，奖励提升了 **10.2%**，平均跟踪误差从 **6% 降低至 3%**，综合指标提升了 **29.6%**。
*   **稳定性**：在无人机基座存在漂移的情况下，该方法能将末端跟踪误差稳定在 **5 厘米**以内，显著优于非规划类方法。
*   **收敛速度**：自适应规划机制使模型达到 95% 最终性能的速度比非规划方法快了约 **25%**。
*   **搜索规律**：发现高曲率轨迹需要“宽而浅”的搜索，而平滑轨迹适合“窄而深”的搜索。

#### 7. 优点与亮点
*   **动态平衡**：通过元学习解决了束搜索中“计算成本”与“控制精度”之间的矛盾，实现了算力的按需分配。
*   **不确定性感知**：利用评价网络的分歧作为元策略的输入，增强了系统在面对未知扰动时的鲁棒性。
*   **物理一致性**：在规划过程中集成了完整的运动学和动力学模型，确保了模拟展开的轨迹在物理上是可行的。

#### 8. 不足与局限
*   **现实迁移（Sim-to-Real）**：实验完全在仿真环境中完成。虽然考虑了扰动，但真实世界的传感器噪声、执行器延迟和复杂的空气动力学效应（如地面效应）可能比仿真更严苛。
*   **计算开销**：尽管有元自适应调整，但在嵌入式无人机平台上运行 Transformer + 多步束搜索仍可能面临实时性挑战。
*   **特征依赖**：元策略的效果高度依赖于预设的特征向量（如误差趋势、熵等），对于极端未见过的工况可能存在泛化风险。

（完）
