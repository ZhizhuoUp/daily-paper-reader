---
title: "DiffusionAnything: End-to-End In-context Diffusion Learning for Unified Navigation and Pre-Grasp Motion"
title_zh: DiffusionAnything：用于统一导航与抓取前运动的端到端上下文扩散学习
authors: "Iana Zhura, Yara Mahmoud, Jeffrin Sam, Hung Khang Nguyen, Didar Seyidov, Miguel Altamirano Cabrera, Dzmitry Tsetserukou"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26322v1"
tags: ["query:q6"]
score: 8.0
evidence: 机器人操作的统一导航和抓取前运动
tldr: 针对机器人视觉运动规划中目标指定复杂及模型资源消耗大的问题，本文提出 DiffusionAnything。这是一个统一的图像空间扩散策略，通过多尺度特征调制，仅需5分钟自监督数据即可实现米级导航与厘米级抓取前运动。该模型仅依赖 RGB 输入，具备低显存占用和高推理频率，在未知场景中展现出强大的零样本泛化能力。
selection_source: fresh_fetch
motivation: 传统的视觉动作模型计算资源需求高且在未知场景中泛化能力差，难以在端侧设备上实现统一的导航与操作规划。
method: 采用多尺度 FiLM 条件机制、轨迹对齐的深度预测以及基于 AnyTraverse 的自监督注意力机制，构建了一个端到端的扩散策略模型。
result: 模型仅需 2.0 GB 显存即可在 10 Hz 频率下运行，并能在完全未见的场景中实现稳健的零样本泛化。
conclusion: 该研究证明了通过轻量化的扩散模型和自监督学习，可以实现高效、统一且具备强泛化能力的机器人多尺度运动规划。
---

## 摘要
直接从视觉高效预测运动规划仍然是机器人领域的一个基本挑战，通常规划需要明确的目标说明和特定任务的设计。最近的视觉-语言-动作（VLA）模型直接从视觉输入推断动作，但需要巨大的计算资源和大量的训练数据，且在面对新场景时无法实现零样本泛化。我们提出了一种统一的图像空间扩散策略，通过多尺度特征调制，仅需每个任务 5 分钟的自监督数据，即可处理米级导航和厘米级操作。该框架由三项关键创新驱动：(1) 基于任务模式、深度尺度和空间注意力的多尺度 FiLM 条件化，使单一模型能够实现适应任务的行为；(2) 轨迹对齐的深度预测将度量 3D 推理集中在生成的路径点上；(3) 来自 AnyTraverse 的自监督注意力实现了无需视觉语言模型和深度传感器的目标导向推理。该模型纯粹基于 RGB 输入运行（2.0 GB 内存，10 Hz），在实现对新场景的鲁棒零样本泛化的同时，仍适用于机载部署。

## Abstract
Efficiently predicting motion plans directly from vision remains a fundamental challenge in robotics, where planning typically requires explicit goal specification and task-specific design. Recent vision-language-action (VLA) models infer actions directly from visual input but demand massive computational resources, extensive training data, and fail zero-shot in novel scenes. We present a unified image-space diffusion policy handling both meter-scale navigation and centimeter-scale manipulation via multi-scale feature modulation, with only 5 minutes of self-supervised data per task. Three key innovations drive the framework: (1) Multi-scale FiLM conditioning on task mode, depth scale, and spatial attention enables task-appropriate behavior in a single model; (2) trajectory-aligned depth prediction focuses metric 3D reasoning along generated waypoints; (3) self-supervised attention from AnyTraverse enables goal-directed inference without vision-language models and depth sensors. Operating purely from RGB input (2.0 GB memory, 10 Hz), the model achieves robust zero-shot generalization to novel scenes while remaining suitable for onboard deployment.

---

## 论文详细总结（自动生成）

以下是对论文《DiffusionAnything: End-to-End In-context Diffusion Learning for Unified Navigation and Pre-Grasp Motion》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在单一、轻量级的模型中统一处理机器人**米级（导航）**与**厘米级（操作/抓取前运动）**的视觉运动规划。
*   **研究背景**：
    *   传统架构通常将导航与操作分离，导致系统复杂、延迟高且存在累积误差。
    *   现有的视觉-语言-动作（VLA）大模型（如 RT-2, GR00T）虽然试图统一任务，但计算开销巨大（需高性能 GPU）、依赖海量数据，且在未见过的场景中零样本泛化能力较差。
    *   缺乏一种能直接从 RGB 输入、在端侧实时运行且具备跨尺度空间推理能力的通用策略。

### 2. 方法论
*   **核心思想**：构建一个基于**扩散策略（Diffusion Policy）**的统一图像空间规划框架，通过上下文感知（Context-aware）的特征调制，使模型能根据任务需求动态调整其空间关注度和度量精度。
*   **关键技术细节**：
    1.  **多尺度 FiLM 条件化**：在 UNet 编码器的多个尺度上应用特征线性调制（FiLM）。输入向量包含：任务模式（导航/抓取）、深度尺度（米/厘米）和空间注意力图。这使得单一模型能针对导航增强全局特征，针对抓取增强局部物体特征。
    2.  **轨迹对齐深度预测**：模型不预测全图深度，而是仅预测生成轨迹路径点上的深度值。这种设计将 3D 推理集中在任务相关区域，显著降低了计算开销。
    3.  **AnyTraverse 自监督注意力**：利用 AnyTraverse 框架自动生成可通行区域和物体注意力图作为监督信号。模型学习预测这些注意力图，从而在推理时无需视觉语言模型（VLM）即可实现目标导向的行为。
    4.  **目标学习机制**：支持两种模式——自主目标选择（基于场景语义）和用户指定目标（通过参考图进行特征匹配）。

### 3. 实验设计
*   **实验场景**：
    *   **导航**：室内走廊、通往特定家具（桌子、椅子）的路径。
    *   **抓取前规划**：人形机器人面对桌面，对多样化物体进行接近运动。
*   **数据集**：采集了约 20 分钟的 RGB 视频数据，每个特定任务仅需 5 分钟的自监督数据进行微调。
*   **对比基准（Benchmark）**：
    *   **导航类**：与 **NoMaD**（视觉导航扩散策略）对比。
    *   **操作类**：与 **NVIDIA GR00T (n1.5/n1.6)** 在 Unitree G1 人形机器人上的表现进行对比。
*   **评估指标**：成功率（SR）、避障间距（Clearance）、目标接近精度（Distance Error）。

### 4. 资源与算力
*   **硬件环境**：使用单张 **NVIDIA RTX 4090 GPU**。
*   **训练开销**：
    *   联合训练 3 个任务（50 epochs）耗时约 **2 小时**。
    *   新任务微调（10 epochs）仅需 **15-20 分钟**。
*   **推理性能**：
    *   显存占用仅 **2.0 GB**。
    *   推理频率达 **10 Hz**，支持机载实时部署。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了探索导航、目标导向导航、抓取前规划三种核心模式。
*   **充分性评价**：
    *   **客观性**：在真实的人形机器人（Unitree G1）上进行了实机部署，而非仅限于仿真。
    *   **对比公平性**：直接在相同硬件和场景下挑战了 SOTA 的 VLA 模型（GR00T），证明了在数据稀缺和新场景下的优势。
    *   **消融/定性分析**：通过可视化不同任务下的注意力图（图 4）和 3D 轨迹（图 6），直观展示了多尺度调制的有效性。

### 6. 主要结论与发现
*   **跨尺度统一**：通过深度尺度条件化，模型在抓取任务中达到了 **4.71 cm** 的精度，比导航精度（29.68 cm）提升了 6 倍，证明了单一模型处理不同精度任务的可行性。
*   **零样本泛化**：在完全未见的测试场景中，该模型实现了 100% 的导航成功率，而 GR00T 等大模型在面对分布外（OOD）场景时出现了系统性失败。
*   **高效性**：证明了结构化的几何推理（可通行性、空间注意力）比纯端到端的语义映射（VLA）在机器人泛化任务中更有效且更节省资源。

### 7. 优点
*   **极低资源需求**：2GB 显存占用使其能运行在大多数嵌入式计算平台上。
*   **极高数据效率**：仅需 5 分钟数据即可适配新任务，远低于传统模型所需的数小时演示。
*   **无需额外传感器**：纯 RGB 输入，无需昂贵的深度相机或激光雷达，通过学习实现度量推理。
*   **灵活的目标指定**：支持视觉特征匹配，方便用户通过图像指定新目标。

### 8. 不足与局限
*   **监督依赖**：模型性能高度依赖于 AnyTraverse 自动生成的伪标签质量，若底层视觉编码器失效，规划也会受限。
*   **任务深度有限**：目前主要解决“接近物体”的规划（Pre-grasp），尚未涉及复杂的接触力控制或多步长程操作任务。
*   **反馈机制缺失**：目前不包含触觉或力反馈，在处理易碎或重型物体时可能存在风险。
*   **环境动态性**：实验主要在相对静态的室内环境中进行，对高速动态障碍物的处理能力尚待验证。

（完）
