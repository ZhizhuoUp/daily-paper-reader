---
title: Towards Safe Learning-Based Non-Linear Model Predictive Control through Recurrent Neural Network Modeling
title_zh: 基于循环神经网络建模的安全学习型非线性模型预测控制研究
authors: "Mihaela-Larisa Clement, Mónika Farsang, Agnes Poks, Johannes Edelmann, Manfred Plöchl, Radu Grosu, Ezio Bartocci"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24503v1"
tags: ["query:q2"]
score: 9.0
evidence: 高控制率下的安全学习型非线性模型预测控制
tldr: 针对非线性模型预测控制（NMPC）在线计算开销大的问题，本文提出了一种基于循环神经网络建模的序贯近似方法 Sequential-AMPC。该方法通过在预测时域内共享参数生成候选控制序列，并结合安全增强的在线评估与回退机制。相比传统前馈策略，它显著减少了对专家数据的依赖，提高了控制序列的可行性和闭环安全性，在处理高维系统时表现出更优的收敛性和稳定性。
selection_source: fresh_fetch
motivation: 传统的非线性模型预测控制在线计算成本高，而现有的学习近似方法往往需要海量的专家数据和高昂的训练成本。
method: 提出 Sequential-AMPC 策略，通过在预测时域内共享参数生成控制序列，并引入安全增强的在线评估与回退机制。
result: 实验表明该方法在减少专家数据需求的同时，显著提升了候选序列的可行性、闭环安全性和高维系统的学习效率。
conclusion: 该研究证明了通过序贯建模和安全保障机制，可以实现更高效、更安全且数据驱动的非线性模型预测控制近似。
---

## 摘要
非线性模型预测控制 (NMPC) 的实际部署通常受限于在线计算：在高控制频率下求解非线性规划在嵌入式硬件上可能成本高昂，特别是当模型复杂或预测时域较长时。基于学习的 NMPC 近似方法将计算转移到线下，但通常需要大规模专家数据集和昂贵的训练。我们提出了 Sequential-AMPC，这是一种序列神经策略，通过在预测时域内共享参数来生成 MPC 候选控制序列。在部署时，我们将该策略封装在安全增强的在线评估和回退机制中，从而得到 Safe Sequential-AMPC。在多个基准测试中，与朴素的前馈策略基准相比，Sequential-AMPC 所需的专家 MPC 展开显著减少，并能生成具有更高可行率和更优闭环安全性的候选序列。在高维系统上，它在更少的训练轮数中表现出更好的学习动态和性能，同时在普通前馈基准可能停滞不前的情况下保持稳定的验证改进。

## Abstract
The practical deployment of nonlinear model predictive control (NMPC) is often limited by online computation: solving a nonlinear program at high control rates can be expensive on embedded hardware, especially when models are complex or horizons are long. Learning-based NMPC approximations shift this computation offline but typically demand large expert datasets and costly training. We propose Sequential-AMPC, a sequential neural policy that generates MPC candidate control sequences by sharing parameters across the prediction horizon. For deployment, we wrap the policy in a safety-augmented online evaluation and fallback mechanism, yielding Safe Sequential-AMPC. Compared to a naive feedforward policy baseline across several benchmarks, Sequential-AMPC requires substantially fewer expert MPC rollouts and yields candidate sequences with higher feasibility rates and improved closed-loop safety. On high-dimensional systems, it also exhibits better learning dynamics and performance in fewer epochs while maintaining stable validation improvement where the feedforward baseline can stagnate.

---

## 论文详细总结（自动生成）

### 论文结构化总结：基于循环神经网络建模的安全学习型非线性模型预测控制

#### 1. 核心问题与整体含义（研究动机和背景）
非线性模型预测控制（NMPC）在处理具有严格约束的复杂系统时表现优异，但其高昂的在线计算开销限制了在嵌入式硬件上的实时应用。现有的“学习型近似方法”通过训练神经网络（NN）模仿 MPC 策略来降低计算负担，但存在以下痛点：
*   **结构不匹配**：大多数方法使用前馈全连接网络（MLP），忽略了控制序列的时间依赖性。
*   **参数爆炸**：随着预测时域（Horizon）增加，MLP 的输出层参数呈线性增长。
*   **数据饥渴**：训练高质量的近似器通常需要海量的专家演示数据。
*   **安全性缺失**：纯神经网络输出难以保证硬约束的满足。

本文旨在通过引入**循环神经网络（RNN）**建模和**安全增强评估机制**，提升近似 MPC 的参数效率、数据利用率及闭环安全性。

#### 2. 论文提出的方法论
核心思想是将 NMPC 的序列生成过程建模为一个递归过程，并嵌入安全过滤框架。
*   **Sequential-AMPC (Seq-AMPC)**：
    *   **核心架构**：使用 RNN（如简单 RNN 单元）替代 MLP。RNN 通过隐藏状态（Hidden State）传递时间信息，递归地生成整个预测时域内的控制序列 $\hat{u}_{0:N-1}$。
    *   **参数共享**：由于 RNN 在每个时间步共享权重，其参数量不随预测时域 $N$ 的增加而增加（复杂度为 $O(1)$），而 MLP 为 $O(N)$。
    *   **归纳偏置**：RNN 的递归结构与动力学系统的马尔可夫性质相契合，提供了更好的归纳偏置。
*   **Safe Seq-AMPC (安全增强框架)**：
    *   **在线评估**：在部署时，将 RNN 生成的候选序列输入安全检查器。
    *   **回退机制（Fallback）**：如果候选序列不可行（违反约束），则执行“回退策略”（通常是前一时刻解的移位加上末端控制器）。
    *   **成本择优**：如果候选序列可行，则在候选序列与回退序列中选择成本（Cost）更低的一个执行。

#### 3. 实验设计
论文在三个具有挑战性的基准场景上进行了验证：
*   **场景 1：四旋翼无人机（Quadcopter）**：10 维状态，3 维输入，强调姿态调节和高度控制。
*   **场景 2：单轨车辆运动学模型（Kinematic Vehicle）**：4 维状态，包含障碍物规避约束。
*   **场景 3：单轨车辆动力学模型（Dynamic Vehicle）**：8 维状态，考虑侧滑、偏航率耦合及执行器延迟，难度最高。
*   **Benchmark（对比基准）**：
    *   **AMPC (MLP)**：基于全连接网络的近似 MPC（由 Hose 等人在 2023 年提出）。
    *   **专家 NMPC**：作为地面真值（Ground Truth）用于生成训练数据集。

#### 4. 资源与算力
*   **参数规模**：MLP 基准模型约有 100 万个参数，而提出的 RNN 模型仅需约 50 万个参数（减少了 50%）。
*   **训练数据**：四旋翼任务使用了 960 万个样本；车辆任务分别使用了 5.5 万和 11.6 万个样本。
*   **算力说明**：论文**未明确提及**具体的 GPU 型号或训练时长，但通过“训练轮数（Epochs）”进行了间接对比。例如，在四旋翼任务中，Seq-AMPC 达到收敛所需的轮数比 MLP 少约 97%。

#### 5. 实验数量与充分性
*   **实验规模**：在每个任务中使用了 1000 个随机初始状态进行闭环测试，并对 1000 个样本进行了开环可行性统计。
*   **消融/扩展实验**：针对四旋翼任务进行了**数据缩减实验**（分别使用 1x, 1/4x, 1/10x 的数据量），验证了 RNN 在小样本下的鲁棒性。
*   **客观性**：实验涵盖了从简单到复杂的动力学模型，并详细分析了安全机制触发的原因（状态约束、末端约束或成本因素），评估较为全面、客观。

#### 6. 论文的主要结论与发现
*   **更高的可行性**：在开环预测中，Seq-AMPC 生成可行序列的概率显著高于 MLP（例如四旋翼任务从 72% 提升至 83.6%）。
*   **更强的安全性**：在闭环测试中，Safe Seq-AMPC 的安全完成率普遍高于 Safe AMPC。
*   **数据效率显著**：Seq-AMPC 在仅使用 1/10 数据量时的表现，仍能接近 MLP 在全量数据下的性能。
*   **学习动态更稳定**：在复杂的高维动力学任务中，MLP 容易出现验证集损失停滞或发散，而 RNN 展现出更持续的优化能力。

#### 7. 优点（亮点）
*   **参数效率极高**：通过参数共享解决了长时域预测导致的模型肥大问题。
*   **理论支撑扎实**：论文通过数学命题证明了 RNN 在处理序列控制任务时的参数缩放优势和结构偏置优势。
*   **实用性强**：结合了离线学习的效率和在线安全过滤的可靠性，为 NMPC 在低功耗嵌入式设备上的部署提供了可行路径。

#### 8. 不足与局限
*   **干预率依然较高**：在复杂的避障任务中，安全机制的干预率（Intervention Rate）接近饱和（约 95%），说明神经网络生成的原始序列在满足“末端集合约束”方面仍有困难。
*   **计算延迟未量化**：虽然理论上 RNN 参数更少，但 RNN 的递归计算是串行的，而 MLP 是并行的，论文未对比两者在嵌入式硬件上的实际推理延迟（Latency）。
*   **回退策略依赖性**：系统性能在很大程度上仍依赖于保守的回退候选序列，神经网络对性能的实际提升在某些复杂场景下受限。

（完）
