---
title: Collision-Free Velocity Scheduling for Multi-Agent Systems on Predefined Routes via Inexact-Projection ADMM
title_zh: 基于非精确投影 ADMM 的预定义路径多智能体系统无碰撞速度调度
authors: "Seungyeop Lee, Jong-Han Kim"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21913v1"
tags: ["query:q1"]
score: 8.0
evidence: 多智能体系统的无碰撞速度调度
tldr: 本研究针对预定义路径下的多智能体系统，提出了一种基于非精确投影交替方向乘子法（ADMM）的无碰撞速度调度方法。通过优化航点通过时间而非改变路径，利用可微代理轨迹模型捕捉动态特性，并将避障约束转化为时间网格上的距离惩罚。该方法避免了复杂的整数规划，在多种拥堵场景下实现了高效、平滑的协同调度，显著缩短了任务完成时间。
selection_source: fresh_fetch
motivation: 在路径受限的多智能体运输系统中，如何在不改变预设路线的前提下，通过优化速度调度实现高效且无碰撞的协同。
method: 采用可微代理轨迹模型映射航点时间，并利用不含整数变量的非精确投影ADMM算法求解非线性非凸的速度优化问题。
result: 实验表明该方法在随机交叉、瓶颈和图网络场景中均能生成可行且高效的调度方案，且在瓶颈案例中优于分层基准方法。
conclusion: 该算法为路径受限的多智能体协同提供了一种无需离散排序变量的高效数值求解框架，提升了系统的整体通行效率。
---

## 摘要
在结构化多智能体运输系统中，智能体通常必须遵循预定义路径，这使得空间重路由变得不可取或不可能。本文通过优化航点通过时间来解决受路径约束的多智能体协同问题，同时保持每个智能体分配的航点顺序和名义路径分配。一种可微代理轨迹模型将航点时间映射到平滑的位置剖面，并捕捉一阶跟踪滞后，从而能够通过在跨越任务周期的密集时间网格上评估基于距离的惩罚项来编码两两安全性。由此产生的非线性和非凸速度调度问题使用一种非精确投影交替方向乘子法 (ADMM) 算法求解，该算法结合了结构化时间更新与基于梯度的碰撞修正步骤，并避免了显式的整数排序变量。在随机交叉、瓶颈和基于图的网络场景中进行的数值实验表明，所提出的方法在各种拥堵水平下都能计算出可行且高效的时间调度，并且在测试的瓶颈案例中，其任务完成时间比具有代表性的分层基准方法更短。

## Abstract
In structured multi-agent transportation systems, agents often must follow predefined routes, making spatial rerouting undesirable or impossible. This paper addresses route-constrained multi-agent coordination by optimizing waypoint passage times while preserving each agent's assigned waypoint order and nominal route assignment. A differentiable surrogate trajectory model maps waypoint timings to smooth position profiles and captures first-order tracking lag, enabling pairwise safety to be encoded through distance-based penalties evaluated on a dense temporal grid spanning the mission horizon. The resulting nonlinear and nonconvex velocity-scheduling problem is solved using an inexact-projection Alternating Direction Method of Multipliers (ADMM) algorithm that combines structured timing updates with gradient-based collision-correction steps and avoids explicit integer sequencing variables. Numerical experiments on random-crossing, bottleneck, and graph-based network scenarios show that the proposed method computes feasible and time-efficient schedules across a range of congestion levels and yields shorter mission completion times than a representative hierarchical baseline in the tested bottleneck cases.

---

## 论文详细总结（自动生成）

### 论文总结：基于非精确投影 ADMM 的预定义路径多智能体系统无碰撞速度调度

#### 1. 核心问题与研究动机
在城市空中交通（UAM）、自动化仓库物流和走廊机器人作业等场景中，智能体通常受限于预定义的结构化路径（如航路或轨道），无法通过改变空间路径来避障。因此，**无碰撞协同必须完全依赖于对沿路径运动的时间调度（即速度控制）**。
*   **研究动机**：现有的全局协调方法（如基于冲突搜索或混合整数编程 MIP）在处理大规模或高密度场景时，由于离散排序变量导致的组合爆炸，存在计算扩展性差和数值敏感的问题。本文旨在提出一种无需显式整数变量、能够高效处理非凸避障约束的连续优化框架。

#### 2. 方法论
该论文提出了一种基于航点时间优化的速度调度方法，核心技术包括：
*   **可微代理轨迹模型**：为了解决分段恒速模型在航点处速度不连续、导致距离函数不可微的问题，引入了基于**Sigmoid 函数**的平滑过渡模型。该模型不仅能将航点时间映射为连续的位置剖面，还能通过引入时间偏差 $b$ 和缩放参数 $\beta$ 来捕捉实际控制系统的一阶跟踪滞后。
*   **非精确投影 ADMM 算法**：将优化问题转化为共识 ADMM 形式。
    *   **t-更新**：求解一个稀疏线性系统，确定航点时间。
    *   **x-更新**：将速度限制投影到凸集上。
    *   **z-更新（核心创新）**：针对非凸的避障约束，不进行精确投影（计算成本极高），而是采用**非精确投影（Inexact-Projection）**。利用基于梯度的 Polyak 型步骤和**自适应动量机制**来修正碰撞，从而避开数值停滞并处理非凸性。
*   **目标函数**：最小化未指定到达时间智能体的总任务完成时间，同时满足速度边界和连续时间避障约束。

#### 3. 实验设计
论文在多种模拟场景下验证了算法性能：
*   **实验场景**：
    1.  **随机交叉（Random Crossing）**：包括同向（追赶）和对向（迎头）冲突。
    2.  **瓶颈场景（Bottleneck）**：所有智能体必须通过一条狭窄的共享走廊。
    3.  **图网络场景（Graph-based Network）**：基于 Delaunay 三角剖分生成的复杂有向图网络。
*   **对比基准（Benchmark）**：一种代表性的分层时间调度方法，即 **MIP-SOCP（混合整数编程-二阶锥规划）** 框架。
*   **评估指标**：收敛迭代次数、任务完成时间开销（相对于最小行驶时间）、成功率（SR）以及占用密度（Occupancy Density）。

#### 4. 资源与算力
*   论文**未明确说明**具体的硬件配置（如 CPU/GPU 型号）或具体的计算耗时（秒/毫秒）。
*   文中提到 $t$-更新中的系数矩阵是常数，可以预先进行因子分解以提高效率，强调了算法在数值计算上的高效性。

#### 5. 实验数量与充分性
*   **实验规模**：针对对向交叉和图网络场景，进行了 **100 次蒙特卡洛模拟**。
*   **变量控制**：测试了不同的智能体数量（$K=3, 7, 11$）和不同的空间占用密度（$\phi=1\%, 5\%, 10\%$）。
*   **充分性评价**：实验设计较为充分，涵盖了从简单交叉到复杂网络拓扑的多种情况，并通过统计成功率和时间开销验证了算法在不同拥堵程度下的鲁棒性。对比实验也直接针对了现有的主流分层优化方法。

#### 6. 主要结论与发现
*   **高效性**：所提方法在所有测试场景中均能生成可行且高效的速度剖面，迭代次数通常在几百次内即可收敛。
*   **优于基准**：在瓶颈场景中，该方法生成的调度方案比 MIP-SOCP 基准更紧凑，任务完成时间更短。在高密度（$\phi=10\%$）下，基准方法可能失效，而本文方法仍能找到可行解。
*   **拓扑限制**：在图网络中，算法的成功率受限于几何可行性（即安全距离 $d_{safe}$ 与节点间距 $d_{node}$ 的比例）。只要满足几何可达性，算法表现稳定。

#### 7. 优点
*   **无需整数变量**：通过连续优化手段处理排序问题，避免了组合爆炸，提升了扩展性。
*   **动态感知**：代理轨迹模型考虑了跟踪滞后，使生成的轨迹在物理上更易于被底层控制器执行。
*   **自适应修正**：引入自适应动量的非精确投影步骤，有效解决了非凸避障约束带来的数值停滞问题。

#### 8. 不足与局限
*   **物理可行性边界**：在极高密度的对向冲突中，若物理上不存在避障解（受限于速度上下限），算法仍会失败。
*   **离散化依赖**：避障约束是在密集的时间网格上评估的，虽然文中进行了细网格验证，但在极极端情况下仍存在网格间隙发生碰撞的理论风险。
*   **缺乏实物验证**：所有实验均在仿真环境下完成，未在真实多机器人系统或具有复杂非线性动力学的平台上进行测试。

（完）
