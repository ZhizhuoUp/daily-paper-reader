---
title: "Dynamic Control Barrier Function Regulation with Vision-Language Models for Safe, Adaptive, and Realtime Visual Navigation"
title_zh: 基于视觉语言模型的动态控制障碍函数调节，用于安全、自适应且实时的视觉导航
authors: "Jeffrey Chen, Rohan Chandra"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21142v1"
tags: ["query:q1"]
score: 8.0
evidence: 通过控制屏障函数实现实时避障
tldr: 针对机器人动态导航中控制屏障函数（CBF）参数固定导致的保守或冒险问题，本文提出AlphaAdj框架。该框架利用视觉语言模型（VLM）根据第一视角RGB图像实时评估环境风险，并动态调节CBF的安全约束强度。通过引入几何限速机制和陈旧度门控融合策略解决VLM推理延迟问题，在保证碰撞规避的同时，显著提升了导航效率和鲁棒性。
selection_source: fresh_fetch
motivation: 传统的CBF安全滤波器通常采用固定参数，难以在复杂多变的动态环境中平衡导航的安全性和效率。
method: 提出AlphaAdj框架，利用VLM从视觉输入中提取风险标量来动态更新CBF参数，并结合几何限速和异步融合策略应对推理延迟。
result: "实验表明，AlphaAdj在多种静态和动态场景中将导航效率提升了高达18.5%，且在成功率和鲁棒性上优于固定参数及无限制基准。"
conclusion: 通过VLM实时调节安全约束参数，可以有效解决传统安全控制器的局限性，实现更智能、高效且实时的视觉导航。
---

## 摘要
在动态、非结构化环境中运行的机器人必须在感知可能受限的情况下平衡安全性和效率。虽然控制障碍函数（CBF）通过安全滤波提供了一种原则性的避障方法，但其行为通常由固定参数决定，这可能导致在良性场景中过于保守，或在危险附近过于宽松。我们提出了 AlphaAdj，这是一个从视觉到控制的导航框架，它利用第一视角 RGB 输入实时调整 CBF 安全滤波器的保守程度。视觉语言模型（VLM）根据当前摄像机视图生成一个有界的标量风险估计值，我们将其映射以动态更新 CBF 参数，从而调节安全约束的执行强度。为了解决实际应用中的异步推理和显著的 VLM 延迟问题，我们将几何速度感知动态上限和陈旧度门控融合策略与轻量级实现方案相结合，从而降低了端到端推理开销。我们在多种环境下的多个静态和动态障碍物场景中评估了 AlphaAdj，并与固定参数和无上限的消融实验进行了对比。结果表明，AlphaAdj 在保持无碰撞导航的同时，相对于固定设置将效率（就路径长度和到达目标的时间而言）提高了多达 18.5%，并相对于无上限基准提高了鲁棒性和成功率。

## Abstract
Robots operating in dynamic, unstructured environments must balance safety and efficiency under potentially limited sensing. While control barrier functions (CBFs) provide principled collision avoidance via safety filtering, their behavior is often governed by fixed parameters that can be overly conservative in benign scenes or overly permissive near hazards. We present AlphaAdj, a vision-to-control navigation framework that uses egocentric RGB input to adapt the conservativeness of a CBF safety filter in real time. A vision-language model(VLM) produces a bounded scalar risk estimate from the current camera view, which we map to dynamically update a CBF parameter that modulates how strongly safety constraints are enforced. To address asynchronous inference and non-trivial VLM latency in practice, we combine a geometric, speed-aware dynamic cap and a staleness-gated fusion policy with lightweight implementation choices that reduce end-to-end inference overhead. We evaluate AlphaAdj across multiple static and dynamic obstacle scenarios in a variety of environments, comparing against fixed-parameter and uncapped ablations. Results show that AlphaAdj maintains collision-free navigation while improving efficiency (in terms of path length and time to goal) by up to 18.5% relative to fixed settings and improving robustness and success rate relative to an uncapped baseline.

---

## 论文详细总结（自动生成）

### 论文总结：AlphaAdj —— 基于视觉语言模型的动态 CBF 调节框架

#### 1. 核心问题与研究动机
在机器人导航中，**控制障碍函数（CBF）** 是确保安全避障的核心数学框架。然而，传统 CBF 的安全参数（如 $\alpha$）通常是**固定**的：
*   **过于保守：** 在空旷区域速度过慢，导致效率低下。
*   **过于冒险：** 在复杂或动态危险区域反应不足，导致碰撞。
*   **感知局限：** 传统方法多依赖几何距离，缺乏对环境语义（如“行人可能突然横穿”）的理解。
*   **延迟挑战：** 视觉语言模型（VLM）虽具备语义理解力，但推理延迟高，难以直接用于高频实时控制。

本文旨在通过 VLM 实时评估环境风险并动态调节 CBF 参数，在保证安全的前提下提升导航效率。

#### 2. 方法论核心思想
AlphaAdj 框架结合了高层语义感知与底层安全滤波：
*   **VLM 风险评估：** 机器人以 2Hz 频率异步调用 VLM（LLaVA-1.5 7B），输入第一视角 RGB 图像及当前速度、距离等上下文，输出标量风险值 $r \in [0, 1]$。
*   **风险到参数的映射：** 使用幂律函数将风险 $r$ 映射为 CBF 参数 $\alpha_{vlm}$。风险越高，$\alpha$ 越小（越保守）；风险越低，$\alpha$ 越大（越激进）。
*   **延迟感知融合策略：**
    *   **动态几何上限（Dynamic Cap）：** 基于机器人与最近障碍物的距离及当前速度计算一个“安全包络线”，防止 VLM 给出过于激进的指令。
    *   **陈旧度门控（Staleness Gating）：** 监测 VLM 响应时间。若响应超时（陈旧），系统自动回退到基于几何距离的保守“硬上限”模式；若响应及时，则在 VLM 建议值与“软上限”之间取最小值。
*   **实时控制：** 底层控制器以 60Hz 运行，通过求解二次规划（QP）问题，在满足动态 $\alpha$ 约束的同时，尽可能接近目标速度。

#### 3. 实验设计与基准对比
*   **实验环境：** 使用 NVIDIA IsaacSim 仿真平台。
*   **测试场景：** 包含“单障碍物”、“杂乱场”、“动态交叉障碍物”和“动态正面障碍物”四种典型任务。
*   **对比环境：** 简单的“玩具环境（Toy）”和复杂的“仓库环境（Warehouse）”。
*   **对比基准（Baselines）：**
    1.  **固定参数法：** 分别固定 $\alpha$ 为最小值（极保守）和最大值（极冒险）。
    2.  **无上限消融（No Cap）：** 仅使用 VLM 输出，不加几何上限约束。
    3.  **AlphaAdj（本文方法）：** 包含 VLM 调节、动态上限和延迟处理。

#### 4. 资源与算力
*   **硬件：** AMD Ryzen Threadripper PRO 7955WX 处理器，**NVIDIA RTX 6000 Ada GPU**。
*   **模型优化：** 使用了 **4-bit 量化** 技术的 LLaVA-1.5 7B 模型，通过 FastAPI 本地部署。
*   **延迟表现：** 经过优化（限制输出格式、量化等），端到端延迟从 1482ms 降至 **695ms**（提升 53%）。

#### 5. 实验数量与充分性
*   **实验规模：** 在 8 个“环境-场景”组合中进行了测试，每组实验重复运行 **25 次**并取平均值。
*   **充分性：** 实验涵盖了静态与动态障碍物，并对比了不同固定参数下的性能曲线（Sweep）。消融实验验证了“动态上限”在应对 VLM 延迟和异常值时的必要性。整体实验设计较为客观，充分考虑了实时系统中的异步性问题。

#### 6. 主要结论与发现
*   **安全性：** AlphaAdj 在所有测试场景中均实现了 **100% 的成功率**（零碰撞），而固定大参数或无上限的方法均出现了碰撞。
*   **效率提升：** 相比于固定保守参数（$\alpha_{min}$），AlphaAdj 将路径长度缩短了约 **18.5%**，到达目标的时间缩短了约 **10.8%**。
*   **鲁棒性：** 即使在 VLM 推理存在近 0.7 秒延迟的情况下，通过陈旧度检测和几何备份机制，系统依然能保持稳定运行，证明了异步架构的有效性。

#### 7. 优点与亮点
*   **语义与几何结合：** 巧妙地利用 VLM 提取环境语义风险，同时用几何规则兜底，解决了大模型“幻觉”或失效带来的安全隐患。
*   **实时性优化：** 针对 VLM 部署做了大量工程优化（量化、异步、指令精简），使其能融入 60Hz 的控制循环。
*   **自适应性：** 机器人能像人类一样，在开阔地带加速，在狭窄或危险地带主动减速并增大避让距离。

#### 8. 不足与局限
*   **仿真依赖：** 实验完全在 IsaacSim 仿真中完成，尚未在真实物理机器人和复杂光照/传感器噪声环境下验证。
*   **风险定义主观：** VLM 输出的风险值 $r$ 缺乏严格的数学定义，高度依赖 Prompt 的设计和预训练模型的先验知识。
*   **场景多样性有限：** 虽然包含动态障碍物，但环境种类（仓库、玩具场景）仍较少，未测试极端拥挤或高度非结构化的室外环境。

（完）
