---
title: Distributed MPC For Coordinated Path-Following
title_zh: 用于协同路径跟踪的分布式模型预测控制
authors: "Lusine Poghosyan, Anna Manucharyan, Mikayel Aramyan, Naira Hovakimyan, Tigran Bakaryan"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24748v1"
tags: ["query:q2"]
score: 8.0
evidence: 用于协同路径跟随的分布式模型预测控制
tldr: 本文针对多智能体协同路径跟踪问题，提出了一种分布式模型预测控制（MPC）算法。该方法基于时空解耦框架，将协调问题简化为一维时间协调，并利用归一化拉普拉斯矩阵的特性将MPC动力学解耦为独立模式。研究证明了在预测时域为1且网络连通的情况下，系统具有指数稳定性。该方案不仅通信开销低、实时性强，还能灵活处理车辆限制、避障及冲突解决等约束，在复杂任务中表现出极高的灵活性和鲁棒性。
selection_source: fresh_fetch
motivation: 旨在解决多智能体协同路径跟踪中，如何在分布式架构下实现高效的时间协调并处理复杂的任务约束。
method: 采用时空解耦框架，结合归一化拉普拉斯矩阵解耦动力学，构建了一种支持约束优化的分布式MPC方案。
result: 理论证明了系统在特定条件下的指数稳定性，并通过仿真验证了其在通信故障和复杂场景下的收敛性。
conclusion: 该分布式MPC方案为大规模多智能体协同控制提供了一种可扩展、高效且具备理论稳定性保障的解决方案。
---

## 摘要
本文研究了一种用于协同路径跟踪的分布式模型预测控制（MPC）算法。基于时间关键型协同路径跟踪框架（该框架将空间与时间解耦，并将协同问题简化为一维设置），我们制定了一种用于时间协同的分布式 MPC 方案。利用归一化拉普拉斯矩阵的性质，我们将 MPC 动力学解耦为独立模态，并推导出了连接当前状态与预测状态的递归关系。利用该结构，我们证明了在预测时域 $K=1$ 且通信网络固定连通的情况下，即使存在路径跟踪误差，系统也是指数稳定的。这为该框架下离散时间分布式 MPC 方案的收敛性分析提供了首个结果。所提出的方法能够实现具有低通信开销的可扩展且高效的实时部署。此外，与传统的时间关键型协同路径跟踪框架相比，这种基于优化的结构通过允许纳入特定任务需求（如车辆限制、避障和冲突解决），放宽了对预规划的依赖。仿真结果证明了其在复杂场景下的适用性，突显了在通信故障下的灵活性和指数收敛性。

## Abstract
In this paper, we consider a distributed model predictive control (MPC) algorithm for coordinated path-following. Relying on the time-critical cooperative path-following framework, which decouples space and time and reduces the coordination problem to a one-dimensional setting, we formulate a distributed MPC scheme for time coordination. Leveraging properties of the normalized Laplacian, we decouple the MPC dynamics into independent modes and derive a recursive relation linking current and predicted states. Using this structure, we prove that, for prediction horizon $K=1$ and a fixed connected communication network, the system is exponentially stable even in the presence of path-following errors. This provides a first result on the convergence analysis of discrete-time distributed MPC schemes within this framework.   The proposed approach enables scalable and efficient real-time implementation with low communication overhead. Moreover, in contrast to the time-critical cooperative path-following framework, the optimization-based structure relaxes the reliance on preplanning by allowing the incorporation of mission-specific requirements, such as vehicle limitations, collision avoidance, and conflict resolution. Simulation results demonstrate applicability to complex scenarios, highlighting agility and exponential convergence under communication failures.

---

## 论文详细总结（自动生成）

这是一份关于论文《Distributed MPC For Coordinated Path-Following》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
该论文针对多无人机（UAV）系统的**协同路径跟踪（Coordinated Path-Following）**问题，旨在解决如何在分布式架构下实现多个智能体的时间同步，同时满足物理约束（如速度、加速度限制）和任务需求（如避障、冲突解决）。
*   **研究动机**：传统的比例积分（PI）协调控制器虽然高效，但高度依赖预规划，难以灵活处理动态约束或任务变更。
*   **核心背景**：采用“时间关键型协同路径跟踪”框架，将问题解耦为空间（路径跟踪）和时间（协同控制）两个维度。本文重点研究时间维度的分布式模型预测控制（DMPC）方案。

### 2. 论文提出的方法论
核心思想是将协同问题转化为一维的虚拟时间（Virtual Time）同步问题，并利用 DMPC 进行求解。
*   **核心变量**：引入虚拟时间 $\gamma_i$ 表示进度，定义偏差变量 $\delta_i$（虚拟时间与名义时间的差）及其变化率。
*   **DMPC 建模**：每个智能体在每个采样时刻求解一个局部有限时域优化问题。代价函数包含三部分：与邻居的同步误差（基于拉普拉斯矩阵）、维持名义速度的惩罚、控制能量消耗。
*   **关键技术细节**：
    *   **模态解耦分析**：利用归一化拉普拉斯矩阵的谱性质，将复杂的 $N$ 智能体动力学解耦为独立的模态。
    *   **稳定性证明**：针对预测时域 $K=1$ 的情况，推导出了闭环系统的 $2 \times 2$ 递归矩阵。利用 Gelfand 公式证明了在固定连通网络下，该矩阵是收缩的，从而证明了系统的**指数稳定性**。
    *   **误差补偿**：引入修正项 $\alpha_i^k$ 来处理底层路径跟踪产生的几何误差。

### 3. 实验设计
*   **实验场景**：
    1.  **基准测试**：不同数量的无人机（5、10、15台）在圆形路径上进行协同。
    2.  **复杂任务（走廊导航）**：模拟多机通过狭窄走廊，要求依次进入以避免碰撞。
*   **对比方法（Benchmark）**：
    *   对比了不同预测时域 $K$（1, 5, 10, 25）对收敛速度和计算开销的影响。
    *   在走廊场景中，对比了“离线预定义顺序”与“基于博弈的自主排序”两种策略。
*   **仿真工具**：使用高保真多旋翼仿真器 **RotorPy**，物理平台模拟 Bitcraze Crazyflie 无人机。

### 4. 资源与算力
*   **硬件环境**：GIGABYTE G5 MF5 笔记本电脑，配置 Intel Core i7-13620H 处理器（16核），32 GiB RAM。
*   **计算效率**：
    *   当 $K=1$ 时，平均更新频率约为 **100 Hz**。
    *   单次 MPC 求解时间在 0.005s 到 0.01s 之间（见表 I）。
    *   文中指出，随着无人机数量增加，计算时间几乎保持不变，体现了良好的可扩展性。

### 5. 实验数量与充分性
*   **实验规模**：进行了多组消融实验，涵盖了不同智能体规模（5-15台）和不同预测时域。
*   **充分性评价**：实验设计较为充分。不仅验证了理论证明的 $K=1$ 情况，还探索了更大 $K$ 值的表现。走廊导航实验展示了该方法在处理非线性逻辑（如竞争通过）时的灵活性。实验考虑了通信延迟和路径跟踪误差，具有较强的客观性和说服力。

### 6. 论文的主要结论与发现
*   **理论突破**：首次在离散时间 DMPC 框架下，针对该协同模型给出了指数稳定性的显式证明。
*   **性能发现**：预测时域 $K=1$ 虽然收敛到共识的速度比 $K=25$ 慢，但计算效率极高，且足以保证稳定性。
*   **自主性提升**：基于博弈/优化的 DMPC 方案在面对扰动时，比离线预规划方法更鲁棒。在走廊实验中，自主排序方案成功避免了碰撞，而预定义顺序方案因扰动导致了碰撞。

### 7. 优点
*   **理论严密**：通过显式模态分析提供了收敛率的解析表达。
*   **高效可扩展**：计算开销不随集群规模显著增长，适合资源受限的微型无人机。
*   **约束处理能力**：相比传统的 PI 控制，能直接在优化过程中处理速度、加速度等物理限制。

### 8. 不足与局限
*   **理论限制**：目前的稳定性证明仅限于预测时域 $K=1$ 和固定通信拓扑。对于 $K>1$ 或时变网络的理论分析尚不完整。
*   **代价函数形式**：分析主要基于二次代价函数，对于非凸或更复杂的任务目标，其收敛性保障尚待研究。
*   **实机验证**：虽然使用了高保真仿真，但尚未在真实的物理无人机集群上进行户外飞行验证。

（完）
