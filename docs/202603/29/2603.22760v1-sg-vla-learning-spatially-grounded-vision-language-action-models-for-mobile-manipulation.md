---
title: "SG-VLA: Learning Spatially-Grounded Vision-Language-Action Models for Mobile Manipulation"
title_zh: SG-VLA：学习用于移动操作的空间基准视觉-语言-动作模型
authors: "Ruisen Tu, Arth Shukla, Sohyun Yoo, Xuanlin Li, Junxi Li, Jianwen Xie, Hao Su, Zhuowen Tu"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.22760v1"
tags: ["query:q6"]
score: 8.0
evidence: 涉及底座运动和机械臂关节协调的移动操作
tldr: 针对移动操作机器人在复杂家居环境中的挑战，本文提出SG-VLA框架。通过整合多视图RGB、深度信息及历史观测，并结合全局位置、抓取启发等辅助任务协同训练，增强了模型的空间感知能力。该方法在13维动作空间下实现了精准控制，在家庭重排任务中显著优于直接模仿学习，为通用家务机器人提供了有效的空间表征方案。
selection_source: fresh_fetch
motivation: 现有的VLA模型在处理复杂家居环境中的全局布局、精细几何和高维连续动作时表现欠缺。
method: 提出一种空间感知的VLA框架，通过多模态输入增强和协同训练辅助解码器（如重建机器人位置、抓取位姿等）来强化表征。
result: 在拾取、放置、开启和关闭等家庭重排任务中，该方法相比直接模仿学习取得了显著且一致的性能提升。
conclusion: 辅助任务和多模态学习提供的空间感知能力是提升通用家务机器人VLA模型性能的关键方向。
---

## 摘要
视觉-语言-动作（VLA）模型在机器人控制方面展现出巨大潜力，但在复杂的家庭环境中的表现仍不理想。移动操作需要对全局场景布局、细粒度几何结构以及高维连续动作进行推理，这使得标准的模仿学习难以胜任。我们引入了一个学习空间基准 VLA 模型的框架，通过辅助任务协同训练和多模态输入增强来强化感知与表示。我们的方法解决了控制 13 维动作空间的挑战，该空间涉及底盘运动、机械臂关节运动和夹持器动作的协同。为了丰富空间理解，该模型整合了多视图 RGB 观测、深度线索和短期时间历史，提供了全局场景结构和局部操作上下文的视角。为了提高表示质量，我们协同训练了辅助解码器，从共享的视觉-语言特征中重建可解释的中间信号——包括机器人全局位置、关节配置、抓取可行性（affordances）、目标物体相对位姿以及分割掩码。这些目标提供了密集监督，促使骨干网络开发出具有空间基准和操作感知的潜表示。通过对家庭重排任务的广泛评估，我们的方法在拾取、放置、打开和关闭操作中均取得了持续的改进，显著优于直接模仿学习。我们的研究结果表明，通过辅助学习和多模态学习实现空间基准，为将 VLA 模型扩展到通用家用机器人提供了一个强有力的方向。

## Abstract
Vision-Language-Action (VLA) models show promise for robotic control, yet performance in complex household environments remains sub-optimal. Mobile manipulation requires reasoning about global scene layout, fine-grained geometry, and high-dimensional continuous actions, making standard imitation learning insufficient. We introduce a framework for learning spatially-grounded VLA models that strengthens perception and representation through auxiliary task co-training and multi-modal input enhancement. Our method addresses the challenge of controlling a 13-dimensional action space involving coordinated base motion, arm articulation, and gripper actuation. To enrich spatial understanding, the model incorporates multi-view RGB observations, depth cues, and short temporal history, providing perspectives of both global scene structure and local manipulation context. To improve representation quality, we co-train auxiliary decoders that reconstruct interpretable intermediate signals - including global robot position, joint configurations, grasp affordances, target-object relative pose, and segmentation masks - from shared visual-language features. These objectives provide dense supervision that encourages the backbone to develop spatially grounded, manipulation-aware latent representations. Through extensive evaluation on home rearrangement tasks, our approach achieves consistent improvements across picking, placing, opening, and closing operations, substantially outperforming direct imitation learning. Our findings suggest that spatial grounding through auxiliary and multi-modal learning provides a strong direction for scaling VLA models toward general-purpose domestic robots.

---

## 论文详细总结（自动生成）

以下是对论文《SG-VLA: Learning Spatially-Grounded Vision-Language-Action Models for Mobile Manipulation》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的视觉-语言-动作（VLA）模型在桌面操作中表现出色，但在复杂的**移动操作（Mobile Manipulation）**任务中效果不佳。
*   **研究背景**：移动操作要求机器人同时处理全局场景布局、细粒度物体几何以及高维连续动作（涉及底座、机械臂和夹持器的协同）。
*   **动机**：作者认为标准的直接模仿学习（Direct Imitation Learning）提供的监督信号过于稀疏，不足以让模型学习到复杂的空间推理能力。因此，需要通过增强感知输入和引入辅助任务来强化模型的“空间基准（Spatial Grounding）”能力。

### 2. 方法论
*   **核心思想**：通过多模态输入增强和多任务协同训练（Co-training），迫使模型在生成动作的同时，学习重建环境的空间特征。
*   **关键技术细节**：
    *   **模型架构**：基于 Prismatic VLM 骨干，视觉编码器融合了 DINOv2（空间特征）和 SigLIP（语义特征），语言模型采用 Qwen2.5-0.5B。
    *   **多模态输入**：整合了头部和手部摄像头的 **RGB 图像**、**深度图（Depth）**以及短期**历史观测**。
    *   **13维动作空间**：包括底座位姿（3D）、躯干高度（1D）、机械臂关节角（7D）和夹持器状态（2D）。
    *   **辅助解码器（Auxiliary Decoders）**：在训练期间，模型需同时预测：
        1.  **全局位置**（机器人 2D 坐标）；
        2.  **抓取成功率**（二分类）；
        3.  **物体位姿**（3D 位置 + 四元数）；
        4.  **关节配置**（12 维关节状态）；
        5.  **分割掩码**（目标物体的 128x128 二进制掩码）。
*   **三阶段训练流程**：
    1.  **解码器适配阶段**：冻结 VLM 骨干，仅训练辅助解码器，避免随机初始化的噪声梯度破坏预训练权重。
    2.  **联合精炼阶段**：开启全量梯度回传，协同优化动作预测和辅助任务。
    3.  **动作头独立训练阶段**：冻结 VLM，专门训练可选的流匹配（Flow Matching）动作专家头，以生成高精度连续动作。

### 3. 实验设计
*   **数据集/场景**：使用 **ManiSkill-HAB** 仿真基准，包含 TidyHouse、PrepareGroceries 和 SetTable 三个长程家庭重排任务。
*   **数据规模**：共 4.4 万个情节（Episodes），包含 140 万次状态转换。
*   **任务类型**：涵盖拾取（Pick）、放置（Place）、打开（Open）和关闭（Close）四种基本操作。
*   **对比方法**：
    *   **OpenVLA**（7B 参数量级的基础模型）。
    *   **Base VLM**（直接模仿学习，无辅助任务）。
    *   针对不同输入模态（单视图 vs 多视图、有无深度图）和不同辅助任务组合的消融实验。

### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA A100 GPU**。
*   **训练配置**：使用 Adam 优化器，学习率 2e-5，全局 Batch Size 为 512。
*   **时长**：分割任务训练 5 个 Epoch，其他任务训练 10 个 Epoch。文中未明确给出总训练小时数，但从 Epoch 数和模型规模来看，属于中等规模训练。

### 5. 实验数量与充分性
*   **实验规模**：进行了多维度的消融实验，包括输入模态（表2）、辅助任务类型（表3、表4）以及动作生成方式（表5）。
*   **充分性**：每个任务运行了 30 个情节来计算平均成功率。实验覆盖了从感知到架构再到训练策略的全过程，对比了不同参数规模的模型（如 7B 的 OpenVLA 与 1.3B 的 SG-VLA），结果显示小模型在增强感知后能超越大模型，证明了方法的有效性。
*   **客观性**：实验在统一的仿真基准下进行，对比基准设置合理。

### 6. 主要结论与发现
*   **空间基准至关重要**：引入辅助任务（尤其是关节位置预测和分割掩码）显著提升了成功率，SG-VLA 的平均成功率从 60% 提升至 **73%**。
*   **多模态融合优势**：多视图 RGB 结合深度信息比单视图 RGB 性能提升巨大（从 0.04 提升至 0.32 以上）。
*   **训练策略的影响**：直接协同训练会导致性能下降，而**分阶段递进式训练**是多任务 VLA 成功的关键。
*   **动作头权衡**：流匹配动作头在精细的“拾取”任务中表现更好，但在需要大范围移动的“打开”任务中反而不如离散 Token 预测。

### 7. 优点
*   **高效性**：通过 1.3B 的轻量级模型达到了优于 7B 模型的性能，适合实际部署。
*   **可解释性**：辅助任务（如分割和位姿预测）不仅提供了监督信号，还让模型的中间表征更具可解释性。
*   **系统性**：详细探索了移动操作中感知、推理与执行的耦合关系，并给出了成熟的训练方案。

### 8. 不足与局限
*   **仿真与现实差距**：所有实验均在 ManiSkill-HAB 仿真环境中完成，缺乏真实世界机器人的验证。
*   **时间历史的负面影响**：实验发现加入历史观测反而降低了性能，这与直觉相悖，论文未能深入解释其根本原因（可能是模型未能有效处理时序冗余）。
*   **任务局限**：虽然涵盖了家庭常见任务，但对于更复杂的动态环境（如有人走动）或更柔软的物体操作尚未涉及。

（完）
