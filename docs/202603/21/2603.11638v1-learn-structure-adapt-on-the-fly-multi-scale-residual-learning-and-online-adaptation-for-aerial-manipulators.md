---
title: "Learn Structure, Adapt on the Fly: Multi-Scale Residual Learning and Online Adaptation for Aerial Manipulators"
title_zh: 学习结构，即时自适应：空中机械臂的多尺度残差学习与在线自适应
authors: "Samaksh Ujjawal, Naveen Sudheer Nair, Shivansh Pratap Singh, Rishabh Dev Yadav, Wei Pan, Spandan Roy"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11638v1"
tags: ["query:profile-1"]
score: 8.0
evidence: 空中机械臂的实时残差建模
tldr: 针对空中机械臂在负载变化和构型切换时面临的非平稳、多尺度残差动力学挑战，本文提出一种预测-自适应框架。核心包含分解动力学Transformer（FDT）以解耦跨变量耦合与多尺度时间效应，以及潜空间残差适配器（LRA）通过递归最小二乘法实现实时在线学习。实验证明该方法在保持实时性的同时，显著提升了轨迹跟踪精度和对未知负载的鲁棒性。
selection_source: carryover_cache
motivation: 传统的解析模型和静态数据驱动模型难以应对空中机械臂在复杂任务中表现出的高度耦合、非平稳及多尺度残差动力学特性。
method: 提出分解动力学Transformer（FDT）进行多尺度建模，并结合潜空间残差适配器（LRA）利用递归最小二乘法实现低计算开销的在线参数更新。
result: 真实世界实验表明，该框架在预测精度、扰动抑制速度和闭环跟踪性能方面均优于现有的先进学习基准。
conclusion: 该研究通过结构化建模与实时在线适配的结合，有效解决了空中机械臂在动态环境下的精确控制难题，具有极高的实用价值。
---

## 摘要
自主空中机械臂 (AAMs) 是固有的耦合非线性系统，表现出非平稳和多尺度的残差动力学，特别是在机械臂构型重组和负载突变期间。传统的解析动力学模型依赖于固定的参数结构，而静态数据驱动模型假设动力学是平稳的，在构型改变和负载变化时性能会下降。此外，现有的学习架构没有显式地分解跨变量耦合和多尺度时间效应，将瞬时惯性动力学与长时域的状态演化混为一谈。我们提出了一种用于 AAMs 实时残差建模与补偿的预测-自适应框架。该框架的核心是分解动力学 Transformer (Factorized Dynamics Transformer, FDT)，它将物理变量视为独立的 token。这种设计实现了显式的跨变量注意力，同时在结构上将短时域惯性依赖与长时域气动效应分离开来。为了应对部署时的分布偏移，潜空间残差适配器 (Latent Residual Adapter, LRA) 通过递归最小二乘法在潜空间中执行快速线性自适应，在不产生过高计算开销的情况下保留了离线非线性表示。自适应后的残差预测被直接集成到残差补偿自适应控制器中。在承受未知负载的空中机械臂上进行的真实世界实验表明，与最先进的学习基准相比，该方法具有更高的预测保真度、更快的扰动衰减和更优的闭环跟踪精度，同时保持了严格的实时可行性。

## Abstract
Autonomous Aerial Manipulators (AAMs) are inherently coupled, nonlinear systems that exhibit nonstationary and multiscale residual dynamics, particularly during manipulator reconfiguration and abrupt payload variations. Conventional analytical dynamic models rely on fixed parametric structures, while static data-driven model assume stationary dynamics and degrade under configuration changes and payload variations. Moreover, existing learning architectures do not explicitly factorize cross-variable coupling and multi-scale temporal effects, conflating instantaneous inertial dynamics with long-horizon regime evolution. We propose a predictive-adaptive framework for real-time residual modeling and compensation in AAMs. The core of this framework is the Factorized Dynamics Transformer (FDT), which treats physical variables as independent tokens. This design enables explicit cross-variable attention while structurally separating short-horizon inertial dependencies from long-horizon aerodynamic effects. To address deployment-time distribution shifts, a Latent Residual Adapter (LRA) performs rapid linear adaptation in the latent space via Recursive Least Squares, preserving the offline nonlinear representation without prohibitive computational overhead. The adapted residual forecast is directly integrated into a residual-compensated adaptive controller. Real-world experiments on an aerial manipulator subjected to unseen payloads demonstrate higher prediction fidelity, accelerated disturbance attenuation, and superior closed-loop tracking precision compared to state-of-the-art learning baselines, all while maintaining strict real-time feasibility.

---

## 论文详细总结（自动生成）

### 论文总结：学习结构，即时自适应——空中机械臂的多尺度残差学习与在线自适应

#### 1. 核心问题与研究背景
空中机械臂（AAMs）在执行抓取、放置和运输任务时，面临极其复杂的动力学挑战。其核心问题在于：
*   **高度耦合与非线性：** 无人机基座与机械臂之间存在构型相关的惯性耦合。
*   **非平稳残差动力学：** 机械臂动作、负载突变以及旋翼下洗流引起的气动干扰，使得系统动力学随时间剧烈变化。
*   **现有模型局限：** 传统解析模型难以精确建模气动效应；而静态数据驱动模型在面对未见过的负载或构型变化时，预测精度会迅速下降，且缺乏对多尺度时间效应（瞬时惯性 vs. 长期气动演化）的显式处理。

#### 2. 方法论
论文提出了一种**预测-自适应框架**，旨在实时建模并补偿残差动力学：
*   **分解动力学Transformer (FDT)：**
    *   **变量级Token化：** 不同于传统按时间步Token化的Transformer，FDT将每个物理变量（状态、控制输入）视为独立Token，通过注意力机制显式捕捉跨变量的物理耦合。
    *   **双流架构：** 采用短时域自注意力（捕捉瞬时惯性耦合）和长时域交叉注意力（通过全局Token聚合长期的气动和负载效应），实现多尺度特征提取。
*   **潜空间残差适配器 (LRA)：**
    *   **核心思想：** 冻结预训练的FDT非线性表示，仅在潜空间（Latent Space）中引入一个线性修正层。
    *   **递归最小二乘法 (RLS)：** 利用RLS算法在线更新线性权重，实现对分布偏移（如新负载）的快速自适应，避免了全网络重训的高额计算开销。
    *   **协方差重置机制：** 当预测误差超过阈值时重置RLS协方差，以应对突发的动力学状态切换。
*   **残差补偿自适应控制：** 将自适应后的残差预测集成到基于Lyapunov稳定性的控制律中，通过一个简单的自适应增益项来抵消剩余的估计误差。

#### 3. 实验设计
*   **硬件平台：** Tarot-650 四旋翼无人机，搭载 2-DOF 机械臂。
*   **数据集：** 采集了 0g、200g、400g 三种负载下的随机轨迹数据（每种 5 分钟，100Hz）。
*   **测试场景：**
    *   **场景 A：** 高度变化的 S 形取放任务（存在地面效应和垂直状态切换）。
    *   **场景 B：** 持续的 8 字形轨迹（诱发持续的横滚/俯仰运动）。
*   **对比基准 (Benchmarks)：**
    *   **传统方法：** 稀疏识别 (SysID)、L1 自适应控制。
    *   **深度学习基准：** GP (高斯过程)、LSTM、标准 Transformer、GPT-2。
    *   **在线自适应基准：** Neural-fly (元学习)、MLP (仅更新最后一层)。

#### 4. 资源与算力
*   **推理端：** 使用机载 **NVIDIA Jetson Orin Nano (8GB)** 运行 FDT 推理和自适应控制，推理频率满足实时性要求（FDT+LRA 耗时约 3.02ms）。
*   **训练端：** 论文提到使用 Adam 优化器训练 100 个 Epoch，但**未明确说明**具体使用的离线训练 GPU 型号及总训练时长。
*   **通信：** 采用 Micro XRCE-DDS 实现低延迟数据交换。

#### 5. 实验数量与充分性
*   **实验规模：** 涵盖了开环预测精度评估、闭环轨迹跟踪（分布内 300g 和分布外 500g 负载）、以及不同飞行速度（0.5m/s 和 1.0m/s）下的对比。
*   **消融实验：** 针对 LRA、全局 Token、短时域编码、长时域记忆等核心组件进行了 4 组消融对比，验证了各模块对降低 RMSE 的贡献。
*   **评价：** 实验设计较为充分，通过引入“分布外负载”测试了模型的泛化与自适应能力，对比方法涵盖了从经典控制到前沿深度学习的多个维度，结果具有较强的说服力和客观性。

#### 6. 主要结论与发现
*   **预测精度：** FDT+LRA 在残差预测上的 RMSE 比最接近的基准（Neural-fly）降低了约 56%，$R^2$ 达到 0.972。
*   **跟踪性能：** 在 500g 未见负载下，该方法在高速飞行时的跟踪误差显著低于其他自适应方法，证明了潜空间线性适配的有效性。
*   **实时性：** 尽管架构复杂，但通过变量分解设计，推理延迟保持在 3ms 左右，完全适配 50Hz 的控制循环。

#### 7. 优点
*   **结构化归纳偏置：** 通过变量级 Token 化引入了符合物理直觉的归纳偏置，比通用序列模型更适合动力学建模。
*   **高效自适应：** 结合 RLS 的潜空间适配既保留了深度特征的表达能力，又实现了毫秒级的参数更新。
*   **多尺度解耦：** 成功分离了快变惯性力和慢变环境力，提升了模型在复杂任务中的鲁棒性。

#### 8. 不足与局限
*   **局部线性假设：** LRA 假设状态偏移在潜空间中是局部线性的，在极端非线性或剧烈动力学突变下（如机械臂碰撞），该假设可能失效。
*   **实验覆盖范围：** 实验主要集中在平稳飞行和取放任务，未探讨更极端的特技飞行或强阵风环境下的表现。
*   **超参数敏感性：** RLS 的忘记因子和协方差重置阈值可能需要针对不同机型进行精细调优。

（完）
