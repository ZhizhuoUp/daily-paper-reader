---
title: "AeroGrab: A Unified Framework for Aerial Grasping in Cluttered Environments"
title_zh: AeroGrab：一种用于杂乱环境中空中抓取的统一框架
authors: "Shivansh Pratap Singh, Naveen Sudheer Nair, Samaksh Ujjawal, Sarthak Mishra, Soham Patil, Rishabh Dev Yadav, Spandan Roy"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15097v1"
tags: ["query:profile-1"]
score: 8.0
evidence: 复杂环境下的空中操作与抓取，涉及碰撞风险
tldr: AeroGrab是一个针对复杂环境设计的统一空中抓取框架。针对遮挡和碰撞风险，该系统集成了语言指令识别、主动探索、6自由度抓取位姿生成及碰撞感知评估。通过结合主动感知与可行性分析，AeroGrab实现了在杂乱场景中对目标物体的可靠识别与精准抓取，填补了现有空中操纵系统在端到端集成方面的空白。
selection_source: fresh_fetch
motivation: 现有的空中操纵系统缺乏抓取生成、主动探索与语言任务规范的深度集成，难以在杂乱且有遮挡的环境中实现可靠抓取。
method: 提出一个集成框架，通过语言指令定位目标，利用主动探索获取最佳视角，并结合6自由度抓取预测与碰撞感知可行性评估来选择最优抓取位姿。
result: 在真实的杂乱场景实验中，该系统展示了鲁棒且可靠的抓取执行能力。
conclusion: 将主动感知与可行性感知抓取选择相结合，是提升空中操纵系统在复杂环境下作业效能的关键。
---

## 摘要
由于遮挡和碰撞风险，在杂乱环境中实现可靠的空中抓取仍然具有挑战性。现有的空中操作流程主要依赖于基于质心的抓取，且缺乏抓取姿态生成模型、主动探索与语言级任务规范之间的集成，导致缺乏完整的端到端系统。在这项工作中，我们提出了一种用于杂乱环境中可靠空中抓取的集成流程。给定场景和语言指令，系统会识别目标物体并对其进行主动探索，以获得更好的物体视角。在探索过程中，抓取生成网络为每个视角预测多个 6 自由度（6-DoF）抓取候选。每个候选抓取都使用碰撞感知的可行性框架进行评估，并使用标准轨迹生成和控制方法选择并执行全局最佳抓取。在杂乱的真实场景中进行的实验证明了稳健且可靠的抓取执行，突显了将主动感知与可行性感知抓取选择相结合在空中操作中的有效性。

## Abstract
Reliable aerial grasping in cluttered environments remains challenging due to occlusions and collision risks. Existing aerial manipulation pipelines largely rely on centroid-based grasping and lack integration between the grasp pose generation models, active exploration, and language-level task specification, resulting in the absence of a complete end-to-end system. In this work, we present an integrated pipeline for reliable aerial grasping in cluttered environments. Given a scene and a language instruction, the system identifies the target object and actively explores it to gain better views of the object. During exploration, a grasp generation network predicts multiple 6-DoF grasp candidates for each view. Each candidate is evaluated using a collision-aware feasibility framework, and the overall best grasp is selected and executed using standard trajectory generation and control methods. Experiments in cluttered real-world scenarios demonstrate robust and reliable grasp execution, highlighting the effectiveness of combining active perception with feasibility-aware grasp selection for aerial manipulation.

---

## 论文详细总结（自动生成）

### AeroGrab：杂乱环境中空中抓取的统一框架论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
空中机器人（无人机）在执行抓取任务时，面临着环境杂乱、目标遮挡和碰撞风险高等严峻挑战。现有的空中操纵系统通常存在以下局限：
*   **抓取策略单一**：大多依赖于目标质心（Centroid-based）抓取，无法处理形状复杂或功能性部件（如把手、边缘）的物体。
*   **缺乏主动感知**：假设目标清晰可见，但在杂乱环境中，物体常被遮挡，需要机器人主动调整视角。
*   **系统集成度低**：缺乏从自然语言指令到感知、规划及碰撞感知执行的端到端整合。
**AeroGrab** 旨在提出一个统一框架，通过集成语言引导、主动探索和碰撞感知的 6-DoF（六自由度）抓取生成，解决复杂环境下的可靠空中抓取问题。

#### 2. 方法论：核心思想与关键技术
该框架将抓取过程分为四个主要阶段：
*   **语言引导的目标定位**：利用大语言模型（LLM）将指令（如“抓取桌上的银色瓶子”）分解为场景提示和目标提示。使用 **SAM-3** 进行实时分割，获取目标掩码。
*   **主动感知与探索**：当目标不可见或视角不佳时，无人机围绕场景质心执行轨道飞行（Orbital Exploration），动态调整半径以保持视野，直到识别并定位目标。
*   **6-DoF 抓取合成与初步过滤**：
    *   采用 **Hybrid CNN-ViT** 架构（基于 GraspNet-1Billion 训练）生成候选抓取位姿。
    *   **初步过滤**：剔除违反重力方向（从下往上抓）的位姿，并根据抓取点与质心的距离进行稳定性加权评分。
*   **碰撞感知可行性评估（核心创新）**：
    *   将无人机全躯干（机身+机械臂）简化为一组**凸包（Convex Hulls）**。
    *   利用 **GPU 加速的批量处理**，在预定义的轨迹路径上同时对数百个候选抓取进行碰撞检测。
    *   计算碰撞惩罚项并修正抓取得分，若得分低于阈值则继续主动探索。
*   **轨迹生成与控制**：使用最小分段平滑轨迹（Minimum-snap）和自适应控制器，确保在接近物体时的动态稳定性和抗干扰能力。

#### 3. 实验设计
*   **实验场景**：
    1.  **桌面杂乱（Tabletop Clutter）**：测试近场杂乱环境下的抓取选择。
    2.  **窗口/孔径限制（Window-Constrained）**：测试狭窄空间进入和部分观测下的表现。
    3.  **货架可达性（Shelf Reachability）**：测试多层货架深度和高度限制下的抓取。
*   **Benchmark 与对比方法**：
    *   **Bauer-CoRL25**：基于质心的抓取方法。
    *   **RamonSoria-Eng20**：传统的 6-DoF 空中操纵基准。
*   **评估指标**：抓取精度（GA）、搜索到抓取的延迟（SGL）、无碰撞抓取率（CFGR）、碰撞引起的失败（CIF）等。

#### 4. 资源与算力
*   **机载计算（Onboard）**：使用 **NVIDIA Orin Nano Super**，负责抓取合成、可行性评分、轨迹生成和控制逻辑。
*   **机外计算（Offboard）**：使用配备 **NVIDIA RTX 4090 GPU** (24GB VRAM) 的工作站，专门用于运行 **SAM-3** 分割模型并通过 ROS2 流式传输结果。
*   **传感器**：ZED 2i 立体相机。
*   **训练数据**：抓取网络基于 GraspNet-1Billion 数据集预训练。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   **硬件实验**：每个场景进行 30 次实验（共 90 次以上）。
    *   **仿真实验**：在 Gazebo 环境中每个场景进行 50 次实验（共 150 次）。
*   **消融实验**：针对“无障碍物感知”、“无实时更新（开环）”和“仅 CPU 碰撞检测”进行了 30 组对比实验。
*   **充分性评价**：实验设计涵盖了从仿真到实物的迁移，且对比了质心法和传统 6-DoF 法，消融实验有力地证明了 GPU 加速碰撞检测和主动感知的必要性。实验样本量（N=30/50）在空中机器人领域属于较为充分且客观的范畴。

#### 6. 主要结论与发现
*   **性能提升**：在杂乱环境中，AeroGrab 的无碰撞抓取率（80%）远高于质心法（10%）和传统 6-DoF 法（23.33%）。
*   **实时性**：通过 GPU 加速，感知到规划的计算延迟仅为 **41ms**，能够支持在线决策。
*   **主动感知的价值**：主动轨道探索能有效解决初始视角下的遮挡问题，显著降低了目标搜索失败率（OSF）。
*   **碰撞评估的重要性**：消融实验显示，若不考虑全躯干碰撞，抓取失败率会飙升至 50% 以上。

#### 7. 优点
*   **端到端集成**：首次将自然语言理解、主动视觉探索与复杂的 6-DoF 碰撞感知抓取整合进空中平台。
*   **高效的碰撞检测**：利用凸包简化和 GPU 并行计算，解决了空中机器人算力有限与实时碰撞检测之间的矛盾。
*   **鲁棒性**：自适应控制器有效补偿了机械臂运动对无人机重心的影响。

#### 8. 不足与局限
*   **依赖外部算力**：目前最耗资源的分割模型（SAM-3）仍需机外工作站支持，未实现完全的单机自主。
*   **环境依赖**：实验在配备 OptiTrack 动捕系统的室内进行，尚未在无 GPS/无动捕的纯野外环境中验证。
*   **抓取器限制**：目前仅针对平行二指夹爪进行了优化，对于更复杂的柔性或多指抓取器的适用性有待研究。
*   **动态物体**：框架目前假设环境是静态的，无法处理移动目标或动态障碍物。

（完）
