---
title: "Koopman Meets Discrete-Time Control Barrier Functions: A Linear Model Predictive Control Framework"
title_zh: Koopman 遇上离散时间控制屏障函数：一种线性模型预测控制框架
authors: "Shuo Liu, Liang Wu, Dawei Zhang, Jan Drgona, Calin. A. Belta"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21070v1"
tags: ["query:profile-1"]
score: 9.0
evidence: 基于Koopman的线性MPC用于安全关键控制
tldr: 本文提出了一种基于Koopman算子的线性模型预测控制（LMPC）框架，用于非线性离散时间系统的安全关键控制。针对传统离散时间控制屏障函数（DCBF）在MPC中导致的高计算量非线性规划问题，该研究利用Koopman算子将非线性动力学和屏障函数提升至高维线性空间，从而将复杂的非线性约束优化问题转化为高效的二次规划（QP）问题，并引入松弛变量以平衡安全性与可行性。
selection_source: fresh_fetch
motivation: 旨在解决基于控制屏障函数的非线性系统安全控制中，非线性规划带来的高计算负担和实时性不足的问题。
method: 通过Koopman算子理论构建增广动力学系统，将非线性动力学和屏障函数线性化，从而建立基于二次规划的线性MPC框架。
result: 在非线性机器人导航任务的仿真中，该框架成功实现了安全的轨迹生成，并展现出极高的实时控制效率。
conclusion: 该研究证明了结合Koopman算子与线性模型预测控制可以有效且高效地处理非线性系统的安全约束控制问题。
---

## 摘要
本文提出了一种基于 Koopman 的线性模型预测控制 (LMPC) 框架，用于非线性离散时间系统的安全性关键控制。现有的基于离散时间控制屏障函数 (DCBF) 的 MPC 方案通过屏障约束来保证安全性，但通常会导致计算量巨大的非线性规划问题。为了应对这一挑战，我们构建了一个 DCBF 增强的动力系统，并利用 Koopman 算子理论将非线性动力学提升到高维空间，在该空间中，系统动力学和屏障函数均可表示为线性预测器。这使得非线性安全约束 MPC 问题能够转化为二次规划 (QP) 问题。为了在保证安全性的同时提高可行性，我们为屏障约束引入了带有松弛变量的松弛机制。该方法结合了 Koopman 算子的建模能力与 QP 的计算效率。对具有非线性动力学的机器人导航任务进行的数值模拟表明，所提出的框架实现了安全的轨迹生成和高效的实时控制。

## Abstract
This paper proposes a Koopman-based linear model predictive control (LMPC) framework for safety-critical control of nonlinear discrete-time systems. Existing MPC formulations based on discrete-time control barrier functions (DCBFs) enforce safety through barrier constraints but typically result in computationally demanding nonlinear programming. To address this challenge, we construct a DCBF-augmented dynamical system and employ Koopman operator theory to lift the nonlinear dynamics into a higher-dimensional space where both the system dynamics and the barrier function admit a linear predictor representation. This enables the transformation of the nonlinear safety-constrained MPC problem into a quadratic program (QP). To improve feasibility while preserving safety, a relaxation mechanism with slack variables is introduced for the barrier constraints. The resulting approach combines the modeling capability of Koopman operators with the computational efficiency of QP. Numerical simulations on a navigation task for a robot with nonlinear dynamics demonstrate that the proposed framework achieves safe trajectory generation and efficient real-time control.

---

## 论文详细总结（自动生成）

这是一份关于论文《Koopman Meets Discrete-Time Control Barrier Functions: A Linear Model Predictive Control Framework》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在保证非线性系统安全性的同时，实现具有预测能力且计算高效的实时控制？
*   **研究动机**：
    *   **安全性需求**：自动驾驶和机器人等系统需要严格的安全性保障（如避障）。控制屏障函数（CBF）是处理安全约束的有效工具。
    *   **预测与计算的矛盾**：传统的离散时间控制屏障函数（DCBF）若结合模型预测控制（MPC），通常会演变为复杂的**非线性规划（NLP）**问题，计算量巨大，难以满足实时性要求。
    *   **现有方案不足**：现有的迭代线性化方法（如 iMPC）虽然降低了难度，但无法保证收敛性，且在处理高度非线性约束时依然低效。

### 2. 方法论
*   **核心思想**：利用 **Koopman 算子理论** 将非线性动力学和非线性安全约束（DCBF）同时“提升”到一个高维线性空间，从而将非线性安全约束 MPC 问题转化为高效的**凸二次规划（QP）**问题。
*   **关键技术细节**：
    1.  **DCBF 增广系统构建**：不只是提升原始状态 $x$，而是构建一个包含屏障函数值 $h(x)$ 的增广状态向量 $\bar{x} = [x^\top, h(x)]^\top$。
    2.  **Koopman 线性预测器**：采用扩展动态模式分解（EDMD）方法，从数据中学习线性算子 $A, B, C$，使得在高维提升空间中，动力学演化和屏障函数值均表现为线性关系。
    3.  **线性 MPC 建模（LMPC-DCBF）**：在提升空间内建立目标函数，并将 DCBF 安全约束转化为关于决策变量的线性不等式。
    4.  **松弛机制**：引入松弛变量 $\omega$ 来处理可行性问题。通过巧妙的递归设计，确保即使在引入松弛后，约束在形式上依然保持线性，且能保证开环安全性。

### 3. 实验设计
*   **实验场景**：非线性单轮车（Unicycle）机器人的避障导航任务。
*   **对比基准（Benchmarks）**：
    *   **NMPC-DCBF**：基于非线性规划的传统方法（使用 IPOPT 求解器）。
    *   **iMPC-DCBF**：迭代凸优化方法（使用 OSQP 求解器）。
    *   **K-LMPC**：不带安全约束的 Koopman 线性 MPC（作为对照组）。
*   **评估指标**：计算耗时（ms）、轨迹安全性（屏障函数值 $h$）、预测精度、控制输入平滑度。

### 4. 资源与算力
*   **硬件环境**：Windows 桌面端，Intel Core i7-11750F CPU @ 2.50 GHz。
*   **软件工具**：MATLAB 环境，使用 `quadprog` 求解 QP，`YALMIP` 建模，`IPOPT` 求解非线性基准。
*   **数据规模**：采集了 $2 \times 10^5$ 组状态转移样本用于训练 Koopman 算子。

### 5. 实验数量与充分性
*   **实验设置**：
    *   验证了不同采样时间（$\Delta t$）下 Koopman 模型的预测精度。
    *   测试了不同预测时域（$N$）和安全超参数（$\gamma$）对系统表现的影响。
    *   进行了多组对比实验，涵盖了安全性、实时性和路径最优性的综合评估。
*   **充分性评价**：实验设计较为充分，客观展示了该方法在计算速度上的量级提升（3500倍于 NMPC）。但实验目前仅局限于单轮车模型和圆形障碍物，尚未在更复杂的动态环境或更高维的物理系统上进行验证。

### 6. 主要结论与发现
*   **计算效率极高**：所提方法（K-LMPC-DCBF）的平均单步计算耗时仅为 **3-5 ms**，而 iMPC 需要 100-330 ms，NMPC 则高达 10-16 秒。
*   **安全性保障**：通过调节超参数 $\gamma$，可以有效补偿 Koopman 模型的近似误差，确保机器人在非线性环境下不碰撞。
*   **线性化的优势**：通过增广状态技巧，成功将原本会导致非凸优化的松弛变量项线性化，维持了 QP 的结构。

### 7. 优点（亮点）
*   **首创性**：首次将 Koopman 建模、MPC 预测和 DCBF 安全约束集成在一个统一的线性 QP 框架内。
*   **数学巧妙性**：通过将 $h(x)$ 纳入增广状态，避开了对非线性屏障函数进行实时线性化的步骤，减少了误差累积。
*   **实时性极佳**：计算复杂度低，非常适合部署在算力受限的嵌入式机器人平台上。

### 8. 不足与局限
*   **模型依赖性**：安全性高度依赖于 Koopman 算子的拟合精度。如果训练数据未覆盖某些区域，模型误差可能导致安全违规。
*   **理论保证的局限**：由于 Koopman 是一种近似，论文承认其无法在严格意义上提供像解析法那样的绝对安全证明，需通过保守设计（如安全裕度）来弥补。
*   **场景单一**：目前的数值仿真较为简单，缺乏多障碍物、动态障碍物或高维复杂动力学系统的验证。

（完）
