---
title: "GAPG: Geometry Aware Push-Grasping Synergy for Goal-Oriented Manipulation in Clutter"
title_zh: GAPG：面向杂乱场景中目标导向操作的几何感知推-抓协同框架
authors: "Lijingze Xiao, Jinhong Du, Yang Cong, Supeng Diao, Yu Ren"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21195v1"
tags: ["query:q6"]
score: 8.0
evidence: 杂乱环境中机器人操作的推抓协同
tldr: 针对杂乱环境中目标抓取难题，本文提出GAPG框架，通过点云数据实现几何感知的推-抓协同。该框架利用抓取评估模块分析夹爪与物体的几何关系，并结合推送评估模块预测动作对未来抓取空间的影响。实验证明，该方法在仿真和现实场景中均表现出更高的安全性、效率和泛化能力，有效解决了遮挡和堆叠物体的操作挑战。
selection_source: fresh_fetch
motivation: 现有的推-抓协同方法因忽视场景几何信息，导致在杂乱环境下的操作稳定性和效率较低。
method: 提出一种基于点云的几何感知框架，通过分析夹爪闭合区域内的点云分布来评估抓取稳定性，并据此指导推送动作以创造抓取空间。
result: 在仿真和真实世界的多种复杂场景中，该方法均实现了更可靠的物体操作，并展现出对未知物体的良好泛化性。
conclusion: 结合几何信息的推-抓协同策略能有效将不可抓取状态转化为可抓取状态，显著提升了机器人在杂乱环境中的作业性能。
---

## 摘要
抓取目标物体是机器人操作的一项基本技能，但在物体堆叠或遮挡的杂乱环境中，单步抓取往往难以实现。为解决这一问题，以往的研究引入了推动（pushing）作为辅助动作，以创造可抓取空间。然而，这些方法往往在稳定性和效率方面表现不佳，因为它们忽略了场景的几何信息，而几何信息对于评估抓取鲁棒性以及确保推动动作的安全有效至关重要。为此，我们提出了一种几何感知的推-抓协同框架，利用点云数据来整合抓取和推动评估。具体而言，抓取评估模块分析了夹爪点云与闭合区域内点云之间的几何关系，以确定抓取的可行性和稳定性。在此引导下，推动评估模块预测推动动作如何影响未来的可抓取空间，使机器人能够选择可靠地将不可抓取状态转换为可抓取状态的动作。通过对抓取和推动中的几何信息进行联合推理，我们的框架在杂乱设置中实现了更安全、更高效且更可靠的操作。我们的方法在仿真和真实世界的多种场景中进行了广泛测试。实验结果表明，我们的模型能够很好地泛化到真实场景和未见过的物体。

## Abstract
Grasping target objects is a fundamental skill for robotic manipulation, but in cluttered environments with stacked or occluded objects, a single-step grasp is often insufficient. To address this, previous work has introduced pushing as an auxiliary action to create graspable space. However, these methods often struggle with both stability and efficiency because they neglect the scene's geometric information, which is essential for evaluating grasp robustness and ensuring that pushing actions are safe and effective. To this end, we propose a geometry-aware push-grasp synergy framework that leverages point cloud data to integrate grasp and push evaluation. Specifically, the grasp evaluation module analyzes the geometric relationship between the gripper's point cloud and the points enclosed within its closing region to determine grasp feasibility and stability. Guided by this, the push evaluation module predicts how pushing actions influence future graspable space, enabling the robot to select actions that reliably transform non-graspable states into graspable ones. By jointly reasoning about geometry in both grasping and pushing, our framework achieves safer, more efficient, and more reliable manipulation in cluttered settings. Our method is extensively tested in simulation and real-world environments in various scenarios. Experimental results demonstrate that our model generalizes well to real-world scenes and unseen objects.

---

## 论文详细总结（自动生成）

### GAPG: 几何感知的推-抓协同框架论文总结

#### 1. 核心问题与整体含义
在杂乱、堆叠或存在遮挡的环境中，机器人往往无法通过单次抓取动作直接获取目标物体。现有的“推-抓协同”方法大多依赖 2.5D 高度图（Heightmap）和强化学习，由于缺乏对 3D 空间几何信息的深度感知，导致抓取判断不够稳定，且推送动作（Pushing）的效率较低。本文的核心动机是利用 **3D 点云数据**，通过分析夹爪与物体之间的几何匹配关系，实现更安全、高效且目标导向的杂乱场景操作。

#### 2. 方法论
论文提出了 **GAPG (Geometry Aware Push-Grasping)** 框架，主要包含两个核心模块：
*   **抓取评估模块 (Grasp Evaluation Module)**：
    *   **核心思想**：不仅考虑物体点云，还将夹爪本身的几何结构转化为点云。
    *   **技术细节**：使用 GraspNet 采样候选抓取位姿，将夹爪闭合区域内的点云与虚拟夹爪点云进行拼接，通过 PointNet++ 提取全局几何特征，最后由 MLP 判断该位姿的可行性和稳定性。这种“几何匹配”能有效识别碰撞并评估接触面的质量。
*   **推送评估模块 (Push Evaluation Module)**：
    *   **核心思想**：在抓取模块的监督下，学习推送动作对未来抓取空间的影响。
    *   **技术细节**：将推送动作抽象为一个带有位姿信息的点，将场景点云变换到以推送点为中心的参考坐标系下。通过 PointNet++ 提取特征，预测该推送动作是否能将“不可抓取状态”转化为“可抓取状态”。
*   **算法流程**：系统首先采样抓取位姿，若抓取评估得分超过阈值则执行抓取；否则，采样并执行得分最高的推送动作，循环往复直至成功。

#### 3. 实验设计
*   **实验场景**：
    *   **仿真环境**：基于 PyBullet，设置了 15 个物体（轻度杂乱）、30 个物体（重度杂乱）以及人工设计的挑战性场景（如目标被围困）。
    *   **真实世界**：使用 Rokae xMate 机械臂和 DH 夹爪，配合 Photoneo 和 ZED 2i 相机。
*   **Benchmark 与对比方法**：
    *   主要对比对象是 **Xu et al. [8] (Efficient Push-Grasping)**，这是一种基于高度图和深度强化学习的先进方法。
*   **评估指标**：任务完成率（Completion Rate）、抓取成功率（Grasp Success Rate）、平均动作次数（Motion Number）。

#### 4. 资源与算力
*   **硬件设备**：
    *   **数据采集**：使用 Nvidia RTX A6000 GPU。
    *   **模型训练**：使用 NVIDIA GeForce 4090 GPU。
*   **训练细节**：
    *   抓取模块：训练 85 个 epoch，batch size 为 256，使用 AdamW 优化器。
    *   推送模块：训练 100 个 epoch，batch size 为 128，使用 Adam 优化器。
    *   数据集规模：抓取和推送模块各采集了 40,000 个样本。

#### 5. 实验数量与充分性
*   **实验规模**：仿真实验中每种场景重复 30 次测试；真实世界中每种场景重复 5 次测试。
*   **消融实验**：专门设计了“移除夹爪点云信息”的对比实验，验证了几何匹配对提升抓取准确率（提升约 22%）和推送效率（提升约 75%）的关键作用。
*   **泛化性测试**：使用了 15 种训练集中未见过的物体进行测试。
*   **评价**：实验设计较为全面，涵盖了从仿真到现实的迁移（Sim-to-Real），且对比了不同复杂度的杂乱程度，实验结果具有较强的说服力和客观性。

#### 6. 主要结论与发现
*   **几何感知至关重要**：引入夹爪的 3D 几何信息能显著减少碰撞并提高抓取稳定性。
*   **高效协同**：GAPG 在 30 个物体的重度杂乱场景中，平均动作次数仅为 2.8 次，比基准方法效率提升了约 75%。
*   **强泛化性**：该模型无需在真实世界数据上微调，即可直接应用于未见过的物体和复杂的真实场景，任务完成率保持在 95% 以上。

#### 7. 优点
*   **创新的几何匹配**：将抓取评估转化为夹爪点云与环境点云的特征匹配，比单纯处理环境点云更具物理意义。
*   **Sim-to-Real 能力强**：完全基于点云的表征减少了视觉渲染差异的影响，使得仿真训练的模型能直接部署到现实。
*   **目标导向明确**：推送模块受抓取模块引导，避免了无意义的随机乱推，提高了操作的针对性。

#### 8. 不足与局限
*   **依赖分割质量**：系统依赖 SAM 等模型提供准确的目标物体掩码（Mask），若分割失败，后续评估将失效。
*   **感知局限**：真实实验中提到，相机标定误差和点云噪声仍是导致失败的主要原因。
*   **动作空间限制**：目前的推送和抓取主要基于平面工作台，对于更复杂的 3D 空间（如货架深处）的适应性有待进一步验证。
*   **实时性挑战**：虽然文中未详述推理耗时，但大规模点云采样和 PointNet++ 处理在极高频响应场景下可能存在压力。

（完）
