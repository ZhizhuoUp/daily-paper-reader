---
title: Task-Space Singularity Avoidance for Control Affine Systems Using Control Barrier Functions
title_zh: 基于控制屏障函数的控制仿射系统任务空间奇异性规避
authors: "Kimia Forghani, Suraj Raval, Lamar Mair, Axel Krieger, Yancy Diaz-Mercado"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23753v1"
tags: ["query:q2"]
score: 8.0
evidence: 用于控制仿射系统奇异性规避的控制屏障函数
tldr: 针对控制仿射系统中的任务空间奇异性问题，本文提出了一种基于控制障碍函数（CBF）的规避框架。通过分析输入输出映射矩阵的特征值来识别奇异位形，并构建障碍函数以维持安全裕度。该方法在保证轨迹跟踪精度的同时，有效避免了秩亏损区域。仿真结果表明，该方法能显著降低控制输入峰值，提升系统稳定性。
selection_source: fresh_fetch
motivation: 机器人系统中的奇异性会导致控制输入无法确定及轨迹跟踪失效，限制了系统在特定方向产生力的能力。
method: 利用输入输出映射矩阵的特征值构建控制障碍函数，通过数学约束确保系统状态远离秩亏损的奇异区域。
result: 在平面双连杆机械臂和磁驱动针头的仿真中，该方法实现了平滑跟踪，并将控制输入峰值较常规控制器降低了多达100倍。
conclusion: 该CBF框架为控制仿射系统提供了一种具有理论安全保证的奇异性规避方案，显著提高了复杂任务下的控制性能。
---

## 摘要
机器人和动力系统中的奇异性出现在控制输入到任务空间运动的映射发生降秩时，这会导致无法确定输入。这限制了系统在期望方向上产生力和力矩的能力，并阻碍了精确的轨迹跟踪。本文提出了一种用于规避控制仿射系统中此类奇异性的控制屏障函数（CBF）框架。通过状态相关的输入-输出映射矩阵的特征值来识别奇异构型，并构建屏障函数以保持与降秩区域的安全裕度。文中提供了作为执行器动力学函数的安全性理论保证条件。在平面双连杆机械臂和磁驱动针上的仿真表明，该方法在规避奇异构型的同时实现了平滑的轨迹跟踪，且与标称控制器相比，控制输入峰值降低了高达 100 倍。

## Abstract
Singularities in robotic and dynamical systems arise when the mapping from control inputs to task-space motion loses rank, leading to an inability to determine inputs. This limits the system's ability to generate forces and torques in desired directions and prevents accurate trajectory tracking. This paper presents a control barrier function (CBF) framework for avoiding such singularities in control-affine systems. Singular configurations are identified through the eigenvalues of a state-dependent input-output mapping matrix, and barrier functions are constructed to maintain a safety margin from rank-deficient regions. Conditions for theoretical guarantees on safety are provided as a function of actuator dynamics. Simulations on a planar 2-link manipulator and a magnetically actuated needle demonstrate smooth trajectory tracking while avoiding singular configurations and reducing control input spikes by up to 100x compared to the nominal controller.

---

## 论文详细总结（自动生成）

以下是对论文《Task-Space Singularity Avoidance for Control Affine Systems Using Control Barrier Functions》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：在机器人和动力系统中，当控制输入到任务空间运动的映射（雅可比矩阵）发生降秩时，会产生“奇异性”。这会导致控制器（尤其是依赖矩阵求逆的控制器）产生极大的控制输入峰值、失去特定方向的运动能力，甚至引发系统不稳定。
*   **核心问题**：如何为控制仿射系统设计一种具有理论安全保证的框架，在保证任务执行的同时，实时规避这些状态相关的奇异区域。

### 2. 论文提出的方法论
该论文提出了一种基于**控制屏障函数（CBF）**的通用规避框架：
*   **核心思想**：将奇异性规避建模为一个安全约束问题。通过监控输入-输出映射矩阵的特征值，确保系统状态始终远离特征值为零（即降秩）的区域。
*   **关键技术细节**：
    *   **映射矩阵分析**：定义状态相关的映射矩阵 $\phi(x) = J(x)g(x)$。通过计算 $M(x) = \phi(x)\phi(x)^T$（或其转置乘积）的特征值 $\lambda_i$ 来量化系统距离奇异点的“距离”。
    *   **CBF 构建**：构建屏障函数 $h(x) = \lambda(x) - \epsilon$，其中 $\epsilon$ 是预设的安全裕度。
    *   **二次规划（QP）求解**：将控制问题转化为一个 QP 问题，在最小化实际控制输入与名义控制输入（Nominal Control）偏差的同时，满足 CBF 约束条件 $\dot{h} \ge -\alpha(h)$。
    *   **数值化扩展**：针对解析表达式过于复杂的系统，提出将奇异区域视为 3D 虚拟障碍物，利用奇异值分解（SVD）和 Alpha-shape 算法重建安全边界，从而将奇异性规避转化为障碍物规避问题。

### 3. 实验设计
论文设计了两个具有代表性的仿真场景：
*   **场景 A：平面双连杆机械臂**
    *   **目标**：验证解析 CBF 的有效性。
    *   **对比基准**：不带 CBF 的名义控制器（基于伪逆的轨迹跟踪）。
*   **场景 B：磁驱动手术针头（缝合任务）**
    *   **目标**：验证数值化 CBF 在复杂、超冗余驱动系统中的表现。
    *   **任务**：在 35mm 的工作空间内操纵磁针完成缝合轨迹，同时规避磁场奇异位形。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件型号（如 GPU/CPU）或计算耗时。
*   **实现平台**：根据文中提到的“MATLAB built-in SVD function”和仿真图像，推断实验是在 MATLAB 环境下完成的。由于 QP 求解和数值 SVD 在低维空间（如文中 3D 任务空间）计算量较小，通常普通的桌面级 CPU 即可满足实时性要求。

### 5. 实验数量与充分性
*   **实验规模**：论文展示了两组核心实验。第一组针对简单机械臂进行了深入的解析推导和对比；第二组针对复杂的磁驱动系统进行了路径规划和奇异区域可视化。
*   **充分性评价**：实验设计较为**充分且具有代表性**。它涵盖了从简单解析系统到复杂数值系统的过渡，验证了框架的通用性。通过对比轨迹平滑度和控制输入幅值，客观地证明了 CBF 在消除控制“尖峰”方面的优越性。

### 6. 论文的主要结论与发现
*   **规避效果显著**：CBF 框架能有效引导系统绕过奇异构型，确保映射矩阵始终满秩。
*   **控制输入平滑**：相比于传统控制器在奇异点附近产生的巨大输入，该方法将控制输入峰值降低了 **70 到 100 倍**。
*   **任务精度保持**：在规避奇异性的同时，系统仍能以较小的误差（如磁针实验中位置均方根误差仅 0.99mm）完成预设任务。

### 7. 优点
*   **理论保证**：利用 CBF 提供了前向不变性（Forward Invariance）的数学证明，确保了安全性。
*   **通用性强**：适用于欠驱动、全驱动和过驱动（Over-actuated）等多种控制仿射系统。
*   **数值化方案务实**：针对解析推导困难的复杂机器人，给出了基于点云和障碍物构造的数值化替代方案，增加了工程实用性。

### 8. 不足与局限
*   **输入约束冲突**：虽然 CBF 理论上保证安全，但在存在严格执行器饱和（Actuator Saturation）的情况下，QP 可能无解（论文在 Remark 2 中提到了这一点，但未给出通用解决方案）。
*   **数值敏感性**：数值化 CBF 方案依赖于 SVD 和表面重建，在噪声环境下或高维状态空间中可能存在数值不稳定或计算量激增的问题。
*   **参数调节**：安全裕度 $\epsilon$ 和 Class-K 函数 $\alpha$ 的选择仍依赖经验调试，缺乏自动化的参数优化机制。

（完）
